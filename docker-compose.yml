services:
  tapo-dashboard:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
      - ./certs:/app/certs
      - ./.env:/app/.env:ro
      - ./credentials.json:/app/credentials.json:ro
    restart: unless-stopped
    environment:
      - PYTHONUNBUFFERED=1
