# Node
node_modules/

# Claude worktrees
.claude/worktrees/

# Python
__pycache__/
*.py[cod]
*$py.class
*.pyc
*.egg-info/
dist/
build/
*.egg

# Virtual environments
.venv/
venv/
ENV/

# IDE
.idea/
.vscode/
*.swp
*.swo

# Environment
.env

certs/

# Database
data/*.db
# Ficheros temporales del modo WAL de SQLite
data/*.db-shm
data/*.db-wal
# Copias que _backup_db() escribe antes de cada migracion pendiente
data/backups/

# Clip thumbnails (Fase 20) — generated at runtime from the trigger frame
data/thumbnails/

# Snapshots de evento (Fase 30) — recortes JPEG escritos en caliente por evento
data/snapshots/

# Referencias externas
third_party/

# OS
.DS_Store
Thumbs.db
credentials.json
data/token.json

# Modelos ONNX descargados por scripts/fetch_models.py (Fase 25) — ~900 KB de binario
models/
