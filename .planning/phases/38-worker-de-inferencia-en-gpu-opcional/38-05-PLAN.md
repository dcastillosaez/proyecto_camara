---
phase: 38-worker-de-inferencia-en-gpu-opcional
plan: 05
type: execute
wave: 4
depends_on: ["38-02", "38-04"]
files_modified:
  - tests/test_inference_benchmark.py
  - .planning/phases/38-worker-de-inferencia-en-gpu-opcional/38-GPU-NOTES.md
  - .planning/REQUIREMENTS.md
autonomous: false
requirements: [SCALE-11, SCALE-12]

must_haves:
  truths:
    - "Existe un arnés que compara p50 de latencia CPU vs CUDA y se salta limpiamente sin GPU"
    - "La decisión de no implementar batching multi-cámara está escrita con sus cinco razones y su condición de reapertura"
    - "El criterio 3 del ROADMAP (>=3x FPS) queda como pendiente explícito, sin cifra inventada"
    - "El log de arranque dice, por motor, qué dispositivo quedó activo"
  artifacts:
    - path: "tests/test_inference_benchmark.py"
      provides: "arnés perf CPU vs CUDA con skip condicional"
      contains: "pytest.mark.perf"
    - path: ".planning/phases/38-worker-de-inferencia-en-gpu-opcional/38-GPU-NOTES.md"
      provides: "decisiones de cierre: batching descartado, criterio 3 pendiente, checklist de instalación GPU"
      min_lines: 60
  key_links:
    - from: "tests/test_inference_benchmark.py"
      to: "backend/inference/device.py"
      via: "skip cuando la sonda de torch dice que no hay CUDA"
      pattern: "_torch_cuda_available"
---

<objective>
Dejar el arnés de medición listo para el día que se instale el stack GPU, y cerrar por
escrito las dos decisiones que esta fase no implementa: el batching multi-cámara y la cifra
del ≥3×.

Purpose: CLAUDE.md prohíbe optimizar sin medir e inventar cifras. Este plan entrega el
instrumento de medida sin fingir el resultado, y convierte "no implementamos batching" en
una decisión razonada y auditable en vez de un hueco.
Output: `tests/test_inference_benchmark.py`, `38-GPU-NOTES.md` y las notas de trazabilidad
en `REQUIREMENTS.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/38-worker-de-inferencia-en-gpu-opcional/38-CONTEXT.md
@.planning/phases/38-worker-de-inferencia-en-gpu-opcional/38-RESEARCH.md
@.planning/phases/38-worker-de-inferencia-en-gpu-opcional/38-PATTERNS.md
@CLAUDE.md

<interfaces>
```python
# backend/detector.py  (plan 38-02)
PersonDetector(model_path=..., confidence=..., imgsz=640, device: str | None = None)
    .device_effective   # "cuda" | "cpu"
    .fallback_reason    # str | None

# backend/inference/device.py  (plan 38-01)
def _torch_cuda_available() -> bool: ...
```
</interfaces>

Anclajes verificados:
- `tests/test_detector.py:219` — `_BUS_JPG = Path("F:/Documentos/IA/Proyecto_Camara/.venv/Lib/site-packages/ultralytics/assets/bus.jpg")`
- `tests/test_detector.py:216-269` — fixtures `real_detector`/`bench_frame`, `@pytest.mark.perf`
  y la metodología de p50 con medidas **intercaladas** para repartir la deriva de carga
- `pytest.ini:4-8` — el marcador `perf` ya está registrado, no hay que añadir nada
- `.planning/REQUIREMENTS.md:295-296` — líneas de SCALE-11 y SCALE-12
- `.planning/ROADMAP.md:720-731` — los 6 criterios de éxito de la fase
- Entorno real verificado: `torch 2.11.0+cpu`, `onnxruntime 1.28.0` con providers
  `['AzureExecutionProvider', 'CPUExecutionProvider']`, GPU física RTX 2070 SUPER 8 GB
</context>

<tasks>

<task type="auto">
  <name>Task 1: Arnés de benchmark CPU vs CUDA con skip limpio</name>
  <read_first>
    - tests/test_detector.py (líneas 210-269: `_BUS_JPG`, fixtures `real_detector`/`bench_frame`, el test `perf` con medidas intercaladas y su docstring metodológica)
    - tests/test_reid_engine.py (líneas 46-60: `TEST_reid_latency_under_20ms`, misma metodología p50)
    - tests/integration/test_redis_bus.py (líneas 1-28: `pytestmark = pytest.mark.skipif(...)` por capacidad ausente)
    - backend/detector.py (constructor con `device`, ya modificado por el plan 38-02)
    - .planning/phases/38-worker-de-inferencia-en-gpu-opcional/38-PATTERNS.md § 7
  </read_first>
  <action>
Crear `tests/test_inference_benchmark.py` con este contenido:

```python
"""Arnes de medicion CPU vs CUDA para el criterio 3 de la Fase 38 (>=3x FPS).

NO cierra el criterio: este .venv tiene torch 2.11.0+cpu y un build de onnxruntime sin
CUDA EP, y la Fase 38 no instala dependencias por decision del usuario (38-CONTEXT.md).
El test queda preparado y se SALTA limpiamente; el numero real se mide el dia que se
instale el stack GPU y se anota en 38-GPU-NOTES.md. No se inventa ni se estima.

Metodologia identica a tests/test_detector.py:216-269 y a TEST_reid_latency_under_20ms:
p50 sobre N repeticiones (nunca una sola llamada, el jitter del planificador de Windows
lo haria flaky) y las dos configuraciones medidas INTERCALADAS, para que cualquier deriva
de carga de la maquina se reparta por igual entre ambas muestras en vez de sumarse entera
a la segunda.
"""

from __future__ import annotations

import statistics
import time
from pathlib import Path

import cv2
import numpy as np
import pytest

from backend.detector import PersonDetector
from backend.inference.device import _torch_cuda_available

_BUS_JPG = Path(
    "F:/Documentos/IA/Proyecto_Camara/.venv/Lib/site-packages/ultralytics/assets/bus.jpg"
)
_MIN_SPEEDUP = 3.0      # criterio 3 del ROADMAP
_REPS = 30
_WARMUP = 5


@pytest.fixture(scope="module")
def bench_frame() -> np.ndarray:
    if not _BUS_JPG.exists():
        pytest.skip(f"{_BUS_JPG} ausente — no se puede medir latencia real")
    img = cv2.imread(str(_BUS_JPG))
    if img is None:
        pytest.skip(f"{_BUS_JPG} ilegible")
    return cv2.resize(img, (1280, 720))


def _sample(detector: PersonDetector, frame: np.ndarray, out: list[float]) -> None:
    t0 = time.perf_counter()
    detector.detect_sv(frame)
    out.append(time.perf_counter() - t0)


@pytest.mark.perf
def TEST_cuda_detection_is_at_least_3x_faster_than_cpu(bench_frame):
    """Criterio 3 del ROADMAP. Se salta sin CUDA — nunca falla por ausencia de GPU."""
    if not _torch_cuda_available():
        pytest.skip(
            "torch sin CUDA en este entorno (torch 2.11.0+cpu). El criterio 3 de la "
            "Fase 38 queda PENDIENTE por decision de alcance, no por fallo: ver "
            ".planning/phases/38-worker-de-inferencia-en-gpu-opcional/38-GPU-NOTES.md"
        )

    # device=None es la ruta CPU intacta: a ultralytics NO se le pasa "cpu", que
    # escribiria CUDA_VISIBLE_DEVICES="" en el proceso y cegaria al detector CUDA
    # construido justo despues (38-RESEARCH.md, Pitfall 1).
    cpu = PersonDetector(model_path="yolo26n.pt", confidence=0.45, device=None)
    gpu = PersonDetector(model_path="yolo26n.pt", confidence=0.45, device="cuda:0")
    if gpu.device_effective != "cuda":
        pytest.skip(f"El detector no quedo en CUDA: {gpu.fallback_reason}")

    for _ in range(_WARMUP):
        cpu.detect_sv(bench_frame)
        gpu.detect_sv(bench_frame)

    cpu_samples: list[float] = []
    gpu_samples: list[float] = []
    for _ in range(_REPS):
        _sample(cpu, bench_frame, cpu_samples)
        _sample(gpu, bench_frame, gpu_samples)

    cpu_p50 = statistics.median(cpu_samples)
    gpu_p50 = statistics.median(gpu_samples)
    speedup = cpu_p50 / gpu_p50
    print(
        f"\nCPU p50 = {cpu_p50 * 1000:.1f} ms ({1 / cpu_p50:.1f} FPS) | "
        f"CUDA p50 = {gpu_p50 * 1000:.1f} ms ({1 / gpu_p50:.1f} FPS) | "
        f"speedup = {speedup:.2f}x"
    )
    assert speedup >= _MIN_SPEEDUP, (
        f"speedup {speedup:.2f}x por debajo del criterio 3 ({_MIN_SPEEDUP}x)"
    )


def TEST_benchmark_harness_skips_without_cuda():
    """El arnes NO debe fallar en una maquina sin GPU — solo saltarse.

    Se invoca la funcion del test directamente (no via pytest) para comprobar que la
    primera sentencia es el skip y que nunca llega a cargar un modelo.
    """
    if _torch_cuda_available():
        pytest.skip("hay CUDA en esta maquina: este test solo cubre el camino sin GPU")
    with pytest.raises(BaseException) as excinfo:
        TEST_cuda_detection_is_at_least_3x_faster_than_cpu(
            np.zeros((720, 1280, 3), dtype=np.uint8)
        )
    assert excinfo.typename == "Skipped"
```

No añadir marcadores ni fixtures a `pytest.ini`: `perf` ya está registrado.
No crear ningún script en `scripts/`: el precedente del repo para medir latencia es el test
`@pytest.mark.perf`, no un script (38-PATTERNS.md § 7).
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/test_inference_benchmark.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_inference_benchmark.py` existe
    - `grep -n "@pytest.mark.perf" tests/test_inference_benchmark.py` devuelve 1 línea
    - `grep -n "_MIN_SPEEDUP = 3.0" tests/test_inference_benchmark.py` devuelve 1 línea
    - `grep -n "_torch_cuda_available" tests/test_inference_benchmark.py` devuelve al menos 2 líneas
    - `.venv/Scripts/python.exe -m pytest tests/test_inference_benchmark.py -q` sale con código 0 y su resumen incluye `skipped` y no incluye `failed`
    - `.venv/Scripts/python.exe -m pytest tests/ -q -m perf` sale con código 0
  </acceptance_criteria>
  <done>El arnés existe, mide p50 intercalado, se salta limpiamente en este entorno sin GPU y no contiene ninguna cifra de speedup inventada.</done>
</task>

<task type="auto">
  <name>Task 2: Documentar el batching descartado y el criterio 3 pendiente</name>
  <read_first>
    - .planning/phases/38-worker-de-inferencia-en-gpu-opcional/38-RESEARCH.md § Q7 (las cinco razones contra el batching) y § "Environment Availability"
    - .planning/phases/38-worker-de-inferencia-en-gpu-opcional/38-CONTEXT.md § decisión 6 y § Deferred Ideas
    - .planning/ROADMAP.md (líneas 720-731: los 6 criterios de éxito de la Fase 38)
    - .planning/REQUIREMENTS.md (líneas 293-297: bloque SCALE-09..SCALE-12)
  </read_first>
  <action>
1. Crear `.planning/phases/38-worker-de-inferencia-en-gpu-opcional/38-GPU-NOTES.md`, en
   español, tono llano, sin emojis, con estas secciones y títulos literales:

   - `# Fase 38 — Notas de cierre: GPU, batching y el criterio 3`

   - `## Criterio 1 — DirectML queda fuera de alcance`
     Explicar que solo se implementó CUDA con fallback a CPU; que DirectML no es
     verificable en esta máquina y duplicaría la matriz de pruebas sin aportar nada
     comprobable; y que añadirlo mañana es añadir una entrada al dict `ONNX_PROVIDERS` de
     `backend/inference/device.py` más su sonda, no reescribir la lógica. Citar la decisión
     del usuario en `38-CONTEXT.md`.

   - `## Criterio 3 — el ≥3× queda PENDIENTE, con el instrumento listo`
     Dejar claro que no hay cifra porque el `.venv` tiene `torch 2.11.0+cpu` y un
     `onnxruntime 1.28.0` build CPU (providers
     `['AzureExecutionProvider', 'CPUExecutionProvider']`), y que esta fase no toca
     `requirements.txt` por decisión explícita del usuario. Señalar que el arnés vive en
     `tests/test_inference_benchmark.py` y se salta solo.
     **Prohibido escribir cualquier estimación o rango de speedup.**

   - `### Checklist para el día que se instale el stack GPU`
     En este orden:
     1. Verificar primero que CUDA 13 mantiene el soporte de Turing (SM 7.5) — la GPU es
        una RTX 2070 SUPER. Si no lo mantiene, fijar `onnxruntime-gpu <= 1.26` (CUDA 12.8).
     2. Decidir `requirements-gpu.txt` frente al `.venv` principal, y no instalar
        `onnxruntime` y `onnxruntime-gpu` en el mismo entorno.
     3. Ejecutar `.venv/Scripts/python.exe -m pytest tests/ -q -m perf -s` y anotar aquí el
        p50 de CPU, el p50 de CUDA y el cociente.
     4. Revalidar el fallback con el CUDA EP realmente presente: es el caso que hoy no se
        puede probar (`38-RESEARCH.md`, sección "Válido hasta").
     5. Medir el consumo de VRAM con N cámaras antes de fijar cualquier techo.

   - `## Criterio 6 — batching multi-cámara: NO se implementa`
     Las **cinco** razones, cada una con su anclaje de código:
     1. Broker, hilo y modelo son por cámara por diseño explícito (`manager.py:96`,
        `factory.py:73` y el comentario de `factory.py:10-11`, "nunca se comparte un modelo
        YOLO entre pipelines"). Batchear exige lo contrario: un modelo compartido
        alimentado por un colector que lea de N brokers — un rediseño del punto más
        sensible del pipeline, no una opción configurable.
     2. Rompe el invariante 2 de CLAUDE.md ("cada worker consume del broker a su ritmo"):
        esperar a que N cámaras tengan frame acopla la latencia de cada una a la más lenta,
        y cerrar el lote por timeout mete un temporizador nuevo en la ruta caliente.
     3. Cada cámara tiene su propio `AdaptiveRate` con su escalón (`rate.py:26`,
        `STEPS = (12.0, 8.0, 5.0, 3.0)`) y desde la Fase 36 un techo externo por reparto de
        CPU. Dos cámaras a 12 y 3 FPS no forman lotes.
     4. Hoy hay una sola cámara: el lote sería de tamaño 1, es decir el código actual con
        una capa de indirección encima. Y con varias, YOLO26n a 640 px en una 2070 SUPER no
        satura la GPU a 8 FPS por cámara: el cuello estaría en la decodificación RTSP y en
        el pre/post-proceso, que el batching no toca.
     5. Está listado como diferible en `38-CONTEXT.md` § Deferred Ideas.

   - `### Condición de reapertura`
     Más de 3 cámaras simultáneas **y** utilización de GPU medida por encima del 70 %.
     Mientras no se cumplan las dos, el batching sigue descartado.

   - `## Cómo queda cubierto el criterio 6`
     El criterio pide que el batching sea "opcional y desactivable". Con esta decisión, el
     estado por defecto y único es "desactivado", justificado y con condición de reapertura
     escrita. No hay código muerto que mantener.

   - `## Qué queda observable hoy`
     La clave `devices` de `GET /api/v2/cameras/{id}/health`, la línea de log por motor en
     el arranque (`Motor yolo en cpu`, `Motor reid en cpu`, `Motor face en cpu`) y el evento
     `DEGRADED_MODE` con el motivo real cuando el dispositivo efectivo no es el pedido.

2. En `.planning/REQUIREMENTS.md`, **sin tocar las casillas** `- [ ]` de SCALE-11 y SCALE-12
   (las marca la verificación de fase), añadir una nota indentada inmediatamente después de
   la línea de SCALE-12 (línea 296):

```markdown
  - *Nota (Fase 38):* la lógica de detección, selección, cableado y fallback está
    implementada y probada sin GPU. El criterio 3 del ROADMAP (FPS de detección ≥3×) queda
    **pendiente de medición**: requiere instalar `torch` con CUDA y `onnxruntime-gpu`, fuera
    del alcance de la fase por decisión de alcance. Arnés listo en
    `tests/test_inference_benchmark.py`; detalle en
    `.planning/phases/38-worker-de-inferencia-en-gpu-opcional/38-GPU-NOTES.md`.
```

No modificar ninguna otra línea de `REQUIREMENTS.md`.
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/test_inference_benchmark.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/38-worker-de-inferencia-en-gpu-opcional/38-GPU-NOTES.md` existe y tiene 60 líneas o más (`wc -l`)
    - `grep -n "Condición de reapertura" .planning/phases/38-worker-de-inferencia-en-gpu-opcional/38-GPU-NOTES.md` devuelve 1 línea
    - `grep -n "ONNX_PROVIDERS" .planning/phases/38-worker-de-inferencia-en-gpu-opcional/38-GPU-NOTES.md` devuelve al menos 1 línea
    - `grep -n "test_inference_benchmark.py" .planning/phases/38-worker-de-inferencia-en-gpu-opcional/38-GPU-NOTES.md` devuelve al menos 1 línea
    - `grep -c "^     [1-5]\.\|^     " .planning/phases/38-worker-de-inferencia-en-gpu-opcional/38-GPU-NOTES.md` confirma que la lista de cinco razones del batching está presente (5 elementos numerados bajo "Criterio 6")
    - `grep -n "test_inference_benchmark.py" .planning/REQUIREMENTS.md` devuelve 1 línea
    - `grep -n "^- \[ \] \*\*SCALE-11\*\*\|^- \[ \] \*\*SCALE-12\*\*" .planning/REQUIREMENTS.md` sigue devolviendo 2 líneas (las casillas NO se marcaron)
    - `git diff --stat .planning/REQUIREMENTS.md` muestra solo líneas añadidas, ninguna eliminada
  </acceptance_criteria>
  <done>`38-GPU-NOTES.md` recoge las cinco razones del batching con su condición de reapertura, el criterio 3 como pendiente sin cifra inventada y el checklist de instalación; `REQUIREMENTS.md` tiene la nota de trazabilidad.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verificación humana del log de arranque por motor</name>
  <read_first>
    - .planning/phases/38-worker-de-inferencia-en-gpu-opcional/38-VALIDATION.md § "Manual-Only Verifications"
    - backend/pipeline/factory.py (función `_report_device_fallbacks`, añadida por el plan 38-04)
    - backend/main.py (línea `logger.info("Motor face en %s", ...)`, añadida por el plan 38-04)
  </read_first>
  <what-built>
    Toda la ruta de dispositivo está automatizada y cubierta por tests sin GPU: selección
    (`tests/test_inference_device.py`), cableado a los tres motores
    (`tests/test_detector.py`, `tests/test_face_engine.py`, `tests/test_reid_engine.py`),
    emisión de `DEGRADED_MODE` y clave `devices` en `/health` (`tests/test_manager.py`,
    `tests/test_cameras_api.py`), y el arnés de benchmark que se salta sin CUDA.

    Lo único que un test no puede juzgar es si el log de arranque **se lee bien**: el
    criterio 1 del ROADMAP pide "log claro del dispositivo elegido", y eso es un juicio
    humano.
  </what-built>
  <how-to-verify>
    1. Arrancar el backend desde la raíz del repositorio:
       `.venv/Scripts/python.exe -m uvicorn backend.main:app --host 0.0.0.0 --port 8000`
    2. En el arranque deben aparecer tres líneas de nivel INFO, una por motor, con este
       aspecto (en esta máquina, sin CUDA, las tres deben decir `cpu`):
       - `Motor yolo en cpu`
       - `Motor reid en cpu`
       - `Motor face en cpu`
       Y una línea previa del selector:
       - `Dispositivo de inferencia: torch=cpu onnx=CPUExecutionProvider (inference_device=auto: torch_cuda=False, onnx_cuda=False)`
       No debe aparecer ninguna línea `ERROR` de `Fallback de dispositivo`: sin GPU en modo
       `auto` no hay fallback, hay elección de CPU desde el principio.
    3. Con el servidor arriba, comprobar el endpoint:
       `curl http://127.0.0.1:8000/api/v2/cameras/cam1/health`
       El JSON debe incluir un objeto `devices` con `requested: "auto"` y `effective: "cpu"`
       en `yolo`, `reid` y `face`, y todas las claves anteriores (`capture_fps`,
       `detection_fps`, `broker_stats`, `workers`) intactas.
    4. Opcional, para ver el modo de diagnóstico: parar el servidor, arrancarlo con
       `INFERENCE_DEVICE=cuda` y confirmar que **aborta** con un mensaje que explica que
       ninguna familia de motores puede usar CUDA. Volver a `auto` después.
  </how-to-verify>
  <resume-signal>Escribe "aprobado" si las líneas de log se entienden y el JSON es correcto, o describe qué falta o qué se lee mal.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| documentación de planificación → repositorio | `38-GPU-NOTES.md` y `REQUIREMENTS.md` se versionan en git |
| arnés de benchmark → sistema de ficheros | lectura de un asset dentro del `.venv` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-17 | Information disclosure | `38-GPU-NOTES.md` en el repo | mitigate | El documento solo contiene decisiones técnicas y rutas relativas del proyecto; prohibido incluir `CAMERA_URL`, credenciales RTSP o rutas con datos personales |
| T-38-18 | Repudiation | criterio 3 declarado como cumplido sin medición | mitigate | La nota en `REQUIREMENTS.md` y el `pytest.skip` explícito dejan constancia de que el número está pendiente; el test falla si el speedup real no llega a 3× |
| T-38-19 | Denial of service | el arnés `perf` cargando dos modelos YOLO a la vez | accept | Solo corre bajo `-m perf` y únicamente cuando hay CUDA; en el entorno actual se salta antes de construir nada |
</threat_model>

<verification>
```bash
.venv/Scripts/python.exe -m pytest tests/test_inference_benchmark.py -q
.venv/Scripts/python.exe -m pytest tests/ -q
```
La suite completa debe quedar en verde. El test del arnés debe reportar `skipped`, nunca
`failed`: sin stack GPU, saltarse es el resultado correcto.
</verification>

<success_criteria>
- El arnés de medición existe, sigue la metodología p50 intercalada del repo y se salta sin CUDA
- No hay ninguna cifra de speedup inventada en ningún fichero de la fase
- La decisión de no implementar batching está escrita con sus cinco razones ancladas en código y su condición de reapertura
- `REQUIREMENTS.md` refleja que el criterio 3 queda pendiente de medición
- El checkpoint humano confirma que el log de arranque dice, por motor, qué dispositivo quedó activo
</success_criteria>

<output>
Al terminar, crear `.planning/phases/38-worker-de-inferencia-en-gpu-opcional/38-05-SUMMARY.md`
</output>
