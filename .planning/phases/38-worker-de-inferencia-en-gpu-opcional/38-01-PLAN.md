---
phase: 38-worker-de-inferencia-en-gpu-opcional
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/inference/__init__.py
  - backend/inference/device.py
  - backend/config.py
  - backend/api/v2/config_schema.py
  - tests/test_inference_device.py
autonomous: true
requirements: [SCALE-11, SCALE-12]

must_haves:
  truths:
    - "Existe un único punto de decisión que traduce inference_device (auto|cpu|cuda) al dispositivo efectivo por familia de motor"
    - "Con inference_device=cpu la decisión es CPU sin sondear nada"
    - "Con inference_device=auto y sin CUDA utilizable la decisión es CPU sin lanzar"
    - "Con inference_device=cuda y sin CUDA utilizable el arranque falla con mensaje explícito"
    - "El módulo del selector es importable aunque torch u onnxruntime falten"
    - "Settings.inference_device existe y está registrado en config_schema"
  artifacts:
    - path: "backend/inference/device.py"
      provides: "DeviceChoice + resolve_device() + sondas por familia"
      exports: ["DeviceChoice", "resolve_device", "ONNX_PROVIDERS"]
    - path: "backend/config.py"
      provides: "Settings.inference_device"
      contains: "inference_device"
    - path: "backend/api/v2/config_schema.py"
      provides: "FieldDef de inference_device"
      contains: "INFERENCE_DEVICE"
    - path: "tests/test_inference_device.py"
      provides: "cobertura de las 3 rutas y de la disponibilidad por familia"
      min_lines: 80
  key_links:
    - from: "backend/inference/device.py"
      to: "torch / onnxruntime"
      via: "import diferido dentro de la sonda, envuelto en try/except"
      pattern: "def _torch_cuda_available"
    - from: "backend/api/v2/config_schema.py"
      to: "backend.config.Settings"
      via: "igualdad de conjuntos verificada por tests/test_config_schema.py"
      pattern: "key=\"inference_device\""
---

<objective>
Crear el punto único de decisión de dispositivo de inferencia y el ajuste de configuración
que lo gobierna, sin cablearlo todavía a ningún motor.

Purpose: los tres motores (YOLO, ArcFace, OSNet) viven en sitios distintos y ninguno es
dueño natural de la decisión. Un selector cacheado por proceso garantiza que los tres vean
la misma elección y que nadie decida en función de un entorno ya mutado por otro
(Pitfall 1 de 38-RESEARCH.md).
Output: `backend/inference/device.py`, `Settings.inference_device`, su `FieldDef` y el
fichero de tests del selector.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/38-worker-de-inferencia-en-gpu-opcional/38-CONTEXT.md
@.planning/phases/38-worker-de-inferencia-en-gpu-opcional/38-RESEARCH.md
@.planning/phases/38-worker-de-inferencia-en-gpu-opcional/38-PATTERNS.md
@CLAUDE.md

<interfaces>
<!-- Contratos que este plan CREA y que consumen 38-02, 38-03 y 38-04. -->
<!-- No explorar el codebase para inferirlos: son estos, exactamente. -->

```python
# backend/inference/device.py
@dataclass(frozen=True)
class DeviceChoice:
    torch_device: str | None          # "cuda:0" | None  (None = NO pasar device a Ultralytics)
    onnx_providers: tuple[str, ...]   # ("CUDAExecutionProvider","CPUExecutionProvider") | ("CPUExecutionProvider",)
    mode: str                         # "auto" | "cpu" | "cuda"  (lo pedido por configuración)
    reason: str                       # texto para el log y para el payload de DEGRADED_MODE

ONNX_PROVIDERS: dict[str, tuple[str, ...]]   # mapeo intención -> lista ordenada de providers

@lru_cache(maxsize=None)
def resolve_device(mode: str) -> DeviceChoice: ...
```

```python
# backend/config.py
inference_device: Literal["auto", "cpu", "cuda"] = "auto"
```
</interfaces>

Puntos de anclaje verificados (fichero:línea) que las tareas necesitan:
- `backend/config.py:61` — `yolo_imgsz: int = 640` (el campo nuevo va justo debajo)
- `backend/config.py:420-423` — `@lru_cache def get_settings()` (idioma de caché del proyecto)
- `backend/api/v2/config_schema.py:221-227` — `FieldDef(key="yolo_imgsz", ...)` (el `FieldDef`
  nuevo va justo después, dentro de `Group(key="personas")` de `Section(key="deteccion")`)
- `backend/api/v2/config_schema.py:78-83` — `FieldDef(key="camera_driver", ..., type="enum", enum_values=("tapo","generic"), applies="restart_server")` (molde exacto de enum)
- `tests/test_config_schema.py:24-26` — igualdad de conjuntos `all_fields()` ↔ `Settings.model_fields`
- `tests/test_reid_engine.py:73-97` y `tests/test_face_engine.py:64-74` — molde de `monkeypatch.setattr` sobre el símbolo importado en el módulo
</context>

<tasks>

<task type="auto">
  <name>Task 1: Añadir Settings.inference_device y su FieldDef en config_schema</name>
  <read_first>
    - backend/config.py (líneas 1-70: constantes de módulo, campo yolo_imgsz, estilo de validadores)
    - backend/api/v2/config_schema.py (líneas 28-45: dataclass FieldDef; líneas 76-84: FieldDef de camera_driver; líneas 188-228: Section "deteccion" / Group "personas")
    - tests/test_config_schema.py (líneas 1-40: TEST_all_fields_covers_every_settings_attribute y TEST_no_field_has_empty_hint)
    - .planning/phases/38-worker-de-inferencia-en-gpu-opcional/38-PATTERNS.md § 2
  </read_first>
  <action>
En `backend/config.py`:

1. Añadir `Literal` al import de typing en la cabecera del fichero:
   `from typing import Literal` (colocar tras `from pathlib import Path`, respetando el orden
   de imports existente).
2. Justo después de la línea `yolo_imgsz: int = 640` (config.py:61), añadir el campo con su
   comentario:

```python
    # Dispositivo de inferencia para los tres motores (YOLO, ArcFace, OSNet).
    # "auto" (default): detecta CUDA por familia de motor y cae a CPU si no la hay —
    #   en una maquina sin GPU el comportamiento es identico al de la Fase 37 (SCALE-12).
    # "cpu": fuerza CPU sin sondear nada.
    # "cuda": exige GPU y aborta el arranque si ninguna familia puede usarla (diagnostico).
    inference_device: Literal["auto", "cpu", "cuda"] = "auto"
```

   NO añadir `@field_validator`: `Literal` ya hace que pydantic-settings rechace cualquier
   otro valor. Es la opción que 38-PATTERNS.md § 2 declara coherente y más simple.

En `backend/api/v2/config_schema.py`, dentro de `Section(key="deteccion")` →
`Group(key="personas")`, inmediatamente **después** del `FieldDef(key="yolo_imgsz", ...)`
que termina en la línea 227, añadir:

```python
                    FieldDef(
                        key="inference_device", env="INFERENCE_DEVICE",
                        label="Dispositivo de inferencia",
                        hint="'auto' detecta la GPU y cae a CPU si no está disponible; "
                             "'cpu' fuerza CPU; 'cuda' exige GPU y falla el arranque si no "
                             "la hay (para diagnóstico). Afecta a YOLO, ArcFace y OSNet.",
                        type="enum", default="auto",
                        enum_values=("auto", "cpu", "cuda"),
                        applies="restart_server",
                    ),
```

`applies="restart_server"` porque cambiar de dispositivo obliga a recargar los tres modelos;
no hay ruta en caliente y este módulo solo señaliza (config_schema.py:15-19).
`secret` y `readonly` se dejan en su default (`False`).
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/test_config_schema.py tests/test_config.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'inference_device: Literal\["auto", "cpu", "cuda"\] = "auto"' backend/config.py` devuelve 1 línea
    - `grep -n 'from typing import Literal' backend/config.py` devuelve 1 línea
    - `grep -n 'key="inference_device", env="INFERENCE_DEVICE"' backend/api/v2/config_schema.py` devuelve 1 línea
    - `grep -n 'enum_values=("auto", "cpu", "cuda")' backend/api/v2/config_schema.py` devuelve 1 línea
    - `.venv/Scripts/python.exe -m pytest tests/test_config_schema.py tests/test_config.py -q` sale con código 0
    - `.venv/Scripts/python.exe -c "from backend.config import Settings; print(Settings().inference_device)"` imprime `auto`
  </acceptance_criteria>
  <done>`Settings.inference_device` existe con default `"auto"`, está registrado en `config_schema` y `tests/test_config_schema.py` sigue en verde.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Crear backend/inference/device.py con resolve_device() y sus tests</name>
  <read_first>
    - .planning/phases/38-worker-de-inferencia-en-gpu-opcional/38-RESEARCH.md § "Code Examples" (sondas verificadas) y § Q2 (fallback silencioso de ONNXRuntime)
    - .planning/phases/38-worker-de-inferencia-en-gpu-opcional/38-PATTERNS.md § 1 y § 6
    - backend/events/bus.py (líneas 247-255: `create_event_bus`, molde de "único punto de decisión")
    - backend/config.py (líneas 418-423: `@lru_cache def get_settings()`)
    - tests/test_reid_engine.py (líneas 73-97: molde de monkeypatch sobre el símbolo del módulo)
    - tests/test_architecture.py (líneas 1-30: `INFERENCE_CALLS` — el selector NO debe llamarse `detect` ni `embed`)
  </read_first>
  <behavior>
    - `resolve_device("cpu")` → `DeviceChoice(torch_device=None, onnx_providers=("CPUExecutionProvider",), mode="cpu", reason=<contiene "forzado">)`, y NO llama a ninguna sonda
    - `resolve_device("auto")` con ambas sondas `False` → `torch_device is None`, `onnx_providers == ("CPUExecutionProvider",)`
    - `resolve_device("auto")` con ambas sondas `True` → `torch_device == "cuda:0"`, `onnx_providers == ("CUDAExecutionProvider", "CPUExecutionProvider")`
    - `resolve_device("auto")` con torch=True y ort=False → `torch_device == "cuda:0"` y `onnx_providers == ("CPUExecutionProvider",)` (disponibilidad POR FAMILIA)
    - `resolve_device("auto")` con torch=False y ort=True → `torch_device is None` y `onnx_providers == ("CUDAExecutionProvider", "CPUExecutionProvider")`
    - `resolve_device("cuda")` con ambas sondas `False` → lanza `RuntimeError` cuyo mensaje contiene `inference_device='cuda'`
    - `resolve_device("cuda")` con al menos una sonda `True` → no lanza y usa CUDA solo en la familia que puede
    - `_torch_cuda_available()` devuelve `False` sin lanzar cuando el import de torch falla
    - `_ort_cuda_available()` devuelve `False` sin lanzar cuando el import de onnxruntime falla
    - `resolve_device` es idempotente por `@lru_cache`: dos llamadas con el mismo `mode` devuelven el MISMO objeto (`is`)
  </behavior>
  <action>
Crear el paquete `backend/inference/` con `__init__.py` (docstring de una línea:
`"""Seleccion de dispositivo de inferencia (CUDA/CPU) compartida por los tres motores."""`)
y el módulo `backend/inference/device.py` con este contenido exacto de contrato:

```python
"""Punto UNICO de decision del dispositivo de inferencia (Fase 38, SCALE-11/SCALE-12).

Mismo rol que `create_event_bus` (events/bus.py:247-255) para el bus de la Fase 37: la
configuracion se traduce aqui una sola vez y el resto del codigo no vuelve a ramificar.

Tres reglas no negociables, todas verificadas en 38-RESEARCH.md:
1. La ruta CPU devuelve `torch_device=None` — a Ultralytics NO se le pasa device en
   absoluto, porque `select_device("cpu")` escribe CUDA_VISIBLE_DEVICES="" a nivel de
   PROCESO (ultralytics/utils/torch_utils.py:214) y cegaria tambien al CUDA EP de
   onnxruntime construido despues.
2. La disponibilidad es POR FAMILIA de motor: torch puede tener CUDA y el build de
   onnxruntime no (o al reves). Un unico booleano `gpu_available` enmascararia ese caso.
3. Los imports de torch/onnxruntime son DIFERIDOS y envueltos en try/except: el modulo
   debe ser importable y testeable sin ninguno de los dos.

Extensibilidad (DirectML u otro proveedor): anadir una entrada a ONNX_PROVIDERS y su
sonda correspondiente. No hay que reescribir la logica.
"""

from __future__ import annotations

import logging
from dataclasses import dataclass
from functools import lru_cache

logger = logging.getLogger(__name__)

# Orden = politica de fallback de onnxruntime: preferido primero, CPUExecutionProvider
# SIEMPRE al final. ORT no lanza cuando falta un provider (verificado en 1.28.0): lo
# descarta y sigue, asi que la lista ordenada ES el mecanismo de fallback.
ONNX_PROVIDERS: dict[str, tuple[str, ...]] = {
    "cuda": ("CUDAExecutionProvider", "CPUExecutionProvider"),
    "cpu": ("CPUExecutionProvider",),
}

_CPU_ONLY = ONNX_PROVIDERS["cpu"]


@dataclass(frozen=True)
class DeviceChoice:
    """Intencion resuelta. Cada motor la traduce a su propio dialecto."""

    torch_device: str | None
    onnx_providers: tuple[str, ...]
    mode: str
    reason: str


def _torch_cuda_available() -> bool:
    """True si ESTE proceso puede usar CUDA con torch. Nunca lanza."""
    try:
        import torch

        return bool(torch.cuda.is_available())
    except Exception:
        return False


def _ort_cuda_available() -> bool:
    """True si ESTE build de onnxruntime expone el CUDA EP. Nunca lanza.

    get_available_providers() lista lo que el build puede usar; get_all_providers()
    lista lo que ORT conoce en abstracto e incluiria CUDAExecutionProvider incluso en
    un build CPU — usarla seria el bug clasico.
    """
    try:
        import onnxruntime as ort

        return "CUDAExecutionProvider" in ort.get_available_providers()
    except Exception:
        return False


def _ort_providers_repr() -> str:
    try:
        import onnxruntime as ort

        return str(ort.get_available_providers())
    except Exception:
        return "<onnxruntime no importable>"


@lru_cache(maxsize=None)
def resolve_device(mode: str) -> DeviceChoice:
    """Traduce `Settings.inference_device` al dispositivo efectivo por familia.

    Cacheado por proceso para que los tres motores vean la MISMA decision aunque algo
    mute el entorno entre construcciones. Los tests deben llamar a
    `resolve_device.cache_clear()` entre casos.
    """
    if mode == "cpu":
        choice = DeviceChoice(
            torch_device=None,
            onnx_providers=_CPU_ONLY,
            mode=mode,
            reason="CPU forzado por configuracion (inference_device=cpu)",
        )
        logger.info("Dispositivo de inferencia: %s", choice.reason)
        return choice

    torch_ok = _torch_cuda_available()
    ort_ok = _ort_cuda_available()

    if mode == "cuda" and not torch_ok and not ort_ok:
        raise RuntimeError(
            "inference_device='cuda' pero ninguna familia de motores puede usar CUDA: "
            f"torch.cuda.is_available()={torch_ok}, "
            f"onnxruntime.get_available_providers()={_ort_providers_repr()}. "
            "Usa inference_device='auto' para caer a CPU automaticamente."
        )

    choice = DeviceChoice(
        torch_device="cuda:0" if torch_ok else None,
        onnx_providers=ONNX_PROVIDERS["cuda"] if ort_ok else _CPU_ONLY,
        mode=mode,
        reason=(
            f"inference_device={mode}: torch_cuda={torch_ok}, onnx_cuda={ort_ok}"
        ),
    )
    logger.info(
        "Dispositivo de inferencia: torch=%s onnx=%s (%s)",
        choice.torch_device or "cpu",
        choice.onnx_providers[0],
        choice.reason,
    )
    return choice
```

Crear después `tests/test_inference_device.py` cubriendo cada punto de `<behavior>` como una
función `TEST_*` independiente (convención `python_functions = TEST_*` de `pytest.ini`).
Reglas de los tests:
- Usar `monkeypatch.setattr(device_module, "_torch_cuda_available", lambda: True/False)` y
  lo mismo con `_ort_cuda_available` — el molde de `tests/test_reid_engine.py:81-97`
  (parchear el símbolo tal como está importado en el módulo).
- Llamar a `device_module.resolve_device.cache_clear()` al principio de CADA test (o vía
  una fixture `autouse` local en el fichero) — sin eso el `@lru_cache` contamina los casos.
- El caso "import falla" se cubre con `monkeypatch.setitem(sys.modules, "torch", None)` y
  `monkeypatch.setitem(sys.modules, "onnxruntime", None)`, afirmando `is False` sin excepción.
- Nombrar los tests para que los `-k` del mapa de validación casen:
  `TEST_auto_prefers_cuda_when_both_probes_true`,
  `TEST_auto_falls_back_to_cpu_without_cuda`,
  `TEST_cuda_forced_raises_without_any_cuda`,
  `TEST_mixed_availability_torch_only`, `TEST_mixed_availability_onnx_only`,
  `TEST_cpu_mode_never_probes`, `TEST_probes_return_false_without_packages`,
  `TEST_resolve_device_is_cached_per_mode`.
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/test_inference_device.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `backend/inference/__init__.py` y `backend/inference/device.py` existen
    - `grep -c "def TEST_" tests/test_inference_device.py` devuelve un número >= 8
    - `grep -n "cache_clear" tests/test_inference_device.py` devuelve al menos 1 línea
    - `grep -n "get_all_providers" backend/inference/device.py` NO devuelve ninguna línea
    - `grep -n 'ONNX_PROVIDERS' backend/inference/device.py` muestra el dict con las claves `"cuda"` y `"cpu"`
    - `.venv/Scripts/python.exe -m pytest tests/test_inference_device.py -q` sale con código 0
    - `.venv/Scripts/python.exe -c "from backend.inference.device import resolve_device; c=resolve_device('auto'); print(c.torch_device, c.onnx_providers)"` imprime `None ('CPUExecutionProvider',)` en este .venv (torch 2.11.0+cpu, onnxruntime build CPU)
    - `.venv/Scripts/python.exe -m pytest tests/test_architecture.py -q` sale con código 0
  </acceptance_criteria>
  <done>`resolve_device()` resuelve las tres modalidades con disponibilidad por familia, nunca lanza en `auto`/`cpu`, lanza `RuntimeError` en `cuda` sin CUDA, y los tests pasan sin GPU.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operador → `Settings.inference_device` | valor de configuración (env var o tabla `app_config`) que entra al proceso |
| proceso → variables de entorno del SO | `CUDA_VISIBLE_DEVICES` puede ser mutada por terceros (ultralytics) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-01 | Tampering | `Settings.inference_device` | mitigate | `Literal["auto","cpu","cuda"]` en el campo: pydantic rechaza cualquier otro valor y aborta el arranque; no hay ruta que lo interpole en un comando ni en una ruta de fichero |
| T-38-02 | Information disclosure | `reason` del `DeviceChoice` y log de arranque | mitigate | El texto solo contiene booleanos de sonda y nombres de provider; `secret=False` es correcto porque no hay credencial ni ruta absoluta en el campo |
| T-38-03 | Denial of service | `resolve_device` en modo `cuda` | accept | Abortar el arranque es el comportamiento pedido (decisión 5 de 38-CONTEXT.md): es un modo de diagnóstico explícito, no alcanzable por defecto |
| T-38-04 | Tampering | `CUDA_VISIBLE_DEVICES` mutada por otro componente del proceso | mitigate | `@lru_cache` fija la decisión ANTES de construir el primer motor y la ruta CPU no pasa `"cpu"` a Ultralytics (Pitfall 1 de 38-RESEARCH.md) |
</threat_model>

<verification>
```bash
.venv/Scripts/python.exe -m pytest tests/test_inference_device.py tests/test_config_schema.py tests/test_config.py tests/test_architecture.py -q
```
Debe salir con código 0. El comportamiento del `.venv` actual (torch `+cpu`, onnxruntime build
CPU) debe seguir siendo CPU en modo `auto`: es exactamente lo que exige SCALE-12.
</verification>

<success_criteria>
- `Settings.inference_device` existe, valida por `Literal` y está en `config_schema`
- `resolve_device()` cubre `auto`/`cpu`/`cuda` con disponibilidad por familia de motor
- El módulo es importable sin torch y sin onnxruntime
- `tests/test_inference_device.py` tiene >= 8 tests `TEST_*` y pasa sin GPU
- Ningún fichero de `requirements.txt` ni del `.venv` ha sido tocado
</success_criteria>

<output>
Al terminar, crear `.planning/phases/38-worker-de-inferencia-en-gpu-opcional/38-01-SUMMARY.md`
</output>
