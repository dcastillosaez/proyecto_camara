---
phase: 38-worker-de-inferencia-en-gpu-opcional
plan: 02
type: execute
wave: 2
depends_on: ["38-01"]
files_modified:
  - backend/detector.py
  - tests/test_detector.py
autonomous: true
requirements: [SCALE-11, SCALE-12]

must_haves:
  truths:
    - "PersonDetector acepta un device opcional y, si es CUDA, mueve el modelo con .to()"
    - "Con device=None (ruta CPU) PersonDetector se comporta exactamente como en la Fase 37: no llama a .to() ni pasa device= a la inferencia"
    - "Construir un PersonDetector en CPU no muta CUDA_VISIBLE_DEVICES"
    - "Si .to('cuda:0') falla, el detector sigue usable en CPU y publica el motivo real"
    - "El calentamiento solo ocurre en la ruta CUDA"
  artifacts:
    - path: "backend/detector.py"
      provides: "PersonDetector con device_requested / device_effective / fallback_reason"
      contains: "fallback_reason"
    - path: "tests/test_detector.py"
      provides: "cobertura de ruta CPU intacta, ruta CUDA y fallback"
      contains: "cpu_path_untouched"
  key_links:
    - from: "backend/detector.py"
      to: "ultralytics YOLO"
      via: ".to(device) solo cuando device empieza por 'cuda'"
      pattern: "self\\._model\\.to\\("
---

<objective>
Cablear la decisión de dispositivo al motor YOLO sin tocar la ruta CPU.

Purpose: `PersonDetector` es el único punto del código que conoce la API de Ultralytics.
Aquí se traduce la intención (`torch_device`) al dialecto de esa librería y se lee el
dispositivo efectivo, que es lo que después dispara `DEGRADED_MODE` en el plan 38-04.
Output: `PersonDetector` con `device`, atributos de observabilidad y warm-up condicional.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/38-worker-de-inferencia-en-gpu-opcional/38-RESEARCH.md
@.planning/phases/38-worker-de-inferencia-en-gpu-opcional/38-PATTERNS.md
@CLAUDE.md

<interfaces>
<!-- Contrato ya creado por el plan 38-01. No hace falta explorar backend/inference/. -->

```python
# backend/inference/device.py
@dataclass(frozen=True)
class DeviceChoice:
    torch_device: str | None          # "cuda:0" | None  (None = NO pasar device a Ultralytics)
    onnx_providers: tuple[str, ...]
    mode: str
    reason: str
```

<!-- Contrato que ESTE plan crea y consume 38-04 (factory.py) -->
```python
# backend/detector.py
class PersonDetector:
    def __init__(self, model_path=..., confidence=..., classes=..., label=...,
                 imgsz=640, device: str | None = None) -> None: ...
    device_requested: str        # "cuda" | "cpu"
    device_effective: str        # "cuda" | "cpu"
    fallback_reason: str | None  # None si efectivo == pedido
```
</interfaces>

Hechos verificados que la tarea NO debe re-descubrir (38-RESEARCH.md Q1 y Pitfall 1):
- `YOLO.__init__` NO acepta `device` (firma real: `(model, task=None, verbose=False)`)
- `.to(device)` NO cambia `id(self._model)` → el contrato de `set_classes` en caliente
  (`detector.py:59-81`) queda intacto y el `WorkerSupervisor` no ve ninguna parada
- Pasar `device="cpu"` a Ultralytics escribe `os.environ["CUDA_VISIBLE_DEVICES"] = ""` a
  nivel de PROCESO y cegaría al CUDA EP de onnxruntime construido después → por eso la
  ruta CPU no pasa `device` en absoluto
- `.to("cuda")` sin CUDA lanza `AssertionError`, y `select_device("cuda")` lanza
  `ValueError` → hay que capturar `Exception`, no un tipo concreto
- `yolo_model_path` acepta `.onnx` (`config.py:46`) y `.to()` revienta sobre un modelo no
  PyTorch → el `try/except` cubre también ese caso
- La primera inferencia cuesta 203 ms frente a 89 ms en caliente y `AdaptiveRate.observe()`
  siembra la EMA con la primera medida (`rate.py:81-83`) → warm-up solo en CUDA
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PersonDetector acepta device, verifica el efectivo y calienta solo en CUDA</name>
  <read_first>
    - backend/detector.py (fichero completo: constructor en 23-35, detect/detect_sv, docstring de set_classes en 59-81)
    - .planning/phases/38-worker-de-inferencia-en-gpu-opcional/38-RESEARCH.md § Q1, § Pitfall 1, § Pitfall 3, § Pitfall 7, § Pitfall 8
    - .planning/phases/38-worker-de-inferencia-en-gpu-opcional/38-PATTERNS.md § 3.a
    - backend/perception/reid/engine.py (líneas 36-66: molde de `try/except Exception` + `logger.exception` sin `except: pass`)
    - tests/test_architecture.py (líneas 1-30: `INFERENCE_CALLS` — el método de calentamiento NO puede llamarse `detect` ni `embed`)
  </read_first>
  <behavior>
    - `PersonDetector(model_path=..., device=None)` no invoca `_model.to` en absoluto y deja `device_requested == "cpu"`, `device_effective == "cpu"`, `fallback_reason is None`
    - `PersonDetector(..., device="cuda:0")` con `.to()` que funciona y `model.device == "cuda:0"` deja `device_effective == "cuda"` y `fallback_reason is None`
    - `PersonDetector(..., device="cuda:0")` con `.to()` que lanza deja `device_effective == "cpu"`, `fallback_reason` no vacío conteniendo el `repr` de la excepción, y el detector sigue construido (no propaga)
    - `PersonDetector(..., device="cuda:0")` con `.to()` que no lanza pero deja `model.device == "cpu"` deja `device_effective == "cpu"` y `fallback_reason` no vacío
    - Construir con `device=None` no crea ni modifica `os.environ["CUDA_VISIBLE_DEVICES"]`
    - Con `device=None` no se ejecuta ninguna inferencia de calentamiento (el mock del modelo no recibe llamadas durante `__init__`)
    - `detect()` y `detect_sv()` siguen sin recibir el kwarg `device` en ninguna ruta
  </behavior>
  <action>
Editar `backend/detector.py`:

1. Añadir `import logging` y `logger = logging.getLogger(__name__)` tras los imports
   existentes (el fichero hoy no tiene logger).
2. Cambiar la firma del constructor añadiendo un último parámetro `device: str | None = None`
   y dejar la línea `self._model = YOLO(model_path)` **exactamente como está** (detector.py:31).
3. Tras las asignaciones actuales (`self._imgsz = imgsz`), añadir el bloque de dispositivo:

```python
        # Fase 38 (SCALE-11/SCALE-12). device=None es la ruta CPU y debe quedar
        # BYTE A BYTE como en la Fase 37: nada de .to("cpu") ni de device= en la
        # inferencia. select_device("cpu") de Ultralytics escribe
        # os.environ["CUDA_VISIBLE_DEVICES"]="" a nivel de PROCESO
        # (ultralytics/utils/torch_utils.py:214) y cegaria tambien al CUDA EP de
        # onnxruntime construido despues.
        self.device_requested = "cuda" if (device or "").startswith("cuda") else "cpu"
        self.device_effective = "cpu"
        self.fallback_reason: str | None = None
        if self.device_requested == "cuda":
            try:
                # .to() NO cambia id(self._model) (verificado en 38-RESEARCH.md Q1),
                # asi que el contrato de set_classes en caliente sigue intacto.
                self._model.to(device)
                effective = str(getattr(self._model, "device", "cpu"))
                self.device_effective = "cuda" if effective.startswith("cuda") else "cpu"
                if self.device_effective != "cuda":
                    self.fallback_reason = (
                        f"ultralytics quedo en {effective!r} tras .to({device!r})"
                    )
                    logger.warning("PersonDetector: %s", self.fallback_reason)
                else:
                    self._warmup()
            except Exception as exc:
                # AssertionError por .to("cuda") sin CUDA, ValueError por select_device,
                # ImportError si falta torch, y tambien el caso de un yolo_model_path
                # .onnx (config.py:46 lo permite y _check_is_pytorch_model lanza).
                # except Exception + traza real, nunca `except: pass`.
                self.fallback_reason = f"PersonDetector: .to({device!r}) fallo: {exc!r}"
                logger.exception("PersonDetector: fallo al mover el modelo a %s", device)
```

4. Añadir el método privado de calentamiento justo debajo del constructor:

```python
    def _warmup(self) -> None:
        """Una inferencia sintetica para pagar el arranque en frio ANTES del worker.

        Solo en la ruta CUDA. AdaptiveRate.observe() siembra la EMA con la primera
        medida (rate.py:81-83): una primera inferencia lenta arrastraria el FPS objetivo
        durante decenas de segundos. En CPU NO se hace, porque anadir un paso nuevo al
        arranque cambiaria el comportamiento respecto a la Fase 37 (SCALE-12).

        Se llama _warmup y no detect/embed a proposito: esos nombres estan en
        INFERENCE_CALLS de tests/test_architecture.py.
        """
        try:
            blank = np.zeros((self._imgsz, self._imgsz, 3), dtype=np.uint8)
            self._model(
                blank, classes=self._classes, conf=self._confidence,
                imgsz=self._imgsz, verbose=False,
            )
        except Exception:
            logger.exception("PersonDetector: warm-up fallido (no bloqueante)")
```

NO tocar `detect`, `detect_sv`, `set_classes` ni `annotate`.
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/test_detector.py tests/test_architecture.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "device: str | None = None" backend/detector.py` devuelve 1 línea dentro de la firma de `__init__`
    - `grep -n 'self._model = YOLO(model_path)' backend/detector.py` sigue devolviendo la línea sin cambios
    - `grep -n '\.to("cpu")\|device="cpu"' backend/detector.py` NO devuelve ninguna línea
    - `grep -n "def _warmup" backend/detector.py` devuelve 1 línea
    - `grep -n "device=" backend/detector.py | grep -v "def __init__\|device=device" ` no muestra ningún `device=` dentro de las llamadas de `detect`/`detect_sv`
    - `.venv/Scripts/python.exe -m pytest tests/test_detector.py tests/test_architecture.py -q` sale con código 0
  </acceptance_criteria>
  <done>`PersonDetector` acepta `device`, mueve el modelo solo en CUDA, expone `device_requested`/`device_effective`/`fallback_reason` y la ruta CPU no cambió ni una línea de comportamiento.</done>
</task>

<task type="auto">
  <name>Task 2: Tests de la ruta CPU intacta, la ruta CUDA y el fallback</name>
  <read_first>
    - tests/test_detector.py (líneas 1-50: helpers `_make_yolo_box`, fixture `detector` con `patch("backend.detector.YOLO")`; líneas 216-269: fixtures `real_detector`/`bench_frame` y el marcador `perf`)
    - backend/detector.py (constructor ya modificado en la Task 1)
    - .planning/phases/38-worker-de-inferencia-en-gpu-opcional/38-RESEARCH.md § "Validation Architecture" (mapa requisito → test, columna de comandos `-k`)
  </read_first>
  <action>
Añadir al final de `tests/test_detector.py` una sección nueva con estos tests, usando el
mismo estilo del fichero (`patch("backend.detector.YOLO")` de `unittest.mock`, convención
`TEST_*`). Los nombres son los del mapa de validación y deben respetarse literalmente para
que los comandos `-k` funcionen:

1. `TEST_cpu_path_untouched_never_calls_to()`
   Construir `PersonDetector(model_path="yolov8n.pt", device=None)` con `YOLO` parcheado.
   Afirmar: `MockYOLO.return_value.to.call_count == 0`,
   `MockYOLO.return_value.call_count == 0` (no hubo warm-up),
   `d.device_requested == "cpu"`, `d.device_effective == "cpu"`,
   `d.fallback_reason is None`.

2. `TEST_cpu_path_no_env_mutation()`
   Guardar `os.environ.get("CUDA_VISIBLE_DEVICES")` antes, construir el detector con
   `device=None`, y afirmar que el valor después es idéntico al de antes (incluido el caso
   `None`). Importar `os` en la cabecera del fichero si no está.

3. `TEST_cuda_path_moves_model_and_warms_up()`
   Con `YOLO` parcheado y `MockYOLO.return_value.device = "cuda:0"`, construir con
   `device="cuda:0"`. Afirmar: `to` llamado exactamente una vez con `"cuda:0"`,
   `d.device_effective == "cuda"`, `d.fallback_reason is None`, y que el modelo fue
   invocado al menos una vez (warm-up).

4. `TEST_cuda_path_falls_back_when_to_raises()`
   `MockYOLO.return_value.to.side_effect = AssertionError("Torch not compiled with CUDA enabled")`.
   Afirmar: el constructor NO propaga, `d.device_effective == "cpu"`,
   `"Torch not compiled" in d.fallback_reason`.

5. `TEST_cuda_path_detects_silent_downgrade()`
   `to` no lanza pero `MockYOLO.return_value.device = "cpu"`.
   Afirmar: `d.device_effective == "cpu"` y `d.fallback_reason` contiene `"cpu"`.

6. `TEST_detect_never_passes_device_kwarg()`
   Con la fixture `detector` existente (ruta CPU), llamar a `detect(blank_frame)` y a
   `detect_sv(blank_frame)` y afirmar que en `d._mock_model.call_args.kwargs` NO aparece
   la clave `"device"`. Esta es la aserción literal del criterio 4 del ROADMAP.

No modificar ningún test existente del fichero.
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/test_detector.py -q -m "not perf"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "def TEST_cpu_path_untouched_never_calls_to\|def TEST_cpu_path_no_env_mutation\|def TEST_cuda_path_moves_model_and_warms_up\|def TEST_cuda_path_falls_back_when_to_raises\|def TEST_cuda_path_detects_silent_downgrade\|def TEST_detect_never_passes_device_kwarg" tests/test_detector.py` devuelve 6
    - `.venv/Scripts/python.exe -m pytest tests/test_detector.py -q -m "not perf"` sale con código 0
    - `.venv/Scripts/python.exe -m pytest tests/test_detector.py -k cpu_path_untouched -q` recoge y pasa exactamente 1 test
    - `.venv/Scripts/python.exe -m pytest tests/test_detector.py -k no_env_mutation -q` recoge y pasa exactamente 1 test
  </acceptance_criteria>
  <done>Los seis tests existen, pasan sin GPU y fijan por contrato que la ruta CPU no cambió.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `Settings.yolo_model_path` → `PersonDetector` | ruta de fichero de modelo procedente de configuración |
| proceso → `os.environ` | `CUDA_VISIBLE_DEVICES` es estado global del proceso |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-05 | Tampering | `PersonDetector._warmup` con `yolo_model_path` apuntando a un `.onnx` | mitigate | El `try/except Exception` del bloque de dispositivo captura `_check_is_pytorch_model`; el warm-up solo corre tras un `.to()` exitoso y además tiene su propio `try/except` no bloqueante |
| T-38-06 | Tampering | `CUDA_VISIBLE_DEVICES` | mitigate | Test `TEST_cpu_path_no_env_mutation` fija que construir en CPU no altera la variable; la ruta CPU no pasa `device` a Ultralytics |
| T-38-07 | Denial of service | warm-up en el arranque | accept | Solo corre en la ruta CUDA, una vez por detector, y su fallo es no bloqueante (log + continuar) |
| T-38-08 | Information disclosure | `fallback_reason` incluye `repr(exc)` | accept | La excepción de Ultralytics/torch no contiene credenciales; `yolo_model_path` está validado para quedar dentro del proyecto (`config.py:47-53`) |
</threat_model>

<verification>
```bash
.venv/Scripts/python.exe -m pytest tests/test_detector.py tests/test_architecture.py -q
```
Debe salir con código 0. Además, la ruta CPU debe quedar demostrablemente intacta:
ninguna llamada a `.to()`, ningún `device=` en la inferencia, ninguna mutación de
`CUDA_VISIBLE_DEVICES`.
</verification>

<success_criteria>
- `PersonDetector` acepta `device: str | None = None` y lo traduce al dialecto de Ultralytics
- Ruta CPU sin cambio de comportamiento observable (SCALE-12)
- Fallback de CUDA a CPU detectado por comparación con `model.device`, con motivo real
- Warm-up exclusivo de la ruta CUDA
- 6 tests nuevos en verde sin GPU
</success_criteria>

<output>
Al terminar, crear `.planning/phases/38-worker-de-inferencia-en-gpu-opcional/38-02-SUMMARY.md`
</output>
