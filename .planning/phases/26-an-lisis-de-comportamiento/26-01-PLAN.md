---
phase: 26-an-lisis-de-comportamiento
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/perception/behavior.py
  - tests/test_behavior_analyzer.py
  - tests/test_memory_bounds.py
autonomous: true
requirements: [BEH-01, BEH-02, BEH-03, BEH-05]

must_haves:
  truths:
    - "Un track que se queda dentro de un cuadrado de 20 px durante mas de 60 s produce exactamente un finding IMMOBILE"
    - "Un track que lleva mas de 120 s en una zona sin alejarse 80 px del punto de entrada produce exactamente un finding LOITERING"
    - "Un track que se desplaza a mas de 350 px/s durante 1 s produce exactamente un finding RUNNING"
    - "Cinco tracks simultaneos en el mismo frame producen exactamente un finding CROWD"
    - "Repetir 100 frames con la condicion activa sigue produciendo un solo finding (latch por episodio)"
    - "Cada finding lleva las magnitudes que lo justifican, con duration_s como nombre literal"
    - "El estado del analizador se mantiene acotado tras 10.000 tracks efimeros, con y sin llamar a prune()"
  artifacts:
    - path: "backend/perception/behavior.py"
      provides: "BehaviorKind, BehaviorFinding, BehaviorAnalyzer (dominio puro)"
      exports: ["BehaviorKind", "BehaviorFinding", "BehaviorAnalyzer", "REARM_RATIO"]
      min_lines: 200
    - path: "tests/test_behavior_analyzer.py"
      provides: "Tests de dominio: 4 reglas, 4 latches, payload y trayectorias sinteticas"
      min_lines: 200
    - path: "tests/test_memory_bounds.py"
      provides: "TEST_behavior_state_bounded / TEST_behavior_state_bounded_without_prune"
      contains: "TEST_behavior_state_bounded"
  key_links:
    - from: "tests/test_behavior_analyzer.py"
      to: "backend/perception/behavior.py"
      via: "import directo del dominio, sin bus ni asyncio"
      pattern: "from backend.perception.behavior import"
    - from: "BehaviorAnalyzer.analyze"
      to: "BehaviorAnalyzer._enforce_cap"
      via: "camino de escritura (no solo prune)"
      pattern: "_enforce_cap\\(\\)"
---

<objective>
Crear `backend/perception/behavior.py`: el dominio puro que decide LOITERING, RUNNING,
IMMOBILE y CROWD a partir de las posiciones del frame, con estado O(1) por track,
latch por episodio para los cuatro comportamientos y doble guarda de expiracion.

Purpose: es el corazon de la fase. Los criterios 1, 2, 3 y 4 del ROADMAP se cierran
integramente aqui, sin tocar el pipeline, porque el analizador es testeable en
aislamiento con reloj inyectado y trayectorias sinteticas (patron de las Fases 24/25).
Output: `backend/perception/behavior.py` + `tests/test_behavior_analyzer.py` +
dos tests de cota en `tests/test_memory_bounds.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/26-an-lisis-de-comportamiento/26-CONTEXT.md
@.planning/phases/26-an-lisis-de-comportamiento/26-RESEARCH.md
@.planning/phases/26-an-lisis-de-comportamiento/26-PATTERNS.md
@backend/perception/reid/gallery.py
@backend/perception/face/identity.py

<interfaces>
<!-- Contratos que el ejecutor DEBE producir. No explorar el codebase para deducirlos. -->

```python
# backend/perception/behavior.py — contrato completo de este plan

REARM_RATIO = 0.8   # histeresis de re-armado para umbrales numericos (RUNNING, CROWD)

class BehaviorKind(str, Enum):
    LOITERING = "loitering"
    RUNNING = "running"
    IMMOBILE = "immobile"
    CROWD = "crowd"

@dataclass
class BehaviorFinding:
    kind: BehaviorKind
    track_id: int | None = None            # None solo para CROWD (evento de escena)
    zone_id: str | None = None             # LOITERING: None = escena implicita (D-02)
    duration_s: float | None = None        # nombre OBLIGATORIO (rules.py:88-91)
    net_displacement_px: float | None = None
    speed_px_s: float | None = None
    track_count: int | None = None
    def magnitudes(self) -> dict[str, float | int]: ...

class BehaviorAnalyzer:
    def __init__(
        self,
        loiter_secs: float = 120.0,
        loiter_radius_px: float = 80.0,
        loiter_require_zone: bool = False,
        run_speed_px_s: float = 350.0,
        run_window_secs: float = 1.0,
        immobile_secs: float = 60.0,
        immobile_radius_px: float = 20.0,
        crowd_threshold: int = 5,
        max_tracks: int = 256,
        state_ttl: float = 30.0,
    ) -> None: ...

    def analyze(
        self,
        centroids: dict[int, tuple[float, float]],
        zone_membership: dict[str, set[int]],
        histories: dict[int, Sequence[tuple[float, float, float]]],
        now: float,
    ) -> list[BehaviorFinding]: ...

    def prune(self, now: float, frame_ids: set[int]) -> None: ...
    def _enforce_cap(self) -> None: ...
```

<!-- El consumidor de estos contratos es EventEngine.emit_behavior (plan 26-03),
     que hace `payload=finding.magnitudes()` y `track_id=finding.track_id`. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Contratos, estado O(1) y doble guarda de expiracion en behavior.py</name>

  <read_first>
    - `backend/perception/reid/gallery.py` COMPLETO (129 lineas utiles) — molde del modulo:
      docstring de pureza (1-14), `_GalleryEntry` con un comentario por campo (23-31),
      `__init__` con umbrales inyectados y un solo dict de estado (33-56),
      `prune()` + `_enforce_cap()` (129-158). Se copia la ESTRUCTURA literal.
    - `backend/perception/face/identity.py:29-45` — `IdentityTransition`, el molde exacto
      de `BehaviorFinding` (campos obligatorios primero, opcionales con default despues,
      comentario inline solo en el campo no obvio, docstring que explica por que NO
      devuelve un `Event`).
    - `.planning/phases/26-an-lisis-de-comportamiento/26-PATTERNS.md` seccion
      "`backend/perception/behavior.py` (CREAR)" — los excerpts 1..5 con los que
      comparar el resultado.
  </read_first>

  <files>backend/perception/behavior.py</files>

  <behavior>
    - `BehaviorFinding(kind=BehaviorKind.IMMOBILE, track_id=7, duration_s=61.0, net_displacement_px=3.2).magnitudes()` devuelve exactamente `{"duration_s": 61.0, "net_displacement_px": 3.2}` (las claves con valor `None` se omiten).
    - `BehaviorFinding(kind=BehaviorKind.CROWD, track_count=5).magnitudes()` devuelve exactamente `{"track_count": 5}`.
    - Tras 10.000 tracks efimeros con `prune(now, frame_ids={tid})` en cada iteracion, `len(analyzer._aggs) <= 256`.
    - Tras 10.000 tracks efimeros SIN llamar nunca a `prune()`, `len(analyzer._aggs) <= 256` (la cota dura actua desde el camino de escritura).
    - `import backend.perception.behavior` no importa `time` ni `backend.events` ni `numpy`.
  </behavior>

  <action>
Crear `backend/perception/behavior.py` con imports SOLO de stdlib:
`from __future__ import annotations`, `import math`, `from collections.abc import Sequence`,
`from dataclasses import dataclass`, `from enum import Enum`.
PROHIBIDO: `import time` (rompe el reloj inyectado), `import numpy` (aqui son 2
coordenadas, no vectores 512D — RESEARCH § Standard Stack), cualquier import de
`backend.events` (la direccion de la dependencia es `events/ -> perception/`, nunca al
reves — es lo que hace que `BehaviorFinding` no pueda ser un `Event`, RESEARCH D-3).

1. **Docstring de modulo** calcado de `gallery.py:1-14`, con estas cuatro afirmaciones
   literales: (a) "Dominio puro: no importa `time`, no arranca hilos, no hace I/O y no
   construye eventos"; (b) "Reloj inyectado: todos los metodos que dependen del reloj lo
   reciben como parametro `now: float` (monotonico)"; (c) "Un solo hilo lo llama
   (DetectionWorker._loop), por eso no hay lock"; (d) bloque "Fuera de alcance aqui:
   construir Event, conocer camera_id, conocer el reloj de pared, decidir la severidad y
   emitir ZONE_ENTERED/ZONE_EXITED (los emite EventEngine.process_zone desde la Fase 19;
   reimplementarlos aqui duplicaria cada evento de zona — 26-CONTEXT.md H-2)". Anotar
   tambien que la firma de SPEC_v2.md §5.7 (`analyze(...) -> list[Event]`) queda
   corregida a `-> list[BehaviorFinding]` por 26-RESEARCH.md D-3.

2. **Constante de modulo:**
   `REARM_RATIO = 0.8  # histeresis de re-armado: un umbral numerico oscilaria en el borde`

3. **`class BehaviorKind(str, Enum)`** con exactamente 4 miembros:
   `LOITERING = "loitering"`, `RUNNING = "running"`, `IMMOBILE = "immobile"`,
   `CROWD = "crowd"`. NO añadir `DIRECTION_CHANGED` ni ningun otro (fuera de alcance,
   ademas no existe en `EventType` y `tests/test_event_types.py:12` congela el catalogo).

4. **`@dataclass class BehaviorFinding`** con los campos y defaults EXACTOS del bloque
   `<interfaces>`. Docstring calcado de `IdentityTransition` (identity.py:29-35):
   "El analizador devuelve esto, NO un Event: perception/ no conoce camera_id ni el
   reloj de pared. EventEngine.emit_behavior() lo traduce a Event (26-RESEARCH.md D-3)."
   Comentarios inline obligatorios en `track_id` ("None solo para CROWD: evento de
   escena"), `zone_id` ("None = escena implicita cuando el track no esta en ninguna
   zona, D-02") y `duration_s` ("nombre OBLIGATORIO: backend/events/rules.py:88-91 lee
   literalmente payload['duration_s'] para resolver duration_gte").
   Metodo `magnitudes(self) -> dict[str, float | int]`: devuelve un dict con las claves
   `duration_s`, `net_displacement_px`, `speed_px_s`, `track_count` **omitiendo las que
   valen None**, en ese orden. Es lo que `EventEngine.emit_behavior` pasara como
   `payload=` (BEH-05).

5. **`@dataclass class _TrackAgg`** — estado por track, ~7 floats + 2 bool, con un
   comentario por campo al estilo de `_GalleryEntry` (gallery.py:23-31):
   `imm_anchor_t: float` (ancla temporal del episodio de inmovilidad),
   `imm_min_x: float`, `imm_max_x: float`, `imm_min_y: float`, `imm_max_y: float`
   (caja envolvente de las posiciones desde el ancla; su `span` ES el desplazamiento
   real, mientras que la distancia al ancla permitiria un diametro de 2R),
   `last_seen: float` (base del TTL y del LRU de la cota dura),
   `imm_latched: bool = False`, `run_latched: bool = False` (un evento por episodio).

6. **`@dataclass class _ZoneAgg`** — ancla de permanencia por `(track, zona)` (D-04:
   con zonas solapadas se emite un LOITERING por zona):
   `anchor_t: float`, `anchor_x: float`, `anchor_y: float`, `last_seen: float`,
   `latched: bool = False`.

7. **`class BehaviorAnalyzer`** con el `__init__` EXACTO del bloque `<interfaces>`
   (todos los defaults son los locked de SPEC_v2.md §5.7 recogidos en 26-CONTEXT.md).
   Docstring con las mismas 3 afirmaciones de pureza del modulo mas: "No aplica politica
   de producto: LOITERING e IMMOBILE coexisten (D-03); el aislamiento que exige el
   criterio 2 se consigue en las trayectorias de test, no con supresion mutua aqui."
   Estado, en tres atributos y ninguno mas:
   `self._aggs: dict[int, _TrackAgg] = {}`,
   `self._loiter: dict[tuple[int, str | None], _ZoneAgg] = {}`,
   `self._crowd_latched = False`  (latch de escena, analogo 1:1 de
   `EventEngine._camera_offline`, engine.py:38).

8. **`analyze(...)`**: en este task solo el esqueleto que garantiza la cota — firma
   completa del bloque `<interfaces>`, creacion/refresco de `_TrackAgg` por cada
   `track_id` de `centroids` (creandolo con `imm_anchor_t=now`, caja envolvente
   colapsada en `(x, y)` y `last_seen=now`), llamada final a `self._enforce_cap()`
   **desde este camino de escritura** y `return findings` (lista vacia por ahora).
   Las cuatro reglas las añade el Task 2.

9. **`prune(self, now: float, frame_ids: set[int]) -> None`** — copiar
   `gallery.py:129-143` cambiando `_entries` por `_aggs` e `inherit_window` por
   `state_ttl`, y aplicando lo mismo a `_loiter` (cuyas claves son tuplas: el track de
   la clave es `key[0]`). Docstring con la estructura "Guarda 1 (TTL) / Guarda 2 (cota
   dura)" de `gallery.py:175-181`. Termina con `self._enforce_cap()`.

10. **`_enforce_cap(self) -> None`** — copiar `gallery.py:145-158` literalmente
    (`max_tracks` en vez de `max_entries`, LRU por `last_seen`), aplicado a `_aggs` y a
    `_loiter` por separado. Docstring que cite el motivo textual del analogo: "Actua
    aunque nadie llame a prune() a tiempo" (seguro de vida de la Fase 22). Al eliminar
    un track de `_aggs`, eliminar tambien sus claves en `_loiter`.
  </action>

  <verify>
    <automated>F:/Documentos/IA/Proyecto_Camara/.venv/Scripts/python.exe -c "import backend.perception.behavior as b, inspect, sys; src=inspect.getsource(b); assert 'import time' not in src and 'backend.events' not in src and 'numpy' not in src, 'import prohibido'; f=b.BehaviorFinding(kind=b.BehaviorKind.IMMOBILE, track_id=7, duration_s=61.0, net_displacement_px=3.2); assert f.magnitudes()=={'duration_s':61.0,'net_displacement_px':3.2}, f.magnitudes(); a=b.BehaviorAnalyzer(max_tracks=256); [a.analyze({t:(1.0,1.0)}, {}, {}, float(t)) for t in range(10000)]; assert len(a._aggs)<=256, len(a._aggs); print('OK')"</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "^import time" backend/perception/behavior.py` devuelve 0.
    - `grep -c "backend.events" backend/perception/behavior.py` devuelve 0.
    - `grep -c "numpy" backend/perception/behavior.py` devuelve 0.
    - `grep -n "class BehaviorKind(str, Enum)" backend/perception/behavior.py` acierta.
    - `grep -n "REARM_RATIO = 0.8" backend/perception/behavior.py` acierta.
    - `grep -n "def magnitudes" backend/perception/behavior.py` acierta.
    - `grep -n "_loiter: dict\[tuple\[int, str | None\], _ZoneAgg\]" backend/perception/behavior.py` acierta.
    - `grep -n "self._crowd_latched" backend/perception/behavior.py` acierta.
    - `grep -c "_enforce_cap()" backend/perception/behavior.py` devuelve >= 3 (definicion + llamada desde `analyze` + llamada desde `prune`).
    - `grep -n "duration_s" backend/perception/behavior.py` acierta y el comentario cita `rules.py:88-91`.
    - El comando de `<verify>` imprime `OK` con codigo de salida 0.
  </acceptance_criteria>

  <done>El modulo importa, `BehaviorFinding.magnitudes()` omite los `None`, y `_aggs` queda acotado a 256 entradas tras 10.000 tracks efimeros sin llamar nunca a `prune()`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Las cuatro reglas con latch por episodio dentro de analyze()</name>

  <read_first>
    - `backend/perception/behavior.py` (el fichero del Task 1, completo).
    - `.planning/phases/26-an-lisis-de-comportamiento/26-RESEARCH.md` § "Pattern 2 —
      Agregado incremental con ancla" (lineas 808-838), § "Pattern 3 — Ventana corta
      leida del centroid_history existente" (839-864) y § "Pattern 4 — Latch de estado
      con re-armado" (865-898). Los tres traen el codigo a copiar.
    - `backend/events/engine.py:144-154` — `camera_offline`/`camera_recovered`, el latch
      literal del que sale el patron (guarda al entrar, flag a True al emitir).
  </read_first>

  <files>backend/perception/behavior.py</files>

  <behavior>
    - Track quieto en (100,100) con jitter de ±5 px durante 70 s: exactamente 1 finding IMMOBILE, con `duration_s > 60` y `net_displacement_px <= 20`.
    - Ese mismo track durante 200 frames mas: 0 findings adicionales (latch).
    - Track que salta a (400,400) tras estar inmovil: el ancla se resetea y `imm_latched` vuelve a False; 60 s despues vuelve a emitirse IMMOBILE.
    - Track dentro de la zona "z1" 130 s derivando ±30 px: exactamente 1 finding LOITERING con `zone_id="z1"`, `duration_s > 120`, `net_displacement_px < 80`.
    - Sin zonas configuradas (`zone_membership={}`) y `loiter_require_zone=False`: el mismo track emite LOITERING con `zone_id=None` (D-02).
    - Con `loiter_require_zone=True` y sin zonas: 0 findings LOITERING.
    - Track en dos zonas solapadas z1 y z2 durante 130 s: 2 findings LOITERING, uno por zona (D-04).
    - Track con historial rectilineo a 400 px/s durante 2 s: exactamente 1 finding RUNNING con `speed_px_s > 350` y `duration_s == run_window_secs`.
    - Historial con menos de `run_window_secs` de antiguedad: 0 findings RUNNING (guarda de historia insuficiente).
    - Historial de jitter puro (±5 px sin desplazamiento neto): 0 findings RUNNING.
    - 5 tracks en el frame con `crowd_threshold=5`: exactamente 1 finding CROWD con `track_count=5`; repetir el frame 100 veces sigue dando 1.
    - Bajar a 3 tracks (< 5*0.8) y volver a 5: se emite un segundo CROWD.
  </behavior>

  <action>
Completar `analyze()`. Orden de recorrido FIJO para que los tests sean deterministas:
`for tid in sorted(centroids):` -> IMMOBILE, luego RUNNING, luego LOITERING por cada
zona en `sorted(...)`; el finding de CROWD se añade al FINAL de la lista.

**IMMOBILE (BEH-02) — agregado incremental con caja envolvente (RESEARCH Pattern 2):**
```python
agg.imm_min_x = min(agg.imm_min_x, x); agg.imm_max_x = max(agg.imm_max_x, x)
agg.imm_min_y = min(agg.imm_min_y, y); agg.imm_max_y = max(agg.imm_max_y, y)
span = max(agg.imm_max_x - agg.imm_min_x, agg.imm_max_y - agg.imm_min_y)
if span > self._immobile_radius_px:
    agg.imm_anchor_t = now
    agg.imm_min_x = agg.imm_max_x = x
    agg.imm_min_y = agg.imm_max_y = y
    agg.imm_latched = False              # re-armado: el episodio empieza de cero
else:
    dur = now - agg.imm_anchor_t
    if not agg.imm_latched and dur > self._immobile_secs:
        agg.imm_latched = True
        findings.append(BehaviorFinding(kind=BehaviorKind.IMMOBILE, track_id=tid,
                                        duration_s=round(dur, 3),
                                        net_displacement_px=round(span, 3)))
```
Usar la caja envolvente y NO la distancia al ancla: con la distancia la garantia seria
"todos los puntos a <= R del ancla", que permite un diametro real de 2R; `span` ES la
extension real del recorrido, que es lo que dice la regla "desplazamiento < 20 px".

**RUNNING (BEH-02) — ventana de `run_window_secs` sobre el historial recibido
(RESEARCH Pattern 3).** Añadir la funcion de modulo `_window_speed(history, now, window_s)
-> float | None` copiada literalmente de RESEARCH lineas 845-859: recorre el historial
`reversed(...)` hasta la primera muestra con `t_now - t >= window_s`, devuelve
`math.hypot(dx, dy) / dt` (desplazamiento NETO, nunca longitud de camino: la longitud de
camino integra el jitter del bbox y daria velocidad creciente con el FPS para un track
parado, Pitfall 6), y devuelve `None` si el track no tiene `window_s` de historia
(sin esa guarda, un track recien creado con 2 muestras separadas 20 ms daria miles de
px/s). El corte es SIEMPRE por el `t` de la tupla, jamas por `len(history)` ni por
indice: 150 muestras son 12,5 s o 50 s segun el escalon de `AdaptiveRate` (Pitfall 1).
Latch con histeresis:
```python
speed = _window_speed(histories.get(tid), now, self._run_window_secs)
if speed is not None:
    if not agg.run_latched and speed > self._run_speed_px_s:
        agg.run_latched = True
        findings.append(BehaviorFinding(kind=BehaviorKind.RUNNING, track_id=tid,
                                        speed_px_s=round(speed, 3),
                                        duration_s=round(self._run_window_secs, 3)))
    elif agg.run_latched and speed < self._run_speed_px_s * REARM_RATIO:
        agg.run_latched = False
```

**LOITERING (BEH-01) — ancla por `(track, zona)` (D-04):**
Calcular primero las zonas del track en este frame:
`zones_of_track = sorted(zid for zid, ids in zone_membership.items() if tid in ids)`.
Si esa lista queda vacia: si `self._loiter_require_zone` es True se salta LOITERING para
ese track; si es False se usa `[None]` (escena implicita, D-02 — una instalacion limpia
tiene cero zonas porque `get_zones()` lee de BD y no hay seed, y sin este fallback el
evento no se emitiria nunca). Por cada `zid`:
```python
z = self._loiter.get((tid, zid))
if z is None:
    z = _ZoneAgg(anchor_t=now, anchor_x=x, anchor_y=y, last_seen=now)
    self._loiter[(tid, zid)] = z
z.last_seen = now
disp = math.hypot(x - z.anchor_x, y - z.anchor_y)
if disp >= self._loiter_radius_px:
    z.anchor_t, z.anchor_x, z.anchor_y = now, x, y
    z.latched = False                    # se alejo: episodio nuevo
elif not z.latched and now - z.anchor_t > self._loiter_secs:
    z.latched = True
    findings.append(BehaviorFinding(kind=BehaviorKind.LOITERING, track_id=tid,
                                    zone_id=zid, duration_s=round(now - z.anchor_t, 3),
                                    net_displacement_px=round(disp, 3)))
```
Al terminar el bucle de tracks, borrar las claves `(tid, zid)` de `self._loiter` cuyo
`tid` SI esta en `centroids` pero cuyo `zid` no aparece en `zones_of_track` de este
frame (el track salio de esa zona: la re-entrada debe reiniciar el ancla). Las claves de
tracks que ya no estan en `centroids` las limpia `prune()` por TTL.

**CROWD (BEH-03) — latch de escena, analogo 1:1 de `camera_offline` (engine.py:144-154):**
```python
count = len(centroids)
if not self._crowd_latched and count >= self._crowd_threshold:
    self._crowd_latched = True
    findings.append(BehaviorFinding(kind=BehaviorKind.CROWD, track_count=count))
elif self._crowd_latched and count < self._crowd_threshold * REARM_RATIO:
    self._crowd_latched = False          # re-armado SILENCIOSO: CROWD_CLEARED no existe
```
`track_id` queda a `None` a proposito: es un evento de escena, y asi el debounce de
`rules.yaml` cae en la clave global por camara (rules.py:147-150), que es lo correcto.

**Los cuatro latches son obligatorios, no solo el de CROWD.** LOITERING, RUNNING e
IMMOBILE son condiciones continuas: una persona parada 10 min genera ~4.800 eventos
IMMOBILE a 8 FPS, cada uno escrito en SQLite y difundido por WebSocket. El docstring del
propio `EventEngine` (engine.py:3-4) llama a esto "the point where v1 failed
conceptually". `debounce_secs` de rules.yaml NO sirve como sustituto: actua despues de
persistir y difundir.

NO añadir supresion mutua entre LOITERING e IMMOBILE: coexisten (D-03).
NO llamar a `time.monotonic()` en ningun punto.
  </action>

  <verify>
    <automated>F:/Documentos/IA/Proyecto_Camara/.venv/Scripts/python.exe -c "from backend.perception.behavior import BehaviorAnalyzer, BehaviorKind as K; a=BehaviorAnalyzer(); f=[]; [f.extend(a.analyze({1:(100.0+ (i%3), 100.0)}, {}, {}, i*0.125)) for i in range(600)]; imm=[x for x in f if x.kind is K.IMMOBILE]; assert len(imm)==1, len(imm); assert imm[0].duration_s>60 and imm[0].net_displacement_px<=20; b=BehaviorAnalyzer(crowd_threshold=5); c=[]; [c.extend(b.analyze({t:(1.0,1.0) for t in range(5)}, {}, {}, float(i))) for i in range(100)]; cr=[x for x in c if x.kind is K.CROWD]; assert len(cr)==1 and cr[0].track_count==5, cr; print('OK')"</automated>
  </verify>

  <acceptance_criteria>
    - `grep -n "def _window_speed" backend/perception/behavior.py` acierta.
    - `grep -n "REARM_RATIO" backend/perception/behavior.py` aparece al menos 3 veces (definicion + RUNNING + CROWD).
    - `grep -c "imm_latched" backend/perception/behavior.py` devuelve >= 4 (declaracion, re-armado, guarda, armado).
    - `grep -c "latched" backend/perception/behavior.py` devuelve >= 10 — los CUATRO comportamientos tienen latch.
    - `grep -n "len(history)\|history\[-[0-9]\|int(secs \* fps)" backend/perception/behavior.py` NO devuelve nada (todo se mide en segundos, Pitfall 1).
    - `grep -n "loiter_require_zone" backend/perception/behavior.py` acierta.
    - `grep -n "self._loiter\[(tid, zid)\]\|self._loiter\[(track_id, zid)\]" backend/perception/behavior.py` acierta (ancla por (track, zona), D-04).
    - `grep -n "time.monotonic" backend/perception/behavior.py` NO devuelve nada.
    - El comando de `<verify>` imprime `OK` con codigo de salida 0.
  </acceptance_criteria>

  <done>Los cuatro comportamientos emiten en el flanco de subida y una sola vez por episodio; el jitter no dispara RUNNING; sin zonas configuradas LOITERING sale con `zone_id=None`.</done>
</task>

<task type="auto">
  <name>Task 3: Tests de dominio y de cota (criterios 1, 2, 3 y 4)</name>

  <read_first>
    - `backend/perception/behavior.py` completo (resultado de los Tasks 1 y 2).
    - `tests/test_track_gallery.py` — estilo de test de dominio puro del repo (instanciar,
      pasar datos y un `now` inventado, comprobar el veredicto; sin hilos, sin bus).
    - `tests/test_memory_bounds.py:286-312` — `TEST_track_gallery_bounded` y
      `TEST_track_gallery_bounded_without_prune`: el par con/sin prune es EL molde a
      replicar, incluida la banda de comentario `# ─── ... ───` que explica el motivo.
    - `pytest.ini` — `python_functions = TEST_*`.
  </read_first>

  <files>tests/test_behavior_analyzer.py, tests/test_memory_bounds.py</files>

  <action>
**Convencion de nombres OBLIGATORIA:** todas las funciones de test se llaman `TEST_*`.
`pytest.ini` fija `python_functions = TEST_*`; en Windows el matching es
case-insensitive y un `test_*` pareceria funcionar, pero en el CI Linux dejaria de
recogerse en silencio.

Crear `tests/test_behavior_analyzer.py` con un helper LOCAL al fichero (el repo mantiene
los helpers junto a sus tests: `_tracked` en `test_detection_worker.py:27`,
`_fake_tracked` en `test_memory_bounds.py:34`; NO tocar `conftest.py`):
```python
def _walk(x0, y0, vx, vy, secs, fps=8.0):
    """Trayectoria rectilinea: devuelve [(t, x, y), ...] a fps constante."""
    n = int(secs * fps)
    return [(i / fps, x0 + vx * i / fps, y0 + vy * i / fps) for i in range(n)]
```

Tests a escribir, agrupados por la palabra clave que usa el mapa de validacion
(`-k loiter`, `-k running`, `-k immobile`, `-k crowd`, `-k latch`, `-k payload`,
`-k trajectory`):

1. `TEST_immobile_after_threshold` — 70 s de jitter ±5 px: 1 IMMOBILE, `duration_s > 60`.
2. `TEST_immobile_resets_when_track_moves` — salto de 400 px: el ancla se resetea y no
   se emite hasta 60 s despues del salto.
3. `TEST_immobile_latch_emits_once_per_episode` — 600 frames con la condicion activa:
   `len([f for f in findings if f.kind is BehaviorKind.IMMOBILE]) == 1`.
4. `TEST_loiter_in_zone_after_threshold` — 130 s dentro de "z1" con deriva ±30 px:
   1 LOITERING con `zone_id == "z1"`.
5. `TEST_loiter_falls_back_to_implicit_scene_without_zones` — `zone_membership={}` y
   `loiter_require_zone=False`: 1 LOITERING con `zone_id is None` (D-02).
6. `TEST_loiter_requires_zone_when_configured` — mismo escenario con
   `loiter_require_zone=True`: 0 findings.
7. `TEST_loiter_emits_once_per_overlapping_zone` — track en z1 y z2 durante 130 s:
   `{f.zone_id for f in loiterings} == {"z1", "z2"}` y `len(loiterings) == 2` (D-04).
8. `TEST_loiter_latch_emits_once_per_episode`.
9. `TEST_running_over_speed_threshold` — historial `_walk(0, 0, 400, 0, 2.0)`:
   1 RUNNING con `speed_px_s > 350`.
10. `TEST_running_ignores_insufficient_history` — historial de 0,2 s: 0 findings.
11. `TEST_running_ignores_jitter` — historial de jitter ±5 px sin desplazamiento neto:
    0 findings (Pitfall 6).
12. `TEST_running_latch_rearms_below_hysteresis` — velocidad alta -> 1 RUNNING; caida por
    debajo de `350 * 0.8` -> re-armado; subida de nuevo -> 2º RUNNING.
13. `TEST_crowd_detected_at_threshold` — 5 tracks: 1 CROWD con `track_count == 5`.
14. `TEST_crowd_latch_emits_once_while_crowded` — 100 frames con 5 tracks: 1 CROWD.
15. `TEST_payload_magnitudes_use_exact_keys` — comprobar los NOMBRES exactos de
    `finding.magnitudes()` para los cuatro comportamientos:
    IMMOBILE -> `{"duration_s", "net_displacement_px"}`,
    LOITERING -> `{"duration_s", "net_displacement_px"}`,
    RUNNING -> `{"speed_px_s", "duration_s"}`,
    CROWD -> `{"track_count"}`. El test debe afirmar sobre `set(...keys())` y contener un
    assert explicito de que la clave es `duration_s` y no `duration`/`dwell_s`, porque
    `backend/events/rules.py:88-91` lee literalmente `payload["duration_s"]` y con otro
    nombre `duration_gte` dejaria de disparar EN SILENCIO (Pitfall 8).
16. **Criterio 2 — cuatro tests de trayectoria con igualdad de CONJUNTO** (los otros dos
    eventos del criterio, ZONE_ENTERED/ZONE_EXITED, se cubren en el plan 26-03 sobre
    `EventEngine.process_zone`, que es su unico dueño desde la Fase 19):
    `TEST_trajectory_immobile_emits_only_immobile`,
    `TEST_trajectory_loiter_emits_only_loitering`,
    `TEST_trajectory_running_emits_only_running`,
    `TEST_trajectory_crowd_emits_only_crowd`.
    Cada uno DEBE afirmar
    `assert {f.kind for f in findings} == {BehaviorKind.X}` — igualdad de conjunto, nunca
    `assert BehaviorKind.X in kinds`: con la forma de "presencia" el test pasaria aunque
    la trayectoria emitiera ademas un evento espurio, que es exactamente lo que el
    criterio 2 prohibe. El aislamiento se consigue DISEÑANDO la trayectoria (la de
    IMMOBILE dura menos de `loiter_secs` o corre con `loiter_require_zone=True`; la de
    LOITERING deriva mas de `immobile_radius_px`; la de CROWD usa tracks que se mueven lo
    justo para no ser inmoviles ni corredores), NO metiendo supresion mutua en el dominio
    (D-03).

Ampliar `tests/test_memory_bounds.py` con DOS tests (el par con/sin prune es lo que
demuestra la doble guarda), cada uno precedido de su banda de comentario al estilo de
las lineas 286-292:
- `TEST_behavior_state_bounded` — 10.000 tracks efimeros con
  `analyze({tid: (x, y)}, {}, {}, now)` + `prune(now, frame_ids={tid})` por iteracion;
  `assert len(analyzer._aggs) <= 256` y `assert len(analyzer._loiter) <= 256`.
- `TEST_behavior_state_bounded_without_prune` — el mismo bucle SIN llamar nunca a
  `prune()`; mismos asserts. Motivo a escribir en el comentario: ByteTrack asigna ids
  monotonamente crecientes y nunca los reutiliza, asi que sin doble guarda un proceso
  24/7 haria crecer el estado sin limite (criterio 4 del ROADMAP, invariante de la
  Fase 22). Acceder a los atributos privados directamente es la convencion aceptada en
  este fichero.
  </action>

  <verify>
    <automated>F:/Documentos/IA/Proyecto_Camara/.venv/Scripts/python.exe -m pytest tests/test_behavior_analyzer.py "tests/test_memory_bounds.py::TEST_behavior_state_bounded" "tests/test_memory_bounds.py::TEST_behavior_state_bounded_without_prune" -q</automated>
  </verify>

  <acceptance_criteria>
    - `pytest tests/test_behavior_analyzer.py -q` sale en verde con al menos 16 tests recogidos.
    - `grep -c "^def test_" tests/test_behavior_analyzer.py` devuelve 0 (todos son `TEST_*`).
    - `grep -c "== {BehaviorKind\." tests/test_behavior_analyzer.py` devuelve >= 4 (igualdad de conjunto en los 4 tests de trayectoria).
    - `grep -n "in kinds\b" tests/test_behavior_analyzer.py` NO devuelve nada en los tests `TEST_trajectory_*`.
    - `grep -n "duration_s" tests/test_behavior_analyzer.py` acierta dentro de `TEST_payload_magnitudes_use_exact_keys`.
    - `grep -n "TEST_behavior_state_bounded_without_prune" tests/test_memory_bounds.py` acierta.
    - `pytest tests/test_memory_bounds.py -q` sigue verde (no se rompe ningun test previo).
  </acceptance_criteria>

  <done>16+ tests `TEST_*` verdes en `tests/test_behavior_analyzer.py`, los cuatro tests de trayectoria afirman igualdad de conjunto, y los dos tests de cota pasan con y sin `prune()`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pipeline -> dominio | Posiciones de tracks producidas por YOLO/ByteTrack entran en `analyze()`; entrada no adversaria pero si ruidosa y de cardinalidad no acotada |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-01 | Denial of Service | `BehaviorAnalyzer.analyze` | mitigate | Latch por episodio en los 4 comportamientos: sin el, una persona parada 10 min genera ~4.800 eventos IMMOBILE a 8 FPS, cada uno una fila en SQLite y un mensaje WebSocket (auto-DoS). Verificado por `TEST_*_latch_emits_once_per_episode` |
| T-26-02 | Denial of Service | `BehaviorAnalyzer._aggs` / `_loiter` | mitigate | Doble guarda TTL (`state_ttl=30.0` en `prune`) + cota dura (`max_tracks=256` en `_enforce_cap`, invocado tambien desde `analyze`). Verificado por el par `TEST_behavior_state_bounded` / `..._without_prune` |
| T-26-03 | Information Disclosure | `BehaviorFinding` | accept | El finding solo lleva numeros y `track_id`; sin `person_name`, sin recortes, sin PII. No se añade ninguna |
| T-26-04 | Tampering | umbrales del constructor | transfer | La validacion de rango vive en `Settings.validate_behavior_params` (plan 26-02); el dominio acepta lo que le den, igual que `TrackGallery` |
</threat_model>

<verification>
```bash
F:/Documentos/IA/Proyecto_Camara/.venv/Scripts/python.exe -m pytest tests/test_behavior_analyzer.py tests/test_memory_bounds.py tests/test_architecture.py -q
```
`test_architecture.py` debe seguir verde: `behavior.py` no importa `fastapi` ni arranca hilos.
</verification>

<success_criteria>
- `backend/perception/behavior.py` existe, no importa `time`/`numpy`/`backend.events` y expone `BehaviorKind`, `BehaviorFinding`, `BehaviorAnalyzer`.
- Los cuatro comportamientos emiten una sola vez por episodio y se re-arman cuando la condicion deja de cumplirse.
- LOITERING funciona sin zonas configuradas (`zone_id=None`, D-02) y emite uno por zona con zonas solapadas (D-04).
- `finding.magnitudes()` usa los nombres `duration_s`, `net_displacement_px`, `speed_px_s`, `track_count`.
- 16+ tests `TEST_*` verdes y el estado acotado a 256 entradas con y sin `prune()`.
</success_criteria>

<output>
Al terminar, crear `.planning/phases/26-an-lisis-de-comportamiento/26-01-SUMMARY.md`.
</output>
