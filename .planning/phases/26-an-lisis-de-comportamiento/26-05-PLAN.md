---
phase: 26-an-lisis-de-comportamiento
plan: 05
type: execute
wave: 4
depends_on: [26-04]
files_modified:
  - tests/test_rule_engine.py
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
autonomous: false
requirements: [BEH-01, BEH-02, BEH-03, BEH-04, BEH-05]

must_haves:
  truths:
    - "Un rules.yaml con los cuatro eventos de comportamiento carga con load_rules sin errores y sin tocar el RuleEngine"
    - "Una regla LOITERING con duration_gte: 120 dispara sobre un evento con payload duration_s=130 y no dispara con 90"
    - "La suite completa esta verde con la linea base 413 mas los tests nuevos"
    - "Los 5 criterios de exito del ROADMAP estan trazados a un comando pytest que pasa"
    - "BEH-01..BEH-05 quedan marcados [x] en REQUIREMENTS.md"
  artifacts:
    - path: "tests/test_rule_engine.py"
      provides: "Criterio 5 por el camino real: YAML en tmp_path + load_rules + evaluate"
      contains: "TEST_behavior_events_usable_as_when_event"
    - path: ".planning/phases/26-an-lisis-de-comportamiento/26-05-SUMMARY.md"
      provides: "Tabla criterio -> comando -> resultado"
  key_links:
    - from: "tests/test_rule_engine.py"
      to: "backend/events/rules.py"
      via: "load_rules sobre un fichero real, no Rule.model_validate"
      pattern: "load_rules\\(str\\(path\\)\\)"
---

<objective>
Cerrar el criterio 5 (los eventos de comportamiento son usables como `when.event` en
`rules.yaml` sin cambios en el `RuleEngine`) con un test que recorra el camino real
—fichero YAML + `load_rules` + `evaluate`— y ejecutar la puerta de fase: suite completa
verde y trazabilidad de los 5 criterios del ROADMAP a comandos que pasan.

Purpose: el criterio 5 es "cero codigo" (`When.event: EventType` valida contra el enum
completo, rules.py:24-25) pero solo se sostiene si `duration_gte` encuentra la clave
`duration_s` en el payload — y eso hay que demostrarlo, no asumirlo.
Output: 2-3 tests nuevos + `26-05-SUMMARY.md` + ROADMAP/REQUIREMENTS/STATE al dia.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-an-lisis-de-comportamiento/26-VALIDATION.md
@.planning/phases/26-an-lisis-de-comportamiento/26-04-SUMMARY.md
@backend/events/rules.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Criterio 5 — reglas de comportamiento cargadas desde YAML real</name>

  <read_first>
    - `tests/test_rule_engine.py:13-45` — `SPEC_RULES_YAML`: **ya contiene una regla
      `permanencia_excesiva` con `event: LOITERING` y `duration_gte: 120`** (lineas 40-44).
      El criterio 5 arranca a medias: lo que falta es el test de evaluacion end-to-end.
    - `tests/test_rule_engine.py:60-67` — `TEST_loads_valid_yaml`: el camino real
      (`tmp_path` + `write_text` + `load_rules(str(path))`), que es lo que RESEARCH exige
      en vez de `Rule.model_validate` a secas.
    - `tests/test_rule_engine.py:48-57` — factories `make_event` / `make_rule`.
    - `tests/test_rule_engine.py:94-102` — molde de la parte de evaluacion
      (`await engine.evaluate(event)`).
    - `backend/events/rules.py:24-33` (`class When`), `72-91` (`_matches`, incluido
      `event.payload.get("duration_s")`), `105-120` (`load_rules` desactiva la regla
      invalida sin bloquear el arranque), `147-158` (clave de debounce).
  </read_first>

  <files>tests/test_rule_engine.py</files>

  <behavior>
    - Un YAML con cuatro reglas (`event: LOITERING`, `RUNNING`, `IMMOBILE`, `CROWD_DETECTED`) carga con `load_rules` devolviendo `errors == []` y 4 reglas.
    - `RuleEngine.evaluate(Event(type=LOITERING, payload={"duration_s": 130.0}))` devuelve `["merodeo_prolongado"]` con la regla `duration_gte: 120`.
    - El mismo evento con `payload={"duration_s": 90.0}` no dispara.
    - El mismo evento con `payload={"duration": 130.0}` (nombre equivocado) NO dispara — es la prueba explicita de por que la clave tiene que llamarse `duration_s`.
    - `when.zone: "z1"` filtra un LOITERING emitido con `zone_id="z1"` y no uno con `zone_id=None`.
  </behavior>

  <action>
Añadir a `tests/test_rule_engine.py` (nombres `TEST_*`, todos con "behavior" o el nombre
del evento para que `pytest -k behavior` los recoja):

1. Constante local `BEHAVIOR_RULES_YAML` con `textwrap.dedent`, al estilo de
   `SPEC_RULES_YAML`, con cuatro reglas — una por evento de comportamiento:
```yaml
    version: 1
    rules:
      - name: merodeo_prolongado
        enabled: true
        when:
          event: LOITERING
          duration_gte: 120
        debounce_secs: 300
        actions: [ {type: notify} ]

      - name: carrera_detectada
        when: {event: RUNNING}
        actions: [ {type: log} ]

      - name: inmovilidad_prolongada
        when: {event: IMMOBILE, duration_gte: 60}
        actions: [ {type: log} ]

      - name: aglomeracion
        when: {event: CROWD_DETECTED}
        actions: [ {type: notify} ]
```
   **Este YAML vive SOLO en el test.** NO añadirlo a `config/rules.yaml`: CONTEXT difiere
   explicitamente poblar el fichero de reglas de produccion (que alertas quiere el
   usuario es decision suya, no de esta fase), y ademas
   `scripts/generate_initial_rules.py` lo sobrescribe.

2. `TEST_behavior_events_usable_as_when_event(tmp_path)` — escribe el YAML en `tmp_path`,
   llama a `load_rules(str(path))`, comprueba `errors == []` y
   `{r.name for r in rules} == {"merodeo_prolongado", "carrera_detectada",
   "inmovilidad_prolongada", "aglomeracion"}`. Docstring citando el motivo:
   `When.event` es de tipo `EventType` (rules.py:25), asi que Pydantic valida contra el
   enum COMPLETO y los cuatro tipos, que existen en el catalogo desde la Fase 19, cargan
   sin tocar una linea del `RuleEngine` (criterio 5).

3. `TEST_behavior_duration_gte_reads_duration_s(tmp_path)` — carga el YAML, construye un
   `RuleEngine(rules, registry={})` y hace tres asserts:
   - `await engine.evaluate(make_event(type=EventType.LOITERING, payload={"duration_s": 130.0}))`
     dispara `merodeo_prolongado`;
   - con `payload={"duration_s": 90.0}` no dispara;
   - con `payload={"duration": 130.0}` (clave equivocada a proposito) NO dispara.
   El tercer assert es el que documenta el acoplamiento: `rules.py:88-91` hace
   `event.payload.get("duration_s")` y devuelve `False` si la clave falta, sin error ni
   log — con otro nombre el criterio 5 se cumpliria a medias y en silencio (Pitfall 8).
   Añadir un mensaje de assert explicito diciendolo.

4. `TEST_behavior_zone_filter_uses_first_class_zone_id(tmp_path)` — una regla con
   `when: {event: LOITERING, zone: "z1"}` dispara sobre un evento con `zone_id="z1"` y no
   sobre uno con `zone_id=None` (rules.py:75-76). Demuestra por que `zone_id` va como
   campo de primer nivel del `Event` y no dentro del payload.

Ojo con el debounce: `RuleEngine` cachea por `(rule.name, camera_id, person_id or track_id)`
(rules.py:147-150). Si un test evalua dos eventos de la misma regla y el mismo track
seguidos, el segundo puede quedar silenciado por `debounce_secs`. Usar `track_id`
distintos o reglas sin `debounce_secs` en los tests de evaluacion.
  </action>

  <verify>
    <automated>F:/Documentos/IA/Proyecto_Camara/.venv/Scripts/python.exe -m pytest tests/test_rule_engine.py -q</automated>
  </verify>

  <acceptance_criteria>
    - `pytest tests/test_rule_engine.py -k behavior -q` recoge >= 3 tests y sale verde.
    - `grep -n "load_rules(str(path))" tests/test_rule_engine.py` acierta dentro de los tests nuevos.
    - `grep -n '"duration": 130' tests/test_rule_engine.py` acierta (el assert negativo del nombre equivocado).
    - `git diff --stat config/rules.yaml` esta vacio.
    - `git diff --stat backend/events/rules.py` esta vacio — el criterio 5 se cumple SIN cambios en el `RuleEngine`.
    - `pytest tests/test_rule_engine.py -q` verde entero, sin romper `TEST_loads_valid_yaml`.
  </acceptance_criteria>

  <done>Los cuatro eventos de comportamiento cargan desde un YAML real, `duration_gte` dispara leyendo `duration_s`, y `backend/events/rules.py` no se ha tocado.</done>
</task>

<task type="auto">
  <name>Task 2: Puerta de fase — suite completa y trazabilidad de los 5 criterios</name>

  <read_first>
    - `.planning/ROADMAP.md` § Phase 26 (lineas 470-481) — los 5 criterios verbatim.
    - `.planning/phases/26-an-lisis-de-comportamiento/26-VALIDATION.md` § "Criterios de
      exito del ROADMAP -> comando" — los comandos ya definidos por el research.
    - `.planning/phases/25-re-identificaci-n-de-personas-reid/25-06-SUMMARY.md` — el
      formato de la tabla criterio -> comando -> resultado de la fase anterior.
    - `.planning/STATE.md` — secciones a actualizar (Current Position, tabla de las 22
      fases, Test Coverage, Accumulated Context / Decisions, Session Continuity).
  </read_first>

  <files>.planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/STATE.md</files>

  <action>
1. **Suite completa** (obligatorio: la fase toca pipeline, eventos y configuracion, y
   CLAUDE.md § Tests punto 2 lo exige):
   `F:/Documentos/IA/Proyecto_Camara/.venv/Scripts/python.exe -m pytest tests/ -q`
   Linea base 413/413 antes de la fase. Si algo falla, CORREGIRLO, no ocultarlo ni
   marcarlo como skip.

2. **Trazabilidad.** Ejecutar los 6 comandos y registrar el resultado real (numero de
   tests y codigo de salida) en `26-05-SUMMARY.md`:

   | # | Criterio | Comando |
   |---|---|---|
   | 1 | Los 6 eventos se emiten con umbrales configurables | `pytest tests/test_behavior_analyzer.py tests/test_config.py tests/test_event_engine.py -k "behavior or loiter or crowd or immobile or running or zone" -q` |
   | 2 | Seis trayectorias -> exactamente el evento esperado y ninguno mas | `pytest tests/test_behavior_analyzer.py -k trajectory -q` + `pytest tests/test_event_engine.py -k "zone_dwell or zone_transitions" -q` |
   | 3 | Cada evento incluye en payload las magnitudes que lo justifican | `pytest tests/test_behavior_analyzer.py -k payload -q` + `pytest tests/test_event_engine.py -k emit_behavior -q` |
   | 4 | Historial acotado, no crece con la sesion | `pytest tests/test_memory_bounds.py -k "behavior or zone_entry" -q` |
   | 5 | Usables como `when.event` sin cambios en el RuleEngine | `pytest tests/test_rule_engine.py -k behavior -q` |
   | — | Sin regresion | `pytest tests/ -q` |

   El criterio 2 se reparte en dos comandos a proposito: cuatro de las seis trayectorias
   (LOITERING, RUNNING, IMMOBILE, CROWD) viven en el dominio puro, y las dos de zona
   (ZONE_ENTERED, ZONE_EXITED) en `EventEngine.process_zone`, que es su unico dueño desde
   la Fase 19 — reimplementarlas en el analizador habria duplicado cada evento de zona
   (26-CONTEXT.md H-2). Dejarlo escrito en el SUMMARY para que la division no parezca un
   hueco.

3. **REQUIREMENTS.md** — marcar `[x]` BEH-01, BEH-02, BEH-03, BEH-04 y BEH-05
   (lineas 231-235). NO tocar BEH-06..BEH-09 (Fase 27).

4. **ROADMAP.md** — marcar la Fase 26 y sus 5 planes como completos, con la misma forma
   que la Fase 25 (lineas 460-468).

5. **STATE.md** — actualizar: `stopped_at`, `last_updated`, `completed_phases` (9 -> 10),
   `completed_plans` (25 -> 30), `total_plans` (25 -> 30), `percent`, la fila de la Fase
   26 en la tabla de las 22 fases, `Test Coverage` con la cifra nueva, y las decisiones
   acumuladas de esta fase, que son al menos estas cinco:
   - El problema del historial de 120 s se DISUELVE con agregados incrementales O(1)
     (584 B/track medidos frente a 141,8 KB si se hubiera ampliado `history_len` a 1000);
     `backend/pipeline/tracking.py` no se toco y `centroid_history` solo se lee para la
     ventana de 1 s de RUNNING.
   - Los CUATRO comportamientos llevan latch por episodio, no solo CROWD: sin el, una
     persona parada 10 min genera ~4.800 eventos IMMOBILE a 8 FPS. `debounce_secs` de
     `rules.yaml` no sirve como sustituto porque actua despues de persistir y difundir.
   - `analyze()` devuelve `list[BehaviorFinding]`, no `list[Event]` — corrige SPEC §5.7
     (D-3 del research): `perception/` no conoce `camera_id` ni el reloj de pared.
   - LOITERING usa la escena completa como zona implicita (`zone_id=None`) cuando el
     track no esta en ninguna zona, con `loiter_require_zone=False` como default (D-02):
     una instalacion limpia tiene cero zonas y sin el fallback el evento no se emitiria
     nunca. LOITERING e IMMOBILE coexisten (D-03) y con zonas solapadas se emite uno por
     zona (D-04).
   - La clave del payload se llama `duration_s` literal porque `rules.py:88-91` la lee
     asi para resolver `duration_gte`; cualquier otro nombre rompe el criterio 5 en
     silencio.
   - Los 4 comportamientos se quedan en `Severity.INFO` (D-01, cambio cero): subirlos a
     WARNING habria activado la subida automatica de clips a Google Drive.
   Añadir el checkpoint del Task 3 a la lista de checkpoints manuales abiertos (pasa a
   ser el 8º) y dejar como siguiente paso `/gsd:plan-phase 27`.
  </action>

  <verify>
    <automated>F:/Documentos/IA/Proyecto_Camara/.venv/Scripts/python.exe -m pytest tests/ -q</automated>
  </verify>

  <acceptance_criteria>
    - `pytest tests/ -q` sale con codigo 0 y un total >= 413 + los tests nuevos, sin skips nuevos.
    - `grep -c "^- \[x\] \*\*BEH-0[1-5]\*\*" .planning/REQUIREMENTS.md` devuelve 5.
    - `grep -n "BEH-06" .planning/REQUIREMENTS.md` sigue con `[ ]` (Fase 27 intacta).
    - `.planning/phases/26-an-lisis-de-comportamiento/26-05-SUMMARY.md` contiene una tabla con los 5 criterios, su comando y el resultado real.
    - `grep -n "26 — Analisis de comportamiento\|26 — Análisis de comportamiento" .planning/STATE.md` muestra la fila marcada como completa.
    - `git diff --stat backend/` no incluye `tracking.py`, `types.py`, `rules.py` ni `config/rules.yaml`.
  </acceptance_criteria>

  <done>Suite completa verde, los 5 criterios trazados a comandos que pasan, BEH-01..BEH-05 marcados y STATE/ROADMAP al dia.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Calibracion de umbrales con camara real (8º checkpoint manual)</name>

  <what-built>
El analisis de comportamiento completo y automatizado: LOITERING, RUNNING, IMMOBILE y
CROWD_DETECTED con latch por episodio, `duration_s` en `ZONE_EXITED`, 10 umbrales
configurables y ~30 tests deterministas con trayectorias sinteticas. Todo lo que se puede
verificar sin camara ya esta verde.

Lo que NO se puede verificar sin camara: los umbrales `run_speed_px_s=350`,
`loiter_radius_px=80` e `immobile_radius_px=20` vienen de SPEC_v2.md §5.7 y estan en
PIXELES DEL FRAME PROCESADO. Su valor real depende de la perspectiva y la distancia de la
escena: en un pasillo estrecho 350 px/s puede ser andar deprisa, y en un plano general
puede ser inalcanzable corriendo.
  </what-built>

  <how-to-verify>
1. Arrancar el sistema con la camara real:
   `F:/Documentos/IA/Proyecto_Camara/.venv/Scripts/python.exe -m uvicorn backend.main:app --host 0.0.0.0 --port 8000`
2. Abrir el dashboard y observar el WebSocket de eventos durante ~15 minutos de actividad
   normal.
3. Comprobar cuatro cosas:
   a. **RUNNING**: caminar a ritmo normal por la escena -> NO debe emitirse RUNNING. Correr
      -> debe emitirse una vez por episodio. Si andar dispara RUNNING, subir
      `RUN_SPEED_PX_S`; si correr no lo dispara, bajarlo.
   b. **IMMOBILE**: quedarse quieto mas de 60 s -> exactamente UN evento, no una rafaga.
      Si salen varios, hay un latch roto.
   c. **LOITERING**: sin zonas configuradas debe emitirse con `zone_id: null` tras 120 s.
      Si la escena tiene mucho transito y resulta ruidoso, la salida limpia es
      `LOITER_REQUIRE_ZONE=true` o una regla con `debounce_secs` en `rules.yaml`.
   d. **CROWD_DETECTED**: con `CROWD_THRESHOLD` bajado temporalmente a 2, dos personas
      deben producir UN evento mientras dure la aglomeracion.
4. Anotar los valores finales de los umbrales en `.env` y en el SUMMARY.

**Este checkpoint NO bloquea avanzar a la Fase 27**: los umbrales son configurables en
caliente por `.env`, todos los criterios deterministas ya estan verdes con trayectorias
sinteticas, y `behavior_enabled=False` desactiva la via entera si molestara. Si no hay
camara en esta sesion, diferirlo explicitamente en `STATE.md` como se hizo con los 7
checkpoints anteriores (25-06 Task 2 fue el ultimo).
  </how-to-verify>

  <resume-signal>Escribe "aprobado" con los valores de umbral finales, o "diferido" si no hay camara disponible en esta sesion.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fichero rules.yaml -> proceso | Reglas escritas a mano por el operador que se cargan al arrancar |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-16 | Tampering | carga de `rules.yaml` | accept | `load_rules` ya valida cada regla con Pydantic y desactiva la invalida sin bloquear el arranque (rules.py:105-120); sin `eval()` en ninguna parte (rules.py:3-4). Sin cambios en esta fase |
| T-26-17 | Denial of Service | reglas de comportamiento en produccion | transfer | Esta fase NO puebla `config/rules.yaml`: que alertas quiere el usuario es decision suya. El latch del dominio ya acota los eventos en origen y `debounce_secs` queda como segunda capa configurable |
| T-26-18 | Repudiation | umbrales sin calibrar contra una escena real | accept | Documentado como 8º checkpoint manual (Task 3); `behavior_enabled=False` y los 10 parametros por `.env` son la valvula de escape |
</threat_model>

<verification>
```bash
F:/Documentos/IA/Proyecto_Camara/.venv/Scripts/python.exe -m pytest tests/ -q
```
</verification>

<success_criteria>
- Los cuatro eventos de comportamiento cargan como `when.event` desde un YAML real y `duration_gte` dispara por `duration_s`.
- `backend/events/rules.py` y `config/rules.yaml` sin cambios (el criterio 5 es cero codigo).
- Suite completa verde con los 5 criterios trazados a comandos que pasan.
- BEH-01..BEH-05 marcados `[x]`; ROADMAP y STATE al dia.
- El checkpoint de calibracion queda aprobado o explicitamente diferido.
</success_criteria>

<output>
Al terminar, crear `.planning/phases/26-an-lisis-de-comportamiento/26-05-SUMMARY.md`.
</output>
