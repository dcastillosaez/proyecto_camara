---
phase: 26-an-lisis-de-comportamiento
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/config.py
  - tests/test_config.py
autonomous: true
requirements: [BEH-01, BEH-02, BEH-03]

must_haves:
  truths:
    - "Settings() expone los 10 parametros de comportamiento con los defaults locked de SPEC_v2.md §5.7"
    - "Un umbral <= 0 hace fallar el arranque con un mensaje que explica la consecuencia, no solo el rango"
    - "run_window_secs > 12.0 es rechazado porque centroid_history solo garantiza 12,5 s a 12 FPS"
    - "Un .env viejo sin estos parametros sigue arrancando con los defaults"
  artifacts:
    - path: "backend/config.py"
      provides: "Bloque 'Analisis de comportamiento (Fase 26)' + validate_behavior_params"
      contains: "validate_behavior_params"
    - path: "tests/test_config.py"
      provides: "TEST_behavior_* — defaults y validadores de rango"
      contains: "TEST_behavior_defaults_match_spec"
  key_links:
    - from: "backend/config.py"
      to: "pydantic model_validator"
      via: "@model_validator(mode='after')"
      pattern: "@model_validator\\(mode=\"after\"\\)"
---

<objective>
Añadir a `backend/config.py` los 10 parametros de umbral del analisis de comportamiento
con sus defaults locked y un validador de rango que impide configuraciones imposibles.

Purpose: el criterio 1 del ROADMAP exige "con umbrales configurables". Este plan no
depende de nada (corre en paralelo con el dominio, wave 1) porque `config.py` no importa
`behavior.py`: los defaults son numeros, no tipos.
Output: bloque de config por fase + `validate_behavior_params` + 3 tests `TEST_behavior_*`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/26-an-lisis-de-comportamiento/26-CONTEXT.md
@.planning/phases/26-an-lisis-de-comportamiento/26-PATTERNS.md
@backend/config.py

<interfaces>
<!-- Los 10 parametros y sus defaults. Nombres LOCKED: se usan identicos en
     config.py, manager.py (plan 26-04) y main.py (plan 26-04). -->

```python
behavior_enabled: bool = True
loiter_secs: float = 120.0
loiter_radius_px: float = 80.0
loiter_require_zone: bool = False     # D-02: escape para exigir zona explicita
run_speed_px_s: float = 350.0
run_window_secs: float = 1.0
immobile_secs: float = 60.0
immobile_radius_px: float = 20.0
crowd_threshold: int = 5
behavior_max_tracks: int = 256
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bloque de config de comportamiento y validador de rango</name>

  <read_first>
    - `backend/config.py:149-177` — bloque ReID (Fase 25): la forma OBLIGATORIA del
      comentario de cabecera (`--- <Tema> (Fase N — IDs de requisito) ---` + parrafo que
      justifica *por que* los defaults son esos y que pasa si alguien los cambia).
    - `backend/config.py:211` — `model_config` (los validadores van TODOS juntos despues,
      nunca mezclados con los campos).
    - `backend/config.py:213-229` — `validate_identity_params`: el molde del validador de
      coherencia entre dos magnitudes (mensaje que explica la consecuencia).
    - `backend/config.py:231-248` — `validate_reid_params`: el molde del validador de
      rango (`@model_validator(mode="after")`, `-> "Settings"`, un `if` por parametro).
  </read_first>

  <files>backend/config.py</files>

  <action>
**Campos.** Insertar el bloque justo DESPUES del bloque ReID (`config.py:163`, tras
`reid_max_gallery_entries`), con los 10 campos y defaults EXACTOS del bloque
`<interfaces>`. Comentario de cabecera obligatorio, con este contenido:

```python
    # --- Analisis de comportamiento (Fase 26 — BEH-01..BEH-05) ---
    # Defaults locked de SPEC_v2.md §5.7 (26-CONTEXT.md § Umbrales y reglas). Los
    # umbrales temporales estan en SEGUNDOS y los espaciales en PIXELES DEL FRAME
    # PROCESADO (process_width x process_height, 1280x720 por defecto): cambiar la
    # resolucion de proceso cambia el significado de loiter_radius_px, run_speed_px_s
    # e immobile_radius_px, que ademas no estan calibrados contra una escena real
    # (26-RESEARCH.md § Environment Availability: la calibracion con camara real es un
    # checkpoint manual abierto).
    # loiter_require_zone=False es el fallback de D-02: una instalacion limpia tiene
    # cero zonas (get_zones() lee de BD y no hay seed), asi que sin el LOITERING no se
    # emitiria nunca. A True exige zona explicita.
    # Los cuatro comportamientos salen con Severity.INFO por defecto del catalogo
    # (types.py:49-57, D-01): subirlos a WARNING activaria la subida automatica de
    # clips a Drive (upload_min_severity="warning", config.py:115 -> recording.py:309).
```

**Validador.** Añadirlo al final de la clase, junto a los otros `@model_validator`,
DESPUES de `validate_reid_params`:

```python
    @model_validator(mode="after")
    def validate_behavior_params(self) -> "Settings":
        for name in ("loiter_secs", "run_window_secs", "immobile_secs",
                     "loiter_radius_px", "run_speed_px_s", "immobile_radius_px"):
            if getattr(self, name) <= 0:
                raise ValueError(f"{name} debe ser > 0")
        if self.crowd_threshold < 1:
            raise ValueError(
                "crowd_threshold debe ser >= 1: con 0 se emitiria CROWD_DETECTED "
                "con la escena vacia"
            )
        if self.behavior_max_tracks < 1:
            raise ValueError("behavior_max_tracks debe ser >= 1")
        if self.run_window_secs > 12.0:
            raise ValueError(
                "run_window_secs no puede superar 12.0 s: centroid_history solo "
                "garantiza 12.5 s de historial a 12 FPS (tracking.py:47 history_len=150, "
                "rate.py:26 AdaptiveRate.STEPS[0]=12.0); una ventana mayor no se podria "
                "calcular y RUNNING no se emitiria nunca"
            )
        return self
```

El validador de `run_window_secs > 12.0` es el que convierte H-4 (el historial corto) en
algo imposible de romper por configuracion. Es la misma clase de guarda que
`validate_identity_params` (impide `vote_window < min_votes`, es decir una configuracion
que nunca alcanzaria el minimo).

**NO tocar `DEFAULT_SEVERITY` en `backend/events/types.py`** (D-01: los cuatro
comportamientos se quedan en `INFO`, cambio cero).
**NO tocar `config/rules.yaml`** (CONTEXT lo difiere explicitamente).
  </action>

  <verify>
    <automated>F:/Documentos/IA/Proyecto_Camara/.venv/Scripts/python.exe -c "from backend.config import Settings; s=Settings(); assert (s.behavior_enabled, s.loiter_secs, s.loiter_radius_px, s.loiter_require_zone, s.run_speed_px_s, s.run_window_secs, s.immobile_secs, s.immobile_radius_px, s.crowd_threshold, s.behavior_max_tracks)==(True,120.0,80.0,False,350.0,1.0,60.0,20.0,5,256); import pytest; [pytest.raises(Exception).__enter__() for _ in ()]; ok=0
for kw in ({'loiter_secs':0},{'run_window_secs':13.0},{'crowd_threshold':0},{'immobile_radius_px':-1}):
    try: Settings(**kw)
    except Exception: ok+=1
assert ok==4, ok; print('OK')"</automated>
  </verify>

  <acceptance_criteria>
    - `grep -n "Analisis de comportamiento (Fase 26" backend/config.py` acierta.
    - `grep -c "loiter_secs\|loiter_radius_px\|loiter_require_zone\|run_speed_px_s\|run_window_secs\|immobile_secs\|immobile_radius_px\|crowd_threshold\|behavior_enabled\|behavior_max_tracks" backend/config.py` devuelve >= 10.
    - `grep -n "def validate_behavior_params" backend/config.py` acierta y esta despues de `def validate_reid_params`.
    - `grep -n "run_window_secs no puede superar 12.0" backend/config.py` acierta.
    - `git diff --stat backend/events/types.py` esta vacio (D-01: severidad sin cambios).
    - `git diff --stat config/rules.yaml` esta vacio (CONTEXT lo difiere).
    - El comando de `<verify>` imprime `OK`.
  </acceptance_criteria>

  <done>`Settings()` expone los 10 parametros con los defaults locked y rechaza `loiter_secs=0`, `run_window_secs=13.0`, `crowd_threshold=0` e `immobile_radius_px=-1`.</done>
</task>

<task type="auto">
  <name>Task 2: Tests de defaults y de rango en tests/test_config.py</name>

  <read_first>
    - `tests/test_config.py:224-265` — `TEST_reid_defaults_match_spec` y
      `TEST_reid_time_params_must_be_positive`: el molde exacto (banda de comentario
      `# ─── ... ───` con el motivo, un `assert` por parametro, y
      `pytest.raises((ValidationError, ValueError))` para los rangos).
    - `backend/config.py` — el bloque escrito en el Task 1.
  </read_first>

  <files>tests/test_config.py</files>

  <action>
Añadir tres tests `TEST_behavior_*` al final del fichero, cada uno precedido de su banda
de comentario:

1. `TEST_behavior_defaults_match_spec()` — un `assert` por parametro contra los 10
   defaults del bloque `<interfaces>`. Banda de comentario explicando que
   `loiter_require_zone=False` NO es una omision del plan sino el fallback de D-02 (sin
   el, una instalacion limpia con cero zonas no emitiria LOITERING jamas y el criterio 1
   no seria verificable).
2. `TEST_behavior_params_must_be_positive()` — `pytest.raises((ValidationError, ValueError))`
   para `Settings(loiter_secs=0)`, `Settings(immobile_secs=-1)`,
   `Settings(loiter_radius_px=0)`, `Settings(run_speed_px_s=0)`,
   `Settings(crowd_threshold=0)` y `Settings(behavior_max_tracks=0)`.
3. `TEST_behavior_run_window_capped_by_history()` — `Settings(run_window_secs=13.0)` lanza
   y `Settings(run_window_secs=12.0)` NO lanza. Banda de comentario citando el motivo:
   `centroid_history` es `deque(maxlen=150)` (tracking.py:47) y el escalon mas alto de
   `AdaptiveRate` es 12 FPS (rate.py:26), asi que 150 muestras cubren 12,5 s en el peor
   caso; una ventana mayor no se podria calcular nunca.

Nombres `TEST_*` obligatorios (pytest.ini `python_functions = TEST_*`; en el CI Linux los
`test_*` no se recogen). Los tres nombres contienen "behavior" para que el comando del
mapa de validacion (`pytest tests/test_config.py -k behavior -q`) los recoja.
  </action>

  <verify>
    <automated>F:/Documentos/IA/Proyecto_Camara/.venv/Scripts/python.exe -m pytest tests/test_config.py -k behavior -q</automated>
  </verify>

  <acceptance_criteria>
    - `pytest tests/test_config.py -k behavior -q` recoge exactamente 3 tests y sale en verde.
    - `pytest tests/test_config.py -q` sigue verde entero (sin regresion en los tests previos).
    - `grep -c "TEST_behavior_" tests/test_config.py` devuelve >= 3.
    - `grep -n "loiter_require_zone" tests/test_config.py` acierta.
    - `grep -n "run_window_secs=13.0" tests/test_config.py` acierta.
  </acceptance_criteria>

  <done>3 tests `TEST_behavior_*` verdes y `tests/test_config.py` completo sin regresiones.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| .env / entorno -> proceso | Los umbrales llegan por `pydantic-settings` desde el entorno del operador |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-05 | Tampering | `Settings` umbrales de comportamiento | mitigate | `validate_behavior_params` rechaza valores <= 0 y `run_window_secs > 12.0` al arrancar, con mensaje que explica la consecuencia (ASVS V5). Verificado por `TEST_behavior_params_must_be_positive` |
| T-26-06 | Denial of Service | arranque con `.env` antiguo | accept | `Settings.model_config` ya tiene `extra: "ignore"` (config.py:211): un `.env` sin los parametros nuevos arranca con los defaults. Sin cambio |
| T-26-07 | Elevation of Privilege | severidad de los eventos | mitigate | NO se toca `DEFAULT_SEVERITY` (D-01): los 4 comportamientos siguen en `INFO` y por tanto no cruzan `upload_min_severity="warning"` ni disparan subida de clips a Drive. Verificado con `git diff --stat backend/events/types.py` vacio |
</threat_model>

<verification>
```bash
F:/Documentos/IA/Proyecto_Camara/.venv/Scripts/python.exe -m pytest tests/test_config.py -q
```
</verification>

<success_criteria>
- Los 10 parametros existen con los defaults locked de SPEC_v2.md §5.7.
- `validate_behavior_params` rechaza umbrales <= 0, `crowd_threshold < 1` y `run_window_secs > 12.0`.
- `backend/events/types.py` y `config/rules.yaml` sin cambios.
- 3 tests `TEST_behavior_*` verdes en `tests/test_config.py`.
</success_criteria>

<output>
Al terminar, crear `.planning/phases/26-an-lisis-de-comportamiento/26-02-SUMMARY.md`.
</output>
