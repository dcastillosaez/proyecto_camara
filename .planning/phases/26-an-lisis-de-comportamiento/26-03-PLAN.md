---
phase: 26-an-lisis-de-comportamiento
plan: 03
type: execute
wave: 2
depends_on: [26-01]
files_modified:
  - backend/events/engine.py
  - tests/test_event_engine.py
  - tests/test_memory_bounds.py
autonomous: true
requirements: [BEH-01, BEH-02, BEH-03, BEH-04, BEH-05]

must_haves:
  truths:
    - "Un BehaviorFinding de cada tipo se traduce al EventType correspondiente del catalogo"
    - "El evento publicado lleva en payload exactamente las magnitudes del finding, con duration_s literal"
    - "Los eventos de comportamiento salen con Severity.INFO sin que emit_behavior pase severity="
    - "ZONE_EXITED lleva duration_s con el tiempo de permanencia del track en la zona"
    - "process_zone sin now_monotonic se comporta exactamente igual que hoy"
    - "_zone_entry_at queda vacio tras 10.000 tracks efimeros que entran y salen"
  artifacts:
    - path: "backend/events/engine.py"
      provides: "emit_behavior() + _BEHAVIOR_EVENT_TYPE + _zone_entry_at + dwell time en ZONE_EXITED"
      contains: "def emit_behavior"
    - path: "tests/test_event_engine.py"
      provides: "TEST_emit_behavior_* y TEST_zone_dwell_*"
      contains: "TEST_zone_dwell"
    - path: "tests/test_memory_bounds.py"
      provides: "TEST_zone_entry_at_bounded"
      contains: "TEST_zone_entry_at_bounded"
  key_links:
    - from: "backend/events/engine.py"
      to: "backend/perception/behavior.py"
      via: "import del tipo de dominio (events/ -> perception/, nunca al reves)"
      pattern: "from backend.perception.behavior import"
    - from: "EventEngine.emit_behavior"
      to: "EventEngine._publish"
      via: "payload=finding.magnitudes()"
      pattern: "payload=finding.magnitudes\\(\\)"
---

<objective>
Traducir los veredictos del dominio a eventos tipados (`emit_behavior`) y añadir el
tiempo de permanencia a `ZONE_EXITED` (BEH-04), ambos dentro de `EventEngine`, que es el
unico componente que conoce `camera_id`, el reloj de pared y el bus.

Purpose: cierra BEH-04 y BEH-05 y da al pipeline (plan 26-04) la unica API que necesita.
`perception/` no puede construir `Event` (26-RESEARCH.md D-3, que corrige la firma
`analyze() -> list[Event]` de SPEC_v2.md §5.7), asi que la traduccion vive aqui.
Output: `emit_behavior()` + `_zone_entry_at` + 6-8 tests nuevos.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/26-an-lisis-de-comportamiento/26-PATTERNS.md
@.planning/phases/26-an-lisis-de-comportamiento/26-01-SUMMARY.md
@backend/events/engine.py
@backend/perception/behavior.py

<interfaces>
<!-- Producido por el plan 26-01, consumido aqui -->
```python
class BehaviorKind(str, Enum):
    LOITERING = "loitering"; RUNNING = "running"; IMMOBILE = "immobile"; CROWD = "crowd"

@dataclass
class BehaviorFinding:
    kind: BehaviorKind
    track_id: int | None = None
    zone_id: str | None = None
    duration_s: float | None = None
    net_displacement_px: float | None = None
    speed_px_s: float | None = None
    track_count: int | None = None
    def magnitudes(self) -> dict[str, float | int]: ...   # omite las claves None
```

<!-- Firmas resultantes de este plan, consumidas por el plan 26-04 -->
```python
class EventEngine:
    def emit_behavior(self, finding: BehaviorFinding, now: datetime.datetime,
                      captured_at: float | None = None,
                      processed_at: float | None = None) -> None: ...

    def process_zone(self, zone_id: str, inside_track_ids: set[int],
                     now: datetime.datetime, captured_at: float | None = None,
                     processed_at: float | None = None,
                     now_monotonic: float | None = None) -> None: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: emit_behavior() y la tabla de traduccion al catalogo</name>

  <read_first>
    - `backend/events/engine.py:163-217` — `_identity_event_type` + `emit_identity`: la
      estructura completa a calcar (tabla estatica de traduccion -> guarda de emision ->
      un solo `self._publish(...)`).
    - `backend/events/engine.py:46-66` — `_publish`: la firma que da la via abierta
      (`**fields` con `payload` extraido).
    - `backend/events/engine.py:21` — `from backend.perception.face.identity import ...`:
      la direccion de la dependencia `events/ -> perception/`.
    - `backend/events/types.py:19-57` — los 4 `EventType` ya existentes y
      `DEFAULT_SEVERITY` (que NO los incluye -> caen en `INFO`).
    - `tests/test_event_engine.py:16-34` — `wait_until` y `make_engine`, que este task
      reutiliza (el bus es asincrono: todo test aqui es `async`).
  </read_first>

  <files>backend/events/engine.py, tests/test_event_engine.py</files>

  <behavior>
    - `emit_behavior(BehaviorFinding(kind=IMMOBILE, track_id=7, duration_s=61.0, net_displacement_px=3.2), now)` publica un `Event` con `type is EventType.IMMOBILE`, `track_id == 7` y `payload["duration_s"] == 61.0`.
    - `emit_behavior(BehaviorFinding(kind=CROWD, track_count=6), now)` publica `EventType.CROWD_DETECTED` con `track_id is None` y `payload["track_count"] == 6`.
    - `emit_behavior(BehaviorFinding(kind=LOITERING, track_id=3, zone_id="z1", duration_s=130.0, net_displacement_px=42.0), now)` publica `EventType.LOITERING` con `zone_id == "z1"`.
    - Los cuatro eventos publicados tienen `severity is Severity.INFO`.
    - El payload no contiene claves con valor `None`.
  </behavior>

  <action>
1. Import del tipo de dominio junto a los demas, `backend/events/engine.py:21`:
   `from backend.perception.behavior import BehaviorFinding, BehaviorKind`.
   La direccion es `events/ -> perception/`, NUNCA al reves.

2. Tabla estatica a nivel de modulo (analoga a `_identity_event_type`, pero como dict
   porque aqui la traduccion es 1:1 y total):
```python
_BEHAVIOR_EVENT_TYPE: dict[BehaviorKind, EventType] = {
    BehaviorKind.LOITERING: EventType.LOITERING,
    BehaviorKind.RUNNING: EventType.RUNNING,
    BehaviorKind.IMMOBILE: EventType.IMMOBILE,
    BehaviorKind.CROWD: EventType.CROWD_DETECTED,
}
```
   Los cuatro `EventType` YA existen en `backend/events/types.py:19-46` desde la Fase 19
   (`LOITERING`, `RUNNING`, `IMMOBILE`, `CROWD_DETECTED`). NO añadir tipos nuevos al
   catalogo: `tests/test_event_types.py:12` congela la lista de nombres.

3. Metodo nuevo, en su propia seccion con la banda de comentario del fichero
   (`# --- Comportamiento (Fase 26) ---`), despues de `emit_identity`:
```python
    def emit_behavior(
        self,
        finding: BehaviorFinding,
        now: datetime.datetime,
        captured_at: float | None = None,
        processed_at: float | None = None,
    ) -> None:
        """Publica el evento de comportamiento correspondiente a *finding*, si lo hay.

        La guarda de idempotencia NO esta aqui: vive en el latch por episodio de
        BehaviorAnalyzer, igual que `emits` vive en la FSM para emit_identity. Sin ese
        latch, una persona parada 10 min generaria ~4.800 IMMOBILE a 8 FPS — "the point
        where v1 failed conceptually" (docstring de esta clase).
        """
        event_type = _BEHAVIOR_EVENT_TYPE.get(finding.kind)
        if event_type is None:
            return
        self._publish(
            event_type,
            ts=now,
            captured_at=captured_at,
            processed_at=processed_at,
            track_id=finding.track_id,
            zone_id=finding.zone_id,
            payload=finding.magnitudes(),
        )
```
   **NO pasar `severity=`**: `emit_identity` tampoco lo hace, y asi el `@model_validator`
   de `Event` aplica el default del catalogo. Como `DEFAULT_SEVERITY` (types.py:49-57) no
   incluye estos cuatro tipos, salen `INFO` — que es exactamente D-01. Pasar
   `severity=Severity.WARNING` activaria la subida automatica de clips a Google Drive
   (`upload_min_severity="warning"`, config.py:115 -> recording.py:309).
   `zone_id` va como campo de PRIMER NIVEL, no dentro del payload: asi `when.zone` de
   `rules.yaml` lo filtra (rules.py:75-76) y la columna dedicada lo indexa.

4. Tests en `tests/test_event_engine.py`, reutilizando `make_engine()` y `wait_until()`
   del propio fichero (lineas 16-34):
   - `TEST_emit_behavior_translates_the_four_kinds` — emite los 4 findings y comprueba
     `{e.type for e in received} == {EventType.LOITERING, EventType.RUNNING,
     EventType.IMMOBILE, EventType.CROWD_DETECTED}`.
   - `TEST_emit_behavior_payload_carries_magnitudes` — comprueba `duration_s`,
     `net_displacement_px`, `speed_px_s` y `track_count` con sus nombres literales, y que
     ninguna clave publica vale `None`.
   - `TEST_emit_behavior_keeps_default_info_severity` — `all(e.severity is Severity.INFO)`
     para los cuatro (D-01).
   Nombres `TEST_*` obligatorios.
  </action>

  <verify>
    <automated>F:/Documentos/IA/Proyecto_Camara/.venv/Scripts/python.exe -m pytest tests/test_event_engine.py -k "emit_behavior" -q</automated>
  </verify>

  <acceptance_criteria>
    - `grep -n "from backend.perception.behavior import BehaviorFinding, BehaviorKind" backend/events/engine.py` acierta.
    - `grep -n "_BEHAVIOR_EVENT_TYPE" backend/events/engine.py` acierta y contiene las 4 entradas.
    - `grep -n "def emit_behavior" backend/events/engine.py` acierta.
    - `grep -n "payload=finding.magnitudes()" backend/events/engine.py` acierta.
    - `sed -n '/def emit_behavior/,/^    def /p' backend/events/engine.py | grep -c "severity="` devuelve 0 (D-01).
    - `git diff --stat backend/events/types.py` esta vacio (catalogo congelado).
    - `pytest tests/test_event_engine.py -k emit_behavior -q` recoge 3 tests y sale verde.
  </acceptance_criteria>

  <done>Los cuatro `BehaviorKind` se traducen a sus `EventType`, el payload lleva las magnitudes con nombres literales y la severidad sigue siendo `INFO`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Tiempo de permanencia en ZONE_EXITED (BEH-04)</name>

  <read_first>
    - `backend/events/engine.py:117-138` — `process_zone` completo: es lo que se modifica
      de forma PURAMENTE ADITIVA.
    - `backend/events/engine.py:36-38` — donde se declaran `_known_tracks`, `_zone_inside`
      y `_camera_offline`: el nuevo `_zone_entry_at` va en la misma tanda, con anotacion
      de tipo explicita.
    - `tests/test_event_engine.py:52-65` — `TEST_zone_transitions`: el test existente que
      NO se puede romper (no inspecciona el payload, asi que un cambio aditivo lo deja
      verde). Ejecutarlo antes y despues.
    - `backend/events/rules.py:88-91` — el acoplamiento literal
      `event.payload.get("duration_s")` que fija el nombre de la clave.
  </read_first>

  <files>backend/events/engine.py, tests/test_event_engine.py</files>

  <behavior>
    - `process_zone("z1", {1}, now, now_monotonic=100.0)` seguido de `process_zone("z1", set(), now2, now_monotonic=112.0)` publica `ZONE_EXITED` con `payload["duration_s"] == 12.0`.
    - `process_zone` sin `now_monotonic` (todos los llamadores actuales y todos los tests previos) publica `ZONE_EXITED` sin la clave `duration_s`, y el resto del comportamiento es identico al de hoy.
    - `ZONE_ENTERED` no lleva `duration_s` (no tiene duracion por definicion).
    - Tras la salida, `engine._zone_entry_at["z1"]` no contiene el track.
  </behavior>

  <action>
1. En `__init__`, junto a `self._zone_inside` (engine.py:37):
   `self._zone_entry_at: dict[str, dict[int, float]] = {}`

2. Añadir a `process_zone` el parametro `now_monotonic: float | None = None` AL FINAL de
   la firma (opcion (b) de 26-RESEARCH.md Q3: `captured_at`/`processed_at` son conceptos
   privados de latencia OBS-03 y no deben reutilizarse como reloj semantico; restar dos
   `datetime.datetime.now()` seria sensible a saltos de reloj por NTP o cambio de hora).
   Cuerpo resultante:
```python
        previous = self._zone_inside.get(zone_id, set())
        entered = inside_track_ids - previous
        exited = previous - inside_track_ids
        entry_at = self._zone_entry_at.setdefault(zone_id, {})
        for track_id in entered:
            if now_monotonic is not None:
                entry_at[track_id] = now_monotonic
            self._publish(EventType.ZONE_ENTERED, ts=now, captured_at=captured_at,
                          processed_at=processed_at, track_id=track_id, zone_id=zone_id)
        for track_id in exited:
            t0 = entry_at.pop(track_id, None)     # el pop ES la politica de limpieza
            payload: dict[str, Any] = {}
            if t0 is not None and now_monotonic is not None:
                payload["duration_s"] = round(now_monotonic - t0, 3)   # BEH-04
            self._publish(EventType.ZONE_EXITED, ts=now, captured_at=captured_at,
                          processed_at=processed_at, track_id=track_id, zone_id=zone_id,
                          payload=payload)
        self._zone_inside[zone_id] = set(inside_track_ids)
```
   El `pop` va en el MISMO bucle `for track_id in exited:` que emite `ZONE_EXITED`; como
   `exited = previous - inside_track_ids`, todo track que desaparece pasa por ahi y el
   dict queda acotado por "tracks dentro de zonas ahora mismo" (Pitfall 5).

   **La clave se llama `duration_s`, literal.** `backend/events/rules.py:88-91` hace
   `event.payload.get("duration_s")` para resolver `duration_gte`; con `duration`,
   `dwell_s`, `elapsed` o `secs` la regla cargaria bien pero no dispararia nunca, sin
   error ni log — el criterio 5 se cumpliria a medias y en silencio (Pitfall 8).

   **Compatibilidad:** con `now_monotonic=None` el comportamiento es identico al de hoy
   salvo un `payload` vacio. NO cambiar el orden de los parametros existentes: el unico
   llamador (`detection.py:248-251`) los pasa posicionalmente.

3. **NO cablear `TrackState.zones` ni `TrackState.zone_entry_times`**
   (`tracking.py:28-29`). Estan muertos (cero escritores, cero lectores en todo el repo)
   y deben seguir muertos: escribirlos crearia un segundo dueño del estado de zona
   sincronizado a mano con `_zone_inside`, contra el invariante 7 de CLAUDE.md.
   `backend/pipeline/tracking.py` NO se toca en toda la fase.

4. Tests en `tests/test_event_engine.py`, con `make_engine()`/`wait_until()`:
   - `TEST_zone_dwell_time_in_exited_payload` — entrada con `now_monotonic=100.0`, salida
     con `now_monotonic=112.0` -> `payload["duration_s"] == 12.0`.
   - `TEST_zone_dwell_absent_without_monotonic_clock` — sin `now_monotonic`:
     `"duration_s" not in event.payload`.
   - `TEST_zone_dwell_entry_is_popped_on_exit` — tras la salida,
     `1 not in engine._zone_entry_at["z1"]`.
  </action>

  <verify>
    <automated>F:/Documentos/IA/Proyecto_Camara/.venv/Scripts/python.exe -m pytest tests/test_event_engine.py -k "zone" -q</automated>
  </verify>

  <acceptance_criteria>
    - `grep -n "_zone_entry_at: dict\[str, dict\[int, float\]\]" backend/events/engine.py` acierta.
    - `grep -n "now_monotonic: float | None = None" backend/events/engine.py` acierta dentro de la firma de `process_zone`.
    - `grep -n 'payload\["duration_s"\]' backend/events/engine.py` acierta.
    - `grep -n "entry_at.pop(track_id, None)" backend/events/engine.py` acierta.
    - `git diff --stat backend/pipeline/tracking.py` esta vacio.
    - `pytest tests/test_event_engine.py -k zone -q` sale verde e incluye el `TEST_zone_transitions` preexistente.
  </acceptance_criteria>

  <done>`ZONE_EXITED` lleva `duration_s` cuando se pasa el reloj monotonico, no lo lleva cuando no, y `TEST_zone_transitions` sigue verde.</done>
</task>

<task type="auto">
  <name>Task 3: Cota de _zone_entry_at (criterio 4, segunda estructura)</name>

  <read_first>
    - `tests/test_memory_bounds.py:42-55` — `TEST_track_registry_bounded`: el molde del
      bucle de 10.000 tracks efimeros con su banda de comentario.
    - `tests/test_memory_bounds.py:286-312` — el par con/sin prune de la Fase 25.
    - `backend/events/engine.py` — `process_zone` tras el Task 2.
  </read_first>

  <files>tests/test_memory_bounds.py</files>

  <action>
`EventEngine._zone_entry_at` es la SEGUNDA estructura que esta fase añade al estado en
memoria del proceso (la primera, `BehaviorAnalyzer._aggs`/`_loiter`, la cubre el plan
26-01). El criterio 4 del ROADMAP y el invariante de la Fase 22 exigen un test por
estructura.

Añadir `TEST_zone_entry_at_bounded()` con su banda de comentario `# ─── ... ───`
explicando el motivo: sin el `pop` en la rama de salida, cada track que entra en una zona
dejaria una entrada viva para siempre; ByteTrack asigna ids monotonamente crecientes y
nunca los reutiliza.

Forma del test (sincrono, sin bus real: se puede construir el `EventEngine` con un
`EventBus` cuyo `publish_threadsafe` no requiera loop activo, o reutilizar el helper
`make_engine()` de `tests/test_event_engine.py` importandolo; elegir lo que deje el test
sin `await` y sin flakiness):
```python
    for tid in range(10_000):
        engine.process_zone("z1", {tid}, wall_now, now_monotonic=float(tid))
        engine.process_zone("z1", set(), wall_now, now_monotonic=float(tid) + 1.0)
    assert engine._zone_entry_at["z1"] == {}
```
Repetirlo con DOS zonas (`"z1"`, `"z2"`) para cubrir el caso de zonas solapadas: ambos
dicts deben quedar vacios. Acceder al atributo privado directamente es la convencion
aceptada en este fichero.
  </action>

  <verify>
    <automated>F:/Documentos/IA/Proyecto_Camara/.venv/Scripts/python.exe -m pytest tests/test_memory_bounds.py -q</automated>
  </verify>

  <acceptance_criteria>
    - `grep -n "TEST_zone_entry_at_bounded" tests/test_memory_bounds.py` acierta.
    - `grep -n "_zone_entry_at" tests/test_memory_bounds.py` acierta con un assert de dict vacio.
    - `pytest tests/test_memory_bounds.py -q` sale verde entero, incluidos los dos tests `TEST_behavior_state_bounded*` del plan 26-01.
  </acceptance_criteria>

  <done>`_zone_entry_at` queda vacio tras 10.000 entradas/salidas en dos zonas.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dominio -> bus de eventos | Los findings cruzan a `Event`, que se persiste en SQLite y se difunde por WebSocket a los clientes autenticados del dashboard |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-08 | Denial of Service | `EventEngine._zone_entry_at` | mitigate | `entry_at.pop(track_id, None)` dentro del mismo bucle `for track_id in exited:`; como `exited = previous - inside_track_ids`, todo track que desaparece pasa por ahi. Verificado por `TEST_zone_entry_at_bounded` |
| T-26-09 | Elevation of Privilege | severidad de los eventos publicados | mitigate | `emit_behavior` no pasa `severity=`, asi que aplica el default `INFO` del catalogo y los comportamientos no cruzan `upload_min_severity="warning"` (D-01). Verificado por `TEST_emit_behavior_keeps_default_info_severity` y por el grep de `severity=` en el cuerpo del metodo |
| T-26-10 | Information Disclosure | `Event.payload` de comportamiento | accept | Solo numeros y `track_id`; sin `person_name`, sin `snapshot_path`, sin recortes. La columna `payload` es `JSON` y ya se serializa entera al WebSocket |
| T-26-11 | Tampering | firma de `process_zone` | mitigate | `now_monotonic` es aditivo con default `None`; el unico llamador pasa el resto posicionalmente y `TEST_zone_transitions` (preexistente) queda como barrera de regresion |
</threat_model>

<verification>
```bash
F:/Documentos/IA/Proyecto_Camara/.venv/Scripts/python.exe -m pytest tests/test_event_engine.py tests/test_memory_bounds.py tests/test_event_types.py -q
```
</verification>

<success_criteria>
- `emit_behavior` traduce los 4 `BehaviorKind` a sus `EventType` con `payload=finding.magnitudes()` y sin `severity=`.
- `ZONE_EXITED` lleva `duration_s` (clave literal) cuando se pasa `now_monotonic`.
- `backend/events/types.py` y `backend/pipeline/tracking.py` sin cambios.
- 6-8 tests nuevos verdes y `TEST_zone_transitions` sin romperse.
</success_criteria>

<output>
Al terminar, crear `.planning/phases/26-an-lisis-de-comportamiento/26-03-SUMMARY.md`.
</output>
