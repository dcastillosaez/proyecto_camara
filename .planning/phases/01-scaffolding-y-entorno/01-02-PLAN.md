---
phase: 01-scaffolding-y-entorno
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified: []
autonomous: true
requirements: []

must_haves:
  truths:
    - "El entorno virtual usa Python 3.12.x, no 3.14"
    - "Todas las dependencias del requirements.txt estan instaladas sin error"
    - "Los imports clave del stack funcionan: fastapi, cv2, ultralytics, supervision, aiosqlite, sqlalchemy, pydantic_settings"
    - "backend/config.py es importable y get_settings() devuelve un objeto Settings valido"
  artifacts:
    - path: ".venv/Scripts/python.exe"
      provides: "Interprete Python 3.12 del entorno virtual"
    - path: ".venv/Scripts/activate"
      provides: "Script de activacion del venv para bash en Windows"
  key_links:
    - from: ".venv/Scripts/python.exe"
      to: "Python 3.12.10"
      via: "py -3.12 -m venv"
      pattern: "Python 3\\.12"
    - from: "backend/config.py"
      to: "pydantic_settings"
      via: "import inside venv"
      pattern: "from pydantic_settings import BaseSettings"
---

<objective>
Crear el entorno virtual con Python 3.12, instalar todas las dependencias, y verificar que el stack completo es importable y que config.py funciona.

Purpose: Dejar el entorno de desarrollo listo para que las fases 2-8 puedan ejecutar codigo sin configuracion adicional.
Output: Entorno virtual funcional con todas las dependencias instaladas y verificadas.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-scaffolding-y-entorno/01-CONTEXT.md
@.planning/phases/01-scaffolding-y-entorno/01-RESEARCH.md
@.planning/phases/01-scaffolding-y-entorno/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Crear entorno virtual con Python 3.12 e instalar dependencias</name>
  <files></files>
  <read_first>requirements.txt, .planning/phases/01-scaffolding-y-entorno/01-RESEARCH.md</read_first>
  <action>
1. Crear el entorno virtual usando EXACTAMENTE `py -3.12 -m venv .venv` (no `python -m venv` que usaria 3.14).

2. Verificar la version de Python dentro del venv:
   ```bash
   .venv/Scripts/python.exe --version
   ```
   DEBE mostrar `Python 3.12.x`. Si muestra 3.14 o cualquier otra version, PARAR y diagnosticar.

3. Actualizar pip dentro del venv:
   ```bash
   .venv/Scripts/python.exe -m pip install --upgrade pip
   ```

4. Instalar dependencias:
   ```bash
   .venv/Scripts/python.exe -m pip install -r requirements.txt
   ```
   Si hay errores de compilacion (especialmente opencv-python), diagnosticar antes de reintentar. La investigacion indica que Python 3.12 tiene wheels precompilados disponibles.

5. Verificar que los imports clave funcionan (DENTRO del venv):
   ```bash
   .venv/Scripts/python.exe -c "import fastapi; import cv2; import ultralytics; import supervision; import aiosqlite; import sqlalchemy; import pydantic_settings; print('All imports OK')"
   ```

6. Verificar que backend/config.py es importable y funcional:
   ```bash
   .venv/Scripts/python.exe -c "import sys; sys.path.insert(0, '.'); from backend.config import get_settings; s = get_settings(); print(f'camera_url={s.camera_url}'); print(f'port={s.port}')"
   ```
   Debe imprimir `camera_url=rtsp://192.168.1.132:554/stream1` y `port=8000`.

IMPORTANTE:
- No usar `source .venv/Scripts/activate` en los comandos — invocar `.venv/Scripts/python.exe` directamente para evitar problemas con activacion de venv en subshells.
- No usar `python` ni `python3` a secas — siempre la ruta completa al interprete del venv.
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe --version && .venv/Scripts/python.exe -c "import fastapi, cv2, ultralytics, supervision, aiosqlite, sqlalchemy, pydantic_settings; print('OK')" && .venv/Scripts/python.exe -c "import sys; sys.path.insert(0, '.'); from backend.config import get_settings; s = get_settings(); assert s.port == 8000; print('config OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/Scripts/python.exe --version` muestra `Python 3.12.x` (no 3.14)
    - `.venv/Scripts/python.exe -c "import fastapi"` sale con codigo 0
    - `.venv/Scripts/python.exe -c "import cv2"` sale con codigo 0
    - `.venv/Scripts/python.exe -c "import ultralytics"` sale con codigo 0
    - `.venv/Scripts/python.exe -c "import supervision"` sale con codigo 0
    - `.venv/Scripts/python.exe -c "import aiosqlite"` sale con codigo 0
    - `.venv/Scripts/python.exe -c "import sqlalchemy"` sale con codigo 0
    - `.venv/Scripts/python.exe -c "import pydantic_settings"` sale con codigo 0
    - `from backend.config import get_settings` no da error y `get_settings().port` devuelve `8000`
    - `get_settings().camera_url` devuelve `rtsp://192.168.1.132:554/stream1`
  </acceptance_criteria>
  <done>El venv usa Python 3.12.x, todas las dependencias estan instaladas, los 7 imports clave funcionan sin error, y backend/config.py devuelve valores por defecto correctos.</done>
</task>

</tasks>

<verification>
- `.venv/Scripts/python.exe --version` muestra Python 3.12.x
- `.venv/Scripts/python.exe -c "import fastapi, cv2, ultralytics, supervision, aiosqlite, sqlalchemy, pydantic_settings"` sale con codigo 0
- `.venv/Scripts/python.exe -c "import sys; sys.path.insert(0, '.'); from backend.config import get_settings; s = get_settings(); print(s.camera_url, s.port)"` imprime los valores por defecto
- `grep -c '=' .env.example` devuelve 5
</verification>

<success_criteria>
El entorno virtual con Python 3.12 esta creado en .venv/, todas las 11 dependencias de requirements.txt estan instaladas, los imports del stack completo funcionan, y config.py carga la configuracion con valores por defecto correctos.
</success_criteria>

<output>
After completion, create `.planning/phases/01-scaffolding-y-entorno/01-02-SUMMARY.md`
</output>
