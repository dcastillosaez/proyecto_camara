---
phase: 01-scaffolding-y-entorno
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitignore
  - requirements.txt
  - .env.example
  - backend/__init__.py
  - backend/main.py
  - backend/stream.py
  - backend/detector.py
  - backend/tracker.py
  - backend/database.py
  - backend/config.py
  - frontend/index.html
  - frontend/app.js
  - tests/__init__.py
  - tests/test_detector.py
  - tests/test_tracker.py
  - data/.gitkeep
autonomous: true
requirements: []

must_haves:
  truths:
    - "Todos los directorios del proyecto existen (backend/, frontend/, tests/, data/)"
    - "Todos los modulos de backend son importables sin error"
    - "requirements.txt lista todas las dependencias del stack"
    - ".env.example documenta las 5 variables de configuracion"
    - "backend/config.py define Settings con pydantic-settings y get_settings con lru_cache"
  artifacts:
    - path: ".gitignore"
      provides: "Exclusion de .venv/, __pycache__, data/*.db, .env"
      contains: ".venv"
    - path: "requirements.txt"
      provides: "Todas las dependencias del proyecto"
      contains: "fastapi"
    - path: ".env.example"
      provides: "Documentacion de variables de configuracion"
      contains: "CAMERA_URL"
    - path: "backend/config.py"
      provides: "Clase Settings con pydantic-settings y singleton via lru_cache"
      contains: "class Settings"
    - path: "backend/main.py"
      provides: "Placeholder del modulo principal FastAPI"
      min_lines: 1
    - path: "data/.gitkeep"
      provides: "Directorio data/ en git"
  key_links:
    - from: "backend/config.py"
      to: "pydantic_settings"
      via: "import BaseSettings"
      pattern: "from pydantic_settings import BaseSettings"
---

<objective>
Crear toda la estructura de ficheros del proyecto: .gitignore, directorios, modulos placeholder de backend, frontend, tests, requirements.txt, .env.example y backend/config.py funcional.

Purpose: Establecer la base de ficheros sobre la que se instalan dependencias y se construyen las fases 2-8.
Output: Arbol completo de ficheros listos para `pip install -r requirements.txt`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-scaffolding-y-entorno/01-CONTEXT.md
@.planning/phases/01-scaffolding-y-entorno/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Crear .gitignore, estructura de directorios y ficheros placeholder</name>
  <files>.gitignore, backend/__init__.py, backend/main.py, backend/stream.py, backend/detector.py, backend/tracker.py, backend/database.py, frontend/index.html, frontend/app.js, tests/__init__.py, tests/test_detector.py, tests/test_tracker.py, data/.gitkeep</files>
  <read_first>CLAUDE.md</read_first>
  <action>
1. Crear `.gitignore` con template Python de GitHub mas estas entradas adicionales:
   - `.venv/`
   - `data/*.db`
   - `.env`
   - `__pycache__/`
   - `*.pyc`
   - `.idea/`
   - `.vscode/`
   - `*.egg-info/`
   - `dist/`
   - `build/`

2. Crear directorios: `backend/`, `frontend/`, `tests/`, `data/`

3. Crear `data/.gitkeep` (fichero vacio)

4. Crear `backend/__init__.py` con contenido vacio (solo un comentario: `# backend package`)

5. Crear modulos placeholder en `backend/` — cada uno con solo un docstring de una linea:
   - `backend/main.py`: `"""FastAPI application entry point."""`
   - `backend/stream.py`: `"""RTSP stream capture and MJPEG broadcast."""`
   - `backend/detector.py`: `"""YOLOv8 person detection wrapper."""`
   - `backend/tracker.py`: `"""ByteTrack person tracking and line crossing counter."""`
   - `backend/database.py`: `"""SQLite async database models and operations."""`

6. Crear `frontend/index.html` con placeholder HTML5 minimo:
   ```html
   <!DOCTYPE html>
   <html lang="es">
   <head>
       <meta charset="UTF-8">
       <meta name="viewport" content="width=device-width, initial-scale=1.0">
       <title>Tapo Dashboard</title>
   </head>
   <body>
       <h1>Tapo Dashboard</h1>
       <p>Placeholder — se implementa en Phase 7.</p>
   </body>
   </html>
   ```

7. Crear `frontend/app.js` con placeholder:
   ```javascript
   // Tapo Dashboard — WebSocket client and Chart.js bindings
   // Implementation in Phase 7
   ```

8. Crear `tests/__init__.py` con contenido vacio (`# tests package`)

9. Crear `tests/test_detector.py` con placeholder:
   ```python
   """Tests for person detection module."""
   ```

10. Crear `tests/test_tracker.py` con placeholder:
    ```python
    """Tests for tracking and line crossing counter."""
    ```
  </action>
  <verify>
    <automated>ls backend/main.py backend/stream.py backend/detector.py backend/tracker.py backend/database.py backend/config.py backend/__init__.py frontend/index.html frontend/app.js tests/__init__.py tests/test_detector.py tests/test_tracker.py data/.gitkeep .gitignore</automated>
  </verify>
  <acceptance_criteria>
    - `.gitignore` contiene `.venv/` y `data/*.db` y `.env`
    - `backend/main.py` contiene `"""FastAPI application entry point."""`
    - `backend/__init__.py` existe
    - `frontend/index.html` contiene `<title>Tapo Dashboard</title>`
    - `frontend/app.js` contiene `// Tapo Dashboard`
    - `tests/test_detector.py` contiene `"""Tests for person detection module."""`
    - `tests/test_tracker.py` contiene `"""Tests for tracking and line crossing counter."""`
    - `data/.gitkeep` existe
  </acceptance_criteria>
  <done>Los 16 ficheros existen con el contenido correcto. El comando ls no da error para ninguno.</done>
</task>

<task type="auto">
  <name>Task 2: Crear requirements.txt, .env.example y backend/config.py</name>
  <files>requirements.txt, .env.example, backend/config.py</files>
  <read_first>CLAUDE.md, .planning/phases/01-scaffolding-y-entorno/01-CONTEXT.md, .planning/phases/01-scaffolding-y-entorno/01-RESEARCH.md</read_first>
  <action>
1. Crear `requirements.txt` con estas dependencias exactas (operador `>=`, una por linea, sin comentarios entre medias):
   ```
   fastapi>=0.115
   uvicorn[standard]>=0.30
   opencv-python>=4.10
   ultralytics>=8.4
   supervision>=0.25
   aiosqlite>=0.20
   sqlalchemy[asyncio]>=2.0
   pydantic-settings>=2.0
   websockets>=12.0
   pytest>=7.0
   httpx>=0.24
   ```
   NO incluir `databases` (encode) ni `python-dotenv` — decisiones bloqueadas en CONTEXT.md.

2. Crear `.env.example` con este contenido exacto:
   ```
   # Tapo Dashboard — Variables de configuracion
   # Copiar a .env y ajustar valores segun tu entorno

   CAMERA_URL=rtsp://192.168.1.132:554/stream1
   YOLO_CONFIDENCE=0.45
   DB_PATH=data/events.db
   HOST=0.0.0.0
   PORT=8000
   ```

3. Crear `backend/config.py` con este contenido exacto:
   ```python
   """Centralized configuration via pydantic-settings."""

   from functools import lru_cache

   from pydantic_settings import BaseSettings


   class Settings(BaseSettings):
       """Application settings loaded from environment / .env file."""

       camera_url: str = "rtsp://192.168.1.132:554/stream1"
       yolo_confidence: float = 0.45
       db_path: str = "data/events.db"
       host: str = "0.0.0.0"
       port: int = 8000

       model_config = {"env_file": ".env"}


   @lru_cache
   def get_settings() -> Settings:
       """Return cached Settings singleton."""
       return Settings()
   ```
  </action>
  <verify>
    <automated>grep -c "fastapi" requirements.txt && grep -c "CAMERA_URL" .env.example && grep -c "class Settings" backend/config.py</automated>
  </verify>
  <acceptance_criteria>
    - `requirements.txt` contiene exactamente 11 lineas de dependencias: fastapi, uvicorn[standard], opencv-python, ultralytics, supervision, aiosqlite, sqlalchemy[asyncio], pydantic-settings, websockets, pytest, httpx
    - `requirements.txt` NO contiene `databases` ni `python-dotenv`
    - `.env.example` contiene exactamente 5 variables: CAMERA_URL, YOLO_CONFIDENCE, DB_PATH, HOST, PORT
    - `backend/config.py` contiene `from pydantic_settings import BaseSettings`
    - `backend/config.py` contiene `class Settings(BaseSettings):`
    - `backend/config.py` contiene `@lru_cache` antes de `def get_settings`
    - `backend/config.py` contiene `camera_url: str`, `yolo_confidence: float`, `db_path: str`, `host: str`, `port: int`
    - `backend/config.py` contiene `model_config = {"env_file": ".env"}`
  </acceptance_criteria>
  <done>requirements.txt tiene las 11 dependencias, .env.example tiene las 5 variables, config.py define Settings con pydantic-settings y get_settings con lru_cache.</done>
</task>

</tasks>

<verification>
- `ls backend/main.py backend/stream.py backend/detector.py backend/tracker.py backend/database.py backend/config.py backend/__init__.py frontend/index.html frontend/app.js tests/__init__.py tests/test_detector.py tests/test_tracker.py data/.gitkeep .gitignore requirements.txt .env.example` — todos existen sin error
- `grep "fastapi" requirements.txt` — aparece
- `grep "CAMERA_URL" .env.example` — aparece
- `grep "class Settings" backend/config.py` — aparece
</verification>

<success_criteria>
Los 18 ficheros del proyecto existen con el contenido especificado. requirements.txt tiene 11 dependencias. .env.example tiene 5 variables. config.py es importable con pydantic-settings instalado.
</success_criteria>

<output>
After completion, create `.planning/phases/01-scaffolding-y-entorno/01-01-SUMMARY.md`
</output>
