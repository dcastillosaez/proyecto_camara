---
phase: 30
plan: 09
type: execute
wave: 7
depends_on: ["30-06", "30-07"]
files_modified:
  - frontend/js/components/alertCenter.js
  - frontend/js/views/dashboard.js
autonomous: true
requirements: [OPS-11]

must_haves:
  truths:
    - "La campana de la cabecera muestra cuantas alertas activas hay y de que color segun la mas grave"
    - "El cajon lateral lista las alertas agrupadas por regla, con su recuento y la hora del ultimo disparo"
    - "Se puede silenciar una regla eligiendo una duracion, y reactivarla antes de que expire"
    - "Una regla silenciada sigue visible pero atenuada, y desaparece del top-3 de la Fase 29"
    - "El panel Alertas activas de la Fase 29 se alimenta de la misma agrupacion, sin filtrar nada en el navegador"
  artifacts:
    - path: "frontend/js/components/alertCenter.js"
      provides: "loadAlerts(), bindAlertCenter(), openDrawer/closeDrawer, silenciado y reactivacion"
      contains: "export function bindAlertCenter"
    - path: "frontend/js/views/dashboard.js"
      provides: "loadActiveAlerts retirado (se muda a alertCenter.js); el resto del modulo intacto"
  key_links:
    - from: "frontend/js/components/alertCenter.js"
      to: "GET /api/v2/alerts"
      via: "apiFetch, agrupacion ya resuelta en servidor"
      pattern: "/api/v2/alerts"
    - from: "frontend/js/components/alertCenter.js"
      to: "POST /api/v2/alerts/mute"
      via: "popover de duracion con data-duration"
      pattern: "alerts/mute"
    - from: "frontend/js/components/alertCenter.js"
      to: "#alerts-active-list (panel top-3 de la Fase 29)"
      via: "misma respuesta, tres primeros grupos no silenciados"
      pattern: "alerts-active-list"
---

<objective>
Construir el centro de alertas del navegador: badge de la campana, cajón lateral con los grupos que devuelve `GET /api/v2/alerts`, popover de silenciado con duración obligatoria y reactivación; y mudar `loadActiveAlerts()` desde `dashboard.js` para que el panel top-3 de la Fase 29 se alimente de la misma agrupación en servidor.

Purpose: OPS-11 y el criterio de éxito 6. La mudanza no es cosmética: `dashboard.js` está en 290 de las 300 líneas que impone `TEST_line_limit`, así que no cabe nada más ahí; y el top-3 actual filtra `severity !== 'info'` y ordena en el navegador sobre `GET /api/v2/events?limit=10`, que ya no es la fuente correcta ahora que existe la agrupación por regla.
Output: `frontend/js/components/alertCenter.js` (módulo nuevo) y `dashboard.js` aligerado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-event-timeline-y-centro-de-alertas/30-CONTEXT.md
@.planning/phases/30-event-timeline-y-centro-de-alertas/30-UI-SPEC.md
@.planning/phases/30-event-timeline-y-centro-de-alertas/30-RESEARCH.md
@.planning/phases/30-event-timeline-y-centro-de-alertas/30-PATTERNS.md
@CLAUDE.md
</context>

<interfaces>
Este plan CONSUME el contrato de `GET /api/v2/alerts` y `POST /api/v2/alerts/mute|unmute` definido en el bloque `<interfaces>` de `30-06-PLAN.md`, y los ids del DOM definidos en `30-07-PLAN.md`. Leer ambos antes de empezar.

API pública del módulo nuevo:
```javascript
// frontend/js/components/alertCenter.js
export async function loadAlerts()      // pide /api/v2/alerts y repinta badge + cajon + top-3
export function bindAlertCenter()       // bindings de campana, cierre, "Ver todas", popover
export function openAlertDrawer()       // tambien la usa #btn-alerts-view-all
export function closeAlertDrawer()
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: alertCenter.js — badge, cajón y agrupación; retirada de loadActiveAlerts de dashboard.js</name>
  <files>frontend/js/components/alertCenter.js, frontend/js/views/dashboard.js</files>
  <read_first>
    - frontend/js/views/dashboard.js líneas 1-30 (`showToast`, `TOAST_STYLES`) y 230-290 (`_statusRow` compartido, `loadActiveAlerts` completo y su `setInterval(loadActiveAlerts, 5000)` de la línea 290) — se muda y se adapta
    - frontend/js/components/personGallery.js líneas 79-125 (patrón de apertura/cierre de modal: `classList.remove('hidden')`, cierre por botón y por clic en el backdrop)
    - frontend/js/components/eventCard.js líneas 172-188 (manejador global de `Escape` ya existente para modales — el cajón debe sumarse a ese patrón)
    - frontend/js/api.js (`apiFetch`)
    - frontend/index.html (ids de 30-07: `#btn-alert-center`, `#alert-badge`, `#alert-drawer`, `#alert-groups`, `#alert-empty`, `#alert-count-hero`, `#alert-footer-count`, `#alert-checked-at`, `#btn-alert-close`, `#btn-alerts-view-all`; y los de la Fase 29 `#alerts-active-list`, `#alerts-active-empty`, `#alerts-active-checked-at`)
    - .planning/phases/30-event-timeline-y-centro-de-alertas/30-UI-SPEC.md sección "Centro de alertas" (disparador, contador héroe, agrupación, grupos silenciados, pie) y "Accesibilidad" (`role="dialog"`, foco atrapado, `Escape`, foco de vuelta a la campana)
  </read_first>
  <action>
    **1) Crear `frontend/js/components/alertCenter.js`:**

    - `loadAlerts()`: `apiFetch('/api/v2/alerts?hours=24')` y con la respuesta:
      - **Badge**: `#alert-badge` muestra `active_count`, o `"9+"` si es ≥ 10; se oculta (`hidden`) si es 0 — nunca se pinta un "0". Clase de fondo `bg-red-500` si `critical_count > 0`, `bg-amber-500` si no. Actualizar `#btn-alert-center` con `aria-label = "Centro de alertas, {N} activas"`.
      - **Contador héroe**: `#alert-count-hero`.textContent = `active_count`.
      - **Grupos**: vaciar `#alert-groups` y crear un `.alert-group` por entrada, con punto de severidad (`.sev-dot` con `--sev`), nombre de la regla en `.chip .rule-chip .mono` (o el tipo de evento en lenguaje llano si `rule_name` es `null`), descripción llana de la última ocurrencia, contador `.mono` `×{count}` y hora del último disparo. Los grupos con `muted_until` llevan la clase `muted`, un chip ámbar `.mono` `silenciada hasta {HH:MM}` y el botón "Reactivar regla" en lugar de "Silenciar" — **no se esconden**: el operador debe ver de un vistazo qué se está callando.
      - **Acciones por grupo**: "Silenciar" (solo si `mutable`) y "Ver en la línea temporal" (enlace azul).
      - `#alert-empty` visible solo si no hay grupos; `#alert-footer-count` con el recuento total y `#alert-checked-at` con `checked_at` formateado `HH:MM:SS` en `.mono`.
      - **Top-3 de la Fase 29**: rellenar `#alerts-active-list` con los tres primeros grupos NO silenciados usando `_statusRow`-equivalente (replicar el marcado que hoy genera `dashboard.js`, no importar la función privada), y `#alerts-active-empty`/`#alerts-active-checked-at` como hasta ahora. Sin `.filter()` ni `.sort()` por severidad en el navegador: el servidor ya los devuelve ordenados.
    - **XSS**: el nombre de la regla y la descripción son datos del backend — estructura estática por `innerHTML` y contenido por `textContent`, exactamente igual que en la fila de la timeline.
    - **Errores**: en fallo de `apiFetch`, mostrar `#alert-empty` y NO borrar el badge anterior; nunca un `catch {}` vacío.
    - `openAlertDrawer()`: `#alert-drawer`.classList.add('open'), guardar el elemento con foco previo, mover el foco al `#btn-alert-close` y atrapar el `Tab` dentro del panel. `closeAlertDrawer()`: quitar `open`, cerrar el popover si estaba abierto y devolver el foco a `#btn-alert-center`.
    - `bindAlertCenter()`: `#btn-alert-center` y `#btn-alerts-view-all` abren; `#btn-alert-close` y el clic sobre el backdrop (`e.target === drawer`) cierran; `keydown` con `Escape` cierra si está abierto. Al final del módulo, `setInterval(loadAlerts, 5000)` — el mismo ritmo que tenía `loadActiveAlerts`.
    - "Ver en la línea temporal": cierra el cajón y despacha `document.dispatchEvent(new CustomEvent('timeline:filter-rule', { detail: { ruleName } }))`; `timeline.js` no depende de este módulo ni al revés.

    **2) `frontend/js/views/dashboard.js`:** borrar `SEVERITY_RANK`, `loadActiveAlerts` y su `setInterval` (líneas 263-290). Si `_statusRow` deja de usarse en el fichero, conservarlo solo si otra función lo usa; en caso contrario borrarlo también. NO tocar nada más del módulo.
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/test_frontend_modules.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/js/components/alertCenter.js` existe y `wc -l` < 300
    - `grep -c "export async function loadAlerts\|export function bindAlertCenter\|export function openAlertDrawer\|export function closeAlertDrawer" frontend/js/components/alertCenter.js` → 4
    - `grep -n "'/api/v2/alerts" frontend/js/components/alertCenter.js` → ≥ 1 match
    - `grep -n "loadActiveAlerts\|SEVERITY_RANK" frontend/js/views/dashboard.js` → 0 matches
    - `grep -rn "loadActiveAlerts" frontend/js/` → 0 matches (también fuera de `dashboard.js`; el import de `app.js` se corrige en 30-10)
    - `wc -l frontend/js/views/dashboard.js` → menor que 265
    - `grep -n "alerts-active-list" frontend/js/components/alertCenter.js` → ≥ 1 match (el top-3 de la Fase 29 sigue alimentándose)
    - `grep -n "9+" frontend/js/components/alertCenter.js` → 1 match (regla del badge de dos cifras)
    - `grep -c "\${g\.\|\${group\." frontend/js/components/alertCenter.js` → 0 dentro de plantillas `innerHTML` (sin interpolación de datos del backend)
    - `grep -n "Escape" frontend/js/components/alertCenter.js` → ≥ 1 match
    - `.venv/Scripts/python.exe -m pytest tests/test_frontend_modules.py -q` en verde
  </acceptance_criteria>
  <done>El badge y el cajón se pintan desde la agrupación del servidor, los grupos silenciados quedan visibles pero atenuados, el top-3 de la Fase 29 usa la misma fuente y `dashboard.js` recupera ~28 líneas de margen.</done>
</task>

<task type="auto">
  <name>Task 2: Popover de silenciado con duración obligatoria y reactivación</name>
  <files>frontend/js/components/alertCenter.js</files>
  <read_first>
    - frontend/js/components/alertCenter.js (creado en Task 1)
    - frontend/index.html (`#alert-mute-popover`, `#alert-mute-title` y los tres botones con `data-duration="900|3600|28800"` creados en 30-07)
    - frontend/js/views/dashboard.js líneas 19-28 (`showToast(msg, type, ms)` y `TOAST_STYLES`: `info`/`success`/`error`/`warn`)
    - .planning/phases/30-event-timeline-y-centro-de-alertas/30-UI-SPEC.md tabla "Copywriting Contract" — literales exactos: título "Silenciar «{regla}»", "No verás estas alertas durante ese tiempo. Puedes reactivarla cuando quieras.", "Reactivar regla", chip "silenciada hasta {HH:MM}", toast de error "No se pudo silenciar la regla. Inténtalo de nuevo."
    - .planning/phases/30-event-timeline-y-centro-de-alertas/30-06-PLAN.md bloque `<interfaces>` (cuerpos exactos de `POST /mute` y `POST /unmute` y el 400 de duración no válida)
  </read_first>
  <action>
    Añadir a `frontend/js/components/alertCenter.js`:

    - **Apertura del popover**: al pulsar "Silenciar" en un grupo, posicionar `#alert-mute-popover` junto al botón (`getBoundingClientRect()` + `position: fixed` con `top`/`left`), poner `#alert-mute-title`.textContent = `Silenciar «{rule_name}»` (con `textContent`, el nombre viene del backend) y añadir la clase `open`. Guardar el `rule_name` en curso en una variable de módulo, no en el DOM.
    - **Confirmación implícita**: la duración ES la confirmación (D-07) — no se añade ningún `confirm()` ni ningún diálogo extra a la fase. Al pulsar un botón con `data-duration`:
      ```javascript
      await apiFetch('/api/v2/alerts/mute', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({ rule_name: _muteTarget, duration_secs: Number(btn.dataset.duration) }),
      });
      ```
      Éxito: cerrar el popover y `await loadAlerts()` (el estado real siempre se relee del servidor, la UI nunca miente sobre él — mismo criterio que `detectionClasses.js` de la Fase 27). Error: `showToast('No se pudo silenciar la regla. Inténtalo de nuevo.', 'error')` y recargar igualmente con `loadAlerts()`.
    - **Reactivar**: el botón "Reactivar regla" de un grupo silenciado hace `POST /api/v2/alerts/unmute` con `{ rule_name }` y luego `loadAlerts()`. En error, mismo patrón de toast + recarga.
    - **Cierre del popover**: clic fuera, `Escape` (antes de cerrar el cajón: si el popover está abierto, `Escape` lo cierra solo a él) y al cerrar el cajón.
    - Vigilar el tope de 300 líneas del módulo; si no cabe, extraer el popover a `frontend/js/components/alertMute.js` antes que superarlo.
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/test_frontend_modules.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "alerts/mute" frontend/js/components/alertCenter.js` → 1 match; `grep -n "alerts/unmute" frontend/js/components/alertCenter.js` → 1 match
    - `grep -n "duration_secs" frontend/js/components/alertCenter.js` → 1 match, tomado de `dataset.duration` (nunca un valor libre escrito por el usuario)
    - `grep -n "confirm(" frontend/js/components/alertCenter.js` → 0 matches (D-07: ningún `confirm()` nuevo en la fase)
    - `grep -c "loadAlerts()" frontend/js/components/alertCenter.js` → ≥ 4 (tras silenciar, tras reactivar y en los dos caminos de error: el estado siempre se relee del servidor)
    - `grep -n "No se pudo silenciar la regla" frontend/js/components/alertCenter.js` → 1 match (literal del contrato de copy)
    - `grep -n "Silenciar «" frontend/js/components/alertCenter.js` → 1 match
    - `wc -l frontend/js/components/alertCenter.js` < 300 (igual para `alertMute.js` si se llegó a extraer)
    - `.venv/Scripts/python.exe -m pytest tests/test_frontend_modules.py -q` en verde
  </acceptance_criteria>
  <done>Silenciar exige elegir duración de la lista blanca, sin diálogos nativos; el resultado siempre se relee del servidor y los errores se avisan con toast sin dejar la UI mintiendo.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `GET /api/v2/alerts` → DOM del cajón | Nombres de regla (texto del operador en `rules.yaml`) y descripciones llegan al marcado |
| Cajón → `POST /api/v2/alerts/mute` | Una acción de la UI cambia estado de configuración compartido |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-31 | Tampering (XSS almacenado) | `rule_name` en el chip del grupo y en el título del popover | mitigate | `textContent` obligatorio, estructura estática por `innerHTML`; criterio de aceptación grep-verificable de que no hay `${g.…}` en plantillas |
| T-30-32 | Tampering | Duración de silenciado manipulada desde la consola | mitigate | La lista blanca real vive en el servidor (`MUTE_DURATIONS`, 30-06): un valor arbitrario recibe 400. El cliente solo ofrece los tres botones |
| T-30-33 | Repudiation | Silenciar sin que el operador sepa qué está callado | mitigate | Los grupos silenciados NO se ocultan: quedan atenuados con chip "silenciada hasta {HH:MM}" y acción de reactivar; además el servidor emite `CONFIG_CHANGED` |
| T-30-34 | Denial of Service (funcional) | UI mostrando un estado de silenciado que el servidor no aplicó | mitigate | Tras cualquier mutación —con éxito o con error— se vuelve a leer el estado real con `loadAlerts()`, patrón ya usado por `detectionClasses.js` en la Fase 27 |
</threat_model>

<verification>
- `.venv/Scripts/python.exe -m pytest tests/test_frontend_modules.py -q` — en verde
- `wc -l frontend/js/views/dashboard.js frontend/js/components/alertCenter.js` — ambos por debajo de 300
- Comprobación manual anotada en el SUMMARY: la campana muestra el badge, el cajón abre y cierra con `Escape` y el foco vuelve a la campana
</verification>

<success_criteria>
- El badge refleja `active_count` con el color de la severidad más grave y no se pinta con 0
- El cajón muestra un grupo por regla con recuento, hora y estado de silenciado
- Silenciar y reactivar funcionan contra el backend y la UI relee el estado real
- El top-3 de la Fase 29 se alimenta de la misma agrupación, sin filtrar en el navegador
- `dashboard.js` baja de 290 líneas y ningún módulo supera 300
</success_criteria>

<output>
After completion, create `.planning/phases/30-event-timeline-y-centro-de-alertas/30-09-SUMMARY.md`
</output>
