---
phase: 30
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/events/rules.py
  - backend/main.py
  - tests/test_event_bus.py
autonomous: true
requirements: [OPS-10, OPS-11]

must_haves:
  truths:
    - "Un evento que casa una regla se persiste en SQLite CON los nombres de esa regla dentro de payload.rules"
    - "El mensaje WebSocket del evento sale siempre DESPUES del INSERT y ya lleva payload.rules"
    - "Las acciones lentas de una regla (Telegram/webhook/grabacion) nunca bloquean ni retrasan la persistencia del evento"
    - "RuleEngine.evaluate() sigue comportandose exactamente igual que antes del refactor"
  artifacts:
    - path: "backend/events/rules.py"
      provides: "RuleEngine.match() puro/sincrono + RuleEngine.run_actions() + evaluate() como wrapper"
      contains: "def match"
    - path: "backend/main.py"
      provides: "make_event_pipeline() (suscriptor unico ordenado del bus) y _broadcast_event() (mensaje type:\"event\" por /ws)"
      contains: "make_event_pipeline"
    - path: "tests/test_event_bus.py"
      provides: "tests de la carrera resuelta: rules_persisted y broadcast_order"
  key_links:
    - from: "backend/main.py::make_event_pipeline"
      to: "backend/events/rules.py::RuleEngine.match"
      via: "llamada sincrona antes del insert, mutando event.payload['rules']"
      pattern: "rule_engine\\.match\\("
    - from: "backend/main.py::make_event_pipeline"
      to: "backend/main.py::_broadcast_event"
      via: "await tras el insert"
      pattern: "\"type\": \"event\""
    - from: "backend/main.py::lifespan"
      to: "backend/events/bus.py::EventBus.subscribe"
      via: "un unico subscribe(\"event_pipeline\", ...) en lugar de los cuatro actuales"
      pattern: "subscribe\\(\"event_pipeline\""
---

<objective>
Colapsar los cuatro suscriptores concurrentes del `EventBus` en un único suscriptor ordenado (`_event_pipeline`) que evalúa las reglas ANTES de persistir, escribe los nombres de regla en `event.payload["rules"]` y emite un mensaje nuevo `{"type": "event", ...}` por el `/ws` legacy después del `INSERT`.

Purpose: es la pieza central de la fase. Resuelve la condición de carrera real de D-14/Hallazgo 3 (hoy `_apply_rules` muta el evento *después* de que `_persist_event` lo haya escrito, así que `payload.rules` se pierde SIEMPRE, no "a veces") y habilita a la vez OPS-11 (qué regla disparó, persistido y auditable) y OPS-10 (el evento tipado completo viaja por el canal que el frontend realmente escucha — hoy `/ws` solo recibe `LINE_CROSSED` en formato legacy sin `id`, sin `severity`, sin `zone_id`).
Output: `RuleEngine.match()`/`run_actions()`, `make_event_pipeline()`, `_broadcast_event()` y los dos tests que demuestran que la carrera está cerrada.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-event-timeline-y-centro-de-alertas/30-CONTEXT.md
@.planning/phases/30-event-timeline-y-centro-de-alertas/30-RESEARCH.md
@.planning/phases/30-event-timeline-y-centro-de-alertas/30-PATTERNS.md
@CLAUDE.md
</context>

<interfaces>
Contrato del mensaje WebSocket nuevo que este plan produce y que consumen 30-08/30-10 literalmente:

```json
{
  "type": "event",
  "event": { "id": "6f0…", "type": "INTRUSION", "camera_id": "cam1", "ts": "2026-08-20T18:30:00",
             "severity": "critical", "track_id": 12, "person_id": null, "person_name": null,
             "zone_id": "jardin", "confidence": 0.91, "bbox": [10,20,30,40],
             "snapshot_path": null, "recording_id": null,
             "payload": { "rules": ["Intrusión nocturna"], "_emitted_at": 123.4 } },
  "media": { "recording_id": null, "clip_url": null, "thumbnail_url": null, "snapshot_url": null }
}
```

El bloque `event` es literalmente `json.loads(event.model_dump_json())` — la misma forma que devuelve `GET /api/v2/events`. `media` va como bloque hermano (nunca dentro de `event`) porque el `Event` de Pydantic es el contrato persistido y no debe llevar campos de presentación (`repositories.py:1-5`). En este plan `media` sale siempre con las cuatro claves a `null`; 30-04 rellena `snapshot_url`.

Firma pública nueva de `RuleEngine`:
```python
def match(self, event: Event) -> list[str]                     # puro, SIN await, sin E/S
async def run_actions(self, event: Event, fired: list[str]) -> None
async def evaluate(self, event: Event) -> list[str]            # wrapper: match + run_actions
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Partir RuleEngine.evaluate() en match() (puro) + run_actions()</name>
  <files>backend/events/rules.py, tests/test_rule_engine.py</files>
  <read_first>
    - backend/events/rules.py completo (194 líneas): `class Rule` línea 46, `_matches()` líneas 72-102 (ya puro y sync), `_debounce_key`/`_is_debounced`/`_purge_stale` líneas 147-167, y `evaluate()` líneas 169-193 — el bucle exacto que hay que partir
    - tests/test_rule_engine.py completo — este fichero NO se modifica; es la red de seguridad del refactor y debe seguir verde SIN tocarlo
    - .planning/phases/30-event-timeline-y-centro-de-alertas/30-RESEARCH.md sección "Pattern 2: Suscriptor único ordenado (la solución a D-14)", subsección "Y el corte en RuleEngine"
  </read_first>
  <behavior>
    - `match()` devuelve los mismos nombres que devolvía `evaluate()` para el mismo evento, sin ejecutar ninguna acción (verificable con un registry cuyo handler incremente un contador: tras `match()` el contador sigue a 0).
    - `match()` hace el bookkeeping de debounce: llamarlo dos veces seguidas con el mismo evento y una regla con `debounce_secs > 0` devuelve `[nombre]` la primera vez y `[]` la segunda.
    - `run_actions(event, fired)` ejecuta las acciones únicamente de las reglas cuyo `name` está en `fired`, en el orden de `self._rules`.
    - `evaluate()` sigue devolviendo la lista de nombres y ejecutando las acciones (los tests existentes de `tests/test_rule_engine.py` lo prueban y deben pasar sin modificarse).
  </behavior>
  <action>
    En `backend/events/rules.py`, sustituir el método `evaluate()` (líneas 169-193) por estos TRES métodos, en este orden, conservando el resto del fichero intacto:

    ```python
    def match(self, event: Event) -> list[str]:
        """Reglas que casan y no estan en debounce. Puro: sin await, sin efectos externos.

        El bookkeeping de debounce SI ocurre aqui — una regla "casada" cuenta como
        disparada aunque sus acciones se ejecuten despues (30-RESEARCH.md Pattern 2).
        Es sincrono a proposito: make_event_pipeline() lo llama antes del INSERT,
        y un await ahi reabriria la carrera que esta fase viene a cerrar (D-14).
        """
        self._purge_stale(event.ts)
        fired: list[str] = []
        for rule in self._rules:
            if not rule.enabled:
                continue
            if not _matches(rule.when, event):
                continue
            if self._is_debounced(rule, event):
                continue
            self._last_fired[self._debounce_key(rule, event)] = event.ts
            fired.append(rule.name)
        return fired

    async def run_actions(self, event: Event, fired: list[str]) -> None:
        """Ejecuta las acciones de las reglas ya casadas por match(). Lento: Telegram,
        webhook, grabacion. Nunca debe correr antes del INSERT (30-RESEARCH.md Hallazgo 3)."""
        for rule in self._rules:
            if rule.name not in fired:
                continue
            for action in rule.actions:
                handler = self._registry.get(action.type)
                if handler is None:
                    logger.error("Accion desconocida %r en regla %r", action.type, rule.name)
                    continue
                try:
                    await handler(event, action, rule.name)
                except Exception:
                    logger.exception("Accion %r de regla %r fallo", action.type, rule.name)

    async def evaluate(self, event: Event) -> list[str]:
        """Compatibilidad: match + run_actions, misma firma y semantica que antes del corte."""
        fired = self.match(event)
        await self.run_actions(event, fired)
        return fired
    ```

    NO tocar `tests/test_rule_engine.py`: que siga verde sin modificaciones es precisamente la prueba de que el corte no cambió el comportamiento.
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/test_rule_engine.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "def match" backend/events/rules.py` → exactamente 1 línea, y NO empieza por `async def`
    - `grep -n "async def run_actions" backend/events/rules.py` → exactamente 1 línea
    - `grep -c "await" backend/events/rules.py` no aumenta dentro de `match()`: `sed -n '/def match/,/def run_actions/p' backend/events/rules.py | grep -c await` → 0
    - `grep -n "logger.error(\"Accion desconocida" backend/events/rules.py` → 1 match, dentro de `run_actions` (nunca dentro de `match`)
    - `git diff --stat tests/test_rule_engine.py` → sin cambios (0 líneas modificadas)
    - `.venv/Scripts/python.exe -m pytest tests/test_rule_engine.py -q` en verde
  </acceptance_criteria>
  <done>`match()` es síncrono y sin efectos, `run_actions()` conserva el logging de acción desconocida/fallida, `evaluate()` es un wrapper, y la suite de reglas existente pasa sin haberse tocado.</done>
</task>

<task type="auto">
  <name>Task 2: make_event_pipeline() y _broadcast_event() — suscriptor único ordenado</name>
  <files>backend/main.py</files>
  <read_first>
    - backend/main.py líneas 79-123 (`_broadcast()`, `_broadcast_v2()`, `_broadcast_v1_compat()` — se reutilizan SIN modificarlos; solo cambia quién los llama y en qué orden)
    - backend/main.py líneas 260-310 (`lifespan()`: creación de `event_bus`, `rule_engine`, `event_repo` y los cuatro `event_bus.subscribe(...)` de las líneas 303-306 que hay que sustituir)
    - backend/events/bus.py líneas 60-90 (`_consume()` con `asyncio.ensure_future` por suscriptor — la razón de que el orden entre suscriptores no exista)
    - backend/events/types.py líneas 13-80 (contrato `Event`, `payload` es `dict`)
    - .planning/phases/30-event-timeline-y-centro-de-alertas/30-RESEARCH.md secciones "Pattern 2", "Hallazgo 2" y "Hallazgo 3" (tabla de riesgos y mitigaciones)
  </read_first>
  <action>
    **1) Nuevo `_broadcast_event()`** — en `backend/main.py`, inmediatamente después de `_broadcast_v1_compat()` (tras la línea 123, antes de `_camera_watchdog`), añadir:

    ```python
    async def _broadcast_event(event: Event, media: dict | None = None) -> None:
        """Emite el evento tipado completo por el /ws legacy (Fase 30, OPS-10).

        El /ws que usa el frontend solo recibia LINE_CROSSED en formato v1 sin id,
        severity ni zone_id (_broadcast_v1_compat): insuficiente para la linea temporal.
        Mismo criterio que la Fase 29 con type:"tracks" — un mensaje mas en el socket
        que ya existe, nunca una segunda conexion (30-RESEARCH.md Hallazgo 2).
        NO marca latency_tracker.mark_ws_sent(): eso ya lo hace _broadcast_v2 y
        marcarlo dos veces contaminaria la metrica EVENT_TO_WS.
        """
        await _broadcast({
            "type": "event",
            "event": json.loads(event.model_dump_json()),
            "media": media or {
                "recording_id": None,
                "clip_url": None,
                "thumbnail_url": None,
                "snapshot_url": None,
            },
        })
    ```

    **2) Nueva fábrica `make_event_pipeline()`** — añadir justo debajo de `_broadcast_event`:

    ```python
    def make_event_pipeline(event_repo, rule_engine, *, broadcast_event=None,
                            broadcast_v2=None, broadcast_v1=None, schedule=None):
        """Suscriptor UNICO del bus: dentro de una sola corrutina el orden si esta
        garantizado por el lenguaje, no por el scheduler (30-RESEARCH.md Hallazgo 3).

        Sustituye a los cuatro subscribe() concurrentes de la Fase 19. Los parametros
        inyectables existen para poder probar la secuencia sin levantar la app entera.
        """
        broadcast_event = broadcast_event or _broadcast_event
        broadcast_v2 = broadcast_v2 or _broadcast_v2
        broadcast_v1 = broadcast_v1 or _broadcast_v1_compat
        schedule = schedule or asyncio.ensure_future

        async def _event_pipeline(event: Event) -> None:
            fired: list[str] = []
            try:
                fired = rule_engine.match(event)
                if fired:
                    event.payload["rules"] = fired      # mutacion ANTES de cualquier lectura
            except Exception:
                logger.exception("RuleEngine.match failed for event %s", event.id)

            try:
                await event_repo.insert(event)          # la fila ya lleva payload.rules
            except Exception:
                logger.exception("Failed to persist event %s", event.id)

            try:
                await broadcast_event(event)            # /ws  {"type": "event", ...}
                await broadcast_v2(event)               # /api/v2/ws — contrato v2 intacto
                schedule(broadcast_v1(event))           # legacy: consulta la DB, no bloquear
            except Exception:
                logger.exception("Broadcast failed for event %s", event.id)

            if fired:
                schedule(rule_engine.run_actions(event, fired))  # lentas: nunca bloquean 2 ni 3

        return _event_pipeline
    ```

    **3) Sustituir los suscriptores en `lifespan()`** — borrar las funciones locales `_persist_event` (líneas 287-291) y `_apply_rules` (líneas 293-297), el comentario de las líneas 299-302 y los cuatro `event_bus.subscribe(...)` (líneas 303-306), y dejar en su lugar exactamente:

    ```python
        # Un unico suscriptor ordenado (Fase 30, D-14): reglas -> payload.rules -> INSERT
        # -> broadcast -> acciones. Con cuatro suscriptores concurrentes el orden lo decidia
        # el scheduler (bus.py:69 lanza ensure_future por handler) y payload.rules se perdia
        # siempre, porque _apply_rules mutaba el evento despues del INSERT.
        event_bus.subscribe("event_pipeline", make_event_pipeline(event_repo, rule_engine))
    ```

    Verificar que `json` y `asyncio` ya están importados a nivel de módulo en `backend/main.py` (lo están); no añadir imports nuevos.
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/test_stream.py tests/test_event_bus.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "event_bus.subscribe(" backend/main.py` → exactamente 1
    - `grep -n "subscribe(\"event_pipeline\"" backend/main.py` → 1 match
    - `grep -n "_persist_event\|_apply_rules" backend/main.py` → 0 matches (las funciones locales desaparecen)
    - `grep -n '"type": "event"' backend/main.py` → 1 match, dentro de `_broadcast_event`
    - `grep -n "mark_ws_sent" backend/main.py` → sigue apareciendo solo dentro de `_broadcast_v2` (1 match)
    - En el cuerpo de `_event_pipeline`, `event.payload["rules"] = fired` aparece ANTES de `await event_repo.insert(event)`: `sed -n '/async def _event_pipeline/,/return _event_pipeline/p' backend/main.py | grep -n "payload\[.rules.\]\|event_repo.insert"` muestra la asignación con número de línea menor que el insert
    - Hay exactamente 3 bloques `try:` dentro de `_event_pipeline` (granularidad por paso, no un try único): `sed -n '/async def _event_pipeline/,/return _event_pipeline/p' backend/main.py | grep -c "try:"` → 3
    - `.venv/Scripts/python.exe -m pytest tests/test_stream.py -q` en verde (la app sigue arrancando)
  </acceptance_criteria>
  <done>El bus tiene un único suscriptor; las reglas se evalúan y se escriben en `payload` antes del INSERT; el broadcast sale después; las acciones y `_broadcast_v1_compat` van en fire-and-forget.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Tests de la carrera cerrada en tests/test_event_bus.py</name>
  <files>tests/test_event_bus.py</files>
  <read_first>
    - tests/test_event_bus.py completo (fixtures y estilo `TEST_*` del fichero; `pytest.ini` fija `python_functions = TEST_*` y `asyncio_mode = auto`)
    - tests/test_repositories.py líneas 1-40 (fixture `db`: engine async sobre `tmp_path` — patrón a reutilizar si el test necesita un `EventRepo` real)
    - backend/events/rules.py (ya modificado en Task 1) y backend/main.py `make_event_pipeline` (Task 2)
    - backend/events/rules.py líneas 40-70 (`class Rule` y `When`: campos exactos para construir una regla en el test sin YAML)
  </read_first>
  <behavior>
    - `TEST_rules_are_persisted_in_payload_before_insert`: con un `EventRepo` real sobre SQLite temporal y un `RuleEngine` con una regla que casa el evento, ejecutar el pipeline devuelto por `make_event_pipeline(repo, engine, broadcast_event=AsyncMock(), broadcast_v2=AsyncMock(), broadcast_v1=lambda e: None, schedule=collector)`; después `await repo.get(event.id)` debe devolver una fila cuyo `payload["rules"] == ["<nombre de la regla>"]`. Es el test que hoy fallaría con los cuatro suscriptores.
    - `TEST_ws_event_broadcast_happens_after_insert_and_carries_rules`: el doble de `broadcast_event` consulta `await repo.get(event.id)` en el momento de ser llamado; debe encontrar la fila ya escrita (read-your-writes) y el `event` recibido debe traer `payload["rules"]` no vacío.
    - `TEST_slow_rule_actions_do_not_block_persistence`: con un `run_actions` que duerme (o un `schedule` que solo colecciona corrutinas sin ejecutarlas), el `await` del pipeline termina y la fila ya está en la base de datos aunque las acciones no se hayan ejecutado todavía; después de ejecutar las corrutinas coleccionadas, las acciones sí corren.
    - `TEST_match_failure_does_not_prevent_persistence`: con un `rule_engine.match` que lanza `RuntimeError`, el evento se persiste igualmente y `payload` no contiene la clave `rules`.
  </behavior>
  <action>
    Añadir los 4 tests de `<behavior>` al final de `tests/test_event_bus.py`. Patrón obligatorio para el `schedule` inyectado (evita `RuntimeWarning: coroutine was never awaited`):

    ```python
    scheduled: list = []

    def _collect(coro):
        scheduled.append(coro)
        return coro
    ...
    pipeline = make_event_pipeline(repo, engine, broadcast_event=fake_broadcast,
                                   broadcast_v2=_noop, broadcast_v1=_noop, schedule=_collect)
    await pipeline(event)
    for coro in scheduled:      # las corrutinas diferidas se ejecutan explicitamente
        await coro
    ```

    donde `async def _noop(event): return None`. Construir el `Event` con `backend.events.types.Event(...)` directamente (uuid4 en `id`, `ts=datetime.datetime.now()`), y el `RuleEngine` con `RuleEngine([Rule(name="Intrusión nocturna", when=When(event="INTRUSION"), actions=[], enabled=True, debounce_secs=0)], registry=ActionRegistry())` — leer la firma exacta de `Rule`/`When` en `backend/events/rules.py` antes de escribirla, no asumirla.

    Importar `make_event_pipeline` con `from backend.main import make_event_pipeline` dentro de la función de test (no a nivel de módulo) si el fichero no importa ya `backend.main`, para no arrastrar el coste de importar la app en el resto del fichero.
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/test_event_bus.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^def TEST_\|^async def TEST_" tests/test_event_bus.py` aumenta exactamente en 4 respecto al valor previo al plan
    - `grep -n "TEST_rules_are_persisted_in_payload_before_insert" tests/test_event_bus.py` → 1 match
    - `grep -n "payload\[.rules.\]" tests/test_event_bus.py` → al menos 2 matches (aserciones sobre la fila leída de la BD)
    - Ningún test nuevo usa `asyncio.ensure_future` directamente (todos inyectan `schedule=`): `grep -n "ensure_future" tests/test_event_bus.py` → 0 matches
    - `.venv/Scripts/python.exe -m pytest tests/test_event_bus.py -q` en verde, sin warnings de "coroutine was never awaited": `.venv/Scripts/python.exe -m pytest tests/test_event_bus.py -q -W error::RuntimeWarning` también en verde
  </acceptance_criteria>
  <done>Cuatro tests nuevos verdes que fijan el contrato de orden: reglas → payload → INSERT → broadcast → acciones, con aislamiento de fallos por paso.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser ↔ `/ws` (WebSocket) | El cliente ya autenticado empieza a recibir el `Event` tipado completo (severidad, zona, `track_id`, `person_id`) en lugar de solo el resumen `detection` |
| `rules.yaml` → `payload.rules` → fila persistida | El nombre de regla lo escribe el operador en un fichero local y acaba almacenado y servido a otros navegadores |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-01 | Information Disclosure | Mensaje `type:"event"` sobre `/ws` | mitigate | El socket ya exige `verify_ws_token` en el `accept()` (`backend/main.py:787`, `TEST_vuln_05_ws_token_expires_after_ttl`); no se abre superficie nueva. `person_name` viaja solo en memoria, ya expuesto hoy por `_broadcast_v1_compat` |
| T-30-02 | Tampering (XSS almacenado) | `payload.rules` con el nombre de regla llegando al navegador | mitigate | El backend nunca renderiza; la mitigación vive en 30-08 (`textContent`, nunca `innerHTML` interpolado — CodeQL `js/xss`). Aquí solo se documenta el origen del dato |
| T-30-03 | Denial of Service | Una acción de regla colgada (webhook sin timeout) | mitigate | `run_actions` va DESPUÉS del `INSERT` y en fire-and-forget (`schedule(...)`): un webhook colgado ya no puede impedir que el evento se persista ni que llegue al navegador. Es una mejora sobre el estado actual, no una regresión |
| T-30-04 | Repudiation | Reglas que disparan sin dejar rastro | mitigate | `payload.rules` persistido convierte "qué regla disparó qué" en historial auditable (base de OPS-11) |
</threat_model>

<verification>
- `.venv/Scripts/python.exe -m pytest tests/test_rule_engine.py tests/test_event_bus.py tests/test_stream.py -q` — los tres ficheros en verde
- `git diff --stat tests/test_rule_engine.py` — vacío (el refactor no necesitó tocar sus tests)
- `.venv/Scripts/python.exe -m pytest tests/ -q` — suite completa sin regresiones
</verification>

<success_criteria>
- `RuleEngine.match()` es síncrono y sin efectos externos; `run_actions()` conserva el logging original; `evaluate()` sigue siendo compatible
- El `EventBus` tiene un único suscriptor (`event_pipeline`) y el orden reglas → persistencia → broadcast → acciones está garantizado por construcción
- Un evento que casa una regla se lee de SQLite con `payload["rules"]` poblado
- El `/ws` legacy emite `{"type": "event", "event": {...}, "media": {...}}` después del INSERT
- Suite completa en verde
</success_criteria>

<output>
After completion, create `.planning/phases/30-event-timeline-y-centro-de-alertas/30-01-SUMMARY.md`
</output>
