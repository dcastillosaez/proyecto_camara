---
phase: 30
plan: 04
type: execute
wave: 2
depends_on: ["30-01"]
files_modified:
  - backend/config.py
  - backend/api/v2/deps.py
  - backend/main.py
  - tests/test_config.py
  - tests/test_snapshots.py
autonomous: true
requirements: [OPS-07, OPS-08]

must_haves:
  truths:
    - "Un evento con bbox deja escrita en disco una miniatura recortada de la escena en ese momento"
    - "La ruta del snapshot queda en la fila del evento y se sirve por HTTP como fichero estatico"
    - "La escritura de la imagen nunca ocurre dentro del event loop"
    - "Los snapshots se limitan por frecuencia por track y se borran solos pasada la retencion"
  artifacts:
    - path: "backend/config.py"
      provides: "snapshot_enabled, snapshot_dir, snapshot_max_width, snapshot_min_interval_secs, snapshot_retention_days + validador de ruta"
      contains: "snapshot_dir"
    - path: "backend/main.py"
      provides: "_capture_event_snapshot(), _purge_old_snapshots(), mount /snapshots y cableado del hook en make_event_pipeline"
      contains: "_capture_event_snapshot"
    - path: "backend/api/v2/deps.py"
      provides: "snapshot_url() — traduccion ruta en disco -> URL publica, compartida por main.py y el router de 30-05"
      contains: "def snapshot_url"
    - path: "tests/test_snapshots.py"
      provides: "tests del recorte, del throttle, del to_thread y de la retencion (fichero nuevo)"
  key_links:
    - from: "backend/main.py::make_event_pipeline"
      to: "backend/main.py::_capture_event_snapshot"
      via: "snapshot_hook invocado antes del INSERT, con try/except propio"
      pattern: "snapshot_hook"
    - from: "backend/main.py::_capture_event_snapshot"
      to: "cv2.imwrite"
      via: "await asyncio.to_thread(...) — nunca llamada directa desde la corrutina"
      pattern: "to_thread"
    - from: "backend/main.py app.mount"
      to: "settings.snapshot_dir"
      via: "StaticFiles bajo /snapshots, igual que /gallery y /clips"
      pattern: "\"/snapshots\""
---

<objective>
Crear la imagen del evento: recortar el bbox del último frame en el momento en que se publica el evento, escribirla en `data/snapshots/{YYYYMMDD}/{event_id}.jpg` fuera del event loop, dejar la ruta en `event.snapshot_path` y servirla por `/snapshots`.

Purpose: hoy `snapshot_path` **nunca se escribe** — no hay `_snapshot_hook` cableado en ningún sitio y `grep imwrite` solo devuelve la galería de personas ya identificadas y la miniatura de clip (30-RESEARCH.md Hallazgo 4). Sin esta pieza, la miniatura del criterio de éxito 1 caería al marcador en casi todas las filas y "Marcar como persona" (criterio 5) no podría precargar el recorte del evento, solo el frame actual de la cámara. Es la opción 1 recomendada por el research, acotada con throttle y retención.
Output: cinco ajustes de configuración, la captura con recorte, el mount estático, la retención diaria y sus tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-event-timeline-y-centro-de-alertas/30-CONTEXT.md
@.planning/phases/30-event-timeline-y-centro-de-alertas/30-RESEARCH.md
@.planning/phases/30-event-timeline-y-centro-de-alertas/30-PATTERNS.md
@.planning/phases/30-event-timeline-y-centro-de-alertas/30-UI-SPEC.md
@CLAUDE.md
</context>

<interfaces>
```python
# backend/main.py
async def _capture_event_snapshot(event: Event) -> str | None
    # devuelve la ruta relativa en disco ("data/snapshots/20260820/<uuid>.jpg") o None

async def _purge_old_snapshots(retention_days: int) -> int
    # borra subdirectorios YYYYMMDD anteriores al corte; devuelve nº de ficheros borrados

# backend/api/v2/deps.py
def snapshot_url(snapshot_path: str | None) -> str | None
    # "data/snapshots/20260820/x.jpg" -> "/snapshots/20260820/x.jpg";  None -> None
```

Bloque `media` que emite `_broadcast_event` tras este plan (el resto de claves las rellena el router en 30-05, no el WS):
```json
{"recording_id": null, "clip_url": null, "thumbnail_url": null, "snapshot_url": "/snapshots/20260820/6f0….jpg"}
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Ajustes de configuración del snapshot con validación de ruta</name>
  <files>backend/config.py, tests/test_config.py</files>
  <read_first>
    - backend/config.py líneas 95-115 (bloque de `clips_dir`/grabación — molde de dónde y cómo se declaran los ajustes de directorio)
    - backend/config.py líneas 230-260 (`@field_validator("reid_model_path")` con `_PROJECT_ROOT`: patrón EXACTO de validación de contención de rutas, SEC-16; y `gallery_dir` en la línea 260)
    - backend/config.py líneas 1-20 y 365-380 (imports, `class Settings(BaseSettings)`, `@lru_cache` de `get_settings`)
    - tests/test_config.py (tests de validadores existentes: estilo `TEST_*`, cómo se instancian `Settings` con valores inválidos y se espera `ValidationError`)
  </read_first>
  <behavior>
    - `TEST_snapshot_defaults`: `Settings()` devuelve `snapshot_enabled is True`, `snapshot_dir == "data/snapshots"`, `snapshot_max_width == 320`, `snapshot_min_interval_secs == 5.0`, `snapshot_retention_days == 30`.
    - `TEST_snapshot_dir_outside_project_is_rejected`: `Settings(snapshot_dir="C:/Windows/Temp")` (o `/etc` en POSIX) lanza `ValidationError`.
    - `TEST_snapshot_dir_traversal_is_rejected`: `Settings(snapshot_dir="data/../../secretos")` lanza `ValidationError`.
    - `TEST_snapshot_max_width_range`: `Settings(snapshot_max_width=0)` y `Settings(snapshot_max_width=5000)` lanzan `ValidationError`; `320` es válido.
  </behavior>
  <action>
    En `backend/config.py`, dentro de `class Settings`, justo después del bloque de `gallery_dir` (línea ~260), añadir:

    ```python
        # --- Snapshot de evento (Fase 30, OPS-07/OPS-08) ---
        # snapshot_path existia en el contrato Event desde la Fase 19 pero NADIE lo escribia:
        # sin esto la miniatura de la linea temporal cae siempre al marcador y "Marcar como
        # persona" no puede precargar el recorte del evento (30-RESEARCH.md Hallazgo 4).
        snapshot_enabled: bool = True
        snapshot_dir: str = "data/snapshots"
        snapshot_max_width: int = 320          # px; el recorte se reescala si es mas ancho
        snapshot_min_interval_secs: float = 5.0  # throttle por (camera_id, track_id)
        snapshot_retention_days: int = 30
    ```

    Añadir un validador replicando literalmente el patrón de `validate_reid_model_path` (leerlo antes de escribirlo — usa `_PROJECT_ROOT` y `Path(...).resolve()`):

    ```python
        @field_validator("snapshot_dir")
        @classmethod
        def validate_snapshot_dir(cls, v: str) -> str:
            """El directorio de snapshots debe quedar dentro del proyecto (SEC-16).

            Se sirve por StaticFiles bajo /snapshots: un valor fuera del arbol del
            proyecto convertiria ese mount en una fuga de ficheros arbitrarios.
            """
            ...  # misma mecanica de resolve() + contencion en _PROJECT_ROOT que reid_model_path
    ```

    Y un validador de rangos para los numéricos, siguiendo el molde de `validate_reid_params`:
    `snapshot_max_width` en `[64, 1920]`, `snapshot_min_interval_secs` en `[0.0, 3600.0]`, `snapshot_retention_days` en `[0, 3650]` (0 = sin purga).

    Añadir los 4 tests de `<behavior>` a `tests/test_config.py`.
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/test_config.py -k snapshot -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "snapshot_" backend/config.py` → al menos 8 matches (5 ajustes + validadores)
    - `grep -n "def validate_snapshot_dir" backend/config.py` → 1 match
    - `grep -n "_PROJECT_ROOT" backend/config.py` aparece también dentro del validador nuevo: `sed -n '/def validate_snapshot_dir/,/return v/p' backend/config.py | grep -c "_PROJECT_ROOT"` → ≥1
    - `.venv/Scripts/python.exe -c "from backend.config import Settings; s=Settings(); print(s.snapshot_dir, s.snapshot_max_width, s.snapshot_retention_days)"` imprime `data/snapshots 320 30`
    - `.venv/Scripts/python.exe -m pytest tests/test_config.py -k snapshot -q` en verde (4 tests)
  </acceptance_criteria>
  <done>Los cinco ajustes existen con sus defaults, la ruta está contenida en el proyecto y los rangos numéricos se validan.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: _capture_event_snapshot() y _purge_old_snapshots()</name>
  <files>backend/main.py, tests/test_snapshots.py</files>
  <read_first>
    - backend/main.py líneas 424-445 (`_save_gallery_capture`: recorte de bbox + `cv2.imwrite` ya existente — molde del recorte, del clamp de coordenadas y del nombre de fichero)
    - backend/main.py líneas 156-175 (`_purge_local_clip_files`: molde de purga de ficheros en disco con `os.remove` y logging)
    - backend/pipeline/manager.py líneas 279-284 (`CameraPipeline.get_frame()` → último frame del broker, YA reescalado a `process_size`: las coordenadas de `event.bbox` están en ese mismo espacio) y línea 421 (`CameraManager.get(camera_id)`)
    - backend/events/engine.py línea 235 (dónde se rellena `bbox=` al publicar el evento — confirma el espacio de coordenadas)
    - backend/events/types.py (campo `snapshot_path: str | None` del contrato `Event`)
    - CLAUDE.md sección "Código y rendimiento" ("No ejecutar CPU pesado en el event loop")
  </read_first>
  <behavior>
    - `TEST_capture_snapshot_writes_cropped_jpeg`: con `camera_manager` parcheado por un doble cuyo `get(...)` devuelve un objeto con `get_frame()` que retorna un `np.zeros((240, 320, 3), np.uint8)`, y un evento con `bbox=(10, 20, 110, 140)`, la función devuelve una ruta que existe en disco bajo `tmp_path`, con extensión `.jpg`, y el fichero pesa > 0 bytes.
    - `TEST_capture_snapshot_returns_none_without_bbox`: evento con `bbox=None` → `None` y ningún fichero escrito.
    - `TEST_capture_snapshot_returns_none_when_disabled`: `snapshot_enabled=False` → `None`.
    - `TEST_capture_snapshot_throttles_per_track`: dos eventos consecutivos del mismo `(camera_id, track_id)` dentro de `snapshot_min_interval_secs` → el segundo devuelve `None`; con `track_id` distinto sí escribe.
    - `TEST_capture_snapshot_uses_to_thread`: parcheando `asyncio.to_thread` con un `AsyncMock`, la función lo invoca (prueba de que `cv2.imwrite` no se llama directamente desde la corrutina).
    - `TEST_capture_snapshot_clamps_bbox_to_frame`: `bbox=(-20, -20, 9999, 9999)` sobre un frame 320×240 → escribe igualmente, sin excepción.
    - `TEST_purge_old_snapshots_removes_old_day_dirs`: dos directorios `YYYYMMDD` (uno de hace 60 días, otro de hoy) con un fichero cada uno y `retention_days=30` → solo se borra el antiguo; el retorno es 1.
  </behavior>
  <action>
    En `backend/main.py`, añadir un estado de módulo acotado y las dos funciones, inmediatamente después de `_broadcast_event` (creado en 30-01):

    ```python
    # Throttle de snapshots por (camera_id, track_id). Acotado a proposito: sin cota seria
    # estado global creciente (CLAUDE.md "no crear estado global oculto").
    _snapshot_last: dict[tuple[str, int], float] = {}
    _SNAPSHOT_STATE_CAP = 256


    async def _capture_event_snapshot(event: Event) -> str | None:
        """Recorta el bbox del ultimo frame y lo escribe como JPEG (Fase 30, OPS-07/08).

        Devuelve la ruta relativa en disco o None. El imwrite va SIEMPRE en
        asyncio.to_thread: esta funcion la llama _event_pipeline, que es una corrutina,
        y un imwrite sincrono ahi produce micro-pausas en MJPEG y WS (Pitfall 8).
        """
        settings = get_settings()
        if not settings.snapshot_enabled or event.bbox is None:
            return None
        key = (event.camera_id, event.track_id if event.track_id is not None else -1)
        now = time.monotonic()
        last = _snapshot_last.get(key)
        if last is not None and (now - last) < settings.snapshot_min_interval_secs:
            return None
        pipeline = camera_manager.get(event.camera_id) if camera_manager is not None else None
        frame = pipeline.get_frame() if pipeline is not None else None
        if frame is None:
            return None
        if len(_snapshot_last) >= _SNAPSHOT_STATE_CAP:
            cutoff = now - max(settings.snapshot_min_interval_secs * 10, 60.0)
            for k in [k for k, t in _snapshot_last.items() if t < cutoff]:
                del _snapshot_last[k]
        _snapshot_last[key] = now

        h, w = frame.shape[:2]
        x1, y1, x2, y2 = (int(v) for v in event.bbox)
        x1 = max(0, min(x1, w - 1)); y1 = max(0, min(y1, h - 1))
        x2 = max(x1 + 1, min(x2, w)); y2 = max(y1 + 1, min(y2, h))
        crop = frame[y1:y2, x1:x2]
        if crop.size == 0:
            return None
        if crop.shape[1] > settings.snapshot_max_width:
            scale = settings.snapshot_max_width / crop.shape[1]
            crop = cv2.resize(crop, (settings.snapshot_max_width, max(1, int(crop.shape[0] * scale))))

        day_dir = Path(settings.snapshot_dir) / event.ts.strftime("%Y%m%d")
        day_dir.mkdir(parents=True, exist_ok=True)
        path = day_dir / f"{event.id}.jpg"     # nombre derivado del uuid4, nunca de entrada del usuario
        ok = await asyncio.to_thread(cv2.imwrite, str(path), crop)
        if not ok:
            logger.warning("No se pudo escribir el snapshot del evento %s", event.id)
            return None
        return path.as_posix()


    async def _purge_old_snapshots(retention_days: int) -> int:
        """Borra los directorios YYYYMMDD de snapshots anteriores al corte. Devuelve ficheros borrados."""
        import shutil
        base = Path(get_settings().snapshot_dir)
        if not base.is_dir():
            return 0
        cutoff = (datetime.datetime.now() - datetime.timedelta(days=retention_days)).strftime("%Y%m%d")
        removed = 0
        for child in base.iterdir():
            if not child.is_dir() or len(child.name) != 8 or not child.name.isdigit():
                continue
            if child.name >= cutoff:
                continue
            removed += sum(1 for _ in child.glob("*"))
            await asyncio.to_thread(shutil.rmtree, str(child), True)
        if removed:
            logger.info("Purgados %d snapshots anteriores a %s", removed, cutoff)
        return removed
    ```

    Verificar que `cv2`, `time`, `Path`, `datetime` y `asyncio` ya están importados a nivel de módulo en `backend/main.py` (lo están); no añadir imports nuevos salvo el `shutil` local.

    Crear `tests/test_snapshots.py` (fichero nuevo) con los 7 tests de `<behavior>`. Parchear `backend.main.get_settings` para inyectar un `Settings(snapshot_dir=str(tmp_path/"snaps"))` y `backend.main.camera_manager` con un doble; usar `monkeypatch.setattr(backend.main, "_snapshot_last", {})` al principio de cada test para aislar el throttle.
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/test_snapshots.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "async def _capture_event_snapshot\|async def _purge_old_snapshots" backend/main.py` → 2 matches
    - `grep -n "to_thread(cv2.imwrite" backend/main.py` → 1 match
    - `sed -n '/async def _capture_event_snapshot/,/^async def _purge_old_snapshots/p' backend/main.py | grep -c "cv2.imwrite"` → 1 (aparece solo dentro del `to_thread`, nunca como llamada directa)
    - `grep -n "_SNAPSHOT_STATE_CAP" backend/main.py` → 2 matches (definición y uso: el estado está acotado)
    - `grep -n "f\"{event.id}.jpg\"" backend/main.py` → 1 match (nombre derivado del uuid, no de entrada del usuario)
    - `tests/test_snapshots.py` existe y contiene 7 funciones `TEST_*`
    - `.venv/Scripts/python.exe -m pytest tests/test_snapshots.py -q` en verde
  </acceptance_criteria>
  <done>El recorte se escribe fuera del event loop, con clamp de coordenadas, throttle por track acotado y purga por directorio de día.</done>
</task>

<task type="auto">
  <name>Task 3: Cableado — hook en el pipeline, URL pública, mount y retención diaria</name>
  <files>backend/main.py, backend/api/v2/deps.py, tests/test_snapshots.py</files>
  <read_first>
    - backend/main.py (`make_event_pipeline` y `_broadcast_event` creados en 30-01 — se modifican aquí)
    - backend/main.py líneas 216-248 (`_purge_loop`: dónde encajar la purga nueva, con el mismo patrón `if settings.X_retention_days > 0:`)
    - backend/main.py líneas 628-638 (mounts de `/static`, `/gallery`, `/clips` — molde exacto de `mkdir(parents=True, exist_ok=True)` + `app.mount`)
    - backend/api/v2/deps.py completo (32 líneas: dónde añadir el helper compartido sin romper su docstring de módulo)
    - tests/test_snapshots.py (creado en Task 2 — se amplía aquí)
  </read_first>
  <action>
    **1) `backend/api/v2/deps.py`** — añadir al final del fichero:

    ```python
    def snapshot_url(snapshot_path: str | None) -> str | None:
        """Ruta en disco del snapshot -> URL publica servida por el mount /snapshots.

        Vive aqui (y no en main.py) porque lo necesitan main.py -- para el bloque `media`
        del mensaje WS -- y el router de eventos; importar main desde un router seria
        un ciclo de imports.
        """
        if not snapshot_path:
            return None
        from pathlib import Path
        from backend.config import get_settings
        base = Path(get_settings().snapshot_dir).as_posix().rstrip("/")
        p = Path(snapshot_path).as_posix()
        if p.startswith(base + "/"):
            p = p[len(base) + 1:]
        return "/snapshots/" + p.lstrip("/")
    ```

    **2) `backend/main.py` — hook en el pipeline.** Añadir el parámetro `snapshot_hook=None` a `make_event_pipeline(...)` y, dentro de `_event_pipeline`, insertar este bloque ENTRE el try de `rule_engine.match` y el try del `insert` (el snapshot debe estar en la fila desde la primera escritura, sin segundo UPDATE):

    ```python
            if snapshot_hook is not None and event.snapshot_path is None:
                try:
                    path = await snapshot_hook(event)
                    if path:
                        event.snapshot_path = path
                except Exception:
                    logger.exception("Snapshot capture failed for event %s", event.id)
    ```

    Y en la llamada de `lifespan()`, pasar el hook:
    ```python
        event_bus.subscribe("event_pipeline", make_event_pipeline(
            event_repo, rule_engine, snapshot_hook=_capture_event_snapshot))
    ```

    **3) `backend/main.py` — `media` en el broadcast.** En `_broadcast_event`, sustituir el `media or {...}` por un default que ya resuelva la URL del snapshot:
    ```python
        from backend.api.v2.deps import snapshot_url
        ...
            "media": media or {
                "recording_id": None,
                "clip_url": None,
                "thumbnail_url": None,
                "snapshot_url": snapshot_url(event.snapshot_path),
            },
    ```
    (el import puede ir a nivel de módulo junto al resto de imports de `backend.api.v2`).

    **4) `backend/main.py` — mount.** Junto a los mounts de `/gallery` y `/clips` (tras la línea 637):
    ```python
    _snapshots_dir = Path(_settings.snapshot_dir)
    _snapshots_dir.mkdir(parents=True, exist_ok=True)
    app.mount("/snapshots", StaticFiles(directory=str(_snapshots_dir)), name="snapshots")
    ```

    **5) `backend/main.py` — retención.** Dentro del `try` de `_purge_loop`, tras el bloque de `local_retention_days`:
    ```python
                if settings.snapshot_retention_days > 0:
                    await _purge_old_snapshots(settings.snapshot_retention_days)
    ```

    **6) Tests** — añadir a `tests/test_snapshots.py`:
    - `TEST_snapshot_url_maps_disk_path_to_public_url`: `snapshot_url("data/snapshots/20260820/x.jpg")` → `"/snapshots/20260820/x.jpg"`; `snapshot_url(None)` → `None`.
    - `TEST_pipeline_writes_snapshot_path_before_insert`: con `make_event_pipeline(repo, engine, snapshot_hook=AsyncMock(return_value="data/snapshots/20260820/x.jpg"), ...)`, tras ejecutar el pipeline la fila leída con `repo.get(event.id)` tiene `snapshot_path == "data/snapshots/20260820/x.jpg"`.
    - `TEST_pipeline_survives_snapshot_failure`: con un `snapshot_hook` que lanza, el evento se persiste igualmente y `snapshot_path is None`.
    - `TEST_snapshots_mount_serves_file`: `TestClient(app).get("/snapshots/<subdir>/<fichero>")` devuelve 200 para un fichero creado bajo `settings.snapshot_dir` (o, si el mount usa el directorio real del proyecto, comprobar que la ruta `/snapshots` existe en `app.routes`).
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/test_snapshots.py tests/test_event_bus.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "snapshot_hook" backend/main.py` → al menos 3 matches (parámetro, uso en `_event_pipeline`, paso en `lifespan`)
    - En `_event_pipeline`, la asignación de `event.snapshot_path` ocurre ANTES de `await event_repo.insert`: `sed -n '/async def _event_pipeline/,/return _event_pipeline/p' backend/main.py | grep -n "snapshot_path = path\|event_repo.insert"` muestra el snapshot con número de línea menor
    - `grep -n '"/snapshots"' backend/main.py` → 1 match (el mount)
    - `grep -n "_purge_old_snapshots" backend/main.py` → 2 matches (definición y llamada desde `_purge_loop`)
    - `grep -n "def snapshot_url" backend/api/v2/deps.py` → 1 match
    - `.venv/Scripts/python.exe -c "from backend.main import app; print([r.path for r in app.routes if getattr(r,'path','')=='/snapshots'])"` imprime `['/snapshots']`
    - `.venv/Scripts/python.exe -m pytest tests/test_snapshots.py tests/test_event_bus.py -q` en verde
  </acceptance_criteria>
  <done>El pipeline escribe el snapshot antes del INSERT, el mensaje WS ya trae `snapshot_url`, `/snapshots` sirve los ficheros y la purga diaria los limpia.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Disco (`data/snapshots/`) ↔ HTTP (`/snapshots`) | Un directorio del proyecto pasa a servirse por HTTP a cualquier cliente autenticado de la LAN |
| Frame de la cámara → fichero JPEG | Imagen con personas reconocibles queda persistida en disco sin cifrar |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-11 | Tampering (path traversal) | Nombre del fichero de snapshot | mitigate | El nombre es `{event.id}.jpg` con `event.id` = uuid4 generado en servidor, y el directorio es `{YYYYMMDD}` derivado de `event.ts`. Ninguna parte de la ruta procede del cliente |
| T-30-12 | Information Disclosure | Mount `/snapshots` apuntando fuera del proyecto por un `.env` mal puesto | mitigate | `validate_snapshot_dir` exige contención en `_PROJECT_ROOT` (mismo control que `reid_model_path`, SEC-16). La auth global de la app cubre el mount igual que `/gallery` y `/clips` |
| T-30-13 | Denial of Service (disco lleno) | Un snapshot por evento sin cota | mitigate | Throttle de `snapshot_min_interval_secs` por `(camera_id, track_id)`, reescalado a `snapshot_max_width` (JPEG de KB) y purga diaria por `snapshot_retention_days`. `snapshot_enabled=False` es la válvula de escape sin tocar código |
| T-30-14 | Denial of Service (latencia) | `cv2.imwrite` dentro del event loop | mitigate | `await asyncio.to_thread(cv2.imwrite, ...)` obligatorio, con criterio de aceptación grep-verificable |
</threat_model>

<verification>
- `.venv/Scripts/python.exe -m pytest tests/test_snapshots.py tests/test_config.py tests/test_event_bus.py -q` — en verde
- `.venv/Scripts/python.exe -m pytest tests/test_architecture.py -q` — los invariantes de pipeline siguen protegidos
- `.venv/Scripts/python.exe -m pytest tests/ -q` — suite completa sin regresiones
</verification>

<success_criteria>
- Un evento con `bbox` deja un JPEG recortado en `data/snapshots/{YYYYMMDD}/{event_id}.jpg` y su ruta en la fila persistida
- El `imwrite` nunca se ejecuta dentro del event loop
- El mensaje WS `type:"event"` trae `media.snapshot_url` resuelta
- `/snapshots` sirve los ficheros con la misma auth global que `/gallery` y `/clips`
- El throttle acota la frecuencia por track y la purga diaria acota el disco
- Suite completa en verde
</success_criteria>

<output>
After completion, create `.planning/phases/30-event-timeline-y-centro-de-alertas/30-04-SUMMARY.md`
</output>
