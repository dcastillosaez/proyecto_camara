---
phase: 30
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/storage/models.py
  - backend/storage/migrations.py
  - backend/storage/repositories.py
  - tests/test_migrations.py
  - tests/test_repositories.py
autonomous: true
requirements: [OPS-09]

must_haves:
  truths:
    - "Una base de datos v2 existente se migra a v3 creando idx_events_ts_id, y volver a migrar es un no-op"
    - "La consulta paginada acepta varios tipos de evento a la vez sin degradar el plan de consulta"
    - "La consulta paginada puede filtrar por nombre de regla disparada, leyendo payload.rules"
    - "El total de coincidencias de un filtro se puede pedir sin descargar todas las paginas"
  artifacts:
    - path: "backend/storage/models.py"
      provides: "Index idx_events_ts_id (ts DESC, id DESC) en Event.__table_args__"
      contains: "idx_events_ts_id"
    - path: "backend/storage/migrations.py"
      provides: "SCHEMA_VERSION=3 y _migrate_v2_to_v3 idempotente"
      contains: "_migrate_v2_to_v3"
    - path: "backend/storage/repositories.py"
      provides: "EventRepo.query() con type multi-valor y filtro rule; EventRepo.count() con los mismos filtros"
      contains: "async def count"
  key_links:
    - from: "backend/storage/repositories.py::EventRepo.query"
      to: "SQLite json_each(events.payload, '$.rules')"
      via: "subconsulta EXISTS con bindparam, nunca f-string"
      pattern: "json_each"
    - from: "backend/storage/migrations.py::MIGRATIONS"
      to: "backend/storage/migrations.py::_migrate_v2_to_v3"
      via: "tupla (3, ..., _migrate_v2_to_v3) añadida a la lista"
      pattern: "\\(3, "
---

<objective>
Preparar el almacenamiento para la línea temporal: índice compuesto `idx_events_ts_id (ts DESC, id DESC)` con su migración v2→v3, y extensión de `EventRepo.query()` con tipo multi-valor y filtro por regla, más un `EventRepo.count()` nuevo para el contador "{N} de {total}".

Purpose: OPS-09 exige que los filtros y la paginación se resuelvan en servidor. El cursor `(ts,id)` en base64 y los filtros básicos YA existen (`repositories.py:92-141`) — esta fase los extiende, no los reinventa. El índice está medido: elimina el `TEMP B-TREE FOR ORDER BY` de toda consulta filtrada y da 10× de margen sobre el objetivo de 10.000 eventos del criterio 3.
Output: migración v3, `query()` extendido y `count()`, con tests de plan de consulta y de idempotencia.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-event-timeline-y-centro-de-alertas/30-CONTEXT.md
@.planning/phases/30-event-timeline-y-centro-de-alertas/30-RESEARCH.md
@.planning/phases/30-event-timeline-y-centro-de-alertas/30-PATTERNS.md
@CLAUDE.md
</context>

<interfaces>
Firma resultante de `EventRepo` (el retorno de `query()` NO cambia de aridad — hay 7 llamadores hoy en `backend/database.py:166,189`, `backend/main.py:853` y `tests/test_repositories.py`, y ninguno debe tocarse):

```python
async def query(
    self, *,
    type: EventType | list[EventType] | None = None,   # el nombre 'type' se conserva: database.py lo usa
    severity: Severity | None = None,
    person_id: int | None = None,
    zone_id: str | None = None,
    camera_id: str | None = None,
    rule: str | None = None,                            # NUEVO
    ts_from: datetime.datetime | None = None,
    ts_to: datetime.datetime | None = None,
    cursor: str | None = None,
    limit: int = 50,
) -> tuple[list[EventDTO], str | None]:                 # sin cambios

async def count(                                        # NUEVO metodo, mismos filtros sin cursor/limit
    self, *, type=None, severity=None, person_id=None, zone_id=None,
    camera_id=None, rule=None, ts_from=None, ts_to=None,
) -> int:
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Índice idx_events_ts_id y migración de esquema v2 → v3</name>
  <files>backend/storage/models.py, backend/storage/migrations.py, tests/test_migrations.py</files>
  <read_first>
    - backend/storage/models.py líneas 85-110 (`class Event`: columnas y el bloque `__table_args__` con los cuatro `Index(...)` actuales — el nuevo se añade con la misma forma)
    - backend/storage/migrations.py completo, con especial atención a: `SCHEMA_VERSION` (línea ~24), `_backup_db()` (líneas 56-67), el final de `_migrate_v1_to_v2()` (líneas 150-158: cómo se registra la versión en `app_config`), la lista `MIGRATIONS` (líneas 160-162) y `run_migrations()` (líneas 165-180 — NO se toca)
    - tests/test_migrations.py completo (patrón de construcción de una base v1/v2 en `tmp_path` y de aserción sobre `schema_version`)
  </read_first>
  <behavior>
    - `TEST_migration_v3_creates_timeline_index`: partiendo de una base ya en v2 (creada con `create_all` + `schema_version=2` en `app_config`), tras `run_migrations(engine)` la consulta `SELECT name FROM sqlite_master WHERE type='index' AND name='idx_events_ts_id'` devuelve una fila y `app_config.schema_version` vale 3.
    - `TEST_migration_v3_is_idempotent`: ejecutar `run_migrations(engine)` dos veces seguidas no lanza y deja `schema_version = 3` y un único índice `idx_events_ts_id`.
    - `TEST_fresh_db_has_timeline_index`: una base creada desde cero con `init_db()`/`create_all` tiene el índice (viene de `__table_args__`).
  </behavior>
  <action>
    **1) `backend/storage/models.py`** — dentro de `Event.__table_args__`, añadir como QUINTA entrada (después de `Index("idx_events_person", "person_id", ts.desc())`):

    ```python
        Index("idx_events_ts_id", ts.desc(), id.desc()),
    ```

    Motivo a dejar como comentario en la línea anterior: `# cursor (ts,id) por valor de fila y ORDER BY sin TEMP B-TREE (Fase 30, medido: severity sola 19,0ms -> 0,60ms @100k)`.

    **2) `backend/storage/migrations.py`** — subir `SCHEMA_VERSION` de `2` a `3`, añadir la función de migración justo después de `_migrate_v1_to_v2` y registrarla en `MIGRATIONS`:

    ```python
    def _migrate_v2_to_v3(conn: Connection) -> None:
        """Indice compuesto para la linea temporal (Fase 30, OPS-09).

        create_all() no crea indices sobre tablas que ya existen, por eso va explicito.
        CREATE INDEX IF NOT EXISTS lo hace idempotente por sintaxis SQL, ademas del
        guard de version de run_migrations().
        """
        conn.execute(text(
            "CREATE INDEX IF NOT EXISTS idx_events_ts_id ON events (ts DESC, id DESC)"))
        conn.execute(text("DELETE FROM app_config WHERE key='schema_version'"))
        conn.execute(
            text("INSERT INTO app_config (key, value, updated_at) VALUES ('schema_version', :v, :now)"),
            {"v": json.dumps(SCHEMA_VERSION), "now": datetime.datetime.now().isoformat(sep=" ")},
        )


    MIGRATIONS: list[tuple[int, str, Callable[[Connection], None]]] = [
        (2, "esquema v2 completo", _migrate_v1_to_v2),
        (3, "indice compuesto de la linea temporal", _migrate_v2_to_v3),
    ]
    ```

    NO modificar `run_migrations()` ni `_backup_db()`: el backup automático ya se dispara antes de cualquier migración pendiente.

    **3)** Añadir los 3 tests de `<behavior>` a `tests/test_migrations.py`, reutilizando las fixtures y helpers ya presentes en ese fichero.
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/test_migrations.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "SCHEMA_VERSION = 3" backend/storage/migrations.py` → 1 match
    - `grep -n "idx_events_ts_id" backend/storage/models.py backend/storage/migrations.py` → al menos 2 matches (uno en cada fichero)
    - `grep -n "CREATE INDEX IF NOT EXISTS" backend/storage/migrations.py` → 1 match
    - `grep -c "Index(" backend/storage/models.py` aumenta exactamente en 1
    - `git diff backend/storage/migrations.py | grep "^[-+].*def run_migrations"` → vacío (la función no se tocó)
    - `.venv/Scripts/python.exe -m pytest tests/test_migrations.py -q` en verde con al menos 3 tests nuevos
  </acceptance_criteria>
  <done>El índice existe en el modelo y en la migración; una base v2 real sube a v3 con el índice creado y volver a migrar no hace nada.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: EventRepo.query() con tipo multi-valor y filtro por regla</name>
  <files>backend/storage/repositories.py, tests/test_repositories.py</files>
  <read_first>
    - backend/storage/repositories.py líneas 1-145 (imports ya presentes: `and_, func, select, text, tuple_`; `_encode_cursor`/`_decode_cursor` líneas 31-39; `EventRepo.query()` líneas 92-141 completo — la base a extender)
    - backend/database.py líneas 160-195 (los dos llamadores de `query()`: `type=EventType.LINE_CROSSED` y el desempaquetado `items, cursor = await repo.query(...)` — deben seguir funcionando SIN cambios)
    - tests/test_repositories.py líneas 1-100 y 240-260 (fixture `db`, `TEST_query_by_type_and_range`, `TEST_query_performance_100k` con `scripts/seed_events.py`)
    - scripts/seed_events.py (firma de `seed_events(db_path, n, days, camera_id)`, determinista con `seed=42`)
    - .planning/phases/30-event-timeline-y-centro-de-alertas/30-RESEARCH.md sección "Code Examples → Extender EventRepo.query" y "Pitfall 2: type IN (…) degrada la consulta 100×"
  </read_first>
  <behavior>
    - `TEST_query_accepts_multiple_types`: con eventos de tres tipos sembrados, `query(type=[EventType.INTRUSION, EventType.UNKNOWN_PERSON])` devuelve solo esos dos tipos, ordenados por `ts DESC`.
    - `TEST_query_single_type_still_accepts_enum`: `query(type=EventType.LINE_CROSSED)` (forma antigua, un enum suelto) sigue funcionando — es la llamada que hace `backend/database.py:166`.
    - `TEST_query_filters_by_rule_name`: dos eventos, uno con `payload={"rules": ["Intrusión nocturna"]}` y otro con `payload={"rules": ["Otra"]}`; `query(rule="Intrusión nocturna")` devuelve exactamente uno. Un evento sin clave `rules` nunca aparece.
    - `TEST_query_rule_filter_is_not_interpolated`: `query(rule="' OR 1=1 --")` devuelve 0 resultados y no lanza (prueba de que va por `bindparam`, no por f-string).
    - `TEST_count_matches_query_filters`: con 30 eventos de un tipo y 10 de otro, `count(type=X)` devuelve 30 mientras `query(type=X, limit=10)` devuelve 10.
    - `TEST_multi_type_query_plan_uses_timeline_index`: sobre una base sembrada, `EXPLAIN QUERY PLAN` de la consulta multi-tipo NO contiene la cadena `USE TEMP B-TREE FOR ORDER BY` (regresión del Pitfall 2).
  </behavior>
  <action>
    En `backend/storage/repositories.py`, dentro de `EventRepo`:

    **1)** Extraer la construcción de condiciones a un helper privado nuevo, colocado justo antes de `query()`:

    ```python
        @staticmethod
        def _filter_conditions(*, type=None, severity=None, person_id=None, zone_id=None,
                               camera_id=None, rule=None, ts_from=None, ts_to=None):
            """Condiciones WHERE compartidas por query() y count(). Devuelve (conditions, params).

            El prefijo '+' unario del IN multi-valor desactiva idx_events_type_ts SOLO en ese
            termino: sin el, SQLite elige ese indice y ordena con TEMP B-TREE (medido: 54ms
            vs 0,52ms @100k, 30-RESEARCH.md Hallazgo 7).
            """
            conditions: list = []
            params: dict = {}
            types = [type] if isinstance(type, EventType) else (list(type) if type else [])
            if len(types) == 1:
                conditions.append(models.Event.type == types[0].value)
            elif len(types) > 1:
                conditions.append(
                    text("+events.type IN :types").bindparams(
                        bindparam("types", expanding=True))
                )
                params["types"] = [t.value for t in types]
            if severity is not None:
                conditions.append(models.Event.severity == severity.value)
            if person_id is not None:
                conditions.append(models.Event.person_id == person_id)
            if zone_id is not None:
                conditions.append(models.Event.zone_id == zone_id)
            if camera_id is not None:
                conditions.append(models.Event.camera_id == camera_id)
            if rule is not None:
                conditions.append(text(
                    "EXISTS (SELECT 1 FROM json_each(events.payload, '$.rules') je "
                    "WHERE je.value = :rule)"
                ))
                params["rule"] = rule
            if ts_from is not None:
                conditions.append(models.Event.ts >= ts_from)
            if ts_to is not None:
                conditions.append(models.Event.ts <= ts_to)
            return conditions, params
    ```

    Añadir `bindparam` al import de SQLAlchemy de la línea 15: `from sqlalchemy import and_, bindparam, func, select, text, tuple_`.

    **2)** Reescribir `query()` para que use el helper, añada el parámetro `rule: str | None = None`, acepte `type: EventType | list[EventType] | None` y aplique los parámetros con `.params(**params)` sobre el `select`. La condición de cursor (`tuple_(...) < tuple_(...)`), el `order_by(ts.desc(), id.desc())`, el `limit` y el cálculo de `next_cursor` se conservan EXACTAMENTE como están hoy (líneas 121-141). El retorno sigue siendo `tuple[list[EventDTO], str | None]`.

    **3)** Añadir el método nuevo justo después de `query()`:

    ```python
        async def count(self, *, type=None, severity=None, person_id=None, zone_id=None,
                        camera_id=None, rule=None, ts_from=None, ts_to=None) -> int:
            """COUNT(*) con los mismos filtros que query(), sin cursor ni limit.

            Se llama SOLO en la primera pagina y solo si hay filtros activos: medido
            21ms @100k / 0,9ms @10k, pedirlo en cada scroll seria malgastarlo (Pitfall 9).
            """
            conditions, params = self._filter_conditions(
                type=type, severity=severity, person_id=person_id, zone_id=zone_id,
                camera_id=camera_id, rule=rule, ts_from=ts_from, ts_to=ts_to)
            q = select(func.count()).select_from(models.Event)
            if conditions:
                q = q.where(and_(*conditions))
            if params:
                q = q.params(**params)
            async with self._sf() as session:
                result = await session.execute(q)
                return int(result.scalar_one())
    ```

    **4)** Añadir los 6 tests de `<behavior>` a `tests/test_repositories.py` reutilizando la fixture `db` existente. Para el test del plan de consulta, ejecutar `EXPLAIN QUERY PLAN` con `session.execute(text("EXPLAIN QUERY PLAN " + str(q.compile(compile_kwargs={'literal_binds': True}))))` o, más simple y robusto, con `sqlite3` directamente sobre el fichero de la base sembrada.
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/test_repositories.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "def _filter_conditions" backend/storage/repositories.py` → 1 match
    - `grep -n "async def count" backend/storage/repositories.py` → 1 match dentro de `EventRepo`
    - `grep -n "+events.type IN :types" backend/storage/repositories.py` → 1 match (el `+` unario está presente)
    - `grep -n "json_each" backend/storage/repositories.py` → 1 match, dentro de `_filter_conditions`
    - `grep -n 'f"' backend/storage/repositories.py | grep -i "rule\|type IN"` → 0 matches (ninguna interpolación en SQL)
    - `grep -n "bindparam" backend/storage/repositories.py` → al menos 2 matches (import y uso)
    - Los llamadores existentes no se tocaron: `git diff --stat backend/database.py` vacío
    - `.venv/Scripts/python.exe -m pytest tests/test_repositories.py -q` en verde con 6 tests nuevos
  </acceptance_criteria>
  <done>`query()` acepta lista de tipos y filtro por regla sin cambiar su aridad de retorno; `count()` existe con los mismos filtros; el plan de consulta multi-tipo ya no usa TEMP B-TREE.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Parámetros de filtro de la API → SQL | `rule` y `type[]` llegan del navegador y acaban dentro de fragmentos `text()` de SQLAlchemy |
| Fichero de base de datos existente → migración | `run_migrations` altera el esquema de `data/events.db` en producción |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-05 | Tampering (SQL injection) | Filtro `rule` dentro de `text("EXISTS (... je.value = :rule)")` | mitigate | `bindparam`/`:rule` obligatorio; test explícito `TEST_query_rule_filter_is_not_interpolated` con una carga `' OR 1=1 --`. Igual con `types` vía `bindparam(expanding=True)` |
| T-30-06 | Denial of Service | `COUNT(*)` en cada página de scroll | mitigate | `count()` es un método aparte que el router (30-05) llama solo en la primera página y solo con filtros activos |
| T-30-07 | Tampering (pérdida de datos en migración) | `_migrate_v2_to_v3` sobre `data/events.db` real | mitigate | La migración solo hace `CREATE INDEX IF NOT EXISTS`: no altera filas ni columnas. `_backup_db()` ya copia la base antes de cualquier migración pendiente, sin código nuevo |
</threat_model>

<verification>
- `.venv/Scripts/python.exe -m pytest tests/test_migrations.py tests/test_repositories.py -q` — ambos en verde
- `.venv/Scripts/python.exe -m pytest tests/ -q` — suite completa sin regresiones (los 7 llamadores de `query()` siguen funcionando)
</verification>

<success_criteria>
- `SCHEMA_VERSION = 3` y una base v2 real migra creando `idx_events_ts_id`, de forma idempotente
- `EventRepo.query()` acepta `type` como enum suelto o lista, y filtra por nombre de regla vía `json_each` con `bindparam`
- `EventRepo.count()` devuelve el total de coincidencias de los mismos filtros
- Ningún llamador existente de `query()` necesitó modificarse
- Suite completa en verde
</success_criteria>

<output>
After completion, create `.planning/phases/30-event-timeline-y-centro-de-alertas/30-02-SUMMARY.md`
</output>
