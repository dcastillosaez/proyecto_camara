---
phase: 30
plan: 08
type: execute
wave: 6
depends_on: ["30-05", "30-07"]
files_modified:
  - frontend/js/views/timeline-row.js
  - frontend/js/views/timeline.js
autonomous: true
requirements: [OPS-07, OPS-08, OPS-09, OPS-10]

must_haves:
  truths:
    - "Cada fila muestra hora, barra de severidad, miniatura, descripcion en lenguaje llano, chip de zona, chip de regla y cuatro acciones"
    - "Los filtros se envian al servidor y reinician el cursor y la lista antes de pedir la primera pagina"
    - "Al acercarse al final de la lista se pide la pagina siguiente con el cursor, sin listener de scroll"
    - "El DOM nunca supera 400 filas y al recortar por arriba el contenido no salta"
    - "Un evento nuevo aparece arriba si la lista esta al principio; si el usuario ha bajado, aparece una pildora en vez de mover el scroll"
  artifacts:
    - path: "frontend/js/views/timeline-row.js"
      provides: "timelineRow(), describe(), SEV_COLOR, estado de descartados en localStorage"
      contains: "export function timelineRow"
    - path: "frontend/js/views/timeline.js"
      provides: "initTimeline(), applyTimelineFilters(), onLiveEvent(), setTimelineOffline()"
      contains: "IntersectionObserver"
  key_links:
    - from: "frontend/js/views/timeline.js"
      to: "GET /api/v2/events"
      via: "apiFetch con querystring de filtros + cursor"
      pattern: "/api/v2/events"
    - from: "frontend/js/views/timeline.js"
      to: "frontend/js/views/timeline-row.js::timelineRow"
      via: "una fila por evento, con el mapa media y el nombre de persona resuelto"
      pattern: "timelineRow\\("
    - from: "frontend/js/views/timeline-row.js"
      to: "frontend/js/components/eventCard.js::openClipModal"
      via: "accion Ver clip reutilizando el modal existente"
      pattern: "openClipModal"
---

<objective>
Construir la línea temporal: el módulo que dibuja una fila (`timeline-row.js`) y el que gestiona estado, filtros en servidor, paginación por cursor, scroll infinito con ventana de DOM de 400 filas y el evento en vivo (`timeline.js`).

Purpose: es el corazón visible de la fase — OPS-07 (contenido de la fila), OPS-08 (acciones directas), OPS-09 (filtros en servidor) y OPS-10 (evento nuevo sin recargar). Se parte en dos ficheros porque `tests/test_frontend_modules.py::TEST_line_limit` impone 300 líneas por módulo y ni la fila ni el motor caben juntos.
Output: dos módulos ES nuevos, sin dependencias, que consumen el contrato de `GET /api/v2/events` de 30-05 y los ids de 30-07.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-event-timeline-y-centro-de-alertas/30-CONTEXT.md
@.planning/phases/30-event-timeline-y-centro-de-alertas/30-UI-SPEC.md
@.planning/phases/30-event-timeline-y-centro-de-alertas/30-RESEARCH.md
@.planning/phases/30-event-timeline-y-centro-de-alertas/30-PATTERNS.md
@CLAUDE.md
</context>

<interfaces>
Este plan CONSUME (no define) el contrato de `GET /api/v2/events` de 30-05 y los ids del DOM de 30-07. Lee ambos bloques `<interfaces>` antes de empezar.

API pública de los dos módulos nuevos:
```javascript
// frontend/js/views/timeline-row.js
export const SEV_COLOR = { critical: '#ef4444', warning: '#f59e0b', info: '#64748b' };
export function describe(ev, personName)            // frase en pasado, lenguaje llano
export function timelineRow(ev, media, personName)  // -> HTMLElement con clase .timeline-row
export function isDismissed(eventId)                // localStorage 'timeline.dismissed'
export function dismiss(eventId) / export function undismiss(eventId)

// frontend/js/views/timeline.js
export function initTimeline()                      // bindings + primera pagina + observer
export async function applyTimelineFilters()        // reinicia cursor, vacia y repinta
export function onLiveEvent(event, media)           // mensaje WS type:"event"
export function setTimelineOffline(offline)         // barra ambar de "sin tiempo real"
export function refreshPersonNames()                // recarga el Map<person_id, name>
```

Endpoints que se consumen (todos ya existentes): `GET /api/v2/events`, `GET /persons` (mapa de nombres — `personGallery.js:7` ya lo usa), `GET /api/zones` (poblar el select de zona — `zoneEditor.js:70`).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: timeline-row.js — fila, descripción llana y estado de descartados</name>
  <files>frontend/js/views/timeline-row.js</files>
  <read_first>
    - frontend/js/views/dashboard-events.js (versión previa a 30-07, vía `git show HEAD~1:frontend/js/views/dashboard-events.js` si ya está retirada) líneas 110-144: `addEvent` — el patrón anti-XSS del repo, estructura estática por `innerHTML` + contenido por `textContent`
    - frontend/js/components/eventCard.js líneas 1-45 y 97-104 (`_recRow` con `dataset`/`textContent`, comentario anti-XSS de las líneas 16-18, y `openClipModal(src)`)
    - frontend/css/components.css (bloques `.timeline-row`, `.sev-bar`, `.tl-thumb`, `.chip`, `.rule-chip`, `.row-action` creados en 30-07 — las clases exactas a aplicar)
    - .planning/phases/30-event-timeline-y-centro-de-alertas/30-UI-SPEC.md sección "Fila de la línea temporal" (los 7 elementos en orden y sus reglas) y "Copywriting Contract" (regla de estilo: descripción en pasado y en lenguaje llano, nunca el identificador crudo del catálogo)
    - .planning/phases/30-event-timeline-y-centro-de-alertas/30-RESEARCH.md "Code Examples → Fila de la timeline sin XSS" (usar ese snippet como base) y "Hallazgo 5" (`person_name` NO viaja en la respuesta del endpoint: se resuelve con un `Map<person_id, name>`) y "Hallazgo 10" (descartar = `localStorage`, sin endpoint)
  </read_first>
  <action>
    Crear `frontend/js/views/timeline-row.js`:

    **1) Constantes y descripción llana.** Un mapa `EVENT_TEXT` de `EventType` → plantilla en pasado, cubriendo como mínimo: `LINE_CROSSED` ("{quién} cruzó la línea"), `ZONE_ENTERED` ("{quién} entró en la zona {zona}"), `ZONE_EXITED` ("{quién} salió de la zona {zona}"), `INTRUSION` ("Intrusión detectada{en zona}"), `UNKNOWN_PERSON` ("Persona desconocida detectada"), `PERSON_RECOGNIZED` ("{quién} fue reconocido"), `IDENTITY_LOST`, `LOITERING`, `RUNNING`, `IMMOBILE`, `CROWD_DETECTED`, `OBJECT_LEFT`, `CAMERA_OFFLINE`, `CAMERA_RECOVERED`, `RECORDING_STARTED`, `RECORDING_UPLOADED`, `UPLOAD_FAILED`, `DEGRADED_MODE`, `CONFIG_CHANGED`. Leer los nombres exactos en `backend/events/types.py` y no inventarlos. Fallback para un tipo desconocido: el tipo con guiones bajos sustituidos por espacios y en minúsculas — nunca reventar.
    `describe(ev, personName)` devuelve la frase; `{quién}` es `personName` si lo hay, si no "Desconocido".

    **2) `timelineRow(ev, media, personName)`** siguiendo literalmente el snippet de RESEARCH § "Fila de la timeline sin XSS", con estos detalles:
    - `row.dataset.id = ev.id`, `row.dataset.trackId = ev.track_id ?? ''`, `row.dataset.ts = ev.ts`.
    - `row.style.setProperty('--sev', SEV_COLOR[ev.severity] ?? SEV_COLOR.info)`.
    - Estructura estática por `innerHTML` con elementos VACÍOS; todo dato variable por `textContent`/`dataset`/`src` DESPUÉS de montar. El nombre de persona y el nombre de regla son exactamente el vector que este patrón evita (CodeQL `js/xss`).
    - Miniatura: `media?.snapshot_url ?? media?.thumbnail_url`; si no hay ninguna, sustituir el `<img>` por un marcador con icono de cámara sobre `#1e293b` (nunca una imagen rota ni una caja vacía). `loading="lazy"`, `width="64" height="36"`.
    - Nombre de persona confirmada en verde (`text-green-400`) dentro de la frase; "Desconocido" hereda el color de la severidad de la fila.
    - Chip de zona solo si `ev.zone_id`; chip de regla solo si `ev.payload?.rules?.length`, con prefijo `⚡`, `title` con todas las reglas unidas por ` · `, clase `.chip .rule-chip .mono` y NUNCA azul.
    - Cuatro `<button class="row-action">` con `data-action="clip|snapshot|person|dismiss"`, icono SVG de 16px y `aria-label` explícito que incluya la hora del evento (p. ej. `Ver clip del evento de las 18:42:05`). "Ver clip" se deshabilita con `title="Este evento no tiene grabación"` si no hay `media?.clip_url`; "Marcar como persona" se deshabilita si no hay `ev.track_id`.
    - La fila NO es un `<button>` y no lleva `tabindex`: solo sus acciones y la miniatura son focalizables (UI-SPEC, evita capturar el foco 400 veces).

    **3) Descartados en `localStorage`**, clave `timeline.dismissed`, array de ids con tope FIFO de 500:
    ```javascript
    const DISMISS_KEY = 'timeline.dismissed';
    const DISMISS_CAP = 500;
    // El descarte es estado de CLIENTE: no borra nada de la base de datos (UI-SPEC) y el
    // servidor no debe saber que ha ocultado un navegador concreto. El filtrado se aplica
    // al pintar, jamas en la consulta.
    ```
    `isDismissed(id)`, `dismiss(id)` (añade y recorta por FIFO) y `undismiss(id)` (quita), todas tolerantes a `localStorage` no disponible o a JSON corrupto (try/catch devolviendo array vacío).

    Vigilar el tope de 300 líneas del módulo.
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/test_frontend_modules.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/js/views/timeline-row.js` existe y `wc -l` < 300
    - `grep -c "export function timelineRow\|export function describe\|export function isDismissed\|export function dismiss\|export function undismiss\|export const SEV_COLOR" frontend/js/views/timeline-row.js` → 6
    - `grep -c "data-action=" frontend/js/views/timeline-row.js` → ≥ 4 (las cuatro acciones)
    - `grep -n "textContent" frontend/js/views/timeline-row.js` → ≥ 4 matches
    - Ningún dato del backend se interpola en el template: `grep -n 'innerHTML' frontend/js/views/timeline-row.js` muestra solo plantillas SIN `${ev.` ni `${personName` ni `${rules` dentro — verificar con `grep -n 'innerHTML' -A 12 frontend/js/views/timeline-row.js | grep -c '\${ev\.\|\${personName\|\${rules'` → 0
    - `grep -n "timeline.dismissed" frontend/js/views/timeline-row.js` → 1 match
    - `grep -n "aria-label" frontend/js/views/timeline-row.js` → ≥ 4 matches
    - `.venv/Scripts/python.exe -m pytest tests/test_frontend_modules.py -q` en verde
  </acceptance_criteria>
  <done>La fila se construye sin interpolar datos del backend, con los 7 elementos del UI-SPEC en orden, cuatro acciones etiquetadas y el descarte guardado solo en el navegador.</done>
</task>

<task type="auto">
  <name>Task 2: timeline.js — filtros en servidor, cursor y scroll infinito con ventana de 400 filas</name>
  <files>frontend/js/views/timeline.js</files>
  <read_first>
    - frontend/js/api.js completo (`apiFetch`: normaliza cualquier error a `Error(detail)` — usarlo SIEMPRE, nunca `fetch` crudo con `catch {}` silencioso)
    - frontend/index.html (ids creados en 30-07: `#timeline-list`, `#timeline-sentinel`, `#timeline-loading`, `#timeline-end`, `#timeline-error`, `#timeline-empty*`, `#tl-filter-*`, `#btn-tl-apply`, `#btn-tl-clear`, `#tl-active-filters`, `#timeline-count`)
    - .planning/phases/30-event-timeline-y-centro-de-alertas/30-UI-SPEC.md secciones "Filtros combinables" (chips azules de filtro activo con ×, contador "{N}" vs "{N} de {total}") y "Scroll infinito con 10.000 eventos" (página 50, `rootMargin: '200px'`, ventana de 400, 3 filas esqueleto, plan B a 1000 sin recortar)
    - .planning/phases/30-event-timeline-y-centro-de-alertas/30-RESEARCH.md "Code Examples → Centinela de scroll infinito", "Hallazgo 1 → Cursor inverso" (NO hace falta: la timeline guarda el array completo en memoria y el DOM es solo una ventana sobre él) y "Hallazgo 9" (los separadores se calculan en JS, sin endpoint `/api/v2/timeline`)
    - frontend/js/components/zoneEditor.js línea 70 (`fetch('/api/zones')`) y frontend/js/components/personGallery.js línea 7 (`fetch('/persons')`)
  </read_first>
  <action>
    Crear `frontend/js/views/timeline.js` con este diseño:

    **Estado de módulo** (documentado en un comentario de cabecera):
    ```javascript
    // El array `_all` guarda TODOS los eventos ya descargados (10.000 x ~250B ~= 2,5MB,
    // aceptable) y el DOM es solo una ventana de MAX_ROWS sobre el. Asi el recorte por
    // arriba y el volver a subir se resuelven repintando desde memoria, sin red y sin
    // cursor inverso: un parametro menos de API y una clase entera de bugs menos
    // (paginas hacia atras desalineadas con inserciones nuevas) — 30-RESEARCH.md Hallazgo 1.
    const PAGE_SIZE = 50;
    const MAX_ROWS = 400;      // plan B si el salto de scroll es perceptible: subir a 1000 y no recortar
    let _all = [];             // eventos descargados, mas recientes primero
    let _media = {};           // event_id -> media
    let _cursor = null;
    let _loading = false;
    let _exhausted = false;
    let _persons = new Map();  // person_id -> name  (Hallazgo 5: person_name no viaja en la respuesta)
    let _pending = [];         // eventos en vivo no insertados (pildora)
    ```

    **`_filterParams()`** construye el `URLSearchParams` desde los controles: `type` repetido una vez por chip activo de `#tl-filter-types`, `severity` del chip activo de `#tl-filter-severity`, `zone_id`, `person_id` (resuelto contra `_persons` a partir del texto del input; si no casa ningún nombre, no se manda el parámetro y se marca el chip como no aplicado), `from`/`to` a partir de los `input[type=date]` (`T00:00:00` y `T23:59:59`), y `limit=PAGE_SIZE`. Devuelve también un flag `hasFilters`.

    **`loadPage({ reset })`**: si `reset`, vacía `_all`, `_media`, `_cursor`, `_exhausted` y el contenedor. Muestra `#timeline-loading`, llama a `apiFetch('/api/v2/events?' + params)`, acumula en `_all`/`_media`, guarda `_cursor = data.cursor`, marca `_exhausted` si `data.cursor === null` (y muestra `#timeline-end`), y actualiza `#timeline-count` con `"{N} eventos"` sin filtros o `"{N} de {total}"` cuando `data.total !== null`. En error: oculta la lista y muestra `#timeline-error` (nunca un `catch {}` silencioso — UI-SPEC lo exige explícitamente).

    **`render()`**: repinta desde `_all` filtrando `isDismissed(id)`, insertando separadores `.timeline-sep` cuando cambia la hora o el día respecto a la fila anterior (texto "Hoy · 18:00", "Ayer · 23:00", "12 ago · 09:00"), y recortando a `MAX_ROWS` filas. Al recortar por arriba, compensar `scrollTop` restando `filasEliminadas * 60` (52px de fila + 8px de `gap`) para que el contenido no salte — la altura es fija precisamente para esto. Estados vacíos: `#timeline-empty` si no hay filtros, `#timeline-empty-filtered` si los hay.

    **Centinela**:
    ```javascript
    const io = new IntersectionObserver((entries) => {
      if (entries[0].isIntersecting && _cursor && !_loading) loadPage({ reset: false });
    }, { root: document.getElementById('timeline-list'), rootMargin: '200px' });
    io.observe(document.getElementById('timeline-sentinel'));
    ```
    Nunca un listener de `scroll` (UI-SPEC).

    **`applyTimelineFilters()`**: reinicia cursor y vacía la lista ANTES de pedir la primera página, y repinta `#tl-active-filters` con un chip azul por filtro activo, cada uno con una `×` que lo quita individualmente. `#btn-tl-clear` y `#btn-tl-clear-empty` limpian todos los controles y vuelven a cargar.

    **`initTimeline()`**: puebla `#tl-filter-types` con los chips de tipo (usar los tipos frecuentes: `INTRUSION`, `UNKNOWN_PERSON`, `LINE_CROSSED`, `ZONE_ENTERED`, `LOITERING`, `OBJECT_LEFT`), puebla `#tl-filter-zone` desde `GET /api/zones` y `#tl-person-options` desde `GET /persons` (rellenando también `_persons`), engancha `#btn-tl-apply`, `#btn-tl-clear`, `#btn-tl-retry`, los chips de severidad (excluyentes) y de tipo (multi-selección, alternando la clase `active`), monta el observer y carga la primera página. `refreshPersonNames()` recarga `_persons` y repinta.

    **Delegación de acciones** sobre `#timeline-list`: `click` en `.row-action` según `data-action` — `clip` → `openClipModal(media.clip_url)` importado de `../components/eventCard.js`; `snapshot` → `openClipModal(media.snapshot_url)` (el mismo modal, con `<img>` en vez de `<video>` si el modal lo soporta; si no, abrir la imagen en una pestaña nueva con `window.open(url, '_blank', 'noopener')`); `dismiss` → `dismiss(id)` + repintado + toast con acción "Deshacer" de 5 s que llama a `undismiss(id)`; `person` → despacha un `CustomEvent('timeline:mark-person', { detail: { eventId, trackId, snapshotUrl } })` sobre `document` — 30-11 escucha ese evento, así este módulo no depende de él.

    Vigilar el tope de 300 líneas. Si no cabe, extraer los separadores y el recorte a `frontend/js/views/timeline-virtualize.js` (fichero adicional de este mismo plan) antes que superar el límite.
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/test_frontend_modules.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/js/views/timeline.js` existe y `wc -l` < 300 (igual para `timeline-virtualize.js` si se llegó a crear)
    - `grep -c "export function initTimeline\|export async function applyTimelineFilters\|export function onLiveEvent\|export function setTimelineOffline\|export function refreshPersonNames" frontend/js/views/timeline.js` → 5 (`onLiveEvent`/`setTimelineOffline` se implementan en Task 3; en esta tarea pueden quedar como stubs exportados)
    - `grep -n "IntersectionObserver" frontend/js/views/timeline.js` → 1 match
    - `grep -n "addEventListener('scroll'\|addEventListener(\"scroll\"" frontend/js/views/timeline.js` → 0 matches
    - `grep -n "rootMargin: '200px'" frontend/js/views/timeline.js` → 1 match
    - `grep -n "MAX_ROWS = 400" frontend/js/views/timeline.js` → 1 match
    - `grep -n "scrollTop" frontend/js/views/timeline.js` → ≥ 1 match (compensación al recortar)
    - `grep -n "apiFetch" frontend/js/views/timeline.js` → ≥ 1 match; `grep -c "catch {}" frontend/js/views/timeline.js` → 0 (los errores se muestran, no se tragan)
    - `grep -n "'/api/v2/events" frontend/js/views/timeline.js` → ≥ 1 match; `grep -n "'/api/events" frontend/js/views/timeline.js` → 0 (nunca el endpoint v1)
    - `grep -n "\.filter(" frontend/js/views/timeline.js | grep -i "severity\|type ==="` → 0 matches (los filtros de datos se resuelven en servidor, nunca sobre el array descargado; el único `filter` admitido es el de descartados)
    - `.venv/Scripts/python.exe -m pytest tests/test_frontend_modules.py -q` en verde
  </acceptance_criteria>
  <done>La timeline pide páginas de 50 con cursor al servidor, filtra en servidor, pagina con `IntersectionObserver`, mantiene el DOM en 400 filas compensando `scrollTop` y muestra estados de vacío, error y fin de lista.</done>
</task>

<task type="auto">
  <name>Task 3: Evento en vivo — inserción arriba, píldora "N eventos nuevos" y barra de sin-tiempo-real</name>
  <files>frontend/js/views/timeline.js</files>
  <read_first>
    - frontend/js/views/timeline.js (creado en Task 2: `_all`, `_media`, `_pending`, `render()`, `_filterParams()`)
    - frontend/index.html (`#timeline-newpill`, `#timeline-offline` creados en 30-07)
    - frontend/js/websocket.js líneas 40-84 (`onopen`/`onclose` y `setWsConnected` — la barra ámbar se ata a ese estado ya existente, sin crear un segundo mecanismo de detección de desconexión)
    - .planning/phases/30-event-timeline-y-centro-de-alertas/30-UI-SPEC.md sección "Evento nuevo en menos de 1 s" (las tres reglas: `scrollTop < 8px` inserta arriba con `slide-in` y realce de 2 s; si el usuario ha bajado, píldora sin mover el scroll; si hay filtros y el evento no los cumple, no se inserta ni se cuenta)
    - .planning/phases/30-event-timeline-y-centro-de-alertas/30-01-PLAN.md bloque `<interfaces>` (forma exacta del mensaje `{"type":"event","event":…,"media":…}`)
  </read_first>
  <action>
    Implementar en `frontend/js/views/timeline.js`:

    **`_matchesActiveFilters(ev)`** — comprobación local mínima contra los controles activos (tipo, severidad, zona, persona, rango). Es la ÚNICA lógica de filtrado que vive en el navegador y solo se aplica a los eventos que llegan por WebSocket, nunca a la lista descargada: un evento en vivo que no cumple los filtros actuales no se inserta ni se cuenta en la píldora (UI-SPEC).

    **`onLiveEvent(event, media)`**:
    ```javascript
    export function onLiveEvent(event, media) {
      if (!_matchesActiveFilters(event)) return;
      if (_all.some(e => e.id === event.id)) return;      // idempotente ante reconexiones
      _all.unshift(event);
      if (media) _media[event.id] = media;
      const list = document.getElementById('timeline-list');
      if (!list) return;
      if (list.scrollTop < 8) {
        render();                                          // inserta arriba, nada mas se mueve
        const first = list.querySelector('.timeline-row');
        if (first) {
          first.classList.add('slide-in');
          const bar = first.querySelector('.sev-bar');
          if (bar) { bar.style.filter = 'brightness(1.8)'; setTimeout(() => { bar.style.filter = ''; }, 2000); }
        }
      } else {
        _pending.push(event.id);                           // no se toca el scroll
        const pill = document.getElementById('timeline-newpill');
        pill.textContent = `${_pending.length} evento${_pending.length === 1 ? '' : 's'} nuevo${_pending.length === 1 ? '' : 's'}`;
        pill.classList.remove('hidden');
      }
    }
    ```
    Al pulsar `#timeline-newpill`: `list.scrollTo({ top: 0, behavior: 'smooth' })`, `_pending = []`, ocultar la píldora y `render()`.

    **`setTimelineOffline(offline)`**: muestra u oculta `#timeline-offline`. Cuando pasa de `true` a `false` (reconexión), dispara `loadPage({ reset: true })` para que la lista se sincronice sin recargar la página (OPS-06 + criterio 4).

    Si la clase `slide-in` no existe todavía en `components.css`, comprobarlo con `grep -n "slide-in" frontend/css/components.css` y, si falta, usar la animación ya disponible o añadir el keyframe en `timeline-row.js` vía clase — no inventar un tercer mecanismo de animación.
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/test_frontend_modules.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "export function onLiveEvent" frontend/js/views/timeline.js` → 1 match, con cuerpo real (no un stub `return;`)
    - `grep -n "scrollTop < 8" frontend/js/views/timeline.js` → 1 match
    - `grep -n "_pending" frontend/js/views/timeline.js` → ≥ 4 matches (acumulación, texto de la píldora, vaciado al pulsar)
    - `grep -n "timeline-newpill" frontend/js/views/timeline.js` → ≥ 2 matches
    - `grep -n "_matchesActiveFilters" frontend/js/views/timeline.js` → ≥ 2 matches (definición y uso en `onLiveEvent`)
    - `grep -n "scrollTo\|scrollIntoView" frontend/js/views/timeline.js` → aparece SOLO dentro del handler de la píldora (nunca en `onLiveEvent` fuera de la rama `scrollTop < 8`)
    - `grep -n "export function setTimelineOffline" frontend/js/views/timeline.js` → 1 match
    - `wc -l frontend/js/views/timeline.js` < 300
    - `.venv/Scripts/python.exe -m pytest tests/test_frontend_modules.py -q` en verde
  </acceptance_criteria>
  <done>Un evento nuevo aparece arriba sin mover nada si la lista está al principio, y como píldora si el usuario ha bajado; la reconexión sincroniza la lista sin recargar.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Respuesta de `/api/v2/events` y mensaje WS → DOM | Nombres de persona y de regla (texto que escribe el operador) acaban dentro de la fila |
| `localStorage` → filtrado de la lista | Estado de descartados controlado por el propio navegador |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-27 | Tampering (XSS almacenado) | `person_name` y `Rule.name` en la fila y en el chip | mitigate | Estructura estática por `innerHTML`, contenido por `textContent`/`dataset` (patrón obligatorio del repo, CodeQL `js/xss`), con criterio de aceptación grep-verificable de que ningún `${ev.…}` entra en un template |
| T-30-28 | Tampering | `clip_url`/`snapshot_url` usados como `src` | mitigate | Ambas URLs las construye el servidor a partir de ids (30-05); el cliente nunca envía rutas. Se asignan por propiedad del DOM (`img.src`), nunca interpoladas en HTML |
| T-30-29 | Denial of Service (cliente) | 10.000 filas en el DOM | mitigate | Ventana de `MAX_ROWS = 400` con recorte y compensación de `scrollTop`; el array en memoria son ~2,5 MB medidos, no el DOM |
| T-30-30 | Information Disclosure | Filtrado en cliente de datos ya descargados | mitigate | Los filtros de datos se resuelven en servidor (OPS-09); el único filtrado local es el de descartados y el de los eventos en vivo |
</threat_model>

<verification>
- `.venv/Scripts/python.exe -m pytest tests/test_frontend_modules.py -q` — en verde (los módulos nuevos se añaden a `LOCKED_JS` en 30-10)
- `wc -l frontend/js/views/*.js` — ningún módulo por encima de 300 líneas
- Comprobación manual (anotada en el SUMMARY, sin runner JS en el repo): con el servidor arrancado, la lista se llena, los filtros repintan y el scroll pide más páginas
</verification>

<success_criteria>
- La fila muestra los 7 elementos del UI-SPEC en orden, sin interpolar datos del backend en HTML
- Los filtros viajan al servidor y reinician cursor y lista
- El scroll infinito usa `IntersectionObserver` con `rootMargin: 200px` y páginas de 50
- El DOM nunca supera 400 filas y el `scrollTop` se compensa al recortar
- Un evento nuevo aparece arriba o como píldora según la posición del scroll, nunca forzando el scroll
- Ningún módulo supera 300 líneas
</success_criteria>

<output>
After completion, create `.planning/phases/30-event-timeline-y-centro-de-alertas/30-08-SUMMARY.md`
</output>
