---
phase: 30
plan: 05
type: execute
wave: 3
depends_on: ["30-01", "30-02", "30-03", "30-04"]
files_modified:
  - backend/api/v2/events.py
  - backend/main.py
  - tests/test_events_api.py
autonomous: true
requirements: [OPS-07, OPS-08, OPS-09]

must_haves:
  truths:
    - "El navegador puede pedir una pagina de eventos filtrada por varios tipos, severidad, zona, persona, regla y rango, y recibir el cursor de la siguiente"
    - "Cada pagina trae, para los eventos que tengan algo, la URL de su miniatura, su captura y su clip"
    - "Con filtros activos, la primera pagina indica cuantas coincidencias hay en total"
    - "Se puede consultar el alcance retroactivo de un evento y aplicarlo, en dos llamadas separadas"
  artifacts:
    - path: "backend/api/v2/events.py"
      provides: "Router /api/v2/events: lista paginada con media/total, detalle, track-scope y assign-person"
      contains: "APIRouter(prefix=\"/api/v2/events\""
    - path: "backend/main.py"
      provides: "Registro del router nuevo y borrado del endpoint suelto /api/v2/events"
      contains: "events_v2_router"
    - path: "tests/test_events_api.py"
      provides: "tests ASGI del contrato del endpoint (fichero nuevo)"
  key_links:
    - from: "backend/api/v2/events.py::list_events"
      to: "backend/storage/repositories.py::EventRepo.query"
      via: "type como lista, rule, cursor y limit acotado por pagination_limit()"
      pattern: "EventRepo|_event_repo\\(\\)"
    - from: "backend/api/v2/events.py::_media_map"
      to: "backend/storage/repositories.py::RecordingRepo.by_trigger_event_ids"
      via: "una sola consulta por pagina"
      pattern: "by_trigger_event_ids"
    - from: "backend/main.py"
      to: "backend/api/v2/events.py::router"
      via: "app.include_router(events_v2_router) en el bloque de routers v2"
      pattern: "include_router\\(events_v2_router\\)"
---

<objective>
Mover el endpoint `GET /api/v2/events` que hoy vive suelto en `backend/main.py:829-861` a un router propio `backend/api/v2/events.py`, extenderlo con tipo multi-valor, filtro por regla, `total` y el mapa `media`, y añadir el detalle, la previsualización de alcance de track y la asignación retroactiva de identidad.

Purpose: OPS-09 (filtros y cursor en servidor), OPS-07 (miniatura de cada fila) y OPS-08 (acciones directas: ver clip, ver captura, marcar como persona). El envelope `{"events": [...], "cursor": ...}` **no se renombra**: `frontend/js/views/dashboard.js:274` (panel "Alertas activas" de la Fase 29) ya lo consume; solo se añaden claves.
Output: router v2 completo con rate limit y cap de paginación, endpoint viejo borrado en el mismo commit, y tests ASGI del contrato.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-event-timeline-y-centro-de-alertas/30-CONTEXT.md
@.planning/phases/30-event-timeline-y-centro-de-alertas/30-RESEARCH.md
@.planning/phases/30-event-timeline-y-centro-de-alertas/30-PATTERNS.md
@.planning/phases/30-event-timeline-y-centro-de-alertas/30-UI-SPEC.md
@CLAUDE.md
</context>

<interfaces>
Contrato HTTP exacto que consumen 30-08 (timeline) y 30-11 (marcar como persona):

```jsonc
// GET /api/v2/events?type=INTRUSION&type=UNKNOWN_PERSON&severity=critical&zone_id=jardin
//                    &person_id=3&camera_id=cam1&rule=Intrusi%C3%B3n%20nocturna
//                    &from=2026-08-01T00:00:00&to=2026-08-20T23:59:59&cursor=…&limit=50
{
  "events": [ { "id": "6f0…", "type": "INTRUSION", "ts": "…", "severity": "critical",
                "track_id": 12, "person_id": null, "zone_id": "jardin",
                "snapshot_path": "data/snapshots/20260820/6f0….jpg",
                "payload": { "rules": ["Intrusión nocturna"] } } ],
  "cursor": "MjAyNi0wOC0yMFQxODozMDowMHxhYmMt…",   // null = no hay mas paginas
  "total": 137,                                     // null salvo 1a pagina CON filtros
  "media": {                                        // clave = event.id; solo los que tienen algo
    "6f0…": { "recording_id": 12,
              "clip_url": "/clips/clip_20260820_183000.mp4",
              "thumbnail_url": "/api/v2/recordings/12/thumbnail",
              "snapshot_url": "/snapshots/20260820/6f0….jpg" }
  }
}

// GET /api/v2/events/{event_id}          -> 404 si no existe
{ "event": { … }, "media": { "recording_id": …, "clip_url": …, "thumbnail_url": …, "snapshot_url": … } }

// GET /api/v2/events/{event_id}/track-scope   -> 404 si el evento no existe
{ "count": 5, "from": "2026-08-20T18:30:00", "to": "2026-08-20T18:31:20", "event_ids": ["…"] }
// evento sin track_id -> {"count": 0, "from": null, "to": null, "event_ids": []}

// POST /api/v2/events/{event_id}/assign-person   body: {"person_id": 7}
{ "person_id": 7, "updated": 5, "event_ids": ["…"] }
```

`media` va como mapa hermano y **nunca** dentro del objeto evento: el `Event` de Pydantic es el contrato persistido (`repositories.py:1-5`) y meterle campos de presentación arrastraría también a `/api/v2/ws`.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Router backend/api/v2/events.py con lista paginada, media y total</name>
  <files>backend/api/v2/events.py, backend/main.py, tests/test_events_api.py</files>
  <read_first>
    - backend/api/v2/recordings.py completo (102 líneas) — molde EXACTO del router: docstring de módulo sobre auth heredada, imports, `_recording_repo()` helper, `@router.get("")` + `@limiter.limit(V2_RATE_LIMIT)` + `request: Request` + `limit = pagination_limit()`, validación de enum con 400, 404 de recurso individual
    - backend/api/v2/deps.py completo (incluida `snapshot_url()` añadida en 30-04)
    - backend/api/v2/context.py líneas 28-40 (patrón `configure()` para inyectar estado vivo desde el `lifespan`)
    - backend/main.py líneas 824-862 (el endpoint `@app.get("/api/v2/events")` que hay que BORRAR en este mismo commit) y líneas 616-627 (bloque de `include_router` de los cuatro routers v2)
    - backend/storage/repositories.py (`EventRepo.query()` y `count()` de 30-02, `RecordingRepo.by_trigger_event_ids()` de 30-03)
    - tests/test_detection_config_api.py completo — patrón de test ASGI de un router v2 con `TestClient` y parcheo del repo del módulo (`detection_module._…`), a replicar
    - .planning/phases/30-30-RESEARCH: sección "Hallazgo 1" (tabla de lo que falta y envelope recomendado) y "Pitfall 5/9/10"
  </read_first>
  <behavior>
    - `TEST_list_events_returns_envelope_with_events_and_cursor`: `GET /api/v2/events?limit=2` devuelve 200 con claves exactas `events`, `cursor`, `total`, `media`.
    - `TEST_list_events_accepts_repeated_type_param`: `?type=INTRUSION&type=UNKNOWN_PERSON` devuelve solo esos dos tipos.
    - `TEST_list_events_rejects_unknown_type`: `?type=NO_EXISTE` devuelve 400 con `detail` no vacío.
    - `TEST_list_events_rejects_unknown_severity`: `?severity=urgentisimo` devuelve 400.
    - `TEST_total_is_null_without_filters`: `GET /api/v2/events` (sin filtros) devuelve `total is None`.
    - `TEST_total_is_present_on_first_filtered_page`: `?severity=critical` devuelve `total` entero; la misma consulta con `&cursor=…` devuelve `total is None`.
    - `TEST_media_map_only_contains_events_with_media`: dos eventos, uno con snapshot y grabación asociada y otro sin nada → `media` tiene exactamente 1 clave.
    - `TEST_limit_is_capped_at_200`: `?limit=500` devuelve 422 (lo impone `pagination_limit()`).
  </behavior>
  <action>
    **1) Crear `backend/api/v2/events.py`** copiando la estructura de `recordings.py`:

    ```python
    """API v2 — eventos tipados: linea temporal, detalle, alcance de track y asignacion (Fase 30).

    Auth and rate limiting: the app applies auth globally (FastAPI(dependencies=[Depends(verify)])),
    so routers included via app.include_router() inherit it automatically. Rate limiting
    (SEC-16, Fase 22) uses the shared limiter/rate value from backend/api/v2/deps.py.

    Sustituye al endpoint suelto que vivia en main.py:829 desde la Fase 19 (mismo envelope
    {"events": [...], "cursor": ...} — dashboard.js:274 ya lo consume, no renombrar claves).
    """
    from __future__ import annotations

    import datetime
    import json
    import os
    from typing import Any

    from fastapi import APIRouter, Body, HTTPException, Query, Request

    from backend.api.v2.deps import V2_RATE_LIMIT, limiter, pagination_limit, snapshot_url
    from backend.database import get_session_factory
    from backend.events.types import EventType, Severity
    from backend.storage.repositories import EventRepo, RecordingRepo

    router = APIRouter(prefix="/api/v2/events", tags=["events"])


    def _event_repo() -> EventRepo:
        return EventRepo(get_session_factory())


    def _recording_repo() -> RecordingRepo:
        return RecordingRepo(get_session_factory())
    ```

    **2) Helper de medios** (una sola consulta por página):

    ```python
    async def _media_map(events: list) -> dict[str, dict]:
        """event_id -> {recording_id, clip_url, thumbnail_url, snapshot_url}, solo con lo que exista.

        events.recording_id nunca se escribe: el vinculo real es recordings.trigger_event_id
        (30-RESEARCH.md Hallazgo 4). Una consulta por pagina (<=200 ids), nunca una por fila.
        """
        by_event = await _recording_repo().by_trigger_event_ids([e.id for e in events])
        out: dict[str, dict] = {}
        for ev in events:
            snap = snapshot_url(ev.snapshot_path)
            rec = by_event.get(ev.id)
            if snap is None and rec is None:
                continue
            entry: dict[str, Any] = {
                "recording_id": None, "clip_url": None,
                "thumbnail_url": None, "snapshot_url": snap,
            }
            if rec is not None:
                entry["recording_id"] = rec["recording_id"]
                if rec.get("local_path"):
                    entry["clip_url"] = "/clips/" + os.path.basename(rec["local_path"])
                if rec.get("thumbnail_path"):
                    entry["thumbnail_url"] = f"/api/v2/recordings/{rec['recording_id']}/thumbnail"
            out[ev.id] = entry
        return out
    ```

    **3) `GET ""` (lista)** — con validación de enums ANTES de consultar (copiar el patrón de 400 de `main.py:846-850`), `type: list[str] | None = Query(default=None)` para admitir el parámetro repetido, y `total` solo en la primera página con filtros:

    ```python
    @router.get("")
    @limiter.limit(V2_RATE_LIMIT)
    async def list_events(
        request: Request,
        type: list[str] | None = Query(default=None),
        severity: str | None = Query(default=None),
        person_id: int | None = Query(default=None),
        zone_id: str | None = Query(default=None),
        camera_id: str | None = Query(default=None),
        rule: str | None = Query(default=None, max_length=100),
        from_dt: datetime.datetime | None = Query(default=None, alias="from"),
        to_dt: datetime.datetime | None = Query(default=None, alias="to"),
        cursor: str | None = Query(default=None, max_length=200),
        limit: int = pagination_limit(),
    ) -> dict[str, Any]:
        """Eventos tipados con filtros combinables y paginacion por cursor (OPS-09)."""
        try:
            types = [EventType(t) for t in (type or [])] or None
            event_severity = Severity(severity) if severity else None
        except ValueError as exc:
            raise HTTPException(status_code=400, detail=str(exc))

        filters = dict(type=types, severity=event_severity, person_id=person_id,
                       zone_id=zone_id, camera_id=camera_id, rule=rule,
                       ts_from=from_dt, ts_to=to_dt)
        repo = _event_repo()
        try:
            items, next_cursor = await repo.query(**filters, cursor=cursor, limit=limit)
        except (ValueError, TypeError):          # cursor corrupto o no decodificable
            raise HTTPException(status_code=400, detail="cursor invalido")

        has_filters = any(v is not None for v in filters.values())
        total = await repo.count(**filters) if (cursor is None and has_filters) else None

        return {
            "events": [json.loads(e.model_dump_json()) for e in items],
            "cursor": next_cursor,
            "total": total,
            "media": await _media_map(items),
        }
    ```

    **4) `GET "/{event_id}"`** — 404 si no existe, mismo envelope `{"event": …, "media": …}` (con `_media_map([ev]).get(ev.id, {...cuatro claves a null})`).

    **5) Borrar el endpoint viejo** `@app.get("/api/v2/events")` completo de `backend/main.py` (líneas 829-861, incluyendo su decorador y el import local de `Severity`) y **registrar el router** en el bloque de `include_router` (tras el de `context`, líneas 625-626):

    ```python
    from backend.api.v2.events import router as events_v2_router
    app.include_router(events_v2_router)
    ```

    Es obligatorio hacer ambas cosas en el mismo commit: si conviven, FastAPI resuelve por orden de registro y el resultado depende de dónde esté el `include_router` (Pitfall 10).

    **6) Crear `tests/test_events_api.py`** con los 8 tests de `<behavior>`, replicando el patrón de `tests/test_detection_config_api.py` (TestClient sobre `backend.main.app`, parcheo de la fábrica de sesión del módulo para apuntar a una base temporal).
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/test_events_api.py tests/test_security_regression.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `backend/api/v2/events.py` existe y `grep -n 'APIRouter(prefix="/api/v2/events"' backend/api/v2/events.py` → 1 match
    - `grep -c "@limiter.limit(V2_RATE_LIMIT)" backend/api/v2/events.py` → igual al número de endpoints definidos en el fichero (ninguno sin rate limit)
    - `grep -n "pagination_limit()" backend/api/v2/events.py` → 1 match (en `list_events`)
    - `grep -n "api_v2_events\|@app.get(\"/api/v2/events\")" backend/main.py` → 0 matches (el endpoint viejo desapareció)
    - `grep -n "include_router(events_v2_router)" backend/main.py` → 1 match
    - `grep -n "by_trigger_event_ids" backend/api/v2/events.py` → 1 match, dentro de `_media_map` (una consulta por página, no por fila)
    - `.venv/Scripts/python.exe -c "from backend.main import app; print(sorted({r.path for r in app.routes if 'v2/events' in getattr(r,'path','')}))"` imprime exactamente `['/api/v2/events', '/api/v2/events/{event_id}']` en este punto del plan
    - `.venv/Scripts/python.exe -m pytest tests/test_security_regression.py -k v2 -q` en verde (rate limit y cap de `limit` en todos los endpoints v2)
    - `.venv/Scripts/python.exe -m pytest tests/test_events_api.py -q` en verde (8 tests)
  </acceptance_criteria>
  <done>El router v2 sirve la lista paginada con `media` y `total`, el endpoint suelto de `main.py` ya no existe y los tests de regresión de seguridad siguen verdes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: track-scope y assign-person</name>
  <files>backend/api/v2/events.py, tests/test_events_api.py</files>
  <read_first>
    - backend/api/v2/events.py (creado en Task 1)
    - backend/storage/repositories.py (`EventRepo.track_scope()` y `assign_person()` de 30-03: firmas y forma exacta del retorno)
    - backend/api/v2/recordings.py líneas 88-102 (`retry_upload`: patrón de endpoint POST con validación de precondición y 409)
    - backend/main.py líneas 907-940 (`/api/enroll_face`: el endpoint que YA valida `content_type`, tamaño ≤10 MB y `max_length=100` del nombre — este plan NO lo duplica; el frontend llamará primero a él para obtener `person_id`)
    - .planning/phases/30-event-timeline-y-centro-de-alertas/30-RESEARCH.md "Hallazgo 6" (los 5 pasos y la recomendación de dos llamadas)
  </read_first>
  <behavior>
    - `TEST_track_scope_returns_count_and_range`: sobre un evento con tres hermanos contiguos del mismo track, `GET /api/v2/events/{id}/track-scope` devuelve 200 con `count == 4` y `from`/`to` no nulos.
    - `TEST_track_scope_404_for_unknown_event`: id inexistente → 404.
    - `TEST_track_scope_zero_without_track`: evento sin `track_id` → 200 con `{"count": 0, "event_ids": []}` (no 404: el evento existe).
    - `TEST_assign_person_updates_block`: `POST /api/v2/events/{id}/assign-person` con `{"person_id": 7}` devuelve `updated == 4` y una consulta posterior a `GET /api/v2/events` muestra esos eventos con `person_id == 7`.
    - `TEST_assign_person_requires_positive_person_id`: body `{"person_id": 0}` o `{"person_id": -3}` → 422 o 400 (validado, nunca aplicado).
    - `TEST_assign_person_404_for_unknown_event`: id inexistente → 404.
  </behavior>
  <action>
    Añadir a `backend/api/v2/events.py`, después de `get_event`:

    ```python
    @router.get("/{event_id}/track-scope")
    @limiter.limit(V2_RATE_LIMIT)
    async def get_track_scope(request: Request, event_id: str) -> dict[str, Any]:
        """Cuantos eventos anteriores/posteriores del mismo track recibirian la identidad.

        Es la previsualizacion que exige D-06/UI-SPEC ("Se aplicara tambien a los eventos
        anteriores de este track (N)"): mismo calculo que assign-person, sin escribir nada.
        """
        repo = _event_repo()
        if await repo.get(event_id) is None:
            raise HTTPException(status_code=404, detail="Event not found")
        scope = await repo.track_scope(event_id)
        if scope is None:
            return {"count": 0, "from": None, "to": None, "event_ids": []}
        return {
            "count": scope["count"],
            "from": scope["from"].isoformat(),
            "to": scope["to"].isoformat(),
            "event_ids": scope["event_ids"],
        }


    @router.post("/{event_id}/assign-person")
    @limiter.limit(V2_RATE_LIMIT)
    async def assign_person(request: Request, event_id: str,
                            person_id: int = Body(..., embed=True, ge=1)) -> dict[str, Any]:
        """Aplica una identidad ya enrolada al bloque contiguo del track (OPS-08, criterio 5).

        El enrolado en si NO se hace aqui: el cliente llama antes a POST /api/enroll_face,
        que ya valida content_type, tamano y longitud del nombre con tests de regresion de
        seguridad asociados. Duplicar esa validacion aqui seria una regresion.
        """
        repo = _event_repo()
        if await repo.get(event_id) is None:
            raise HTTPException(status_code=404, detail="Event not found")
        return await repo.assign_person(event_id, person_id)
    ```

    Añadir los 6 tests de `<behavior>` a `tests/test_events_api.py`.
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/test_events_api.py tests/test_security_regression.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "track-scope\|assign-person" backend/api/v2/events.py` → 2 matches (una ruta cada uno)
    - `grep -n "ge=1" backend/api/v2/events.py` → 1 match (validación de `person_id`)
    - `grep -n "enroll" backend/api/v2/events.py` → 0 matches en código ejecutable (solo puede aparecer en el docstring: la validación de enrolado NO se duplica)
    - `.venv/Scripts/python.exe -c "from backend.main import app; print(sorted({r.path for r in app.routes if 'v2/events' in getattr(r,'path','')}))"` incluye `/api/v2/events/{event_id}/track-scope` y `/api/v2/events/{event_id}/assign-person`
    - `.venv/Scripts/python.exe -m pytest tests/test_events_api.py -q` en verde (14 tests en total en el fichero)
    - `.venv/Scripts/python.exe -m pytest tests/test_security_regression.py -q` en verde
  </acceptance_criteria>
  <done>Los dos endpoints existen con rate limit, devuelven 404 sobre eventos inexistentes, validan `person_id` y no duplican la validación de enrolado.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Navegador → `/api/v2/events*` | Parámetros de filtro, cursor y `person_id` cruzan desde el cliente al SQL y a una escritura masiva acotada |
| Respuesta → navegador | `person_id`, `zone_id`, `track_id` y nombres de regla salen del servidor hacia el DOM |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-15 | Denial of Service | `limit` enorme o `COUNT(*)` por página | mitigate | `pagination_limit()` (cap 200, verificado por `TEST_all_v2_list_endpoints_have_limit_cap`) + `total` solo en la primera página con filtros. `@limiter.limit(V2_RATE_LIMIT)` en los cuatro endpoints |
| T-30-16 | Tampering | Cursor manipulado desde el cliente | mitigate | `_decode_cursor` puede lanzar; se captura y se responde 400 `"cursor invalido"`, nunca un 500 con traza. `max_length=200` en el parámetro |
| T-30-17 | Information Disclosure | Enumeración de identidades desde el endpoint de eventos | accept | `person_id` ya se exponía en el endpoint que este plan sustituye; **no** se añade `person_name` a la respuesta (además de que no está persistido). Mismo criterio que `context.py:9-11` |
| T-30-18 | Tampering | `assign-person` aplicando identidad a eventos ajenos | mitigate | La cota vive en el repositorio (30-03: `camera_id` + ventana + contigüidad); aquí se valida `person_id >= 1` y se exige que el evento ancla exista (404 si no) |
| T-30-19 | Elevation of Privilege | Enrolado sin las validaciones de imagen | mitigate | `assign-person` NO enrola: recibe un `person_id` ya existente. El enrolado sigue pasando por `/api/enroll_face` con su rate limit propio (5/minute) y sus validaciones de tipo/tamaño |
</threat_model>

<verification>
- `.venv/Scripts/python.exe -m pytest tests/test_events_api.py tests/test_security_regression.py tests/test_repositories.py -q` — en verde
- `.venv/Scripts/python.exe -c "from backend.main import app"` — la app importa sin colisión de rutas
- `.venv/Scripts/python.exe -m pytest tests/ -q` — suite completa sin regresiones
</verification>

<success_criteria>
- `GET /api/v2/events` sirve filtros combinables (incluido `type` repetido y `rule`), cursor, `total` condicional y mapa `media`
- El endpoint suelto de `main.py` está borrado en el mismo commit que se registra el router
- `GET /api/v2/events/{id}`, `/track-scope` y `POST /assign-person` responden con los contratos del bloque `<interfaces>`
- Todos los endpoints nuevos llevan `@limiter.limit(V2_RATE_LIMIT)` y el de lista lleva `pagination_limit()`
- Suite completa en verde
</success_criteria>

<output>
After completion, create `.planning/phases/30-event-timeline-y-centro-de-alertas/30-05-SUMMARY.md`
</output>
