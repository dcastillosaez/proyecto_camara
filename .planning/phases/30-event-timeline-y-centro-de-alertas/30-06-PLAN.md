---
phase: 30
plan: 06
type: execute
wave: 4
depends_on: ["30-05"]
files_modified:
  - backend/api/v2/alerts.py
  - backend/main.py
  - tests/test_alerts.py
autonomous: true
requirements: [OPS-11]

must_haves:
  truths:
    - "Las alertas activas llegan al navegador ya agrupadas por la regla que las disparo, con su recuento y la hora del ultimo disparo"
    - "Una regla se puede silenciar por una duracion acotada y deja de contar como alerta activa hasta que expire"
    - "El silenciado sobrevive a recargar la pagina y a reiniciar el backend"
    - "Silenciar deja rastro en el historial de eventos"
    - "Silenciar no impide que la regla siga evaluandose ni que sus acciones sigan ejecutandose"
  artifacts:
    - path: "backend/api/v2/alerts.py"
      provides: "GET /api/v2/alerts, POST /api/v2/alerts/mute, POST /api/v2/alerts/unmute"
      contains: "APIRouter(prefix=\"/api/v2/alerts\""
    - path: "backend/main.py"
      provides: "configure(event_engine) del router de alertas en lifespan + include_router"
      contains: "alerts_v2_router"
    - path: "tests/test_alerts.py"
      provides: "tests de agrupacion, silenciado, expiracion por TTL y validacion de duracion (fichero nuevo)"
  key_links:
    - from: "backend/api/v2/alerts.py::list_alerts"
      to: "backend/storage/repositories.py::EventRepo.query"
      via: "dos consultas acotadas por severidad y ventana temporal, agrupadas en Python por payload.rules"
      pattern: "payload"
    - from: "backend/api/v2/alerts.py::mute_rule"
      to: "backend/storage/repositories.py::ConfigRepo"
      via: "clave app_config 'alerts.muted_rules' con lectura-modificacion-escritura serializada por asyncio.Lock"
      pattern: "alerts.muted_rules"
    - from: "backend/api/v2/alerts.py::mute_rule"
      to: "backend/events/engine.py::EventEngine.config_changed"
      via: "emision de CONFIG_CHANGED con el nombre de la regla y la duracion"
      pattern: "config_changed"
---

<objective>
Servir el centro de alertas desde el backend: `GET /api/v2/alerts` agrupa las alertas de una ventana por la regla que las disparó (o por tipo de evento si no hubo regla), y `POST /api/v2/alerts/mute` / `unmute` gestionan el silenciado temporal persistido en `app_config`.

Purpose: OPS-11 y el criterio de éxito 6 ("el centro de alertas agrupa, permite silenciar por regla y muestra qué regla disparó"). La agrupación se hace en servidor a propósito: agruparla en el navegador exigiría descargar todo el historial (30-RESEARCH.md, Architectural Responsibility Map). El silenciado va en `app_config` vía el `ConfigRepo` ya existente en lugar de una tabla nueva — como mucho hay tantas filas como reglas en `rules.yaml`, y una tabla costaría otra subida de `SCHEMA_VERSION` (ya se gasta una en el índice de 30-02).
Output: router de alertas con los tres endpoints, cableado en `lifespan`, y tests deterministas de agrupación, silenciado y expiración.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-event-timeline-y-centro-de-alertas/30-CONTEXT.md
@.planning/phases/30-event-timeline-y-centro-de-alertas/30-RESEARCH.md
@.planning/phases/30-event-timeline-y-centro-de-alertas/30-PATTERNS.md
@.planning/phases/30-event-timeline-y-centro-de-alertas/30-UI-SPEC.md
@CLAUDE.md
</context>

<interfaces>
```jsonc
// GET /api/v2/alerts?hours=24
{
  "groups": [
    { "key": "rule:Intrusión nocturna",     // "rule:<name>" o "type:<EVENT_TYPE>"
      "rule_name": "Intrusión nocturna",     // null si el grupo no viene de una regla
      "event_type": "INTRUSION",
      "severity": "critical",                // la mas alta del grupo
      "count": 7,
      "last_ts": "2026-08-20T18:42:05",
      "last_event_id": "6f0…",
      "zone_id": "jardin",                   // el del ultimo evento, o null
      "muted_until": null,                   // iso si esta silenciada
      "mutable": true },                     // false en los grupos "type:*" (D-16: se silencia por Rule.name)
    …
  ],
  "active_count": 3,       // grupos NO silenciados — es el numero del badge de la campana
  "critical_count": 1,     // grupos no silenciados con severity critical -> badge en rojo
  "muted_count": 1,
  "window_hours": 24,
  "truncated": false,      // true si se alcanzo el tope de 200 eventos por severidad
  "checked_at": "2026-08-20T18:45:10"
}

// POST /api/v2/alerts/mute    body: {"rule_name": "Intrusión nocturna", "duration_secs": 3600}
{ "rule_name": "Intrusión nocturna", "until": "2026-08-20T19:45:10" }
// duration_secs fuera de {900, 3600, 28800} -> 400

// POST /api/v2/alerts/unmute  body: {"rule_name": "Intrusión nocturna"}
{ "rule_name": "Intrusión nocturna", "muted": false }
```

Estructura persistida en `app_config`, clave `alerts.muted_rules`:
```jsonc
{ "Intrusión nocturna": { "until": "2026-08-20T19:45:10", "camera_id": null } }
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: GET /api/v2/alerts — agrupación por regla en servidor</name>
  <files>backend/api/v2/alerts.py, backend/main.py, tests/test_alerts.py</files>
  <read_first>
    - backend/api/v2/events.py completo (creado en 30-05) — molde inmediato: docstring de módulo, `_event_repo()`, rate limit, validación de parámetros
    - backend/api/v2/context.py completo — patrón `configure()` para inyectar estado vivo y su cableado desde `lifespan`
    - backend/storage/repositories.py `EventRepo.query()` (firma tras 30-02) y `ConfigRepo` completo (líneas ~621-645: `get(key, default)` / `set(key, value)`)
    - frontend/js/views/dashboard.js líneas 263-289 (`loadActiveAlerts` actual: filtra `severity !== 'info'` y ordena por `SEVERITY_RANK` en el navegador — la semántica que este endpoint reemplaza)
    - backend/main.py líneas 616-627 (bloque `include_router`) y 449-454 (bloque de `configure()` dentro de `lifespan`)
    - .planning/phases/30-event-timeline-y-centro-de-alertas/30-RESEARCH.md "Hallazgo 8" (dónde guardar el silenciado, TTL perezoso, semántica del silencio) y 30-UI-SPEC.md sección "Centro de alertas"
  </read_first>
  <behavior>
    - `TEST_alerts_group_by_rule_name`: cinco eventos con `payload={"rules": ["Intrusión nocturna"]}` y dos con `{"rules": ["Merodeo"]}` → dos grupos, con `count` 5 y 2, `key` `"rule:…"` y `mutable is True`.
    - `TEST_alerts_group_by_type_when_no_rule`: eventos `severity="critical"` sin clave `rules` → un grupo con `key == "type:INTRUSION"`, `rule_name is None`, `mutable is False`.
    - `TEST_alerts_exclude_info_severity`: eventos `info` que no dispararon regla no aparecen en ningún grupo.
    - `TEST_alerts_include_info_events_that_fired_a_rule`: un evento `info` con `payload.rules` sí aparece (si una regla se molestó en disparar, es una alerta).
    - `TEST_alerts_sorted_by_severity_then_recency`: un grupo `warning` más reciente va DESPUÉS de un grupo `critical` más antiguo.
    - `TEST_alerts_respect_window_hours`: un evento de hace 48 h no aparece con `?hours=24`.
    - `TEST_alerts_hours_param_is_bounded`: `?hours=0` y `?hours=1000` → 422 (rango `1..168`).
    - `TEST_alerts_counts_match_groups`: `active_count` == número de grupos no silenciados y `critical_count` == número de esos que son `critical`.
  </behavior>
  <action>
    **1) Crear `backend/api/v2/alerts.py`:**

    ```python
    """API v2 — centro de alertas: agrupacion por regla y silenciado temporal (Fase 30, OPS-11).

    Auth y rate limiting: heredados de la app (FastAPI(dependencies=[Depends(verify)])) y
    de backend/api/v2/deps.py, igual que el resto de routers v2.

    La agrupacion se hace aqui y no en el navegador a proposito: agrupar en el cliente
    exigiria descargarse el historial entero (30-RESEARCH.md, Architectural Responsibility Map).
    """
    from __future__ import annotations

    import asyncio
    import datetime
    from typing import Any

    from fastapi import APIRouter, Body, HTTPException, Query, Request

    from backend.api.v2.deps import V2_RATE_LIMIT, limiter
    from backend.database import get_session_factory
    from backend.events.types import Severity
    from backend.storage.repositories import ConfigRepo, EventRepo

    router = APIRouter(prefix="/api/v2/alerts", tags=["alerts"])

    MUTED_KEY = "alerts.muted_rules"
    MUTE_DURATIONS = (900, 3600, 28800)      # 15 min / 1 h / 8 h — lista blanca (UI-SPEC)
    SEVERITY_RANK = {"critical": 2, "warning": 1, "info": 0}
    PAGE_CAP = 200                            # tope por severidad; ver docstring de list_alerts

    _mute_lock = asyncio.Lock()               # serializa el read-modify-write de app_config
    _event_engine: Any = None


    def configure(event_engine: Any = None) -> None:
        """Wire the live EventEngine (para emitir CONFIG_CHANGED). Called once from lifespan."""
        global _event_engine
        _event_engine = event_engine
    ```

    **2) Helpers de silenciado** (TTL perezoso: al leer se descartan las entradas caducadas):

    ```python
    async def _load_muted(now: datetime.datetime) -> dict[str, dict]:
        raw = await ConfigRepo(get_session_factory()).get(MUTED_KEY, {}) or {}
        return {
            name: data for name, data in raw.items()
            if data.get("until") and datetime.datetime.fromisoformat(data["until"]) > now
        }
    ```

    **3) `GET ""`:**

    ```python
    @router.get("")
    @limiter.limit(V2_RATE_LIMIT)
    async def list_alerts(request: Request, hours: int = Query(default=24, ge=1, le=168)) -> dict[str, Any]:
        """Alertas activas de la ventana, agrupadas por la regla que las disparo (OPS-11).

        Se consultan como mucho PAGE_CAP eventos por severidad (critical y warning) mas
        los que dispararon regla: el recuento por grupo se calcula sobre ese conjunto y
        `truncated` avisa si se alcanzo el tope. Es deliberado: el cajon muestra actividad
        reciente, no un censo historico, y un COUNT por grupo obligaria a una consulta por regla.
        """
        now = datetime.datetime.now()
        since = now - datetime.timedelta(hours=hours)
        repo = EventRepo(get_session_factory())
        collected: list = []
        truncated = False
        for sev in (Severity.CRITICAL, Severity.WARNING):
            items, next_cursor = await repo.query(severity=sev, ts_from=since, limit=PAGE_CAP)
            collected.extend(items)
            truncated = truncated or next_cursor is not None
        # eventos info que SI dispararon una regla: si una regla se molesto en disparar, es alerta
        info_items, info_cursor = await repo.query(severity=Severity.INFO, ts_from=since, limit=PAGE_CAP)
        collected.extend([e for e in info_items if e.payload.get("rules")])
        truncated = truncated or info_cursor is not None

        muted = await _load_muted(now)
        groups: dict[str, dict] = {}
        for ev in collected:
            rules = ev.payload.get("rules") or []
            keys = [("rule:" + r, r) for r in rules] or [("type:" + ev.type.value, None)]
            for key, rule_name in keys:
                g = groups.setdefault(key, {
                    "key": key, "rule_name": rule_name, "event_type": ev.type.value,
                    "severity": ev.severity.value, "count": 0,
                    "last_ts": ev.ts, "last_event_id": ev.id, "zone_id": ev.zone_id,
                    "mutable": rule_name is not None,
                })
                g["count"] += 1
                if SEVERITY_RANK.get(ev.severity.value, 0) > SEVERITY_RANK.get(g["severity"], 0):
                    g["severity"] = ev.severity.value
                if ev.ts > g["last_ts"]:
                    g.update(last_ts=ev.ts, last_event_id=ev.id,
                             zone_id=ev.zone_id, event_type=ev.type.value)

        out = []
        for g in groups.values():
            entry = dict(g)
            entry["last_ts"] = g["last_ts"].isoformat()
            m = muted.get(g["rule_name"]) if g["rule_name"] else None
            entry["muted_until"] = m["until"] if m else None
            out.append(entry)
        out.sort(key=lambda g: (-SEVERITY_RANK.get(g["severity"], 0), g["last_ts"]), reverse=False)
        out.sort(key=lambda g: (SEVERITY_RANK.get(g["severity"], 0), g["last_ts"]), reverse=True)

        active = [g for g in out if g["muted_until"] is None]
        return {
            "groups": out,
            "active_count": len(active),
            "critical_count": sum(1 for g in active if g["severity"] == "critical"),
            "muted_count": len(out) - len(active),
            "window_hours": hours,
            "truncated": truncated,
            "checked_at": now.isoformat(),
        }
    ```
    (Si el doble `sort` resulta redundante al implementarlo, dejar UNO solo: `out.sort(key=lambda g: (SEVERITY_RANK.get(g["severity"], 0), g["last_ts"]), reverse=True)` — severidad descendente y, dentro de la misma severidad, más reciente primero, que es lo que exige UI-SPEC.)

    **4) Cablear en `backend/main.py`:** en el bloque de `include_router` (tras el de events, 30-05):
    ```python
    from backend.api.v2.alerts import router as alerts_v2_router
    app.include_router(alerts_v2_router)
    ```
    y en `lifespan()`, junto a los `configure()` existentes (líneas 449-454):
    ```python
    from backend.api.v2 import alerts as alerts_v2_module
    alerts_v2_module.configure(event_engine)
    ```

    **5) Crear `tests/test_alerts.py`** con los 8 tests de `<behavior>`, siguiendo el patrón ASGI de `tests/test_events_api.py`.
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/test_alerts.py tests/test_security_regression.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `backend/api/v2/alerts.py` existe y `grep -n 'APIRouter(prefix="/api/v2/alerts"' backend/api/v2/alerts.py` → 1 match
    - `grep -n "MUTED_KEY = \"alerts.muted_rules\"" backend/api/v2/alerts.py` → 1 match
    - `grep -c "@limiter.limit(V2_RATE_LIMIT)" backend/api/v2/alerts.py` → igual al número de endpoints del fichero
    - `grep -n "include_router(alerts_v2_router)" backend/main.py` → 1 match
    - `grep -n "alerts_v2_module.configure" backend/main.py` → 1 match
    - `.venv/Scripts/python.exe -c "from backend.main import app; print(sorted({r.path for r in app.routes if 'v2/alerts' in getattr(r,'path','')}))"` incluye `/api/v2/alerts`
    - `.venv/Scripts/python.exe -m pytest tests/test_alerts.py -q` en verde (8 tests)
  </acceptance_criteria>
  <done>El endpoint devuelve grupos ordenados por severidad y recencia, con `count`, `last_ts`, `muted_until` y los contadores del badge, sin que el navegador tenga que filtrar nada.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Silenciar y reactivar reglas con persistencia en app_config</name>
  <files>backend/api/v2/alerts.py, tests/test_alerts.py</files>
  <read_first>
    - backend/api/v2/alerts.py (creado en Task 1: `MUTED_KEY`, `MUTE_DURATIONS`, `_mute_lock`, `_load_muted`, `_event_engine`)
    - backend/storage/repositories.py `ConfigRepo.get`/`set` (upsert de un JSON arbitrario, ya probado)
    - backend/api/v2/detection.py (el PUT de clases: orden validar → persistir → propagar → emitir `CONFIG_CHANGED`; molde exacto de esta tarea, incluido el mensaje de error en lenguaje llano)
    - backend/events/engine.py líneas 310-325 (`EventEngine.config_changed(now, **detail)`: firma exacta)
    - .planning/phases/30-event-timeline-y-centro-de-alertas/30-RESEARCH.md "Hallazgo 8", subsección "Semántica del silencio — decidir explícitamente"
  </read_first>
  <behavior>
    - `TEST_mute_persists_until_in_app_config`: `POST /api/v2/alerts/mute {"rule_name": "R", "duration_secs": 900}` → 200 con `until` ≈ ahora+15 min; la clave `alerts.muted_rules` de `app_config` contiene esa entrada.
    - `TEST_mute_rejects_arbitrary_duration`: `duration_secs: 12345` → 400 con `detail` legible que menciona las duraciones válidas.
    - `TEST_mute_rejects_empty_rule_name`: `rule_name: ""` o ausente → 400/422.
    - `TEST_muted_rule_is_excluded_from_active_count`: tras silenciar una regla, `GET /api/v2/alerts` sigue devolviendo su grupo (atenuado, con `muted_until` no nulo) pero `active_count` baja en 1.
    - `TEST_expired_mute_is_ignored_and_purged_on_write`: con una entrada cuyo `until` ya pasó, `GET /api/v2/alerts` la trata como no silenciada; tras un `mute`/`unmute` posterior, la entrada caducada ya no está en `app_config`.
    - `TEST_unmute_removes_entry`: `POST /api/v2/alerts/unmute` devuelve `{"muted": false}` y la entrada desaparece de `app_config`.
    - `TEST_mute_emits_config_changed`: con `_event_engine` parcheado por un doble, silenciar invoca `config_changed` con el nombre de la regla y la duración.
  </behavior>
  <action>
    Añadir a `backend/api/v2/alerts.py`:

    ```python
    @router.post("/mute")
    @limiter.limit(V2_RATE_LIMIT)
    async def mute_rule(request: Request,
                        rule_name: str = Body(..., min_length=1, max_length=100),
                        duration_secs: int = Body(...)) -> dict[str, Any]:
        """Silencia una regla durante un tiempo acotado (OPS-11, D-16/D-17).

        Silenciar es SOLO de presentacion: la regla se sigue evaluando, sus acciones se
        siguen ejecutando (sigue grabando el clip y mandando el aviso) y el evento se
        sigue persistiendo. Lo unico que cambia es que su grupo sale atenuado en el cajon
        y no cuenta para el badge. Silenciar desde la UI no debe hacerte perder pruebas
        (30-RESEARCH.md Hallazgo 8).
        """
        if duration_secs not in MUTE_DURATIONS:
            raise HTTPException(
                status_code=400,
                detail=f"duracion no valida: elige una de {MUTE_DURATIONS} segundos")
        now = datetime.datetime.now()
        until = now + datetime.timedelta(seconds=duration_secs)
        async with _mute_lock:                    # read-modify-write sobre una unica fila
            repo = ConfigRepo(get_session_factory())
            current = await _load_muted(now)      # ya purgado de caducadas
            current[rule_name] = {"until": until.isoformat(), "camera_id": None}
            await repo.set(MUTED_KEY, current)
        if _event_engine is not None:
            _event_engine.config_changed(now, muted_rule=rule_name, duration_secs=duration_secs)
        return {"rule_name": rule_name, "until": until.isoformat()}


    @router.post("/unmute")
    @limiter.limit(V2_RATE_LIMIT)
    async def unmute_rule(request: Request,
                          rule_name: str = Body(..., embed=True, min_length=1, max_length=100)
                          ) -> dict[str, Any]:
        """Reactiva una regla silenciada antes de que expire su duracion."""
        now = datetime.datetime.now()
        async with _mute_lock:
            repo = ConfigRepo(get_session_factory())
            current = await _load_muted(now)
            current.pop(rule_name, None)
            await repo.set(MUTED_KEY, current)
        if _event_engine is not None:
            _event_engine.config_changed(now, unmuted_rule=rule_name)
        return {"rule_name": rule_name, "muted": False}
    ```

    Nota de implementación: con dos campos en el body, FastAPI ya espera un objeto JSON `{"rule_name": ..., "duration_secs": ...}` sin `embed`; en `unmute`, con un solo campo, `embed=True` es obligatorio para que el body siga siendo un objeto y no un string suelto. Verificarlo con el test antes de dar la tarea por hecha.

    Añadir los 7 tests de `<behavior>` a `tests/test_alerts.py`.
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/test_alerts.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "MUTE_DURATIONS = (900, 3600, 28800)" backend/api/v2/alerts.py` → 1 match (lista blanca, no entero libre)
    - `grep -c "_mute_lock" backend/api/v2/alerts.py` → 3 matches (definición + los dos `async with`)
    - `grep -n "config_changed" backend/api/v2/alerts.py` → 2 matches (mute y unmute dejan rastro)
    - `grep -n "rule_engine\|run_actions\|enabled = False" backend/api/v2/alerts.py` → 0 matches (silenciar NO desactiva la regla ni sus acciones)
    - `.venv/Scripts/python.exe -c "from backend.main import app; print(sorted({r.path for r in app.routes if 'v2/alerts' in getattr(r,'path','')}))"` imprime `['/api/v2/alerts', '/api/v2/alerts/mute', '/api/v2/alerts/unmute']`
    - `.venv/Scripts/python.exe -m pytest tests/test_alerts.py -q` en verde (15 tests en total en el fichero)
    - `.venv/Scripts/python.exe -m pytest tests/test_security_regression.py -q` en verde
  </acceptance_criteria>
  <done>Silenciar/reactivar persisten en `app_config` de forma serializada, la duración está en lista blanca, la expiración es perezosa y cada cambio deja un `CONFIG_CHANGED` en el historial.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Navegador → `POST /api/v2/alerts/mute` | Una petición del cliente muta estado de configuración que afecta a lo que ve todo operador |
| `app_config` → `GET /api/v2/alerts` | Estado de silenciado leído en cada consulta del cajón |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-20 | Repudiation | Silenciar una regla crítica sin dejar rastro | mitigate | Cada `mute`/`unmute` emite `EventType.CONFIG_CHANGED` con el nombre de la regla y la duración (mismo criterio que el PUT de clases de la Fase 27, ASVS V4 sin roles) |
| T-30-21 | Tampering | Silenciado permanente o duración arbitraria | mitigate | Lista blanca `MUTE_DURATIONS = (900, 3600, 28800)`; cualquier otro valor → 400. No existe "para siempre" |
| T-30-22 | Denial of Service (pérdida de evidencia) | Silenciar suprimiendo grabación o aviso | mitigate | El silencio es solo de presentación por diseño: `alerts.py` no toca `RuleEngine` ni `run_actions` (criterio de aceptación grep-verificable). Las acciones siguen ejecutándose |
| T-30-23 | Tampering (condición de carrera) | Dos silenciados simultáneos sobre la misma fila de `app_config` | mitigate | `asyncio.Lock` de módulo alrededor del read-modify-write; `ConfigRepo` se mantiene agnóstico de negocio |
| T-30-24 | Denial of Service | `hours` enorme → ventana ilimitada | mitigate | `Query(ge=1, le=168)` y tope `PAGE_CAP=200` por severidad, con `truncated` en la respuesta |
</threat_model>

<verification>
- `.venv/Scripts/python.exe -m pytest tests/test_alerts.py tests/test_events_api.py tests/test_security_regression.py -q` — en verde
- `.venv/Scripts/python.exe -m pytest tests/ -q` — suite completa sin regresiones
</verification>

<success_criteria>
- `GET /api/v2/alerts` agrupa por regla (y por tipo cuando no hubo regla), ordena por severidad y recencia y expone `active_count`/`critical_count`
- Silenciar persiste en `app_config`, expira solo y no altera la evaluación de reglas ni sus acciones
- La duración está limitada a 15 min / 1 h / 8 h
- Cada silenciado/reactivación deja un `CONFIG_CHANGED` en el historial
- Suite completa en verde
</success_criteria>

<output>
After completion, create `.planning/phases/30-event-timeline-y-centro-de-alertas/30-06-SUMMARY.md`
</output>
