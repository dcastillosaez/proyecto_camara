---
phase: 30
plan: 10
type: execute
wave: 8
depends_on: ["30-08", "30-09"]
files_modified:
  - frontend/js/websocket.js
  - frontend/js/app.js
  - tests/test_frontend_modules.py
autonomous: true
requirements: [OPS-07, OPS-10, OPS-11]

must_haves:
  truths:
    - "Un evento nuevo emitido por el backend llega a la linea temporal sin recargar la pagina"
    - "Un cruce de linea aparece UNA sola vez en la linea temporal, aunque el backend emita dos mensajes distintos por el"
    - "Al perder la conexion aparece el aviso de sin tiempo real y al recuperarla la lista se sincroniza sola"
    - "La linea temporal y el centro de alertas arrancan con el resto del dashboard"
  artifacts:
    - path: "frontend/js/websocket.js"
      provides: "case 'event' -> timeline.onLiveEvent y aviso de desconexion atado a onopen/onclose"
      contains: "msg.type === 'event'"
    - path: "frontend/js/app.js"
      provides: "initTimeline() y bindAlertCenter()/loadAlerts() en el arranque; loadActiveAlerts retirado"
      contains: "initTimeline"
    - path: "tests/test_frontend_modules.py"
      provides: "los modulos nuevos declarados en LOCKED_JS"
      contains: "views/timeline.js"
  key_links:
    - from: "frontend/js/websocket.js"
      to: "frontend/js/views/timeline.js::onLiveEvent"
      via: "un case mas en el dispatch por msg.type, igual que 'tracks' en la Fase 29"
      pattern: "onLiveEvent"
    - from: "frontend/js/app.js"
      to: "frontend/js/components/alertCenter.js::bindAlertCenter"
      via: "bindings en DOMContentLoaded"
      pattern: "bindAlertCenter"
---

<objective>
Cablear la línea temporal y el centro de alertas al arranque de la aplicación y al canal WebSocket: un `case 'event'` más en el dispatch de `websocket.js`, el aviso de "sin tiempo real" atado al `onopen`/`onclose` que ya existe, y las inicializaciones en `app.js`. Declarar los módulos nuevos en `LOCKED_JS`.

Purpose: cierra OPS-10 (evento nuevo en <1 s sin recargar) y pone en marcha lo construido en 30-08 y 30-09. Va en un plan aparte porque toca los tres ficheros que ya se habían modificado en 30-07 y porque el riesgo principal —pintar dos veces el mismo `LINE_CROSSED`— se verifica aquí, no antes.
Output: `websocket.js` con el caso nuevo, `app.js` completo y el test de contrato de módulos actualizado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-event-timeline-y-centro-de-alertas/30-CONTEXT.md
@.planning/phases/30-event-timeline-y-centro-de-alertas/30-RESEARCH.md
@.planning/phases/30-event-timeline-y-centro-de-alertas/30-PATTERNS.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: websocket.js — case 'event' y aviso de sin tiempo real</name>
  <files>frontend/js/websocket.js</files>
  <read_first>
    - frontend/js/websocket.js completo (85 líneas, ya limpiado de `addEvent` en 30-07): imports de la línea 2-5, dispatch `onmessage` líneas 47-74 con el `else if (msg.type === 'tracks')` de la Fase 29 —el molde exacto—, `onopen` líneas 40-45 y `onclose` líneas 76-82 con el backoff que NO se toca
    - frontend/js/views/timeline.js (`onLiveEvent(event, media)` y `setTimelineOffline(offline)` de 30-08)
    - .planning/phases/30-event-timeline-y-centro-de-alertas/30-01-PLAN.md bloque `<interfaces>` (forma exacta del mensaje `{"type":"event","event":…,"media":…}`)
    - .planning/phases/30-event-timeline-y-centro-de-alertas/30-RESEARCH.md "Pitfall 6: doble pintado del evento de cruce de línea" y "Don't Hand-Roll → Reconexión WebSocket"
  </read_first>
  <action>
    **1) Import**: añadir `import { onLiveEvent, setTimelineOffline } from './views/timeline.js';`.

    **2) Dispatch**: añadir un `else if` más al final del `onmessage`, con el mismo molde que `tracks`:
    ```javascript
        } else if (msg.type === 'event') {
          // Evento tipado completo (Fase 30, OPS-10). El case 'detection' de arriba sigue
          // alimentando la grafica horaria y los contadores de la Fase 5, pero ya NO pinta
          // filas: un LINE_CROSSED llega por los dos mensajes y se pintaria dos veces
          // (30-RESEARCH.md Pitfall 6).
          onLiveEvent(msg.event, msg.media);
        }
    ```

    **3) Aviso de desconexión**: en `onopen`, tras `setWsConnected(true, 0)`, añadir `setTimelineOffline(false);`. En `onclose`, tras `setWsStatus(false)`, añadir `setTimelineOffline(true);`. No crear un segundo mecanismo de detección de desconexión ni tocar `_wsRetry`/`_wsCloseCount`: el backoff de 1 s→30 s ya cumple OPS-06 desde la Fase 29.

    **4)** Confirmar que en el bloque `else if (msg.type === 'detection')` NO queda ninguna llamada que pinte filas (debe haber quedado así tras 30-07) y que `updateStat`, `bumpHourBar` y `showToast` siguen ahí.
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/test_frontend_modules.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "msg.type === 'event'" frontend/js/websocket.js` → 1 match
    - `grep -n "onLiveEvent(msg.event, msg.media)" frontend/js/websocket.js` → 1 match
    - `grep -c "setTimelineOffline" frontend/js/websocket.js` → 3 (import + `onopen` + `onclose`)
    - `grep -n "addEvent(" frontend/js/websocket.js` → 0 matches (el `LINE_CROSSED` no se pinta dos veces)
    - `grep -n "updateStat\|bumpHourBar\|showToast" frontend/js/websocket.js` → siguen presentes en el bloque `'detection'`
    - `git diff frontend/js/websocket.js | grep "^[-+].*_wsRetry\|^[-+].*_wsCloseCount"` → vacío (el backoff no se tocó)
    - `wc -l frontend/js/websocket.js` < 300
    - `.venv/Scripts/python.exe -m pytest tests/test_frontend_modules.py -q` en verde
  </acceptance_criteria>
  <done>El evento tipado llega a la timeline por el socket que ya existía, sin duplicar el cruce de línea y sin duplicar el mecanismo de reconexión.</done>
</task>

<task type="auto">
  <name>Task 2: app.js — arranque de timeline y centro de alertas; LOCKED_JS actualizado</name>
  <files>frontend/js/app.js, tests/test_frontend_modules.py</files>
  <read_first>
    - frontend/js/app.js completo (47 líneas, ya con `bindEventExport` tras 30-07): bloque de bindings líneas 17-21 y bloque de carga inicial líneas 25-46, incluida la llamada a `loadActiveAlerts()` de la línea 43 que ya no existe como export
    - frontend/js/views/timeline.js (`initTimeline`, `refreshPersonNames`) y frontend/js/components/alertCenter.js (`bindAlertCenter`, `loadAlerts`) — nombres exactos de los exports
    - tests/test_frontend_modules.py líneas 20-45 (`LOCKED_CSS`, `LOCKED_JS`, `TEST_js_modules_exist`)
    - frontend/js/components/personGallery.js líneas 5-10 (`loadPersons` con `setInterval` de 30 s — el mapa de nombres de la timeline debe refrescarse al mismo ritmo)
  </read_first>
  <action>
    **1) `frontend/js/app.js`:**
    - Añadir los imports: `import { initTimeline, refreshPersonNames } from './views/timeline.js';` y `import { bindAlertCenter, loadAlerts } from './components/alertCenter.js';`
    - Quitar `loadActiveAlerts` del import de `./views/dashboard.js` y su llamada de la línea 43 (se mudó a `alertCenter.js` en 30-09).
    - En el bloque de bindings, añadir `bindAlertCenter();` junto a los demás `bind*()`.
    - En el bloque de carga inicial, añadir `initTimeline();` (después de `loadInitialData()` y antes de `connectWS()`, para que el observer y los controles existan cuando lleguen los primeros mensajes) y `loadAlerts();` donde estaba `loadActiveAlerts()`.
    - Junto a `setInterval(loadPersons, 30000)`, añadir `setInterval(refreshPersonNames, 30000);` para que el `Map<person_id, name>` de la timeline no se quede rancio (Hallazgo 5: `person_name` no está persistido y se resuelve en cliente).

    **2) `tests/test_frontend_modules.py`:** añadir a `LOCKED_JS` las entradas de los módulos nuevos, en el orden que corresponda:
    ```python
        "views/timeline.js",
        "views/timeline-row.js",
        "components/alertCenter.js",
    ```
    (más `"views/timeline-virtualize.js"` y/o `"components/alertMute.js"` SOLO si 30-08/30-09 llegaron a crearlos). Actualizar el docstring del módulo mencionando que la Fase 30 añade estos módulos al contrato.
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/test_frontend_modules.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "initTimeline\|bindAlertCenter\|loadAlerts\|refreshPersonNames" frontend/js/app.js` → ≥ 6 (imports + llamadas)
    - `grep -n "loadActiveAlerts" frontend/js/app.js` → 0 matches
    - `grep -n "setInterval(refreshPersonNames, 30000)" frontend/js/app.js` → 1 match
    - `grep -c "views/timeline.js\|views/timeline-row.js\|components/alertCenter.js" tests/test_frontend_modules.py` → 3
    - `.venv/Scripts/python.exe -m pytest tests/test_frontend_modules.py -q` en verde (`TEST_js_modules_exist` confirma que los tres ficheros existen y `TEST_line_limit` que ninguno supera 300 líneas)
    - `.venv/Scripts/python.exe -m pytest tests/ -q` — suite completa en verde
  </acceptance_criteria>
  <done>El dashboard arranca con la línea temporal y el centro de alertas, el mapa de nombres se refresca cada 30 s y los módulos nuevos están declarados en el contrato mecánico de la Fase 28.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `/ws` → `onLiveEvent` | El mensaje del socket entra directamente al renderizado de la lista |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-35 | Tampering (XSS) | `msg.event` pasado sin validar a `onLiveEvent` | mitigate | El renderizado usa `textContent` (30-08); además el socket ya exige `verify_ws_token` en el `accept()`, así que el mensaje no procede de un tercero no autenticado |
| T-30-36 | Denial of Service (funcional) | Doble pintado de `LINE_CROSSED` por los dos mensajes | mitigate | `addEvent` retirado del `case 'detection'` en 30-07 y verificado a cero aquí; además `onLiveEvent` descarta ids ya presentes en `_all` |
</threat_model>

<verification>
- `.venv/Scripts/python.exe -m pytest tests/test_frontend_modules.py -q` — en verde
- `.venv/Scripts/python.exe -m pytest tests/ -q` — suite completa en verde
- Comprobación manual anotada en el SUMMARY: con el servidor arrancado, un evento real aparece en la lista sin recargar y un cruce de línea aparece una sola vez
</verification>

<success_criteria>
- `websocket.js` despacha `type:"event"` a la timeline y ata el aviso de desconexión al backoff existente
- Ningún evento se pinta dos veces
- `app.js` inicializa timeline y centro de alertas, y ya no importa `loadActiveAlerts`
- `LOCKED_JS` incluye los módulos nuevos y `tests/test_frontend_modules.py` sigue en verde
- Suite completa en verde
</success_criteria>

<output>
After completion, create `.planning/phases/30-event-timeline-y-centro-de-alertas/30-10-SUMMARY.md`
</output>
