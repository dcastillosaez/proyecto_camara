---
phase: 30
plan: 07
type: execute
wave: 5
depends_on: ["30-05", "30-06"]
files_modified:
  - frontend/css/components.css
  - frontend/index.html
  - frontend/js/views/dashboard-events.js
  - frontend/js/views/dashboard.js
  - frontend/js/websocket.js
  - frontend/js/app.js
autonomous: true
requirements: [OPS-07, OPS-08, OPS-11]

must_haves:
  truths:
    - "El card de eventos de la columna derecha es la linea temporal nueva, en el mismo sitio, sin vista nueva ni router"
    - "La cabecera tiene un boton campana de 44x44 con badge que abre un cajon lateral de alertas"
    - "Existe un modal propio para marcar como persona, con hueco para el recorte y el aviso de alcance retroactivo"
    - "Nada del codigo JS antiguo apunta a elementos del DOM que ya no existen"
  artifacts:
    - path: "frontend/css/components.css"
      provides: "clases .timeline-row .sev-bar .sev-dot .rule-chip .chip .row-action .timeline-sep .tl-skeleton .filter-chip .new-pill .alert-group #alert-drawer"
      contains: ".timeline-row"
    - path: "frontend/index.html"
      provides: "card de linea temporal, boton campana + badge, aside del cajon de alertas y modal de marcar como persona"
      contains: "timeline-list"
    - path: "frontend/js/views/dashboard-events.js"
      provides: "solo la grafica horaria y el panel de borrado; addEvent/applyFilters/bindEventFilters retirados"
  key_links:
    - from: "frontend/index.html"
      to: "frontend/css/components.css"
      via: "clases nuevas aplicadas al marcado de la timeline y del cajon"
      pattern: "timeline-row|alert-drawer"
    - from: "frontend/js/app.js"
      to: "frontend/js/views/dashboard-events.js"
      via: "bindEventExport() sustituye a bindEventFilters()"
      pattern: "bindEventExport"
---

<objective>
Sustituir el card "Eventos recientes" por el marcado de la línea temporal, añadir el botón campana con badge, el cajón lateral de alertas y el modal de "Marcar como persona", con sus estilos en `components.css`; y retirar en el mismo plan todo el JS que apuntaba a los elementos eliminados.

Purpose: es el andamiaje del que dependen 30-08 (timeline.js), 30-09 (alertCenter.js) y 30-11 (markPerson.js). La retirada de lo obsoleto va aquí y no después a propósito: `bindEventFilters()` y `loadInitialData()` hacen `document.getElementById('btn-apply-filters').addEventListener(...)` sin comprobar `null`, así que borrar el marcado sin borrar esas llamadas rompería el arranque completo de `app.js`.
Output: marcado y estilos completos según `30-UI-SPEC.md`, con los ids que los módulos posteriores consumen como contrato, y `dashboard-events.js`/`dashboard.js`/`websocket.js`/`app.js` limpios.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-event-timeline-y-centro-de-alertas/30-CONTEXT.md
@.planning/phases/30-event-timeline-y-centro-de-alertas/30-UI-SPEC.md
@.planning/phases/30-event-timeline-y-centro-de-alertas/30-PATTERNS.md
@.planning/phases/30-event-timeline-y-centro-de-alertas/30-RESEARCH.md
@CLAUDE.md
</context>

<interfaces>
**Contrato de ids del DOM** — 30-08, 30-09 y 30-11 los consumen literalmente. Cualquier cambio de nombre aquí rompe esos planes.

Línea temporal:
`#timeline-card` · `#timeline-count` · `#timeline-list` · `#timeline-empty` · `#timeline-empty-filtered` · `#btn-tl-clear-empty` · `#timeline-error` · `#btn-tl-retry` · `#timeline-loading` · `#timeline-end` · `#timeline-sentinel` · `#timeline-newpill` · `#timeline-offline`

Filtros:
`#tl-filter-types` (contenedor de chips, cada chip `<button class="filter-chip" data-type="INTRUSION">`) · `#tl-filter-severity` (tres `<button class="filter-chip" data-severity="critical|warning|info">`) · `#tl-filter-zone` (`<select>`) · `#tl-filter-person` (`<input list="tl-person-options">` + `<datalist id="tl-person-options">`) · `#tl-filter-from` · `#tl-filter-to` · `#btn-tl-apply` · `#btn-tl-clear` · `#tl-active-filters`

Centro de alertas:
`#btn-alert-center` (44×44 en la cabecera, a la izquierda de `#clock`) · `#alert-badge` · `#alert-drawer` · `#alert-drawer-title` · `#alert-count-hero` · `#alert-groups` · `#alert-empty` · `#alert-footer-count` · `#alert-checked-at` · `#btn-alert-close` · `#alert-mute-popover` · `#alert-mute-title` (los botones de duración dentro del popover llevan `data-duration="900|3600|28800"`) · `#btn-alerts-view-all` (enlace "Ver todas" en la cabecera del panel "Alertas activas" de la Fase 29)

Marcar como persona:
`#mark-person-modal` · `#mark-person-preview` (`<img>` 96×96) · `#mark-person-name` (`<input list="mark-person-options">` + `<datalist id="mark-person-options">`) · `#mark-person-scope` · `#mark-person-error` · `#btn-mark-person-confirm` · `#btn-mark-person-cancel`

Acciones de fila (las crea 30-08 en JS, pero las clases se definen aquí):
`.row-action` con `data-action="clip|snapshot|person|dismiss"`
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Estilos nuevos en components.css</name>
  <files>frontend/css/components.css</files>
  <read_first>
    - frontend/css/components.css completo (81 líneas: `.ptz-btn` líneas 1-15 —la caja 44×44 a reutilizar—, `.event-item` líneas 33-42, `.toast` líneas 44-49, `.intrusion-badge` líneas 57-61, `.filter-input` líneas 67-73, `#clip-modal` líneas 75-81 —el mecanismo `display:none` → `.open` a imitar—)
    - .planning/phases/30-event-timeline-y-centro-de-alertas/30-UI-SPEC.md secciones "Spacing Scale" (excepciones: 32×32 acciones, 44×44 campana, 64×36 miniatura, fila de 52px, chips de 20px), "Typography" (4 tamaños exactos, 2 pesos), "Color" (tabla de severidad y regla del acento azul) y "Layout & Component Contracts"
    - tests/test_frontend_modules.py líneas 48-55 (`TEST_line_limit`: tope de 300 líneas también para `frontend/css/*.css`)
  </read_first>
  <action>
    Añadir al final de `frontend/css/components.css` los bloques siguientes, respetando los valores EXACTOS del UI-SPEC (no aproximar):

    ```css
    /* ── Fase 30: linea temporal ─────────────────────────────────────── */
    .timeline-row {
      display: flex; align-items: center; gap: 8px;
      height: 52px; padding: 0 8px 0 0;
      background: #1e293b; border-radius: 10px;
      position: relative; overflow: hidden;
    }
    .sev-bar { width: 3px; height: 100%; flex-shrink: 0; background: var(--sev, #64748b); }
    .sev-dot { width: 6px; height: 6px; border-radius: 9999px; background: var(--sev, #64748b); flex-shrink: 0; }
    .tl-thumb {
      width: 64px; height: 36px; flex-shrink: 0;
      object-fit: cover; border-radius: 6px; background: #0f172a;
      border: 1px solid transparent;
    }
    .tl-thumb:hover { border-color: #3b82f6; }
    .chip {
      display: inline-flex; align-items: center; height: 20px;
      padding: 4px 8px; line-height: 1; border-radius: 6px; font-size: 12px;
      background: #1e293b; border: 1px solid #334155; color: #94a3b8; flex-shrink: 0;
    }
    .rule-chip { /* misma caja que .chip; el color lo pone la clase .chip acompañante */ }
    .row-action {
      width: 32px; height: 32px; display: flex; align-items: center; justify-content: center;
      border-radius: 8px; color: #94a3b8; background: transparent; border: none;
      cursor: pointer; opacity: 0.55; transition: opacity 120ms ease, color 120ms ease;
    }
    .timeline-row:hover .row-action,
    .timeline-row:focus-within .row-action { opacity: 1; }
    .row-action:hover { color: #e2e8f0; }
    .row-action:focus-visible { outline: 2px solid #3b82f6; outline-offset: 2px; opacity: 1; }
    .row-action[disabled] { opacity: 0.25; cursor: not-allowed; }
    .timeline-sep {
      position: sticky; top: 0; z-index: 1;
      height: 24px; display: flex; align-items: center;
      font-size: 12px; letter-spacing: 0.05em; color: #475569;
      background: rgba(15,23,42,0.85); backdrop-filter: blur(6px);
    }
    .tl-skeleton { height: 52px; border-radius: 10px; background: #1e293b; animation: tl-pulse 1.2s ease-in-out infinite; }
    @keyframes tl-pulse { 0%,100% { opacity: 0.35; } 50% { opacity: 0.7; } }
    .filter-chip {
      height: 24px; padding: 4px 8px; line-height: 1; border-radius: 6px; font-size: 12px;
      font-weight: 600; background: #1e293b; border: 1px solid #334155; color: #94a3b8; cursor: pointer;
    }
    .filter-chip.active { background: #2563eb; border-color: #3b82f6; color: #fff; }
    .filter-chip:focus-visible { outline: 2px solid #3b82f6; outline-offset: 2px; }
    .new-pill {
      position: absolute; left: 50%; transform: translateX(-50%); top: 4px; z-index: 2;
      background: #2563eb; color: #fff; font-size: 12px; font-weight: 600;
      padding: 4px 8px; border-radius: 9999px; cursor: pointer; border: none;
    }

    /* ── Fase 30: centro de alertas ──────────────────────────────────── */
    #alert-drawer {
      display: none; position: fixed; inset: 0; z-index: 50;
      background: rgba(2,6,23,0.6); backdrop-filter: blur(6px);
    }
    #alert-drawer.open { display: block; }
    #alert-drawer .drawer-panel {
      position: absolute; top: 0; right: 0; height: 100%; width: 380px; max-width: 100vw;
      background: #0f172a; border-left: 1px solid #1e293b;
      display: flex; flex-direction: column; gap: 24px; padding: 16px;
    }
    .alert-group {
      display: flex; align-items: center; gap: 8px;
      padding: 8px; border-radius: 10px; background: #1e293b; border: 1px solid #334155;
    }
    .alert-group.muted { opacity: 0.5; }
    #alert-badge {
      position: absolute; top: -4px; right: -4px;
      width: 20px; height: 20px; border-radius: 9999px;
      font-size: 12px; font-weight: 700; line-height: 1;
      display: flex; align-items: center; justify-content: center; color: #fff;
    }
    #alert-mute-popover { display: none; position: absolute; z-index: 55; }
    #alert-mute-popover.open { display: block; }
    ```

    Comprobar el tope de líneas al terminar: `components.css` pasa de 81 a ~200 líneas, muy por debajo de las 300 que exige `TEST_line_limit`.
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/test_frontend_modules.py -k line_limit -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^\.timeline-row\|^\.sev-bar\|^\.sev-dot\|^\.rule-chip\|^\.row-action\|^\.timeline-sep\|^\.alert-group\|^#alert-drawer\|^\.filter-chip\|^\.new-pill\|^\.tl-skeleton\|^\.chip\|^\.tl-thumb" frontend/css/components.css` → ≥ 13
    - `grep -n "height: 52px" frontend/css/components.css` → ≥ 1 (fila de altura fija, requisito de la virtualización)
    - `grep -n "width: 32px; height: 32px" frontend/css/components.css` → 1 match en `.row-action` (WCAG 2.5.8)
    - `grep -n "width: 64px; height: 36px" frontend/css/components.css` → 1 match en `.tl-thumb`
    - `grep -n "width: 380px" frontend/css/components.css` → 1 match en el panel del cajón
    - `grep -n "font-size: 1[01]px\|font-size: [0-9]px" frontend/css/components.css` → ningún match nuevo introducido por este plan (los 4 tamaños del UI-SPEC son 12/14/16/30px; el 9px de `.intrusion-badge` es legado preexistente)
    - `wc -l frontend/css/components.css` → menor que 300
    - `.venv/Scripts/python.exe -m pytest tests/test_frontend_modules.py -q` en verde
  </acceptance_criteria>
  <done>Los 13 bloques del contrato visual existen con las medidas exactas del UI-SPEC y el fichero sigue bajo el tope de 300 líneas.</done>
</task>

<task type="auto">
  <name>Task 2: Marcado — card de línea temporal, campana + cajón y modal de marcar como persona</name>
  <files>frontend/index.html</files>
  <read_first>
    - frontend/index.html líneas 18-44 (cabecera: `#cam-status` y `#clock` — la campana va a la izquierda del reloj)
    - frontend/index.html líneas 210-230 (panel "Alertas activas" de la Fase 29: `#alerts-active-list`, `#alerts-active-empty`, `#alerts-active-checked-at` — su cabecera gana el enlace "Ver todas", el resto NO se toca)
    - frontend/index.html líneas 445-525 completas (card "EVENTS LIST": cabecera con `#events-badge`/`#btn-export-csv`/`#btn-delete-events`, barra de filtros `#filter-*`, panel `#delete-events-panel`, `#events-list`/`#events-empty`) — se sustituye el contenido conservando `#btn-export-csv`, `#btn-delete-events` y TODO `#delete-events-panel` intactos
    - frontend/index.html (bloque de `#clip-modal` y `#enroll-modal`: molde de modal con overlay, y `z-index` relativo — `#alert-drawer` va a 50, por debajo de `#clip-modal`)
    - .planning/phases/30-event-timeline-y-centro-de-alertas/30-UI-SPEC.md secciones "Ubicación en el layout", "Fila de la línea temporal", "Filtros combinables", "Scroll infinito", "Centro de alertas", "Marcar como persona" y "Copywriting Contract" (los 21 textos en español son literales, no parafrasear)
    - tests/test_frontend_modules.py `TEST_no_inline_logic` (prohibido cualquier `<script>` sin `src` y cualquier `<style>`)
  </read_first>
  <action>
    **1) Cabecera** — insertar el botón campana justo ANTES de `<time id="clock" …>` (línea 42), envuelto en un contenedor `relative` para que el badge se posicione:

    ```html
      <div class="relative">
        <button id="btn-alert-center" class="ptz-btn" aria-label="Centro de alertas, 0 activas" title="Centro de alertas">
          <svg class="w-4 h-4" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" aria-hidden="true">
            <path d="M18 8A6 6 0 0 0 6 8c0 7-3 9-3 9h18s-3-2-3-9"/><path d="M13.73 21a2 2 0 0 1-3.46 0"/>
          </svg>
        </button>
        <span id="alert-badge" class="hidden bg-red-500"></span>
      </div>
    ```

    **2) Panel "Alertas activas" (Fase 29)** — añadir en su cabecera, a la derecha del título, sin tocar nada más:
    ```html
    <button id="btn-alerts-view-all" class="text-xs font-semibold text-blue-400 hover:text-blue-300 cursor-pointer">Ver todas</button>
    ```

    **3) Card de la línea temporal** — sustituir el contenido del card de las líneas 445-525 conservando el contenedor `.card`, la cabecera (con `#btn-export-csv` y `#btn-delete-events` intactos) y `#delete-events-panel` completo. Cambios:
    - Título: `Línea temporal` en lugar de `Eventos recientes`.
    - `#events-badge` → `<span id="timeline-count" …>0 eventos</span>` (mismas clases del badge actual).
    - Barra de filtros: BORRAR `#filter-direction`, `#filter-person`, `#filter-intrusion`, `#filter-from`, `#filter-to`, `#btn-apply-filters`, `#btn-clear-filters` y poner en su lugar los controles del contrato de ids: `#tl-filter-types` (contenedor vacío de chips, lo puebla 30-08), `#tl-filter-severity` con TRES botones `.filter-chip` con `data-severity="critical|warning|info"` y textos "Crítico", "Aviso", "Info"; `#tl-filter-zone` (`<select class="filter-input filter-select">` con `<option value="">Todas las zonas</option>`); `#tl-filter-person` (`<input class="filter-input w-24" list="tl-person-options" placeholder="Persona…" maxlength="50">` + `<datalist id="tl-person-options">`); `#tl-filter-from` y `#tl-filter-to` (`<input type="date" class="filter-input">`); `#btn-tl-apply` ("Filtrar", estilo azul del botón actual) y `#btn-tl-clear` ("Limpiar filtros", estilo slate del actual).
    - Debajo de la barra: `<div id="tl-active-filters" class="flex flex-wrap gap-1 mb-2"></div>`.
    - Contenedor de lista, con `position: relative` para anclar la pill y la barra de desconexión:
      ```html
      <div class="relative" style="min-height:0">
        <div id="timeline-offline" class="hidden text-xs font-semibold text-amber-400 bg-amber-500/10 border border-amber-500/30 rounded-lg px-2 py-1 mb-2">Sin tiempo real — los eventos nuevos pueden tardar en aparecer.</div>
        <button id="timeline-newpill" class="new-pill hidden" type="button"></button>
        <div id="timeline-list" class="overflow-y-auto flex flex-col gap-2 lg:max-h-[320px] max-h-[240px]"
             aria-label="Línea temporal de eventos" aria-live="polite">
          <div id="timeline-empty" class="flex flex-col items-center justify-center py-8 text-center">
            <p class="text-sm font-semibold text-slate-400">Sin actividad registrada</p>
            <p class="text-xs text-slate-600 mt-1">Cuando la cámara detecte algo, aparecerá aquí en cuanto ocurra.</p>
          </div>
          <div id="timeline-empty-filtered" class="hidden flex-col items-center justify-center py-8 text-center">
            <p class="text-sm font-semibold text-slate-400">Ningún evento coincide</p>
            <p class="text-xs text-slate-600 mt-1">Amplía el rango de fechas o quita algún filtro.</p>
            <button id="btn-tl-clear-empty" class="filter-chip mt-2">Limpiar filtros</button>
          </div>
          <div id="timeline-error" class="hidden flex-col items-center justify-center py-8 text-center">
            <p class="text-sm font-semibold text-slate-400">No se pudieron cargar los eventos.</p>
            <button id="btn-tl-retry" class="filter-chip mt-2">Reintentar carga</button>
          </div>
          <div id="timeline-loading" class="hidden flex-col gap-2">
            <div class="tl-skeleton"></div><div class="tl-skeleton"></div><div class="tl-skeleton"></div>
          </div>
          <p id="timeline-end" class="hidden text-xs text-slate-600 mono text-center py-2">No hay más eventos en este rango.</p>
          <div id="timeline-sentinel" style="height:1px"></div>
        </div>
      </div>
      ```
    - BORRAR por completo `#events-list` y `#events-empty`.

    **4) Cajón de alertas** — añadir como último hijo del `<body>`, junto a los modales existentes:
    ```html
    <aside id="alert-drawer" role="dialog" aria-modal="true" aria-labelledby="alert-drawer-title">
      <div class="drawer-panel">
        <div class="flex items-start justify-between">
          <div>
            <p id="alert-count-hero" class="text-3xl font-bold leading-none text-slate-100">0</p>
            <p class="text-xs font-semibold text-slate-500 mt-1">alertas activas</p>
            <h2 id="alert-drawer-title" class="text-sm font-semibold text-slate-200 mt-4">Centro de alertas</h2>
          </div>
          <button id="btn-alert-close" class="ptz-btn" aria-label="Cerrar centro de alertas">
            <svg class="w-4 h-4" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" aria-hidden="true"><path d="M18 6 6 18M6 6l12 12"/></svg>
          </button>
        </div>
        <div id="alert-groups" class="flex flex-col gap-2 overflow-y-auto flex-1"></div>
        <div id="alert-empty" class="flex flex-col items-center justify-center py-8 text-center">
          <p class="text-sm font-semibold text-slate-400">Todo en orden</p>
          <p class="text-xs text-slate-600 mt-1">Sin alertas activas. Última comprobación: <span id="alert-checked-at" class="mono">--:--:--</span>.</p>
        </div>
        <p id="alert-footer-count" class="text-xs text-slate-600 mono"></p>
      </div>
      <div id="alert-mute-popover" class="bg-slate-800 border border-slate-700 rounded-xl p-3 flex flex-col gap-2">
        <p id="alert-mute-title" class="text-xs font-semibold text-slate-200"></p>
        <div class="flex gap-1">
          <button class="filter-chip" data-duration="900">15 minutos</button>
          <button class="filter-chip" data-duration="3600">1 hora</button>
          <button class="filter-chip" data-duration="28800">8 horas</button>
        </div>
        <p class="text-xs text-slate-500">No verás estas alertas durante ese tiempo. Puedes reactivarla cuando quieras.</p>
      </div>
    </aside>
    ```

    **5) Modal "Marcar como persona"** — añadir junto a `#enroll-modal`, con el MISMO patrón visual (overlay + panel centrado + `hidden`):
    ```html
    <div id="mark-person-modal" class="hidden fixed inset-0 z-[60] items-center justify-center" style="background:rgba(2,6,23,0.85);backdrop-filter:blur(6px)">
      <div class="bg-slate-900 border border-slate-800 rounded-2xl p-4 flex flex-col gap-4 w-80">
        <h2 class="text-sm font-semibold text-slate-200">Marcar como persona</h2>
        <img id="mark-person-preview" width="96" height="96" alt="Recorte del evento" class="rounded-lg self-center object-cover" style="width:96px;height:96px">
        <input id="mark-person-name" class="filter-input" list="mark-person-options" maxlength="100" placeholder="Nombre de la persona">
        <datalist id="mark-person-options"></datalist>
        <p id="mark-person-scope" class="text-xs text-slate-500"></p>
        <p id="mark-person-error" class="hidden text-xs text-red-400"></p>
        <div class="flex gap-2 justify-end">
          <button id="btn-mark-person-cancel" class="filter-chip">Cancelar</button>
          <button id="btn-mark-person-confirm" class="text-xs font-semibold px-3 py-1.5 rounded-lg bg-blue-600 hover:bg-blue-500 text-white cursor-pointer">Marcar como persona</button>
        </div>
      </div>
    </div>
    ```
    Nota de desviación a documentar en el SUMMARY: el UI-SPEC dice "reutiliza `#enroll-modal`". Se crea un modal propio con el mismo patrón visual y las tres diferencias que el contrato exige (recorte precargado a 96×96, autocompletado de personas conocidas, aviso de alcance retroactivo) porque el `submit` de `#enroll-modal` ya está enlazado por `personGallery.js` con otra semántica (subir fichero o usar el frame actual); reutilizar el mismo nodo obligaría a meterle un modo condicional a ese handler. Cero pérdida de funcionalidad respecto al contrato.
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/test_frontend_modules.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "id=\"timeline-list\"\|id=\"timeline-count\"\|id=\"timeline-empty\"\|id=\"timeline-empty-filtered\"\|id=\"timeline-error\"\|id=\"timeline-loading\"\|id=\"timeline-end\"\|id=\"timeline-sentinel\"\|id=\"timeline-newpill\"\|id=\"timeline-offline\"" frontend/index.html` → 10
    - `grep -c "id=\"tl-filter-types\"\|id=\"tl-filter-severity\"\|id=\"tl-filter-zone\"\|id=\"tl-filter-person\"\|id=\"tl-filter-from\"\|id=\"tl-filter-to\"\|id=\"btn-tl-apply\"\|id=\"btn-tl-clear\"\|id=\"tl-active-filters\"" frontend/index.html` → 9
    - `grep -c "id=\"btn-alert-center\"\|id=\"alert-badge\"\|id=\"alert-drawer\"\|id=\"alert-groups\"\|id=\"alert-empty\"\|id=\"alert-count-hero\"\|id=\"alert-mute-popover\"\|id=\"btn-alert-close\"\|id=\"btn-alerts-view-all\"\|id=\"alert-footer-count\"" frontend/index.html` → 10
    - `grep -c "id=\"mark-person-modal\"\|id=\"mark-person-preview\"\|id=\"mark-person-name\"\|id=\"mark-person-scope\"\|id=\"btn-mark-person-confirm\"\|id=\"btn-mark-person-cancel\"" frontend/index.html` → 6
    - `grep -c "id=\"events-list\"\|id=\"events-empty\"\|id=\"events-badge\"\|id=\"filter-direction\"\|id=\"filter-person\"\|id=\"filter-intrusion\"\|id=\"filter-from\"\|id=\"filter-to\"\|id=\"btn-apply-filters\"\|id=\"btn-clear-filters\"" frontend/index.html` → 0
    - `grep -c "id=\"btn-export-csv\"\|id=\"btn-delete-events\"\|id=\"delete-events-panel\"\|id=\"delete-from\"\|id=\"delete-to\"\|id=\"delete-events-confirm\"" frontend/index.html` → 6 (nada del borrado por rango se perdió)
    - `grep -c "data-duration=" frontend/index.html` → 3
    - `grep -n "Sin actividad registrada\|Ningún evento coincide\|No se pudieron cargar los eventos.\|No hay más eventos en este rango.\|Todo en orden\|Marcar como persona" frontend/index.html` → los 6 literales del contrato de copy presentes
    - `.venv/Scripts/python.exe -m pytest tests/test_frontend_modules.py -q` en verde (sin `<script>` inline ni `<style>`)
  </acceptance_criteria>
  <done>El marcado de la timeline, la campana con badge, el cajón y el modal existen con los ids del contrato y los textos literales del UI-SPEC; el marcado viejo de eventos ha desaparecido sin llevarse por delante el export CSV ni el borrado por rango.</done>
</task>

<task type="auto">
  <name>Task 3: Retirar el JS que apuntaba al card de eventos eliminado</name>
  <files>frontend/js/views/dashboard-events.js, frontend/js/views/dashboard.js, frontend/js/websocket.js, frontend/js/app.js</files>
  <read_first>
    - frontend/js/views/dashboard-events.js completo (204 líneas: la gráfica y `#delete-events-panel` se CONSERVAN; `addEvent` líneas 110-144, `_eventsFilterParams` líneas 147-161, `applyFilters` líneas 163-185 y `bindEventFilters` líneas 187-204 se retiran, salvo el binding de `#btn-export-csv` de las líneas 199-203)
    - frontend/js/views/dashboard.js líneas 100-160 (`fetchCounts` escribe en `#events-badge` en la línea 109; `loadInitialData` líneas 128-158 puebla `#events-list` en las líneas 139-156 — ambas partes se retiran, la carga de `/api/stats` y del chart se conserva)
    - frontend/js/websocket.js líneas 47-74 (el `case 'detection'`: `addEvent` en la línea 54 se retira; `updateStat`, `bumpHourBar` y `showToast` se conservan — la gráfica horaria y los contadores de la Fase 5 siguen alimentándose de ahí)
    - frontend/js/app.js líneas 1-25 (imports y `bindEventFilters()` en la línea 21)
    - .planning/phases/30-event-timeline-y-centro-de-alertas/30-RESEARCH.md "Hallazgo 2 → Doble emisión que hay que gestionar" y "State of the Art → Obsoleto / a retirar en esta fase"
  </read_first>
  <action>
    **1) `frontend/js/views/dashboard-events.js`:** borrar `addEvent`, `_eventsFilterParams`, `applyFilters` y `bindEventFilters`. Añadir en su lugar:

    ```javascript
    // El export CSV sigue apuntando al endpoint v1 (/api/events/export, tabla crossing_events):
    // no esta en OPS-07..11 y se deja intacto, ya sin filtros porque la barra vieja
    // desaparecio con el card de eventos (30-RESEARCH.md Open Question 2 -> anotado para OPS-15).
    export function bindEventExport() {
      const btn = document.getElementById('btn-export-csv');
      if (btn) btn.addEventListener('click', () => { window.location.href = '/api/events/export'; });
    }
    ```
    Si el fichero deja de usar el import de `loadInitialData`, retirarlo también.

    **2) `frontend/js/views/dashboard.js`:** en `fetchCounts`, borrar la línea `document.getElementById('events-badge').textContent = d.total ?? 0;`. En `loadInitialData`, borrar el bloque `if (eventsRes.ok) { … }` completo y la petición `fetch('/api/events?limit=50')` del `Promise.all` (queda solo `fetch('/api/stats')`), y retirar el import de `addEvent`.

    **3) `frontend/js/websocket.js`:** en el bloque `else if (msg.type === 'detection')`, borrar la llamada `addEvent(...)` y quitar `addEvent` del import de `./views/dashboard-events.js`. Conservar `updateStat`, `bumpHourBar` y `showToast` tal cual — si se borran, se pierden la gráfica horaria y el contador de cruces de la Fase 5.

    **4) `frontend/js/app.js`:** sustituir el import y la llamada de `bindEventFilters` por `bindEventExport`.
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/test_frontend_modules.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "addEvent\|applyFilters\|bindEventFilters\|_eventsFilterParams" frontend/js/` → 0 matches (excluyendo `addEventListener`, que es otra cosa: comprobar con `grep -rn "addEvent(" frontend/js/` → 0)
    - `grep -rn "events-list\|events-empty\|events-badge\|filter-direction\|filter-intrusion\|btn-apply-filters\|btn-clear-filters" frontend/js/` → 0 matches
    - `grep -n "bindEventExport" frontend/js/views/dashboard-events.js frontend/js/app.js` → 2 matches
    - `grep -n "btn-export-csv" frontend/js/views/dashboard-events.js` → 1 match (el CSV sigue funcionando)
    - `grep -n "updateStat\|bumpHourBar\|showToast" frontend/js/websocket.js` → siguen presentes en el bloque `'detection'`
    - `grep -n "delete-events-panel\|delete-events-confirm" frontend/js/views/dashboard-events.js` → siguen presentes (el borrado por rango no se tocó)
    - `wc -l frontend/js/views/dashboard-events.js` → menor que 130 (204 − ~80 retiradas)
    - `.venv/Scripts/python.exe -m pytest tests/test_frontend_modules.py -q` en verde
  </acceptance_criteria>
  <done>Ningún módulo JS referencia ya elementos del DOM inexistentes; la gráfica horaria, los contadores, el export CSV y el borrado por rango siguen funcionando.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Marcado estático → DOM | Este plan solo añade estructura y estilos; ningún dato del backend cruza aquí (eso ocurre en 30-08/30-09/30-11) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-25 | Tampering (XSS) | Marcado de la timeline y del cajón | mitigate | Todos los contenedores que recibirán datos del backend se crean VACÍOS; el contrato para 30-08/30-09 es estructura estática por `innerHTML` y contenido por `textContent` (CodeQL `js/xss`) |
| T-30-26 | Denial of Service (funcional) | Bindings huérfanos tras borrar el marcado | mitigate | La retirada de `bindEventFilters`/`loadInitialData`/`addEvent` va en este mismo plan; criterios de aceptación con `grep` a cero sobre los ids eliminados |
</threat_model>

<verification>
- `.venv/Scripts/python.exe -m pytest tests/test_frontend_modules.py -q` — en verde (existencia de módulos, tope de 300 líneas, sin lógica inline)
- Arrancar el servidor y abrir `/`: la consola del navegador no debe mostrar `TypeError: Cannot read properties of null` (comprobación manual anotada en el SUMMARY; el card de eventos aparecerá vacío hasta 30-08, es el estado esperado)
</verification>

<success_criteria>
- `components.css` contiene los 13 bloques del contrato visual con las medidas exactas del UI-SPEC y sigue bajo 300 líneas
- `index.html` tiene los 35 ids del contrato, los literales de copy en español y ninguno de los ids del card viejo
- `#btn-export-csv`, `#btn-delete-events` y `#delete-events-panel` siguen intactos y funcionando
- Ningún módulo JS referencia elementos eliminados
- `tests/test_frontend_modules.py` en verde
</success_criteria>

<output>
After completion, create `.planning/phases/30-event-timeline-y-centro-de-alertas/30-07-SUMMARY.md`
</output>
