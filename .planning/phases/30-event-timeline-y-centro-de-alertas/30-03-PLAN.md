---
phase: 30
plan: 03
type: execute
wave: 2
depends_on: ["30-02"]
files_modified:
  - backend/storage/repositories.py
  - tests/test_repositories.py
autonomous: true
requirements: [OPS-08]

must_haves:
  truths:
    - "Se puede saber, antes de confirmar nada, a cuantos eventos anteriores del mismo track se aplicaria una identidad"
    - "Asignar una identidad a un evento la propaga solo al bloque contiguo de ese track, nunca a un track homonimo de otro dia"
    - "Un evento UNKNOWN_PERSON al que se le asigna identidad deja de ser una advertencia y pasa a ser informativo"
    - "Se puede resolver en una sola consulta que grabacion corresponde a cada evento de una pagina"
  artifacts:
    - path: "backend/storage/repositories.py"
      provides: "EventRepo.track_scope() y EventRepo.assign_person(); RecordingRepo.by_trigger_event_ids()"
      contains: "async def track_scope"
    - path: "tests/test_repositories.py"
      provides: "tests de contiguidad, de track homonimo separado en el tiempo y del mapa de grabaciones"
  key_links:
    - from: "backend/storage/repositories.py::EventRepo.assign_person"
      to: "backend/storage/repositories.py::EventRepo.track_scope"
      via: "reutiliza el mismo calculo de bloque contiguo; el UPDATE va por lista explicita de ids"
      pattern: "WHERE id IN|Event.id.in_"
    - from: "backend/storage/repositories.py::RecordingRepo.by_trigger_event_ids"
      to: "tabla recordings columna trigger_event_id"
      via: "IN (:ids) con bindparam expanding"
      pattern: "trigger_event_id"
---

<objective>
Añadir al repositorio las tres operaciones que necesita "Marcar como persona" y la miniatura de la fila: `EventRepo.track_scope()` (previsualización del alcance retroactivo), `EventRepo.assign_person()` (actualización retroactiva acotada) y `RecordingRepo.by_trigger_event_ids()` (mapa evento → grabación de una página completa en una sola consulta).

Purpose: OPS-08 y el criterio de éxito 5 ("actualiza retroactivamente los eventos del track"). El riesgo real está medido: los `tracker_id` de ByteTrack **se reinician** al recrear el tracker (`backend/tracker.py:181`) y la tabla `tracks` nunca se escribe, así que `track_id=7` de hoy y `track_id=7` de anteayer son personas distintas — un `UPDATE ... WHERE track_id = 7` asignaría la identidad a otra persona (Pitfall 3). Y `events.recording_id` nunca se escribe: el vínculo real con el clip es `recordings.trigger_event_id → events.id`.
Output: tres métodos de repositorio con tests deterministas, incluida la regresión explícita del track homónimo.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-event-timeline-y-centro-de-alertas/30-CONTEXT.md
@.planning/phases/30-event-timeline-y-centro-de-alertas/30-RESEARCH.md
@.planning/phases/30-event-timeline-y-centro-de-alertas/30-PATTERNS.md
@CLAUDE.md
</context>

<interfaces>
```python
# backend/storage/repositories.py
TRACK_GAP_SECS = 60.0        # constante de modulo, documentada
TRACK_WINDOW_HOURS = 6       # constante de modulo, documentada

class EventRepo:
    async def track_scope(self, event_id: str) -> dict | None:
        """None si el evento no existe o no tiene track_id.
        {"event_ids": [...], "count": N, "from": datetime, "to": datetime}"""

    async def assign_person(self, event_id: str, person_id: int) -> dict:
        """{"person_id": N, "updated": N, "event_ids": [...]}  (updated=0 si no hay alcance)"""

class RecordingRepo:
    async def by_trigger_event_ids(self, ids: list[str]) -> dict[str, dict]:
        """{event_id: {"recording_id": int, "local_path": str|None, "thumbnail_path": str|None}}"""
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: track_scope() — bloque contiguo del track, acotado por cámara y ventana</name>
  <files>backend/storage/repositories.py, tests/test_repositories.py</files>
  <read_first>
    - backend/storage/repositories.py líneas 42-145 (`EventRepo`: `_to_dto`, `get()`, `query()` ya extendido por 30-02, y el helper `_filter_conditions` nuevo)
    - backend/storage/models.py líneas 85-110 (columnas de `events`: `track_id` NO tiene índice; los cuatro índices son por `ts`, `type+ts`, `camera_id+ts`, `person_id+ts`)
    - backend/tracker.py líneas 170-195 (reinstanciación de `sv.ByteTrack` — la razón de que `track_id` no sea único en el tiempo)
    - tests/test_repositories.py líneas 1-100 (fixture `db` y estilo de construcción de eventos)
    - .planning/phases/30-event-timeline-y-centro-de-alertas/30-RESEARCH.md "Hallazgo 6 — actualización retroactiva por track_id" (algoritmo de 5 pasos) y "Pitfall 3"
  </read_first>
  <behavior>
    - `TEST_track_scope_returns_contiguous_block`: cinco eventos del mismo `camera_id`+`track_id` separados 10 s entre sí; `track_scope(id_del_tercero)` devuelve `count == 5` y los cinco ids.
    - `TEST_track_scope_cuts_on_gap`: tres eventos seguidos, luego un hueco de 5 minutos (> `TRACK_GAP_SECS`), luego dos más; `track_scope()` sobre uno del primer bloque devuelve `count == 3` y no incluye los dos posteriores.
    - `TEST_track_scope_ignores_homonym_track_from_another_day`: dos eventos con el mismo `track_id=7` separados 48 h (fuera de la ventana de ±6 h); `track_scope()` sobre el reciente devuelve `count == 1`. Regresión explícita del Pitfall 3.
    - `TEST_track_scope_respects_camera_id`: dos eventos con el mismo `track_id` y el mismo instante pero `camera_id` distinto; solo se incluye el de la cámara del ancla.
    - `TEST_track_scope_returns_none_without_track_id`: evento con `track_id=None` → devuelve `None`.
  </behavior>
  <action>
    En `backend/storage/repositories.py`, añadir dos constantes de módulo justo debajo de `_decode_cursor` (línea 39):

    ```python
    # Ventana y contiguidad para la asignacion retroactiva por track (Fase 30, OPS-08).
    # Los tracker_id de ByteTrack se reinician al recrear el tracker (backend/tracker.py:181)
    # y la tabla `tracks` nunca se escribe: sin estas dos cotas, un UPDATE por track_id
    # asignaria la identidad a otra persona de otro dia (30-RESEARCH.md Pitfall 3).
    # 60s es holgado: ByteTrack pierde el track a los 60 frames (~4s @15fps).
    TRACK_GAP_SECS = 60.0
    TRACK_WINDOW_HOURS = 6
    ```

    Añadir a `EventRepo`, después de `count()`:

    ```python
        async def track_scope(self, event_id: str) -> dict | None:
            """Bloque CONTIGUO de eventos del mismo track alrededor de *event_id*.

            Nunca 'WHERE track_id = ?' a secas: se acota por camera_id + ventana de
            +-TRACK_WINDOW_HOURS y se corta en el primer hueco > TRACK_GAP_SECS.
            """
            anchor = await self.get(event_id)
            if anchor is None or anchor.track_id is None:
                return None
            window = datetime.timedelta(hours=TRACK_WINDOW_HOURS)
            q = (
                select(models.Event.id, models.Event.ts)
                .where(and_(
                    models.Event.camera_id == anchor.camera_id,
                    models.Event.track_id == anchor.track_id,
                    models.Event.ts >= anchor.ts - window,
                    models.Event.ts <= anchor.ts + window,
                ))
                .order_by(models.Event.ts.asc(), models.Event.id.asc())
            )
            async with self._sf() as session:
                rows = list((await session.execute(q)).all())
            if not rows:
                return None
            idx = next((i for i, r in enumerate(rows) if r.id == anchor.id), None)
            if idx is None:
                return None
            start = idx
            while start > 0 and (rows[start].ts - rows[start - 1].ts).total_seconds() <= TRACK_GAP_SECS:
                start -= 1
            end = idx
            while end + 1 < len(rows) and (rows[end + 1].ts - rows[end].ts).total_seconds() <= TRACK_GAP_SECS:
                end += 1
            block = rows[start:end + 1]
            return {
                "event_ids": [r.id for r in block],
                "count": len(block),
                "from": block[0].ts,
                "to": block[-1].ts,
            }
    ```

    Añadir los 5 tests de `<behavior>` a `tests/test_repositories.py` usando la fixture `db` existente.
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/test_repositories.py -k track_scope -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "TRACK_GAP_SECS\|TRACK_WINDOW_HOURS" backend/storage/repositories.py` → al menos 4 matches (2 definiciones + 2 usos)
    - `grep -n "async def track_scope" backend/storage/repositories.py` → 1 match
    - El `WHERE` de `track_scope` incluye simultáneamente `camera_id ==`, `track_id ==` y las dos cotas de `ts`: `sed -n '/async def track_scope/,/return {/p' backend/storage/repositories.py | grep -c "camera_id ==\|track_id ==\|Event.ts >=\|Event.ts <="` → 4
    - `.venv/Scripts/python.exe -m pytest tests/test_repositories.py -k track_scope -q` en verde (5 tests)
  </acceptance_criteria>
  <done>`track_scope()` devuelve solo el bloque contiguo del track dentro de la ventana y de la misma cámara; el track homónimo de otro día queda fuera, con test de regresión.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: assign_person() — UPDATE retroactivo por lista explícita de ids</name>
  <files>backend/storage/repositories.py, tests/test_repositories.py</files>
  <read_first>
    - backend/storage/repositories.py (`track_scope()` recién creado en Task 1; `insert()`/`get()` líneas 82-90 para el patrón `async with self._sf() as session: async with session.begin():`)
    - backend/events/types.py (valores de `EventType.UNKNOWN_PERSON` y de `Severity.INFO`/`Severity.WARNING`, para escribir los literales correctos)
    - tests/test_repositories.py (tests de `track_scope` de Task 1: reutilizar sus helpers de construcción de eventos)
  </read_first>
  <behavior>
    - `TEST_assign_person_updates_only_contiguous_block`: cinco eventos contiguos + dos de un bloque posterior separado por un hueco largo; tras `assign_person(id_del_tercero, person_id=7)`, los cinco del bloque tienen `person_id == 7` y los dos de fuera siguen con `person_id is None`; el retorno trae `updated == 5`.
    - `TEST_assign_person_downgrades_unknown_person_severity`: un evento `UNKNOWN_PERSON` con `severity="warning"` y otro `LINE_CROSSED` con `severity="info"` en el mismo bloque; tras asignar, el `UNKNOWN_PERSON` queda en `severity == "info"` y el `LINE_CROSSED` conserva su severidad original.
    - `TEST_assign_person_without_track_returns_zero`: evento sin `track_id` → `{"updated": 0, "event_ids": []}` y ninguna fila modificada.
    - `TEST_assign_person_does_not_touch_homonym_track`: mismo montaje que el test de homónimo de Task 1; el evento de hace 48 h conserva `person_id is None`.
  </behavior>
  <action>
    Añadir a `EventRepo`, inmediatamente después de `track_scope()`:

    ```python
        async def assign_person(self, event_id: str, person_id: int) -> dict:
            """Propaga una identidad al bloque contiguo del track (Fase 30, OPS-08, criterio 5).

            El UPDATE va por lista EXPLICITA de ids calculada por track_scope(), nunca
            'WHERE track_id = ?': eso alcanzaria tracks homonimos de otros dias (Pitfall 3).
            Un UNKNOWN_PERSON deja de ser advertencia al ganar identidad; el resto de
            tipos conserva su severidad.
            """
            scope = await self.track_scope(event_id)
            if scope is None or not scope["event_ids"]:
                return {"person_id": person_id, "updated": 0, "event_ids": []}
            ids = scope["event_ids"]
            async with self._sf() as session:
                async with session.begin():
                    await session.execute(
                        update(models.Event)
                        .where(models.Event.id.in_(ids))
                        .values(
                            person_id=person_id,
                            severity=case(
                                (models.Event.type == EventType.UNKNOWN_PERSON.value,
                                 Severity.INFO.value),
                                else_=models.Event.severity,
                            ),
                        )
                    )
            return {"person_id": person_id, "updated": len(ids), "event_ids": ids}
    ```

    Añadir `case` y `update` al import de SQLAlchemy de la línea 15: `from sqlalchemy import and_, bindparam, case, func, select, text, tuple_, update`.

    Añadir los 4 tests de `<behavior>` a `tests/test_repositories.py`.
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/test_repositories.py -k assign_person -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "async def assign_person" backend/storage/repositories.py` → 1 match
    - `grep -n "models.Event.id.in_(ids)" backend/storage/repositories.py` → 1 match
    - `grep -n "update(models.Event).*track_id\|where(models.Event.track_id" backend/storage/repositories.py` → 0 matches (nunca se actualiza filtrando por `track_id`)
    - `grep -n "UNKNOWN_PERSON" backend/storage/repositories.py` → 1 match, dentro de `assign_person`
    - `.venv/Scripts/python.exe -m pytest tests/test_repositories.py -k assign_person -q` en verde (4 tests)
  </acceptance_criteria>
  <done>`assign_person()` actualiza exactamente el bloque contiguo, baja la severidad solo de los `UNKNOWN_PERSON` y devuelve el recuento real.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: RecordingRepo.by_trigger_event_ids() — mapa evento → clip de una página</name>
  <files>backend/storage/repositories.py, tests/test_repositories.py</files>
  <read_first>
    - backend/storage/repositories.py `class RecordingRepo` completa (métodos `insert`/`get`/`expired_local`/`clear_local_path` — patrón de sesión y de construcción de dicts de salida)
    - backend/storage/models.py (`class Recording`: columnas `id`, `trigger_event_id`, `local_path`, `thumbnail_path`, `upload_state`)
    - backend/main.py líneas 350-360 (`_on_clip_ready`: quién escribe `trigger_event_id` — confirma que ese es el vínculo real y que `events.recording_id` nunca se rellena)
    - backend/api/v2/recordings.py líneas 72-85 (endpoint de thumbnail ya existente, con `Cache-Control: max-age=86400`)
  </read_first>
  <behavior>
    - `TEST_by_trigger_event_ids_maps_only_matching`: tres grabaciones, dos con `trigger_event_id` entre los ids pedidos; el resultado tiene exactamente 2 claves y cada valor trae `recording_id`, `local_path` y `thumbnail_path`.
    - `TEST_by_trigger_event_ids_empty_input`: `by_trigger_event_ids([])` devuelve `{}` sin ejecutar consulta.
    - `TEST_by_trigger_event_ids_keeps_latest_per_event`: dos grabaciones apuntando al mismo `trigger_event_id`; el mapa devuelve una sola entrada, la de `id` mayor (la más reciente).
  </behavior>
  <action>
    Añadir a `RecordingRepo`:

    ```python
        async def by_trigger_event_ids(self, ids: list[str]) -> dict[str, dict]:
            """Mapa event_id -> datos de su grabacion, para una pagina completa (<=200 ids).

            events.recording_id NUNCA se escribe: el vinculo real lo pone _on_clip_ready
            en recordings.trigger_event_id (backend/main.py:353-357). La tabla recordings
            es pequena y no tiene indice por esa columna; el escaneo es despreciable
            frente a anadir un quinto indice (30-RESEARCH.md Hallazgo 4).
            """
            if not ids:
                return {}
            q = (
                select(models.Recording)
                .where(models.Recording.trigger_event_id.in_(ids))
                .order_by(models.Recording.id.asc())
            )
            async with self._sf() as session:
                rows = list((await session.execute(q)).scalars().all())
            out: dict[str, dict] = {}
            for row in rows:            # orden ascendente: la ultima gana
                out[row.trigger_event_id] = {
                    "recording_id": row.id,
                    "local_path": row.local_path,
                    "thumbnail_path": row.thumbnail_path,
                }
            return out
    ```

    Añadir los 3 tests de `<behavior>` a `tests/test_repositories.py`.
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/test_repositories.py -k trigger_event -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "async def by_trigger_event_ids" backend/storage/repositories.py` → 1 match
    - `grep -n "if not ids:" backend/storage/repositories.py` → al menos 1 match dentro de ese método (corto-circuito sin consulta)
    - `grep -n "trigger_event_id.in_(ids)" backend/storage/repositories.py` → 1 match
    - `.venv/Scripts/python.exe -m pytest tests/test_repositories.py -k trigger_event -q` en verde (3 tests)
  </acceptance_criteria>
  <done>Una sola consulta resuelve el clip de todos los eventos de una página, con la grabación más reciente ganando en caso de duplicado.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `POST /api/v2/events/{id}/assign-person` (30-05) → `assign_person()` | Una petición del navegador provoca una escritura masiva acotada sobre `events` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-08 | Tampering (asignación errónea de identidad) | `assign_person()` sobre un `track_id` reciclado | mitigate | Triple cota — `camera_id` + ventana de ±6 h + corte por hueco de 60 s — y `UPDATE ... WHERE id IN (lista explícita)`. Test de regresión con dos tracks homónimos separados 48 h |
| T-30-09 | Denial of Service | `UPDATE` sin cota sobre una tabla de 10⁵ filas | mitigate | El conjunto actualizado es como mucho el bloque contiguo del track; el `SELECT` previo va acotado por `camera_id`+ventana (medido 23 ms @100k, ~2 ms @10k) |
| T-30-10 | Tampering (SQL injection) | Lista de ids en `IN (...)` | mitigate | `Event.id.in_(ids)` / `Recording.trigger_event_id.in_(ids)` de SQLAlchemy: parametrizado, nunca interpolado |
</threat_model>

<verification>
- `.venv/Scripts/python.exe -m pytest tests/test_repositories.py -q` — fichero completo en verde (tests de 30-02 + los 12 nuevos de este plan)
- `.venv/Scripts/python.exe -m pytest tests/ -q` — suite completa sin regresiones
</verification>

<success_criteria>
- `track_scope()` previsualiza el alcance real sin escribir nada
- `assign_person()` propaga la identidad solo al bloque contiguo y baja la severidad únicamente de los `UNKNOWN_PERSON`
- `by_trigger_event_ids()` resuelve el clip de una página completa en una consulta
- Existe test de regresión del track homónimo separado en el tiempo
- Suite completa en verde
</success_criteria>

<output>
After completion, create `.planning/phases/30-event-timeline-y-centro-de-alertas/30-03-SUMMARY.md`
</output>
