---
phase: 31
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/pipeline/detection.py
  - tests/test_detection_worker.py
autonomous: true
requirements: [OPS-12]

must_haves:
  truths:
    - "El mapa de calor se pinta con una rampa perceptualmente uniforme (INFERNO), no con arcoiris (JET)"
    - "El worker puede decir cual es el pico y la media de la mascara acumulada sin exponer la mascara entera"
    - "Pedir la escala sin actividad acumulada devuelve None, no un cero enganoso"
    - "La lectura de la escala es thread-safe: se hace bajo el mismo lock que compose_heatmap"
  artifacts:
    - path: "backend/pipeline/detection.py"
      provides: "compose_heatmap con COLORMAP_INFERNO y heatmap_scale() nuevo"
      contains: "COLORMAP_INFERNO"
    - path: "tests/test_detection_worker.py"
      provides: "Tests de heatmap_scale (None sin mascara, None con pico 0, valores reales) y guarda del colormap"
      contains: "heatmap_scale"
  key_links:
    - from: "backend/pipeline/detection.py::heatmap_scale"
      to: "DetectionWorker._heat_mask"
      via: "with self._lock: copia de la mascara, igual que compose_heatmap"
      pattern: "with self\\._lock"
---

<objective>
Versionar el mapa de calor a v2 por el lado del pipeline: cambiar `cv2.COLORMAP_JET` por `cv2.COLORMAP_INFERNO` en `compose_heatmap` y anadir un metodo hermano `heatmap_scale()` que exponga el pico y la media de la mascara acumulada.

Purpose: D-13 fija el cambio de rampa (JET es arcoiris, no perceptualmente uniforme, inventa fronteras donde los datos son continuos y su extremo bajo azul oscuro desaparece sobre un frame nocturno; INFERNO es monotona en luminosidad y su extremo bajo se funde con el frame, que es lo que queremos). Y el UI-SPEC exige una **leyenda numerica obligatoria** en el panel 3 — "un heatmap sin escala numerica es decoracion" — con tres marcas tomadas del propio servidor; `compose_heatmap` solo devuelve pixeles, asi que ese dato hoy no sale a ningun sitio.
Output: pipeline listo para que 31-06 publique `GET /api/v2/analytics/heatmap` y `GET /api/v2/analytics/heatmap/scale`.

**El endpoint v1 `/api/heatmap` (`backend/main.py:1141-1152`) NO se toca en este plan ni en ningun otro de la fase.** Se queda tal cual (heredara INFERNO por usar el mismo `compose_heatmap`, que es correcto); no se convierte en alias ni se borra — borrar superficie publica no es "el cambio minimo" de CLAUDE.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-vista-de-anal-tica/31-CONTEXT.md
@.planning/phases/31-vista-de-anal-tica/31-RESEARCH.md
@.planning/phases/31-vista-de-anal-tica/31-PATTERNS.md
@.planning/phases/31-vista-de-anal-tica/31-UI-SPEC.md
@CLAUDE.md
</context>

<interfaces>
Contrato que consume 31-06 (`GET /api/v2/analytics/heatmap/scale`):

```python
class DetectionWorker:
    def heatmap_scale(self) -> dict[str, float] | None:
        """{"peak": float, "mean": float} o None si no hay actividad acumulada."""
```

Semantica de la unidad, que 31-06 tiene que respetar al escribir la leyenda:
la mascara acumula, por cada frame procesado, un disco de radio 40 px alrededor
del ancla `BOTTOM_CENTER` de cada track (`detection.py:486-493`). Su unidad es
**"frames de deteccion con presencia", no personas**, y `compose_heatmap` la
normaliza dividiendo por el maximo — el color es **siempre relativo**. Por eso la
leyenda del UI-SPEC es `0` / `50 %` / `pico` y el valor absoluto va en el `title`,
no una cifra de personas inventada (mismo principio de honestidad de D-12).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: COLORMAP_INFERNO y heatmap_scale() en DetectionWorker</name>
  <files>backend/pipeline/detection.py</files>
  <read_first>
    - backend/pipeline/detection.py lineas 155-190 — `get_object_boxes()` (molde del `with self._lock:` para lectura thread-safe) y `compose_heatmap()` completo, que es el metodo que se modifica
    - backend/pipeline/detection.py lineas ~480-495 — donde se acumula `self._heat_mask` (disco de radio 40 px sobre el ancla BOTTOM_CENTER), para entender la unidad
    - .planning/phases/31-vista-de-anal-tica/31-UI-SPEC.md seccion "Rampa secuencial del heatmap" y "Panel 3 — Mapa de calor"
    - .planning/phases/31-vista-de-anal-tica/31-RESEARCH.md seccion "Q7"
    - .planning/phases/31-vista-de-anal-tica/31-PATTERNS.md seccion "backend/pipeline/detection.py::compose_heatmap"
  </read_first>
  <action>
    **1) Cambio de una linea** en `compose_heatmap` (linea 180 hoy):

    ```python
    # ANTES
    colored = cv2.applyColorMap(temp, cv2.COLORMAP_JET)
    # DESPUES (D-13): INFERNO es monotona en luminosidad, se ordena bien en escala de
    # grises y su extremo bajo se funde con el frame; JET es arcoiris, no perceptualmente
    # uniforme, y su extremo azul oscuro desaparece sobre un frame nocturno.
    colored = cv2.applyColorMap(temp, cv2.COLORMAP_INFERNO)
    ```
    No tocar `addWeighted(frame, 0.5, colored, 0.5, 0)` ni `out[active] = blended[active]`: la mezcla actual se conserva.

    **2) Metodo nuevo `heatmap_scale()`**, inmediatamente despues de `compose_heatmap` y con el mismo patron de copia bajo lock:

    ```python
    def heatmap_scale(self) -> dict[str, float] | None:
        """Pico y media de la mascara acumulada, para la leyenda numerica del panel de analitica.

        La escala es SIEMPRE relativa (D-12/D-13): compose_heatmap normaliza dividiendo
        por el pico, asi que el extremo de la rampa es el 100 % de esta mascara, no una
        cifra de personas. La unidad de estos numeros es "frames de deteccion con
        presencia" (disco de 40 px por track y frame), no visitantes.

        Devuelve None cuando no hay mascara o su pico es 0 — mismo criterio que
        compose_heatmap, para que el endpoint pueda distinguir 404 (sin actividad)
        de 503 (sin frame).
        """
        with self._lock:
            mask = None if self._heat_mask is None else self._heat_mask.copy()
        if mask is None:
            return None
        peak = float(mask.max())
        if peak <= 0:
            return None
        return {"peak": peak, "mean": float(mask.mean())}
    ```

    **No** anadir polling, cache ni estado nuevo: es una lectura puntual, igual que `get_object_boxes()`.
  </action>
  <verify>
    <automated>cd "F:\Documentos\IA\Proyecto_Camara" && .venv/Scripts/python.exe -c "import cv2, backend.pipeline.detection as d; assert hasattr(cv2,'COLORMAP_INFERNO'); assert hasattr(d.DetectionWorker,'heatmap_scale'); print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "cv2.COLORMAP_JET" backend/pipeline/detection.py` devuelve `0`
    - `grep -c "cv2.COLORMAP_INFERNO" backend/pipeline/detection.py` devuelve `1`
    - `grep -n "def heatmap_scale" backend/pipeline/detection.py` encuentra el metodo
    - El cuerpo de `heatmap_scale` contiene `with self._lock:` (misma comprobacion que hace `TEST_architecture` para el resto de lecturas compartidas)
    - `grep -c "cv2.COLORMAP_JET" backend/main.py` devuelve `0` (el endpoint v1 nunca declaro el colormap; si aparece, se ha tocado algo que no tocaba)
  </acceptance_criteria>
  <done>`compose_heatmap` usa INFERNO y `DetectionWorker.heatmap_scale()` devuelve `{"peak","mean"}` o `None`, leyendo bajo `self._lock`.</done>
</task>

<task type="auto">
  <name>Task 2: Tests de heatmap_scale y guarda del colormap</name>
  <files>tests/test_detection_worker.py</files>
  <read_first>
    - tests/test_detection_worker.py completo — fixtures y forma de construir un `DetectionWorker` de prueba ya establecidas en la suite; hay tests de heatmap previos, reutilizar su montaje
    - backend/pipeline/detection.py — `compose_heatmap` y `heatmap_scale` ya modificados en la Task 1
  </read_first>
  <action>
    Cuatro tests nuevos en `tests/test_detection_worker.py`, con el mismo montaje de worker que ya usan los tests de heatmap existentes (no inventar una fixture nueva):

    - `TEST_heatmap_scale_returns_none_without_mask`: worker recien construido, `_heat_mask is None` -> `heatmap_scale() is None`.
    - `TEST_heatmap_scale_returns_none_when_peak_is_zero`: asignar `_heat_mask = np.zeros((10, 10), dtype=np.float32)` -> `heatmap_scale() is None`. Este es el caso que distingue "sin actividad" de "sin frame" y que 31-06 traduce a 404.
    - `TEST_heatmap_scale_returns_peak_and_mean`: mascara conocida (por ejemplo `np.zeros((2,2), np.float32)` con un unico valor `8.0`) -> `{"peak": 8.0, "mean": 2.0}`, comparando con `pytest.approx`.
    - `TEST_compose_heatmap_uses_inferno_colormap`: guarda del cambio de rampa. Parchear `cv2.applyColorMap` con `unittest.mock.patch` y comprobar que se le llama con `cv2.COLORMAP_INFERNO` como segundo argumento; alternativamente, comprobar por inspeccion del fuente
      (`inspect.getsource(DetectionWorker.compose_heatmap)` contiene `COLORMAP_INFERNO` y no contiene `COLORMAP_JET`). Elegir la de parcheo si el montaje del worker ya permite llamar a `compose_heatmap` con un frame sintetico; si no, la de inspeccion, documentando el motivo en el docstring del test.
  </action>
  <verify>
    <automated>cd "F:\Documentos\IA\Proyecto_Camara" && .venv/Scripts/python.exe -m pytest tests/test_detection_worker.py -k "heatmap" -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "heatmap_scale" tests/test_detection_worker.py` devuelve al menos `3`
    - `grep -n "COLORMAP_INFERNO" tests/test_detection_worker.py` encuentra la guarda del colormap
    - `.venv/Scripts/python.exe -m pytest tests/test_detection_worker.py -k heatmap -q` en verde, con los 4 tests nuevos incluidos
  </acceptance_criteria>
  <done>Los cuatro casos de `heatmap_scale` (sin mascara, pico 0, valores reales) y el colormap INFERNO estan cubiertos por tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| hilo de deteccion -> event loop | `_heat_mask` es estado mutable del `DetectionWorker`; cualquier lectura desde una corrutina cruza esta frontera |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-04 | Tampering | Lectura de `_heat_mask` sin lock desde otro hilo | mitigate | `heatmap_scale()` copia la mascara dentro de `with self._lock:` antes de calcular, exactamente como `compose_heatmap` y `get_object_boxes` |
| T-31-05 | Information disclosure | Exponer la mascara completa por HTTP | mitigate | `heatmap_scale()` devuelve solo dos escalares (`peak`, `mean`); la mascara nunca sale del proceso |
| T-31-06 | Denial of Service | `mask.copy()` en cada peticion de escala | accept | La mascara tiene el tamano del frame reducido y la copia es de microsegundos; ademas el endpoint de 31-06 lleva `@limiter.limit(V2_RATE_LIMIT)` (60/min/IP) |
</threat_model>

<verification>
```bash
cd "F:\Documentos\IA\Proyecto_Camara"
.venv/Scripts/python.exe -m pytest tests/test_detection_worker.py -q
.venv/Scripts/python.exe -m pytest tests/test_architecture.py tests/test_memory_bounds.py -q
```
Los dos ultimos porque tocan `detection.py` y protegen los invariantes de hilos y de cotas de memoria.
</verification>

<success_criteria>
- `compose_heatmap` usa `cv2.COLORMAP_INFERNO` y ninguna referencia a `COLORMAP_JET` sobrevive en `backend/`
- `DetectionWorker.heatmap_scale()` existe, lee bajo `self._lock` y devuelve `None` tanto sin mascara como con pico 0
- `tests/test_detection_worker.py`, `tests/test_architecture.py` y `tests/test_memory_bounds.py` en verde
- `backend/main.py` no se ha modificado
</success_criteria>

<output>
Al terminar, crear `.planning/phases/31-vista-de-anal-tica/31-02-SUMMARY.md`.
</output>
