---
phase: 31
plan: 04
type: execute
wave: 2
depends_on: ["31-01"]
files_modified:
  - backend/storage/repositories.py
  - tests/test_repositories.py
autonomous: true
requirements: [OPS-12, OPS-13, OPS-14]

must_haves:
  truths:
    - "Las cuatro agregaciones de la fase se resuelven en SQL, en una sola consulta cada una, y devuelven el orden ya resuelto"
    - "La comparacion con el periodo anterior sale de la misma consulta que el periodo actual, no de una segunda"
    - "Las cuatro agregaciones responden en menos de 500 ms sobre 100.000 eventos"
    - "El ranking de personas devuelve filas reales sobre datos sembrados con identidad, no una lista vacia"
    - "La ocupacion por zona devuelve filas reales sobre datos sembrados con zona"
  artifacts:
    - path: "backend/storage/repositories.py"
      provides: "AnalyticsRepo con hourly, summary, occupancy, persons_ranking y person_avatars"
      contains: "class AnalyticsRepo"
    - path: "tests/test_repositories.py"
      provides: "4 tests de presupuesto @100k + 2 tests de regresion de datos no vacios"
      contains: "analytics_budget"
  key_links:
    - from: "backend/storage/repositories.py::AnalyticsRepo.persons_ranking"
      to: "idx_events_analytics"
      via: "INDEXED BY idx_events_analytics — falla la consulta si el indice no existe (precondicion de 31-01)"
      pattern: "INDEXED BY idx_events_analytics"
    - from: "backend/storage/repositories.py::AnalyticsRepo"
      to: "events.ts"
      via: "substr(ts,1,13) / substr(ts,1,10) protegido por TEST_datetime_storage_format_is_fixed_width_iso"
      pattern: "substr\\(ts,1,1[03]\\)"
    - from: "backend/storage/repositories.py::AnalyticsRepo.person_avatars"
      to: "captures.image_path"
      via: "captures VIVE en events.db; el nombre NO, ese lo resuelve el router (31-05)"
      pattern: "captures"
---

<objective>
Anadir `AnalyticsRepo` a `backend/storage/repositories.py` con las cuatro agregaciones de la fase —serie temporal, resumen, ocupacion por zona y ranking de personas— todas resueltas en SQL sobre `events`, y medir las cuatro contra el presupuesto del criterio 4.

Purpose: OPS-14 dice literalmente que las agregaciones se calculan en base de datos, no en el navegador. Este plan es donde eso se cumple o no: el total, el porcentaje de variacion, la hora pico, el orden del ranking y el orden de las zonas salen todos de SQL, para que 31-05 solo tenga que serializar y los modulos del navegador no tengan nada que calcular.
Output: repositorio de analitica completo y medido.

**Fuente de datos: `events`, nunca `detection_stats`.** Tres razones, en orden de peso: (1) el histograma "Actividad hoy" del dashboard actual ya se alimenta de `EventRepo.hourly_counts(..., type=LINE_CROSSED)` (`backend/database.py:222`), y si la analitica contara otra cosa para la misma hora el operador veria dos cifras distintas del mismo dia en la misma aplicacion; (2) invariante 9 de CLAUDE.md — "Conteo = tracking/cruce, no suma de detecciones por frame": `detection_stats.detections` depende del FPS que haya elegido `AdaptiveRate`; (3) medido, `detection_stats` es **mas caro** (60,7 ms frente a 27,0 ms), porque su preagregado es por minuto, no por hora. `detection_stats` sigue siendo la fuente de `/api/v2/analytics/context` de la Fase 27 y ahi se queda.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-vista-de-anal-tica/31-CONTEXT.md
@.planning/phases/31-vista-de-anal-tica/31-RESEARCH.md
@.planning/phases/31-vista-de-anal-tica/31-PATTERNS.md
@.planning/phases/31-vista-de-anal-tica/31-01-SUMMARY.md
@CLAUDE.md
</context>

<interfaces>
API publica de `AnalyticsRepo`, que consume 31-05 (router) y 31-09 (export):

```python
BUCKET_HOUR_MAX_DAYS = 7   # <=7 dias -> cubo horario; por encima, cubo diario


def bucket_for(cur_from: datetime.datetime, cur_to: datetime.datetime) -> str:
    """'hour' | 'day'. Decision de LEGIBILIDAD, no de tamano — ver docstring."""


class AnalyticsRepo:
    def __init__(self, session_factory) -> None: ...

    async def hourly(self, camera_id, cur_from, cur_to, bucket) -> list[tuple[str, int, int]]:
        """[(bucket, actual, anterior), ...] ordenado por bucket. Cubos VACIOS NO aparecen:
        el relleno a cero lo hace el router, que es quien conoce el eje completo."""

    async def summary(self, camera_id, cur_from, cur_to, bucket) -> dict:
        """{'total': int, 'previous_total': int,
            'peak_bucket': str|None, 'peak_value': int,
            'min_bucket': str|None, 'min_value': int,
            'known': int, 'unknown': int}"""

    async def occupancy(self, camera_id, cur_from, cur_to, limit=10) -> tuple[list[dict], int]:
        """([{'zone_id','name','value'}, ...] ordenado desc, total_zones)"""

    async def persons_ranking(self, camera_id, cur_from, cur_to, limit=10) -> list[tuple[int, int, int]]:
        """[(person_id, actual, anterior), ...] ordenado por actual desc. SIN nombre:
        los nombres viven en persons.db, otro fichero — los resuelve el router."""

    async def person_avatars(self, person_ids: list[int]) -> dict[int, str]:
        """{person_id: '/gallery/{id}/{fichero}'} del capture mas reciente de cada uno."""
```

**Aviso que el plan no puede dejar implicito: `events.db` tiene una tabla `persons` del esquema v2 (`models.py:41-53`) que NADIE ha poblado nunca.** Los nombres reales viven en `persons.db`, un fichero SQLite distinto (`main.py:472`, `recognizer.py:110`). El `JOIN` obvio `JOIN persons p ON p.id = e.person_id` **compila, se ejecuta y devuelve cero filas**, y quien lo escriba creera que "no hay personas identificadas". **Prohibido usarlo aqui.** Y prohibido `ATTACH DATABASE`: complejidad desproporcionada para un lookup en un diccionario de 60 entradas, contra la Regla final de CLAUDE.md.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: AnalyticsRepo con bucket_for, hourly() y summary()</name>
  <files>backend/storage/repositories.py, tests/test_repositories.py</files>
  <read_first>
    - backend/storage/repositories.py lineas 1-60 (cabecera de imports y `class EventRepo.__init__` con `self._sf`) y 346-480 (`DetectionStatRepo` completo, en especial `hourly_baseline()` — molde de docstring con justificacion de diseno y de doble agregacion)
    - backend/storage/repositories.py lineas 102-191 (`EventRepo._filter_conditions` y `query()`) — patron de `bindparams`, nunca f-strings, con el comentario del `+` unario
    - backend/events/types.py — valores exactos de `EventType.LINE_CROSSED` y `EventType.ZONE_ENTERED`
    - .planning/phases/31-vista-de-anal-tica/31-RESEARCH.md secciones "Q1", "Q2", "Q4", "Pattern 1", "Pattern 2", "Pattern 4" y "Code Examples"
    - .planning/phases/31-vista-de-anal-tica/31-PATTERNS.md seccion "backend/storage/repositories.py::AnalyticsRepo"
  </read_first>
  <behavior>
    - `TEST_analytics_bucket_for_switches_at_seven_days`: rango de 1 dia -> `"hour"`; de 7 dias -> `"hour"`; de 8 dias -> `"day"`; de 30 dias -> `"day"`.
    - `TEST_analytics_hourly_splits_current_and_previous`: con eventos `LINE_CROSSED` sembrados a mano en dos ventanas contiguas, cada fila devuelve `actual` y `anterior` en la posicion correcta y una sola consulta cubre las dos.
    - `TEST_analytics_hourly_ignores_other_event_types`: un `INTRUSION` en la misma hora no suma.
    - `TEST_analytics_summary_returns_peak_and_min`: con 3 cubos de valores 5, 34 y 0, `peak_value == 34` con su bucket, `min_value == 0` con el suyo y `total == 39`.
    - `TEST_analytics_summary_known_unknown_counts_distinct_people`: 3 eventos de `person_id=1`, 2 de `person_id=2` y 4 sin identidad con dos `track_id` distintos -> `known == 2`, `unknown == 2` (personas distintas, no eventos).
    - `TEST_analytics_summary_previous_total_zero_is_not_an_error`: sin datos en el periodo anterior, `previous_total == 0` y el repo NO calcula porcentaje (eso es del router).
  </behavior>
  <action>
    Anadir al final de `backend/storage/repositories.py`, despues de las clases existentes:

    **1) Constante y helpers de modulo:**
    ```python
    BUCKET_HOUR_MAX_DAYS = 7


    def bucket_for(cur_from: datetime.datetime, cur_to: datetime.datetime) -> str:
        """Cubo horario hasta 7 dias, diario por encima.

        ES UNA DECISION DE LEGIBILIDAD, NO DE TAMANO. Medido (31-RESEARCH.md,
        criterio 3): 30 dias en cubo horario pesan 11,3 KB con arrays paralelos y
        hasta el rango maximo de 90 dias horarios cabe en 57,0 KB — el limite del
        criterio son 100 KB, o sea 8,8x de margen. El umbral existe porque 720
        barras no se leen, y porque la regla de tipo de grafica del UI-SPEC cambia
        a linea por encima de 48 cubos. Que nadie lo "optimice" creyendo que esta
        aqui por peso.
        """
        return "hour" if (cur_to - cur_from) <= datetime.timedelta(days=BUCKET_HOUR_MAX_DAYS) else "day"


    def _bucket_expr(bucket: str) -> str:
        """substr sobre el TEXT ISO de ancho fijo — 2,3x mas rapido que strftime
        (51,8 ms frente a 120,8 ms @100k). El formato de almacenamiento esta
        protegido por TEST_datetime_storage_format_is_fixed_width_iso (31-01):
        si ese test cae, hay que volver a strftime de forma explicita."""
        return "substr(ts,1,13)" if bucket == "hour" else "substr(ts,1,10)"
    ```

    **2) `class AnalyticsRepo:` con `__init__(self, session_factory)` -> `self._sf = session_factory`**, igual que `EventRepo` y `DetectionStatRepo`.

    **3) `hourly()` — serie actual + periodo anterior en UNA consulta (50,6 ms medidos @100k sobre 60 dias):**
    ```python
    sql = text(f"""
        SELECT {_bucket_expr(bucket)} AS b,
               SUM(CASE WHEN ts >= :cur_from THEN 1 ELSE 0 END) AS cur,
               SUM(CASE WHEN ts <  :cur_from THEN 1 ELSE 0 END) AS prev
          FROM events
         WHERE camera_id = :cam AND ts >= :prev_from AND ts < :cur_to
           AND type = :etype
         GROUP BY b ORDER BY b
    """)
    ```
    con `prev_from = cur_from - (cur_to - cur_from)` y `etype = EventType.LINE_CROSSED.value`. Parametros por diccionario en `session.execute(sql, {...})`, **nunca** interpolados: lo unico que entra por f-string es `_bucket_expr()`, que devuelve una de dos constantes del propio modulo.

    Nota de correccion importante: para el cubo diario, los `b` del periodo anterior y del actual **son fechas distintas**, asi que la misma fila nunca lleva `cur` y `prev` a la vez. Es correcto y esperado: el router empareja por **posicion** en el eje (cubo i del actual con cubo i del anterior), no por etiqueta. Devuelve las filas tal cual y deja el emparejamiento al router.

    **4) `summary()` — CTE con total, pico y minimo (60,4 ms medidos) mas la consulta de doble ventana (25,8 ms) y la de conocidas/desconocidas (13,6 ms con el indice de 31-01):**
    ```sql
    -- (a) cubos del periodo actual -> total, pico y minimo
    WITH b AS (
      SELECT {bucket_expr} AS bucket, COUNT(*) AS n
        FROM events
       WHERE camera_id = :cam AND ts >= :cur_from AND ts < :cur_to AND type = :etype
       GROUP BY bucket
    )
    SELECT (SELECT SUM(n) FROM b),
           (SELECT bucket FROM b ORDER BY n DESC, bucket ASC LIMIT 1),
           (SELECT MAX(n)  FROM b),
           (SELECT bucket FROM b ORDER BY n ASC,  bucket ASC LIMIT 1),
           (SELECT MIN(n)  FROM b)

    -- (b) total actual vs anterior, doble ventana en una sola consulta
    SELECT SUM(CASE WHEN ts >= :cur_from THEN 1 ELSE 0 END) AS cur,
           SUM(CASE WHEN ts <  :cur_from THEN 1 ELSE 0 END) AS prev
      FROM events
     WHERE camera_id = :cam AND ts >= :prev_from AND ts < :cur_to AND type = :etype

    -- (c) conocidas / desconocidas del periodo actual
    SELECT COUNT(DISTINCT person_id) AS known,
           COUNT(DISTINCT CASE WHEN person_id IS NULL THEN track_id END) AS unknown
      FROM events
     WHERE camera_id = :cam AND ts >= :cur_from AND ts < :cur_to
    ```
    **Decision cerrada sobre (c) — la pregunta abierta A6 del research se resuelve asi: son PERSONAS DISTINTAS, no eventos.** "342 conocidas" leido como eventos es una cifra enorme y poco intuitiva; leido como personas es 12, que es lo que la etiqueta sugiere en castellano. Y **sin filtro de tipo**: `person_id` se escribe en eventos de identidad, no en los `LINE_CROSSED`, asi que filtrar por tipo devolveria cero. Dejar esto escrito en el docstring del metodo.

    Si `total` sale `None` (rango sin eventos), devolver `0`; si no hay cubos, `peak_bucket`/`min_bucket` van a `None` y sus valores a `0`.
  </action>
  <verify>
    <automated>cd "F:\Documentos\IA\Proyecto_Camara" && .venv/Scripts/python.exe -m pytest tests/test_repositories.py -k "analytics_hourly or analytics_summary or bucket_for" -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "class AnalyticsRepo" backend/storage/repositories.py` encuentra la clase
    - `grep -c "detection_stats" backend/storage/repositories.py` no ha aumentado respecto al valor previo (AnalyticsRepo no toca esa tabla)
    - `grep -c "JOIN persons" backend/storage/repositories.py` devuelve `0`
    - `grep -c "ATTACH DATABASE" backend/storage/repositories.py` devuelve `0`
    - `grep -c "substr(ts,1,13)" backend/storage/repositories.py` devuelve al menos `1` y lo mismo para `substr(ts,1,10)`
    - `grep -n "DECISION DE LEGIBILIDAD" backend/storage/repositories.py` encuentra el docstring de `bucket_for` (para que nadie lo "optimice" por tamano)
    - Los 6 tests del bloque `<behavior>` existen y pasan
  </acceptance_criteria>
  <done>`bucket_for`, `hourly()` y `summary()` existen, resuelven en SQL total, pico, minimo, comparacion con el periodo anterior y conocidas/desconocidas, y tienen sus 6 tests en verde.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: occupancy(), persons_ranking() con INDEXED BY y person_avatars()</name>
  <files>backend/storage/repositories.py, tests/test_repositories.py</files>
  <read_first>
    - backend/storage/repositories.py — `AnalyticsRepo` tal como quedo tras la Task 1
    - backend/database.py lineas 29-62 — modelos `Zone` (tabla `zones`, columna `name`) y `Capture` (tabla `captures`, columnas `person_id`, `timestamp`, `image_path`), AMBAS en events.db
    - backend/main.py lineas 1238-1248 — formato exacto de la URL de galeria: `/gallery/{person_id}/{Path(image_path).name}`
    - .planning/phases/31-vista-de-anal-tica/31-RESEARCH.md secciones "Q5" (split persons.db, resuelto) y "Open Questions" punto 2 (ocupacion filtrada a ZONE_ENTERED)
  </read_first>
  <behavior>
    - `TEST_analytics_occupancy_orders_desc_and_truncates`: 12 zonas con conteos distintos -> devuelve 10 filas ordenadas descendente y `total_zones == 12`.
    - `TEST_analytics_occupancy_only_counts_zone_entered`: un `INTRUSION` con `zone_id` no suma (una intrusion ya esta contada como entrada).
    - `TEST_analytics_occupancy_falls_back_to_zone_id_when_zone_deleted`: `zone_id` sin fila en `zones` -> `name == zone_id`, nunca `None` ni la cadena "null".
    - `TEST_analytics_ranking_orders_desc_and_limits`: 12 personas -> 10 filas ordenadas por visitas del periodo actual.
    - `TEST_analytics_ranking_excludes_null_person_id`: eventos sin identidad no crean una fila fantasma.
    - `TEST_analytics_ranking_returns_previous_window_counts`: una persona con 3 visitas ahora y 5 antes devuelve `(id, 3, 5)`.
    - `TEST_analytics_person_avatars_returns_latest_capture`: dos captures de la misma persona -> gana el `timestamp` mas reciente y la URL sale como `/gallery/{id}/{fichero}`.
  </behavior>
  <action>
    **1) `occupancy()`** — dos consultas, ambas filtradas a `ZONE_ENTERED` (23,8-27,9 ms medidos). "Ocupacion" es cuanta gente **entro** en la zona; una `INTRUSION` ya esta contada como entrada, por eso no se suma:
    ```sql
    SELECT e.zone_id AS zone_id,
           COALESCE(z.name, e.zone_id) AS zone_name,
           COUNT(*) AS n
      FROM events e
      LEFT JOIN zones z ON z.id = e.zone_id
     WHERE e.camera_id = :cam AND e.ts >= :cur_from AND e.ts < :cur_to
       AND e.type = :etype AND e.zone_id IS NOT NULL
     GROUP BY e.zone_id
     ORDER BY n DESC
     LIMIT :limit
    ```
    ```sql
    SELECT COUNT(DISTINCT zone_id) FROM events
     WHERE camera_id = :cam AND ts >= :cur_from AND ts < :cur_to
       AND type = :etype AND zone_id IS NOT NULL
    ```
    El `COALESCE` es obligatorio: si una zona se borro, el `LEFT JOIN` da `NULL` y el panel pintaria la cadena "null" (suposicion A5 del research). El segundo `COUNT` alimenta el pie "Mostrando las 10 zonas con más ocupación." del UI-SPEC.

    **2) `persons_ranking()`** — la unica consulta de la fase que necesita hint (26,7 ms con hint frente a 212,6 ms sin el):
    ```sql
    SELECT person_id,
           SUM(CASE WHEN ts >= :cur_from THEN 1 ELSE 0 END) AS cur,
           SUM(CASE WHEN ts <  :cur_from THEN 1 ELSE 0 END) AS prev
      FROM events INDEXED BY idx_events_analytics
     WHERE camera_id = :cam AND ts >= :prev_from AND ts < :cur_to
       AND person_id IS NOT NULL
     GROUP BY person_id
    HAVING cur > 0
     ORDER BY cur DESC
     LIMIT :limit
    ```
    Sin hint, SQLite elige `idx_events_person` y hace skip-scan. Se probaron **cinco** reescrituras para desviarlo sin hint (`+person_id IS NOT NULL`, `ifnull(person_id,0) > 0`, `person_id+0 > 0`, `HAVING person_id IS NOT NULL`, subconsulta) y **ninguna cambia el plan**: el truco del `+` unario que funciono en la Fase 30 con `type` aqui no sirve porque `camera_id` y `ts` siguen siendo terminos indexables de `idx_events_person`. Documentar esto en el docstring, con el aviso de que **`INDEXED BY` falla la consulta si el indice no existe**, asi que la migracion v4 de 31-01 es precondicion dura y el test de la Task 3 es lo que detecta su desaparicion en CI y no en produccion.

    **3) `person_avatars(person_ids)`** — subconsulta correlacionada del capture mas reciente por persona, sobre `captures`, que **si** vive en `events.db`:
    ```sql
    SELECT c.person_id, c.image_path
      FROM captures c
     WHERE c.person_id IN :ids
       AND c.image_path IS NOT NULL
       AND c.timestamp = (SELECT MAX(c2.timestamp) FROM captures c2 WHERE c2.person_id = c.person_id)
    ```
    con `bindparam("ids", expanding=True)`. Devolver `{person_id: f"/gallery/{person_id}/{Path(image_path).name}"}`, mismo formato que `main.py:1244`. Con `person_ids` vacio, devolver `{}` sin ejecutar la consulta.

    **Prohibido en los tres metodos:** `sorted()` en Python sobre los resultados, `JOIN persons`, `ATTACH DATABASE`, f-strings con datos del cliente.
  </action>
  <verify>
    <automated>cd "F:\Documentos\IA\Proyecto_Camara" && .venv/Scripts/python.exe -m pytest tests/test_repositories.py -k "analytics_occupancy or analytics_ranking or analytics_person_avatars" -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "INDEXED BY idx_events_analytics" backend/storage/repositories.py` devuelve exactamente `1` (solo el ranking lo lleva)
    - `grep -c "COALESCE(z.name, e.zone_id)" backend/storage/repositories.py` devuelve `1`
    - `grep -c "ZONE_ENTERED" backend/storage/repositories.py` devuelve al menos `1`
    - `grep -c "sorted(" backend/storage/repositories.py` no ha aumentado respecto al valor previo
    - `grep -c "/gallery/" backend/storage/repositories.py` devuelve al menos `1`
    - Los 7 tests del bloque `<behavior>` existen y pasan
  </acceptance_criteria>
  <done>Las cuatro agregaciones estan completas; el orden del ranking y el de las zonas lo decide SQL, y el avatar sale de `captures` sin tocar `persons.db`.</done>
</task>

<task type="auto">
  <name>Task 3: Presupuesto del criterio 4 a 100.000 eventos y regresion de datos no vacios</name>
  <files>tests/test_repositories.py</files>
  <read_first>
    - tests/test_repositories.py lineas ~570-630 — `TEST_query_performance_100k` completo (molde de test de presupuesto: `seed_events`, `create_async_engine`, `time.perf_counter()`, `engine.dispose()`, assert con mensaje que incluye el tiempo medido) y el bloque `_BUDGET_10K_SECS` de la Fase 30
    - scripts/seed_events.py tal como quedo tras 31-01 (parametros `persons` y `zones`)
    - backend/storage/migrations.py — `run_migrations`, que la base de test tiene que ejecutar (o crearse con `create_all()`, que ya declara el indice) para que `INDEXED BY` no reviente
    - .planning/phases/31-vista-de-anal-tica/31-RESEARCH.md seccion "Validation Architecture" (nota sobre los presupuestos) y "Criterio 4 — antes y despues del indice"
  </read_first>
  <action>
    **1) Cuatro tests de presupuesto**, con `_ANALYTICS_BUDGET_SECS = 0.5` como constante de modulo (el criterio literal del ROADMAP), sembrando **una sola vez** por test con:
    ```python
    seed_events(db_file, n=100_000, days=30, camera_id="cam1", persons=60, zones=14)
    ```
    Los nombres llevan `analytics_budget` para que `pytest -k analytics_budget` los seleccione todos:
    - `TEST_analytics_budget_hourly_100k`
    - `TEST_analytics_budget_summary_100k`
    - `TEST_analytics_budget_occupancy_100k`
    - `TEST_analytics_budget_persons_ranking_100k`

    Cada uno mide con `time.perf_counter()` alrededor de la llamada al metodo y afirma `elapsed < _ANALYTICS_BUDGET_SECS`, con mensaje `f"{nombre} tardo {elapsed:.3f}s, presupuesto {_ANALYTICS_BUDGET_SECS}s"`. Lo medido en el research es 14-78 ms, o sea entre 6x y 35x de margen: suficiente para una maquina cargada y aun asi capaz de detectar la desaparicion del indice, que devuelve estas consultas a 535-618 ms.

    La base de test **debe tener el indice**: crearla con `models.Base.metadata.create_all()` (que ya lo declara desde 31-01) o pasar por `run_migrations()`. Si falta, `persons_ranking()` no devuelve datos vacios: **revienta** con un error de SQLite por el `INDEXED BY`.

    **2) Dos tests de regresion de datos no vacios** — esta es la guarda contra el fallo silencioso que el research senalo explicitamente:
    - `TEST_analytics_budget_ranking_sees_seeded_persons`: sobre la misma base sembrada con `persons=60`, `persons_ranking()` devuelve **exactamente 10 filas** y la suma de `cur` es mayor que 0. Docstring: *"Sin esto, si seed_events volviera a dejar person_id a NULL, el test de presupuesto mediria sobre datos vacios y pasaria por accidente."*
    - `TEST_analytics_budget_occupancy_sees_seeded_zones`: sobre la misma base sembrada con `zones=14`, `occupancy()` devuelve 10 filas y `total_zones == 14`.

    **3) Guarda del plan de consulta del ranking** — `TEST_analytics_ranking_uses_analytics_index`: ejecutar `EXPLAIN QUERY PLAN` sobre el SQL del ranking contra la base migrada y comprobar que el texto del plan contiene `idx_events_analytics`. Es la senal temprana de que el hint sigue vivo; mismo enfoque que uso la Fase 30 en `30-02`.
  </action>
  <verify>
    <automated>cd "F:\Documentos\IA\Proyecto_Camara" && .venv/Scripts/python.exe -m pytest tests/test_repositories.py -k analytics_budget -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "_ANALYTICS_BUDGET_SECS = 0.5" tests/test_repositories.py` devuelve `1`
    - `grep -c "analytics_budget" tests/test_repositories.py` devuelve al menos `6`
    - `grep -c "persons=60" tests/test_repositories.py` devuelve al menos `1` y `grep -c "zones=14" tests/test_repositories.py` al menos `1` — la siembra usa identidad y zona, no la de por defecto
    - `grep -c "EXPLAIN QUERY PLAN" tests/test_repositories.py` ha aumentado en al menos `1`
    - `.venv/Scripts/python.exe -m pytest tests/test_repositories.py -k analytics_budget -q` en verde con 7 tests
    - La salida del comando anterior no contiene la palabra `skipped`
  </acceptance_criteria>
  <done>Las cuatro agregaciones estan medidas por debajo de 0,5 s sobre 100.000 eventos con identidad y zona sembradas, y hay dos tests que impiden que esa medicion se haga sobre datos vacios.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| parametros de rango del cliente -> SQL | `cur_from`, `cur_to`, `camera_id` y `limit` llegan de la peticion HTTP a traves del router |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-10 | Tampering | Inyeccion SQL via `camera_id` o el rango | mitigate | Todos los parametros van por diccionario de `session.execute(sql, {...})` / `bindparams`. Lo unico interpolado por f-string es `_bucket_expr()`, que devuelve una de dos constantes literales del modulo, nunca entrada del cliente |
| T-31-11 | Tampering | Nombre de indice del `INDEXED BY` construido dinamicamente | mitigate | El nombre es un literal dentro del SQL del metodo; no hay ninguna via para que llegue del cliente |
| T-31-12 | Denial of Service | Rango absurdo que barra la tabla entera | transfer | El tope de 90 dias y la validacion del rango los impone el router (31-05), que es la autoridad; el repo confia en su llamador, igual que `EventRepo` hoy |
| T-31-13 | Information disclosure | El ranking devuelve `person_id`, a diferencia de `/context` de la Fase 27 que es solo de recuentos | accept | Es lo que OPS-13 pide explicitamente. Se documenta como diferencia deliberada en el docstring del router (31-05), no se oculta |
</threat_model>

<verification>
```bash
cd "F:\Documentos\IA\Proyecto_Camara"
.venv/Scripts/python.exe -m pytest tests/test_repositories.py -q
.venv/Scripts/python.exe -m pytest tests/test_architecture.py -q
```
La suite completa de `test_repositories.py` incluye los presupuestos de la Fase 30, que no deben degradarse: el research verifico que con `idx_events_analytics` presente la linea temporal sigue eligiendo `idx_events_ts_id` sin cambio de plan.
</verification>

<success_criteria>
- `AnalyticsRepo` expone `hourly`, `summary`, `occupancy`, `persons_ranking` y `person_avatars`
- Ninguna agregacion usa `detection_stats`, `JOIN persons` ni `ATTACH DATABASE`
- El orden del ranking y el de las zonas los decide `ORDER BY ... LIMIT`, no Python
- Las cuatro agregaciones tardan menos de 0,5 s sobre 100.000 eventos con identidad y zona sembradas
- El ranking devuelve 10 filas reales sobre esa base, no una lista vacia
- `tests/test_repositories.py` completo en verde
</success_criteria>

<output>
Al terminar, crear `.planning/phases/31-vista-de-anal-tica/31-04-SUMMARY.md`.
</output>
