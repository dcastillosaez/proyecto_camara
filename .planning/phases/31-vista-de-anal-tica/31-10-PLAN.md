---
phase: 31
plan: 10
type: execute
wave: 6
depends_on: ["31-06", "31-07", "31-08", "31-09"]
files_modified:
  - frontend/js/views/analytics.js
  - frontend/js/views/analytics-export.js
  - frontend/js/views/analytics-charts.js
  - frontend/js/app.js
autonomous: true
requirements: [OPS-12, OPS-13, OPS-14, OPS-15]

must_haves:
  truths:
    - "Abrir la pestana Analitica por primera vez crea las graficas y pide los datos una sola vez"
    - "Cada panel resuelve su estado por separado: si un endpoint falla, los otros tres se pintan igual"
    - "Cambiar de rango con peticiones en vuelo aborta las anteriores y no pinta respuestas rezagadas"
    - "El mapa de calor distingue sin actividad de camara sin senal, y no se recarga con la pestana oculta"
    - "Los cuatro botones de exportacion descargan el rango visible desde el servidor"
    - "Quien no abra nunca la analitica no paga ni una sola peticion de agregacion"
  artifacts:
    - path: "frontend/js/views/analytics.js"
      provides: "Orquestador: boot diferido, tanda de peticiones, estados por panel, heatmap y pie de actualizacion"
      contains: "AbortController"
    - path: "frontend/js/views/analytics-export.js"
      provides: "URLs de descarga CSV/JSON y estado de los cuatro botones"
      contains: "window.location.href"
    - path: "frontend/js/app.js"
      provides: "initAnalytics() registrado ANTES de initNav()"
      contains: "initAnalytics"
  key_links:
    - from: "frontend/js/views/analytics.js"
      to: "frontend/js/nav.js::registerAnalyticsBoot"
      via: "initAnalytics() registra (bootAnalytics, resizeAnalytics)"
      pattern: "registerAnalyticsBoot"
    - from: "frontend/js/views/analytics.js"
      to: "/api/v2/analytics/heatmap/scale"
      via: "fetch previo que decide el estado del panel (200/404/503) antes de tocar img.src"
      pattern: "heatmap/scale"
    - from: "frontend/js/views/analytics-export.js"
      to: "/api/v2/analytics/export"
      via: "window.location.href con la URL validada por isSafeMediaUrl"
      pattern: "isSafeMediaUrl"
---

<objective>
Cerrar la vista: escribir `frontend/js/views/analytics.js` (el orquestador) y `frontend/js/views/analytics-export.js` (las descargas), y engancharlos desde `app.js`. Aqui es donde el andamiaje de 31-03, las graficas de 31-07, el rango y el ranking de 31-08 y los siete endpoints de 31-05/06/09 se convierten en una vista que funciona.

Purpose: es el plan que hace verdad D-08 y D-09 —cuatro peticiones en paralelo, un estado por panel, un `AbortController` por tanda— y el que resuelve la trampa fina del heatmap: un `<img>` **no puede distinguir un 404 de un 503**, asi que el panel tiene que preguntar antes por la escala, que si devuelve codigos legibles.
Output: la vista de analitica completa y funcionando de punta a punta.

Este plan **no** anade polling, ni websocket, ni cache de respuestas, ni un septimo modulo. Si `analytics.js` se acerca al tope de 300 lineas, la valvula de escape esta escrita en la Task 2 y consiste en mover el panel del heatmap a `analytics-charts.js`, que tiene presupuesto de sobra — **nunca** crear un fichero nuevo, que romperia D-14 y la lista `LOCKED_JS` de 31-11.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-vista-de-anal-tica/31-CONTEXT.md
@.planning/phases/31-vista-de-anal-tica/31-UI-SPEC.md
@.planning/phases/31-vista-de-anal-tica/31-RESEARCH.md
@.planning/phases/31-vista-de-anal-tica/31-03-SUMMARY.md
@.planning/phases/31-vista-de-anal-tica/31-07-SUMMARY.md
@.planning/phases/31-vista-de-anal-tica/31-08-SUMMARY.md
@.planning/phases/31-vista-de-anal-tica/31-09-SUMMARY.md
@CLAUDE.md
</context>

<interfaces>
```javascript
// frontend/js/views/analytics.js
export function initAnalytics();     // registra el arranque diferido en nav.js. NO pide nada todavia
// privadas del modulo: bootAnalytics(), resizeAnalytics(), load(range), loadPanel(name, range, signal)

// frontend/js/views/analytics-export.js
export function initExport(getRange);              // engancha los 4 botones; getRange() -> {from,to}
export function setExportEnabled(panel, enabled);  // panel: 'hourly'|'occupancy'|'persons'|'json'
```

**Orden de arranque en `app.js` — importa, y no es cosmetico:**

```javascript
initAnalytics();   // 1) registra boot y resize en nav.js
initNav();         // 2) resuelve el hash y activa la pestana; si es #analitica, dispara el boot
```

Al reves, abrir la pagina directamente en `#analitica` activaria la pestana **antes** de que haya un boot registrado, y la vista se quedaria en esqueletos para siempre. `nav.js` (31-03) no se modifica para arreglar esto: el orden correcto es responsabilidad de quien arranca.

Estados de panel, con los ids que ya existen en el marcado de 31-03:

| Estado | Que se ve |
|---|---|
| `loading` | `#an-{panel}-loading` visible, contenido y demas estados con `hidden` |
| `ok` | contenido visible, los tres estados con `hidden` |
| `empty` | `#an-{panel}-empty` visible |
| `error` | `#an-{panel}-error` visible, con su boton `#an-{panel}-retry` |

Panel del heatmap: `loading` / `ok` / `empty` (404) / `offline` (503, `#an-heatmap-offline`), y **sin** boton de reintentar — no lo tiene en el marcado, porque su solucion es recuperar la camara, no volver a pulsar.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: analytics.js — arranque diferido, tanda de peticiones y estados por panel</name>
  <files>frontend/js/views/analytics.js</files>
  <read_first>
    - frontend/js/nav.js (31-03) COMPLETO — `registerAnalyticsBoot(bootFn, resizeFn)`, `activeView()` y cuando se llama a cada uno
    - frontend/js/views/analytics-charts.js (31-07) — firmas de `createCharts`, `renderHourly`, `renderOccupancy`, `setCompare`, `resizeCharts`
    - frontend/js/views/analytics-range.js y frontend/js/views/analytics-ranking.js (31-08) — firmas de `initRange`, `currentRange`, `renderCards`, `renderRanking`
    - frontend/js/api.js (9 lineas) — `apiFetch(path, opts)` propaga `opts` a `fetch`, asi que acepta `{ signal }`, y **lanza** con el `detail` del error
    - frontend/js/views/timeline.js — como gestiona hoy el proyecto un estado de carga/vacio/error por bloque
    - frontend/index.html tras 31-03 — la lista completa de ids de estados y de contenido
    - .planning/phases/31-vista-de-anal-tica/31-UI-SPEC.md seccion "Contrato de datos e interacción (OPS-14, criterio 4)"
  </read_first>
  <action>
    Crear `frontend/js/views/analytics.js` con una cabecera de comentario que resuma las tres reglas que lo gobiernan: peticiones en paralelo sin `Promise.all` (D-08), un `AbortController` por tanda (D-09) y **cero agregacion** (D-07).

    **1) `initAnalytics()`** — lo unico que hace es `registerAnalyticsBoot(bootAnalytics, resizeAnalytics)`. Ni pide datos, ni crea graficas, ni toca el DOM: quien no abra la pestana no paga nada.

    **2) `bootAnalytics()`** (primera activacion, una sola vez en la vida de la pagina):
    ```javascript
    createCharts();
    initRange(load);              // los cambios de rango entran por aqui
    initExport(currentRange);
    bindPanelButtons();           // reintentos y chip "Comparar"
    load(currentRange());
    ```

    **3) `load(range)`** — la tanda:
    ```javascript
    let ctrl = null;
    let pending = 0;
    let lastRange = null;

    function load(range) {
      lastRange = range;
      if (ctrl) ctrl.abort();     // D-09: se abortan las peticiones en vuelo de la tanda anterior
      ctrl = new AbortController();
      pending = 5;                // 4 endpoints + heatmap
      ['summary', 'hourly', 'occupancy', 'persons'].forEach(p => loadPanel(p, range, ctrl.signal));
      loadHeatmap();
    }
    ```
    **Nada de `Promise.all`**: cada `loadPanel` es una promesa independiente con su `catch` propio, y un endpoint caido pinta su error dentro de su panel dejando los otros tres intactos. (`forEach` sobre una lista **constante de nombres de panel** no es agregacion de datos del servidor: el criterio de OPS-14 prohibe `.reduce/.sort/.filter/Math.max`, y ninguno de esos aparece.)

    **4) `loadPanel(name, range, signal)`:**
    - `setPanelState(name, 'loading')` y `setExportEnabled(name, false)`.
    - `apiFetch('/api/v2/analytics/' + name + '?camera_id=cam1&from=' + range.from + '&to=' + range.to, { signal })`.
    - En exito, segun el panel:
      - `summary` -> `renderCards(data)`, y ademas `#an-hourly-peak` recibe `Pico {peak.label} · {peak.value}` por `textContent`, o cadena vacia si `data.peak` es `null`.
      - `hourly` -> `#an-hourly-title` recibe `'Personas por hora'` o `'Personas por día'` segun `data.range.bucket`; `renderHourly(data)`; el chip `#an-compare` se deshabilita con `title="No hay suficientes datos del periodo anterior para comparar."` cuando `data.has_previous === false`; estado `ok` si `data.total > 0`, `empty` si no.
      - `occupancy` -> `renderOccupancy(data)`; `#an-occupancy-foot` visible solo si `data.truncated`; `empty` cuando `data.labels.length === 0`.
      - `persons` -> `renderRanking(data)`; `empty` cuando `data.persons.length === 0`.
      - `setExportEnabled(name, true)` **solo** cuando el estado final es `ok`: exportar cero filas es una descarga inutil.
    - En error: **si es un abort, no pintar nada** — `if (err.name === 'AbortError') return;` antes de tocar el DOM. Es la mitad de D-09 que se olvida siempre y la que produce el bug de "la vista parpadea con datos del rango anterior".
    - En `finally`, `settle()`: decrementa `pending` y, al llegar a 0, escribe en `#an-updated` el texto `Actualizado a las {HH:MM:SS}` con `new Date().toLocaleTimeString('es-ES')`. **Se sella cuando TODOS han resuelto**, con exito o con error, no en la primera respuesta.

    **5) `bindPanelButtons()`**: los tres `#an-*-retry` vuelven a llamar a `loadPanel` **solo de su panel** con `lastRange`; `#an-compare` conmuta `.active` y `aria-pressed` y llama a `setCompare(...)` **sin volver a pedir datos** (los datos del periodo anterior ya estan en el modulo de graficas).

    **6) `setPanelState(name, state)`**: helper de tabla que conmuta la propiedad `hidden` de `#an-{name}-loading`, `-empty`, `-error` y del contenido del panel. Nunca `style.display`.

    **7) `resizeAnalytics()`**: llama a `resizeCharts()` y, si hay un heatmap pendiente, lo aplica (Task 2).
  </action>
  <verify>
    <automated>cd "F:\Documentos\IA\Proyecto_Camara" && node --check frontend/js/views/analytics.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --check frontend/js/views/analytics.js` sale con codigo 0
    - `grep -cE "\.reduce\(|\.sort\(|\.filter\(|Math\.max\(" frontend/js/views/analytics.js` devuelve `0`
    - `grep -c "Promise.all" frontend/js/views/analytics.js` devuelve `0`
    - `grep -c "AbortController" frontend/js/views/analytics.js` devuelve `1` y `grep -c "AbortError" frontend/js/views/analytics.js` es al menos `1`
    - `grep -c "registerAnalyticsBoot" frontend/js/views/analytics.js` devuelve `1`
    - `grep -cE "setInterval\(|setTimeout\(" frontend/js/views/analytics.js` devuelve `0` (sin polling)
    - `grep -c "Actualizado a las" frontend/js/views/analytics.js` devuelve `1`
    - `grep -c "style.display" frontend/js/views/analytics.js` devuelve `0`
  </acceptance_criteria>
  <done>La vista arranca en la primera activacion, pide los cuatro endpoints en paralelo, resuelve cada panel por separado, aborta las tandas viejas y sella el pie cuando todo ha terminado.</done>
</task>

<task type="auto">
  <name>Task 2: Panel del mapa de calor — 404 frente a 503, cache-busting y recarga diferida</name>
  <files>frontend/js/views/analytics.js, frontend/js/views/analytics-charts.js</files>
  <read_first>
    - frontend/js/views/analytics.js tal como quedo tras la Task 1
    - frontend/js/nav.js — `activeView()`, que es lo que permite saber si la pestana esta visible
    - frontend/js/views/timeline-row.js lineas 16-18 — `isSafeMediaUrl()`
    - frontend/index.html tras 31-03 — `#an-heatmap-img`, `#an-heatmap-legend`, `#an-heatmap-mark-0`, `#an-heatmap-mark-mid`, `#an-heatmap-mark-peak`, `#an-heatmap-loading`, `#an-heatmap-empty`, `#an-heatmap-offline`, `#an-heatmap-scope`
    - .planning/phases/31-vista-de-anal-tica/31-RESEARCH.md "Pitfall 6" y "Pitfall 7"
    - .planning/phases/31-vista-de-anal-tica/31-UI-SPEC.md seccion "Panel 3 — Mapa de calor"
    - .planning/phases/31-vista-de-anal-tica/31-06-SUMMARY.md — contrato exacto de `/heatmap` y `/heatmap/scale`
  </read_first>
  <action>
    **1) `loadHeatmap()` — se pregunta primero por la escala, y no es un capricho.**
    Un `<img>` que falla dispara `onerror` **igual para un 404 que para un 503**, asi que con la imagen sola es imposible elegir entre "Sin actividad acumulada" y "Cámara sin señal", que son los dos textos distintos que exige el UI-SPEC. La escala si devuelve codigos legibles:

    ```javascript
    async function loadHeatmap() {
      setPanelState('heatmap', 'loading');
      let res;
      // fetch crudo y no apiFetch A PROPOSITO: apiFetch lanza y pierde el codigo, y aqui
      // el codigo ES el dato (404 = sin actividad, 503 = sin senal). No "unificar" esto.
      try {
        res = await fetch('/api/v2/analytics/heatmap/scale?camera_id=cam1');
      } catch (e) { setPanelState('heatmap', 'offline'); settle(); return; }
      if (res.status === 404) { setPanelState('heatmap', 'empty');   settle(); return; }
      if (!res.ok)            { setPanelState('heatmap', 'offline'); settle(); return; }
      applyHeatmap(await res.json());
      settle();
    }
    ```

    **2) `applyHeatmap(scale)` — leyenda relativa y honesta:**
    - `#an-heatmap-mark-0` recibe `'0'`, `#an-heatmap-mark-mid` recibe `'50 %'` y `#an-heatmap-mark-peak` recibe `'pico'`. **Tres constantes**: la rampa se normaliza dividiendo por el maximo, o sea que la escala siempre es relativa; escribir ahi una cifra de personas seria inventar una unidad (D-12).
    - El valor absoluto va en el `title` de `#an-heatmap-legend`: `Escala relativa. Pico {peak} y media {mean} {unit}.` La unidad la manda el servidor, no se escribe a mano en el cliente.
    - `#an-heatmap-scope` ya trae "acumulado desde el arranque" del marcado de 31-03: **no se toca**.

    **3) Recarga diferida y cache-busting** (Pitfall 6 y Pitfall 7):
    ```javascript
    let heatmapPending = null;

    function applyHeatmap(scale) {
      // ...leyenda...
      // Sin el ?t= el navegador sirve de cache y el mapa parece congelado. El servidor
      // ignora el parametro. La URL sigue empezando por '/', asi que isSafeMediaUrl la acepta.
      const url = '/api/v2/analytics/heatmap?camera_id=cam1&t=' + Date.now();
      // <img loading="lazy"> dentro de un contenedor oculto NO dispara la peticion: si la
      // pestana no esta activa se guarda la URL y se aplica al volver (Pitfall 6).
      if (activeView() !== 'analitica') { heatmapPending = url; return; }
      const img = document.getElementById('an-heatmap-img');
      if (isSafeMediaUrl(url)) img.src = url;
      setPanelState('heatmap', 'ok');
    }
    ```
    Y en `resizeAnalytics()`: si hay `heatmapPending`, asignarlo entonces y limpiarlo. `isSafeMediaUrl` se importa aunque la URL la construya el propio modulo: es una linea y evita reabrir la clase de alerta de CodeQL que se cerro al final de la Fase 30.

    **4) Cuando se recarga el heatmap:** al cambiar de rango (aunque **no** respete el rango, para que el boton no parezca muerto) y al volver a la pestana. **Sin polling y sin `setInterval`.**

    **5) Valvula de escape del tope de lineas.** Al terminar, medir `analytics.js`. Si pasa de 300 lineas, mover el bloque completo del heatmap (`loadHeatmap`, `applyHeatmap`, `heatmapPending`) a `analytics-charts.js` como `export function renderHeatmap(...)` —es el modulo de paneles visuales y tiene presupuesto libre— y llamarlo desde el orquestador. **Prohibido crear un septimo modulo**: rompe D-14 y la lista `LOCKED_JS` que fija 31-11.
  </action>
  <verify>
    <automated>cd "F:\Documentos\IA\Proyecto_Camara" && node --check frontend/js/views/analytics.js && node --check frontend/js/views/analytics-charts.js && wc -l frontend/js/views/analytics.js frontend/js/views/analytics-charts.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --check` en verde para los dos ficheros y `wc -l` de cada uno por debajo de `300`
    - `grep -c "heatmap/scale" frontend/js/views/analytics.js` es al menos `1`
    - `grep -c "res.status === 404" frontend/js/views/analytics.js` devuelve `1` (404 y 503 se distinguen de verdad)
    - `grep -c "activeView()" frontend/js/views/analytics.js` es al menos `1` (recarga diferida)
    - `grep -c "Date.now()" frontend/js/views/analytics.js` es al menos `1` (cache-busting)
    - `grep -c "isSafeMediaUrl" frontend/js/views/analytics.js` es al menos `2` (import + uso)
    - `grep -c "50 %" frontend/js/views/analytics.js` devuelve `1` — la leyenda es relativa, no una cifra de personas
    - `grep -cE "\.reduce\(|\.sort\(|\.filter\(|Math\.max\(" frontend/js/views/analytics.js` sigue devolviendo `0`
  </acceptance_criteria>
  <done>El panel del heatmap distingue 404 de 503, pinta la leyenda relativa con el valor absoluto en el `title`, no se recarga con la pestana oculta y no cachea imagen vieja.</done>
</task>

<task type="auto">
  <name>Task 3: analytics-export.js y el arranque desde app.js</name>
  <files>frontend/js/views/analytics-export.js, frontend/js/app.js</files>
  <read_first>
    - frontend/js/views/dashboard-events.js lineas 108-116 — `bindEventExport()`: el patron de descarga del repo es `window.location.href = url`, sin `blob`, sin `URL.createObjectURL`, sin `<a download>` sintetico. Y su comentario, que anota literalmente este trabajo como pendiente de OPS-15
    - frontend/js/views/timeline-row.js lineas 16-18 — `isSafeMediaUrl()`
    - frontend/js/app.js COMPLETO (~58 lineas) — el bloque `DOMContentLoaded` y donde quedo `initNav()` tras 31-03
    - frontend/index.html tras 31-03 — `#an-export-json`, `#an-export-hourly`, `#an-export-occupancy`, `#an-export-persons`
    - .planning/phases/31-vista-de-anal-tica/31-09-SUMMARY.md — parametros exactos de `/api/v2/analytics/export`
    - .planning/phases/31-vista-de-anal-tica/31-UI-SPEC.md seccion "Exportación (criterio 5)"
  </read_first>
  <action>
    **1) Crear `frontend/js/views/analytics-export.js`** (~90 lineas):

    ```javascript
    const BTN = {
      hourly:    'an-export-hourly',
      occupancy: 'an-export-occupancy',
      persons:   'an-export-persons',
      json:      'an-export-json',
    };

    export function initExport(getRange) { ... }
    export function setExportEnabled(panel, enabled) { ... }
    ```
    - `initExport(getRange)` engancha un `click` por boton. Al pulsar: `const r = getRange();` y URL
      `'/api/v2/analytics/export?camera_id=cam1&from=' + r.from + '&to=' + r.to + '&format=csv&panel=' + panel`
      (para `json`, `format=json` y sin `panel`). Antes de asignar: `if (!isSafeMediaUrl(url)) return;` y despues `window.location.href = url`.
    - **El navegador no construye ni el CSV ni el JSON** (D-10): serializar en cliente seria agregar en cliente y chocaria de frente con OPS-14. El fichero lo genera el servidor y el nombre lo fija su `Content-Disposition`.
    - `setExportEnabled(panel, enabled)` pone/quita el atributo `disabled` del boton y le aplica `opacity: 0.25` y `cursor: not-allowed` mientras esta apagado. El handler ademas sale antes si el boton esta deshabilitado (defensa contra un `click` sintetico).
    - `#an-export-json` se habilita cuando **los cuatro** paneles han terminado en `ok`; el orquestador es quien lo decide llamando a `setExportEnabled('json', ...)`.
    - **Sin toast de exito**: la descarga del navegador es la confirmacion, un toast encima seria ruido. Si algun dia hace falta el error, la cadena de la tabla de copy es "No se pudo generar el fichero. Inténtalo de nuevo." — pero con `window.location.href` no hay promesa que capturar, asi que **no se implementa nada de eso ahora**.

    **2) `frontend/js/app.js`** — dos lineas, en este orden:
    ```javascript
    import { initAnalytics } from './views/analytics.js';
    // ...dentro del bloque de bind* del DOMContentLoaded:
    initAnalytics();   // ANTES de initNav(): registra el boot que initNav puede disparar al instante
    initNav();
    ```
    No mover ninguna otra llamada del arranque ni reordenar los `bind*` existentes.
  </action>
  <verify>
    <automated>cd "F:\Documentos\IA\Proyecto_Camara" && node --check frontend/js/views/analytics-export.js && node --check frontend/js/app.js && .venv/Scripts/python.exe -m pytest tests/test_frontend_modules.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `node --check frontend/js/views/analytics-export.js` y `node --check frontend/js/app.js` salen con codigo 0
    - `wc -l < frontend/js/views/analytics-export.js` devuelve menos de `300`
    - `grep -c "window.location.href" frontend/js/views/analytics-export.js` devuelve `1`
    - `grep -cE "createObjectURL|new Blob\(|JSON.stringify\(" frontend/js/views/analytics-export.js` devuelve `0` — el fichero lo genera el servidor
    - `grep -cE "\.reduce\(|\.sort\(|\.filter\(|Math\.max\(" frontend/js/views/analytics-export.js` devuelve `0`
    - `grep -c "isSafeMediaUrl" frontend/js/views/analytics-export.js` es al menos `2`
    - `grep -c "initAnalytics()" frontend/js/app.js` devuelve `1`
    - `.venv/Scripts/python.exe -c "s=open('frontend/js/app.js',encoding='utf-8').read(); assert s.index('initAnalytics()') < s.index('initNav()'), 'initAnalytics debe ir ANTES de initNav'"` no lanza
  </acceptance_criteria>
  <done>Los cuatro botones descargan el rango visible desde el servidor, se apagan cuando su panel no tiene datos, y la vista arranca en el orden correcto desde `app.js`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| respuesta del servidor -> DOM | Cuatro payloads y una imagen entran en el documento en cada tanda |
| URL construida en cliente -> sink de navegacion | `img.src` del heatmap y `window.location.href` de la descarga |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-34 | Tampering | `window.location.href` con una URL manipulada (CodeQL `js/client-side-unvalidated-url-redirection`) | mitigate | `isSafeMediaUrl()` antes de asignar, tanto en la descarga como en el `img.src` del heatmap |
| T-31-35 | Tampering | Respuesta rezagada de una tanda abortada pintando datos de otro rango | mitigate | Un `AbortController` por tanda y salida temprana con `err.name === 'AbortError'` antes de tocar el DOM |
| T-31-36 | Denial of Service | Tandas de cinco peticiones disparadas al teclear en los `input[type=date]` | mitigate | El selector de rango (31-08) solo emite al pulsar "Aplicar rango"; el orquestador no engancha ningun listener propio a los campos de fecha |
| T-31-37 | Information disclosure | Cache del navegador sirviendo un heatmap de otra sesion | mitigate | Cache-busting con `?t=` en cada recarga |
</threat_model>

<verification>
```bash
cd "F:\Documentos\IA\Proyecto_Camara"
node --check frontend/js/views/analytics.js
node --check frontend/js/views/analytics-export.js
node --check frontend/js/app.js
.venv/Scripts/python.exe -m pytest tests/test_frontend_modules.py -q
```
Los seis modulos de la fase entran en `LOCKED_JS` en 31-11, cuando ya existen todos en disco.
La verificacion visual de la vista completa —graficas pintadas, cambio de rango real, descargas—
es el checkpoint de 31-11.
</verification>

<success_criteria>
- `initAnalytics()` registra el boot y se llama antes que `initNav()`
- La primera activacion crea las graficas y dispara una sola tanda de cinco peticiones
- Cada panel resuelve loading/ok/empty/error por separado, sin `Promise.all`
- Las tandas abortadas no pintan nada
- El heatmap distingue 404 de 503, difiere su recarga con la pestana oculta y evita la cache
- Los cuatro botones de exportacion descargan desde el servidor y se apagan sin datos
- Cero `.reduce()`, `.sort()`, `.filter()` y `Math.max()` en los tres modulos nuevos
- Todos los modulos caben en 300 lineas y pasan `node --check`
</success_criteria>

<output>
Al terminar, crear `.planning/phases/31-vista-de-anal-tica/31-10-SUMMARY.md`, indicando si hizo
falta usar la valvula de escape del heatmap y en que fichero quedo finalmente ese bloque.
</output>
