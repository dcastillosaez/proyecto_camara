---
phase: 31
plan: 07
type: execute
wave: 4
depends_on: ["31-03", "31-05"]
files_modified:
  - frontend/js/views/analytics-charts.js
autonomous: true
requirements: [OPS-12, OPS-14]

must_haves:
  truths:
    - "Las dos graficas se crean bajo demanda, nunca al cargar la pagina, y se pueden redimensionar al volver a la pestana"
    - "La grafica de personas cambia sola de barras a linea cuando el servidor lo dice, sin que el cliente cuente cubos"
    - "El periodo anterior se distingue por trazo discontinuo, no solo por color, y se puede apagar"
    - "Cada lienzo lleva un resumen textual que se regenera con cada carga"
    - "Ni una operacion de agregacion sobre los datos del servidor en todo el modulo"
  artifacts:
    - path: "frontend/js/views/analytics-charts.js"
      provides: "createCharts, renderHourly, renderOccupancy, setCompare, resizeCharts"
      contains: "export function createCharts"
  key_links:
    - from: "frontend/js/views/analytics-charts.js::createCharts"
      to: "#an-chart-hourly / #an-chart-occupancy"
      via: "getContext('2d') sobre los dos canvas del contrato de ids de 31-03"
      pattern: "an-chart-hourly"
    - from: "frontend/js/views/analytics-charts.js::renderHourly"
      to: "payload de GET /api/v2/analytics/hourly"
      via: "labels/values/previous/peak_index/chart consumidos por indice, sin agregar"
      pattern: "peak_index"
---

<objective>
Escribir `frontend/js/views/analytics-charts.js`: las dos instancias de Chart.js de la vista (personas por hora/dia y ocupacion por zona), su creacion diferida, su actualizacion desde los payloads del router y el resumen textual accesible de cada lienzo.

Purpose: es el ancla visual de la vista (D-06) y el modulo donde mas facil seria colar agregacion de cliente: el realce del pico pide un `Math.max()`, el tipo de grafica pide un `length > 48`, la leyenda pide un `.some()`. **Ninguna de las tres hace falta**: 31-05 manda `peak_index`, `chart` y `has_previous` ya resueltos. Este modulo es la prueba de que OPS-14 se cumple de verdad y no solo en el backend.
Output: modulo de graficas listo para que 31-10 lo llame con los datos.

Este plan **no** pide datos ni toca el DOM fuera de los dos `<canvas>` y sus `aria-label`: los estados de panel (cargando / vacio / error), la anotacion "Pico {HH:00} · {N}" de la cabecera y el chip "Comparar" los gestiona el orquestador de 31-10, que llama a `setCompare()`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-vista-de-anal-tica/31-CONTEXT.md
@.planning/phases/31-vista-de-anal-tica/31-UI-SPEC.md
@.planning/phases/31-vista-de-anal-tica/31-RESEARCH.md
@.planning/phases/31-vista-de-anal-tica/31-03-SUMMARY.md
@.planning/phases/31-vista-de-anal-tica/31-05-SUMMARY.md
@CLAUDE.md
</context>

<interfaces>
API publica del modulo, que consume 31-10:

```javascript
export function createCharts();          // instancia las dos graficas. Idempotente: si ya existen, no hace nada
export function renderHourly(data);      // data = payload de GET /api/v2/analytics/hourly
export function renderOccupancy(data);   // data = payload de GET /api/v2/analytics/occupancy
export function setCompare(enabled);     // muestra u oculta la serie del periodo anterior
export function resizeCharts();          // chart.resize() de las dos; se llama al reactivar la pestana
```

Payloads que entran (contrato cerrado por 31-05, **no** re-derivar nada de ellos):

```jsonc
// renderHourly
{"range": {"from","to","bucket","days"}, "labels": [...], "values": [...],
 "previous": [...], "total": 312, "peak_index": 17, "min_index": 4,
 "has_previous": true, "chart": "bar" | "line"}

// renderOccupancy
{"range": {...}, "labels": [...], "values": [...], "total_zones": 12, "truncated": true}
```

Constantes de la paleta de series del UI-SPEC, declaradas una vez en la cabecera del modulo:

```javascript
const SERIE_ACTUAL   = '#60a5fa';                    // blue-400, trazo 2px
const SERIE_RELLENO  = 'rgba(96,165,250,0.20)';      // area al 20 %
const SERIE_ANTERIOR = '#475569';                    // slate-600, borderDash [4,4], 1.5px, SIN relleno
const REJILLA        = 'rgba(51,65,85,0.3)';
const TICK           = '#475569';
const TICK_SIZE      = 12;                           // D-04: 12px tambien dentro del lienzo
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: createCharts() y renderHourly() con las dos series y el realce del pico</name>
  <files>frontend/js/views/analytics-charts.js</files>
  <read_first>
    - frontend/js/views/dashboard-events.js lineas 50-95 — el `new Chart(...)` heredado completo: es el molde de instanciacion (tooltip, `scales`, `legend: {display:false}`, `responsive`, `maintainAspectRatio:false`). **Ojo: sus `font: {size: 9}` y `{size: 10/11}` son legado de la Fase 5 y NO se replican** (D-04: 12px)
    - frontend/js/views/timeline.js lineas 1-40 — convencion de modulo del repo (cabecera con comentario de responsabilidad, helper `$`, estado privado del modulo)
    - frontend/index.html tras 31-03 — los ids `#an-chart-hourly`, `#an-chart-occupancy`, y que cada canvas vive en un contenedor de 240px de alto
    - .planning/phases/31-vista-de-anal-tica/31-UI-SPEC.md secciones "Panel 1", "Paleta de series", "Trampa de Chart.js con el contenedor oculto", "Accesibilidad" y Typography
    - .planning/phases/31-vista-de-anal-tica/31-RESEARCH.md "Pitfall 9" (eje Y desde 0) y "Pitfall 10" (tope de 300 lineas)
  </read_first>
  <action>
    Crear `frontend/js/views/analytics-charts.js` con cabecera de comentario explicando: (a) las instancias se crean en la **primera activacion de la pestana** porque un canvas en un contenedor `hidden` mide 0x0 y `responsive: true` se quedaria con ese tamano (D-03); (b) **este modulo no agrega**: `peak_index`, `chart` y `has_previous` llegan del servidor (D-07).

    **1) Estado privado y `createCharts()`:**
    ```javascript
    let hourlyChart = null;
    let occupancyChart = null;
    let compareOn = true;

    export function createCharts() {
      if (hourlyChart) return;      // idempotente: nav.js solo llama al boot una vez, pero
      ...                           // que llamarlo dos veces no duplique instancias
    }
    ```
    Chart.js esta ya cargado por CDN con SRI en `index.html` y expuesto como global `Chart`;
    **no** anadir import ni etiqueta `<script>` (D-01, y `TEST_no_inline_logic` cuenta los `<script>`).

    **2) Grafica horaria** — un solo `new Chart` con **dos datasets** desde el principio (el
    segundo con `data: []` hasta que llegue el payload), porque cambiar el numero de datasets
    en caliente obliga a recrear escalas:
    - dataset 0: `label: 'Personas'`, `borderColor: SERIE_ACTUAL`, `backgroundColor: SERIE_ACTUAL` (barras) / `SERIE_RELLENO` con `fill: true` (linea), `borderWidth: 2`, `borderRadius: 3`, `tension: 0.25`, `pointRadius: 0`.
    - dataset 1: `label: 'Anterior'`, `borderColor: SERIE_ANTERIOR`, `borderDash: [4, 4]`, `borderWidth: 1.5`, `fill: false`, `pointRadius: 0`.
    - `options.plugins.legend.display = false` (la leyenda va como texto propio en la cabecera, ya en el marcado de 31-03).
    - `options.plugins.tooltip`: `backgroundColor:'#1e293b'`, `borderColor:'#334155'`, `borderWidth:1`, `titleColor:'#94a3b8'`, `bodyColor:'#f8fafc'`, `titleFont:{size:12}`, `bodyFont:{size:12}`, `mode:'index'`, `intersect:false`, `callbacks.label: c => c.datasetIndex === 0 ? \` ${c.parsed.y} personas\` : \` ${c.parsed.y} personas (anterior)\``.
    - `options.scales.x`: `grid:{color:REJILLA, drawTicks:false}`, `border:{display:false}`, `ticks:{color:TICK, font:{size:TICK_SIZE}, maxRotation:0, maxTicksLimit:12}`.
    - `options.scales.y`: **`min: 0`** (Pitfall 9: truncar el cero en una grafica de conteos exagera las variaciones), `grid` y `border` igual que en X, `ticks:{color:TICK, font:{size:TICK_SIZE}, precision:0, maxTicksLimit:4}`.
    - `responsive: true, maintainAspectRatio: false, animation: {duration: 300}`.

    **3) `renderHourly(data)`:**
    - `hourlyChart.config.type = data.chart;` y `hourlyChart.data.datasets[0].fill = (data.chart === 'line');` — el **servidor** decide el tipo; el cliente no cuenta cubos.
    - `hourlyChart.data.labels = data.labels;` `datasets[0].data = data.values;` `datasets[1].data = compareOn ? data.previous : [];`
    - **Realce del pico sin `Math.max()`**: color por posicion comparando el indice con `data.peak_index`:
      ```javascript
      const paint = (rgb, alpha) => `rgba(96,165,250,${alpha})`;
      datasets[0].backgroundColor = data.labels.map((_, i) => paint(0, i === data.peak_index ? 1 : 0.55));
      ```
      `.map()` esta permitido **solo** para esto: generar colores por posicion es presentacion, no agregacion; no toca los valores. El resto de barras a 0.55 y la del pico a 1, como manda el UI-SPEC. En modo linea, ademas `datasets[0].pointRadius = data.labels.map((_, i) => (i === data.peak_index ? 4 : 0))`.
    - **Resumen accesible regenerado en cada carga** (UI-SPEC: "un canvas con aria-label='Gráfica' es un canvas sin accesibilidad"), sobre `#an-chart-hourly`:
      ```javascript
      const que = data.range.bucket === 'hour' ? 'Personas por hora' : 'Personas por día';
      const cnv = document.getElementById('an-chart-hourly');
      cnv.setAttribute('aria-label', data.peak_index === null
        ? `${que}, del ${data.range.from} al ${data.range.to}: sin actividad en este rango.`
        : `${que}, del ${data.range.from} al ${data.range.to}: ${data.total} personas en total, ` +
          `máximo ${data.values[data.peak_index]} en ${data.labels[data.peak_index]}, ` +
          `mínimo ${data.values[data.min_index]} en ${data.labels[data.min_index]}.`);
      ```
      Todo por indice: `data.total`, `peak_index` y `min_index` los manda el servidor.
    - Terminar con `hourlyChart.update()`.

    **4) `setCompare(enabled)`**: guarda `compareOn` y repinta el dataset 1 con los ultimos
    `previous` (guardarlos en una variable de modulo al renderizar) o con `[]`. Un `update()` y
    nada mas — no vuelve a pedir datos.
  </action>
  <verify>
    <automated>cd "F:\Documentos\IA\Proyecto_Camara" && node --check frontend/js/views/analytics-charts.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --check frontend/js/views/analytics-charts.js` sale con codigo 0
    - `grep -cE "\.reduce\(|\.sort\(|\.filter\(|Math\.max\(|Math\.min\(" frontend/js/views/analytics-charts.js` devuelve `0` — **criterio de OPS-14, innegociable**
    - `grep -c "peak_index" frontend/js/views/analytics-charts.js` es al menos `2` (color del pico y resumen accesible)
    - `grep -c "data.chart" frontend/js/views/analytics-charts.js` es al menos `1` (el tipo lo decide el servidor)
    - `grep -c "min: 0" frontend/js/views/analytics-charts.js` es al menos `1` (eje Y sin truncar)
    - `grep -oE "size: ?[0-9]+" frontend/js/views/analytics-charts.js | sort -u` solo contiene `size: 12`
    - `grep -c "borderDash" frontend/js/views/analytics-charts.js` devuelve `1` (la serie anterior no se distingue solo por color)
    - `grep -c "setAttribute('aria-label'" frontend/js/views/analytics-charts.js` es al menos `1`
    - `grep -c "<script" frontend/index.html` no ha cambiado
  </acceptance_criteria>
  <done>La grafica horaria se crea bajo demanda, cambia de barras a linea segun el servidor, superpone el periodo anterior en discontinuo, realza el pico por indice y publica un resumen textual real.</done>
</task>

<task type="auto">
  <name>Task 2: renderOccupancy() y resizeCharts()</name>
  <files>frontend/js/views/analytics-charts.js</files>
  <read_first>
    - frontend/js/views/analytics-charts.js tal como quedo tras la Task 1
    - .planning/phases/31-vista-de-anal-tica/31-UI-SPEC.md secciones "Panel 2 — Ocupación por zona", "Paleta de series" (opacidad decreciente por posicion, nunca un color por zona) y "Convención anti-XSS"
    - .planning/phases/31-vista-de-anal-tica/31-RESEARCH.md "Code Examples — Activacion diferida de las graficas"
  </read_first>
  <action>
    **1) Grafica de ocupacion**, creada dentro del mismo `createCharts()`:
    - `type: 'bar'`, `indexAxis: 'y'` (barras horizontales: dejan leer nombres de zona largos sin rotar texto).
    - Un solo dataset, `legend` desactivada, mismas rejillas y ticks de 12px, `scales.x.min = 0`.
    - **Opacidad decreciente por posicion**, calculada solo con el indice y la longitud:
      ```javascript
      // 1.00 la zona mas ocupada, bajando en tramos iguales hasta 0.45 la ultima.
      // El orden lo decidio SQL; aqui solo se pinta.
      const n = data.labels.length;
      ds.backgroundColor = data.labels.map((_, i) => `rgba(96,165,250,${(1 - (i / Math.max(1, n - 1)) * 0.55).toFixed(2)})`);
      ```
      **CUIDADO**: ese `Math.max(1, n - 1)` es una guarda de division por cero sobre una
      longitud, no una agregacion de datos — pero **el criterio de aceptacion prohibe
      `Math.max(` en el fichero** y no distingue matices. Escribirlo como `const paso = n > 1 ? (i / (n - 1)) : 0;` y usar `paso`. La regla se cumple **por lectura literal**, sin excepciones argumentadas: es lo que la hace auditable.
    - **Valor numerico siempre visible** al final de cada barra, sin ratón. La via mas simple sin plugins nuevos (D-01 prohibe anadir `chartjs-plugin-datalabels`): eje X arriba con `ticks` legibles **no basta** segun el UI-SPEC. Usar el eje Y con `ticks.callback` que devuelva `` `${label}` `` y anadir el numero al final de la etiqueta de categoria: `data.labels.map((l, i) => `${l} — ${data.values[i]}`)`. Es texto de categoria, lo pinta Chart.js como valor de dato (nunca como HTML, convencion anti-XSS de D-15) y cumple "visible sin ratón".
    - Resumen accesible sobre `#an-chart-occupancy`, regenerado en cada carga:
      `Ocupación por zona, del {from} al {to}: {total_zones} zonas; la más ocupada, {labels[0]}, con {values[0]} entradas.` Con `labels.length === 0`, la cadena es `Ocupación por zona: sin zonas definidas.` El indice 0 es el maximo **porque lo ordeno SQL**, no porque el cliente lo busque.

    **2) `resizeCharts()`:**
    ```javascript
    export function resizeCharts() {
      if (hourlyChart) hourlyChart.resize();
      if (occupancyChart) occupancyChart.resize();
    }
    ```
    Es el punto 2 del contrato de la trampa de Chart.js: en cada activacion posterior de la
    pestana hay que redimensionar antes de repintar, porque mientras el contenedor estuvo
    oculto el canvas pudo quedarse con un tamano viejo.

    **3) Comprobar el tope de lineas.** El modulo tiene un presupuesto de ~200 lineas y el tope
    duro es 300. Si al terminar pasa de 300, **no** partir en un septimo modulo (rompe D-14):
    recortar comentario redundante y factorizar las opciones comunes de los dos graficos en una
    constante `_BASE_OPTIONS`.
  </action>
  <verify>
    <automated>cd "F:\Documentos\IA\Proyecto_Camara" && node --check frontend/js/views/analytics-charts.js && .venv/Scripts/python.exe -c "p='frontend/js/views/analytics-charts.js'; n=len(open(p,encoding='utf-8').read().splitlines()); assert n<=300, n; print('lineas', n)"</automated>
  </verify>
  <acceptance_criteria>
    - `node --check frontend/js/views/analytics-charts.js` sale con codigo 0 y el fichero tiene 300 lineas o menos
    - `grep -cE "\.reduce\(|\.sort\(|\.filter\(|Math\.max\(|Math\.min\(" frontend/js/views/analytics-charts.js` devuelve `0`
    - `grep -c "indexAxis: 'y'" frontend/js/views/analytics-charts.js` devuelve `1`
    - `grep -c "export function resizeCharts" frontend/js/views/analytics-charts.js` devuelve `1`
    - `grep -c "export function" frontend/js/views/analytics-charts.js` devuelve `5` (createCharts, renderHourly, renderOccupancy, setCompare, resizeCharts)
    - `grep -c "innerHTML" frontend/js/views/analytics-charts.js` devuelve `0` (este modulo no escribe marcado)
    - `grep -c "fetch(" frontend/js/views/analytics-charts.js` devuelve `0` (no pide datos; los recibe)
  </acceptance_criteria>
  <done>Las dos graficas existen con su paleta, su valor visible sin ratón, su resumen accesible y su `resize()`, en un modulo de 300 lineas o menos sin una sola agregacion.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| respuesta del servidor -> DOM | Nombres de zona y etiquetas de eje llegan del backend y acaban dibujados en el lienzo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-24 | Tampering | XSS por nombre de zona en una etiqueta | mitigate | Chart.js recibe los textos como **valores de datos**, nunca como HTML; el modulo no usa `innerHTML` (criterio de aceptacion con `grep -c innerHTML == 0`) |
| T-31-25 | Tampering | Agregacion silenciosa en el cliente que contradiga OPS-14 | mitigate | Criterio de aceptacion `grep -cE "\.reduce\(|\.sort\(|\.filter\(|Math\.max\(" == 0` en las dos tareas, y `TEST_analytics_no_client_aggregation` permanente en 31-11 |
</threat_model>

<verification>
```bash
cd "F:\Documentos\IA\Proyecto_Camara"
node --check frontend/js/views/analytics-charts.js
.venv/Scripts/python.exe -m pytest tests/test_frontend_modules.py -q
```
El modulo **todavia no** entra en `LOCKED_JS`: los seis de la fase entran juntos en 31-11,
cuando todos existen en disco.
</verification>

<success_criteria>
- `analytics-charts.js` exporta exactamente las cinco funciones del contrato
- Las instancias se crean bajo demanda y `resizeCharts()` existe
- Tipo de grafica, pico y disponibilidad de comparacion vienen del servidor
- Cero `.reduce()`, `.sort()`, `.filter()`, `Math.max()` y `Math.min()` en el fichero
- Tipografia de 12px dentro del lienzo y eje Y desde 0
- 300 lineas o menos y `node --check` en verde
</success_criteria>

<output>
Al terminar, crear `.planning/phases/31-vista-de-anal-tica/31-07-SUMMARY.md`.
</output>
