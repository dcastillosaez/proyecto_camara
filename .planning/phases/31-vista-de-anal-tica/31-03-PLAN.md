---
phase: 31
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/index.html
  - frontend/css/components.css
  - frontend/js/nav.js
  - frontend/js/app.js
autonomous: false
requirements: [OPS-12, OPS-13, OPS-15]

must_haves:
  truths:
    - "La cabecera ofrece dos pestanas, Operaciones y Analitica, y al pulsar una se ve su vista y se oculta la otra"
    - "Al cambiar de pestana el video MJPEG sigue conectado y el WebSocket no se reconecta"
    - "El hash de la URL refleja la pestana activa y pegar la URL abre la vista correcta"
    - "La vista de analitica existe con su barra de rango, sus cuatro tarjetas y sus cuatro paneles, todos con sus estados vacio/error/cargando"
    - "La reticula de operaciones sigue viendose exactamente igual que antes del cambio"
  artifacts:
    - path: "frontend/index.html"
      provides: "tablist en cabecera, <main> neutro con dos <section role=tabpanel> y marcado completo de la vista de analitica"
      contains: "view-analitica"
    - path: "frontend/css/components.css"
      provides: "Clases .nav-tab, .analytics-panel, .rank-row, .range-seg, .chart-skeleton con las medidas del UI-SPEC"
      contains: ".nav-tab"
    - path: "frontend/js/nav.js"
      provides: "Conmutador de pestanas con hash, replaceState, flechas y activacion diferida"
      contains: "history.replaceState"
    - path: "frontend/js/app.js"
      provides: "initNav() enganchado en el bloque de bind* del DOMContentLoaded"
      contains: "initNav"
  key_links:
    - from: "frontend/js/app.js"
      to: "frontend/js/nav.js::initNav"
      via: "import y llamada dentro del bloque de bind* del DOMContentLoaded"
      pattern: "initNav\\(\\)"
    - from: "frontend/js/nav.js::activate"
      to: "#view-operaciones / #view-analitica"
      via: "propiedad hidden, nunca display:none manual ni img.src=''"
      pattern: "hidden ="
    - from: "frontend/index.html <main>"
      to: "#view-operaciones"
      via: "las clases de grid bajan de <main> a la seccion de operaciones (Pitfall 8)"
      pattern: "lg:grid-cols-5"
---

<objective>
Construir el andamiaje de la segunda vista: reestructurar `<main>` para que contenga dos `role="tabpanel"` hermanos, anadir el `<nav role="tablist">` a la cabecera, escribir el marcado completo de la vista de analitica con todos sus ids de contrato y sus estados, anadir las cinco clases CSS nuevas y cablear `nav.js` desde `app.js`.

Purpose: D-02 fija que la analitica vive en una **segunda vista conmutada por pestanas**, no en un router ni como seccion apilada. La pagina actual ya mide 804 lineas y su columna derecha apila once paneles; anadirle cuatro paneles mas la convertiria en un deposito. Este plan es el que fija el **contrato de ids** que consumen 31-07, 31-08 y 31-10: sin el, esos tres planes tendrian que inventarse el marcado.
Output: la vista de analitica existe, se puede conmutar, y esta vacia — el comportamiento llega en 31-07/08/10.

Este plan **no** introduce `js/store.js`, ni router declarativo, ni ciclo de vida de vistas, ni `history.pushState`, ni carga diferida de modulos. El mecanismo minimo son dos `role="tabpanel"` y un `hidden` que se conmuta.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-vista-de-anal-tica/31-CONTEXT.md
@.planning/phases/31-vista-de-anal-tica/31-RESEARCH.md
@.planning/phases/31-vista-de-anal-tica/31-PATTERNS.md
@.planning/phases/31-vista-de-anal-tica/31-UI-SPEC.md
@CLAUDE.md
</context>

<interfaces>
**Contrato de ids de la vista de analitica.** 31-07, 31-08 y 31-10 escriben contra esta lista y no exploran el HTML; si un id cambia aqui, hay que cambiarlo alli.

```
Conmutador
  #tab-operaciones   role="tab"  aria-controls="view-operaciones"  aria-selected
  #tab-analitica     role="tab"  aria-controls="view-analitica"
  #view-operaciones  role="tabpanel" aria-labelledby="tab-operaciones"
  #view-analitica    role="tabpanel" aria-labelledby="tab-analitica"  hidden

Fila 0 — barra de rango (12 col)
  #an-subtitle          <p class="mono text-xs">   "{desde} → {hasta}"
  #an-range             contenedor .range-seg de los 4 chips
    button.filter-chip[data-range="today"]   #an-range-today    (activo por defecto)
    button.filter-chip[data-range="7d"]      #an-range-7d
    button.filter-chip[data-range="30d"]     #an-range-30d
    button.filter-chip[data-range="custom"]  #an-range-custom
  #an-custom            wrapper hidden con los dos date y el boton
    #an-from  input[type=date].filter-input
    #an-to    input[type=date].filter-input
    #an-apply button azul  "Aplicar rango"
  #an-range-error       <p class="text-xs"> hidden
  #an-export-json       button  "Exportar JSON"

Fila 1 — tarjetas (3+3+3+3), contenedor #an-cards con aria-live="polite"
  #an-total-value    #an-total-label
  #an-delta-value    #an-delta-range
  #an-peak-value     #an-peak-support
  #an-known-value    #an-unknown-support

Fila 2 — Personas por hora (8 col) / Ocupacion por zona (4 col)
  #an-panel-hourly   .analytics-panel
    #an-hourly-title  h2      "Personas por hora" | "Personas por dia"
    #an-hourly-peak   span.mono  "Pico {HH:00} · {N}"
    #an-compare       button.filter-chip.active  "Comparar"
    #an-export-hourly button 32x32  aria-label="Exportar personas por hora a CSV"
    #an-chart-hourly  canvas role="img" (contenedor de 240px de alto)
    #an-hourly-loading .chart-skeleton | #an-hourly-empty | #an-hourly-error (+ #an-hourly-retry)
  #an-panel-occupancy .analytics-panel
    #an-chart-occupancy canvas role="img" (contenedor de 240px)
    #an-occupancy-foot  p.mono hidden  "Mostrando las 10 zonas con mas ocupacion."
    #an-export-occupancy button 32x32
    #an-occupancy-loading | #an-occupancy-empty | #an-occupancy-error (+ #an-occupancy-retry)

Fila 3 — Mapa de calor (7 col) / Personas mas vistas (5 col)
  #an-panel-heatmap   .analytics-panel   (SIN boton de exportar — es una imagen)
    #an-heatmap-scope  span.chip.mono  "acumulado desde el arranque"
    #an-heatmap-img    img loading="lazy" alt="Mapa de calor de actividad acumulada sobre el encuadre de la camara"
    #an-heatmap-legend div  con #an-heatmap-mark-0, #an-heatmap-mark-mid, #an-heatmap-mark-peak
    #an-heatmap-loading | #an-heatmap-empty | #an-heatmap-offline
  #an-panel-persons   .analytics-panel
    #an-rank-list      div  (overflow-y-auto, max 10 filas visibles)
    #an-export-persons button 32x32
    #an-persons-loading | #an-persons-empty | #an-persons-error (+ #an-persons-retry)

Pie
  #an-updated   p.mono text-xs  "Actualizado a las {HH:MM:SS}"
```

```javascript
// frontend/js/nav.js — API publica que consume app.js (31-03) y analytics.js (31-10)
export function initNav();                    // resuelve el hash, activa la pestana, engancha listeners
export function registerAnalyticsBoot(bootFn, resizeFn);  // 31-10 registra su arranque diferido
export function activeView();                 // 'operaciones' | 'analitica'
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Bajar la reticula de main a la seccion de operaciones y anadir el tablist</name>
  <files>frontend/index.html</files>
  <read_first>
    - frontend/index.html lineas 1-60 — cabecera completa (`<header>`, bloque izquierdo con logo/titulo, bloque derecho con badge del modelo, `#cam-status`, `#btn-alert-center`, `#clock`) y la apertura de `<main>` en la linea 56
    - frontend/index.html linea ~678 — el cierre de `</main>`, para saber exactamente que se envuelve
    - frontend/index.html lineas 18-23 y 44-46 — patron de icono SVG estilo feather ya en uso (viewBox 24x24, stroke-width 2, stroke-linecap round, fill none, aria-hidden)
    - .planning/phases/31-vista-de-anal-tica/31-RESEARCH.md "Pitfall 8" y "Q8"
    - .planning/phases/31-vista-de-anal-tica/31-UI-SPEC.md seccion "Decision estructural: dos pestanas, no un router" y "Reticula de la vista"
  </read_first>
  <action>
    **1) Bajar la reticula un nivel (Pitfall 8, obligatorio).** Hoy `<main>` **es** la reticula:
    ```html
    <main class="flex-1 p-4 grid grid-cols-1 lg:grid-cols-5 gap-4 max-w-[1600px] mx-auto w-full" role="main">
    ```
    Si la vista de analitica se anade como tercer hijo, aparece como **una columna mas** del `lg:grid-cols-5`. Hay que mover las clases de reticula a la seccion de operaciones:
    ```html
    <main class="flex-1 w-full" role="main">
      <section id="view-operaciones" role="tabpanel" aria-labelledby="tab-operaciones"
               class="p-4 grid grid-cols-1 lg:grid-cols-5 gap-4 max-w-[1600px] mx-auto w-full">
        <!-- los dos <section> actuales (lg:col-span-3 y lg:col-span-2) SIN TOCAR -->
      </section>
      <section id="view-analitica" role="tabpanel" aria-labelledby="tab-analitica" hidden
               class="p-4 grid grid-cols-1 lg:grid-cols-12 gap-4 max-w-[1600px] mx-auto w-full">
        <!-- Task 2 lo rellena -->
      </section>
    </main>
    ```
    No reordenar, renombrar ni reindentar nada del contenido actual de operaciones: es puro envoltorio. Si el linter de indentacion obliga a mover lineas, hazlo sin cambiar ni un atributo.

    **2) Tablist en la cabecera.** Entre el badge del modelo y `#cam-status` del bloque derecho, sin reordenar nada de lo existente:
    ```html
    <nav role="tablist" aria-label="Vistas" class="flex items-center gap-1">
      <button id="tab-operaciones" role="tab" aria-selected="true" aria-controls="view-operaciones"
              class="nav-tab active text-sm font-semibold">Operaciones</button>
      <button id="tab-analitica" role="tab" aria-selected="false" aria-controls="view-analitica"
              tabindex="-1" class="nav-tab text-sm font-semibold">Analitica</button>
    </nav>
    ```
    Etiquetas EXACTAS de la tabla de copy: **"Operaciones"** y **"Analítica"** (con tilde en el HTML real; aqui va sin tilde solo por el encoding de este plan). Sin icono: son controles de texto.

    **3) Cargar `nav.js`** anadiendo `<script type="module" src="/static/js/nav.js"></script>`... **NO**: los modulos se importan desde `app.js`, que ya es el unico punto de entrada `type="module"`. No anadir etiquetas `<script>` nuevas — comprobar como se carga `app.js` hoy y no tocarlo.

    **La cabecera es comun a las dos vistas y no se oculta nunca** (estado de camara, reloj y campana siguen visibles en Analitica): es el espiritu de OPS-04 y no se negocia.
  </action>
  <verify>
    <automated>cd "F:\Documentos\IA\Proyecto_Camara" && .venv/Scripts/python.exe -m pytest tests/test_frontend_modules.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'id="view-operaciones"' frontend/index.html` devuelve `1` y `grep -c 'id="view-analitica"' frontend/index.html` devuelve `1`
    - `grep -n '<main class="flex-1 w-full" role="main">' frontend/index.html` encuentra el `<main>` neutro (ya no lleva `grid`)
    - `grep -n 'id="view-operaciones"' frontend/index.html` muestra la linea con `lg:grid-cols-5` — la reticula bajo un nivel
    - `grep -c 'role="tablist"' frontend/index.html` devuelve `1`
    - `grep -c 'id="tab-operaciones"\|id="tab-analitica"' frontend/index.html` devuelve `2`
    - El numero de `<script` en `frontend/index.html` es el mismo antes y despues del cambio
  </acceptance_criteria>
  <done>`<main>` es un contenedor neutro con dos `role="tabpanel"` hermanos, la reticula de operaciones vive en su seccion y la cabecera tiene el tablist.</done>
</task>

<task type="auto">
  <name>Task 2: Marcado completo de la vista de analitica y clases CSS nuevas</name>
  <files>frontend/index.html, frontend/css/components.css</files>
  <read_first>
    - frontend/index.html — el marcado de la linea temporal y del cajon de alertas anadido por la Fase 30 (buscar `#timeline-list`, `#tl-filter-severity`, `#alert-drawer`): es el molde de "panel con sus cuatro estados" y de chips `.filter-chip` a replicar
    - frontend/css/components.css completo (158 lineas) — todas las clases existentes (`.card`, `.ptz-btn`, `.filter-chip`, `.filter-input`, `.chip`, `.timeline-row`, `.row-action`, `#alert-drawer`), para heredar medidas y no duplicar reglas
    - .planning/phases/31-vista-de-anal-tica/31-UI-SPEC.md COMPLETO — Spacing Scale (con las excepciones: `.nav-tab` 32px con padding 8/16, `.rank-row` 40px, avatar 32x32, indicador de posicion 20px, lienzos de 240px, heatmap aspect-ratio 16/9 y max-height 360px, boton de export 32x32), Typography (4 tamanos: 12/14/16/30px, 2 pesos: 600/700), Color, Copywriting Contract (cadenas EXACTAS de todos los estados vacios y de error) y "Reticula de la vista"
    - .planning/phases/31-vista-de-anal-tica/31-CONTEXT.md D-01, D-05, D-06, D-12
  </read_first>
  <action>
    Rellenar `#view-analitica` con la reticula de 12 columnas del UI-SPEC y **todos** los ids del bloque `<interfaces>` de este plan. Cuatro filas:

    - **Fila 0 (12 col):** titulo "Analitica" + `#an-subtitle` (`.mono` 12px), `#an-range` con los cuatro `.filter-chip` (`#an-range-today` con `.active` y `aria-pressed="true"` por defecto), `#an-custom` **`hidden`** (no `disabled`) con `#an-from`, `#an-to` y `#an-apply`, `#an-range-error` oculto, y `#an-export-json` ("Exportar JSON") a la derecha.
    - **Fila 1 (3+3+3+3):** `#an-cards` con `aria-live="polite"` y cuatro tarjetas de 96px, cada una con **un** numero heroe de 30px/700 en `text-slate-200` y su apoyo de 12px. Etiquetas exactas: "Personas en el rango", "Frente al periodo anterior", "Hora punta", "Conocidas / desconocidas". Nada de tarjetas con fondo verde/azul/ambar: son legado de la Fase 5 y el UI-SPEC las declara no replicables.
    - **Fila 2 (8+4):** `#an-panel-hourly` y `#an-panel-occupancy`. Cabecera de panel con titulo 14px/600, y en el de horas ademas `#an-hourly-peak`, el chip `#an-compare` ("Comparar", activo) y la leyenda propia de dos muestras de 12x2px (linea solida azul `#60a5fa` / discontinua gris `#475569`) con etiqueta de 12px — la leyenda de Chart.js va desactivada. Cada `<canvas>` dentro de un contenedor con **`height: 240px` fijo** y `role="img"` en el canvas.
    - **Fila 3 (7+5):** `#an-panel-heatmap` (con `#an-heatmap-scope` = chip `.mono` 12px "acumulado desde el arranque" y su `title="El mapa de calor acumula actividad desde que arrancó la cámara y no sigue el rango seleccionado."`, el `<img>` con `loading="lazy"` y el `alt` del contrato, y `#an-heatmap-legend`: barra de 8px de alto con el degradado INFERNO en CSS y tres marcas `.mono` 12px) y `#an-panel-persons` con `#an-rank-list`.
    - **Pie:** `#an-updated`.

    **Estados de cada panel**, todos presentes en el marcado y ocultos con `hidden`, con las cadenas EXACTAS de la tabla de copy del UI-SPEC:
    - cargando: `<div class="chart-skeleton">` con el alto final del panel, **sin texto y sin spinner**
    - vacio del panel 1: "Sin actividad en este rango" / "Prueba con un rango más amplio o comprueba que la cámara estuvo grabando esos días."
    - vacio del panel 2: "Sin zonas definidas" / "Dibuja una zona sobre el vídeo para medir su ocupación."
    - heatmap sin actividad: "Sin actividad acumulada" / "El mapa aparecerá en cuanto la cámara registre movimiento."
    - heatmap sin senal: "Cámara sin señal" / "El mapa de calor necesita el vídeo en directo para dibujarse sobre el encuadre."
    - vacio del panel 4: "Sin personas identificadas" / "Marca a alguien como persona en la línea temporal y aparecerá aquí."
    - error de cualquier panel: "No se pudieron cargar los datos." + boton `.filter-chip` "Reintentar carga"

    **`frontend/css/components.css`** — cinco clases nuevas al final del fichero, con las medidas del UI-SPEC:
    ```css
    /* Fase 31 — vista de analitica */
    .nav-tab { height: 32px; padding: 8px 16px; border-bottom: 2px solid transparent; color: #94a3b8; }
    .nav-tab.active { color: #3b82f6; border-bottom-color: #3b82f6; }
    .nav-tab:focus-visible { outline: 2px solid #3b82f6; outline-offset: 2px; }
    .analytics-panel { background: #0f172a; border: 1px solid #1e293b; border-radius: 8px; padding: 16px; }
    .range-seg { display: flex; align-items: center; gap: 4px; }
    .rank-row { height: 40px; display: flex; align-items: center; gap: 8px; background: #1e293b; border-radius: 6px; padding: 4px 8px; }
    .chart-skeleton { background: #1e293b; border-radius: 6px; animation: none; }
    ```
    Ajusta valores solo si chocan con una regla ya existente; **no** anadas tamanos de fuente fuera de 12/14/16/30px ni pesos fuera de 600/700, y **no** metas color saturado fuera del acento azul, de la paleta de series (dentro del lienzo) y del degradado del heatmap.
  </action>
  <verify>
    <automated>cd "F:\Documentos\IA\Proyecto_Camara" && .venv/Scripts/python.exe -m pytest tests/test_frontend_modules.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Los 40 ids del bloque `<interfaces>` existen: `for id in an-subtitle an-range an-range-today an-range-7d an-range-30d an-range-custom an-custom an-from an-to an-apply an-range-error an-export-json an-cards an-total-value an-delta-value an-delta-range an-peak-value an-peak-support an-known-value an-unknown-support an-panel-hourly an-hourly-title an-hourly-peak an-compare an-export-hourly an-chart-hourly an-hourly-loading an-hourly-empty an-hourly-error an-hourly-retry an-panel-occupancy an-chart-occupancy an-occupancy-foot an-export-occupancy an-occupancy-loading an-occupancy-empty an-occupancy-error an-occupancy-retry an-panel-heatmap an-heatmap-scope an-heatmap-img an-heatmap-legend an-heatmap-mark-0 an-heatmap-mark-mid an-heatmap-mark-peak an-heatmap-loading an-heatmap-empty an-heatmap-offline an-panel-persons an-rank-list an-export-persons an-persons-loading an-persons-empty an-persons-error an-persons-retry an-updated; do grep -q "id=\"$id\"" frontend/index.html || echo "FALTA $id"; done` no imprime nada
    - `grep -c "acumulado desde el arranque" frontend/index.html` devuelve `1`
    - `grep -c "Reintentar carga" frontend/index.html` devuelve al menos `3` (paneles 1, 2 y 4)
    - `grep -c "id=\"an-export-heatmap\"" frontend/index.html` devuelve `0` — el heatmap NO lleva boton de exportar
    - `grep -cE '\.nav-tab|\.analytics-panel|\.rank-row|\.range-seg|\.chart-skeleton' frontend/css/components.css` devuelve al menos `5`
    - `grep -oE 'text-\[[0-9]+px\]|font-\[[0-9]+\]' frontend/index.html | sort -u` no introduce tamanos fuera de 12/14/16/30
    - `grep -c 'hidden' frontend/index.html` aumenta respecto al valor previo (los estados nacen ocultos)
  </acceptance_criteria>
  <done>La vista de analitica existe completa en el marcado, con todos los ids del contrato, los estados vacios/error/cargando con las cadenas exactas del UI-SPEC, y las cinco clases CSS nuevas.</done>
</task>

<task type="auto">
  <name>Task 3: nav.js y su enganche desde app.js</name>
  <files>frontend/js/nav.js, frontend/js/app.js</files>
  <read_first>
    - frontend/js/app.js completo (~58 lineas) — el bloque `DOMContentLoaded` con los `bind*()` primero y las cargas despues; `initNav()` va con los binds
    - frontend/js/views/timeline.js lineas 1-40 — convencion de modulo del repo (import de `apiFetch`, helper `$`, estado privado del modulo)
    - frontend/js/components/alertCenter.js — patron de trampa de foco y manejo de teclado ya establecido, por si sirve de referencia para las flechas del tablist
    - .planning/phases/31-vista-de-anal-tica/31-RESEARCH.md seccion "Code Examples — Activacion diferida de las graficas" y "Q8"
    - .planning/phases/31-vista-de-anal-tica/31-UI-SPEC.md seccion "Contrato del conmutador" y "Trampa de Chart.js con el contenedor oculto"
  </read_first>
  <action>
    Crear `frontend/js/nav.js` (~70 lineas, tope duro de 300) con exactamente esta API publica:

    ```javascript
    let _boot = null, _resize = null, _booted = false;

    export function registerAnalyticsBoot(bootFn, resizeFn) { _boot = bootFn; _resize = resizeFn; }
    export function activeView() { ... }   // 'operaciones' | 'analitica'
    export function initNav() { ... }
    ```

    Comportamiento, punto por punto del contrato del UI-SPEC:

    1. `activate(view)`:
       ```javascript
       document.getElementById('view-operaciones').hidden = (view !== 'operaciones');
       document.getElementById('view-analitica').hidden   = (view !== 'analitica');
       // aria-selected y tabindex en los dos botones (roving tabindex: 0 el activo, -1 el otro)
       // clase .active solo en la pestana activa
       history.replaceState(null, '', `#${view}`);   // NUNCA location.hash = ...
       if (view !== 'analitica') return;
       if (!_booted && _boot) { _booted = true; _boot(); }   // crea los Chart y pide datos
       else if (_resize) { _resize(); }                       // chart.resize() antes de repintar
       ```
       `history.replaceState` y no `location.hash`: las pestanas no son paginas y no deben llenar el historial.

    2. `initNav()`: resuelve la vista inicial del hash (`#analitica` -> analitica; **cualquier otra cosa, incluido vacio o desconocido, cae en operaciones**), llama a `activate()`, y engancha:
       - `click` en cada `role="tab"`
       - `keydown` en el `[role=tablist]`: `ArrowLeft`/`ArrowRight` cambian de pestana **con activacion automatica** (patron estandar de tabs) y mueven el foco; `Home`/`End` van a la primera/ultima. `Tab` no se intercepta.
       - `hashchange` en `window`, para que pegar una URL funcione.

    3. **Prohibiciones explicitas de D-02, verificables leyendo el modulo:**
       - Nunca `img.src = ''` sobre el MJPEG ni desmontaje de `videoCanvas.js`: su logica de reconexion se disparia sola y al volver a Operaciones el video apareceria "offline".
       - Nunca cerrar ni reabrir el WebSocket: es unico, la linea temporal sigue recibiendo eventos con la vista oculta y la campana sigue actualizando su badge.
       - Ocultar solo con la propiedad `hidden`. No tocar `style.display` a mano.
       - Nada de `.reduce()`/`.sort()`/`.filter()`/`Math.max()` — `nav.js` no toca datos del servidor, asi que no deberia haber ninguno.

    En `frontend/js/app.js`: `import { initNav } from './nav.js';` y llamar a `initNav()` **dentro del bloque de `bind*()`** del `DOMContentLoaded`, junto a `bindAlertCenter()` / `bindMarkPerson()`. No moverlo al bloque de cargas: tiene que existir antes de que nada pinte.
  </action>
  <verify>
    <automated>cd "F:\Documentos\IA\Proyecto_Camara" && node --check frontend/js/nav.js && node --check frontend/js/app.js && .venv/Scripts/python.exe -m pytest tests/test_frontend_modules.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `node --check frontend/js/nav.js` sale con codigo 0
    - `wc -l < frontend/js/nav.js` devuelve menos de 300
    - `grep -c "history.replaceState" frontend/js/nav.js` devuelve al menos `1`
    - `grep -c "location.hash =" frontend/js/nav.js` devuelve `0`
    - `grep -cE "\.reduce\(|\.sort\(|\.filter\(|Math\.max\(" frontend/js/nav.js` devuelve `0`
    - `grep -cE "\.src = ''|\.src = \"\"|close\(\)" frontend/js/nav.js` devuelve `0` (ni MJPEG ni WebSocket se desmontan)
    - `grep -c "export function registerAnalyticsBoot" frontend/js/nav.js` devuelve `1`
    - `grep -c "initNav()" frontend/js/app.js` devuelve `1`
  </acceptance_criteria>
  <done>`nav.js` conmuta las dos vistas con hash, teclado y activacion diferida registrable, y `app.js` lo arranca con el resto de binds.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Checkpoint — la vista de operaciones no se ha movido ni un pixel</name>
  <what-built>
    `<main>` reestructurado en dos `role="tabpanel"`, tablist en la cabecera, marcado completo (y vacio) de la vista de analitica, cinco clases CSS nuevas y `nav.js` cableado desde `app.js`.
  </what-built>
  <how-to-verify>
    Con el servidor en marcha (`.venv/Scripts/python.exe -m uvicorn backend.main:app --host 0.0.0.0 --port 8000`) y el navegador en `http://localhost:8000/`:

    1. **Regresion de la vista de operaciones (lo unico bloqueante).** Todos los paneles de las Fases 29 y 30 estan donde estaban: video a la izquierda, columna derecha con alertas, personas ahora, tarjetas, grafica horaria, ajustes, salud, observabilidad y linea temporal. Las dos columnas siguen a 3/5 y 2/5 en `lg:`. La linea temporal sigue cargando paginas al hacer scroll.
    2. Pulsar **"Analítica"**: aparece la vista nueva (vacia, con esqueletos o estados vacios) y desaparece la de operaciones. La URL pasa a terminar en `#analitica`. La cabecera —estado de camara, reloj, campana— **sigue visible**.
    3. Volver a **"Operaciones"**: el video sigue reproduciendo **sin reconectar** (no aparece el estado "offline" ni un parpadeo del `<img>`), y la consola no muestra ni un cierre de WebSocket.
    4. Recargar con `#analitica` en la URL: abre directamente en Analitica.
    5. Con el foco en una pestana, `←`/`→` cambian de vista; `Home`/`End` van a la primera/ultima.
    6. Consola del navegador **limpia**: ni un error, ni un 404 de modulo.
  </how-to-verify>
  <acceptance_criteria>
    - Ningun panel de operaciones ha cambiado de posicion, tamano ni comportamiento
    - Conmutar de pestana no reconecta el MJPEG ni el WebSocket (comprobable en la pestana Network del navegador: no aparece una peticion nueva a `/video_feed` ni un `/ws` nuevo)
    - La URL refleja la pestana activa y recargar respeta el hash
    - Consola sin errores
  </acceptance_criteria>
  <resume-signal>Escribe "aprobado" o describe lo que se ha roto</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL (hash) -> DOM | El hash es entrada del usuario y decide que vista se activa |
| marcado estatico -> JS de fases anteriores | Reestructurar `<main>` puede romper selectores que las Fases 29/30 dan por buenos |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-07 | Tampering | `location.hash` usado como selector de vista | mitigate | Lista blanca de dos valores (`operaciones`, `analitica`); cualquier otro cae en operaciones. El hash nunca se inyecta en el DOM ni se usa como selector CSS/querySelector |
| T-31-08 | Denial of Service | Conmutar pestana desmontando el MJPEG o el WS | mitigate | `nav.js` solo toca la propiedad `hidden`; criterio de aceptacion con `grep` que exige 0 apariciones de `.src = ''` y de `close()` |
| T-31-09 | Tampering | Reestructuracion de `<main>` que rompa ids consumidos por las Fases 29/30 | mitigate | El contenido de operaciones se envuelve sin editar; el checkpoint de la Task 4 verifica la regresion con el servidor real |
</threat_model>

<verification>
```bash
cd "F:\Documentos\IA\Proyecto_Camara"
node --check frontend/js/nav.js
node --check frontend/js/app.js
.venv/Scripts/python.exe -m pytest tests/test_frontend_modules.py -q
```
`nav.js` **todavia no** se anade a `LOCKED_JS`: los seis modulos de la fase entran juntos en 31-11, cuando todos existen (si se anadieran ahora, `TEST_line_limit` fallaria al buscar modulos que aun no estan en disco).
</verification>

<success_criteria>
- `<main>` es contenedor neutro; las clases de reticula viven en `#view-operaciones`
- Los 40+ ids del contrato existen en `frontend/index.html`
- Las cinco clases nuevas existen en `components.css` con las medidas del UI-SPEC
- `nav.js` pasa `node --check`, cabe en menos de 300 lineas y no desmonta MJPEG ni WebSocket
- La vista de operaciones no ha sufrido ninguna regresion visual ni funcional (checkpoint aprobado)
</success_criteria>

<output>
Al terminar, crear `.planning/phases/31-vista-de-anal-tica/31-03-SUMMARY.md`.
</output>
