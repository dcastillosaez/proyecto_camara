---
phase: 31
plan: 11
type: execute
wave: 7
depends_on: ["31-10"]
files_modified:
  - tests/test_frontend_modules.py
autonomous: false
requirements: [OPS-12, OPS-13, OPS-14, OPS-15]

must_haves:
  truths:
    - "Los seis modulos de la fase estan bajo el contrato mecanico: existen en disco y caben en 300 lineas"
    - "La prohibicion de agregar en el navegador deja de ser una promesa del plan y pasa a ser un test que corre en cada suite"
    - "Los criterios 3 y 4 del ROADMAP quedan medidos con numeros reales, no estimados"
    - "La suite completa esta en verde antes de cerrar la fase"
    - "Un operador puede abrir la vista, cambiar de rango y descargar el CSV sin encontrarse un error"
  artifacts:
    - path: "tests/test_frontend_modules.py"
      provides: "LOCKED_JS ampliado con los 6 modulos + TEST_analytics_no_client_aggregation"
      contains: "TEST_analytics_no_client_aggregation"
  key_links:
    - from: "tests/test_frontend_modules.py::LOCKED_JS"
      to: "frontend/js/nav.js y frontend/js/views/analytics*.js"
      via: "seis entradas nuevas en la lista, que TEST_js_modules_exist y TEST_line_limit recorren"
      pattern: "views/analytics"
    - from: "tests/test_frontend_modules.py::TEST_analytics_no_client_aggregation"
      to: "OPS-14 / D-07"
      via: "barrido de .reduce( .sort( .filter( Math.max( sobre los modulos de analitica"
      pattern: "reduce"
---

<objective>
Puerta de fase: convertir en test permanente la prohibicion de agregar en el navegador, meter los seis modulos nuevos en el contrato mecanico del frontend, medir los criterios 3 y 4 con numeros reales y verificar la vista con el servidor en marcha.

Purpose: los diez planes anteriores dejan la fase construida; este es el que la deja **verificada**. Tres cosas concretas: (1) `TEST_analytics_no_client_aggregation` es lo que impide que dentro de dos fases alguien "arregle" un panel con un `.sort()` y nadie se entere —el criterio de OPS-14 pasa de ser una revision del planificador a una barrera del CI—; (2) los criterios 3 y 4 son los mas exigentes del ROADMAP y el CONTEXT.md pide explicitamente que se midan con datos sembrados, no que se estimen; (3) el checkpoint visual es la unica forma de comprobar que las dos graficas se pintan de verdad al activar la pestana, que es justo la trampa de Chart.js que D-03 intenta evitar.
Output: fase 31 cerrada, con OPS-12..OPS-15 verificados y los numeros escritos en el SUMMARY.

Este plan **no** anade funcionalidad. Si algo falta, sale de aqui como gap y se planifica; no se improvisa dentro de la puerta.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/31-vista-de-anal-tica/31-CONTEXT.md
@.planning/phases/31-vista-de-anal-tica/31-RESEARCH.md
@.planning/phases/31-vista-de-anal-tica/31-UI-SPEC.md
@CLAUDE.md
</context>

<interfaces>
Los seis modulos que entran juntos en `LOCKED_JS` (D-14), en este orden y con estas rutas
relativas a `frontend/js/`:

```python
    # Fase 31: conmutador de vistas y modulos de la vista de analitica.
    "nav.js",
    "views/analytics.js",
    "views/analytics-charts.js",
    "views/analytics-range.js",
    "views/analytics-ranking.js",
    "views/analytics-export.js",
```

Criterios del ROADMAP que este plan tiene que dejar medidos, con el comando que los mide:

| Criterio | Comando | Umbral | Medido en el research |
|---|---|---|---|
| 3 — payload de 30 dias | `pytest tests/test_analytics_api.py -k payload_size -q` | < 100 KB | 0,6-11,3 KB segun cubo |
| 4 — agregaciones @100k | `pytest tests/test_repositories.py -k analytics_budget -q` | < 500 ms | 14-78 ms con el indice |
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: LOCKED_JS + TEST_analytics_no_client_aggregation</name>
  <files>tests/test_frontend_modules.py</files>
  <read_first>
    - tests/test_frontend_modules.py COMPLETO (106 lineas) — el docstring de cabecera (que ya explica como la Fase 30 amplio el contrato: se le anade el parrafo equivalente de la Fase 31), la lista `LOCKED_JS`, `LINE_LIMIT = 300`, y los tests `TEST_js_modules_exist` / `TEST_line_limit` que la recorren
    - Los seis ficheros que entran, para comprobar antes de tocar el test que existen todos en disco: `frontend/js/nav.js` y los cinco `frontend/js/views/analytics*.js`
    - .planning/phases/31-vista-de-anal-tica/31-CONTEXT.md D-07 y D-14
    - .planning/phases/31-vista-de-anal-tica/31-RESEARCH.md "Pitfall 10" y la fila de OPS-14 del mapa requisito -> test
  </read_first>
  <action>
    **1) Ampliar el docstring del fichero** con un parrafo de la Fase 31, en el mismo tono que el que dejo la Fase 30: los seis modulos de la vista de analitica entran en el contrato mecanico, y ademas se anade una comprobacion nueva —la prohibicion de agregar en el navegador— que no es mecanica sino de politica, y por que se verifica con `grep` (D-07 la escribio explicitamente como "verificable con grep" para que fuera auditable sin framework de test JS).

    **2) Anadir las seis entradas a `LOCKED_JS`** con su comentario, tal cual el bloque `<interfaces>`.

    **3) Test nuevo:**
    ```python
    ANALYTICS_JS = ["nav.js", "views/analytics.js", "views/analytics-charts.js",
                    "views/analytics-range.js", "views/analytics-ranking.js",
                    "views/analytics-export.js"]

    # OPS-14 / D-07: las agregaciones se calculan en la base de datos, no en el navegador.
    # El porcentaje de variacion, la hora pico, el orden del ranking y el orden de las zonas
    # llegan ya resueltos del servidor; en el cliente solo se permite aritmetica de FORMATO
    # (separador de millares, simbolo de porcentaje, padStart de horas).
    _FORBIDDEN = re.compile(r"\.reduce\(|\.sort\(|\.filter\(|Math\.max\(|Math\.min\(")


    def TEST_analytics_no_client_aggregation():
        offenders = []
        for rel in ANALYTICS_JS:
            path = FRONTEND / "js" / rel
            for i, line in enumerate(path.read_text(encoding="utf-8").splitlines(), 1):
                if _FORBIDDEN.search(line):
                    offenders.append(f"{rel}:{i}: {line.strip()}")
        assert not offenders, (
            "OPS-14: agregacion en el navegador. Estos datos los resuelve el servidor "
            f"(ver 31-05: peak_index, chart, has_previous, truncated):\n" + "\n".join(offenders))
    ```
    El mensaje del assert **dice donde esta el dato ya resuelto**, no solo que la linea esta
    prohibida: quien se lo encuentre dentro de seis meses necesita saber cual es la alternativa,
    o lo unico que hara sera renombrar la llamada para esquivar la expresion regular.

    **Si algun modulo incumple**, la respuesta es corregir el modulo, **nunca** relajar la
    expresion regular ni anadir una excepcion por fichero. Si el incumplimiento fuera real e
    inevitable, es un gap de la fase y sale por el informe, no por una lista de exclusiones.
  </action>
  <verify>
    <automated>cd "F:\Documentos\IA\Proyecto_Camara" && .venv/Scripts/python.exe -m pytest tests/test_frontend_modules.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "views/analytics" tests/test_frontend_modules.py` es al menos `10` (5 modulos en `LOCKED_JS` + 5 en `ANALYTICS_JS`)
    - `grep -c '"nav.js"' tests/test_frontend_modules.py` devuelve `2`
    - `grep -c "TEST_analytics_no_client_aggregation" tests/test_frontend_modules.py` devuelve `1`
    - `.venv/Scripts/python.exe -m pytest tests/test_frontend_modules.py -q` en verde, incluidos `TEST_js_modules_exist`, `TEST_line_limit` y el test nuevo
    - `.venv/Scripts/python.exe -m pytest tests/test_frontend_modules.py -k no_client_aggregation -q` selecciona exactamente 1 test y pasa
  </acceptance_criteria>
  <done>Los seis modulos estan en `LOCKED_JS` y existe un test permanente que falla si alguien agrega datos del servidor en el navegador.</done>
</task>

<task type="auto">
  <name>Task 2: Medir los criterios 3 y 4 con numeros reales y pasar la suite completa</name>
  <files>tests/test_frontend_modules.py</files>
  <read_first>
    - .planning/phases/31-vista-de-anal-tica/31-05-SUMMARY.md y 31-09-SUMMARY.md — los pesos en bytes que anotaron los tests de payload
    - .planning/phases/31-vista-de-anal-tica/31-01-SUMMARY.md — la nota sobre el `COUNT(*)` filtrado de la linea temporal (563 ms @100k), que este plan tiene que trasladar al SUMMARY de la fase
    - .planning/ROADMAP.md seccion "Phase 31" — los cinco criterios de exito, literales
    - .planning/STATE.md — el formato con el que las fases anteriores dejaron escritos sus checkpoints diferidos
    - CLAUDE.md seccion "Tests" — cuando es obligatoria la suite completa (esta fase toca migraciones, API y frontend: los tres disparadores)
  </read_first>
  <action>
    **Este es un trabajo de medicion y de acta, no de codigo.** No se modifica ningun fichero de
    produccion.

    **1) Criterio 4 — agregaciones sobre 100.000 eventos.** Ejecutar en modo verboso para leer los
    tiempos reales, no solo el verde:
    ```bash
    .venv/Scripts/python.exe -m pytest tests/test_repositories.py -k analytics_budget -q -s
    ```
    Anotar el tiempo de cada una de las cuatro agregaciones. Si alguna pasa de 500 ms, **no se
    sube el presupuesto**: es un gap y sale por el informe.

    **2) Criterio 3 — peso del payload.** Ejecutar los tests de peso y, ademas, capturar el numero
    exacto con una medicion directa contra la app ASGI (rango de 30 dias y rango de 7 dias, que es
    el que mas cubos produce):
    ```bash
    .venv/Scripts/python.exe -m pytest tests/test_analytics_api.py -k "payload_size or export_json" -q -s
    ```
    Anotar los bytes de `/hourly` a 30 dias, de `/hourly` a 7 dias y del JSON completo.

    **3) Suite completa** (obligatoria: la fase toca migraciones, API, pipeline y frontend):
    ```bash
    .venv/Scripts/python.exe -m pytest tests/ -q
    ```
    Anotar el recuento final (`N passed, M skipped`) y compararlo con el que dejo la Fase 30 en
    STATE.md (607 passed / 2 skipped): la cuenta debe haber **subido**, no bajado.

    **4) Comprobacion de migracion sobre una base real ya existente**, que ningun test unitario
    cubre porque todos parten de bases temporales: copiar `data/events.db` a un fichero aparte,
    arrancar la app apuntando a la copia y verificar que `schema_version` pasa a `4`, que
    `idx_events_analytics` aparece en `sqlite_master` y que el recuento de filas de `events` es el
    mismo antes y despues. Si `data/events.db` no existe en este entorno, dejarlo escrito como no
    aplicable en vez de inventar el resultado.

    **5) Escribir el acta en el SUMMARY** con: los cuatro tiempos del criterio 4, los tres pesos
    del criterio 3, el recuento de la suite, el resultado de la migracion sobre base real y —punto
    heredado de 31-01— la nota de que el `COUNT(*)` filtrado de la linea temporal cuesta **563 ms
    @100k**, que **no** es una regresion de esta fase (el criterio de la Fase 30 se midio a 10.000
    eventos) y que `idx_events_analytics` **no lo arregla** porque no contiene `severity`. Queda
    anotado para una revision futura del rendimiento de la linea temporal.
  </action>
  <verify>
    <automated>cd "F:\Documentos\IA\Proyecto_Camara" && .venv/Scripts/python.exe -m pytest tests/ -q</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/Scripts/python.exe -m pytest tests/ -q` termina en verde, con mas tests que los 607 de la Fase 30 y sin `failed` ni `error`
    - `.venv/Scripts/python.exe -m pytest tests/test_repositories.py -k analytics_budget -q` en verde y sin `skipped`
    - `.venv/Scripts/python.exe -m pytest tests/test_analytics_api.py -q` en verde
    - `.venv/Scripts/python.exe -m pytest tests/test_migrations.py tests/test_architecture.py tests/test_security_regression.py -q` en verde
    - El SUMMARY contiene los cuatro tiempos del criterio 4 y los tres pesos del criterio 3 **como cifras concretas**, no como "cumple" ni "dentro del presupuesto"
  </acceptance_criteria>
  <done>Los criterios 3 y 4 estan medidos con numeros reales y escritos, y la suite completa esta en verde.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Checkpoint — la vista de analitica funciona con el servidor real</name>
  <what-built>
    La vista completa: pestana Analitica, selector de rango con cuatro presets y personalizado,
    cuatro tarjetas de tendencia, grafica de personas por hora/dia con superposicion del periodo
    anterior, ocupacion por zona, mapa de calor con leyenda y ranking de personas; exportacion CSV
    por panel y JSON del rango; siete endpoints v2 nuevos y el indice de analitica migrado.
  </what-built>
  <how-to-verify>
    Con el servidor en marcha (`.venv/Scripts/python.exe -m uvicorn backend.main:app --host 0.0.0.0 --port 8000`)
    y el navegador en `http://localhost:8000/#analitica`:

    1. **Arranque directo en la pestana.** La vista abre en Analitica y **pinta**: las dos graficas
       aparecen con su tamano correcto, no borrosas ni de 300x150. Es la trampa de Chart.js con el
       contenedor oculto (D-03); si algo se hizo mal, se ve aqui y en ningun otro sitio.
    2. **Rango.** Pulsar "Hoy", "7 días" y "30 días": el subtitulo cambia a las fechas efectivas, la
       grafica pasa de barras a linea en "7 días" (168 cubos) y vuelve a barras en "30 días" (30
       cubos diarios), y el titulo del panel cambia a "Personas por día".
    3. **Personalizado.** Revela los dos campos de fecha; teclear en ellos **no dispara nada**; con
       "Hasta" anterior a "Desde" y pulsando "Aplicar rango" sale "La fecha «Hasta» debe ser
       posterior a «Desde»."; con mas de 90 dias sale "El rango máximo es de 90 días."
    4. **Independencia de paneles.** En la pestana Red del navegador, las cuatro peticiones salen
       **a la vez**, no en cadena. Si se para la camara, el panel del heatmap muestra "Cámara sin
       señal" y **los otros tres siguen pintados**.
    5. **Exportacion.** Los tres botones de CSV descargan un fichero con nombre
       `analitica-{panel}-{fechas}.csv`; abrirlo y comprobar que los acentos se ven bien. "Exportar
       JSON" descarga un fichero con las cuatro secciones. Lo descargado coincide con lo que se ve.
    6. **Conmutacion.** Volver a Operaciones y regresar a Analitica: las graficas se redimensionan
       correctamente, el video **no se ha reconectado** y no hay ningun WebSocket nuevo.
    7. **Consola limpia** y `Tab` recorriendo la vista con anillo de foco azul visible.

    **Lo que exige camara real y puede diferirse** (mismo criterio que los 11 checkpoints ya
    diferidos en STATE.md): que el mapa de calor pinte actividad acumulada real sobre el encuadre,
    que la leyenda muestre un pico distinto de cero y que el ranking muestre personas reconocidas
    de verdad. Si no hay camara en esta sesion, verificar todo lo demas, dejarlo escrito en el
    SUMMARY como 12º checkpoint manual pendiente **y no bloquear el cierre de la fase**.
  </how-to-verify>
  <acceptance_criteria>
    - Las dos graficas se pintan al abrir directamente en `#analitica`, con tamano correcto
    - Los cuatro presets y el rango personalizado funcionan, con las dos cadenas de error exactas
    - Las cuatro peticiones salen en paralelo y un panel caido no arrastra a los demas
    - Los cuatro ficheros exportados se descargan, con acentos correctos y contenido coherente con la pantalla
    - Conmutar de pestana no reconecta el MJPEG ni el WebSocket
    - Consola sin errores
    - Lo que exija camara real queda escrito como checkpoint diferido, no dado por bueno
  </acceptance_criteria>
  <resume-signal>Escribe "aprobado" o describe lo que se ha roto</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| suite de tests -> superficie publica | La puerta es lo ultimo que mira la fase entera antes de darla por buena |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-38 | Repudiation | Cerrar la fase con los criterios 3 y 4 "estimados" en vez de medidos | mitigate | Criterio de aceptacion que exige cifras concretas en el SUMMARY, y dos comandos que las producen |
| T-31-39 | Tampering | Relajar `TEST_analytics_no_client_aggregation` para que pase | mitigate | Escrito en el plan: si un modulo incumple, se corrige el modulo; ninguna excepcion por fichero ni expresion regular mas laxa |
| T-31-40 | Denial of Service | La migracion v4 sobre la base real del usuario al arrancar | mitigate | Verificacion explicita sobre una **copia** de `data/events.db`, comparando recuento de filas antes y despues; `run_migrations()` ademas hace su backup |
</threat_model>

<verification>
```bash
cd "F:\Documentos\IA\Proyecto_Camara"
.venv/Scripts/python.exe -m pytest tests/ -q
.venv/Scripts/python.exe -m pytest tests/test_analytics_api.py tests/test_repositories.py tests/test_migrations.py tests/test_frontend_modules.py -q
```
El segundo es el barrido de merge de ola que fija el research; el primero es obligatorio porque
la fase toca migraciones, API, pipeline y frontend a la vez.
</verification>

<success_criteria>
- Los seis modulos de la fase estan en `LOCKED_JS` y pasan el limite de 300 lineas
- `TEST_analytics_no_client_aggregation` existe, pasa, y su mensaje explica la alternativa
- Criterio 3 medido: peso real del payload de 30 y de 7 dias, y del JSON completo
- Criterio 4 medido: los cuatro tiempos reales sobre 100.000 eventos sembrados con identidad y zona
- Suite completa en verde, con mas tests que los 607 con los que cerro la Fase 30
- Migracion v3 -> v4 verificada sobre una copia de la base real (o marcada como no aplicable)
- Checkpoint visual aprobado, con lo que exija camara real escrito como diferido
- OPS-12, OPS-13, OPS-14 y OPS-15 verificados uno a uno en el SUMMARY
</success_criteria>

<output>
Al terminar, crear `.planning/phases/31-vista-de-anal-tica/31-11-SUMMARY.md` con el acta de la
fase: los cuatro tiempos del criterio 4, los tres pesos del criterio 3, el recuento de la suite,
el resultado de la migracion sobre base real, la nota del `COUNT(*)` de la linea temporal
heredada de 31-01, y el estado del checkpoint visual (aprobado / parcialmente diferido).
</output>
