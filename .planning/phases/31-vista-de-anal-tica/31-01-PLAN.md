---
phase: 31
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/seed_events.py
  - backend/storage/models.py
  - backend/storage/migrations.py
  - tests/test_migrations.py
  - tests/test_repositories.py
autonomous: true
requirements: [OPS-12, OPS-14]

must_haves:
  truths:
    - "Una base marcada como v3 pasa a v4 al arrancar y gana el indice idx_events_analytics sin perder ni una fila"
    - "Una base nueva creada con create_all() ya trae idx_events_analytics sin pasar por la migracion"
    - "scripts/seed_events.py puede sembrar person_id y zone_id reales, no solo NULL"
    - "Sembrar con los valores por defecto sigue produciendo exactamente las mismas filas que hoy"
    - "Si el formato de almacenamiento de DateTime deja de ser ISO de ancho fijo, cae un test y no una grafica"
  artifacts:
    - path: "backend/storage/models.py"
      provides: "Declaracion de idx_events_analytics en Event.__table_args__ (bases nuevas)"
      contains: "idx_events_analytics"
    - path: "backend/storage/migrations.py"
      provides: "SCHEMA_VERSION = 4 y _migrate_v3_to_v4 registrada en MIGRATIONS (bases existentes)"
      contains: "_migrate_v3_to_v4"
    - path: "scripts/seed_events.py"
      provides: "Siembra con identidad y zona para que los tests de ranking y ocupacion midan sobre datos reales"
      contains: "--persons"
    - path: "tests/test_migrations.py"
      provides: "Terna de tests v3->v4: crea el indice, es idempotente, base nueva lo hereda"
      contains: "idx_events_analytics"
    - path: "tests/test_repositories.py"
      provides: "Guarda del formato de fecha y guarda de la siembra con identidad/zona"
      contains: "TEST_datetime_storage_format_is_fixed_width_iso"
  key_links:
    - from: "backend/storage/migrations.py::MIGRATIONS"
      to: "backend/storage/migrations.py::_migrate_v3_to_v4"
      via: "tupla (4, 'indice compuesto de analitica', _migrate_v3_to_v4)"
      pattern: "_migrate_v3_to_v4"
    - from: "backend/storage/migrations.py::_migrate_v3_to_v4"
      to: "app_config.schema_version"
      via: "_record_version(conn, 4) con el literal 4, nunca SCHEMA_VERSION"
      pattern: "_record_version\\(conn, 4\\)"
    - from: "scripts/seed_events.py::seed_events"
      to: "events.person_id / events.zone_id"
      via: "parametros persons/zones con proporciones 35% y 60%"
      pattern: "persons|zones"
---

<objective>
Poner el almacenamiento en condiciones de sostener la vista de analitica: crear el indice compuesto `idx_events_analytics (camera_id, ts, person_id, zone_id, track_id)` con su migracion v3->v4, y extender `scripts/seed_events.py` para que pueda sembrar `person_id` y `zone_id` reales.

Purpose: el criterio 4 del ROADMAP (<500 ms sobre 100.000 eventos) **falla hoy en tres consultas** — el research las midio en 551 ms (ocupacion por zona), 535 ms (conocidas/desconocidas) y 618 ms (personas distintas por hora). Con este indice bajan a 28, 14 y 78 ms. Y la siembra actual deja `person_id` y `zone_id` a `NULL` (`seed_events.py:46-47`), asi que sin extenderla los tests de presupuesto de 31-04 medirian sobre datos vacios y **pasarian por accidente**. Las dos cosas son precondicion dura del resto de la fase: `INDEXED BY idx_events_analytics` del ranking **falla la consulta** si el indice no existe.
Output: indice declarado y migrado, siembra con identidad y zona, y cuatro tests de guarda.

Nota para el registro (fuera de alcance, no tocar aqui): el research midio que el `COUNT(*)` filtrado de la linea temporal de la Fase 30 cuesta **563 ms @100k**. No es una regresion de esta fase (el criterio de la Fase 30 se midio a 10.000 eventos) y `idx_events_analytics` **no lo arregla** porque no contiene `severity`. Se anota para una revision futura del rendimiento de la linea temporal; 31-11 lo deja escrito en el SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-vista-de-anal-tica/31-CONTEXT.md
@.planning/phases/31-vista-de-anal-tica/31-RESEARCH.md
@.planning/phases/31-vista-de-anal-tica/31-PATTERNS.md
@CLAUDE.md
</context>

<interfaces>
Contrato que consumen 31-04 (AnalyticsRepo) y 31-05 (router):

```sql
-- El indice EXACTO. Ni 4 columnas ni 6: la anchura esta medida en
-- 31-RESEARCH.md "Anchura del indice". Sin track_id, la consulta de personas
-- distintas por hora se queda en 590 ms (falla el criterio 4). Con `type`
-- dentro, el ranking sube a 190 ms.
CREATE INDEX IF NOT EXISTS idx_events_analytics
  ON events (camera_id, ts, person_id, zone_id, track_id);
```

```python
# scripts/seed_events.py — firma nueva (aditiva, defaults compatibles)
def seed_events(
    db_path: str,
    n: int = 100_000,
    days: int = 30,
    camera_id: str = "cam1",
    seed: int = 42,
    persons: int = 0,   # 0 = comportamiento actual (person_id siempre NULL)
    zones: int = 0,     # 0 = comportamiento actual (zone_id siempre NULL)
) -> None: ...

# CLI:  .venv/Scripts/python.exe -m scripts.seed_events out.db -n 100000 --days 30 --persons 60 --zones 14
```

Proporciones (las del banco de pruebas del research, para que los tiempos medidos sean reproducibles):
35 % de las filas con `person_id` entre 1 y `persons`; 60 % con `zone_id` en `"zona-1".."zona-{zones}"`.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Sembrar identidad y zona en scripts/seed_events.py sin romper el determinismo actual</name>
  <files>scripts/seed_events.py, tests/test_repositories.py</files>
  <read_first>
    - scripts/seed_events.py completo (76 lineas) — es el fichero que se modifica; fijarse en que `track_id`, `person_id`, `zone_id` y `confidence` se calculan HOY inline dentro del `rows.append(...)`, en ese orden
    - tests/test_repositories.py, el bloque de `TEST_query_performance_100k` (lineas ~574-594) y `_BUDGET_10K_SECS` (~597-628) — quien consume hoy `seed_events()` y depende de su determinismo (`seed=42`), no romperlo
    - .planning/phases/31-vista-de-anal-tica/31-PATTERNS.md seccion "scripts/seed_events.py (utility, extension)"
    - .planning/phases/31-vista-de-anal-tica/31-RESEARCH.md seccion "Wave 0 Gaps" (ultimo punto) y la nota de metodologia bajo "Mediciones — criterios 3 y 4"
  </read_first>
  <action>
    **1) Extraer a variables locales, en el mismo orden en que hoy se consume `rng`.** Dentro del bucle `for _ in range(n):`, antes del `rows.append(...)`, y respetando el orden exacto de llamadas a `rng` que hay hoy (track_id -> confidence):

    ```python
    ts = start + datetime.timedelta(seconds=rng.randint(0, span_seconds))
    track_id = rng.randint(1, 500) if rng.random() < 0.7 else None
    person_id = None
    if persons:
        # Solo consume rng cuando la siembra pide identidad: con persons=0 la
        # secuencia de rng queda BYTE A BYTE como hoy y los tests de la Fase 30
        # (TEST_query_performance_100k, presupuestos @10k) siguen siendo reproducibles.
        person_id = rng.randint(1, persons) if rng.random() < 0.35 else None
    zone_id = None
    if zones:
        zone_id = f"zona-{rng.randint(1, zones)}" if rng.random() < 0.60 else None
    confidence = round(rng.uniform(0.4, 0.99), 2) if rng.random() < 0.8 else None
    rows.append((
        str(uuid.uuid4()), camera_id, rng.choice(_TYPES), ts.isoformat(sep=" "),
        rng.choice(_SEVERITIES), track_id, person_id, zone_id, confidence,
        None, None, None, json.dumps({}),
    ))
    ```

    **CUIDADO con el orden de `rng`**: hoy `rng.choice(_TYPES)` y `rng.choice(_SEVERITIES)` se evaluan DENTRO del `rows.append` y por tanto DESPUES de `ts` pero ANTES de `track_id`/`confidence` (Python evalua los argumentos de izquierda a derecha). Al extraer variables hay que preservar ese orden real: si al ejecutar el test de determinismo (abajo) los ids/ts no coinciden con la version previa, reordena las extracciones hasta que coincidan. La regla es: **con `persons=0, zones=0` la salida debe ser identica a la de hoy**.

    **2) Anadir los dos parametros** a la firma de `seed_events()` con default `0`, documentados en el docstring con las proporciones 35 % / 60 % y la razon (31-RESEARCH: sin ellos los tests de ranking y ocupacion miden sobre datos vacios).

    **3) Anadir al `argparse` del `__main__`**:
    ```python
    parser.add_argument("--persons", type=int, default=0,
                        help="Numero de identidades distintas; 35%% de las filas reciben una")
    parser.add_argument("--zones", type=int, default=0,
                        help="Numero de zonas distintas (zona-1..zona-N); 60%% de las filas reciben una")
    ```
    y propagarlos en la llamada a `seed_events(...)`.

    **4) Dos tests en `tests/test_repositories.py`** (junto a los de rendimiento existentes, mismo estilo de fixture `db`):

    - `TEST_seed_events_populates_persons_and_zones`: siembra 5.000 filas con `persons=60, zones=14` en una base temporal con el esquema creado, y comprueba con sqlite3 directo que la proporcion de `person_id NOT NULL` cae entre 0.30 y 0.40, la de `zone_id NOT NULL` entre 0.55 y 0.65, que `COUNT(DISTINCT person_id) <= 60` y que todos los `zone_id` casan con `^zona-\d+$`.
    - `TEST_seed_events_defaults_leave_person_and_zone_null`: siembra 500 filas sin `persons`/`zones` y comprueba `COUNT(*) WHERE person_id IS NOT NULL == 0` y lo mismo para `zone_id` — es la guarda de compatibilidad con la Fase 30.
  </action>
  <verify>
    <automated>cd "F:\Documentos\IA\Proyecto_Camara" && .venv/Scripts/python.exe -m pytest tests/test_repositories.py -k seed_events -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '"--persons"' scripts/seed_events.py` devuelve `1` y `grep -c '"--zones"' scripts/seed_events.py` devuelve `1`
    - `grep -n "persons: int = 0" scripts/seed_events.py` encuentra la firma nueva
    - `grep -n 'f"zona-{' scripts/seed_events.py` encuentra la generacion de zonas
    - `.venv/Scripts/python.exe -m pytest tests/test_repositories.py -k seed_events -q` sale en verde con 2 tests
    - `.venv/Scripts/python.exe -m pytest tests/test_repositories.py -k performance -q` sigue en verde (determinismo intacto)
  </acceptance_criteria>
  <done>`seed_events()` acepta `persons`/`zones`, los expone por CLI, y con los defaults produce exactamente las mismas filas que antes del cambio.</done>
</task>

<task type="auto">
  <name>Task 2: idx_events_analytics en models.py y migracion v3 -> v4</name>
  <files>backend/storage/models.py, backend/storage/migrations.py, tests/test_migrations.py</files>
  <read_first>
    - backend/storage/models.py lineas 100-115 — el bloque `Event.__table_args__` completo, con los cinco `Index()` actuales
    - backend/storage/migrations.py completo — en particular `SCHEMA_VERSION` (linea ~25), el docstring de `_record_version` (lineas ~91-99, que avisa por escrito del bug de usar la constante), `_migrate_v2_to_v3` (lineas ~167-176, molde literal a copiar), la lista `MIGRATIONS` (~180-183) y `run_migrations` (~186-205, con el `return` temprano cuando `current >= SCHEMA_VERSION`)
    - tests/test_migrations.py — el helper `make_v2_db()` (lineas ~69-87, incluido su docstring que explica por que el DROP es deliberado) y la terna `TEST_migration_v3_creates_timeline_index` / `TEST_migration_v3_is_idempotent` / `TEST_fresh_db_has_timeline_index` (~242-272), que se replica cambiando el nombre del indice y la version
    - .planning/phases/31-vista-de-anal-tica/31-RESEARCH.md secciones "Q3", "Pitfall 2" y "Pitfall 3"
  </read_first>
  <action>
    **1) `backend/storage/models.py`** — anadir un sexto `Index` al final de `Event.__table_args__`, dejando los cinco existentes intactos:

    ```python
        # Fase 31 (OPS-12/OPS-14): covering index de analitica. Anchura de 5 columnas
        # MEDIDA (31-RESEARCH.md "Anchura del indice"): sin track_id, personas distintas
        # por hora se queda en 590 ms; con `type` dentro, el ranking sube a 190 ms.
        # Medido @100k: ocupacion 551->28 ms, conocidas/desconocidas 535->14 ms,
        # personas distintas por hora 618->78 ms.
        Index("idx_events_analytics", "camera_id", "ts", "person_id", "zone_id", "track_id"),
    ```

    **2) `backend/storage/migrations.py`** — tres cambios:

    - `SCHEMA_VERSION = 4` (era 3).
    - Funcion nueva **justo despues** de `_migrate_v2_to_v3`, copiando su molde linea a linea:
    ```python
    def _migrate_v3_to_v4(conn: Connection) -> None:
        """Indice compuesto de analitica (Fase 31, OPS-12/OPS-14).

        Medido @100k: ocupacion por zona 551 -> 28 ms, conocidas/desconocidas
        535 -> 14 ms, personas distintas por hora 618 -> 78 ms. Las tres estaban
        por encima del presupuesto de 500 ms del criterio 4. create_all() no crea
        indices sobre tablas que ya existen, por eso va explicito. CREATE INDEX
        sobre 102.000 filas tarda 196 ms. No toca filas ni columnas.
        """
        conn.execute(text(
            "CREATE INDEX IF NOT EXISTS idx_events_analytics "
            "ON events (camera_id, ts, person_id, zone_id, track_id)"))
        _record_version(conn, 4)
    ```
    - Registrar en `MIGRATIONS`: `(4, "indice compuesto de analitica", _migrate_v3_to_v4),`

    **PROHIBIDO** escribir `_record_version(conn, SCHEMA_VERSION)`. Tiene que ser el **literal `4`**. Es el bug que la Fase 30 ya cometio y corrigio (`30-02-SUMMARY.md`) y que el docstring de `_record_version` advierte con nombre y apellidos: con la constante, una futura migracion v5 dejaria bases marcadas como v5 sin haberse ejecutado.

    **3) `tests/test_migrations.py`** — helper nuevo y terna de tests:

    - `make_v3_db(db_path)`: analogo a `make_v2_db()`. Crea la base con `create_all()`, ejecuta `DROP INDEX IF EXISTS idx_events_analytics` (deliberado: `create_all()` ya lo declara, y el test necesita partir de una base SIN el indice para comprobar que la migracion lo crea) y fuerza `schema_version = 3` en `app_config`.
    - `TEST_migration_v4_creates_analytics_index`: parte de `make_v3_db`, comprueba que el indice NO esta, ejecuta `run_migrations(engine)`, comprueba que `idx_events_analytics` aparece en `sqlite_master` y que `_schema_version(engine) == 4`.
    - `TEST_migration_v4_is_idempotent`: `run_migrations()` dos veces seguidas sin excepcion, mismo assert final.
    - `TEST_fresh_db_has_analytics_index`: base creada solo con `create_all()` (sin migrar) ya tiene el indice, heredado de `Event.__table_args__`.
  </action>
  <verify>
    <automated>cd "F:\Documentos\IA\Proyecto_Camara" && .venv/Scripts/python.exe -m pytest tests/test_migrations.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "SCHEMA_VERSION = 4" backend/storage/migrations.py` encuentra exactamente 1 linea
    - `grep -n "_record_version(conn, 4)" backend/storage/migrations.py` encuentra exactamente 1 linea
    - `grep -c "_record_version(conn, SCHEMA_VERSION)" backend/storage/migrations.py` devuelve `0`
    - `grep -n 'indice compuesto de analitica", _migrate_v3_to_v4' backend/storage/migrations.py` encuentra el registro en MIGRATIONS
    - `grep -n "idx_events_analytics" backend/storage/models.py` encuentra la declaracion con las 5 columnas en el orden `camera_id, ts, person_id, zone_id, track_id`
    - `.venv/Scripts/python.exe -m pytest tests/test_migrations.py -q` en verde, con los tres tests nuevos `-k v4` o `-k analytics_index` incluidos
  </acceptance_criteria>
  <done>Una base v3 migra a v4 creando el indice, la migracion es idempotente, y una base nueva ya lo trae de `create_all()`.</done>
</task>

<task type="auto">
  <name>Task 3: Guarda del formato de almacenamiento de DateTime (precondicion de substr)</name>
  <files>tests/test_repositories.py</files>
  <read_first>
    - tests/test_repositories.py — cabecera de imports y la fixture `db` que usan los tests de rendimiento
    - backend/storage/models.py — la columna `Event.ts` y su tipo
    - .planning/phases/31-vista-de-anal-tica/31-RESEARCH.md seccion "Pattern 2" y "Pitfall 1" (con el resultado real de la inspeccion: `typeof(ts) = 'text'`, formato `'2026-08-22 09:05:03.123456'`)
  </read_first>
  <action>
    Todas las agregaciones de 31-04 usan `substr(ts,1,13)` y `substr(ts,1,10)` en vez de `strftime(...)` porque es **2,3x mas rapido** (51,8 ms frente a 120,8 ms sobre 100.000 filas). Eso convierte una funcion semantica en una sintactica: si el formato de almacenamiento cambiara, el `GROUP BY` agruparia mal **sin lanzar ningun error** y las graficas saldrian con cubos raros sin que nadie supiera por que.

    Anadir `TEST_datetime_storage_format_is_fixed_width_iso` en `tests/test_repositories.py`:

    1. Insertar **por el ORM real** (no por sqlite3 crudo: lo que se prueba es como serializa SQLAlchemy) dos eventos, uno con microsegundos distintos de cero (`datetime(2026, 8, 22, 9, 5, 3, 123456)`) y otro con microsegundos exactamente cero (`datetime(2026, 8, 22, 9, 5, 3, 0)`) — el segundo es el caso que podria salir corto si el dialecto recortara ceros.
    2. Leer el valor **crudo** con sqlite3 directo: `SELECT typeof(ts), ts, substr(ts,1,13), substr(ts,1,10) FROM events`.
    3. Aserciones:
    ```python
    assert typeof_ts == "text"
    assert re.fullmatch(r"\d{4}-\d{2}-\d{2} \d{2}:\d{2}:\d{2}\.\d{6}", raw_ts), raw_ts
    assert substr13 == "2026-08-22 09"
    assert substr10 == "2026-08-22"
    ```
    4. Docstring del test explicando que es la precondicion de `AnalyticsRepo._bucket_expr()` y que si cae hay que volver a `strftime` (120 ms, sigue dentro de los 500 ms del criterio 4) de forma **explicita**, no por descuido.
  </action>
  <verify>
    <automated>cd "F:\Documentos\IA\Proyecto_Camara" && .venv/Scripts/python.exe -m pytest tests/test_repositories.py -k storage_format -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "TEST_datetime_storage_format_is_fixed_width_iso" tests/test_repositories.py` encuentra el test
    - `grep -n 'typeof(ts)' tests/test_repositories.py` confirma que se lee el tipo crudo, no el valor convertido por el ORM
    - `.venv/Scripts/python.exe -m pytest tests/test_repositories.py -k storage_format -q` en verde
  </acceptance_criteria>
  <done>Existe un test que cae si el formato de almacenamiento de `Event.ts` deja de ser TEXT ISO de ancho fijo.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| disco -> proceso (arranque) | `run_migrations()` altera el esquema de la base de produccion del usuario al arrancar |
| script CLI -> base de datos | `scripts/seed_events.py` escribe filas masivamente; solo se usa en desarrollo y tests |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-01 | Denial of Service | `_migrate_v3_to_v4` sobre la base real del usuario | mitigate | `CREATE INDEX IF NOT EXISTS` es no destructivo y tarda 196 ms medidos sobre 102.000 filas; `run_migrations()` ya hace `_backup_db()` antes de tocar nada |
| T-31-02 | Tampering | `_record_version` con la constante en vez del literal | mitigate | `_record_version(conn, 4)` literal + criterio de aceptacion con `grep -c` que exige 0 apariciones de `_record_version(conn, SCHEMA_VERSION)` |
| T-31-03 | Tampering | `seed_events.py` ejecutado por error contra `data/events.db` | accept | Es un script de desarrollo que exige la ruta de la base como argumento posicional; mismo riesgo que ya asumia la Fase 30 y no se amplia aqui |
</threat_model>

<verification>
```bash
cd "F:\Documentos\IA\Proyecto_Camara"
.venv/Scripts/python.exe -m pytest tests/test_migrations.py -q
.venv/Scripts/python.exe -m pytest tests/test_repositories.py -q
```
Ambos en verde. El segundo incluye los tests de rendimiento de la Fase 30, que **no deben** haberse degradado: el research verifico que con `idx_events_analytics` presente la linea temporal sigue eligiendo `idx_events_ts_id` (primera pagina 0,04 ms, cursor profundo 0,04 ms, sin cambio de plan).
</verification>

<success_criteria>
- `SCHEMA_VERSION == 4` y `MIGRATIONS` tiene tres entradas
- `idx_events_analytics` aparece tanto en `Event.__table_args__` como en `_migrate_v3_to_v4`
- `seed_events(..., persons=60, zones=14)` produce ~35 % de filas con identidad y ~60 % con zona
- `seed_events(...)` con los defaults sigue produciendo `person_id`/`zone_id` a `NULL` en el 100 % de las filas
- `tests/test_migrations.py` y `tests/test_repositories.py` en verde
</success_criteria>

<output>
Al terminar, crear `.planning/phases/31-vista-de-anal-tica/31-01-SUMMARY.md`.
</output>
