---
phase: 28-refactor-del-frontend-a-modulos-es
plan: 03
type: execute
wave: 2
depends_on: [28-01]
files_modified:
  - frontend/js/views/dashboard-ptz.js
  - frontend/js/views/dashboard-observability.js
autonomous: true
requirements: [OPS-01, OPS-02]

must_haves:
  truths:
    - "Los botones PTZ (mover/parar), los atajos de teclado y los presets siguen funcionando identicos, ahora en un modulo ES"
    - "El panel de salud (/api/health) y el de observabilidad Fase 21 (/api/v2/metrics) siguen refrescandose con el mismo polling (30s y 5s)"
  artifacts:
    - path: "frontend/js/views/dashboard-ptz.js"
      provides: "PTZ: steps, move, stop, atajos de teclado, presets (cargar/guardar)"
      exports: ["bindPtzControls", "loadPresets"]
      min_lines: 80
    - path: "frontend/js/views/dashboard-observability.js"
      provides: "Salud del pipeline (Fase 16) + metricas de observabilidad (Fase 21)"
      exports: ["loadHealth", "loadObservability"]
      min_lines: 50
  key_links:
    - from: "frontend/js/views/dashboard-ptz.js"
      to: "frontend/js/views/dashboard.js"
      via: "import { showToast } from './dashboard.js'"
      pattern: "import \\{ showToast \\} from './dashboard.js'"
    - from: "frontend/js/views/dashboard-observability.js"
      to: "backend (/api/health, /api/v2/metrics)"
      via: "fetch directo, sin dependencias de otros modulos nuevos"
      pattern: "fetch\\('/api/health'\\)|fetch\\('/api/v2/metrics'\\)"
---

<objective>
Extraer PTZ (`frontend/index.html:874-985`) a `frontend/js/views/dashboard-ptz.js` y salud +
observabilidad (`frontend/index.html:1955-2023`) a `frontend/js/views/dashboard-observability.js`.

Purpose: ambos son secciones independientes del resto de la fase (PTZ solo depende de `showToast`
del nucleo; observabilidad no depende de nada) — se agrupan en un plan porque ninguno por
separado llega al tamaño minimo de un plan propio (112 y 69 lineas de fuente respectivamente,
segun el recuento verificado de `28-RESEARCH.md`).
Output: 2 modulos ES nuevos, cada uno muy por debajo de 300 lineas.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/28-refactor-del-frontend-a-modulos-es/28-PATTERNS.md

<interfaces>
```javascript
// frontend/js/views/dashboard.js — ya existe desde 28-02, consumido aqui
export function showToast(msg, type = 'info', ms = 3500)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: frontend/js/views/dashboard-ptz.js</name>

  <read_first>
    - `frontend/index.html:874-985` — las 5 subsecciones PTZ completas (steps slider, move, stop +
      listeners + atajos, presets, guardar preset).
    - `.planning/phases/28-refactor-del-frontend-a-modulos-es/28-PATTERNS.md` (seccion
      "frontend/js/views/dashboard-ptz.js") — confirma que `bindPtzControls` agrupa exactamente
      las lineas 912-923 + el listener de `load-presets-btn` (959) + el de `save-preset-btn`
      (963); el listener `input` del slider de pasos (874-878) NO esta en esa lista, se queda
      como efecto de modulo (no depende de orden con otros modulos, igual que los toggles de
      camara en `dashboard.js`).
  </read_first>

  <files>frontend/js/views/dashboard-ptz.js</files>

  <action>
Crear `frontend/js/views/dashboard-ptz.js`:

```javascript
// frontend/js/views/dashboard-ptz.js
import { showToast } from './dashboard.js';

// ── PTZ: steps slider ────────────────────────────────
const stepsInput = document.getElementById('ptz-steps');
const stepsLabel = document.getElementById('ptz-steps-val');
stepsInput.addEventListener('input', () => { stepsLabel.textContent = stepsInput.value; });

// ── PTZ: move ────────────────────────────────────────
async function ptzMove(dir) {
  const steps = parseInt(stepsInput.value, 10);
  const btn   = document.querySelector(`.ptz-btn[data-dir="${dir}"]`);
  if (btn) btn.classList.add('busy');
  try {
    const res = await fetch('/ptz/move', {
      method:  'POST',
      headers: { 'Content-Type': 'application/json' },
      body:    JSON.stringify({ direction: dir, steps }),
    });
    if (!res.ok) {
      const err = await res.json().catch(() => ({ detail: res.statusText }));
      showToast(`PTZ: ${err.detail ?? res.statusText}`, 'error');
    }
  } catch (e) {
    showToast(`PTZ sin respuesta: ${e.message}`, 'error');
  } finally {
    if (btn) btn.classList.remove('busy');
  }
}

// ── PTZ: stop ────────────────────────────────────────
async function ptzStop() {
  try {
    const res = await fetch('/ptz/stop', { method: 'POST' });
    if (res.ok) showToast('Movimiento detenido', 'info');
    else showToast('Error al detener la cámara', 'error');
  } catch {
    showToast('Sin respuesta del servidor PTZ', 'error');
  }
}

// ── PTZ: presets ─────────────────────────────────────
export async function loadPresets() {
  const container = document.getElementById('presets-container');
  container.innerHTML = '<span class="text-xs text-slate-700 italic">Cargando…</span>';
  try {
    const res = await fetch('/ptz/presets');
    if (!res.ok) throw new Error(`HTTP ${res.status}`);
    const data    = await res.json();
    const entries = Object.entries(data ?? {});
    if (!entries.length) {
      container.innerHTML = '<span class="text-xs text-slate-700 italic">Sin presets guardados en la cámara</span>';
      return;
    }
    container.innerHTML = '';
    entries.forEach(([id, name]) => {
      const btn = document.createElement('button');
      btn.className = 'preset-btn';
      btn.textContent = name || `Preset ${id}`;
      btn.setAttribute('aria-label', `Ir al preset: ${name || id}`);
      btn.addEventListener('click', async () => {
        btn.disabled = true; btn.style.opacity = '0.5';
        try {
          const r = await fetch(`/ptz/preset/${id}`, { method: 'POST' });
          if (r.ok) showToast(`Preset "${name || id}" activado`, 'success');
          else showToast('Error al activar preset', 'error');
        } catch { showToast('Sin respuesta del servidor', 'error'); }
        finally { btn.disabled = false; btn.style.opacity = ''; }
      });
      container.appendChild(btn);
    });
  } catch {
    container.innerHTML = '<span class="text-xs text-slate-600 italic">No se pudo conectar con la cámara</span>';
  }
}

export function bindPtzControls() {
  document.querySelectorAll('.ptz-btn[data-dir]').forEach(btn =>
    btn.addEventListener('click', () => ptzMove(btn.dataset.dir))
  );
  document.getElementById('ptz-stop-btn').addEventListener('click', ptzStop);

  document.addEventListener('keydown', e => {
    if (['INPUT', 'TEXTAREA', 'SELECT'].includes(e.target.tagName)) return;
    const dirMap = { ArrowUp:'up', ArrowDown:'down', ArrowLeft:'left', ArrowRight:'right' };
    if (dirMap[e.key]) { e.preventDefault(); ptzMove(dirMap[e.key]); }
    if (e.key === ' ')  { e.preventDefault(); ptzStop(); }
  });

  document.getElementById('load-presets-btn').addEventListener('click', loadPresets);
  loadPresets();

  document.getElementById('save-preset-btn').addEventListener('click', async () => {
    const input = document.getElementById('preset-name-input');
    const name = input.value.trim();
    if (!name) { input.focus(); return; }
    const btn = document.getElementById('save-preset-btn');
    btn.disabled = true; btn.style.opacity = '0.5';
    try {
      const res = await fetch('/ptz/save_preset', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({ name }),
      });
      if (res.ok) {
        showToast(`Preset "${name}" guardado`, 'success');
        input.value = '';
        loadPresets();
      } else {
        const d = await res.json().catch(() => ({}));
        showToast(d.detail ?? 'Error al guardar preset', 'error');
      }
    } catch { showToast('Sin respuesta del servidor', 'error'); }
    finally { btn.disabled = false; btn.style.opacity = ''; }
  });
}
```

**Puntos obligatorios:**
- `loadPresets()` se exporta Y se invoca dentro de `bindPtzControls()` (no a nivel de modulo) —
  `app.js` (28-08) solo llama `bindPtzControls()` una vez, no `loadPresets()` por separado.
- El listener `input` del slider de pasos queda fuera de `bindPtzControls`, a nivel de modulo
  (ejecuta al importar, igual que hoy ejecuta al parsear el script).
  </action>

  <verify>
    <automated>test -f frontend/js/views/dashboard-ptz.js && [ "$(wc -l < frontend/js/views/dashboard-ptz.js)" -le 300 ] && grep -c "^export " frontend/js/views/dashboard-ptz.js</automated>
  </verify>

  <acceptance_criteria>
    - El comando de `<verify>` no falla y `grep -c "^export "` devuelve `2` (`loadPresets`,
      `bindPtzControls`).
    - `grep -n "import { showToast } from './dashboard.js'" frontend/js/views/dashboard-ptz.js` acierta.
    - `grep -c "loadPresets()" frontend/js/views/dashboard-ptz.js` >= 2 (definicion + invocacion
      dentro de `bindPtzControls`).
  </acceptance_criteria>

  <done>`frontend/js/views/dashboard-ptz.js` existe, exporta `bindPtzControls`/`loadPresets`, y
  toda la logica PTZ (mover/parar/atajos/presets) es identica a la original.</done>
</task>

<task type="auto">
  <name>Task 2: frontend/js/views/dashboard-observability.js</name>

  <read_first>
    - `frontend/index.html:1955-2023` — salud (Fase 16) + observabilidad (Fase 21) completas.
      Verificado por lectura directa: ninguna de las 2 funciones llama `showToast` ni `updateStat`
      — este modulo no importa nada de `dashboard.js` (corrige cualquier suposicion de que todos
      los modulos necesitan importar el nucleo).
  </read_first>

  <files>frontend/js/views/dashboard-observability.js</files>

  <action>
Crear `frontend/js/views/dashboard-observability.js`, sin imports:

```javascript
// frontend/js/views/dashboard-observability.js

// ── Phase 16: health metrics ──────────────────────────────────────
function _fmtUptime(secs) {
  const h = Math.floor(secs / 3600), m = Math.floor((secs % 3600) / 60), s = secs % 60;
  if (h > 0) return `${h}h ${m}m`;
  if (m > 0) return `${m}m ${s}s`;
  return `${s}s`;
}

export async function loadHealth() {
  try {
    const res = await fetch('/api/health');
    if (!res.ok) return;
    const d = await res.json();
    document.getElementById('health-cpu').textContent    = d.cpu_percent >= 0 ? `${d.cpu_percent.toFixed(1)}%` : '–';
    document.getElementById('health-ram').textContent    = d.ram_percent >= 0 ? `${d.ram_percent.toFixed(1)}%` : '–';
    document.getElementById('health-fps').textContent    = `${d.fps}`;
    document.getElementById('health-uptime').textContent = _fmtUptime(d.uptime_secs);
    document.getElementById('health-updated').textContent = new Date().toLocaleTimeString();
  } catch {}
}

// ── Fase 21: métricas de observabilidad ───────────────────────────
function _gaugeValue(snapshot, name, labelKey) {
  const series = snapshot.gauges && snapshot.gauges[name];
  if (!series) return null;
  if (labelKey !== undefined) return series[labelKey];
  const values = Object.values(series);
  return values.length ? values[0] : null;
}

function _counterSum(snapshot, name) {
  const series = snapshot.counters && snapshot.counters[name];
  if (!series) return null;
  return Object.values(series).reduce((a, b) => a + b, 0);
}

export async function loadObservability() {
  try {
    const res = await fetch('/api/v2/metrics');
    if (!res.ok) return;
    const d = await res.json();

    const cam = _gaugeValue(d, 'capture_fps', "{'camera': 'cam1'}");
    const det = _gaugeValue(d, 'detection_fps', "{'camera': 'cam1'}");
    const face = _gaugeValue(d, 'face_fps', "{'camera': 'cam1'}");
    const dropped = _counterSum(d, 'frames_dropped_total');
    const recQ = _gaugeValue(d, 'recording_queue_depth');
    const upQ = _gaugeValue(d, 'upload_queue_depth');

    document.getElementById('obs-capture-fps').textContent = cam != null ? `${cam.toFixed(1)} FPS` : '–';
    document.getElementById('obs-detection-fps').textContent = det != null ? `${det.toFixed(1)} FPS` : '–';
    document.getElementById('obs-face-fps').textContent = face != null ? `${face.toFixed(1)} FPS` : '–';
    document.getElementById('obs-dropped').textContent = dropped != null ? `${dropped}` : '–';
    document.getElementById('obs-queues').textContent = (recQ != null && upQ != null) ? `${recQ} / ${upQ}` : '–';

    const pct = d.e2e_percentiles;
    document.getElementById('obs-latency').textContent = pct
      ? `${pct.p50.toFixed(2)}s / ${pct.p95.toFixed(2)}s`
      : '–';

    document.getElementById('obs-updated').textContent = new Date().toLocaleTimeString();
  } catch {}
}
```

**Puntos obligatorios:**
- `loadHealth`/`loadObservability` se exportan pero **no** registran su propio `setInterval` a
  nivel de modulo ni se auto-invocan — `app.js` (28-08) llama cada una una vez y es responsable de
  `setInterval(loadHealth, 30000)`/`setInterval(loadObservability, 5000)`, igual que hoy hacen las
  lineas 1976-1977/2022-2023 pero movidas al orquestador central.
  </action>

  <verify>
    <automated>test -f frontend/js/views/dashboard-observability.js && [ "$(wc -l < frontend/js/views/dashboard-observability.js)" -le 300 ] && grep -c "^export " frontend/js/views/dashboard-observability.js</automated>
  </verify>

  <acceptance_criteria>
    - El comando de `<verify>` no falla y `grep -c "^export "` devuelve `2` (`loadHealth`,
      `loadObservability`).
    - `grep -c "^import" frontend/js/views/dashboard-observability.js` == 0 (sin dependencias de
      otros modulos, verificado contra el codigo real).
    - `grep -n "setInterval" frontend/js/views/dashboard-observability.js` no encuentra nada (el
      `setInterval` se mueve a `app.js`, a diferencia de `dashboard.js` donde `fetchCounts`/etc.
      SI se quedan con su propio `setInterval` — la diferencia es deliberada: esos no estan en la
      lista de `loadX()` movidos, estos si).
  </acceptance_criteria>

  <done>`frontend/js/views/dashboard-observability.js` existe, exporta `loadHealth`/
  `loadObservability` sin `setInterval` propio (lo registra `app.js`), sin imports de otros
  modulos.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Ninguna nueva | PTZ llama a endpoints ya existentes (`/ptz/*`); observabilidad/salud son lecturas de metricas internas, ningun dato de usuario se renderiza sin sanear |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-05 | Tampering | `loadPresets` inserta `name`/`id` del preset (viene de la camara Tapo via backend) en `btn.textContent` | accept | Ya usa `textContent`, no `innerHTML` — no es un patron nuevo, se preserva igual que el original |
| T-28-06 | Denial of Service | `bindPtzControls` registra un listener `keydown` global sobre `document` | accept | Mismo comportamiento que hoy; no introduce superficie nueva, solo cambia de fichero |
</threat_model>

<verification>
```bash
test -f frontend/js/views/dashboard-ptz.js && test -f frontend/js/views/dashboard-observability.js
grep -c "^export " frontend/js/views/dashboard-ptz.js frontend/js/views/dashboard-observability.js
```
</verification>

<success_criteria>
- `dashboard-ptz.js` exporta `bindPtzControls`/`loadPresets`, `dashboard-observability.js` exporta
  `loadHealth`/`loadObservability`; ambos bajo 300 lineas.
- Ningun `setInterval` de red queda huerfano fuera de `app.js` salvo los ya deliberadamente
  dejados en `dashboard.js` (fetchCounts/fetchDetections/tickClock).
</success_criteria>

<output>
Al terminar, crear `.planning/phases/28-refactor-del-frontend-a-modulos-es/28-03-SUMMARY.md`.
</output>
