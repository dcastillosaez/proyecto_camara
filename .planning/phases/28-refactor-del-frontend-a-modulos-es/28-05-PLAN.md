---
phase: 28-refactor-del-frontend-a-modulos-es
plan: 05
type: execute
wave: 2
depends_on: [28-01]
files_modified:
  - frontend/js/components/eventCard.js
  - frontend/js/components/detectionClasses.js
autonomous: true
requirements: [OPS-01, OPS-02]

must_haves:
  truths:
    - "El panel de grabaciones (lista, borrado por rango, estado de subida a Drive) funciona identico a hoy"
    - "El modal de reproduccion de clips abre/cierra igual (click en fila, click fuera, Escape, boton cerrar)"
    - "El panel de clases detectadas (Fase 27-10) sigue guardando/revirtiendo contra GET/PUT /api/v2/detection/classes"
  artifacts:
    - path: "frontend/js/components/eventCard.js"
      provides: "Grabaciones + borrado por rango + modal de reproduccion de clip"
      exports: ["addRecording", "updateRecordingStatus", "loadRecordings", "openClipModal", "bindEventCardControls"]
      min_lines: 100
    - path: "frontend/js/components/detectionClasses.js"
      provides: "Panel de clases detectadas (Fase 27-10)"
      exports: ["loadDetectionClasses", "renderDetectionClasses", "saveDetectionClasses"]
      min_lines: 40
  key_links:
    - from: "frontend/js/components/eventCard.js"
      to: "frontend/js/views/dashboard.js"
      via: "import { showToast } from '../views/dashboard.js'"
      pattern: "import \\{ showToast \\} from '../views/dashboard.js'"
    - from: "frontend/js/components/detectionClasses.js"
      to: "backend (/api/v2/detection/classes, Fase 27-07)"
      via: "GET al cargar, PUT al marcar/desmarcar checkbox, revert en error"
      pattern: "fetch\\('/api/v2/detection/classes'"
---

<objective>
Extraer el panel de grabaciones + modal de clip (`frontend/index.html:1368-1515` + `1858-1894`) a
`frontend/js/components/eventCard.js`, y el panel de clases detectadas de la Fase 27-10
(`frontend/index.html:1791-1856`) a `frontend/js/components/detectionClasses.js`.

Purpose: son 2 componentes CRUD independientes entre si (ninguno importa del otro), agrupados en
un plan por tamaño (187 y 66 lineas de fuente respectivamente).
Output: 2 modulos ES nuevos, cada uno por debajo de 300 lineas.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/28-refactor-del-frontend-a-modulos-es/28-PATTERNS.md

<interfaces>
```javascript
// frontend/js/views/dashboard.js — ya existe desde 28-02, consumido aqui
export function showToast(msg, type = 'info', ms = 3500)
```

<!-- Contrato que este plan produce para 28-07 (websocket.js), consumido via import: -->
```javascript
// frontend/js/components/eventCard.js — exports consumidos por websocket.js (28-07)
export function addRecording(r)
export function updateRecordingStatus(filename, status, gdrive_id)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: frontend/js/components/eventCard.js</name>

  <read_first>
    - `frontend/index.html:1368-1456` — panel de grabaciones completo (`_recRow`, `addRecording`,
      `updateRecordingStatus`, `loadRecordings`).
    - `frontend/index.html:1450` — `document.getElementById('rec-badge').textContent = recs.length;`
      dentro de `loadRecordings`. **Decision deliberada de este plan:** esta linea NO se
      reescribe para usar `setRecBadge` (videoCanvas.js, 28-04) porque el original nunca alterna
      la visibilidad del badge aqui (solo el contador) — es una inconsistencia preexistente
      documentada en `28-PATTERNS.md` que esta fase preserva a proposito para no cambiar
      comportamiento observable (mandato de paridad total de `28-CONTEXT.md`). Si se enrutara por
      `setRecBadge(true, recs.length)`, el badge quedaria visible tras cada poll de 30s aunque no
      haya ninguna grabacion en curso — una regresion funcional real.
    - `frontend/index.html:1457-1516` — borrado de grabaciones por rango.
    - `frontend/index.html:1858-1894` — delegacion de click en `.btn-play-clip` + modal de video +
      cierre por click fuera / boton / `Escape`.
  </read_first>

  <files>frontend/js/components/eventCard.js</files>

  <action>
Crear `frontend/js/components/eventCard.js`:

```javascript
// frontend/js/components/eventCard.js
import { showToast } from '../views/dashboard.js';

// ── Recordings panel ──────────────────────────────────
const STATUS_LABEL = { pending: 'Pendiente', uploading: 'Subiendo…', uploaded: 'Subido', failed: 'Error' };
const STATUS_COLOR = { pending: 'text-amber-400', uploading: 'text-blue-400', uploaded: 'text-green-400', failed: 'text-red-400' };

function _recRow(r) {
  const ts   = r.created_at ? new Date(r.created_at).toLocaleTimeString('es-ES', { hour12: false, hour: '2-digit', minute: '2-digit' }) : '—';
  const st   = r.upload_status ?? 'pending';
  const col  = STATUS_COLOR[st] ?? 'text-slate-400';
  const lbl  = STATUS_LABEL[st] ?? st;
  const row  = document.createElement('div');
  row.className = 'rec-row flex items-center justify-between px-2.5 py-1.5 rounded-xl bg-slate-800 border border-slate-700';
  row.dataset.filename = r.filename;
  // r.filename/r.gdrive_id llegan del backend — se asignan via propiedades
  // del DOM (dataset/href/textContent), nunca interpolados en innerHTML,
  // para que un filename con marcado no se ejecute (CodeQL js/xss).
  row.innerHTML = `
    <div class="flex items-center gap-2 min-w-0">
      <button class="btn-play-clip flex-shrink-0 text-slate-500 hover:text-blue-400 transition-colors cursor-pointer" aria-label="Reproducir clip">
        <svg class="w-3.5 h-3.5" viewBox="0 0 24 24" fill="currentColor"><polygon points="5 3 19 12 5 21 5 3"/></svg>
      </button>
      <span class="rec-name text-xs text-slate-300 truncate max-w-[110px]"></span>
    </div>
    <div class="flex items-center gap-1 flex-shrink-0">
      <span class="rec-status text-xs ${col}"></span>
      <span class="text-slate-600 text-xs ml-1">${ts}</span>
    </div>`;
  row.querySelector('.btn-play-clip').dataset.src = `/clips/${r.filename}`;
  const nameSpan = row.querySelector('.rec-name');
  nameSpan.textContent = r.filename.replace('clip_', '');
  nameSpan.title = r.filename;
  row.querySelector('.rec-status').textContent = lbl;
  if (r.gdrive_id) {
    const link = document.createElement('a');
    link.href = `https://drive.google.com/file/d/${r.gdrive_id}/view`;
    link.target = '_blank'; link.rel = 'noopener';
    link.className = 'text-blue-400 hover:text-blue-300 text-xs ml-1';
    link.setAttribute('aria-label', 'Ver en Drive');
    link.textContent = '↗';
    row.querySelector('.rec-status').insertAdjacentElement('afterend', link);
  }
  return row;
}

export function addRecording(r) {
  const list  = document.getElementById('recordings-list');
  const empty = document.getElementById('recordings-empty');
  if (empty) empty.style.display = 'none';
  list.prepend(_recRow(r));
  while (list.querySelectorAll('.rec-row').length > 20) list.lastChild.remove();
  document.getElementById('recordings-count-badge').textContent = list.querySelectorAll('.rec-row').length;
}

export function updateRecordingStatus(filename, status, gdrive_id) {
  const row = document.querySelector(`.rec-row[data-filename="${filename}"]`);
  if (!row) return;
  const span = row.querySelector('.rec-status');
  if (span) {
    span.className = `rec-status text-xs ${STATUS_COLOR[status] ?? 'text-slate-400'}`;
    span.textContent = STATUS_LABEL[status] ?? status;
  }
  if (gdrive_id && !row.querySelector('a')) {
    const link = document.createElement('a');
    link.href = `https://drive.google.com/file/d/${gdrive_id}/view`;
    link.target = '_blank'; link.rel = 'noopener';
    link.className = 'text-blue-400 hover:text-blue-300 text-xs ml-1';
    link.textContent = '↗';
    span.insertAdjacentElement('afterend', link);
  }
}

export async function loadRecordings() {
  try {
    const res = await fetch('/api/recordings?limit=20');
    if (!res.ok) return;
    const data = await res.json();
    const recs = data.recordings ?? [];
    if (recs.length === 0) return;
    document.getElementById('recordings-empty').style.display = 'none';
    const list = document.getElementById('recordings-list');
    list.querySelectorAll('.rec-row').forEach(el => el.remove());
    recs.forEach(r => list.appendChild(_recRow(r)));
    // No usa setRecBadge (videoCanvas.js) a proposito: el original solo
    // actualiza el contador, nunca el estado visible/oculto del badge
    // (inconsistencia preexistente que esta fase preserva, ver read_first).
    document.getElementById('rec-badge').textContent = recs.length;
  } catch {}
}

// ── Recordings: delete by date range ─────────────────
const deleteRecPanel = document.getElementById('delete-recordings-panel');
const deleteRecMsg   = document.getElementById('delete-rec-msg');

// ── Phase 15: video player modal ──────────────────────────────────
export function openClipModal(src) {
  const modal = document.getElementById('clip-modal');
  const vid   = document.getElementById('clip-video');
  vid.src = src;
  modal.classList.add('open');
  vid.play().catch(() => {});
}

export function bindEventCardControls() {
  document.getElementById('btn-delete-recordings').addEventListener('click', () => {
    deleteRecMsg.classList.add('hidden');
    const now  = new Date();
    const week = new Date(now - 7 * 864e5);
    const fmt  = d => d.toISOString().slice(0, 16);
    document.getElementById('delete-rec-from').value = fmt(week);
    document.getElementById('delete-rec-to').value   = fmt(now);
    deleteRecPanel.classList.toggle('hidden');
  });

  document.getElementById('delete-rec-cancel').addEventListener('click', () => {
    deleteRecPanel.classList.add('hidden');
  });

  document.getElementById('delete-rec-confirm').addEventListener('click', async () => {
    const from = document.getElementById('delete-rec-from').value;
    const to   = document.getElementById('delete-rec-to').value;
    if (!from || !to) return;
    if (new Date(to) < new Date(from)) {
      deleteRecMsg.textContent = 'La fecha «Hasta» debe ser posterior a «Desde».';
      deleteRecMsg.className = 'text-xs text-center text-red-400';
      deleteRecMsg.classList.remove('hidden');
      return;
    }
    const btn = document.getElementById('delete-rec-confirm');
    btn.disabled = true; btn.style.opacity = '0.5';
    try {
      const params = new URLSearchParams({ from_dt: new Date(from).toISOString(), to_dt: new Date(to).toISOString() });
      const res = await fetch(`/api/recordings?${params}`, { method: 'DELETE' });
      if (res.ok) {
        const d = await res.json();
        deleteRecMsg.textContent = `${d.deleted} grabación(es) eliminada(s).`;
        deleteRecMsg.className = 'text-xs text-center text-green-400';
        deleteRecMsg.classList.remove('hidden');
        setTimeout(() => {
          deleteRecPanel.classList.add('hidden');
          document.getElementById('recordings-list').querySelectorAll('.rec-row').forEach(el => el.remove());
          document.getElementById('recordings-empty').style.display = '';
          document.getElementById('recordings-count-badge').textContent = '0';
          loadRecordings();
        }, 1500);
      } else {
        const d = await res.json().catch(() => ({}));
        deleteRecMsg.textContent = d.detail ?? 'Error al borrar.';
        deleteRecMsg.className = 'text-xs text-center text-red-400';
        deleteRecMsg.classList.remove('hidden');
      }
    } catch {
      deleteRecMsg.textContent = 'Sin respuesta.';
      deleteRecMsg.className = 'text-xs text-center text-red-400';
      deleteRecMsg.classList.remove('hidden');
    }
    finally { btn.disabled = false; btn.style.opacity = ''; }
  });

  // ── Phase 15: event delegation for clip play buttons ──────────────
  document.getElementById('recordings-list').addEventListener('click', e => {
    const btn = e.target.closest('.btn-play-clip');
    if (!btn) return;
    openClipModal(btn.dataset.src);
  });

  // clip-modal se declara en el marcado despues de donde vivia este script,
  // por eso el listener original esperaba a DOMContentLoaded; se conserva
  // igual aunque type="module" ya difiere la ejecucion (mas seguro no
  // quitarlo sin verificar — 28-PATTERNS.md).
  document.addEventListener('DOMContentLoaded', () => {
    document.getElementById('clip-modal').addEventListener('click', e => {
      if (e.target === e.currentTarget || e.target.id === 'clip-modal-close') {
        const vid = document.getElementById('clip-video');
        vid.pause(); vid.src = '';
        document.getElementById('clip-modal').classList.remove('open');
      }
    });
  });

  document.addEventListener('keydown', e => {
    if (e.key === 'Escape') {
      const vid = document.getElementById('clip-video');
      vid.pause(); vid.src = '';
      document.getElementById('clip-modal').classList.remove('open');
    }
  });
}
```

**Puntos obligatorios:**
- `loadRecordings` se exporta pero no se auto-invoca ni registra su propio `setInterval` — los
  registra `app.js` (28-08), igual que `loadHealth`/`loadObservability` en 28-03 (patron
  consistente para todos los `loadX` con polling periodico).
- No importar `setRecBadge` de `videoCanvas.js`: no se usa en este fichero (ver decision
  documentada arriba).
  </action>

  <verify>
    <automated>test -f frontend/js/components/eventCard.js && [ "$(wc -l < frontend/js/components/eventCard.js)" -le 300 ] && grep -c "^export " frontend/js/components/eventCard.js</automated>
  </verify>

  <acceptance_criteria>
    - El comando de `<verify>` no falla y `grep -c "^export "` devuelve `5` (`addRecording`,
      `updateRecordingStatus`, `loadRecordings`, `openClipModal`, `bindEventCardControls`).
    - `grep -c "textContent" frontend/js/components/eventCard.js` >= 4 (patron XSS-safe de
      `_recRow` preservado: `nameSpan.textContent`, `rec-status textContent`, etc.).
    - `grep -n "setRecBadge" frontend/js/components/eventCard.js` no encuentra nada (decision
      documentada: este fichero no importa `setRecBadge`).
    - `grep -c "addEventListener('keydown'" frontend/js/components/eventCard.js` == 1 (Escape
      cierra el modal, dentro de `bindEventCardControls`).
  </acceptance_criteria>

  <done>`frontend/js/components/eventCard.js` existe, exporta las 5 funciones del contrato, el
  patron XSS-safe de `_recRow` se conserva, y `loadRecordings` no altera la visibilidad de
  `#rec-badge` (paridad con el comportamiento original).</done>
</task>

<task type="auto">
  <name>Task 2: frontend/js/components/detectionClasses.js</name>

  <read_first>
    - `frontend/index.html:1791-1856` — panel de clases detectadas completo (sin comentario
      banner propio, entre "Zones panel" y "event delegation for clip play buttons" — Fase 27-10).
    - `.planning/phases/27-multi-clase-y-contexto-de-escena/27-10-PLAN.md` — plan original que
      escribio este bloque inline; el contrato GET/PUT `/api/v2/detection/classes` no cambia.
  </read_first>

  <files>frontend/js/components/detectionClasses.js</files>

  <action>
Crear `frontend/js/components/detectionClasses.js`:

```javascript
// frontend/js/components/detectionClasses.js
import { showToast } from '../views/dashboard.js';

const DETECTION_CLASS_LABELS = {
  0: 'Persona', 1: 'Bicicleta', 2: 'Coche', 3: 'Moto', 24: 'Mochila', 28: 'Maleta',
};

export function renderDetectionClasses(data) {
  const list = document.getElementById('detection-classes-list');
  list.innerHTML = '';
  const active = new Set(data.active ?? []);
  const locked = new Set(data.locked ?? []);
  for (const cls of data.available ?? []) {
    const isLocked = locked.has(cls.id) || cls.locked;
    const row = document.createElement('label');
    row.className = 'flex items-center gap-2 text-xs text-slate-300 py-0.5' +
      (isLocked ? ' opacity-60' : ' cursor-pointer');
    row.innerHTML = `
      <input type="checkbox" data-class-id="${cls.id}" class="detection-class-checkbox"
             ${active.has(cls.id) ? 'checked' : ''} ${isLocked ? 'disabled' : ''}>
      <span>${DETECTION_CLASS_LABELS[cls.id] ?? cls.name}</span>`;
    list.appendChild(row);
  }
  list.querySelectorAll('.detection-class-checkbox:not([disabled])').forEach(cb => {
    cb.addEventListener('change', saveDetectionClasses);
  });
}

export async function loadDetectionClasses() {
  try {
    const res = await fetch('/api/v2/detection/classes');
    if (!res.ok) return;
    renderDetectionClasses(await res.json());
  } catch {}
}

export async function saveDetectionClasses() {
  const msg = document.getElementById('detection-classes-msg');
  const checkboxes = document.querySelectorAll('.detection-class-checkbox');
  const classes = Array.from(checkboxes)
    .filter(cb => cb.checked)
    .map(cb => parseInt(cb.dataset.classId, 10));
  checkboxes.forEach(cb => cb.disabled = true);
  msg.classList.add('hidden');
  try {
    const res = await fetch('/api/v2/detection/classes', {
      method: 'PUT',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ classes }),
    });
    if (res.ok) {
      showToast('Clases activas actualizadas', 'success');
      renderDetectionClasses(await res.json());
    } else {
      const d = await res.json().catch(() => ({}));
      msg.textContent = d.detail ?? 'Error al guardar.';
      msg.className = 'text-xs text-center text-red-400 mt-2';
      msg.classList.remove('hidden');
      await loadDetectionClasses();  // revierte los checkboxes al estado real del servidor
    }
  } catch {
    msg.textContent = 'Sin respuesta del servidor.';
    msg.className = 'text-xs text-center text-red-400 mt-2';
    msg.classList.remove('hidden');
    await loadDetectionClasses();
  }
}
```

**Puntos obligatorios (heredados de 27-10-PLAN.md, no se relajan):**
- El `else` (400) lee `d.detail` — nunca un mensaje generico cuando el backend ya manda uno.
- Tras un 400 o fallo de red, siempre `loadDetectionClasses()` para no dejar la UI mintiendo
  sobre el estado real.
- El checkbox de persona (`id=0`) sigue saliendo `disabled` porque `data.locked` lo incluye — la
  UI no decide por su cuenta que clase bloquear.
- `loadDetectionClasses` se exporta pero no se auto-invoca — la llama `app.js` (28-08).
  </action>

  <verify>
    <automated>test -f frontend/js/components/detectionClasses.js && [ "$(wc -l < frontend/js/components/detectionClasses.js)" -le 300 ] && grep -c "^export " frontend/js/components/detectionClasses.js</automated>
  </verify>

  <acceptance_criteria>
    - El comando de `<verify>` no falla y `grep -c "^export "` devuelve `3`
      (`renderDetectionClasses`, `loadDetectionClasses`, `saveDetectionClasses`).
    - `grep -c "api/v2/detection/classes" frontend/js/components/detectionClasses.js` >= 2 (GET y PUT).
    - `grep -n "d.detail" frontend/js/components/detectionClasses.js` acierta dentro de
      `saveDetectionClasses`.
    - `grep -c "loadDetectionClasses()" frontend/js/components/detectionClasses.js` >= 2 (los 2
      caminos de revert, PUT-error y catch).
  </acceptance_criteria>

  <done>`frontend/js/components/detectionClasses.js` existe, exporta las 3 funciones del
  contrato, y preserva el contrato GET/PUT + revert de 27-10 sin cambios de comportamiento.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Backend (grabaciones/Drive) -> `eventCard.js` | `filename`/`gdrive_id` vienen del backend y se pintan en el DOM (nombre de fichero, enlace a Drive) |
| Backend (`/api/v2/detection/classes`) -> `detectionClasses.js` | `available`/`locked` deciden que checkbox se deshabilita — el cliente confia en el servidor, no decide por su cuenta |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-09 | Tampering (XSS) | `eventCard.js::_recRow` | mitigate | Preservar el patron ya existente: estructura estatica via `innerHTML`, `filename`/status/enlace de Drive asignados via `.textContent`/`dataset`/`href` tras montar el nodo — nunca interpolados en el template string (comentario explicito citando CodeQL `js/xss` ya presente en el codigo original) |
| T-28-10 | Tampering | Manipular el DOM para des-deshabilitar el checkbox de persona en `detectionClasses.js` | accept | Defensa en profundidad ya existe en el backend (27-07): el PUT rechaza con 400 cualquier body sin la clase 0 — heredado sin cambios de `27-10-PLAN.md` |
</threat_model>

<verification>
```bash
test -f frontend/js/components/eventCard.js && test -f frontend/js/components/detectionClasses.js
grep -c "^export " frontend/js/components/eventCard.js frontend/js/components/detectionClasses.js
```
</verification>

<success_criteria>
- `eventCard.js` exporta las 5 funciones del contrato; `detectionClasses.js` exporta las 3
  funciones del contrato; ambos bajo 300 lineas.
- El patron XSS-safe (`textContent`, no `innerHTML` para datos del backend) se conserva en ambos.
</success_criteria>

<output>
Al terminar, crear `.planning/phases/28-refactor-del-frontend-a-modulos-es/28-05-SUMMARY.md`.
</output>
