---
phase: 28-refactor-del-frontend-a-modulos-es
plan: 02
type: execute
wave: 2
depends_on: [28-01]
files_modified:
  - frontend/js/views/dashboard.js
  - frontend/js/views/dashboard-events.js
autonomous: true
requirements: [OPS-01, OPS-02]

must_haves:
  truths:
    - "El reloj, los toasts, el estado de camara online/offline, los contadores y el polling de /counts y /detections siguen funcionando identicos a hoy, ahora en un modulo ES"
    - "La carga inicial (/api/stats + /api/events) sigue pintando el chart de actividad y la lista de eventos sin usar window.dashboardAPI"
    - "El ciclo de import real entre dashboard.js y dashboard-events.js (verificado en 28-PATTERNS.md) esta resuelto sin duplicar showToast/updateStat"
  artifacts:
    - path: "frontend/js/views/dashboard.js"
      provides: "Nucleo del dashboard: showToast, updateStat, setCamStatus, loadInitialData, loadCamStatus"
      exports: ["showToast", "updateStat", "setCamStatus", "loadInitialData", "loadCamStatus"]
      min_lines: 150
    - path: "frontend/js/views/dashboard-events.js"
      provides: "Chart.js + lista de eventos + filtros + borrado por rango, sustituye a window.dashboardAPI"
      exports: ["updateChart", "addEvent", "bumpHourBar", "hourlyToArray", "applyFilters", "bindEventFilters"]
      min_lines: 150
  key_links:
    - from: "frontend/js/views/dashboard-events.js"
      to: "frontend/js/views/dashboard.js"
      via: "import { loadInitialData } from './dashboard.js' (delete-events-confirm y btn-clear-filters)"
      pattern: "import \\{ loadInitialData \\} from './dashboard.js'"
    - from: "frontend/js/views/dashboard.js"
      to: "frontend/js/views/dashboard-events.js"
      via: "import { updateChart, addEvent, hourlyToArray } from './dashboard-events.js' (usado solo dentro de loadInitialData)"
      pattern: "import \\{ updateChart, addEvent, hourlyToArray \\} from './dashboard-events.js'"
---

<objective>
Extraer el nucleo del dashboard (`frontend/index.html:787-873` + `1203-1301`) a
`frontend/js/views/dashboard.js`, y el chart de actividad + gestion de eventos
(`frontend/index.html:987-1119` + `1896-1953`) a `frontend/js/views/dashboard-events.js`, sustituyendo
`window.dashboardAPI` por exports nombrados.

Purpose: estos dos ficheros tienen un ciclo de import real y verificado (`dashboard.js` necesita
`updateChart`/`addEvent`/`hourlyToArray` de `dashboard-events.js` dentro de `loadInitialData`;
`dashboard-events.js` necesita `loadInitialData` de `dashboard.js` en 2 sitios) — se escriben en el
mismo plan para no dejar un modulo a medias con imports rotos.
Output: 2 modulos ES nuevos, cada uno por debajo de 300 lineas (fuente: dashboard.js core ~187
lineas, dashboard-events.js ~193 lineas segun el recuento verificado de `28-RESEARCH.md`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/28-refactor-del-frontend-a-modulos-es/28-PATTERNS.md

<interfaces>
<!-- Contrato de exports que el resto de la fase (28-03..28-08) consume de estos 2 ficheros.
     No lo reinventes: websocket.js (28-07) y el resto de componentes importan exactamente
     estos nombres. -->

```javascript
// frontend/js/views/dashboard.js — exports
export function showToast(msg, type = 'info', ms = 3500)
export function updateStat(id, val)
export function setCamStatus(online)
export async function loadInitialData()
export async function loadCamStatus()

// frontend/js/views/dashboard-events.js — exports (sustituyen a window.dashboardAPI)
export function updateChart(hourlyData)
export function addEvent(ts, dir, total, personName, isIntrusion = false)
export function bumpHourBar(hour)          // nueva — extrae actChart.data...[h]++ del handler WS
export function hourlyToArray(hourly)
export async function applyFilters()
export function bindEventFilters()         // agrupa listeners de btn-apply-filters/btn-clear-filters/btn-export-csv
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: frontend/js/views/dashboard.js (nucleo)</name>

  <read_first>
    - `frontend/index.html:787-873` — clock, toast, cam-status, stat counter, counts/detections polling (fuente exacta, copia 1:1 salvo exports).
    - `frontend/index.html:1203-1301` — initial data load + camera toggles + reboot.
    - `.planning/phases/28-refactor-del-frontend-a-modulos-es/28-PATTERNS.md` (seccion "frontend/js/views/dashboard.js (core)") — confirma que `loadResolutions` NO pertenece aqui (va a `videoCanvas.js`, 28-04) y que `bindPtzControls` NO pertenece aqui (va a `dashboard-ptz.js`, 28-03), pese a que el Code Example de `28-RESEARCH.md` los agrupa mal.
  </read_first>

  <files>frontend/js/views/dashboard.js</files>

  <action>
Crear `frontend/js/views/dashboard.js` con el siguiente contenido — extraccion 1:1 de
`index.html:787-873,1203-1301`, con `dashboardAPI.updateChart`/`dashboardAPI.addEvent` sustituidos
por los imports nombrados, y `tickClock`/`fetchCounts`/`fetchDetections`/los listeners de
`.cam-toggle` quedan como efectos de nivel de modulo (igual que hoy: no tienen dependencia de
orden con otros modulos, a diferencia de los `loadX()` que sí se mueven a `app.js` — ver
`28-PATTERNS.md` "Shared Pattern: convencion de estructura"):

```javascript
// frontend/js/views/dashboard.js
import { updateChart, addEvent, hourlyToArray } from './dashboard-events.js';

// ── Clock ─────────────────────────────────────────────
const clockEl = document.getElementById('clock');
function tickClock() {
  clockEl.textContent = new Date().toLocaleTimeString('es-ES', { hour12: false });
}
setInterval(tickClock, 1000);
tickClock();

// ── Toast ─────────────────────────────────────────────
const TOAST_STYLES = {
  info:    'bg-slate-800 border-slate-700 text-slate-200',
  success: 'bg-green-950 border-green-800/60 text-green-300',
  error:   'bg-red-950   border-red-800/60   text-red-300',
  warn:    'bg-amber-950 border-amber-800/60 text-amber-300',
};
export function showToast(msg, type = 'info', ms = 3500) {
  const wrap = document.getElementById('toast-container');
  const el   = document.createElement('div');
  el.className = `toast ${TOAST_STYLES[type] ?? TOAST_STYLES.info} border rounded-xl px-4 py-2.5 text-xs shadow-xl pointer-events-auto max-w-xs`;
  el.setAttribute('role', 'alert');
  el.textContent = msg;
  wrap.appendChild(el);
  requestAnimationFrame(() => el.classList.add('show'));
  setTimeout(() => { el.classList.remove('show'); setTimeout(() => el.remove(), 300); }, ms);
}

// ── Camera online/offline ─────────────────────────────
let _isOnline = true;
export function setCamStatus(online) {
  if (online === _isOnline) return;
  _isOnline = online;
  const wrap = document.getElementById('cam-status');
  const dot  = document.getElementById('status-dot');
  const txt  = document.getElementById('status-text');
  if (online) {
    wrap.className = 'flex items-center gap-1.5 px-3 py-1.5 rounded-full bg-green-500/10 border border-green-500/30 text-green-400 text-xs font-medium';
    dot.className  = 'w-1.5 h-1.5 rounded-full bg-green-400 pulse';
    txt.textContent = 'EN VIVO';
  } else {
    wrap.className = 'flex items-center gap-1.5 px-3 py-1.5 rounded-full bg-red-500/10 border border-red-500/30 text-red-400 text-xs font-medium';
    dot.className  = 'w-1.5 h-1.5 rounded-full bg-red-400';
    txt.textContent = 'SIN SEÑAL';
    showToast('Cámara sin señal — reintentando…', 'warn');
  }
}

// ── Stat counter update (with pop animation) ──────────
export function updateStat(id, val) {
  const el = document.getElementById(id);
  if (el.textContent === String(val)) return;
  el.textContent = val;
  el.classList.remove('stat-pop');
  void el.offsetWidth;
  el.classList.add('stat-pop');
  setTimeout(() => el.classList.remove('stat-pop'), 300);
}

// ── Counts polling (/counts) ──────────────────────────
async function fetchCounts() {
  try {
    const res = await fetch('/counts');
    if (!res.ok) throw new Error(`HTTP ${res.status}`);
    const d = await res.json();
    updateStat('stat-total', d.total ?? 0);
    updateStat('stat-in',    d.in    ?? 0);
    updateStat('stat-out',   d.out   ?? 0);
    document.getElementById('events-badge').textContent = d.total ?? 0;
    setCamStatus(true);
  } catch {
    setCamStatus(false);
  }
}
setInterval(fetchCounts, 2000);
fetchCounts();

// ── Detections polling (/detections) ──────────────────
async function fetchDetections() {
  try {
    const res = await fetch('/detections');
    if (!res.ok) return;
    const d = await res.json();
    document.getElementById('det-num').textContent = d.live_count ?? (d.detections ?? []).length;
  } catch {}
}
setInterval(fetchDetections, 2000);
fetchDetections();

// ── Initial data load ──────────────────────────────────
export async function loadInitialData() {
  try {
    const [statsRes, eventsRes] = await Promise.all([
      fetch('/api/stats'),
      fetch('/api/events?limit=50'),
    ]);
    if (statsRes.ok) {
      const stats = await statsRes.json();
      updateChart(hourlyToArray(stats.hourly));
      updateStat('stat-total', stats.total_today ?? 0);
    }
    if (eventsRes.ok) {
      const data   = await eventsRes.json();
      const events = (data.events ?? []).reverse();
      const list  = document.getElementById('events-list');
      const empty = document.getElementById('events-empty');
      list.querySelectorAll('.event-item').forEach(el => el.remove());
      updateStat('events-badge', 0);
      if (events.length === 0) {
        empty.style.display = '';
      } else {
        empty.style.display = 'none';
        let badge = 0;
        events.forEach(ev => {
          const ts = new Date(ev.timestamp).toLocaleTimeString('es-ES', { hour12: false });
          addEvent(ts, ev.direction, ++badge, ev.person_name ?? null, ev.is_intrusion ?? false);
        });
      }
    }
  } catch {}
}

// ── Camera settings toggles ────────────────────────────
function applyToggleState(btn, enabled) {
  btn.setAttribute('aria-checked', String(enabled));
  btn.dataset.state = String(enabled);
}

export async function loadCamStatus() {
  const toggles = ['toggle-privacy','toggle-led','toggle-motion','toggle-autotrack'];
  toggles.forEach(id => { const el = document.getElementById(id); if (el) el.disabled = true; });
  try {
    const res = await fetch('/camera/status');
    if (!res.ok) return;
    const s = await res.json();
    applyToggleState(document.getElementById('toggle-privacy'),   s.privacy);
    applyToggleState(document.getElementById('toggle-led'),       s.led);
    applyToggleState(document.getElementById('toggle-motion'),    s.motion);
    applyToggleState(document.getElementById('toggle-autotrack'), s.autotrack);
    if (s.errors && Object.keys(s.errors).length) {
      const failed = Object.keys(s.errors).join(', ');
      showToast(`Cámara: no se pudo leer ${failed}`, 'warning');
    }
  } catch {}
  finally {
    toggles.forEach(id => { const el = document.getElementById(id); if (el) el.disabled = false; });
  }
}

document.querySelectorAll('.cam-toggle').forEach(btn => {
  btn.addEventListener('click', async () => {
    const current = btn.dataset.state === 'true';
    const next    = !current;
    btn.disabled  = true;
    try {
      const res = await fetch(btn.dataset.endpoint, {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({ enabled: next }),
      });
      if (res.ok) {
        applyToggleState(btn, next);
      } else {
        const err = await res.json().catch(() => ({}));
        showToast(`Error: ${err.detail ?? res.statusText}`, 'error');
      }
    } catch (e) {
      showToast(`Sin respuesta: ${e.message}`, 'error');
    } finally {
      btn.disabled = false;
    }
  });
});

document.getElementById('cam-settings-refresh').addEventListener('click', loadCamStatus);

document.getElementById('btn-reboot').addEventListener('click', async () => {
  if (!confirm('¿Reiniciar la cámara? El stream se interrumpirá unos segundos.')) return;
  const btn = document.getElementById('btn-reboot');
  btn.disabled = true;
  try {
    const res = await fetch('/camera/reboot', { method: 'POST' });
    if (res.ok) showToast('Cámara reiniciando…', 'warn', 5000);
    else showToast('Error al reiniciar', 'error');
  } catch { showToast('Sin respuesta del servidor', 'error'); }
  finally { setTimeout(() => { btn.disabled = false; }, 8000); }
});
```

**Puntos obligatorios:**
- `tickClock`/`fetchCounts`/`fetchDetections`/los listeners `.cam-toggle`/`cam-settings-refresh`/
  `btn-reboot` **se quedan como efectos de nivel de modulo** (no se mueven a `app.js`): no tienen
  orden de dependencia con otros modulos, verificado contra el codigo real — solo los `loadX()`
  que `28-PATTERNS.md` lista explicitamente (`loadInitialData`, `loadCamStatus`, y los de otros
  modulos) se invocan desde `app.js` en 28-08.
- `loadInitialData`/`loadCamStatus` se exportan pero **no se auto-invocan** dentro de este
  fichero — las llama `app.js` (28-08), igual que hoy las invoca el orden final del script
  (lineas 1362-1366).
  </action>

  <verify>
    <automated>test -f frontend/js/views/dashboard.js && [ "$(wc -l < frontend/js/views/dashboard.js)" -le 300 ] && grep -c "^export " frontend/js/views/dashboard.js</automated>
  </verify>

  <acceptance_criteria>
    - El comando de `<verify>` no falla (fichero existe, <= 300 lineas) y `grep -c "^export "`
      devuelve `5` (`showToast`, `updateStat`, `setCamStatus`, `loadInitialData`, `loadCamStatus`).
    - `grep -n "dashboardAPI" frontend/js/views/dashboard.js` no encuentra nada (el global
      desaparece, sustituido por el import nombrado).
    - `grep -n "import { updateChart, addEvent, hourlyToArray } from './dashboard-events.js'" frontend/js/views/dashboard.js` acierta.
    - `grep -c "loadInitialData()\|loadCamStatus()" frontend/js/views/dashboard.js` == 0 (no se
      auto-invocan dentro del propio modulo).
  </acceptance_criteria>

  <done>`frontend/js/views/dashboard.js` existe, exporta las 5 funciones del contrato, y ningun
  `loadX` se invoca a nivel de modulo (los invoca `app.js` en 28-08).</done>
</task>

<task type="auto">
  <name>Task 2: frontend/js/views/dashboard-events.js (chart + eventos)</name>

  <read_first>
    - `frontend/index.html:987-1119` — borrado de eventos por rango + init de Chart.js +
      `window.dashboardAPI` (fuente exacta a sustituir por exports).
    - `frontend/index.html:1896-1953` — filtros de eventos + export CSV.
    - `frontend/index.html:1169-1180` — rama `detection` del dispatch WebSocket: hoy toca
      `actChart.data.datasets[0].data[h]` directamente. **`actChart` pasa a ser privado de este
      modulo** — `websocket.js` (28-07) no puede tocarlo, necesita una funcion exportada nueva
      (`bumpHourBar`, ver abajo). Este es un acoplamiento real encontrado al leer el codigo, no
      esta en el Code Example de `28-RESEARCH.md`.
    - `.planning/phases/28-refactor-del-frontend-a-modulos-es/28-PATTERNS.md` (seccion
      "Shared Pattern: XSS-safe DOM") — el patron de `addEvent` que hay que preservar exacto.
  </read_first>

  <files>frontend/js/views/dashboard-events.js</files>

  <action>
Crear `frontend/js/views/dashboard-events.js`:

```javascript
// frontend/js/views/dashboard-events.js
import { loadInitialData } from './dashboard.js';

// ── Events: delete by date range ─────────────────────
const deletePanel = document.getElementById('delete-events-panel');
const deleteMsg   = document.getElementById('delete-events-msg');

document.getElementById('btn-delete-events').addEventListener('click', () => {
  deleteMsg.classList.add('hidden');
  const now = new Date();
  const week = new Date(now - 7 * 864e5);
  const fmt = d => d.toISOString().slice(0, 16);
  document.getElementById('delete-from').value = fmt(week);
  document.getElementById('delete-to').value   = fmt(now);
  deletePanel.classList.toggle('hidden');
});

document.getElementById('delete-events-cancel').addEventListener('click', () => {
  deletePanel.classList.add('hidden');
});

document.getElementById('delete-events-confirm').addEventListener('click', async () => {
  const from = document.getElementById('delete-from').value;
  const to   = document.getElementById('delete-to').value;
  if (!from || !to) { return; }
  if (new Date(to) < new Date(from)) {
    deleteMsg.textContent = 'La fecha «Hasta» debe ser posterior a «Desde».';
    deleteMsg.className = 'text-xs text-center text-red-400';
    deleteMsg.classList.remove('hidden');
    return;
  }
  const btn = document.getElementById('delete-events-confirm');
  btn.disabled = true; btn.style.opacity = '0.5';
  try {
    const params = new URLSearchParams({ from_dt: new Date(from).toISOString(), to_dt: new Date(to).toISOString() });
    const res = await fetch(`/api/events?${params}`, { method: 'DELETE' });
    if (res.ok) {
      const d = await res.json();
      deleteMsg.textContent = `${d.deleted} evento(s) eliminado(s).`;
      deleteMsg.className = 'text-xs text-center text-green-400';
      deleteMsg.classList.remove('hidden');
      setTimeout(() => { deletePanel.classList.add('hidden'); loadInitialData(); }, 1500);
    } else {
      const d = await res.json().catch(() => ({}));
      deleteMsg.textContent = d.detail ?? 'Error al borrar.';
      deleteMsg.className = 'text-xs text-center text-red-400';
      deleteMsg.classList.remove('hidden');
    }
  } catch { deleteMsg.textContent = 'Sin respuesta.'; deleteMsg.className = 'text-xs text-center text-red-400'; deleteMsg.classList.remove('hidden'); }
  finally { btn.disabled = false; btn.style.opacity = ''; }
});

// ── Chart (Phase 5) ─────────────────────────────────────
const hours = Array.from({ length: 24 }, (_, i) => `${String(i).padStart(2, '0')}h`);
const curH  = new Date().getHours();
const actChart = new Chart(
  document.getElementById('activity-chart').getContext('2d'),
  {
    type: 'bar',
    data: {
      labels: hours,
      datasets: [{
        label: 'Personas',
        data:  new Array(24).fill(0),
        backgroundColor: hours.map((_, i) => i === curH ? 'rgba(74,222,128,0.4)' : 'rgba(51,65,85,0.3)'),
        borderColor:     hours.map((_, i) => i === curH ? 'rgba(74,222,128,0.7)' : 'rgba(71,85,105,0.5)'),
        borderWidth: 1, borderRadius: 3,
      }],
    },
    options: {
      responsive: true, maintainAspectRatio: false,
      animation: { duration: 300 },
      plugins: {
        legend: { display: false },
        tooltip: {
          backgroundColor: '#1e293b', borderColor: '#334155', borderWidth: 1,
          titleColor: '#94a3b8', bodyColor: '#f8fafc',
          titleFont: { size: 10 }, bodyFont: { size: 11 },
          callbacks: { label: c => ` ${c.parsed.y} personas` },
        },
      },
      scales: {
        x: { grid: { color: 'rgba(51,65,85,0.3)', drawTicks: false }, border: { display: false },
             ticks: { color: '#475569', font: { size: 9 }, maxTicksLimit: 8, maxRotation: 0 } },
        y: { min: 0, grid: { color: 'rgba(51,65,85,0.3)', drawTicks: false }, border: { display: false },
             ticks: { color: '#475569', font: { size: 9 }, precision: 0, maxTicksLimit: 4 } },
      },
    },
  }
);

export function updateChart(hourlyData) {
  actChart.data.datasets[0].data = hourlyData;
  actChart.update('active');
  document.getElementById('chart-placeholder').style.display = 'none';
}

export function bumpHourBar(hour) {
  // Extrae index.html:1174-1177 — websocket.js (28-07) no puede tocar actChart
  // directamente (es privado a este modulo), llama a esta funcion en su lugar.
  actChart.data.datasets[0].data[hour] = (actChart.data.datasets[0].data[hour] || 0) + 1;
  actChart.update('active');
  document.getElementById('chart-placeholder').style.display = 'none';
}

export function hourlyToArray(hourly) {
  return Array.from({ length: 24 }, (_, i) => (hourly ?? {})[String(i).padStart(2, '0')] ?? 0);
}

export function addEvent(ts, dir, total, personName, isIntrusion = false) {
  const list  = document.getElementById('events-list');
  const empty = document.getElementById('events-empty');
  if (empty) empty.style.display = 'none';
  const item = document.createElement('div');
  item.className = 'event-item';
  if (isIntrusion) item.style.borderColor = 'rgba(239,68,68,0.4)';
  const color = dir === 'in' ? 'text-blue-400' : 'text-amber-400';
  const arrow = dir === 'in'
    ? '<polyline points="18 15 12 9 6 15"/>'
    : '<polyline points="6 9 12 15 18 9"/>';
  // dir/ts/personName/total llegan del backend (WS/API) — nunca se
  // interpolan como HTML: se asignan via textContent tras montar la
  // estructura estatica, para que un person_name con marcado no pueda
  // ejecutarse en el navegador de otro operador (CodeQL js/xss).
  const nameTag = personName
    ? `<span class="ev-name text-slate-400 text-xs truncate max-w-[60px]"></span>`
    : '';
  const intrusionTag = isIntrusion ? `<span class="intrusion-badge">INTRUSIÓN</span>` : '';
  item.innerHTML = `
    <svg class="w-3.5 h-3.5 flex-shrink-0 ${color}" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" aria-hidden="true">${arrow}</svg>
    <span class="ev-dir ${color} font-semibold uppercase text-xs w-6"></span>
    <span class="ev-ts text-slate-400 flex-1 mono text-xs"></span>
    ${nameTag}
    ${intrusionTag}
    <span class="ev-total text-slate-600 text-xs tabular-nums"></span>
  `;
  item.querySelector('.ev-dir').textContent = dir;
  item.querySelector('.ev-ts').textContent = ts;
  item.querySelector('.ev-total').textContent = `#${total}`;
  if (personName) item.querySelector('.ev-name').textContent = personName;
  list.prepend(item);
  while (list.children.length > 50) list.lastChild.remove();
  document.getElementById('events-badge').textContent = total;
}

// ── Phase 15: event filters ───────────────────────────────────────
function _eventsFilterParams() {
  const p = new URLSearchParams();
  const dir = document.getElementById('filter-direction').value;
  const pn  = document.getElementById('filter-person').value.trim();
  const intr = document.getElementById('filter-intrusion').checked;
  const from = document.getElementById('filter-from').value;
  const to   = document.getElementById('filter-to').value;
  p.set('limit', '200');
  if (dir)  p.set('direction', dir);
  if (pn)   p.set('person_name', pn);
  if (intr) p.set('is_intrusion', 'true');
  if (from) p.set('from_dt', from + 'T00:00:00');
  if (to)   p.set('to_dt',   to   + 'T23:59:59');
  return p;
}

export async function applyFilters() {
  const params = _eventsFilterParams();
  try {
    const res = await fetch('/api/events?' + params.toString());
    if (!res.ok) return;
    const data   = await res.json();
    const events = (data.events ?? []).reverse();
    const list   = document.getElementById('events-list');
    const empty  = document.getElementById('events-empty');
    list.querySelectorAll('.event-item').forEach(el => el.remove());
    if (events.length === 0) {
      empty.style.display = '';
    } else {
      empty.style.display = 'none';
      let badge = 0;
      events.forEach(ev => {
        const ts = new Date(ev.timestamp).toLocaleTimeString('es-ES', { hour12: false });
        addEvent(ts, ev.direction, ++badge, ev.person_name ?? null, ev.is_intrusion ?? false);
      });
      document.getElementById('events-badge').textContent = events.length;
    }
  } catch {}
}

export function bindEventFilters() {
  document.getElementById('btn-apply-filters').addEventListener('click', applyFilters);

  document.getElementById('btn-clear-filters').addEventListener('click', () => {
    document.getElementById('filter-direction').value = '';
    document.getElementById('filter-person').value = '';
    document.getElementById('filter-intrusion').checked = false;
    document.getElementById('filter-from').value = '';
    document.getElementById('filter-to').value = '';
    loadInitialData();
  });

  document.getElementById('btn-export-csv').addEventListener('click', () => {
    const params = _eventsFilterParams();
    params.delete('limit');
    window.location.href = '/api/events/export?' + params.toString();
  });
}
```

**Puntos obligatorios:**
- El bloque "Events: delete by date range" (`btn-delete-events`/`delete-events-cancel`/
  `delete-events-confirm`) **se queda como efecto de nivel de modulo**, igual que hoy — no esta en
  la lista de `loadX()` que `28-PATTERNS.md` mueve a `app.js`.
- `bindEventFilters()` SI se exporta y la invoca `app.js` (28-08) — agrupa los 3 listeners que hoy
  estan sueltos a nivel de script (lineas 1937,1939-1946,1949-1953), consistente con
  `28-PATTERNS.md`.
- `bumpHourBar` es codigo nuevo (no existe hoy como funcion separada) que sustituye el acceso
  directo a `actChart` desde el dispatch de WebSocket — documentalo en el SUMMARY como la
  correccion no trivial que `28-PATTERNS.md` encontro al leer `index.html:1174-1177` contra la
  definicion de `actChart` en la 1040.
  </action>

  <verify>
    <automated>test -f frontend/js/views/dashboard-events.js && [ "$(wc -l < frontend/js/views/dashboard-events.js)" -le 300 ] && grep -c "^export " frontend/js/views/dashboard-events.js</automated>
  </verify>

  <acceptance_criteria>
    - El comando de `<verify>` no falla y `grep -c "^export "` devuelve `6`
      (`updateChart`, `bumpHourBar`, `hourlyToArray`, `addEvent`, `applyFilters`, `bindEventFilters`).
    - `grep -n "window.dashboardAPI" frontend/js/views/dashboard-events.js` no encuentra nada.
    - `grep -n "textContent" frontend/js/views/dashboard-events.js` acierta al menos 4 veces
      dentro de `addEvent` (patron XSS-safe preservado, no un `innerHTML` con template string).
    - `grep -n "import { loadInitialData } from './dashboard.js'" frontend/js/views/dashboard-events.js` acierta.
  </acceptance_criteria>

  <done>`frontend/js/views/dashboard-events.js` existe, exporta las 6 funciones del contrato,
  `actChart` es privado al modulo, y el patron XSS-safe de `addEvent` (textContent, no interpolar
  en innerHTML) se conserva identico al original.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Backend (WS/API) -> `addEvent`/`loadInitialData` | `person_name`, `timestamps` y otros campos vienen del backend (BD/reconocimiento facial) y se pintan en el DOM del navegador de cualquier operador conectado |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-03 | Tampering (XSS) | `dashboard-events.js::addEvent` | mitigate | Mantener el patron ya existente (comentario citando CodeQL `js/xss` en el codigo, `index.html:1094-1097`): estructura estatica via `innerHTML`, datos del backend asignados via `.textContent` tras montar el nodo — nunca interpolar `personName`/`ts`/`total` directo en el template string del `innerHTML` |
| T-28-04 | Information Disclosure | Ninguna nueva | accept | Esta extraccion no cambia que datos se piden ni se muestran, solo donde vive el codigo que los pinta |
</threat_model>

<verification>
```bash
test -f frontend/js/views/dashboard.js && test -f frontend/js/views/dashboard-events.js
grep -c "^export " frontend/js/views/dashboard.js frontend/js/views/dashboard-events.js
```
La verificacion cruzada completa de que los imports entre ambos ficheros resuelven a exports
reales ocurre en 28-07 (websocket.js, que importa de ambos) y en la puerta de fase 28-08
(`pytest tests/test_frontend_modules.py -q` con todos los modulos ya presentes).
</verification>

<success_criteria>
- `dashboard.js` y `dashboard-events.js` existen, cada uno bajo 300 lineas.
- El ciclo de import entre ambos esta resuelto exactamente como documenta `28-PATTERNS.md`
  (`dashboard-events.js` importa `loadInitialData`; `dashboard.js` importa `updateChart`/
  `addEvent`/`hourlyToArray`).
- `window.dashboardAPI` no existe en ningun fichero nuevo.
- El patron XSS-safe de `addEvent` se conserva identico.
</success_criteria>

<output>
Al terminar, crear `.planning/phases/28-refactor-del-frontend-a-modulos-es/28-02-SUMMARY.md`.
</output>
