---
phase: 28-refactor-del-frontend-a-modulos-es
plan: 09
type: execute
wave: 5
depends_on: [28-08]
files_modified: []
autonomous: false
requirements: [OPS-03]

must_haves:
  truths:
    - "Un operador humano confirma, con el servidor real y un navegador, que las 13 areas funcionales del dashboard se comportan identico a la version v1.2 previa a esta fase"
    - "La carga inicial del dashboard se mide contra la IP de LAN (no localhost) y queda documentada en el SUMMARY, o se difiere explicitamente si no hay un segundo dispositivo disponible en esta sesion"
  artifacts:
    - path: ".planning/phases/28-refactor-del-frontend-a-modulos-es/28-09-SUMMARY.md"
      provides: "Checklist de paridad funcional firmado + medicion (o diferimiento explicito) de carga inicial <1s en LAN"
  key_links:
    - from: "Checklist manual del operador"
      to: "ROADMAP.md Success Criteria #4 (paridad funcional) y #6 (carga <1s en LAN)"
      via: "Firma explicita en el SUMMARY de este plan, unico mecanismo de verificacion posible (sin framework de test JS en el repo)"
      pattern: "N/A — verificacion manual, no automatizable"
---

<objective>
Cerrar los 2 criterios de exito de la Fase 28 que no son automatizables (`28-VALIDATION.md`: "sin
framework de test JS en el repo"): paridad funcional total con v1.2 (criterio 4) y carga inicial
menor de 1s en LAN (criterio 6). Todo el trabajo de codigo de la fase ya esta completo y verificado
(`28-08-PLAN.md`, suite completa en verde) — este plan es puramente de verificacion humana.

Purpose: mismo patron que `27-10`/`27-11` (sin framework de test JS) y que los 9 checkpoints de
camara real ya diferidos en fases anteriores (`STATE.md`) — si no hay acceso a un segundo
dispositivo de LAN en esta sesion, el criterio 6 se difiere explicitamente sin bloquear el cierre
de la fase, tal como autoriza `28-RESEARCH.md` (Pitfall 5).
Output: `28-09-SUMMARY.md` con el checklist firmado y la medicion de carga (o su diferimiento
documentado).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/28-refactor-del-frontend-a-modulos-es/28-CONTEXT.md
@.planning/phases/28-refactor-del-frontend-a-modulos-es/28-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Arrancar el servidor real (preparacion automatizada)</name>

  <read_first>
    - `CLAUDE.md` (seccion "Comandos") — comando exacto de arranque, `CAMERA_URL` desde
      configuracion, nunca copiar credenciales al chat/log.
    - `.planning/phases/28-refactor-del-frontend-a-modulos-es/28-08-SUMMARY.md` — confirma que la
      suite completa quedo en verde antes de este checkpoint (no arrancar el checkpoint humano si
      28-08 no cerro limpio).
  </read_first>

  <files>Ninguno (arranque de proceso, no modifica ficheros)</files>

  <action>
Todo lo automatizable antes del checkpoint humano (regla de automatizacion-primero): arrancar el
servidor real en segundo plano para que el operador solo tenga que abrir el navegador, sin montar
el entorno el mismo.

```bash
.venv/Scripts/python.exe -m uvicorn backend.main:app --host 0.0.0.0 --port 8000
```

Ejecutar en segundo plano (`run_in_background`). Confirmar que arranca sin excepciones (leer las
primeras lineas de log: "Uvicorn running on http://0.0.0.0:8000"). Si `CAMERA_URL` no esta
configurada o la camara no esta accesible en esta sesion, el servidor puede arrancar igual en modo
degradado (el pipeline reconecta con backoff, per invariante 8 de `CLAUDE.md`) — documentarlo en
el SUMMARY si aplica, no es bloqueante para verificar zonas/grabaciones/personas/etc. que no
dependen de video en vivo.

Obtener la IP de LAN de esta maquina para dar al operador la URL exacta (no `localhost`, el
criterio 6 exige LAN real):
```bash
ipconfig | grep -A 1 "IPv4"
```
  </action>

  <verify>
    <automated>curl -s -o /dev/null -w "%{http_code}" http://127.0.0.1:8000/</automated>
  </verify>

  <acceptance_criteria>
    - El comando de `<verify>` devuelve `200`.
    - Se ha identificado y se comunica al operador (en el resumen previo al checkpoint) la IP de
      LAN real de la maquina, no `127.0.0.1`/`localhost`.
  </acceptance_criteria>

  <done>El servidor real esta corriendo y accesible, con la IP de LAN identificada para el
  checkpoint humano siguiente.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Checklist de paridad funcional + medicion de carga en LAN</name>

  <read_first>
    - `.planning/phases/28-refactor-del-frontend-a-modulos-es/28-CONTEXT.md` (seccion "Paridad
      funcional (Success Criteria #4)") — lista exacta de 13 areas funcionales a cubrir.
  </read_first>

  <what-built>
Los 12 modulos ES + 3 CSS de la Fase 28 (`frontend/js/**`, `frontend/css/**`), `frontend/js/app.js`
como bootstrap real, y `frontend/index.html` reescrito como shell puro — todo verificado
mecanicamente por la suite completa de pytest (`28-08-SUMMARY.md`). Falta la unica pieza no
automatizable: confirmar con un navegador real que el comportamiento observable es identico a la
version v1.2 previa a esta fase, y medir la carga inicial contra la IP de LAN.
  </what-built>

  <how-to-verify>
1. Abre `http://<IP-LAN-de-la-maquina>:8000/` en un navegador — idealmente desde un SEGUNDO
   dispositivo de la LAN (no `localhost`/`127.0.0.1`; medir en loopback no valida el criterio 6,
   `28-RESEARCH.md` Pitfall 5).
2. Con DevTools -> Network abierto (cache desactivada para simular una recarga normal), recarga
   la pagina y anota el tiempo hasta `Load`/`DOMContentLoaded`. Objetivo: < 1s en LAN.
3. Recorre el checklist de paridad funcional (las 13 areas que `28-CONTEXT.md` fija como
   alcance de la verificacion):
   - Video en vivo (`#video-feed` carga el MJPEG).
   - PTZ: los 4 botones de direccion mueven la camara, el boton de parar funciona, los atajos de
     teclado (flechas + espacio) funcionan, los presets cargan y se pueden activar/guardar.
   - Contadores (`stat-total`/`stat-in`/`stat-out`) se actualizan.
   - Chart de actividad por hora se pinta y se actualiza en tiempo real al detectar un cruce.
   - Toggles de camara (privacidad/LED/motion/autotrack) cambian de estado.
   - Selector de resoluciones cambia la resolucion y el badge `#res-badge`.
   - Grabaciones: lista, estado de subida a Drive, borrado por rango, reproduccion de clip (modal
     abre/cierra con click fuera, boton cerrar, `Escape`).
   - Personas + galeria: lista de personas conocidas, modal de enrolamiento (frame actual y
     subida de fichero), galeria de capturas.
   - Zonas: crear, listar, borrar.
   - Clases detectadas (Fase 27-10): checkboxes, persona siempre bloqueada, guardado inmediato.
   - Filtros de eventos: aplicar, limpiar, exportar CSV.
   - Salud del pipeline (Fase 16) y observabilidad (Fase 21): paneles se refrescan.
   - WebSocket: badge de estado, reconexion automatica visible si se para el servidor un momento.
4. Si CUALQUIER area se comporta distinto a como se comportaba antes de la Fase 28 (o si no hay
   acceso a un segundo dispositivo de LAN para el paso 2), documentalo explicitamente en vez de
   asumir que esta bien.
  </how-to-verify>

  <acceptance_criteria>
    - Las 13 areas funcionales listadas arriba quedan marcadas explicitamente como OK o con el
      problema concreto encontrado (no una aprobacion generica sin recorrer la lista).
    - La medicion de carga en LAN queda documentada con una cifra real, o diferida explicitamente
      con la razon (sin segundo dispositivo disponible en esta sesion) — mismo patron que los 9
      checkpoints de camara real ya diferidos en el proyecto (`STATE.md`).
  </acceptance_criteria>

  <resume-signal>Responde "aprobado" si las 13 areas funcionan igual que en v1.2 y quieres cerrar
  la fase; describe los problemas encontrados si algo no coincide (se abrira un plan de cierre de
  huecos); o responde "diferido" si no puedes completar la verificacion en esta sesion (se
  documentara como el 10º checkpoint manual diferido del proyecto, sin bloquear el cierre de
  codigo de la fase, igual que los 9 anteriores).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operador -> servidor en LAN | El checkpoint expone el dashboard en `0.0.0.0:8000` durante la verificacion manual — mismo alcance de red que cualquier ejecucion normal del proyecto (LAN, sin exposicion publica, per `CLAUDE.md`) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-18 | Information Disclosure | Servidor arrancado con `--host 0.0.0.0` durante el checkpoint | accept | Mismo comando de arranque que documenta `CLAUDE.md` para uso normal; red LAN domestica, sin exposicion publica, sin cambio de superficie respecto al proyecto ya existente |
</threat_model>

<verification>
Verificacion puramente manual (sin framework de test JS en el repo, `28-VALIDATION.md`) — el
`<resume-signal>` del Task 2 es la unica fuente de verdad de si este plan se completa aprobado o
diferido.
</verification>

<success_criteria>
- El operador confirma paridad funcional completa (13 areas) o documenta explicitamente cualquier
  discrepancia encontrada.
- La medicion de carga en LAN queda documentada o diferida explicitamente, sin bloquear el cierre
  de la fase en codigo (que ya esta completo desde `28-08`).
</success_criteria>

<output>
Al terminar, crear `.planning/phases/28-refactor-del-frontend-a-modulos-es/28-09-SUMMARY.md` con
el checklist firmado (13 areas) y la medicion de carga (o su diferimiento documentado).
</output>
