---
phase: 28-refactor-del-frontend-a-modulos-es
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_frontend_modules.py
  - frontend/css/base.css
  - frontend/css/layout.css
  - frontend/css/components.css
autonomous: true
requirements: [OPS-01, OPS-02]

must_haves:
  truths:
    - "Existe un contrato pytest que define mecanicamente OPS-01/OPS-02/OPS-03 antes de que exista ningun modulo JS nuevo (Wave 0 Gap de 28-VALIDATION.md)"
    - "Los 3 ficheros CSS locked por ADR-08 existen con el contenido 1:1 extraido del <style> actual, sin reglas inventadas ni omitidas"
    - "frontend/css/layout.css existe aunque no tenga reglas que mover (documentado, no un hueco silencioso)"
  artifacts:
    - path: "tests/test_frontend_modules.py"
      provides: "Contrato mecanico OPS-01 (ficheros locked existen)/OPS-02 (limite 300 lineas)/OPS-03 (sin logica inline, MIME real, SRI Chart.js)"
      exports: ["TEST_css_modules_exist", "TEST_js_modules_exist", "TEST_line_limit", "TEST_no_inline_logic", "TEST_app_entry_point_is_real", "TEST_static_js_mime_type", "TEST_static_css_served", "TEST_root_serves_index_html"]
    - path: "frontend/css/base.css"
      provides: "Reset, tipografia, scrollbar, animacion pulse-ring — extraido de index.html:12-34"
      min_lines: 20
    - path: "frontend/css/components.css"
      provides: "Estilos de componentes (ptz-btn, toast, card, event-item, cam-toggle, gallery, filtros, clip-modal) — extraido de index.html:36-116"
      min_lines: 70
    - path: "frontend/css/layout.css"
      provides: "Fichero LOCKED por ADR-08, documentado como vacio a proposito (layout es 100% Tailwind utility-driven)"
  key_links:
    - from: "tests/test_frontend_modules.py"
      to: "frontend/css/*.css, frontend/js/**/*.js, frontend/index.html, backend/main.py (StaticFiles mount)"
      via: "pathlib.Path.rglob + starlette.testclient.TestClient contra backend.main.app"
      pattern: "FRONTEND = Path\\(__file__\\)"
---

<objective>
Crear el contrato de verificacion mecanica de toda la Fase 28 (`tests/test_frontend_modules.py`,
Wave 0 Gap fijado por `28-VALIDATION.md`) y extraer los 3 ficheros CSS locked por ADR-08 desde el
`<style>` inline de `frontend/index.html` (lineas 11-117, verificado por lectura directa en esta
sesion de planificacion — no 11-120 como aproxima `28-CONTEXT.md`).

Purpose: el resto de planes de la fase (28-02..28-08) necesitan un contrato pytest ya existente
para verificar su propio trabajo con comandos `-k` scoped, y los 3 CSS son la unica parte de la
extraccion sin ninguna dependencia cruzada (no importan ni son importados por ningun modulo JS).
Output: `tests/test_frontend_modules.py` (8 tests, la mayoria en rojo hasta que las fases
posteriores creen los modulos JS — es el estado esperado, no un fallo de este plan) +
`frontend/css/{base,layout,components}.css`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/28-refactor-del-frontend-a-modulos-es/28-CONTEXT.md
@.planning/phases/28-refactor-del-frontend-a-modulos-es/28-VALIDATION.md
@.planning/phases/28-refactor-del-frontend-a-modulos-es/28-PATTERNS.md

<interfaces>
<!-- pytest.ini ya fija la convencion de descubrimiento de tests: -->
```ini
[pytest]
python_functions = TEST_*
```
Todas las funciones de test de este fichero deben nombrarse `TEST_*` (no `test_*`) para que
pytest las recoja, igual que el resto de `tests/*.py` del repo (`tests/test_security_regression.py`
usa el mismo prefijo, ver `TEST_vuln_14_chartjs_cdn_has_subresource_integrity`).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Contrato pytest — tests/test_frontend_modules.py</name>

  <read_first>
    - `.planning/phases/28-refactor-del-frontend-a-modulos-es/28-VALIDATION.md` (completo) — tabla
      Requisito -> Test y "Notas de diseño de test": nombres exactos de test a implementar y qué
      debe comprobar cada uno.
    - `backend/main.py:605-623` — mount real de `/static` sobre `FRONTEND_DIR` (linea 609) y la
      ruta `/` que sirve `index.html` (linea 621-623). No se toca, solo se referencia desde el test.
    - `tests/test_security_regression.py:211-217` — `TEST_vuln_14_chartjs_cdn_has_subresource_integrity`
      ya existe y ya cubre el SRI de Chart.js: no lo dupliques en este fichero nuevo.
    - `frontend/index.html:1-10,118-120` — cabecera actual (2 `<script>` CDN + `<style>` que cierra
      en la 117) para entender qué debe seguir vivo tras el refactor.
  </read_first>

  <files>tests/test_frontend_modules.py</files>

  <action>
Crear `tests/test_frontend_modules.py` con el contrato completo de la fase (8 funciones `TEST_*`).
Este fichero se escribe ANTES de que existan los modulos JS nuevos — la mayoria de sus tests
estaran en rojo hasta que 28-08-PLAN.md complete la reescritura de `index.html` y cree
`frontend/js/app.js`; eso es el diseño esperado (Wave 0 Gap de `28-VALIDATION.md`), no un error de
este plan.

```python
"""Contrato mecanico del refactor a modulos ES (Fase 28, OPS-01/OPS-02/OPS-03).

No hay framework de test JS en el repo (sin package.json). Este fichero cubre solo las
propiedades mecanicas verificables desde Python: ficheros locked por 28-CONTEXT.md, limite de
300 lineas, ausencia de logica inline en index.html, y el tipo MIME real servido por la app
ASGI. La paridad funcional completa (video, PTZ, zonas, grabaciones, etc.) es un checklist
manual firmado en el SUMMARY de la puerta de fase (28-09-PLAN.md), igual que 27-10.

La mayoria de estos tests estan en rojo hasta que 28-08-PLAN.md termina la fase: es el estado
esperado mientras 28-02..28-07 van creando los modulos uno a uno.
"""
import re
from pathlib import Path

from starlette.testclient import TestClient

FRONTEND = Path(__file__).resolve().parent.parent / "frontend"
LINE_LIMIT = 300

LOCKED_CSS = ["base.css", "layout.css", "components.css"]

LOCKED_JS = [
    "app.js",
    "api.js",
    "websocket.js",
    "views/dashboard.js",
    "views/dashboard-ptz.js",
    "views/dashboard-events.js",
    "views/dashboard-observability.js",
    "components/videoCanvas.js",
    "components/zoneEditor.js",
    "components/eventCard.js",
    "components/detectionClasses.js",
    "components/personGallery.js",
]


def TEST_css_modules_exist():
    missing = [n for n in LOCKED_CSS if not (FRONTEND / "css" / n).is_file()]
    assert not missing, f"faltan CSS locked por 28-CONTEXT.md: {missing}"


def TEST_js_modules_exist():
    missing = [rel for rel in LOCKED_JS if not (FRONTEND / "js" / rel).is_file()]
    assert not missing, f"faltan modulos JS locked por 28-CONTEXT.md: {missing}"


def TEST_line_limit():
    offenders = []
    paths = list((FRONTEND / "js").rglob("*.js")) + list((FRONTEND / "css").glob("*.css"))
    for path in paths:
        n = len(path.read_text(encoding="utf-8").splitlines())
        if n > LINE_LIMIT:
            offenders.append(f"{path.relative_to(FRONTEND)}: {n} lineas")
    assert not offenders, f"modulos por encima de {LINE_LIMIT} lineas: {offenders}"


def TEST_no_inline_logic():
    html = (FRONTEND / "index.html").read_text(encoding="utf-8")
    assert re.search(r"<style[\s>]", html) is None, "sigue habiendo un <style> inline en index.html"
    for tag in re.findall(r"<script\b[^>]*>", html):
        assert "src=" in tag, f"<script> inline sin src encontrado en index.html: {tag}"


def TEST_app_entry_point_is_real():
    app_js = FRONTEND / "js" / "app.js"
    content = app_js.read_text(encoding="utf-8")
    assert len(content.splitlines()) > 10, "frontend/js/app.js sigue pareciendo el stub de 2 lineas"
    assert "DOMContentLoaded" in content, "app.js debe orquestar el arranque en DOMContentLoaded"
    stub = FRONTEND / "app.js"
    assert not stub.exists(), "frontend/app.js (stub v1.2) debe eliminarse, no quedar huerfano junto al real"


def TEST_static_js_mime_type():
    from backend.main import app
    client = TestClient(app)
    res = client.get("/static/js/app.js")
    assert res.status_code == 200
    assert "javascript" in res.headers["content-type"]


def TEST_static_css_served():
    from backend.main import app
    client = TestClient(app)
    res = client.get("/static/css/base.css")
    assert res.status_code == 200
    assert "css" in res.headers["content-type"]


def TEST_root_serves_index_html():
    from backend.main import app
    client = TestClient(app)
    res = client.get("/")
    assert res.status_code == 200
    assert "text/html" in res.headers["content-type"]
```

**Puntos obligatorios:**
- `TEST_line_limit` usa `Path.rglob`/`Path.glob` sobre directorios que aun no existen en este
  momento del plan (`frontend/js/` no se crea hasta 28-02) — verificado en esta sesion de
  planificacion que `Path.rglob` sobre un directorio ausente devuelve `[]` sin excepcion (no hace
  falta un guard `if not dir.is_dir()`).
- `TEST_app_entry_point_is_real` es quien decide, de forma automatizada, la pregunta abierta del
  quality gate sobre el stub huerfano `frontend/app.js`: falla mientras ese fichero siga
  existiendo junto al `frontend/js/app.js` real. Esto fuerza a 28-08-PLAN.md a borrarlo, no a
  dejarlo huerfano.
- No dupliques el test de SRI de Chart.js (`tests/test_security_regression.py` ya lo cubre);
  este fichero nuevo no debe reimplementarlo.
  </action>

  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/test_frontend_modules.py --collect-only -q && .venv/Scripts/python.exe -m pytest tests/test_frontend_modules.py -k root_serves_index_html -q</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "^def TEST_" tests/test_frontend_modules.py` devuelve `8`.
    - La recoleccion de pytest (`--collect-only`) no falla por error de import (confirma que
      `from backend.main import app` es valido dentro de las funciones, no a nivel de modulo,
      para no romper la recoleccion si `backend.main` tarda en importar).
    - `TEST_root_serves_index_html` pasa en verde ya en este punto de la fase (no depende de
      ningun modulo JS nuevo).
  </acceptance_criteria>

  <done>El contrato mecanico de la fase existe, es recolectable por pytest, y su unico test que
  no depende de trabajo futuro (`TEST_root_serves_index_html`) pasa en verde.</done>
</task>

<task type="auto">
  <name>Task 2: Extraccion 1:1 — frontend/css/{base,layout,components}.css</name>

  <read_first>
    - `frontend/index.html:11-117` — bloque `<style>` completo (verificado en esta sesion de
      planificacion: cierra en la linea 117, no 120 como aproxima `28-CONTEXT.md`).
    - `.planning/phases/28-refactor-del-frontend-a-modulos-es/28-PATTERNS.md` (secciones
      "frontend/css/base.css", "frontend/css/components.css", "frontend/css/layout.css") — reparto
      exacto verificado linea a linea.
  </read_first>

  <files>frontend/css/base.css, frontend/css/layout.css, frontend/css/components.css</files>

  <action>
Copia literal, sin reformatear ni renombrar selectores. `frontend/css/base.css` (reset,
tipografia, scrollbar, animacion — de `index.html:12,14-22,24,26-28,30-34`):

```css
*, *::before, *::after { box-sizing: border-box; }

body {
  font-family: 'Inter', system-ui, sans-serif;
  background: #020617;
  color: #f8fafc;
  min-height: 100svh;
  display: flex;
  flex-direction: column;
  margin: 0;
}

.mono { font-family: 'JetBrains Mono', monospace; }

::-webkit-scrollbar { width: 4px; height: 4px; }
::-webkit-scrollbar-track { background: transparent; }
::-webkit-scrollbar-thumb { background: #334155; border-radius: 2px; }

@keyframes pulse-ring {
  0%, 100% { opacity: 1; transform: scale(1); }
  50%       { opacity: 0.35; transform: scale(0.75); }
}
.pulse { animation: pulse-ring 2s ease-in-out infinite; }
```

`frontend/css/components.css` (de `index.html:36-116`, incluye `#clip-modal` que hoy vive en el
mismo `<style>` aunque su marcado esté fuera del rango 121-784 — hallazgo de `28-PATTERNS.md`):

```css
.ptz-btn {
  width: 44px; height: 44px;
  display: flex; align-items: center; justify-content: center;
  border-radius: 10px;
  background: #1e293b;
  border: 1px solid #334155;
  color: #94a3b8;
  cursor: pointer;
  transition: background 120ms ease, color 120ms ease, border-color 120ms ease, transform 80ms ease;
  -webkit-tap-highlight-color: transparent;
}
.ptz-btn:hover  { background: #2d3f52; color: #e2e8f0; border-color: #475569; }
.ptz-btn:active { background: #2563eb; color: #fff; border-color: #3b82f6; transform: scale(0.92); }
.ptz-btn:focus-visible { outline: 2px solid #3b82f6; outline-offset: 2px; }
.ptz-btn.busy   { opacity: 0.5; pointer-events: none; }

@keyframes stat-pop {
  0%   { transform: scale(1.18); }
  100% { transform: scale(1); }
}
.stat-pop { animation: stat-pop 250ms ease-out; }

.card { transition: border-color 200ms ease; }

.preset-btn {
  font-size: 11px; padding: 5px 12px; border-radius: 8px;
  background: #1e293b; border: 1px solid #334155; color: #94a3b8;
  cursor: pointer; transition: all 120ms ease; font-family: inherit;
}
.preset-btn:hover  { background: #2d3f52; color: #e2e8f0; border-color: #475569; }
.preset-btn:active { background: #2563eb; color: #fff; border-color: #3b82f6; }

.event-item {
  display: flex; align-items: center; gap: 10px;
  padding: 7px 10px; border-radius: 8px;
  background: #1e293b; border: 1px solid #1e3a5f;
  font-size: 12px; animation: slide-in 200ms ease;
}
@keyframes slide-in {
  from { opacity: 0; transform: translateY(-5px); }
  to   { opacity: 1; transform: translateY(0); }
}

.toast {
  transform: translateY(8px);
  opacity: 0;
  transition: transform 280ms ease-out, opacity 280ms ease-out;
}
.toast.show { transform: translateY(0); opacity: 1; }

.cam-toggle[aria-checked="true"]  { background: #2563eb; }
.cam-toggle[aria-checked="true"] .cam-toggle-knob { transform: translateX(20px); background: #fff; }
.cam-toggle[aria-checked="false"] .cam-toggle-knob { transform: translateX(0); }
.cam-toggle:focus-visible { outline: 2px solid #3b82f6; outline-offset: 2px; }
.cam-toggle:disabled { opacity: 0.4; pointer-events: none; }

.intrusion-badge {
  font-size: 9px; padding: 1px 5px; border-radius: 4px; flex-shrink: 0;
  background: rgba(239,68,68,0.15); border: 1px solid rgba(239,68,68,0.4);
  color: #fca5a5; font-weight: 700; letter-spacing: 0.05em;
}
.gallery-grid { display: grid; grid-template-columns: repeat(3,1fr); gap: 6px; }
.gallery-thumb { aspect-ratio: 1; object-fit: cover; border-radius: 8px; border: 1px solid #334155; cursor: pointer; transition: border-color 120ms; }
.gallery-thumb:hover { border-color: #3b82f6; }

/* Phase 15 */
.filter-input {
  background: #1e293b; border: 1px solid #334155; border-radius: 8px;
  padding: 3px 8px; font-size: 11px; color: #cbd5e1;
  outline: none; transition: border-color 120ms;
}
.filter-input:focus { border-color: #3b82f6; }
.filter-select { appearance: none; -webkit-appearance: none; }

#clip-modal {
  display: none; position: fixed; inset: 0; z-index: 60;
  background: rgba(2,6,23,0.85); backdrop-filter: blur(6px);
  align-items: center; justify-content: center;
}
#clip-modal.open { display: flex; }
#clip-modal video { max-width: min(90vw,960px); max-height: 80vh; border-radius: 12px; background: #000; }
```

`frontend/css/layout.css` (sin contenido real que mover — verificado 0 coincidencias de `@media`;
el unico grid de pagina es una clase de utilidad Tailwind en el marcado, no una regla propia):

```css
/* frontend/css/layout.css
 * Vacio a proposito: el layout de columnas del dashboard esta expresado como
 * clases de utilidad de Tailwind directamente en el marcado (frontend/index.html),
 * no como reglas CSS propias. Verificado en 28-RESEARCH.md: 0 coincidencias de
 * @media en el <style> original, y el unico grid de pagina
 * ("grid grid-cols-1 lg:grid-cols-5 gap-4") es una clase de utilidad Tailwind.
 * El fichero existe porque la estructura css/base+layout+components esta LOCKED
 * por ADR-08 (SPEC_v2.md Sec.8.2), aunque hoy no tenga contenido que mover.
 */
```

**No tocar `frontend/index.html` en esta tarea** — el `<style>` original sigue vivo ahi hasta que
`28-08-PLAN.md` lo elimine (todos los modulos JS deben existir primero para que la reescritura del
shell sea segura en un solo paso).
  </action>

  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/test_frontend_modules.py -k "css_modules_exist or line_limit or static_css_served" -q</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "pulse-ring" frontend/css/base.css` >= 1.
    - `grep -c "#clip-modal" frontend/css/components.css` >= 1 (confirma que el hallazgo de
      `28-PATTERNS.md` sobre `#clip-modal` fuera del rango 121-784 quedó cubierto).
    - `grep -c "@media" frontend/css/layout.css` == 0 (confirma que no se inventaron reglas de
      layout que no existen hoy).
    - `git diff --stat frontend/index.html` esta vacio (esta tarea no toca el shell).
  </acceptance_criteria>

  <done>Los 3 CSS locked por ADR-08 existen, cada uno bajo 300 lineas, con el contenido 1:1 del
  `<style>` actual (o documentado como vacio en el caso de layout.css), sin tocar
  `frontend/index.html`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Ninguna nueva | Esta tarea solo mueve CSS estatico y crea un fichero de test que lee ficheros locales — no procesa entrada de red ni de usuario |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-01 | Information Disclosure | `tests/test_frontend_modules.py` importa `backend.main.app` dentro de un `TestClient` | accept | Mismo patron que `tests/test_security_regression.py` y `tests/test_detection_config_api.py`; no expone nada nuevo, solo ejercita rutas ya publicas (`/`, `/static/*`) |
| T-28-02 | Tampering | CSS movido a ficheros publicos bajo `/static/css/` | accept | CSS no ejecuta codigo; el mismo contenido ya era publico como `<style>` inline servido en `/` |
</threat_model>

<verification>
```bash
.venv/Scripts/python.exe -m pytest tests/test_frontend_modules.py -q
```
Se espera que `TEST_js_modules_exist`, `TEST_no_inline_logic`, `TEST_app_entry_point_is_real` y
`TEST_static_js_mime_type` fallen en este punto de la fase (ningun modulo JS existe todavia,
`index.html` conserva su `<script>`/`<style>` inline) — es el estado esperado documentado en el
`<objective>`, no una regresion. `TEST_css_modules_exist`, `TEST_line_limit`,
`TEST_static_css_served` y `TEST_root_serves_index_html` deben pasar.
</verification>

<success_criteria>
- `tests/test_frontend_modules.py` existe, es recolectable por pytest, define las 8 funciones
  `TEST_*` que sirven de contrato para el resto de la fase.
- Los 3 CSS locked por ADR-08 existen con el contenido real 1:1 movido, cada uno bajo 300 lineas.
- Cero cambios en `frontend/index.html`, `frontend/app.js` o cualquier fichero `backend/`.
</success_criteria>

<output>
Al terminar, crear `.planning/phases/28-refactor-del-frontend-a-modulos-es/28-01-SUMMARY.md`.
</output>
