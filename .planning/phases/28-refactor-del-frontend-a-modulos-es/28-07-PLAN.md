---
phase: 28-refactor-del-frontend-a-modulos-es
plan: 07
type: execute
wave: 3
depends_on: [28-02, 28-04, 28-05]
files_modified:
  - frontend/js/websocket.js
autonomous: true
requirements: [OPS-01, OPS-02]

must_haves:
  truths:
    - "La conexion WebSocket /ws con backoff exponencial (1s hasta 30s) sigue reconectando identico a hoy"
    - "Los 5 tipos de mensaje (init, detection, recording_started/_uploaded/_failed) siguen actualizando el chart, los contadores, la lista de eventos y el badge de grabacion"
    - "websocket.js no toca actChart directamente (es privado a dashboard-events.js) — usa bumpHourBar"
  artifacts:
    - path: "frontend/js/websocket.js"
      provides: "Conexion WebSocket, reconexion con backoff, dispatch de mensajes por type"
      exports: ["connectWS"]
      min_lines: 60
  key_links:
    - from: "frontend/js/websocket.js"
      to: "frontend/js/views/dashboard.js, frontend/js/views/dashboard-events.js, frontend/js/components/eventCard.js, frontend/js/components/videoCanvas.js"
      via: "4 import statements verificados linea a linea contra los exports reales de cada fichero (ya existen desde 28-02/28-04/28-05)"
      pattern: "import \\{ .+ \\} from '\\./views/dashboard(-events)?\\.js'|from '\\./components/(eventCard|videoCanvas)\\.js'"
---

<objective>
Extraer la conexion WebSocket (`frontend/index.html:1121-1201`) a `frontend/js/websocket.js`, el
unico modulo cuya correccion depende genuinamente de que otros ficheros ya existan (importa 4
simbolos distintos de 4 modulos creados en la Wave 2: `dashboard.js`, `dashboard-events.js`,
`eventCard.js`, `videoCanvas.js`).

Purpose: por eso este plan va en su propia wave (3), despues de que 28-02/28-04/28-05 ya hayan
creado esos ficheros — permite verificar con `grep` real, no solo con el contrato ya declarado en
`28-PATTERNS.md`, que cada simbolo importado existe de verdad como `export` en el fichero destino.
Output: 1 modulo ES nuevo, muy por debajo de 300 lineas, mas una verificacion cruzada explicita de
los 4 imports.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/28-refactor-del-frontend-a-modulos-es/28-PATTERNS.md

<interfaces>
<!-- Contratos ya creados por planes previos — websocket.js los consume tal cual, no los redefine. -->
```javascript
// frontend/js/views/dashboard.js (28-02)
export function updateStat(id, val)
export function setCamStatus(online)
export function showToast(msg, type = 'info', ms = 3500)

// frontend/js/views/dashboard-events.js (28-02)
export function updateChart(hourlyData)
export function addEvent(ts, dir, total, personName, isIntrusion = false)
export function hourlyToArray(hourly)
export function bumpHourBar(hour)

// frontend/js/components/eventCard.js (28-05)
export function addRecording(r)
export function updateRecordingStatus(filename, status, gdrive_id)

// frontend/js/components/videoCanvas.js (28-04)
export function setRecBadge(visible, count)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: frontend/js/websocket.js</name>

  <read_first>
    - `frontend/index.html:1121-1201` — bloque WebSocket completo (`_ws`/`_wsRetry`,
      `setWsStatus`, `connectWS`, dispatch `onmessage` con 5 ramas).
    - `frontend/index.html:1174-1177` — la rama `detection` toca `actChart.data.datasets[0].data[h]`
      y llama `actChart.update('active')` directamente; `actChart` ahora es privado a
      `frontend/js/views/dashboard-events.js` (28-02) — este plan NO puede replicar ese acceso,
      debe llamar a `bumpHourBar(hour)` (exportada por 28-02 explicitamente para este caso).
    - `.planning/phases/28-refactor-del-frontend-a-modulos-es/28-PATTERNS.md` (seccion
      "frontend/js/websocket.js") — codigo verificado del `connectWS` completo con los 4 imports.
  </read_first>

  <files>frontend/js/websocket.js</files>

  <action>
Crear `frontend/js/websocket.js`:

```javascript
// frontend/js/websocket.js
import { updateStat, setCamStatus, showToast } from './views/dashboard.js';
import { updateChart, addEvent, hourlyToArray, bumpHourBar } from './views/dashboard-events.js';
import { addRecording, updateRecordingStatus } from './components/eventCard.js';
import { setRecBadge } from './components/videoCanvas.js';

let _ws = null;
let _wsRetry = 1000;

function setWsStatus(connected) {
  const badge = document.getElementById('ws-badge');
  const label = document.getElementById('ws-label');
  const icon  = document.getElementById('ws-icon');
  if (connected) {
    badge.className = 'flex-shrink-0 text-xs px-2.5 py-1 rounded-full bg-green-500/10 text-green-400 border border-green-500/30 font-medium';
    badge.textContent = 'Activo';
    label.textContent = 'Eventos en tiempo real';
    icon.className = icon.className.replace('border-slate-700', 'border-green-500/30');
  } else {
    badge.className = 'flex-shrink-0 text-xs px-2.5 py-1 rounded-full bg-amber-500/10 text-amber-400 border border-amber-500/30 font-medium';
    badge.textContent = 'Reconectando';
    label.textContent = 'Intentando reconectar…';
    icon.className = icon.className.replace('border-green-500/30', 'border-slate-700');
  }
}

export async function connectWS() {
  const proto = location.protocol === 'https:' ? 'wss:' : 'ws:';
  let wsUrl = `${proto}//${location.host}/ws`;
  try {
    const res = await fetch('/api/ws-token');
    if (res.ok) {
      const { token } = await res.json();
      if (token) wsUrl += `?token=${token}`;
    }
  } catch (_) {}
  _ws = new WebSocket(wsUrl);

  _ws.onopen = () => {
    _wsRetry = 1000;
    setWsStatus(true);
    setCamStatus(true);
  };

  _ws.onmessage = (e) => {
    const msg = JSON.parse(e.data);
    if (msg.type === 'init') {
      updateChart(hourlyToArray(msg.hourly));
      updateStat('stat-total', msg.total_today ?? 0);
    } else if (msg.type === 'detection') {
      const ts = new Date(msg.timestamp).toLocaleTimeString('es-ES', { hour12: false });
      addEvent(ts, msg.direction, msg.total_today, msg.person_name ?? null, msg.is_intrusion ?? false);
      updateStat('stat-total', msg.total_today);
      bumpHourBar(new Date(msg.timestamp).getHours());
      const who = msg.person_name ? ` — ${msg.person_name}` : '';
      const intrusionSuffix = msg.is_intrusion ? ' ⚠ INTRUSIÓN' : '';
      showToast(`Cruce ${msg.direction.toUpperCase()} detectado${who}${intrusionSuffix}`, msg.is_intrusion ? 'error' : 'success', 2000);
    } else if (msg.type === 'recording_started') {
      addRecording({ id: msg.id, filename: msg.filename, upload_status: 'pending', created_at: new Date().toISOString() });
      setRecBadge(true);
      showToast(`Grabando: ${msg.filename}`, 'info', 3000);
    } else if (msg.type === 'recording_uploaded') {
      updateRecordingStatus(msg.filename, 'uploaded', msg.gdrive_id);
      setRecBadge(false);
    } else if (msg.type === 'recording_failed') {
      updateRecordingStatus(msg.filename, 'failed');
      setRecBadge(false);
    }
  };

  _ws.onclose = () => {
    setWsStatus(false);
    setTimeout(connectWS, _wsRetry);
    _wsRetry = Math.min(_wsRetry * 2, 30000);
  };

  _ws.onerror = () => _ws.close();
}
```

**Puntos obligatorios:**
- `setWsStatus` queda privado (solo la usa `connectWS` internamente, verificado — unica llamada en
  las lineas originales 1160/1195).
- `setRecBadge(true)`/`setRecBadge(false)` sustituyen `document.getElementById('rec-badge').classList.remove/add('hidden')`
  — llamada sin segundo argumento (`count`), que `setRecBadge` ya trata como opcional
  (`if (count != null)`), preservando el comportamiento original (las 3 ramas WS solo alternan
  visibilidad, nunca tocan el contador).
- `connectWS` es la unica funcion exportada — no se auto-invoca dentro de este fichero, la llama
  `app.js` (28-08) al final de su orquestacion, igual que hoy es la ultima linea del bloque de
  arranque (`connectWS();`, linea 1366).
  </action>

  <verify>
    <automated>test -f frontend/js/websocket.js && [ "$(wc -l < frontend/js/websocket.js)" -le 300 ] && grep -c "^export " frontend/js/websocket.js</automated>
  </verify>

  <acceptance_criteria>
    - El comando de `<verify>` no falla y `grep -c "^export "` devuelve `1` (`connectWS`).
    - `grep -n "actChart" frontend/js/websocket.js` no encuentra nada (confirma que no se accede
      directamente al Chart privado de `dashboard-events.js`).
    - `grep -c "bumpHourBar(" frontend/js/websocket.js` == 1 (sustituye el acceso directo a
      `actChart`).
    - `grep -n "getElementById('rec-badge')" frontend/js/websocket.js` no encuentra nada (las 3
      ramas de grabacion usan `setRecBadge`, no tocan el DOM del badge por su cuenta).
  </acceptance_criteria>

  <done>`frontend/js/websocket.js` existe, exporta `connectWS`, y el dispatch de los 5 tipos de
  mensaje usa exclusivamente las funciones importadas (nunca `actChart` ni `#rec-badge`
  directos).</done>
</task>

<task type="auto">
  <name>Task 2: Verificacion cruzada de los 4 imports contra exports reales</name>

  <read_first>
    - `frontend/js/views/dashboard.js`, `frontend/js/views/dashboard-events.js`,
      `frontend/js/components/eventCard.js`, `frontend/js/components/videoCanvas.js` — los 4
      ficheros de los que `websocket.js` importa, ya creados por 28-02/28-04/28-05. Esta tarea
      lee su contenido REAL (no el contrato de `28-PATTERNS.md`) para confirmar que cada simbolo
      importado por `websocket.js` existe de verdad como `export` — es la razon por la que este
      plan vive en su propia wave, despues de esos 3 planes.
  </read_first>

  <files>Ninguno (tarea de verificacion, no modifica ficheros)</files>

  <action>
Ejecutar la verificacion cruzada linea a linea: para cada simbolo que `websocket.js` importa,
confirmar que el fichero destino lo exporta con `export function <nombre>` (o `export async
function <nombre>`). Si alguno falta, es un bug real de integracion que debe corregirse en este
mismo plan antes de darlo por completo (no dejarlo para la puerta de fase 28-08).

Comandos de verificacion (uno por fichero destino):
```bash
grep -E "^export (async )?function (updateStat|setCamStatus|showToast)\(" frontend/js/views/dashboard.js
grep -E "^export function (updateChart|addEvent|hourlyToArray|bumpHourBar)\(" frontend/js/views/dashboard-events.js
grep -E "^export (async )?function (addRecording|updateRecordingStatus)\(" frontend/js/components/eventCard.js
grep -E "^export function setRecBadge\(" frontend/js/components/videoCanvas.js
```
Cada comando debe devolver tantas lineas como simbolos se buscan (3, 4, 2, 1 — total 10). Si
alguno devuelve menos, el nombre del export en `websocket.js` no coincide con el fichero real
(typo, nombre distinto, o falta `export`) — corregir el import en `websocket.js` para que
coincida con el nombre real exportado, no al reves (los contratos de 28-02/28-04/28-05 son la
fuente de verdad, ya verificados en sus propios planes).
  </action>

  <verify>
    <automated>[ "$(grep -cE '^export (async )?function (updateStat|setCamStatus|showToast)\(' frontend/js/views/dashboard.js)" -eq 3 ] && [ "$(grep -cE '^export function (updateChart|addEvent|hourlyToArray|bumpHourBar)\(' frontend/js/views/dashboard-events.js)" -eq 4 ] && [ "$(grep -cE '^export (async )?function (addRecording|updateRecordingStatus)\(' frontend/js/components/eventCard.js)" -eq 2 ] && [ "$(grep -cE '^export function setRecBadge\(' frontend/js/components/videoCanvas.js)" -eq 1 ] && echo "OK: 10/10 exports resueltos"</automated>
  </verify>

  <acceptance_criteria>
    - El comando de `<verify>` imprime `OK: 10/10 exports resueltos`.
    - Cero cambios de fichero en esta tarea salvo, si hiciera falta, una correccion puntual del
      nombre de un import dentro de `frontend/js/websocket.js` (Task 1) — documentar en el
      SUMMARY si esto ocurrio.
  </acceptance_criteria>

  <done>Los 10 simbolos que `websocket.js` importa de 4 modulos distintos estan confirmados como
  exports reales, no solo como contrato documentado.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Backend (`/ws`) -> `websocket.js` | Mensajes JSON del servidor (`type`, `direction`, `person_name`, `filename`, `gdrive_id`) se despachan a funciones de otros modulos que ya sanean su renderizado (textContent, no innerHTML para datos variables) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-13 | Tampering | `_ws.onmessage` hace `JSON.parse(e.data)` sin validar `type` contra una lista cerrada antes del `if/else if` | accept | Comportamiento identico al original; un `type` desconocido simplemente no dispara ninguna rama (no hay `else` con ejecucion generica) — mismo nivel de riesgo que hoy |
| T-28-14 | Denial of Service | Backoff exponencial de reconexion (1s -> 30s) | accept | Ya existe en el original, sin cambios; evita flood de reconexiones si el backend cae |
</threat_model>

<verification>
```bash
test -f frontend/js/websocket.js
grep -c "^export " frontend/js/websocket.js
grep -cE "^export (async )?function (updateStat|setCamStatus|showToast)\(" frontend/js/views/dashboard.js
```
</verification>

<success_criteria>
- `websocket.js` existe, exporta solo `connectWS`, bajo 300 lineas.
- Los 10 simbolos importados de 4 modulos distintos son exports reales confirmados por grep, no
  solo por el contrato documentado en `28-PATTERNS.md`.
- `websocket.js` nunca toca `actChart` ni `#rec-badge` directamente.
</success_criteria>

<output>
Al terminar, crear `.planning/phases/28-refactor-del-frontend-a-modulos-es/28-07-SUMMARY.md`.
</output>
