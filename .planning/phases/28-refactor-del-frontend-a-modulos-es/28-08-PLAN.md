---
phase: 28-refactor-del-frontend-a-modulos-es
plan: 08
type: execute
wave: 4
depends_on: [28-01, 28-02, 28-03, 28-04, 28-05, 28-06, 28-07]
files_modified:
  - frontend/js/app.js
  - frontend/app.js
  - frontend/index.html
autonomous: true
requirements: [OPS-01, OPS-02, OPS-03]

must_haves:
  truths:
    - "frontend/index.html no contiene ningun <script> sin src ni ningun <style> inline — solo shell (marcado, contenedores con id, 2 <script> CDN, 3 <link> CSS, 1 <script type=module>)"
    - "frontend/js/app.js es el punto de entrada real: orquesta DOMContentLoaded, importa y llama a los 12 modulos creados en 28-01..28-07 en el mismo orden que el script original"
    - "frontend/app.js (stub de 2 lineas, Fase 6) ya no existe — no queda huerfano junto al entry point real"
    - "La suite completa de pytest pasa en verde, incluyendo los 8 tests de tests/test_frontend_modules.py (contrato de 28-01, ahora completo)"
  artifacts:
    - path: "frontend/js/app.js"
      provides: "Bootstrap real: DOMContentLoaded orquesta bind*/load* de los 12 modulos, en el mismo orden que index.html:1362-2023 original"
      contains: "DOMContentLoaded"
      min_lines: 25
    - path: "frontend/index.html"
      provides: "Shell puro: marcado 121-784 + #clip-modal intactos, cero logica inline"
      contains: "script type=\"module\" src=\"/static/js/app.js\""
  key_links:
    - from: "frontend/index.html"
      to: "frontend/js/app.js"
      via: "<script type=\"module\" src=\"/static/js/app.js\"> servido por StaticFiles (backend/main.py:609, sin cambios)"
      pattern: "script type=\"module\" src=\"/static/js/app.js\""
    - from: "frontend/js/app.js"
      to: "los 12 modulos de 28-01..28-07"
      via: "import estatico + orquestacion completa en DOMContentLoaded"
      pattern: "import .+ from '\\./(views|components)/"
---

<objective>
Cerrar la fase: crear el bootstrap real `frontend/js/app.js` (sustituyendo el stub `frontend/app.js`
de la Fase 6), reescribir `frontend/index.html` para que sea shell puro (sin `<style>` ni `<script>`
inline), y confirmar con la suite completa de pytest que los 12 modulos + 3 CSS creados en
28-01..28-07 quedan correctamente cableados.

Purpose: este es el unico punto de la fase donde es seguro tocar `index.html` — todos los modulos
JS y CSS que necesita ya existen (28-01..28-07), asi que la eliminacion del `<style>`/`<script>`
inline no deja ninguna funcionalidad sin destino.
Output: `frontend/js/app.js` (bootstrap real), `frontend/index.html` reescrito (shell), suite
completa en verde, `frontend/app.js` (stub) eliminado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/28-refactor-del-frontend-a-modulos-es/28-CONTEXT.md
@.planning/phases/28-refactor-del-frontend-a-modulos-es/28-PATTERNS.md
@.planning/phases/28-refactor-del-frontend-a-modulos-es/28-VALIDATION.md

<interfaces>
<!-- Contrato completo de exports de los 12 modulos creados en 28-01..28-07 — app.js los consume
     todos. No es necesario releerlos, este es el contrato final verificado. -->

```javascript
// frontend/js/websocket.js (28-07)
export function connectWS()

// frontend/js/views/dashboard.js (28-02)
export function showToast(msg, type, ms)      // no se importa en app.js, solo lo usan otros modulos
export function updateStat(id, val)            // idem
export function setCamStatus(online)           // idem
export async function loadInitialData()
export async function loadCamStatus()

// frontend/js/views/dashboard-ptz.js (28-03)
export function bindPtzControls()               // incluye loadPresets() internamente

// frontend/js/views/dashboard-events.js (28-02)
export function bindEventFilters()

// frontend/js/views/dashboard-observability.js (28-03)
export async function loadHealth()
export async function loadObservability()

// frontend/js/components/videoCanvas.js (28-04)
export async function loadResolutions()

// frontend/js/components/zoneEditor.js (28-04)
export async function loadZones()
export function bindZoneForm()

// frontend/js/components/eventCard.js (28-05)
export async function loadRecordings()
export function bindEventCardControls()

// frontend/js/components/detectionClasses.js (28-05)
export async function loadDetectionClasses()

// frontend/js/components/personGallery.js (28-06)
export async function loadPersons()
export function bindPersonGallery()
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: frontend/js/app.js (bootstrap real) + eliminar el stub</name>

  <read_first>
    - `frontend/app.js` — stub actual de 2 lineas a sustituir (verificado: ningun `<script>` de
      `index.html` lo referencia hoy, `TEST_app_entry_point_is_real` de 28-01 falla mientras
      siga existiendo junto al `frontend/js/app.js` real).
    - `frontend/index.html:1362-1366,1454-1455,1654-1655,1789,1856,1976-1977,2022-2023` — orden
      real de invocacion original de los `loadX()`/`setInterval` que este fichero reproduce.
    - `.planning/phases/28-refactor-del-frontend-a-modulos-es/28-PATTERNS.md` (seccion
      "frontend/js/app.js (provider, event-driven)") — confirma que ningun `<script>` referencia
      hoy el stub, y el Code Example base (con las 2 correcciones ya aplicadas en el bloque
      `<interfaces>` de este plan: `loadPresets` NO se llama por separado, `bindPtzControls()` ya
      la invoca).
  </read_first>

  <files>frontend/js/app.js, frontend/app.js</files>

  <action>
Crear `frontend/js/app.js`:

```javascript
// frontend/js/app.js
import { connectWS } from './websocket.js';
import { loadInitialData, loadCamStatus } from './views/dashboard.js';
import { bindPtzControls } from './views/dashboard-ptz.js';
import { bindEventFilters } from './views/dashboard-events.js';
import { loadHealth, loadObservability } from './views/dashboard-observability.js';
import { loadResolutions } from './components/videoCanvas.js';
import { loadZones, bindZoneForm } from './components/zoneEditor.js';
import { loadRecordings, bindEventCardControls } from './components/eventCard.js';
import { loadDetectionClasses } from './components/detectionClasses.js';
import { loadPersons, bindPersonGallery } from './components/personGallery.js';

document.addEventListener('DOMContentLoaded', () => {
  // Wiring de listeners — el orden entre si no importa (no hay llamadas de red aqui,
  // solo addEventListener). bindPtzControls() ya invoca loadPresets() internamente
  // (28-03), igual que el script original lo hacia en linea con el resto del bloque PTZ.
  bindPtzControls();
  bindZoneForm();
  bindEventCardControls();
  bindPersonGallery();
  bindEventFilters();

  // Carga inicial — mismo orden que index.html:1362-2023 en el script original, para
  // minimizar diferencias de comportamiento observable (28-PATTERNS.md).
  loadResolutions();
  loadCamStatus();
  loadInitialData();
  connectWS();

  loadRecordings();
  setInterval(loadRecordings, 30000);

  loadPersons();
  setInterval(loadPersons, 30000);

  loadZones();
  loadDetectionClasses();

  loadHealth();
  setInterval(loadHealth, 30000);

  loadObservability();
  setInterval(loadObservability, 5000);
});
```

Despues, eliminar `frontend/app.js` (el stub de 2 lineas de la Fase 6) con el comando de shell:
```bash
rm frontend/app.js
```
No dejarlo huerfano junto al nuevo `frontend/js/app.js` — `TEST_app_entry_point_is_real` (28-01)
falla explicitamente mientras el stub siga presente.

**Puntos obligatorios:**
- `showToast`/`updateStat`/`setCamStatus` NO se importan en `app.js` — nadie los llama desde aqui
  directamente, solo los consumen otros modulos entre si (`websocket.js`, etc.); importarlos sin
  usarlos seria un import muerto.
- `loadInitialData`/`loadCamStatus`/`loadResolutions`/`loadZones`/`loadRecordings`/`loadPersons`/
  `loadDetectionClasses`/`loadHealth`/`loadObservability` se llaman UNA vez cada uno desde aqui
  (mas su `setInterval` donde el original lo tenia) — ninguno se auto-invoca dentro de su propio
  modulo (verificado en 28-02..28-06, cada `<verify>` de esos planes lo confirmo).
  </action>

  <verify>
    <automated>test -f frontend/js/app.js && ! test -f frontend/app.js && [ "$(wc -l < frontend/js/app.js)" -le 300 ] && grep -c "DOMContentLoaded" frontend/js/app.js</automated>
  </verify>

  <acceptance_criteria>
    - El comando de `<verify>` no falla (confirma que `frontend/js/app.js` existe, `frontend/app.js`
      NO existe, y `app.js` orquesta `DOMContentLoaded`).
    - `grep -c "^import" frontend/js/app.js` == 10 (uno por cada modulo consumido).
    - `grep -c "setInterval" frontend/js/app.js` == 4 (`loadRecordings`, `loadPersons`,
      `loadHealth`, `loadObservability` — mismos 4 intervalos que el original, con los mismos
      valores: 30000, 30000, 30000, 5000).
    - `grep -n "loadResolutions();" frontend/js/app.js` aparece ANTES que
      `grep -n "loadInitialData();" frontend/js/app.js` en el fichero (mismo orden que
      `index.html:1362-1365` original — comprobar con `grep -n` y comparar numeros de linea).
  </acceptance_criteria>

  <done>`frontend/js/app.js` existe como bootstrap real (no un stub), orquesta los 12 modulos en
  el orden original, y `frontend/app.js` (stub v1.2) ha sido eliminado.</done>
</task>

<task type="auto">
  <name>Task 2: Reescribir frontend/index.html como shell puro</name>

  <read_first>
    - `frontend/index.html` completo — este plan es el UNICO de la fase que toca este fichero;
      leerlo integro antes de editar (no asumir el estado a partir de lecturas de otros planes,
      el fichero no ha cambiado desde la planificacion pero debe verificarse en el momento de
      ejecutar).
    - `.planning/phases/28-refactor-del-frontend-a-modulos-es/28-PATTERNS.md` (seccion
      "frontend/index.html (route/shell) — qué queda y qué se elimina") — limites exactos
      verificados: `<style>` cierra en la linea 117 (no 120), `<script>` ocupa 786-2025, el modal
      `#clip-modal` (2027-2035) vive DESPUES del `<script>`, fuera del rango de marcado
      "121-784" que tanto CONTEXT.md como RESEARCH.md citan como "todo el marcado" — se preserva
      igual, sin moverlo.
  </read_first>

  <files>frontend/index.html</files>

  <action>
Aplicar 2 ediciones sobre `frontend/index.html`, sin tocar nada mas (ni el marcado 121-784, ni las
2 etiquetas `<script>` CDN de cabecera, ni el modal `#clip-modal`):

**Edicion 1 — sustituir el bloque `<style>...</style>` completo (la linea que empieza en
`  <style>` hasta la linea `  </style>`, contenido identico al que `frontend/css/base.css` +
`frontend/css/components.css` ya capturaron en 28-01) por 3 `<link>` nuevos, insertados justo
donde estaba el `<style>`, antes de `</head>`:**

```html
  <link rel="stylesheet" href="/static/css/base.css">
  <link rel="stylesheet" href="/static/css/layout.css">
  <link rel="stylesheet" href="/static/css/components.css">
```

**Edicion 2 — sustituir el bloque `<script>...</script>` completo (la linea que empieza en
`  <script>`, justo despues del marcado que cierra en la linea 784-785, hasta la linea
`  </script>` — todo el contenido ya extraido a los 12 modulos de 28-01..28-07) por una unica
linea:**

```html
  <script type="module" src="/static/js/app.js"></script>
```

Esta linea queda en la MISMA posicion donde estaba el `<script>` eliminado — es decir,
inmediatamente ANTES del comentario `<!-- Phase 15: clip player modal -->` y el `<div
id="clip-modal">` (que no se tocan ni se mueven).

**No tocar:**
- Las 2 etiquetas `<script src="https://cdn...">` de la cabecera (lineas 7-8) — la de Chart.js
  DEBE seguir en una unica linea con `integrity=` (Pitfall 4 de `28-RESEARCH.md`,
  `tests/test_security_regression.py::TEST_vuln_14_chartjs_cdn_has_subresource_integrity` la
  verifica literalmente).
- Los 2 `<link>` de Google Fonts (lineas 9-10).
- Todo el marcado del `<body>` (121-784) — mismos `id`, mismas clases, sin reformatear.
- El modal `#clip-modal` (2027-2035 en el fichero original) — se preserva byte a byte.
  </action>

  <verify>
    <automated>! grep -q "<style" frontend/index.html && [ "$(grep -c '<script>' frontend/index.html)" -eq 0 ] && grep -q 'script type="module" src="/static/js/app.js"' frontend/index.html && grep -q 'id="clip-modal"' frontend/index.html</automated>
  </verify>

  <acceptance_criteria>
    - El comando de `<verify>` no falla: sin `<style`, sin ningun `<script>` sin atributos (el
      viejo bloque inline), con el `<script type="module">` presente, y `#clip-modal` preservado.
    - `grep -n "chart.js" frontend/index.html | grep -c "integrity="` == 1 (SRI de Chart.js sigue
      en una sola linea).
    - `grep -c 'href="/static/css/'` frontend/index.html devuelve `3` (los 3 `<link>` nuevos).
    - `grep -c "video-feed\|zones-list\|persons-list\|detection-classes-list\|events-list\|recordings-list"` frontend/index.html
      >= 6 (confirma que los `id` del marcado original siguen presentes, sin reescritura).
    - `wc -l frontend/index.html` — documentar el resultado en el SUMMARY (referencia:
      `28-RESEARCH.md` estimo ~685 lineas resultantes; el criterio de exito #1 del ROADMAP ya fue
      redefinido en `28-CONTEXT.md` a "cero logica inline", no a un recuento estricto — no fallar
      esta tarea si el numero real difiere de la estimacion, solo documentarlo).
  </acceptance_criteria>

  <done>`frontend/index.html` es shell puro: cero `<style>`/`<script>` inline salvo las 2 CDN y el
  `<script type="module">`, marcado y `#clip-modal` intactos, SRI de Chart.js preservado.</done>
</task>

<task type="auto">
  <name>Task 3: Puerta mecanica — suite completa en verde</name>

  <read_first>
    - `.planning/phases/28-refactor-del-frontend-a-modulos-es/28-VALIDATION.md` (seccion
      "Criterios de éxito del ROADMAP → comando") — comandos exactos por criterio.
    - `tests/test_frontend_modules.py` (28-01) — los 8 tests que deben pasar ahora todos en verde
      por primera vez en la fase (todos los ficheros que necesitan ya existen tras Task 1/Task 2
      de este plan).
  </read_first>

  <files>Ninguno (tarea de verificacion, no modifica ficheros)</files>

  <action>
Ejecutar, en este orden, y documentar el resultado real (no solo "paso") en el SUMMARY:

1. `.venv/Scripts/python.exe -m pytest tests/test_frontend_modules.py -v` — deben pasar los 8
   tests (por primera vez en la fase todos en verde: `TEST_css_modules_exist`,
   `TEST_js_modules_exist`, `TEST_line_limit`, `TEST_no_inline_logic`,
   `TEST_app_entry_point_is_real`, `TEST_static_js_mime_type`, `TEST_static_css_served`,
   `TEST_root_serves_index_html`).
2. `.venv/Scripts/python.exe -m pytest tests/test_security_regression.py -k chartjs -q` — el SRI
   de Chart.js sigue verde (no se ha tocado su fichero de test, solo se confirma que la
   reescritura de `index.html` no lo rompio).
3. `.venv/Scripts/python.exe -m pytest tests/ -q` — suite completa (519 tests antes de esta fase +
   los 8 nuevos de `test_frontend_modules.py` = 527 esperados; documentar la cifra real en el
   SUMMARY, igual que hicieron `27-06-SUMMARY.md`/`27-07-SUMMARY.md` con sus discrepancias de
   conteo).
4. Documentar en el SUMMARY el recuento real de lineas de `frontend/index.html` tras la
   reescritura (Task 2) frente a la cifra de partida (2038 lineas, verificada en la planificacion
   de esta fase) — cierra formalmente el criterio de exito #1 redefinido por `28-CONTEXT.md`.

Si algun test falla, diagnosticar y corregir el fichero real que corresponda (no relajar el test
para que pase) antes de dar esta tarea por completa.
  </action>

  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/test_frontend_modules.py -q && .venv/Scripts/python.exe -m pytest tests/test_security_regression.py -k chartjs -q</automated>
  </verify>

  <acceptance_criteria>
    - `pytest tests/test_frontend_modules.py -q` reporta `8 passed`.
    - `pytest tests/ -q` no reporta ningun fallo (0 failed); el numero total de tests recogidos es
      >= 519 + 8.
    - El SUMMARY documenta: (a) el recuento real de lineas de `index.html` tras la reescritura,
      (b) el resultado exacto de la suite completa (`N passed`), (c) confirmacion explicita de que
      `TEST_app_entry_point_is_real` verifica tanto que `app.js` es real como que el stub
      desaparecio.
  </acceptance_criteria>

  <done>Los 8 tests de `tests/test_frontend_modules.py` pasan en verde por primera vez en la
  fase, el SRI de Chart.js sigue intacto, y la suite completa del proyecto pasa sin fallos.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Ninguna nueva | Esta tarea reorganiza donde vive el codigo (shell + modulos ES), no cambia que datos se piden, se muestran ni se envian al backend |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-15 | Tampering | Reescritura de `index.html` podria alterar sin querer el CSP efectivo si se toca `backend/main.py` | mitigate | Este plan NO toca `backend/main.py`; el CSP (`script-src ... 'unsafe-inline'`) sigue permitiendo los 2 handlers inline (`onerror` del video, `onclick` de "Reintentar conexión") sin cambios — Pitfall 3 de `28-RESEARCH.md` |
| T-28-16 | Tampering | El nuevo `<script type="module">` es mismo-origen (`'self'`) | accept | Ya permitido por el CSP existente (`script-src 'self' ...`), verificado en `28-RESEARCH.md`; no requiere cambio de backend |
| T-28-17 | Denial of Service | Un error de sintaxis en `app.js` o en cualquier modulo importado rompe el arranque completo del dashboard (a diferencia del script monolitico, donde un error tambien detenia el resto — mismo riesgo, no uno nuevo) | accept | Mismo comportamiento que el script original (un error de parseo detiene todo lo que sigue); Task 3 ejecuta la suite completa y confirma `TEST_static_js_mime_type`/`TEST_root_serves_index_html`, que fallarian si `app.js` no cargara |
</threat_model>

<verification>
```bash
.venv/Scripts/python.exe -m pytest tests/test_frontend_modules.py -v
.venv/Scripts/python.exe -m pytest tests/ -q
```
</verification>

<success_criteria>
- `frontend/index.html` es shell puro (cero logica inline), `frontend/js/app.js` es el entry point
  real, `frontend/app.js` (stub) eliminado.
- Los 8 tests de `tests/test_frontend_modules.py` pasan en verde.
- La suite completa del proyecto pasa sin fallos.
- El SRI de Chart.js y el mount `/static` siguen funcionando sin cambios en `backend/main.py`.
</success_criteria>

<output>
Al terminar, crear `.planning/phases/28-refactor-del-frontend-a-modulos-es/28-08-SUMMARY.md`.
</output>
