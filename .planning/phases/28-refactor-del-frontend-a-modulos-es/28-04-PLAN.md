---
phase: 28-refactor-del-frontend-a-modulos-es
plan: 04
type: execute
wave: 2
depends_on: [28-01]
files_modified:
  - frontend/js/components/videoCanvas.js
  - frontend/js/components/zoneEditor.js
autonomous: true
requirements: [OPS-01, OPS-02]

must_haves:
  truths:
    - "El selector de resolucion sigue reconectando el MJPEG y actualizando el badge #res-badge identico a hoy"
    - "El CRUD de zonas (crear/listar/borrar) sigue funcionando contra /api/zones sin cambios de comportamiento"
    - "#rec-badge y #res-badge tienen un dueño unico (videoCanvas.js) en vez de 3 sitios tocando el DOM por su cuenta"
  artifacts:
    - path: "frontend/js/components/videoCanvas.js"
      provides: "Selector de resolucion + badges REC/resolucion (dueño del overlay de video)"
      exports: ["loadResolutions", "setRecBadge", "setResolutionBadge"]
      min_lines: 40
    - path: "frontend/js/components/zoneEditor.js"
      provides: "CRUD de zonas de interes"
      exports: ["loadZones", "bindZoneForm"]
      min_lines: 60
  key_links:
    - from: "frontend/js/components/videoCanvas.js"
      to: "frontend/js/views/dashboard.js"
      via: "import { showToast } from '../views/dashboard.js'"
      pattern: "import \\{ showToast \\} from '../views/dashboard.js'"
    - from: "frontend/js/components/zoneEditor.js"
      to: "backend (/api/zones)"
      via: "fetch CRUD completo (POST/GET/DELETE)"
      pattern: "fetch\\('/api/zones'"
---

<objective>
Extraer el selector de resolucion (`frontend/index.html:1303-1361`) a
`frontend/js/components/videoCanvas.js` — añadiendo las 2 funciones exportadas nuevas
`setRecBadge`/`setResolutionBadge` que centralizan el overlay de video — y el CRUD de zonas
(`frontend/index.html:1690-1789`) a `frontend/js/components/zoneEditor.js`.

Purpose: `videoCanvas.js` se convierte en el dueño real de `#rec-badge`/`#res-badge`, que hoy se
tocan desde 3 sitios distintos del script monolitico (dispatch WS, `loadRecordings`, seccion de
resolucion) — `websocket.js` (28-07) y `eventCard.js` (28-05) importaran estas 2 funciones en vez
de tocar el DOM del badge por su cuenta.
Output: 2 modulos ES nuevos, cada uno muy por debajo de 300 lineas.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/28-refactor-del-frontend-a-modulos-es/28-PATTERNS.md

<interfaces>
```javascript
// frontend/js/views/dashboard.js — ya existe desde 28-02, consumido aqui
export function showToast(msg, type = 'info', ms = 3500)
```

<!-- Contrato que este plan produce para 28-05 (eventCard.js) y 28-07 (websocket.js): -->
```javascript
// frontend/js/components/videoCanvas.js — exports consumidos por otros modulos
export function setRecBadge(visible, count)     // count opcional; toggle de #rec-badge
export function setResolutionBadge(text)        // texto de #res-badge, '' oculta
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: frontend/js/components/videoCanvas.js</name>

  <read_first>
    - `frontend/index.html:1303-1361` — selector de resolucion completo (`loadResolutions` +
      listener `change`).
    - `frontend/index.html:1181,1183,1187,1190` (rama WebSocket) y `1450` (`loadRecordings`) —
      los 3 sitios que hoy tocan `#rec-badge` directamente; no se tocan en este plan (los
      consumidores se actualizan en 28-05/28-07), pero confirma por que `setRecBadge` necesita
      un parametro `count` opcional (`loadRecordings` pasa un numero, el WS solo hace toggle).
    - `.planning/phases/28-refactor-del-frontend-a-modulos-es/28-PATTERNS.md` (seccion
      "Shared Pattern: badges de vídeo" y "frontend/js/components/videoCanvas.js") — codigo exacto
      de `setRecBadge`/`setResolutionBadge` ya verificado.
  </read_first>

  <files>frontend/js/components/videoCanvas.js</files>

  <action>
Crear `frontend/js/components/videoCanvas.js`:

```javascript
// frontend/js/components/videoCanvas.js
import { showToast } from '../views/dashboard.js';

export function setRecBadge(visible, count) {
  const badge = document.getElementById('rec-badge');
  badge.classList.toggle('hidden', !visible);
  if (count != null) badge.textContent = count;
}

export function setResolutionBadge(text) {
  const badge = document.getElementById('res-badge');
  badge.textContent = text;
  badge.style.display = text ? 'block' : 'none';
}

// ── Resolution dropdown ────────────────────────────────
export async function loadResolutions() {
  try {
    const res = await fetch('/camera/resolutions');
    if (!res.ok) return;
    const data = await res.json();
    const sel  = document.getElementById('resolution-select');
    sel.innerHTML = '';
    data.options.forEach(opt => {
      const el = document.createElement('option');
      el.value = `${opt.width},${opt.height}`;
      el.textContent = opt.label;
      if (opt.width === data.current.width && opt.height === data.current.height) el.selected = true;
      sel.appendChild(el);
    });
    if (data.current.width > 0) {
      setResolutionBadge(`${data.current.width}×${data.current.height}`);
      const img = document.getElementById('video-feed');
      img.style.imageRendering = data.current.width <= 640 ? 'pixelated' : 'auto';
    }
  } catch {}
}

document.getElementById('resolution-select').addEventListener('change', async (e) => {
  const [w, h] = e.target.value.split(',').map(Number);
  const sel = e.target;
  sel.disabled = true;
  try {
    const res = await fetch('/camera/resolution', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ width: w, height: h }),
    });
    if (res.ok) {
      const d = await res.json();
      showToast(`Resolución → ${d.resolution}`, 'success');
      setResolutionBadge(d.resolution);
      const img = document.getElementById('video-feed');
      img.src = '/video_feed?t=' + Date.now();
      img.style.imageRendering = (w > 0 && w <= 640) ? 'pixelated' : 'auto';
    } else {
      const err = await res.json().catch(() => ({}));
      showToast(`Error: ${err.detail ?? 'desconocido'}`, 'error');
    }
  } catch (ex) {
    showToast(`Sin respuesta: ${ex.message}`, 'error');
  } finally {
    sel.disabled = false;
  }
});
```

**Puntos obligatorios:**
- Los handlers `onerror`/`onclick` inline del marcado (`index.html:163,177` — recarga de
  `#video-feed` y boton "Reintentar conexion") **no se tocan**: siguen dependiendo de
  `'unsafe-inline'` en el CSP (Pitfall 3 de `28-RESEARCH.md`); moverlos a `addEventListener` aqui
  esta fuera de alcance de esta fase.
- `loadResolutions` se exporta pero no se auto-invoca — la llama `app.js` (28-08).
- El listener `change` de `resolution-select` SI queda a nivel de modulo (no depende de orden con
  otros modulos, igual que el resto de listeners de un solo elemento en `dashboard.js`).
  </action>

  <verify>
    <automated>test -f frontend/js/components/videoCanvas.js && [ "$(wc -l < frontend/js/components/videoCanvas.js)" -le 300 ] && grep -c "^export " frontend/js/components/videoCanvas.js</automated>
  </verify>

  <acceptance_criteria>
    - El comando de `<verify>` no falla y `grep -c "^export "` devuelve `3` (`setRecBadge`,
      `setResolutionBadge`, `loadResolutions`).
    - `grep -n "import { showToast } from '../views/dashboard.js'" frontend/js/components/videoCanvas.js` acierta.
    - `grep -c "getElementById('res-badge')\|getElementById('rec-badge')" frontend/js/components/videoCanvas.js` >= 2
      (confirma que las 2 funciones de badge tocan el DOM real, no son stubs vacios).
  </acceptance_criteria>

  <done>`frontend/js/components/videoCanvas.js` existe, exporta `loadResolutions`/`setRecBadge`/
  `setResolutionBadge`, y el comportamiento del selector de resolucion es identico al original.</done>
</task>

<task type="auto">
  <name>Task 2: frontend/js/components/zoneEditor.js</name>

  <read_first>
    - `frontend/index.html:1690-1789` — CRUD de zonas completo (`btn-add-zone`, `zone-cancel-btn`,
      `zone-save-btn`, `loadZones`, borrado por `.btn-del-zone`).
    - `.planning/phases/28-refactor-del-frontend-a-modulos-es/28-PATTERNS.md` (seccion
      "frontend/js/components/zoneEditor.js" y la nota sobre `loadZones()`/`loadPersons()`
      interpolando `.name` directo en `innerHTML` — patron preexistente, no se endurece en esta
      fase, cópiese igual).
  </read_first>

  <files>frontend/js/components/zoneEditor.js</files>

  <action>
Crear `frontend/js/components/zoneEditor.js`:

```javascript
// frontend/js/components/zoneEditor.js
import { showToast } from '../views/dashboard.js';

const addZonePanel = document.getElementById('add-zone-panel');
const zoneMsg = document.getElementById('zone-msg');

export function bindZoneForm() {
  document.getElementById('btn-add-zone').addEventListener('click', () => {
    zoneMsg.classList.add('hidden');
    document.getElementById('zone-id-input').value = '';
    document.getElementById('zone-name-input').value = '';
    document.getElementById('zone-points-input').value = '';
    addZonePanel.classList.toggle('hidden');
  });

  document.getElementById('zone-cancel-btn').addEventListener('click', () => {
    addZonePanel.classList.add('hidden');
  });

  document.getElementById('zone-save-btn').addEventListener('click', async () => {
    const zoneId   = document.getElementById('zone-id-input').value.trim();
    const zoneName = document.getElementById('zone-name-input').value.trim();
    const zonePoints = document.getElementById('zone-points-input').value.trim();
    if (!zoneId || !zoneName || !zonePoints) {
      zoneMsg.textContent = 'Todos los campos son obligatorios.';
      zoneMsg.className = 'text-xs text-center text-red-400';
      zoneMsg.classList.remove('hidden');
      return;
    }
    let pts;
    try {
      pts = JSON.parse(zonePoints);
      if (!Array.isArray(pts) || pts.length < 3) throw new Error();
    } catch {
      zoneMsg.textContent = 'Puntos inválidos. Ejemplo: [[0.1,0.1],[0.9,0.1],[0.5,0.9]]';
      zoneMsg.className = 'text-xs text-center text-red-400';
      zoneMsg.classList.remove('hidden');
      return;
    }
    const btn = document.getElementById('zone-save-btn');
    btn.disabled = true; btn.style.opacity = '0.5';
    try {
      const res = await fetch('/api/zones', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({ id: zoneId, name: zoneName, polygon_json: JSON.stringify(pts), enabled: true }),
      });
      if (res.ok) {
        addZonePanel.classList.add('hidden');
        showToast(`Zona "${zoneName}" guardada`, 'success');
        loadZones();
      } else {
        const d = await res.json().catch(() => ({}));
        zoneMsg.textContent = d.detail ?? 'Error al guardar.';
        zoneMsg.className = 'text-xs text-center text-red-400';
        zoneMsg.classList.remove('hidden');
      }
    } catch {
      zoneMsg.textContent = 'Sin respuesta del servidor.';
      zoneMsg.className = 'text-xs text-center text-red-400';
      zoneMsg.classList.remove('hidden');
    } finally {
      btn.disabled = false; btn.style.opacity = '';
    }
  });
}

export async function loadZones() {
  try {
    const res = await fetch('/api/zones');
    if (!res.ok) return;
    const data = await res.json();
    const zones = data.zones ?? [];
    const list  = document.getElementById('zones-list');
    const empty = document.getElementById('zones-empty');
    list.querySelectorAll('.zone-row').forEach(el => el.remove());
    if (zones.length === 0) { empty.style.display = ''; return; }
    empty.style.display = 'none';
    zones.forEach(z => {
      const row = document.createElement('div');
      row.className = 'zone-row flex items-center justify-between px-2.5 py-1.5 rounded-xl bg-slate-800 border border-slate-700';
      const enabledColor = z.enabled ? 'text-cyan-400' : 'text-slate-600';
      row.innerHTML = `
        <div class="flex items-center gap-2 min-w-0">
          <svg class="w-3 h-3 flex-shrink-0 ${enabledColor}" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><polygon points="3 11 22 2 13 21 11 13 3 11"/></svg>
          <span class="text-xs text-slate-200 truncate">${z.name}</span>
          <span class="text-xs text-slate-600 mono">#${z.id}</span>
        </div>
        <button class="btn-del-zone text-slate-600 hover:text-red-400 transition-colors cursor-pointer flex-shrink-0" aria-label="Eliminar zona ${z.name}">
          <svg class="w-3.5 h-3.5" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><polyline points="3 6 5 6 21 6"/><path d="M19 6l-1 14H6L5 6"/><path d="M9 6V4h6v2"/></svg>
        </button>`;
      row.querySelector('.btn-del-zone').addEventListener('click', async () => {
        if (!confirm(`¿Eliminar la zona "${z.name}"?`)) return;
        try {
          const r = await fetch(`/api/zones/${encodeURIComponent(z.id)}`, { method: 'DELETE' });
          if (r.ok) { showToast(`Zona "${z.name}" eliminada`, 'info'); loadZones(); }
          else showToast('Error al eliminar zona', 'error');
        } catch { showToast('Sin respuesta', 'error'); }
      });
      list.appendChild(row);
    });
  } catch {}
}
```

**Puntos obligatorios:**
- `z.name` se interpola directo en `innerHTML` (linea del `<span>` con `${z.name}`) — es el patron
  YA existente hoy (`28-PATTERNS.md`, nota explicita: "no forma parte del alcance de esta fase
  endurecerlo"); cópiese igual, no lo "mejores" con `textContent` porque cambiaria el
  comportamiento observable de forma no pedida.
- `loadZones` se exporta pero no se auto-invoca — la llama `app.js` (28-08), no
  `bindZoneForm()`.
  </action>

  <verify>
    <automated>test -f frontend/js/components/zoneEditor.js && [ "$(wc -l < frontend/js/components/zoneEditor.js)" -le 300 ] && grep -c "^export " frontend/js/components/zoneEditor.js</automated>
  </verify>

  <acceptance_criteria>
    - El comando de `<verify>` no falla y `grep -c "^export "` devuelve `2` (`bindZoneForm`,
      `loadZones`).
    - `grep -n "import { showToast } from '../views/dashboard.js'" frontend/js/components/zoneEditor.js` acierta.
    - `grep -c "fetch('/api/zones" frontend/js/components/zoneEditor.js` >= 3 (GET, POST y DELETE
      con template literal, confirma el CRUD completo).
  </acceptance_criteria>

  <done>`frontend/js/components/zoneEditor.js` existe, exporta `loadZones`/`bindZoneForm`, y el
  CRUD completo de zonas (crear/listar/borrar) es identico al original.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Navegador -> `/api/zones` | El operador introduce `id`/`name`/`points` de una zona; el backend ya valida (JSON, minimo 3 puntos se comprueba tambien en el cliente antes de enviar, defensa en profundidad) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-07 | Tampering | `zoneEditor.js::loadZones` interpola `z.name` directo en `innerHTML` | accept | Patron preexistente en el codigo actual (no introducido por esta fase); `z.name` viene de zonas creadas por el propio operador autenticado en LAN, no de un tercero no confiable — endurecerlo esta fuera de alcance de una extraccion 1:1 |
| T-28-08 | Tampering (XSS) | Badges de video (`setRecBadge`/`setResolutionBadge`) | mitigate | Usan `textContent`, nunca `innerHTML`, para el texto de resolucion/contador que en el futuro podria derivar de datos del backend |
</threat_model>

<verification>
```bash
test -f frontend/js/components/videoCanvas.js && test -f frontend/js/components/zoneEditor.js
grep -c "^export " frontend/js/components/videoCanvas.js frontend/js/components/zoneEditor.js
```
</verification>

<success_criteria>
- `videoCanvas.js` exporta `loadResolutions`/`setRecBadge`/`setResolutionBadge`; `zoneEditor.js`
  exporta `loadZones`/`bindZoneForm`; ambos bajo 300 lineas.
- `#rec-badge`/`#res-badge` solo se tocan por `id` directo dentro de `videoCanvas.js` en este
  plan (los demas consumidores se actualizan en 28-05/28-07 para usar las funciones exportadas).
</success_criteria>

<output>
Al terminar, crear `.planning/phases/28-refactor-del-frontend-a-modulos-es/28-04-SUMMARY.md`.
</output>
