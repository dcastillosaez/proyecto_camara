---
phase: 28-refactor-del-frontend-a-modulos-es
plan: 06
type: execute
wave: 2
depends_on: [28-01]
files_modified:
  - frontend/js/components/personGallery.js
  - frontend/js/api.js
autonomous: true
requirements: [OPS-01, OPS-02]

must_haves:
  truths:
    - "El panel de personas conocidas, el modal de enrolamiento (frame actual o subida) y la galeria de capturas funcionan identico a hoy"
    - "frontend/js/api.js existe como el fichero locked por ADR-08, con un wrapper fetch reutilizable para consumo futuro"
  artifacts:
    - path: "frontend/js/components/personGallery.js"
      provides: "Personas reconocidas + modal de enrolamiento + galeria de capturas"
      exports: ["loadPersons", "openGallery", "bindPersonGallery"]
      min_lines: 100
    - path: "frontend/js/api.js"
      provides: "Wrapper fetch fino (apiFetch) — sin bloque unico equivalente en index.html, abstraccion nueva sintetizada de ~25 repeticiones del mismo patron try/catch"
      exports: ["apiFetch"]
      min_lines: 5
  key_links:
    - from: "frontend/js/components/personGallery.js"
      to: "frontend/js/views/dashboard.js"
      via: "import { showToast } from '../views/dashboard.js'"
      pattern: "import \\{ showToast \\} from '../views/dashboard.js'"
---

<objective>
Extraer el panel de personas conocidas + modal de enrolamiento + galeria de capturas
(`frontend/index.html:1518-1687`) a `frontend/js/components/personGallery.js`, y crear
`frontend/js/api.js` — el unico fichero de la fase sin analog directo en `index.html` (no existe
hoy un wrapper `fetch`, es una abstraccion nueva sintetizada de ~25 repeticiones del mismo patron
try/catch, `28-PATTERNS.md`).

Purpose: cerrar la estructura de directorios LOCKED por ADR-08 (`frontend/js/api.js` es el ultimo
fichero de nivel superior que falta) sin forzar una reescritura de los ~25 call-sites `fetch` ya
escritos en 28-02..28-05 — esa reescritura no es una extraccion 1:1 y queda fuera de esta fase
(decision documentada abajo, dentro del margen de "Claude's Discretion" que fija `28-RESEARCH.md`).
Output: 2 ficheros nuevos, cada uno bajo 300 lineas.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/28-refactor-del-frontend-a-modulos-es/28-PATTERNS.md

<interfaces>
```javascript
// frontend/js/views/dashboard.js — ya existe desde 28-02, consumido aqui
export function showToast(msg, type = 'info', ms = 3500)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: frontend/js/components/personGallery.js</name>

  <read_first>
    - `frontend/index.html:1518-1656` — panel de personas + modal de enrolamiento completo
      (`loadPersons`, chips de personas conocidas, `enrollForm` submit con `FormData`).
    - `frontend/index.html:1657-1687` — modal de galeria (`openGallery`, cierre por click fuera).
  </read_first>

  <files>frontend/js/components/personGallery.js</files>

  <action>
Crear `frontend/js/components/personGallery.js`:

```javascript
// frontend/js/components/personGallery.js
import { showToast } from '../views/dashboard.js';

// ── Known persons panel ────────────────────────────────
export async function loadPersons() {
  try {
    const res = await fetch('/persons');
    if (!res.ok) return;
    const data = await res.json();
    const list = document.getElementById('persons-list');
    const empty = document.getElementById('persons-empty');
    const named = data.persons.filter(p => p.name && !p.name.startsWith('Person '));
    if (named.length === 0) {
      empty.style.display = '';
      return;
    }
    empty.style.display = 'none';
    list.querySelectorAll('.person-row').forEach(el => el.remove());
    named.forEach(p => {
      const row = document.createElement('div');
      row.className = 'person-row flex items-center justify-between px-2.5 py-1.5 rounded-xl bg-slate-800 border border-slate-700';
      const ts = p.last_seen ? new Date(p.last_seen).toLocaleDateString('es', { day:'2-digit', month:'short' }) : '—';
      row.innerHTML = `
        <div class="flex items-center gap-2 min-w-0">
          <div class="w-6 h-6 rounded-full bg-blue-500/20 border border-blue-500/30 flex items-center justify-center flex-shrink-0">
            <svg class="w-3 h-3 text-blue-400" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><circle cx="12" cy="8" r="4"/><path d="M20 21a8 8 0 1 0-16 0"/></svg>
          </div>
          <span class="text-xs font-medium text-slate-200 truncate">${p.name}</span>
        </div>
        <div class="flex items-center gap-2 flex-shrink-0">
          <span class="text-xs text-slate-600">${p.visit_count}×</span>
          <span class="text-xs text-slate-600">${ts}</span>
          <button class="btn-gallery text-xs text-slate-500 hover:text-blue-400 transition-colors cursor-pointer" title="Ver capturas" aria-label="Ver capturas de ${p.name}">
            <svg class="w-3.5 h-3.5" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><rect x="3" y="3" width="18" height="18" rx="2"/><circle cx="8.5" cy="8.5" r="1.5"/><polyline points="21 15 16 10 5 21"/></svg>
          </button>
        </div>`;
      row.querySelector('.btn-gallery').addEventListener('click', () => openGallery(p.id, p.name));
      list.appendChild(row);
    });
  } catch {}
}

// Enroll modal
const enrollModal = document.getElementById('enroll-modal');
const enrollForm  = document.getElementById('enroll-form');
const enrollError = document.getElementById('enroll-error');
const uploadWrap  = document.getElementById('enroll-upload-wrap');

// Gallery modal
const galleryModal = document.getElementById('gallery-modal');

export async function openGallery(personId, personName) {
  document.getElementById('gallery-title').textContent = `Capturas — ${personName}`;
  const grid = document.getElementById('gallery-grid');
  const empty = document.getElementById('gallery-empty');
  grid.querySelectorAll('img').forEach(el => el.remove());
  empty.style.display = '';
  galleryModal.classList.remove('hidden');
  try {
    const res = await fetch(`/persons/${personId}/captures?limit=30`);
    if (!res.ok) return;
    const data = await res.json();
    const captures = data.captures ?? [];
    if (captures.length === 0) return;
    empty.style.display = 'none';
    captures.forEach(c => {
      const img = document.createElement('img');
      img.src = c.url;
      img.alt = `Captura ${c.timestamp}`;
      img.className = 'gallery-thumb';
      img.title = new Date(c.timestamp).toLocaleString('es-ES');
      img.loading = 'lazy';
      img.addEventListener('click', () => window.open(c.url, '_blank'));
      grid.insertBefore(img, empty);
    });
  } catch {}
}

export function bindPersonGallery() {
  document.getElementById('btn-enroll').addEventListener('click', async () => {
    enrollError.classList.add('hidden');
    enrollForm.reset();
    uploadWrap.classList.add('hidden');
    const knownWrap = document.getElementById('enroll-known-wrap');
    const knownList = document.getElementById('enroll-known-list');
    knownList.innerHTML = '';
    try {
      const res = await fetch('/persons');
      const data = await res.json();
      const named = (data.persons || []).filter(p => p.name && !p.name.startsWith('Person '));
      if (named.length > 0) {
        named.forEach(p => {
          const chip = document.createElement('button');
          chip.type = 'button';
          chip.textContent = `${p.name} (${p.sample_count ?? 1})`;
          chip.className = 'text-xs px-2.5 py-1 rounded-lg bg-slate-700 hover:bg-blue-600 text-slate-300 hover:text-white transition-colors cursor-pointer';
          chip.addEventListener('click', () => {
            document.getElementById('enroll-name').value = p.name;
            knownList.querySelectorAll('button').forEach(b => b.classList.remove('bg-blue-600', 'text-white'));
            chip.classList.add('bg-blue-600', 'text-white');
          });
          knownList.appendChild(chip);
        });
        knownWrap.classList.remove('hidden');
      } else {
        knownWrap.classList.add('hidden');
      }
    } catch (_) { knownWrap.classList.add('hidden'); }
    enrollModal.classList.remove('hidden');
  });

  document.getElementById('enroll-close').addEventListener('click', () => {
    enrollModal.classList.add('hidden');
  });

  enrollModal.addEventListener('click', e => {
    if (e.target === enrollModal) enrollModal.classList.add('hidden');
  });

  enrollForm.querySelectorAll('input[name="enroll-source"]').forEach(radio => {
    radio.addEventListener('change', () => {
      uploadWrap.classList.toggle('hidden', radio.value !== 'upload');
    });
  });

  enrollForm.addEventListener('submit', async e => {
    e.preventDefault();
    enrollError.classList.add('hidden');
    const btn = document.getElementById('enroll-submit');
    btn.disabled = true;
    btn.textContent = 'Registrando…';

    const name   = document.getElementById('enroll-name').value.trim();
    const source = enrollForm.querySelector('input[name="enroll-source"]:checked').value;
    const fd     = new FormData();
    fd.append('name', name);

    if (source === 'frame') {
      fd.append('use_current_frame', 'true');
    } else {
      const file = document.getElementById('enroll-file').files[0];
      if (!file) {
        enrollError.textContent = 'Selecciona una imagen.';
        enrollError.classList.remove('hidden');
        btn.disabled = false; btn.textContent = 'Registrar';
        return;
      }
      fd.append('image', file);
    }

    try {
      const res = await fetch('/api/enroll_face', { method: 'POST', body: fd });
      const data = await res.json();
      if (res.ok) {
        enrollModal.classList.add('hidden');
        showToast(`"${data.name}" registrado (ID ${data.person_id})`, 'success');
        loadPersons();
      } else {
        enrollError.textContent = data.detail ?? 'Error desconocido';
        enrollError.classList.remove('hidden');
      }
    } catch (ex) {
      enrollError.textContent = `Sin respuesta: ${ex.message}`;
      enrollError.classList.remove('hidden');
    } finally {
      btn.disabled = false; btn.textContent = 'Registrar';
    }
  });

  document.getElementById('gallery-close').addEventListener('click', () => galleryModal.classList.add('hidden'));
  galleryModal.addEventListener('click', e => { if (e.target === galleryModal) galleryModal.classList.add('hidden'); });
}
```

**Puntos obligatorios:**
- `p.name` se interpola directo en `innerHTML` (igual que `z.name` en `zoneEditor.js`, 28-04) —
  patron preexistente documentado en `28-PATTERNS.md`, no se endurece en esta fase.
- `loadPersons` se exporta pero no se auto-invoca ni registra su propio `setInterval` — los
  registra `app.js` (28-08): `loadPersons(); setInterval(loadPersons, 30000);`.
  </action>

  <verify>
    <automated>test -f frontend/js/components/personGallery.js && [ "$(wc -l < frontend/js/components/personGallery.js)" -le 300 ] && grep -c "^export " frontend/js/components/personGallery.js</automated>
  </verify>

  <acceptance_criteria>
    - El comando de `<verify>` no falla y `grep -c "^export "` devuelve `3` (`loadPersons`,
      `openGallery`, `bindPersonGallery`).
    - `grep -n "import { showToast } from '../views/dashboard.js'" frontend/js/components/personGallery.js` acierta.
    - `grep -c "FormData" frontend/js/components/personGallery.js` >= 1 (enrolamiento con
      `use_current_frame` o subida de fichero, preservado).
  </acceptance_criteria>

  <done>`frontend/js/components/personGallery.js` existe, exporta las 3 funciones del contrato, y
  el flujo de enrolamiento (frame actual o subida) es identico al original.</done>
</task>

<task type="auto">
  <name>Task 2: frontend/js/api.js</name>

  <read_first>
    - `.planning/phases/28-refactor-del-frontend-a-modulos-es/28-PATTERNS.md` (seccion
      "frontend/js/api.js (service, request-response) — SIN ANALOG DIRECTO") — confirma que no
      existe hoy un wrapper `fetch` en `index.html`; el patron se repite ~25 veces
      (`fetchCounts`, `ptzMove`, `loadPresets`, `loadZones`, `loadPersons`, etc.) siempre con la
      misma forma try/fetch/res.ok/catch.
  </read_first>

  <files>frontend/js/api.js</files>

  <action>
Crear `frontend/js/api.js` con el wrapper fino que autoriza `28-CONTEXT.md` ("puede empezar como
un wrapper fino... sin necesidad de tipado real"):

```javascript
// frontend/js/api.js
export async function apiFetch(path, opts = {}) {
  const res = await fetch(path, opts);
  if (!res.ok) {
    const err = await res.json().catch(() => ({ detail: res.statusText }));
    throw new Error(err.detail ?? res.statusText);
  }
  return res.status === 204 ? null : res.json();
}
```

**Decision explicita de alcance (Claude's Discretion, `28-RESEARCH.md`):** este plan NO reescribe
los ~25 call-sites `fetch` ya escritos en 28-02..28-05 para usar `apiFetch` — eso implicaria tocar
codigo ya extraido y verificado en planes anteriores por una razon de estilo, no una extraccion
1:1, y arriesgaria una regresion de comportamiento (cada call-site tiene su propio manejo de
error/mensaje al usuario, no intercambiable mecanicamente con `throw new Error`). `api.js` existe
porque la estructura de ficheros esta LOCKED por ADR-08 y queda disponible para que fases
posteriores (29+) lo adopten en codigo nuevo. Documentar esta decision en el SUMMARY.
  </action>

  <verify>
    <automated>test -f frontend/js/api.js && [ "$(wc -l < frontend/js/api.js)" -le 300 ] && grep -c "^export " frontend/js/api.js</automated>
  </verify>

  <acceptance_criteria>
    - El comando de `<verify>` no falla y `grep -c "^export "` devuelve `1` (`apiFetch`).
    - `grep -n "export async function apiFetch" frontend/js/api.js` acierta.
    - `git diff --stat frontend/js/views frontend/js/components` (contra el estado tras 28-02..28-05)
      esta vacio — confirma que esta tarea no reescribe ningun modulo ya creado.
  </acceptance_criteria>

  <done>`frontend/js/api.js` existe con `apiFetch` exportado, sin tocar ningun modulo ya creado en
  planes anteriores.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Navegador -> `/api/enroll_face` | El operador sube una imagen o usa el frame actual para enrolar una persona; la validacion real (formato, tamaño) ya vive en el backend, sin cambios en esta fase |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-11 | Tampering | `personGallery.js::loadPersons` interpola `p.name` directo en `innerHTML` | accept | Patron preexistente (no introducido por esta fase); `p.name` proviene de personas enroladas por el propio operador en LAN |
| T-28-12 | Information Disclosure | `api.js::apiFetch` propaga `err.detail` del backend en el mensaje de `Error` | accept | Mismo nivel de detalle que ya exponen los ~25 call-sites `fetch` existentes; no añade informacion nueva |
</threat_model>

<verification>
```bash
test -f frontend/js/components/personGallery.js && test -f frontend/js/api.js
grep -c "^export " frontend/js/components/personGallery.js frontend/js/api.js
```
</verification>

<success_criteria>
- `personGallery.js` exporta las 3 funciones del contrato; `api.js` exporta `apiFetch`; ambos
  bajo 300 lineas.
- Ningun modulo creado en 28-02..28-05 se modifica en este plan.
</success_criteria>

<output>
Al terminar, crear `.planning/phases/28-refactor-del-frontend-a-modulos-es/28-06-SUMMARY.md`.
</output>
