---
phase: 27-multi-clase-y-contexto-de-escena
plan: 08
type: execute
wave: 4
depends_on: [27-03, 27-06]
files_modified:
  - backend/pipeline/streaming.py
  - backend/pipeline/manager.py
  - tests/test_streaming_worker.py
  - tests/test_detection_worker.py
autonomous: true
requirements: [BEH-06]

must_haves:
  truths:
    - "StreamingWorker dibuja las cajas de objeto que devuelve su proveedor, con un color distinto al de las personas y al de las zonas"
    - "Sin proveedor de objetos (default None) o con la lista vacia, el frame anotado es identico al de hoy"
    - "streaming.py sigue sin mencionar PersonDetector ni detect_sv (test de regresion existente)"
    - "CameraPipeline conecta StreamingWorker al mismo get_object_boxes() que ya expone para el resto de la API, sin logica duplicada"
    - "Un reinicio del StreamingWorker por el supervisor sigue usando el proveedor correcto (misma referencia de metodo, no un snapshot congelado)"
  artifacts:
    - path: "backend/pipeline/streaming.py"
      provides: "Parametro object_boxes inyectado + dibujo en _annotate con color propio"
      contains: "object_boxes"
    - path: "backend/pipeline/manager.py"
      provides: "_make_streaming pasa self.get_object_boxes a StreamingWorker"
      contains: "object_boxes=self.get_object_boxes"
  key_links:
    - from: "backend/pipeline/manager.py"
      to: "backend/pipeline/streaming.py"
      via: "inyeccion de solo lectura de self.get_object_boxes (CameraPipeline, 27-06), nunca un segundo registry"
      pattern: "object_boxes=self\\.get_object_boxes"
---

<objective>
Dibujar las cajas de los objetos trackeados sobre el feed MJPEG, con un color distinto al
de las personas y al de las zonas, para que el criterio 1 del ROADMAP ("clases configurables
desde la UI") tenga evidencia visual inmediata al marcar una clase nueva.

Purpose: decision ya cerrada con el usuario (Open Question #1 de 27-RESEARCH.md): SI dibujar
los objetos. La via elegida es **pull**, no push: `set_zone_overlay` es un patron escrito
pero sin ni un solo llamador hoy (27-PATTERNS.md § No Analog Found), mientras que la
inyeccion de una referencia de solo lectura en el constructor (como ya se hace con
`registry`) es el patron vivo del fichero. `StreamingWorker` no debe importar nada de
`ObjectAnalyzer`/`ObjectTracker`/`DetectionWorker` — recibe un `Callable[[], list[dict]]`
generico, exactamente la forma de `CameraPipeline.get_object_boxes()` que 27-06 ya expone.
Output: `StreamingWorker` con dibujo de objetos + cableado en `manager.py` + 5 tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/27-multi-clase-y-contexto-de-escena/27-PATTERNS.md
@.planning/phases/27-multi-clase-y-contexto-de-escena/27-06-SUMMARY.md
@backend/pipeline/streaming.py
@backend/pipeline/manager.py

<interfaces>
<!-- CONTRATO LOCKED. Nadie mas depende de esto en la fase, pero fija la forma del
     unico punto de acoplamiento streaming <-> objetos permitido. -->

```python
# backend/pipeline/streaming.py
class StreamingWorker:
    def __init__(
        self,
        sub: Subscription,
        registry: TrackRegistry,
        tracker: PersonTracker,
        object_boxes: Callable[[], list[dict]] | None = None,   # NUEVO (Fase 27)
    ) -> None: ...
```

`object_boxes()` devuelve el mismo shape que `DetectionWorker.get_object_boxes()`
(27-03/27-06): `[{"track_id": int, "class_id": int, "class_name": str,
"bbox": (x1, y1, x2, y2)}, ...]`.

**Color:** las zonas ya usan `(0, 200, 255)` (naranja, BGR) y `PersonTracker.annotate`
dibuja personas en los colores de `sv.BoxAnnotator`. Los objetos usan **magenta
`(255, 0, 255)`** (BGR), sugerido por 27-RESEARCH.md Open Question #1.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: StreamingWorker — parametro `object_boxes` + dibujo en `_annotate`</name>

  <read_first>
    - `backend/pipeline/streaming.py:39-57` — constructor actual: `sub`, `registry`,
      `tracker`, y el estado `_zone_overlay` ya inyectado por setter (patron a NO copiar
      aqui: `set_zone_overlay` no tiene llamadores, ver `27-PATTERNS.md § No Analog Found`).
    - `backend/pipeline/streaming.py:132-163` — `_annotate` completo: el bloque de
      personas (`self._tracker.annotate(...)`), el snapshot bajo lock de `_zone_overlay`
      y el bucle de dibujo de zonas con `cv2.polylines`/`cv2.putText` y color propio. Es
      el molde exacto para el bloque de objetos.
    - `tests/test_streaming_worker.py:72-76` — `test_overlay_uses_registry_not_detector`:
      el fichero NO puede mencionar `PersonDetector` ni `detect_sv`, ni en comentarios.
  </read_first>

  <files>backend/pipeline/streaming.py</files>

  <action>
**1. Import.** Añadir `Callable` al import de `typing` (linea 12):
`from typing import TYPE_CHECKING, Callable`.

**2. Constructor.** Añadir el parametro al final de la firma, con default `None`, y
guardarlo con un comentario que explique por que es un callable y no un dict compartido:

```python
    def __init__(
        self,
        sub: Subscription,
        registry: TrackRegistry,
        tracker: PersonTracker,
        object_boxes: Callable[[], list[dict]] | None = None,
    ) -> None:
        self._sub = sub
        self._registry = registry
        self._tracker = tracker
        # Objetos trackeados a dibujar (Fase 27, BEH-06). Callable de solo lectura, NO un
        # segundo registry: CameraPipeline.get_object_boxes ya delega en
        # DetectionWorker.get_object_boxes() bajo self._lock (27-03/27-06). Duplicar ese
        # estado aqui divergiria del que usa /api/v2/analytics/context (27-09).
        self._object_boxes = object_boxes
        ...
```

**3. Dibujo en `_annotate`**, justo despues del bucle de zonas (`streaming.py:154-163`),
antes del `return out`:

```python
        if self._object_boxes is not None:
            for b in self._object_boxes():
                x1, y1, x2, y2 = map(int, b["bbox"])
                cv2.rectangle(out, (x1, y1), (x2, y2), (255, 0, 255), 2)
                cv2.putText(
                    out, f'{b["class_name"]} #{b["track_id"]}', (x1, max(0, y1 - 6)),
                    cv2.FONT_HERSHEY_SIMPLEX, 0.45, (255, 0, 255), 1,
                )
        return out
```

`(255, 0, 255)` (magenta, BGR) es deliberadamente distinto del naranja de las zonas
`(0, 200, 255)` y de los colores que usa `sv.BoxAnnotator` para personas. `max(0, y1 - 6)`
evita que la etiqueta salga fuera del frame cuando la caja toca el borde superior.

**Nada de `set_zone_overlay` como molde**: es push y sin llamadores; aqui la via es pull
(inyeccion en el constructor), igual que `registry` y `tracker`. **Nada de importar
`ObjectAnalyzer`, `ObjectTracker` ni `DetectionWorker`**: `object_boxes` es un
`Callable[[], list[dict]]` generico, `streaming.py` no sabe de donde viene.
  </action>

  <verify>
    <automated>F:/Documentos/IA/Proyecto_Camara/.venv/Scripts/python.exe -c "import inspect; from backend.pipeline.streaming import StreamingWorker; sig = inspect.signature(StreamingWorker.__init__); assert 'object_boxes' in sig.parameters; assert sig.parameters['object_boxes'].default is None; src = open('backend/pipeline/streaming.py', encoding='utf-8').read(); assert 'PersonDetector' not in src and 'detect_sv' not in src; assert '(255, 0, 255)' in src; print('OK')"</automated>
  </verify>

  <acceptance_criteria>
    - `grep -n "object_boxes: Callable" backend/pipeline/streaming.py` acierta en el constructor.
    - `grep -n "255, 0, 255" backend/pipeline/streaming.py` acierta dentro de `_annotate`.
    - `grep -n "PersonDetector\|detect_sv" backend/pipeline/streaming.py` NO acierta.
    - `pytest tests/test_streaming_worker.py -k regexp_not_detector -q` no aplica (nombre real es `test_overlay_uses_registry_not_detector`) — usar `pytest tests/test_streaming_worker.py -k not_detector -q`, sigue verde.
  </acceptance_criteria>

  <done>`StreamingWorker` acepta `object_boxes` y dibuja las cajas de objeto en magenta cuando el proveedor las devuelve, sin acoplarse a ningun tipo de la fase.</done>
</task>

<task type="auto">
  <name>Task 2: manager.py — cablear `self.get_object_boxes` a `StreamingWorker`</name>

  <read_first>
    - `backend/pipeline/manager.py:142-152` — `_make_streaming` completo: rescata
      `clients` del worker anterior (para no perder clientes conectados en un reinicio) y
      construye `StreamingWorker(sub, self.registry, tracker)`. Es el unico sitio a tocar.
    - `backend/pipeline/manager.py` (tras 27-06) — `def get_object_boxes(self) -> list[dict]`
      ya existe en `CameraPipeline` (fachada añadida en 27-06-PLAN.md), delegando en
      `self.detection.get_object_boxes() if self.detection else []`.
  </read_first>

  <files>backend/pipeline/manager.py</files>

  <action>
Una linea dentro de `_make_streaming`:

```python
            def _make_streaming() -> StreamingWorker:
                clients = self.streaming.stats["clients"] if self.streaming else 0
                self.streaming = StreamingWorker(
                    self.broker.subscribe("streaming", replace=True), self.registry, tracker,
                    object_boxes=self.get_object_boxes,  # NUEVO (Fase 27): solo lectura
                )
                for _ in range(clients):
                    self.streaming.client_connected()
                return self.streaming
```

`self.get_object_boxes` es un metodo BOUND de `CameraPipeline` (no una funcion libre):
en cada llamada consulta `self.detection` en ese instante, asi que sigue funcionando
correctamente aunque `self.detection` se reconstruya en un reinicio del `DetectionWorker`
— no hace falta volver a pasar la referencia, es la misma cada vez que `_make_streaming`
se re-ejecuta. No crear una funcion nueva ni duplicar la guarda `if self.detection`: ya
vive en `get_object_boxes` desde 27-06.
  </action>

  <verify>
    <automated>F:/Documentos/IA/Proyecto_Camara/.venv/Scripts/python.exe -c "import inspect; from backend.pipeline.manager import CameraPipeline; src = inspect.getsource(CameraPipeline); assert 'object_boxes=self.get_object_boxes' in src; print('OK')"</automated>
  </verify>

  <acceptance_criteria>
    - `grep -n "object_boxes=self.get_object_boxes" backend/pipeline/manager.py` acierta dentro de `_make_streaming`.
    - `git diff backend/pipeline/manager.py | grep "^-" | grep -v "^---"` esta vacio (solo se añade una linea).
  </acceptance_criteria>

  <done>`StreamingWorker` recibe la misma fuente de verdad de objetos que ya usa el resto de la API, sin logica nueva en `manager.py`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Tests — dibujo del overlay y cableado del proveedor</name>

  <read_first>
    - `tests/test_streaming_worker.py:19-30` — `_make_frame` y `_tracker_mock` (el
      `annotate` mockeado devuelve el frame sin cambios, asi que cualquier diferencia en
      el resultado de `_annotate` viene del bloque de objetos/zonas, no de las personas).
    - `tests/test_streaming_worker.py:72-76` — el test de regresion que no debe romperse.
    - `tests/test_detection_worker.py:400-438` (tras 27-06) —
      `TEST_behavior_analyzer_survives_worker_restart` /
      `TEST_object_analyzer_survives_worker_restart`: el patron de invocar la factoria del
      supervisor dos veces y comparar identidad de instancia/referencia — aqui se usa para
      comprobar que la factoria de `streaming` pasa el proveedor correcto.
  </read_first>

  <files>tests/test_streaming_worker.py, tests/test_detection_worker.py</files>

  <behavior>
    - `TEST_object_overlay_drawn_when_boxes_present`: `object_boxes=lambda: [...]` con una caja -> `_annotate(frame)` devuelve una imagen distinta del frame de entrada (`not np.array_equal`).
    - `TEST_no_object_overlay_when_provider_returns_empty`: `object_boxes=lambda: []` con registro vacio y sin zonas -> `_annotate(frame)` devuelve una imagen IGUAL al frame de entrada.
    - `TEST_streaming_worker_without_object_boxes_provider`: sin pasar `object_boxes` (default `None`) -> `_annotate` no lanza y el frame sale igual al de entrada (compatibilidad con las llamadas existentes de 5 posicionales).
    - `TEST_streaming_factory_wires_object_boxes_provider` (en `test_detection_worker.py`): la factoria `_make_streaming` del supervisor construye un `StreamingWorker` cuyo `_object_boxes` ES `pipeline.get_object_boxes` (misma referencia de metodo bound, comparacion `==`).
  </behavior>

  <action>
**A. `tests/test_streaming_worker.py`** — tres tests nuevos, banda de comentario:

```python
# ─── Overlay de objetos: color propio, via pull (Fase 27, BEH-06) ────────────
```

`_annotate` se llama directamente sobre un `np.zeros((360, 640, 3), dtype=np.uint8)`, sin
pasar por el hilo (`_loop`) ni por el broker — igual de rapido que instanciar el worker.
Usar `TrackRegistry()` vacio y `_tracker_mock()` para aislar el efecto del bloque de
objetos.

**B. `tests/test_detection_worker.py`** — un test que reutiliza el patron ya establecido
en 27-06 de invocar dos veces la factoria del supervisor:

```python
def TEST_streaming_factory_wires_object_boxes_provider():
    from backend.pipeline.manager import CameraPipeline
    pipeline = CameraPipeline("cam1", "rtsp://fake", detector=MagicMock(), tracker=MagicMock())
    factory = pipeline.supervisor._entries["streaming"].factory
    worker = factory()
    assert worker._object_boxes == pipeline.get_object_boxes
```

Los metodos bound de Python comparan iguales cuando comparten `__self__` y `__func__`, asi
que este assert falla si alguien pasa una lambda nueva en vez de la referencia real.

Nombres `TEST_*` con "object" u "overlay" para que `pytest -k object` los recoja junto a los
de 27-03/27-06.
  </action>

  <verify>
    <automated>F:/Documentos/IA/Proyecto_Camara/.venv/Scripts/python.exe -m pytest tests/test_streaming_worker.py tests/test_detection_worker.py -q</automated>
  </verify>

  <acceptance_criteria>
    - `pytest tests/test_streaming_worker.py -k object_overlay -q` recoge >= 3 tests y sale verde.
    - `pytest tests/test_detection_worker.py -k streaming_factory -q` recoge 1 test y sale verde.
    - `pytest tests/test_streaming_worker.py -q` verde entero, incluido `test_overlay_uses_registry_not_detector`.
    - `pytest tests/ -q` sin regresion (se toca `manager.py`, que afecta a toda la construccion de `CameraPipeline`).
  </acceptance_criteria>

  <done>El dibujo de objetos esta probado en aislamiento (`_annotate`) y el cableado de `manager.py` esta probado con la misma tecnica de identidad de referencia que ya usan `27-01`/`27-06`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `DetectionWorker` (hilo) -> `StreamingWorker` (hilo) | Estado de objetos leido por otro hilo a traves de una funcion, nunca escrito |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-33 | Tampering | `StreamingWorker` mutando el estado de objetos que lee | mitigate | `get_object_boxes()` (27-03/27-06) ya devuelve copias (`dict(b)` por elemento); `StreamingWorker` solo lee, nunca escribe en la lista devuelta |
| T-27-34 | Information Disclosure | overlay de objetos filtrando datos sensibles | accept | Solo dibuja `class_name`/`track_id`/`bbox`, el mismo shape que ya expone `/api/v2/analytics/context` (27-09); nada de identidad de persona |
| T-27-35 | Denial of Service | acoplamiento streaming/deteccion via import circular | mitigate | `object_boxes` es un `Callable[[], list[dict]]` inyectado, `streaming.py` no importa nada de `perception/`, `pipeline/detection.py` ni `pipeline/manager.py`. Verificado por el test de regresion existente |
</threat_model>

<verification>
```bash
F:/Documentos/IA/Proyecto_Camara/.venv/Scripts/python.exe -m pytest tests/test_streaming_worker.py tests/test_detection_worker.py tests/test_architecture.py -q
```
</verification>

<success_criteria>
- `StreamingWorker` dibuja las cajas de objeto en magenta cuando el proveedor las devuelve, y no altera el frame cuando no hay objetos o no hay proveedor.
- `manager.py` conecta `StreamingWorker` a `self.get_object_boxes` (27-06), sin logica nueva ni segundo registry.
- `streaming.py` sigue sin mencionar `PersonDetector` ni `detect_sv`.
- Suite completa sin regresion.
</success_criteria>

<output>
Al terminar, crear `.planning/phases/27-multi-clase-y-contexto-de-escena/27-08-SUMMARY.md`.
</output>
