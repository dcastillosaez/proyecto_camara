---
phase: 27-multi-clase-y-contexto-de-escena
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/config.py
  - backend/detector.py
  - tests/test_config.py
  - tests/test_detector.py
autonomous: true
requirements: [BEH-06, BEH-07, BEH-08, BEH-09]

must_haves:
  truths:
    - "El modelo por defecto pasa a ser yolo26n.pt, alineado con CLAUDE.md (D-03)"
    - "Settings() expone los 10 parametros object_* y los 4 context_* con los defaults del research"
    - "La clase 0 (person) en object_class_ids hace fallar el arranque con un mensaje que explica por que"
    - "detector.set_classes([0,24]) cambia las clases de la siguiente inferencia sin recargar el modelo"
    - "El p50 de detect_sv con 6 clases no supera en mas de un 15% el p50 con 1 clase (criterio 6)"
  artifacts:
    - path: "backend/config.py"
      provides: "Bloques 'Multi-clase y objetos' y 'Contexto de escena' + validate_object_params + D-03"
      contains: "validate_object_params"
    - path: "backend/detector.py"
      provides: "PersonDetector.set_classes() — mutacion en caliente sin recargar el modelo"
      contains: "def set_classes"
    - path: "tests/test_detector.py"
      provides: "TEST_set_classes + TEST_multiclass_latency (criterio 6)"
      contains: "TEST_multiclass_latency"
  key_links:
    - from: "backend/detector.py"
      to: "self._classes leido en cada inferencia (detector.py:54)"
      via: "rebind atomico del atributo, nunca append/clear"
      pattern: "self\\._classes = list\\(classes\\)"
    - from: "backend/config.py"
      to: "pydantic model_validator"
      via: "@model_validator(mode='after')"
      pattern: "def validate_object_params"
---

<objective>
Dejar cerrada la capa de configuracion de la fase: corregir el modelo por defecto (D-03),
añadir los 14 parametros nuevos con su validador de rango, y darle al `PersonDetector` el
unico metodo que necesita para cambiar de clases en caliente (`set_classes`).

Purpose: `config.py` y `detector.py` no importan nada de la fase, asi que este plan corre
en wave 1 en paralelo con el dominio (27-01) y con la particion por clase (27-03). D-03 va
aqui y no en otro sitio porque el criterio 6 se mide **despues** de aplicarlo:
`yolo26n.pt` es `end2end=True` (NMS-free) y `yolov8n.pt` no, asi que cambian la ruta de
post-proceso sobre la que se mide (27-RESEARCH H-8).
Output: 14 parametros + `validate_object_params` + `set_classes()` + los tests de defaults,
de rango y el benchmark del criterio 6.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/27-multi-clase-y-contexto-de-escena/27-CONTEXT.md
@.planning/phases/27-multi-clase-y-contexto-de-escena/27-PATTERNS.md
@backend/config.py
@backend/detector.py

<interfaces>
<!-- NOMBRES LOCKED. Los consumen 27-03 (object_class_ids), 27-06 (manager/main),
     27-07 (endpoint de clases) y 27-09 (endpoint de contexto). -->

```python
    # --- Multi-clase y objetos (Fase 27 — BEH-06/BEH-07) ---
    object_class_ids: list[int] = [1, 2, 3, 24, 28]   # bicycle, car, motorcycle, backpack, suitcase
    object_left_secs: float = 60.0
    object_still_radius_px: float = 20.0
    object_person_radius_px: float = 150.0
    object_person_radius_ratio: float = 0.5
    object_warmup_secs: float = 10.0
    object_gone_secs: float = 3.0
    object_person_window_secs: float = 10.0
    object_max_tracks: int = 256
    objects_enabled: bool = True

    # --- Contexto de escena (Fase 27 — BEH-08/BEH-09) ---
    context_baseline_days: int = 7
    context_min_sample_days: int = 3
    context_low_ratio: float = 0.5
    context_high_ratio: float = 1.5
```

```python
# backend/detector.py
def set_classes(self, classes: list[int]) -> None: ...
```

Ids COCO de las 6 clases del criterio 1 (leidos de `YOLO('yolo26n.pt').names`):
`person=0, bicycle=1, car=2, motorcycle=3, backpack=24, suitcase=28`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: D-03 + los dos bloques de config + validate_object_params</name>

  <read_first>
    - `backend/config.py:33-37` — la linea del modelo por defecto y su comentario.
    - `backend/config.py:39-51` — `validate_yolo_model_path`: acepta `.pt` y exige
      contencion en el proyecto; **no hay que tocarlo**, `yolo26n.pt` ya pasa.
    - `backend/config.py:54` — `yolo_classes: list[int] = [0]`, que sigue existiendo y
      pasa a ser solo el valor inicial (la BD gana, 27-RESEARCH Q6).
    - `backend/config.py:165-188` — bloque de comportamiento (Fase 26): la forma
      OBLIGATORIA del comentario de cabecera (`--- <Tema> (Fase N — IDs) ---` + parrafo
      que justifica los defaults y dice que pasa si alguien los cambia).
    - `backend/config.py:115` — `upload_min_severity` y su relacion con `recording.py:309`.
    - `backend/config.py:275-295` — `validate_behavior_params`: el molde del validador
      (bucle sobre nombres + mensajes que explican la consecuencia, no solo el rango).
  </read_first>

  <files>backend/config.py</files>

  <action>
**1. D-03.** Cambiar `yolo_model_path: str = "yolov8n.pt"` por `"yolo26n.pt"`
(`config.py:37`). Ambos ficheros existen en la raiz; el validador de las lineas 39-51 lo
acepta sin cambios. Añadir media linea al comentario existente diciendo que es el modelo
fijado en `CLAUDE.md` y que es `end2end=True` (NMS-free), a diferencia de `yolov8n.pt`.

**2. Los dos bloques nuevos**, insertados **despues** del bloque de comportamiento
(tras `behavior_max_tracks`, `config.py:188`), con los valores EXACTOS del bloque
`<interfaces>`. El comentario de cabecera debe decir, como minimo, estas tres cosas:

```python
    # --- Multi-clase y objetos (Fase 27 — BEH-06/BEH-07) ---
    # (a) Los umbrales espaciales estan en PIXELES DEL FRAME PROCESADO
    #     (process_width x process_height, 1280x720 por defecto) y NO estan calibrados
    #     contra una escena real: object_person_radius_px=150 es 1,9 x loiter_radius_px
    #     y ~media altura de persona a media distancia, una propuesta razonada
    #     (27-RESEARCH.md Q1, Assumption A1). La calibracion con camara es el checkpoint
    #     manual de 27-11.
    # (b) OJO, AL REVES QUE EL BLOQUE DE LA FASE 26: OBJECT_LEFT sale con
    #     Severity.WARNING por defecto del catalogo (types.py:55), asi que cruza
    #     upload_min_severity="warning" (config.py:115 -> recording.py:309) y SUBE CLIPS A
    #     GOOGLE DRIVE desde el primer evento. Es intencional (un objeto abandonado es lo
    #     que quieres grabado), pero un radio mal calibrado consume cuota de Drive.
    #     Valvula de escape sin tocar codigo: subir upload_min_severity a "critical".
    # (c) yolo_classes=[] CIEGA el sistema en silencio: verificado, classes=[] devuelve 0
    #     detecciones (no las 80, que es lo que hace classes=None). Por eso el endpoint
    #     PUT la rechaza con 400 y el arranque trata una fila vacia de app_config como
    #     ausente (27-RESEARCH Pitfall 3).
    # object_person_radius_ratio corrige la escala: radio = max(px, ratio * alto_bbox),
    # asi que una persona cerca de la camara (500 px de alto) tiene un "cerca" de 250 px
    # y una lejana se queda en el suelo de 150 px.
```

Y para el bloque de contexto:

```python
    # --- Contexto de escena (Fase 27 — BEH-08/BEH-09) ---
    # El baseline se calcula sobre unique_tracks (personas distintas por minuto), NO sobre
    # detections: detections acumula len(active_track_ids) una vez por FRAME PROCESADO
    # (engine.py:281), asi que depende del FPS que AdaptiveRate haya elegido y no es
    # comparable entre dias (27-RESEARCH H-5). context_min_sample_days=3 hace que un
    # sistema recien instalado diga "unknown" en vez de inventarse un veredicto con
    # AVG sobre una sola muestra (Pitfall 8). Los ratios 0,5/1,5 son la parte de menor
    # confianza de la fase (Assumption A4): dos floats, triviales de ajustar con datos.
```

**3. `validate_object_params`**, al final de la clase, junto a los demas
`@model_validator`, **despues** de `validate_behavior_params`. Copiar literalmente el
validador de 27-RESEARCH § Bloque de configuracion, incluido el mensaje largo del
`if 0 in self.object_class_ids`:

```python
    @model_validator(mode="after")
    def validate_object_params(self) -> "Settings":
        for name in ("object_left_secs", "object_still_radius_px",
                     "object_person_radius_px", "object_warmup_secs",
                     "object_gone_secs", "object_person_window_secs"):
            if getattr(self, name) <= 0:
                raise ValueError(f"{name} debe ser > 0")
        if not 0 <= self.object_person_radius_ratio <= 2.0:
            raise ValueError("object_person_radius_ratio debe estar en [0, 2]")
        if any(not 0 <= c <= 79 for c in self.object_class_ids):
            raise ValueError("object_class_ids deben ser ids COCO validos (0-79)")
        if 0 in self.object_class_ids:
            raise ValueError(
                "la clase 0 (person) no puede estar en object_class_ids: las personas "
                "van al PersonTracker (LineZone + identidad + comportamiento), no al "
                "tracker de objetos"
            )
        if self.object_max_tracks < 1:
            raise ValueError("object_max_tracks debe ser >= 1")
        if not self.context_low_ratio < self.context_high_ratio:
            raise ValueError("context_low_ratio debe ser menor que context_high_ratio")
        if not 1 <= self.context_baseline_days <= 90:
            raise ValueError("context_baseline_days debe estar en [1, 90]")
        if self.context_min_sample_days < 1:
            raise ValueError("context_min_sample_days debe ser >= 1")
        return self
```

**NO tocar `backend/events/types.py`** (H-3: `OBJECT_LEFT`/`OBJECT_REMOVED` ya estan
catalogados y `tests/test_event_types.py:12` congela la lista; la severidad `WARNING` se
mantiene por decision del usuario).
**NO tocar `yolo_classes`** (`config.py:54`): sigue siendo `[0]` como valor inicial.
  </action>

  <verify>
    <automated>F:/Documentos/IA/Proyecto_Camara/.venv/Scripts/python.exe -c "from backend.config import Settings; s=Settings(); assert s.yolo_model_path=='yolo26n.pt', s.yolo_model_path; assert (s.object_class_ids,s.object_left_secs,s.object_still_radius_px,s.object_person_radius_px,s.object_person_radius_ratio,s.object_warmup_secs,s.object_gone_secs,s.object_person_window_secs,s.object_max_tracks,s.objects_enabled)==([1,2,3,24,28],60.0,20.0,150.0,0.5,10.0,3.0,10.0,256,True); assert (s.context_baseline_days,s.context_min_sample_days,s.context_low_ratio,s.context_high_ratio)==(7,3,0.5,1.5); ok=0
for kw in ({'object_left_secs':0},{'object_class_ids':[0,24]},{'object_class_ids':[80]},{'object_person_radius_ratio':3.0},{'context_low_ratio':2.0},{'object_max_tracks':0}):
    try: Settings(**kw)
    except Exception: ok+=1
assert ok==6, ok; print('OK')"</automated>
  </verify>

  <acceptance_criteria>
    - `grep -n 'yolo_model_path: str = "yolo26n.pt"' backend/config.py` acierta.
    - `grep -n "Multi-clase y objetos (Fase 27" backend/config.py` y `grep -n "Contexto de escena (Fase 27" backend/config.py` aciertan.
    - `grep -n "def validate_object_params" backend/config.py` acierta y esta despues de `def validate_behavior_params`.
    - `grep -n "SUBE CLIPS A" backend/config.py` acierta (el aviso (b), invertido respecto al bloque de la Fase 26).
    - `git diff --stat backend/events/types.py` esta vacio.
    - `grep -n "yolo_classes: list\[int\] = \[0\]" backend/config.py` sigue aciertando.
    - El comando de `<verify>` imprime `OK`.
  </acceptance_criteria>

  <done>`Settings()` arranca con `yolo26n.pt` y los 14 parametros nuevos, y rechaza las seis configuraciones imposibles.</done>
</task>

<task type="auto">
  <name>Task 2: PersonDetector.set_classes() — mutar la instancia viva</name>

  <read_first>
    - `backend/detector.py:20-57` — constructor (`self._classes = classes if classes is
      not None else [0]`) y `detect_sv`, que lee `self._classes` **en cada llamada**
      (linea 54). No hay que tocar ninguna de las dos cosas.
    - `backend/tracker.py:129-142` — `PersonTracker.set_frame_rate`: **el analogo exacto y
      prescriptivo** del repo para "mutar un parametro en una instancia viva en vez de
      reconstruir el objeto y perder el estado", con el porque escrito en el docstring.
    - `backend/pipeline/tracking.py:38-45` — la convencion de escritor unico documentado
      (que hilo escribe que campo), que el docstring nuevo debe seguir.
  </read_first>

  <files>backend/detector.py</files>

  <action>
Añadir un unico metodo publico a `PersonDetector`, junto a los demas metodos publicos.
El cuerpo es **una linea**; el docstring es lo que aporta valor:

```python
    def set_classes(self, classes: list[int]) -> None:
        """Cambia las clases activas en caliente, sin recargar el modelo.

        detect_sv() lee self._classes en CADA inferencia (linea 54) y Ultralytics aplica
        `classes` en el post-proceso de la llamada (models/yolo/detect/predict.py:54-58),
        no en la construccion del modelo: la siguiente inferencia ya usa el valor nuevo.
        Verificado en 27-RESEARCH.md Q3 — id(self._model) no cambia.

        Se muta el atributo en vez de reconstruir el PersonDetector (o de reiniciar el
        DetectionWorker) por el mismo motivo que PersonTracker.set_frame_rate muta
        max_time_lost: reconstruir tiraria el estado. Aqui el coste seria mayor todavia
        — WorkerSupervisor._check() cuenta cualquier parada como caida y tres cambios de
        configuracion en 60 s dejarian el worker en FAILED de forma permanente
        (supervisor.py:166-173).

        Escritor: el event loop (endpoint PUT /api/v2/detection/classes). Lector: el hilo
        de deteccion. NO hace falta lock, a diferencia de set_frame_rate: el rebind es un
        unico STORE_ATTR, atomico bajo el GIL, asi que el lector ve la lista vieja o la
        nueva, nunca una a medias. La regla dura es NUNCA mutar la lista in-place
        (append/clear) — eso si seria observable a medias. En el peor caso el detector usa
        las clases viejas durante un frame (<= 333 ms a 3 FPS).
        """
        self._classes = list(classes)
```

No tocar `__init__` ni `detect`/`detect_sv`: el filtrado ya funciona (H-1).
  </action>

  <verify>
    <automated>F:/Documentos/IA/Proyecto_Camara/.venv/Scripts/python.exe -c "import inspect; from backend.detector import PersonDetector; src=inspect.getsource(PersonDetector.set_classes); assert 'self._classes = list(classes)' in src; assert '.append(' not in src and '.clear()' not in src; print('OK')"</automated>
  </verify>

  <acceptance_criteria>
    - `grep -n "def set_classes" backend/detector.py` acierta.
    - `grep -n "self._classes = list(classes)" backend/detector.py` acierta.
    - `grep -n "STORE_ATTR\|atomico bajo el GIL" backend/detector.py` acierta (la convencion de escritor unico esta documentada).
    - `git diff backend/detector.py | grep "^-" | grep -v "^---"` esta vacio (solo se añade codigo, no se modifica nada existente).
  </acceptance_criteria>

  <done>`PersonDetector.set_classes()` existe, hace rebind del atributo y documenta por que no hace falta lock ni reinicio del worker.</done>
</task>

<task type="auto">
  <name>Task 3: Tests de config, de set_classes y benchmark del criterio 6</name>

  <read_first>
    - `tests/test_config.py:283-321` — `TEST_behavior_defaults_match_spec` y
      `TEST_behavior_params_must_be_positive`: banda de comentario con el motivo, un
      `assert` por parametro, `pytest.raises((ValidationError, ValueError))`.
    - `tests/test_detector.py:30-36` — fixture `detector` con `YOLO` mockeado (sin pesos).
    - `tests/test_detector.py:94-101` — `TEST_033_detect_passes_confidence_and_classes_to_model`:
      como se afirma sobre los kwargs que llegan al modelo.
    - `tests/test_reid_engine.py:40-70` — `TEST_reid_latency_under_20ms`: el molde LITERAL
      del benchmark (fixture `scope="module"` con pesos reales, 5 warmup + 30 muestras,
      `statistics.median`, assert sobre el **p50** y no sobre el maximo, y el mensaje de
      fallo con la cifra medida en el research).
  </read_first>

  <files>tests/test_config.py, tests/test_detector.py</files>

  <action>
**A. `tests/test_config.py`** — tres tests nuevos al final, cada uno con su banda de
comentario:

1. `TEST_yolo_model_default_is_yolo26n()` — `Settings().yolo_model_path == "yolo26n.pt"`.
   Comentario: D-03 corrige la deriva de `config.py:37` respecto a la decision de stack de
   `CLAUDE.md`; ademas `yolo26n.pt` es `end2end=True` (NMS-free) y `yolov8n.pt` no, asi que
   el criterio 6 se mide sobre una ruta de post-proceso distinta.
2. `TEST_object_defaults_match_research()` — un `assert` por cada uno de los 10 `object_*`
   y los 4 `context_*`.
3. `TEST_object_params_reject_impossible_values()` — `pytest.raises((ValidationError,
   ValueError))` para `object_left_secs=0`, `object_class_ids=[0, 24]`,
   `object_class_ids=[80]`, `object_person_radius_ratio=3.0`, `context_low_ratio=2.0` y
   `object_max_tracks=0`. El caso `[0, 24]` merece un comentario propio: es la guarda que
   impide, por configuracion, que las personas acaben en el tracker de objetos y se pierda
   el `LineZone` de la Fase 4.

**B. `tests/test_detector.py`** — dos tests nuevos:

4. `TEST_set_classes_changes_next_inference(detector, blank_frame)` — reutilizar la
   fixture `detector` con YOLO mockeado. Guardar `model_before = detector._model`, llamar
   `detector.set_classes([0, 24])`, ejecutar `detector.detect_sv(blank_frame)` y afirmar
   `kwargs["classes"] == [0, 24]` **y** `detector._model is model_before` (el modelo NO se
   recarga). Docstring citando 27-RESEARCH Q3.

5. `TEST_multiclass_latency_under_15_percent()` — **criterio 6 del ROADMAP**, molde
   literal de `TEST_reid_latency_under_20ms`. Carga pesos reales, asi que va con una
   fixture `scope="module"` propia (no la fixture mockeada):

```python
def TEST_multiclass_latency_under_15_percent(real_detector, bench_frame):
    """Criterio 6: activar las 6 clases del ROADMAP no puede subir la latencia de
    inferencia mas de un 15 %. Se mide el p50, no un maximo ni una sola llamada: el
    jitter del planificador de Windows en esta maquina compartida haria flaky un assert
    sobre una muestra (misma metodologia que TEST_reid_latency_under_20ms).

    Medido en 27-RESEARCH.md Q8 con yolo26n.pt sobre bus.jpg a 1280x720: 38,90 ms con 1
    clase y 40,74 ms con 6 (+4,7 %, margen 3x sobre el criterio). El coste marginal es
    practicamente cero porque `classes=` es un filtro de post-proceso dentro de la NMS
    (predict.py:54-58) y yolo26n es NMS-free.
    """
```

Metodologia obligatoria: 5 iteraciones de warmup + 30 muestras + `statistics.median` para
cada configuracion (`[0]` y `[0,1,2,3,24,28]`, aplicadas con `set_classes`), assert
`p50_6 < 1.15 * p50_1` con un mensaje que incluya ambas cifras en ms. El frame de prueba
es `.venv/Lib/site-packages/ultralytics/assets/bus.jpg` reescalado a 1280x720
(`process_width` x `process_height` reales); si el fichero no existe, `pytest.skip` con
motivo explicito — nunca inventar un assert que pase.

Este test tarda unos segundos: marcarlo con la misma convencion que usa
`tests/test_reid_engine.py` para los que cargan pesos reales (fixture `scope="module"`).

Todos los nombres en `TEST_*` (`pytest.ini`).
  </action>

  <verify>
    <automated>F:/Documentos/IA/Proyecto_Camara/.venv/Scripts/python.exe -m pytest tests/test_config.py tests/test_detector.py -q</automated>
  </verify>

  <acceptance_criteria>
    - `pytest tests/test_config.py -k "object or yolo_model_default" -q` recoge 3 tests y sale verde.
    - `pytest tests/test_detector.py -k "TEST_set_classes or TEST_multiclass_latency" -q` recoge 2 tests y sale verde (o el de latencia se salta con motivo si falta `bus.jpg`).
    - `pytest tests/test_config.py tests/test_detector.py -q` verde entero, sin regresion.
    - `grep -n "statistics.median" tests/test_detector.py` acierta.
    - `grep -n "1.15" tests/test_detector.py` acierta (el umbral del criterio 6, no un numero inventado).
    - `grep -n "38,90\|38.90" tests/test_detector.py` acierta (la cifra medida en el research esta en el mensaje de fallo).
  </acceptance_criteria>

  <done>El criterio 6 tiene un comando automatizado que pasa, `set_classes` esta probado sin recarga de modelo y los 14 parametros tienen defaults y rangos verificados.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| .env / entorno -> proceso | Los 14 parametros llegan por `pydantic-settings` desde el entorno del operador |
| event loop -> hilo de deteccion | `set_classes` lo escribe el endpoint PUT y lo lee el hilo de inferencia |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-05 | Tampering | `Settings` parametros de objeto | mitigate | `validate_object_params` rechaza al arrancar valores <= 0, ratios fuera de `[0,2]`, ids fuera de `0..79`, `context_low_ratio >= context_high_ratio` y — sobre todo — la clase 0 en `object_class_ids`, que desviaria las personas al tracker de objetos y romperia el conteo de la Fase 4 (ASVS V5) |
| T-27-06 | Tampering | `PersonDetector._classes` desde otro hilo | mitigate | Rebind atomico (`STORE_ATTR` bajo el GIL) y prohibicion explicita de `append`/`clear` documentada en el docstring; el lector ve la lista vieja o la nueva, nunca una a medias (27-RESEARCH Q3) |
| T-27-07 | Denial of Service | subida automatica de clips a Drive por `OBJECT_LEFT` (`WARNING`) | accept | Decision del usuario: `OBJECT_LEFT` se queda en `WARNING`. Mitigacion documentada en el bloque de config (subir `upload_min_severity` a `"critical"`) y verificacion de la tasa de falsos positivos en el checkpoint manual de 27-11 |
| T-27-08 | Denial of Service | `.env` antiguo sin los parametros nuevos | accept | `model_config` ya tiene `extra: "ignore"`: una instalacion existente arranca con los defaults |
</threat_model>

<verification>
```bash
F:/Documentos/IA/Proyecto_Camara/.venv/Scripts/python.exe -m pytest tests/test_config.py tests/test_detector.py -q
```
</verification>

<success_criteria>
- `yolo_model_path` por defecto es `yolo26n.pt` (D-03) y el validador existente lo acepta sin cambios.
- Los 10 `object_*` y los 4 `context_*` existen con los defaults del research y con el comentario que avisa de la subida a Drive.
- `validate_object_params` rechaza las seis configuraciones imposibles, incluida la clase 0 en `object_class_ids`.
- `PersonDetector.set_classes()` cambia la siguiente inferencia sin recargar el modelo.
- El criterio 6 tiene un test que mide el p50 y pasa con margen.
</success_criteria>

<output>
Al terminar, crear `.planning/phases/27-multi-clase-y-contexto-de-escena/27-02-SUMMARY.md`.
</output>
