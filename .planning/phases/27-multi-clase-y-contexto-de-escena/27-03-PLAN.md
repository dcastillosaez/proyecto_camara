---
phase: 27-multi-clase-y-contexto-de-escena
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tracker.py
  - backend/pipeline/detection.py
  - tests/test_detection_worker.py
autonomous: true
requirements: [BEH-06]

must_haves:
  truths:
    - "Un sv.Detections mixto (persona + coche) no altera get_counts() del LineZone de la Fase 4"
    - "Los tracks de objeto nunca entran en TrackRegistry: registry.frame_ids() solo contiene ids de persona"
    - "Una deteccion de objeto solapada con una de persona no le roba el tracker_id a la persona"
    - "PersonTracker y ObjectTracker reciben el mismo frame_rate cuando AdaptiveRate cambia de escalon"
    - "El ObjectTracker no usa DetectionsSmoother: la clase y la desaparicion llegan frescas"
    - "Con object_class_ids vacio el comportamiento del worker es identico al de hoy"
  artifacts:
    - path: "backend/tracker.py"
      provides: "ObjectTracker — sv.ByteTrack propio, sin LineZone, sin smoother, con set_frame_rate"
      contains: "class ObjectTracker"
    - path: "backend/pipeline/detection.py"
      provides: "Particion por clase antes del tracker + set_object_classes + get_object_stats/get_object_boxes"
      contains: "def set_object_classes"
    - path: "tests/test_detection_worker.py"
      provides: "Tests de regresion del riesgo ByteTrack (LineZone, registry, robo de id)"
      contains: "TEST_object_class_does_not_reach_line_zone"
  key_links:
    - from: "backend/pipeline/detection.py"
      to: "PersonTracker.update"
      via: "recibe person_dets, nunca sv_dets completo"
      pattern: "self\\._tracker\\.update\\(person_dets\\)"
    - from: "backend/pipeline/detection.py"
      to: "ObjectTracker.set_frame_rate"
      via: "_sync_tracker_frame_rate propaga a los DOS trackers"
      pattern: "_object_tracker\\.set_frame_rate"
---

<objective>
Cortar el pipeline por clase **antes** de que las detecciones lleguen a un tracker, y darle
a los objetos un `sv.ByteTrack` propio.

Purpose: es el riesgo de primer orden de la fase y no aparece en `SPEC_v2.md`. `sv.ByteTrack`
es class-agnostic — el tensor que entra al matcher se construye solo con `xyxy` y
`confidence` (`supervision/tracker/byte_tracker/core.py:104-110`) y el reensamblado del
`tracker_id` es otra asignacion humgara por IoU con umbral 0,5. El research **reprodujo**
que un track de mochila le transfiere su `track_id` a una persona colocada casi en la
misma caja. Activar clases nuevas sobre el pipeline actual seria por tanto una regresion
de la Fase 4 (que lleva meses en produccion): `LineZone.trigger(tracked)` cuenta cruces de
*cualquier* track, `RecognitionWorker` buscaria caras en mochilas y `accumulate_detections`
contaminaria `detection_stats`, que es justo la fuente del baseline de BEH-09.
Este plan tiene wave 1 y plan propio a proposito: el riesgo se protege con tests de
regresion explicitos antes de que nada mas se apoye en el.
Output: `ObjectTracker` + la particion en `DetectionWorker._loop` + 6 tests de regresion.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/27-multi-clase-y-contexto-de-escena/27-RESEARCH.md
@.planning/phases/27-multi-clase-y-contexto-de-escena/27-PATTERNS.md
@backend/tracker.py
@backend/pipeline/detection.py

<interfaces>
<!-- CONTRATO LOCKED. Lo consumen 27-06 (cableado), 27-07 (fachada de clases) y
     27-08 (overlay del MJPEG). -->

```python
# backend/tracker.py
class ObjectTracker:
    LOST_TRACK_BUFFER = 60                     # mismo valor que PersonTracker

    def __init__(self, frame_rate: int = 15) -> None: ...
    def update(self, detections: sv.Detections) -> sv.Detections: ...
    def set_frame_rate(self, frame_rate: float) -> None: ...
    def reset(self) -> None: ...               # opcional, solo si PersonTracker lo tiene


# backend/pipeline/detection.py — DetectionWorker
PERSON_CLASS_IDS = (0,)                        # constante de modulo

def set_object_classes(self, class_ids: set[int]) -> None: ...
def get_object_stats(self) -> list[dict]:      # [{"class_name": "backpack", "count": 2}, ...]
def get_object_boxes(self) -> list[dict]:      # [{"track_id": 5, "class_name": "backpack",
                                               #   "bbox": (x1, y1, x2, y2)}, ...]
```

Constructor de `DetectionWorker`: los parametros nuevos van **al final, con default
`None`/vacio** — hay decenas de tests que lo construyen con 5 posicionales y nada mas
(`tests/test_detection_worker.py:245-250`):

```python
        object_tracker: "ObjectTracker | None" = None,
        object_class_ids: set[int] | None = None,
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ObjectTracker en backend/tracker.py — analogo por sustraccion</name>

  <read_first>
    - `backend/tracker.py:11-58` — `PersonTracker.__init__` completo: la constante
      `LOST_TRACK_BUFFER`, el comentario del `frame_rate` (que sigue aplicando tal cual),
      la construccion del `sv.ByteTrack`, el `DetectionsSmoother`, el `LineZone` y los
      anotadores.
    - `backend/tracker.py:60-93` — `PersonTracker.update`: la linea 76 es la unica que se
      conserva. **No se toca este metodo.**
    - `backend/tracker.py:129-142` — `set_frame_rate`: se copia LITERALMENTE, docstring
      incluido. Es obligatorio, ver la nota del `<action>`.
  </read_first>

  <files>backend/tracker.py</files>

  <action>
Añadir una clase `ObjectTracker` al final de `backend/tracker.py`. **`PersonTracker` no se
toca en absoluto** — es el dueño del `LineZone` de la Fase 4, que esta en produccion.

Es un analogo **por sustraccion**: se copia el constructor de `PersonTracker` quitando
tres cosas, y cada una con su motivo en un comentario:

```python
class ObjectTracker:
    """ByteTrack dedicado a las clases de objeto (Fase 27, BEH-06/BEH-07).

    Existe porque sv.ByteTrack es class-agnostic: el tensor que entra al matcher se
    construye solo con xyxy y confidence (supervision/tracker/byte_tracker/core.py:104-110)
    y el reensamblado del tracker_id es una asignacion humgara por IoU con umbral 0,5.
    Reproducido en 27-RESEARCH.md Q4: un track de mochila le TRANSFIERE su id a una
    persona colocada casi en la misma caja. Un tracker por grupo de clases es la unica
    forma de que la identidad no migre entre clases.

    Diferencias deliberadas con PersonTracker:
    - SIN DetectionsSmoother: get_track hace deepcopy(track[0]) — el elemento MAS VIEJO
      del deque — y solo promedia xyxy/confidence, asi que congelaria class_id hasta 5
      frames (0,6 s a 8 FPS, medido) y retrasaria la desaparicion otro tanto. Justo las
      dos señales que BEH-07 necesita frescas.
    - SIN LineZone: el conteo de la Fase 4 es de personas y solo debe ver personas.
    - SIN anotadores: el overlay de objetos se dibuja aparte, con color propio.
    """

    LOST_TRACK_BUFFER = 60

    def __init__(self, frame_rate: int = 15) -> None:
        # Mismo motivo que en PersonTracker (MEJORAS.md punto 14): ByteTrack calcula
        # max_time_lost = frame_rate/30 * lost_track_buffer, asi que con el default (30)
        # y un stream real a ~15 FPS el buffer efectivo en segundos era el doble.
        self._byte_tracker = sv.ByteTrack(
            lost_track_buffer=self.LOST_TRACK_BUFFER, frame_rate=frame_rate
        )
        self._lock = threading.Lock()

    def update(self, detections: sv.Detections) -> sv.Detections:
        """Trackea las detecciones de objeto. Devuelve solo sv.Detections: sin
        cruces de linea, sin suavizado, sin anotar."""
        return self._byte_tracker.update_with_detections(detections)

    def set_frame_rate(self, frame_rate: float) -> None:
        # ... copiar LITERALMENTE el de PersonTracker (tracker.py:129-142), docstring
        # incluido, cambiando solo el nombre de la clase que se cita
```

**Por que `set_frame_rate` es obligatorio y no opcional:**
`DetectionWorker._sync_tracker_frame_rate` (`detection.py:260-265`) ya llama a
`self._tracker.set_frame_rate(fps)` en cada cambio de escalon de `AdaptiveRate`. Si
`ObjectTracker` no expone el mismo metodo y el worker no lo llama tambien, el tracker de
objetos queda desincronizado del FPS real y pierde tracks antes de tiempo (o los mantiene
el doble) — exactamente el bug que documenta el comentario de `tracker.py:25-30`.

`update()` debe tolerar un `sv.Detections` vacio sin lanzar (es el caso normal: la mayoria
de frames no traen objetos). Verificarlo en el `<verify>`.
  </action>

  <verify>
    <automated>F:/Documentos/IA/Proyecto_Camara/.venv/Scripts/python.exe -c "import numpy as np, supervision as sv, inspect; from backend.tracker import ObjectTracker, PersonTracker; t=ObjectTracker(frame_rate=8); out=t.update(sv.Detections.empty()); assert len(out)==0; d=sv.Detections(xyxy=np.array([[10.,10.,50.,90.]]), confidence=np.array([0.9]), class_id=np.array([24])); r=t.update(d); assert r.tracker_id is not None; src=inspect.getsource(ObjectTracker); assert 'DetectionsSmoother' not in src and 'LineZone' not in src, 'ObjectTracker no debe traer smoother ni LineZone'; t.set_frame_rate(12.0); assert t._byte_tracker.max_time_lost==int(12.0/30.0*ObjectTracker.LOST_TRACK_BUFFER); print('OK')"</automated>
  </verify>

  <acceptance_criteria>
    - `grep -n "class ObjectTracker" backend/tracker.py` acierta.
    - `grep -n "DetectionsSmoother\|LineZone" backend/tracker.py` NO acierta dentro del cuerpo de `ObjectTracker` (comprobado por el `<verify>`).
    - `grep -n "def set_frame_rate" backend/tracker.py` devuelve 2 resultados (PersonTracker y ObjectTracker).
    - `git diff backend/tracker.py | grep "^-" | grep -v "^---"` esta vacio: `PersonTracker` intacto, solo se añade codigo.
    - `pytest tests/test_tracker.py -q` sigue verde entero.
    - El comando de `<verify>` imprime `OK`.
  </acceptance_criteria>

  <done>`ObjectTracker` trackea objetos con su propio `sv.ByteTrack`, sin smoother ni LineZone, y responde a `set_frame_rate` con el mismo calculo que `PersonTracker`.</done>
</task>

<task type="auto">
  <name>Task 2: Particion por clase en DetectionWorker._loop + estado de objetos</name>

  <read_first>
    - `backend/pipeline/detection.py:26-32` — el bloque `if TYPE_CHECKING`: los imports de
      tipos van AHI, nunca en runtime.
    - `backend/pipeline/detection.py:49-61` — el constructor: las dependencias opcionales
      van al final con default `None` (asi añadio la Fase 26 el parametro `behavior`).
    - `backend/pipeline/detection.py:81-86` — el bloque de estado bajo `self._lock`
      (`_zones`, `_zones_dirty`, `_zone_states`, ...): ahi van los campos nuevos.
    - `backend/pipeline/detection.py:119-130` — `set_zones` y `get_zone_stats`: el molde
      exacto del setter thread-safe (docstring de una linea acabado en "Thread-safe.",
      `with self._lock`, rebind del atributo) y del getter que copia bajo lock.
    - `backend/pipeline/detection.py:164-189` — el bucle caliente: donde entra la
      particion, donde se mide `inference_latency` y donde se llama a
      `_sync_tracker_frame_rate()`.
    - `backend/pipeline/detection.py:260-265` — `_sync_tracker_frame_rate`.
    - `backend/pipeline/detection.py:281-284` — el snapshot bajo lock desde el hilo caliente.
  </read_first>

  <files>backend/pipeline/detection.py</files>

  <action>
**1. Constructor.** Añadir al FINAL de la firma, con default:
`object_tracker: "ObjectTracker | None" = None` y
`object_class_ids: set[int] | None = None`. Guardar
`self._object_tracker = object_tracker` y
`self._object_class_ids: frozenset[int] = frozenset(object_class_ids or ())`.
Import de `ObjectTracker` bajo `TYPE_CHECKING`, junto a `PersonTracker`.

Estado nuevo, en el bloque de `self._lock` (`detection.py:81-86`):
```python
        self._object_boxes: list[dict] = []      # ultima foto de objetos trackeados;
                                                 # writer: hilo de deteccion,
                                                 # readers: event loop (endpoint de
                                                 # contexto) y StreamingWorker (overlay)
```

**2. Constante de modulo** junto a los demas simbolos de nivel de modulo:
```python
PERSON_CLASS_IDS = (0,)   # la clase persona nunca entra en el tracker de objetos: es la
                          # dueña del LineZone (Fase 4), de la identidad (Fases 23/24),
                          # del ReID (Fase 25) y del analisis de comportamiento (Fase 26)
```

**3. La particion, dentro del `try` de inferencia** (`detection.py:164-171`). Los tres
puntos exactos:

```python
            t0 = time.monotonic()
            try:
                sv_dets = self._detector.detect_sv(frame.image)
                # ─── Particion por clase ANTES del tracker (Fase 27, 27-RESEARCH Q4) ───
                # sv.ByteTrack es class-agnostic: el class_id no llega al matcher
                # (core.py:104-110) y el id puede transferirse entre clases cuando las
                # cajas se solapan (reproducido). Mandar sv_dets completo a
                # PersonTracker haria que un coche cruzando la linea sumara al conteo de
                # personas de la Fase 4, que esta en produccion.
                person_dets, object_dets = self._split_by_class(sv_dets)
                tracked, crossings = self._tracker.update(person_dets)
                obj_tracked = (
                    self._object_tracker.update(object_dets)
                    if self._object_tracker is not None
                    else None
                )
            except Exception:
                self._exceptions += 1
                logger.exception("DetectionWorker: fallo de inferencia, se salta el frame")
                continue
```

La particion va **dentro** del `try` que ya existe para que un `sv_dets` malformado siga
cayendo en el mismo `except`. `_split_by_class` es un metodo privado:

```python
    def _split_by_class(self, dets):
        """Separa personas y objetos. El slicing preserva data["class_name"] (verificado
        en 27-RESEARCH.md, medicion 2 de H-1), asi que el nombre de clase para el payload
        del evento sale del modelo y no de un mapa COCO escrito a mano."""
        cls = dets.class_id
        with self._lock:
            object_ids = self._object_class_ids
        if cls is None or len(dets) == 0:
            return dets, dets[:0]
        person_dets = dets[np.isin(cls, PERSON_CLASS_IDS)]
        object_dets = dets[np.isin(cls, list(object_ids))] if object_ids else dets[:0]
        return person_dets, object_dets
```

**Lo que NO cambia y hay que dejar como esta:**
- `self._rate.observe(inference_latency)` (linea 174) sigue midiendo solo inferencia +
  tracking de personas. **La via de objetos NUNCA llama a `observe()`** — contaminaria el
  control adaptativo. El repo ya tomo esta decision dos veces (Fase 25 con ReID, Fase 26
  con el analizador de comportamiento).
- `_metrics.active_tracks` y `self._registry.update_from_detections(tracked, now)`
  (lineas 183-184) siguen recibiendo `tracked`, es decir SOLO personas. **Los objetos no
  entran nunca en `TrackRegistry`**: es estado de personas con escritor unico por campo
  (`tracking.py:37-45`) y sus tres lectores (`StreamingWorker`, `RecognitionWorker`,
  `RecordingWorker`) lo asumen; ademas los dos ByteTrack tienen espacios de id que
  colisionan (ambos empiezan en 1).
- `_update_zones_and_heat`, `_analyze_behavior`, `_emit_crossings` y
  `_emit_track_lifecycle` siguen recibiendo `tracked`.
- `backend/pipeline/tracking.py` **no se toca** (27-PATTERNS § File Classification).

**4. Foto del estado de objetos.** Despues del tracking de objetos, y bajo `self._lock`,
refrescar `self._object_boxes` con una entrada por objeto trackeado:
`{"track_id": int, "class_id": int, "class_name": str, "bbox": (x1, y1, x2, y2)}`.
`class_name` sale de `obj_tracked.data["class_name"][i]` cuando existe; si no,
`str(class_id)` como fallback. Si `obj_tracked` es `None` o esta vacio, dejar la lista
vacia (no conservar la foto anterior: seria mentir al overlay).

**5. `_sync_tracker_frame_rate`** (`detection.py:260-265`): añadir la propagacion al
tracker de objetos, con comentario del porque:
```python
        if self._object_tracker is not None:
            self._object_tracker.set_frame_rate(fps)   # si no, el tracker de objetos
                                                       # queda desincronizado del FPS real
```

**6. Setter y getters, moldes `set_zones`/`get_zone_stats`:**
```python
    def set_object_classes(self, class_ids: set[int]) -> None:
        """Ids que van al tracker de OBJETOS. Thread-safe."""
        with self._lock:
            self._object_class_ids = frozenset(class_ids)

    def get_object_stats(self) -> list[dict]:
        """Recuento por class_name de los objetos trackeados en el ultimo frame."""
        with self._lock:
            boxes = list(self._object_boxes)
        counts: dict[str, int] = {}
        for b in boxes:
            counts[b["class_name"]] = counts.get(b["class_name"], 0) + 1
        return [{"class_name": k, "count": v} for k, v in sorted(counts.items())]

    def get_object_boxes(self) -> list[dict]:
        """Cajas de objeto del ultimo frame, para el overlay del MJPEG. Thread-safe."""
        with self._lock:
            return [dict(b) for b in self._object_boxes]
```

Este es el patron que 27-RESEARCH Q4 manda usar para el estado de objetos: **un dict bajo
`self._lock`, no un segundo registry**. Escritor: el hilo de deteccion. Lectores: el event
loop y el `StreamingWorker`.

**No añadir aqui `_analyze_objects`** — eso es 27-06, cuando `ObjectAnalyzer` y
`emit_object` ya existan.
  </action>

  <verify>
    <automated>F:/Documentos/IA/Proyecto_Camara/.venv/Scripts/python.exe -m pytest tests/test_detection_worker.py tests/test_workers_isolation.py tests/test_architecture.py -q</automated>
  </verify>

  <acceptance_criteria>
    - `grep -n "PERSON_CLASS_IDS" backend/pipeline/detection.py` acierta y la particion usa `np.isin`.
    - `grep -n "self._tracker.update(person_dets)" backend/pipeline/detection.py` acierta (el tracker de personas NO recibe `sv_dets`).
    - `grep -n "def set_object_classes\|def get_object_stats\|def get_object_boxes\|def _split_by_class" backend/pipeline/detection.py` devuelve 4 resultados.
    - `grep -n "_object_tracker.set_frame_rate" backend/pipeline/detection.py` acierta dentro de `_sync_tracker_frame_rate`.
    - `git diff --stat backend/pipeline/tracking.py` esta vacio.
    - `grep -n "self._rate.observe" backend/pipeline/detection.py` sigue apareciendo una sola vez.
    - `pytest tests/test_architecture.py -q` verde (ningun `await` en hilos, ninguna inferencia en corrutinas).
  </acceptance_criteria>

  <done>El `DetectionWorker` parte las detecciones por clase antes de tocar ningun tracker, los objetos van a su propio ByteTrack y su estado es legible desde fuera bajo lock.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Tests de regresion del riesgo ByteTrack</name>

  <read_first>
    - `tests/test_detection_worker.py:27-35` y `:253-260` — los helpers `_tracked(ids)` y
      `_tracked_at(boxes, tids)`. **Ninguno pone `class_id` util**: hace falta UNA sola
      variante `_tracked_cls(boxes, tids, class_ids)` y reutilizarla. No escribir un
      cuarto helper.
    - `tests/test_detection_worker.py:240-260` — como se construye el worker con 5
      posicionales y `MagicMock()` para detector y tracker.
    - `tests/test_detection_worker.py:329-346` — el molde de "construir el worker con la
      dependencia inyectada y llamar directamente al metodo privado".
    - `backend/tracker.py` — `PersonTracker.get_counts()` / `LineZone`, para el test de
      regresion de la Fase 4.
  </read_first>

  <files>tests/test_detection_worker.py</files>

  <behavior>
    - `TEST_object_class_does_not_reach_line_zone`: un `sv.Detections` mixto (persona en `class_id=0` + coche en `class_id=2`) pasado por `_split_by_class` deja `person_dets` con SOLO la persona; un `PersonTracker` real alimentado con `person_dets` da el mismo `get_counts()` que si el coche no hubiera existido nunca.
    - `TEST_objects_not_in_registry`: tras un frame mixto, `registry.frame_ids()` no contiene ningun id proveniente del tracker de objetos, y `len(registry.snapshot())` es igual al numero de personas.
    - `TEST_bytetrack_ids_do_not_migrate_between_classes`: **el test que reproduce el hallazgo**. Alimentar el `ObjectTracker` con una mochila estable 5 frames en `[300,300,400,500]` y despues alimentar el `PersonTracker` con una persona en `[300,305,405,505]`: los dos trackers mantienen espacios de id independientes y la persona no hereda el id de la mochila. Docstring citando `core.py:104-110` y la reproduccion del research.
    - `TEST_split_by_class_preserves_class_name`: el slicing conserva `data["class_name"]` (por eso no hace falta un mapa COCO a mano).
    - `TEST_sync_frame_rate_reaches_both_trackers`: un cambio de escalon de `AdaptiveRate` llama a `set_frame_rate` en el tracker de personas Y en el de objetos.
    - `TEST_no_object_classes_behaves_like_today`: con `object_class_ids=set()` (o `object_tracker=None`), `person_dets` es el `sv_dets` completo cuando solo hay personas y `object_dets` esta vacio; ninguna llamada al tracker de objetos.
    - `TEST_object_boxes_snapshot_is_a_copy`: `get_object_boxes()` devuelve copias — mutar el resultado no altera el estado interno del worker.
  </behavior>

  <action>
Añadir a `tests/test_detection_worker.py`, cada bloque precedido de su banda de comentario
`# ─── ... ───` con el motivo. Nombres `TEST_*` obligatorios.

**Helper unico, junto a `_tracked_at` (linea 253):**

```python
def _tracked_cls(boxes, tids, class_ids, names=None) -> sv.Detections:
    """Variante de _tracked_at con class_id REAL y data["class_name"].

    Los helpers existentes ponen class_id a ceros, asi que no sirven para probar la
    particion por clase: todo pareceria persona.
    """
    det = sv.Detections(
        xyxy=np.array(boxes, dtype=float),
        confidence=np.ones(len(tids)),
        class_id=np.array(class_ids, dtype=int),
        data={"class_name": np.array(names or [str(c) for c in class_ids])},
    )
    det.tracker_id = np.array(tids)
    return det
```

**Banda de comentario obligatoria** antes del primer test del bloque, porque es la
justificacion de la fase entera:

```python
# ─── Regresion: ByteTrack no es class-aware (Fase 27, 27-RESEARCH.md Q4) ─────
# El tensor que entra al matcher se construye solo con xyxy y confidence
# (supervision/tracker/byte_tracker/core.py:104-110) y el reensamblado del
# tracker_id es una asignacion humgara por IoU con umbral 0,5. El research
# reprodujo que un track de mochila le TRANSFIERE su id a una persona colocada
# casi en la misma caja. Sin la particion por clase, activar "car" haria que los
# vehiculos sumaran al conteo de linea de la Fase 4 — que esta en produccion —,
# RecognitionWorker buscaria caras en mochilas y accumulate_detections
# contaminaria detection_stats, que es la fuente del baseline de BEH-09.
# Estos tests son la barrera que impide que eso vuelva a ser posible.
# ─────────────────────────────────────────────────────────────────────────────
```

Un test por linea del bloque `<behavior>`. Notas de implementacion:

- Para `TEST_object_class_does_not_reach_line_zone` usar un `PersonTracker` **real** (no
  `MagicMock`) con una linea que la persona cruce, y comparar `get_counts()` en dos
  escenarios: con y sin el coche en el `sv.Detections` de entrada. El aserto es de
  igualdad entre ambos, no un valor absoluto — asi el test no depende de la geometria
  concreta de la linea.
- Para `TEST_bytetrack_ids_do_not_migrate_between_classes`, el punto NO es afirmar que los
  ids sean numeros concretos (ambos ByteTrack empiezan en 1), sino que **el track de
  persona no hereda historia de la mochila**: comprobar que el tracker de personas nunca
  ha visto la caja de la mochila y que el id de persona nace en el frame en el que aparece
  la persona.
- Para `TEST_sync_frame_rate_reaches_both_trackers`, `MagicMock()` en ambos trackers y
  llamada directa a `worker._sync_tracker_frame_rate()` tras forzar el cambio de escalon
  de `AdaptiveRate`; afirmar `assert_called_once_with` en los dos.
  </action>

  <verify>
    <automated>F:/Documentos/IA/Proyecto_Camara/.venv/Scripts/python.exe -m pytest tests/test_detection_worker.py tests/test_tracker.py tests/test_track_registry.py -q</automated>
  </verify>

  <acceptance_criteria>
    - `pytest tests/test_detection_worker.py -k "object or split or class" -q` recoge >= 6 tests y sale verde.
    - `pytest tests/test_detection_worker.py -q` verde entero, sin regresion en los tests de las Fases 18-26.
    - `pytest tests/test_tracker.py -q` y `pytest tests/test_track_registry.py -q` verdes.
    - `grep -c "def _tracked_cls" tests/test_detection_worker.py` devuelve exactamente 1 (un solo helper nuevo).
    - `grep -n "core.py:104-110" tests/test_detection_worker.py` acierta (el motivo esta escrito, no solo el assert).
  </acceptance_criteria>

  <done>El riesgo ByteTrack queda cerrado con tests que fallarian si alguien volviera a mandar `sv_dets` completo al `PersonTracker` o metiera objetos en el `TrackRegistry`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| detector -> trackers | Detecciones de clases arbitrarias que entran a dos trackers con espacios de id independientes |
| event loop -> hilo de deteccion | `set_object_classes` lo escribe el endpoint y lo lee el bucle caliente |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-09 | Spoofing | `tracker_id` migrando entre clases en `sv.ByteTrack` | mitigate | Particion por `class_id` antes de cualquier `update()` y un `sv.ByteTrack` por grupo de clases. Verificado por `TEST_bytetrack_ids_do_not_migrate_between_classes` y `TEST_object_class_does_not_reach_line_zone` |
| T-27-10 | Tampering | conteo de linea de la Fase 4 (produccion) | mitigate | `PersonTracker.update` recibe `person_dets`, nunca `sv_dets`. Test de regresion por igualdad de `get_counts()` con y sin el coche |
| T-27-11 | Tampering | `TrackRegistry` como estado de personas con escritor unico | mitigate | Los objetos no entran en el registry: su estado vive en `self._object_boxes` bajo `self._lock`. Verificado por `TEST_objects_not_in_registry` |
| T-27-12 | Denial of Service | `AdaptiveRate` desincronizado del tracker de objetos | mitigate | `_sync_tracker_frame_rate` propaga a los dos trackers; verificado por `TEST_sync_frame_rate_reaches_both_trackers` |
| T-27-13 | Information Disclosure | `get_object_boxes()` exponiendo estado interno mutable | mitigate | Devuelve copias (`dict(b)`), verificado por `TEST_object_boxes_snapshot_is_a_copy` |
</threat_model>

<verification>
```bash
F:/Documentos/IA/Proyecto_Camara/.venv/Scripts/python.exe -m pytest tests/test_detection_worker.py tests/test_tracker.py tests/test_track_registry.py tests/test_architecture.py -q
```
</verification>

<success_criteria>
- `ObjectTracker` existe con su propio `sv.ByteTrack`, sin smoother, sin `LineZone` y con `set_frame_rate`.
- `DetectionWorker` parte `sv_dets` por clase dentro del `try` de inferencia y `PersonTracker` solo ve personas.
- `TrackRegistry`, `backend/pipeline/tracking.py` y `PersonTracker` sin cambios.
- `self._rate.observe()` sigue midiendo solo la via de personas.
- Los 7 tests de regresion pasan y `tests/test_architecture.py` sigue verde.
</success_criteria>

<output>
Al terminar, crear `.planning/phases/27-multi-clase-y-contexto-de-escena/27-03-SUMMARY.md`.
</output>
