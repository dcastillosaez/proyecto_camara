---
phase: 27-multi-clase-y-contexto-de-escena
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/perception/objects.py
  - tests/test_object_analyzer.py
  - tests/test_memory_bounds.py
autonomous: true
requirements: [BEH-07]

must_haves:
  truths:
    - "Un objeto inmovil 60 s sin ninguna persona dentro del radio produce exactamente un OBJECT_LEFT"
    - "El mismo objeto inmovil 70 s CON una persona a 100 px no produce ningun finding"
    - "Un objeto presente en los primeros 10 s de vida del analizador (mobiliario fijo) nunca emite nada"
    - "Un objeto dentro de una zona de exclusion nunca emite nada"
    - "Un objeto estable que desaparece con una persona cerca produce OBJECT_REMOVED al expirar la gracia"
    - "Una desaparicion sin persona cerca es silenciosa, y una oclusion de un frame no dispara nada"
    - "El estado del analizador esta acotado con o sin llamadas a prune()"
  artifacts:
    - path: "backend/perception/objects.py"
      provides: "ObjectKind, ObjectFinding, ObjectObservation, PersonObservation, ObjectAnalyzer (dominio puro, reloj inyectado)"
      contains: "class ObjectAnalyzer"
      min_lines: 200
    - path: "tests/test_object_analyzer.py"
      provides: "Los 9 comportamientos de BEH-07 con trayectorias sinteticas deterministas"
      contains: "TEST_object_left"
    - path: "tests/test_memory_bounds.py"
      provides: "Par con/sin prune de la doble guarda del ObjectAnalyzer"
      contains: "TEST_object_analyzer_bounded"
  key_links:
    - from: "backend/perception/objects.py"
      to: "reloj inyectado"
      via: "now: float en analyze() y prune(); ningun import de time"
      pattern: "def analyze\\("
    - from: "ObjectAnalyzer.analyze"
      to: "_enforce_cap"
      via: "cota dura tambien desde el camino de escritura"
      pattern: "self\\._enforce_cap\\(\\)"
---

<objective>
Crear `backend/perception/objects.py`: el dominio puro que decide `OBJECT_LEFT` y
`OBJECT_REMOVED` a partir de posiciones de objetos y personas, sin conocer el reloj de
pared, `camera_id`, `Event` ni `sv.Detections`.

Purpose: es la pieza sobre la que se apoyan los criterios 2, 3 y 5 del ROADMAP y la unica
que se puede verificar al 100 % sin camara. Va en wave 1 y sin dependencias porque no
importa nada del proyecto: `math`, `dataclasses`, `enum` y `collections.abc`, exactamente
como `behavior.py` (27-PATTERNS.md § objects.py).
Output: `ObjectAnalyzer` + `ObjectFinding` + ~12 tests deterministas + el par de cota.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/27-multi-clase-y-contexto-de-escena/27-CONTEXT.md
@.planning/phases/27-multi-clase-y-contexto-de-escena/27-PATTERNS.md
@backend/perception/behavior.py
@tests/test_behavior_analyzer.py

<interfaces>
<!-- CONTRATO LOCKED. Lo consumen 27-05 (emit_object) y 27-06 (cableado). No renombrar
     nada de aqui sin actualizar esos dos planes. -->

```python
# backend/perception/objects.py

class ObjectKind(str, Enum):
    LEFT = "left"          # -> EventType.OBJECT_LEFT   (Severity.WARNING por catalogo)
    REMOVED = "removed"    # -> EventType.OBJECT_REMOVED (Severity.INFO por catalogo)


@dataclass(frozen=True)
class ObjectObservation:
    """Un objeto trackeado visto en ESTE frame. La construye DetectionWorker."""
    track_id: int
    x: float                                  # ancla BOTTOM_CENTER, no centroide
    y: float
    class_id: int
    class_name: str
    bbox: tuple[float, float, float, float]
    zone_id: str | None = None                # zona en la que cae; None = escena
    excluded: bool = False                    # cae en una zona kind == "exclude_objects"


@dataclass(frozen=True)
class PersonObservation:
    """Una persona visible en ESTE frame. Solo lo que hace falta para el radio."""
    track_id: int
    x: float                                  # ancla BOTTOM_CENTER
    y: float
    height_px: float                          # alto de la bbox: corrige la escala


@dataclass
class ObjectFinding:
    kind: ObjectKind
    track_id: int
    class_id: int | None = None
    class_name: str | None = None
    bbox: tuple[float, float, float, float] | None = None
    zone_id: str | None = None
    duration_s: float | None = None           # NOMBRE OBLIGATORIO (rules.py:88-91)
    net_displacement_px: float | None = None
    person_distance_px: float | None = None
    person_track_id: int | None = None

    def magnitudes(self) -> dict[str, float | int | str]:
        """Payload del Event. OMITE los None (test_event_engine afirma que ningun
        valor del payload es None)."""


class ObjectAnalyzer:
    def __init__(
        self,
        left_secs: float = 60.0,              # <- config object_left_secs
        still_radius_px: float = 20.0,        # <- config object_still_radius_px
        person_radius_px: float = 150.0,      # <- config object_person_radius_px
        person_radius_ratio: float = 0.5,     # <- config object_person_radius_ratio
        warmup_secs: float = 10.0,            # <- config object_warmup_secs
        gone_secs: float = 3.0,               # <- config object_gone_secs
        person_window_secs: float = 10.0,     # <- config object_person_window_secs
        max_tracks: int = 256,                # <- config object_max_tracks
        state_ttl: float = 30.0,
    ) -> None: ...

    def analyze(
        self,
        objects: Sequence[ObjectObservation],
        persons: Sequence[PersonObservation],
        now: float,                           # monotonico INYECTADO
    ) -> list[ObjectFinding]: ...

    def prune(self, now: float, seen_ids: set[int]) -> list[ObjectFinding]:
        """OJO: a diferencia de BehaviorAnalyzer.prune, DEVUELVE findings —
        OBJECT_REMOVED se decide aqui. El llamador debe hacer `findings += prune(...)`."""
```

**Convencion de nombres:** los parametros del analizador son los de `config.py` **sin el
prefijo `object_`** (dentro de una clase llamada `ObjectAnalyzer` seria redundante). El
mapeo esta en los comentarios de arriba y lo aplica 27-06 al construirlo en `manager.py`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: backend/perception/objects.py — dominio puro de objetos abandonados y retirados</name>

  <read_first>
    - `backend/perception/behavior.py:1-15` — docstring de modulo que DECLARA la pureza
      (reloj inyectado, un solo hilo, que queda fuera de alcance). Es el molde literal.
    - `backend/perception/behavior.py:17-22` — la lista COMPLETA de imports permitidos.
      Nada de `numpy`, nada de `time`.
    - `backend/perception/behavior.py:24-73` — `BehaviorKind`, `BehaviorFinding`
      (`magnitudes()` que omite los `None`) y `_TrackAgg` (un comentario por campo
      explicando que invariante sostiene).
    - `backend/perception/behavior.py:160-190` — el bucle `for tid in sorted(...)` y el
      nucleo de inmovilidad: caja envolvente, `span`, reset del ancla, latch con
      re-armado y `round(..., 3)` en las magnitudes. Se copia tal cual.
    - `backend/perception/behavior.py:241-283` — `_enforce_cap()` llamado TAMBIEN desde
      el camino de escritura + `prune()` con la doble guarda (TTL + cota dura LRU).
  </read_first>

  <files>backend/perception/objects.py</files>

  <action>
Crear el modulo replicando la estructura de `behavior.py`. Cinco piezas.

**1. Docstring de modulo.** Mismo formato que `behavior.py:1-15`. Debe declarar
explicitamente que queda FUERA de alcance: construir `Event`, conocer `camera_id`,
conocer el reloj de pared, decidir que detecciones entran en el tracker (eso es
`DetectionWorker`) y calcular la pertenencia a zonas (eso ya lo hace
`sv.PolygonZone.trigger()` en `_update_zones_and_heat`). Citar
`27-RESEARCH.md Q1/Q2` como origen de los umbrales.

**2. Tipos.** `ObjectKind`, `ObjectObservation`, `PersonObservation`, `ObjectFinding`
exactamente como el bloque `<interfaces>`. `magnitudes()` devuelve un dict con
`duration_s`, `class_name`, `class_id`, `net_displacement_px`, `person_distance_px` y
`person_track_id`, **omitiendo los que sean `None`**.

**Por que `ObjectObservation`/`PersonObservation` y no los dicts de `behavior.py`:**
`BehaviorAnalyzer.analyze` recibe `dict[int, tuple[float,float]]` porque solo necesita el
centroide. Aqui cada objeto arrastra seis atributos (ancla, clase, nombre, bbox, zona,
exclusion) y seis dicts paralelos serian un criadero de bugs de desincronizacion.
Dejarlo escrito en un comentario junto a las dataclasses.

**3. `_ObjAgg`** — estado O(1) por objeto, molde `_TrackAgg` (`behavior.py:62-73`), con
un comentario por campo:

```python
@dataclass
class _ObjAgg:
    """Estado O(1) por objeto trackeado (patron _TrackAgg de behavior.py:62-73)."""

    first_seen: float                 # nacimiento del track: guarda contra el warmup
    anchor_t: float                   # ancla temporal del episodio de inmovilidad
    min_x: float                      # caja envolvente desde el ancla: su span ES el
    max_x: float                      # desplazamiento real (la distancia al ancla
    min_y: float                      # permitiria un diametro de 2R)
    max_y: float
    last_seen: float                  # base del TTL, de la gracia de desaparicion y del LRU
    last_x: float                     # ultima posicion conocida: la necesita OBJECT_REMOVED
    last_y: float                     # cuando el track ya no esta en el frame
    class_id: int                     # se congela del ultimo frame visto: el payload del
    class_name: str                   # REMOVED se construye cuando el objeto ya no existe
    bbox: tuple[float, float, float, float]
    zone_id: str | None = None
    last_person_near_t: float | None = None   # LA CLAVE de OBJECT_REMOVED (Patron 3)
    last_person_dist: float | None = None
    last_person_track_id: int | None = None
    left_latched: bool = False        # un unico OBJECT_LEFT por episodio (criterio 5)
    stable: bool = False              # llego a considerarse establecido: ya se puede retirar
```

**4. `analyze()`.** Estructura obligatoria:

```python
    def analyze(self, objects, persons, now) -> list[ObjectFinding]:
        if self._started_at is None:
            self._started_at = now          # el analizador no tiene reloj: lo fija el
                                            # primer frame que lo llama
        findings: list[ObjectFinding] = []
        for obs in sorted(objects, key=lambda o: o.track_id):   # orden determinista
            ...
        self._enforce_cap()                 # cota dura tambien desde este camino de
        return findings                     # escritura (behavior.py:241-243)
```

Cuerpo del bucle, en este orden exacto:

a. **Guarda de ignorados.** Si `obs.track_id in self._ignored`: refrescar
   `self._ignored[tid] = now` y `continue`. Nunca vuelve a ser candidato.

b. **Guarda de warmup y de zona de exclusion (solo al nacer / al entrar en zona).**
   - Si no hay agregado y `now - self._started_at < self._warmup_secs` -> `self._ignored[tid] = now`, `continue`.
     Es la guarda (a) de 27-RESEARCH Q2: en el arranque todo lo que esta en escena nace
     como track nuevo y es indistinguible de algo que acaba de aparecer.
   - Si `obs.excluded` -> `self._aggs.pop(tid, None)`, `self._ignored[tid] = now`, `continue`.
     Guarda (b): zona `kind == "exclude_objects"`.
   - Si no hay agregado y ninguna guarda aplica, crearlo con `first_seen=now`,
     `anchor_t=now` y la caja envolvente degenerada en `(obs.x, obs.y)`.

c. **Refrescar los campos volatiles** del agregado: `last_seen`, `last_x/last_y`,
   `class_id`, `class_name`, `bbox`, `zone_id`.

d. **Persona cerca (Patron 3 del research).** Para cada persona:
   `r = max(self._person_radius_px, self._person_radius_ratio * p.height_px)` y
   `d = math.hypot(obs.x - p.x, obs.y - p.y)`. Si `d <= r`, quedarse con la MENOR `d` del
   frame y escribir `last_person_near_t = now`, `last_person_dist`, `last_person_track_id`.
   El radio corrige la escala: una persona de 300 px de alto da 150 px, una de 500 px
   (muy cerca de la camara) da 250 px, una lejana se queda en el suelo de 150 px.

e. **Inmovilidad — copiar `behavior.py:175-190` literalmente**, cambiando el nombre del
   umbral y del latch:

```python
            agg.min_x = min(agg.min_x, obs.x); agg.max_x = max(agg.max_x, obs.x)
            agg.min_y = min(agg.min_y, obs.y); agg.max_y = max(agg.max_y, obs.y)
            span = max(agg.max_x - agg.min_x, agg.max_y - agg.min_y)
            if span > self._still_radius_px:
                agg.anchor_t = now                      # se movio: episodio nuevo
                agg.min_x = agg.max_x = obs.x
                agg.min_y = agg.max_y = obs.y
                agg.left_latched = False                # re-armado
                agg.stable = False
            else:
                dur = now - agg.anchor_t
                if dur >= self._gone_secs:
                    agg.stable = True                   # ver nota de diseño abajo
                if (
                    not agg.left_latched
                    and dur > self._left_secs
                    and (agg.last_person_near_t is None
                         or now - agg.last_person_near_t > self._left_secs)
                ):
                    agg.left_latched = True
                    findings.append(ObjectFinding(
                        kind=ObjectKind.LEFT, track_id=obs.track_id,
                        class_id=obs.class_id, class_name=obs.class_name,
                        bbox=obs.bbox, zone_id=obs.zone_id,
                        duration_s=round(dur, 3),
                        net_displacement_px=round(span, 3),
                    ))
```

**Nota de diseño obligatoria en el codigo (comentario):** `stable` se deriva de
`object_gone_secs` a proposito, **sin introducir un parametro nuevo**: un objeto que se ha
mantenido quieto al menos tanto como la ventana de gracia con la que se declara su
desaparicion es lo minimo que puede llamarse "establecido". Atarlo en cambio a
`object_left_secs` haria que una maleta dejada 20 s y recogida no pudiera producir nunca
`OBJECT_REMOVED`.

**Nota de diseño obligatoria (comentario) sobre la condicion negativa de `OBJECT_LEFT`:**
el radio se usa aqui en sentido NEGATIVO ("60 s sin ninguna persona dentro"), asi que
pasarse de grande SUPRIME eventos y es el lado seguro; en `OBJECT_REMOVED` se usa en
sentido POSITIVO y pasarse es peligroso. Es la asimetria de 27-RESEARCH Q1: si la
calibracion con camara real obliga a separarlos, hay que SUBIR el de `OBJECT_LEFT` y
BAJAR el de `OBJECT_REMOVED`, nunca al reves.

**5. `prune()` + `_enforce_cap()`** — molde `behavior.py:245-283`, con LA diferencia
estructural de esta fase: **devuelve findings**.

```python
    def prune(self, now: float, seen_ids: set[int]) -> list[ObjectFinding]:
        """Doble guarda de expiracion (TTL + cota dura) que ADEMAS decide OBJECT_REMOVED.

        La desaparicion es la señal mas ruidosa de todas: se decide aqui y no en
        analyze() para poder exigir `gone_secs` de gracia — sin ella una oclusion de
        un frame (alguien pasa por delante) emitiria el evento (27-RESEARCH Pitfall 10).
        """
        findings: list[ObjectFinding] = []
        for tid, agg in list(self._aggs.items()):
            if tid in seen_ids:
                agg.last_seen = now
                continue
            if now - agg.last_seen <= self._gone_secs:
                continue                         # gracia: puede ser una oclusion
            if (
                agg.stable
                and agg.last_person_near_t is not None
                and now - agg.last_person_near_t <= self._person_window_secs
            ):
                findings.append(ObjectFinding(
                    kind=ObjectKind.REMOVED, track_id=tid,
                    class_id=agg.class_id, class_name=agg.class_name,
                    bbox=agg.bbox, zone_id=agg.zone_id,
                    duration_s=round(agg.last_seen - agg.anchor_t, 3),
                    person_distance_px=(round(agg.last_person_dist, 3)
                                        if agg.last_person_dist is not None else None),
                    person_track_id=agg.last_person_track_id,
                ))
            del self._aggs[tid]                  # sin persona cerca: desaparicion silenciosa
        for tid in list(self._ignored):
            if now - self._ignored[tid] > self._state_ttl:
                del self._ignored[tid]
        self._enforce_cap()
        return findings
```

`_enforce_cap()` calcado de `behavior.py:270-283`: si `len(self._aggs) > self._max_tracks`,
borrar los `overflow` mas antiguos por `last_seen` (LRU); lo mismo para `self._ignored`
por su propio valor de tiempo. Docstring citando el "seguro de vida" de la Fase 22: actua
aunque nadie llame a `prune()` a tiempo.

**Estructuras de estado:** `self._aggs: dict[int, _ObjAgg]`,
`self._ignored: dict[int, float]` (track_id -> ultimo instante visto). `_ignored` es un
`dict` y no un `set` porque necesita `last_seen` para el TTL y el LRU, igual que
`_loiter` en `behavior.py`.

**PROHIBIDO en este fichero:** `import time`, `import numpy`, cualquier import de
`backend.events`, `backend.pipeline` o `supervision`. `tests/test_architecture.py` y la
direccion de dependencias `events/ -> perception/` dependen de ello.
  </action>

  <verify>
    <automated>F:/Documentos/IA/Proyecto_Camara/.venv/Scripts/python.exe -c "import ast,sys; src=open('backend/perception/objects.py',encoding='utf-8').read(); t=ast.parse(src); mods={(n.module or '') for n in ast.walk(t) if isinstance(n,ast.ImportFrom)}|{a.name.split('.')[0] for n in ast.walk(t) if isinstance(n,ast.Import) for a in n.names}; bad=[m for m in mods if m.split('.')[0] in ('time','numpy','supervision','cv2','backend')]; assert not bad, bad; from backend.perception.objects import ObjectAnalyzer, ObjectFinding, ObjectKind, ObjectObservation, PersonObservation; a=ObjectAnalyzer(); assert a.analyze([],[],0.0)==[]; assert a.prune(0.0,set())==[]; print('OK')"</automated>
  </verify>

  <acceptance_criteria>
    - `backend/perception/objects.py` existe y define `ObjectKind`, `ObjectObservation`, `PersonObservation`, `ObjectFinding` y `ObjectAnalyzer`.
    - El comando de `<verify>` imprime `OK` (sin `time`/`numpy`/`supervision`/`backend.*` importados).
    - `grep -n "def prune" backend/perception/objects.py` y su firma acaba en `-> list[ObjectFinding]`.
    - `grep -c "self._enforce_cap()" backend/perception/objects.py` devuelve >= 2 (analyze + prune).
    - `grep -n "round(" backend/perception/objects.py` acierta en las magnitudes.
    - `git diff --stat backend/perception/behavior.py` esta vacio (D-1 del research: no se toca).
  </acceptance_criteria>

  <done>`ObjectAnalyzer` importa limpio, es puro (sin reloj ni I/O) y expone `analyze() -> list[ObjectFinding]` y `prune() -> list[ObjectFinding]` con la doble guarda de memoria.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: tests/test_object_analyzer.py — los 9 comportamientos de BEH-07</name>

  <read_first>
    - `tests/test_behavior_analyzer.py:1-46` — docstring del fichero (declara dominio
      puro, reloj sintetico, trayectorias deterministas) y el helper `_jitter_run` con su
      ciclo de 3 fases. Es exactamente el escenario "objeto inmovil con ruido de bbox".
    - `tests/test_behavior_analyzer.py:47-75` — `TEST_immobile_after_threshold`: el estilo
      de aserto (`len(imm) == 1`, `f.kind is <Kind>` con `is`, no `==`).
    - `backend/perception/objects.py` — el modulo escrito en el Task 1.
  </read_first>

  <files>tests/test_object_analyzer.py</files>

  <behavior>
    - `TEST_object_left_after_threshold`: objeto quieto (jitter ±2 px) 70 s a 8 FPS, cero personas -> exactamente 1 finding, `kind is ObjectKind.LEFT`, `duration_s > 60`, `net_displacement_px <= 20`.
    - `TEST_object_left_latched`: la misma trayectoria prolongada a 200 s -> sigue habiendo exactamente 1 LEFT (criterio 5).
    - `TEST_object_left_only_kind`: el conjunto completo de kinds del run es `{ObjectKind.LEFT}` — la mitad dificil del criterio 5 es "y ninguno mas".
    - `TEST_no_left_with_person`: objeto quieto 70 s con una persona a 100 px en TODOS los frames -> `findings == []`.
    - `TEST_left_after_person_leaves`: persona cerca los primeros 10 s y luego ausente -> el LEFT se emite, pero solo cuando han pasado `left_secs` desde la ultima vez que hubo persona.
    - `TEST_warmup_furniture`: objeto presente desde el primer frame (t=0) y quieto 200 s -> `findings == []` y `track_id in analyzer._ignored`.
    - `TEST_excluded_zone`: objeto nacido a t=60 (fuera del warmup) con `excluded=True` y quieto 200 s -> `findings == []`.
    - `TEST_object_removed`: objeto estable 30 s con una persona a 100 px en el ultimo frame; deja de verse; `prune` a `now + gone_secs + 0.1` -> 1 finding `REMOVED` con `person_track_id` y `person_distance_px` presentes.
    - `TEST_removed_needs_person`: idem sin ninguna persona en toda la vida del objeto -> `prune` devuelve `[]` y el agregado se borra igual.
    - `TEST_occlusion_grace`: objeto estable que falta UN frame (0,125 s) y vuelve -> ningun finding y el track sobrevive.
    - `TEST_object_payload_has_no_none`: `magnitudes()` de un LEFT y de un REMOVED no contienen ningun valor `None` y el LEFT trae la clave literal `duration_s`.
  </behavior>

  <action>
Fichero nuevo `tests/test_object_analyzer.py`. Nombres `TEST_*` obligatorios
(`pytest.ini` fija `python_functions = TEST_*`; en Linux los `test_*` no se recogen).

Docstring del fichero calcado de `tests/test_behavior_analyzer.py:1-11`: dominio puro,
sin hilos, sin bus, sin reloj real; el reloj es un float sintetico; las trayectorias son
deterministas.

Dos helpers locales, **uno solo de cada** (no duplicar variantes):

```python
def _obj(tid=1, x=100.0, y=200.0, class_id=24, class_name="backpack",
         zone_id=None, excluded=False):
    return ObjectObservation(track_id=tid, x=x, y=y, class_id=class_id,
                             class_name=class_name, bbox=(x - 20, y - 40, x + 20, y),
                             zone_id=zone_id, excluded=excluded)


def _still_run(analyzer, n, dt=0.125, amp=2.0, start_t=0.0, persons=(), **kw):
    """`n` frames de jitter ciclico de 3 fases (-amp, 0, +amp) alrededor del objeto.

    Ciclo de 3 fases igual que _jitter_run de test_behavior_analyzer.py: el
    desplazamiento entre fases consecutivas es `amp`, nunca `2*amp`, asi que el span
    de la caja envolvente se queda por debajo de still_radius_px.
    """
```

`persons` acepta una lista fija o un callable `(i, t) -> list[PersonObservation]` para el
caso `TEST_left_after_person_leaves`. `dt=0.125` = 8 FPS, el mismo ritmo que usa la Fase
26.

Un test por linea del bloque `<behavior>`, cada uno precedido de una banda de comentario
`# ─── ... ───` con el motivo cuando no sea obvio. Los tres puntos que hay que dejar
escritos en comentarios:

1. En `TEST_warmup_furniture`, citar 27-RESEARCH Q2: en el primer frame todo lo que esta
   en escena nace como track nuevo y es indistinguible de algo que acaba de aparecer;
   la ventana de warmup es lo que separa "mobiliario" de "abandonado".
2. En `TEST_no_left_with_person`, citar el criterio 5 del ROADMAP y que la aserto es
   sobre el conjunto vacio, no sobre la ausencia de un kind concreto.
3. En `TEST_occlusion_grace`, citar Pitfall 10: sin `gone_secs` una oclusion de 125 ms
   emitiria `OBJECT_REMOVED`.

Para los tests que usan `prune`, recordar que `prune` DEVUELVE findings: los asertos van
sobre su valor de retorno, no sobre el de `analyze`.
  </action>

  <verify>
    <automated>F:/Documentos/IA/Proyecto_Camara/.venv/Scripts/python.exe -m pytest tests/test_object_analyzer.py -q</automated>
  </verify>

  <acceptance_criteria>
    - `pytest tests/test_object_analyzer.py -q` recoge >= 10 tests y sale verde.
    - `pytest tests/test_object_analyzer.py -k "TEST_object_left_latched or TEST_no_left_with_person" -q` (criterio 5 del ROADMAP) sale verde.
    - `grep -c "^def TEST_" tests/test_object_analyzer.py` devuelve >= 10.
    - `grep -n "== \[\]" tests/test_object_analyzer.py` acierta en `TEST_no_left_with_person` (igualdad con la lista vacia, no `not in`).
    - `grep -n "kind is ObjectKind" tests/test_object_analyzer.py` acierta (identidad, no `==`).
    - `grep -c "def _still_run\|def _obj" tests/test_object_analyzer.py` devuelve exactamente 2 (un helper de cada, sin duplicados).
  </acceptance_criteria>

  <done>Los criterios 2, 3 y 5 del ROADMAP quedan demostrados con trayectorias sinteticas deterministas, incluida la igualdad de conjunto de kinds.</done>
</task>

<task type="auto">
  <name>Task 3: tests/test_memory_bounds.py — par con/sin prune del ObjectAnalyzer</name>

  <read_first>
    - `tests/test_memory_bounds.py:316-341` — `TEST_behavior_state_bounded` y
      `TEST_behavior_state_bounded_without_prune`: la banda de comentario con el motivo,
      el bucle de `10_000`, el acceso directo al atributo privado (convencion aceptada en
      este fichero) y **el par**, que es lo que demuestra la doble guarda.
  </read_first>

  <files>tests/test_memory_bounds.py</files>

  <action>
Añadir al final del fichero el par, precedido de su banda de comentario:

```python
# ─── ObjectAnalyzer: _aggs / _ignored no acumulan objetos efimeros ───────────
# ByteTrack asigna ids monotonamente crecientes y nunca los reutiliza, y el
# ObjectTracker de objetos tiene su propio contador (27-RESEARCH Q2/Q4). Sin
# doble guarda (TTL de state_ttl + cota dura max_tracks) un proceso 24/7 haria
# crecer _aggs con cada objeto que pasa y _ignored con cada objeto nacido dentro
# de la ventana de warmup o dentro de una zona de exclusion.
# ─────────────────────────────────────────────────────────────────────────────
def TEST_object_analyzer_bounded():
    analyzer = ObjectAnalyzer(max_tracks=256, state_ttl=30.0)
    for tid in range(10_000):
        now = float(tid)
        analyzer.analyze([_obj_at(tid)], [], now)
        analyzer.prune(now, seen_ids={tid})
    assert len(analyzer._aggs) <= 256
    assert len(analyzer._ignored) <= 256


def TEST_object_analyzer_bounded_without_prune():
    """La cota dura actua aunque el mantenimiento periodico nunca se ejecute."""
    analyzer = ObjectAnalyzer(max_tracks=256)
    for tid in range(10_000):
        analyzer.analyze([_obj_at(tid)], [], float(tid))
    assert len(analyzer._aggs) <= 256


def TEST_object_analyzer_ignored_bounded():
    """Los ids ignorados por warmup son la estructura NUEVA de esta fase: 10.000
    objetos naciendo dentro de la ventana de warmup no pueden dejar 10.000 entradas."""
    analyzer = ObjectAnalyzer(max_tracks=256, warmup_secs=1e9)
    for tid in range(10_000):
        analyzer.analyze([_obj_at(tid)], [], float(tid) * 0.001)
    assert len(analyzer._ignored) <= 256
```

`_obj_at(tid)` es un helper local minimo (mismo espiritu que `_fake_tracked` de
`test_memory_bounds.py:34-40`): construye un `ObjectObservation` con ese `track_id`. Si
`ObjectObservation` resulta demasiado verboso aqui, definirlo una sola vez arriba del
bloque, nunca uno por test.

Ojo con `TEST_object_analyzer_bounded`: el bucle avanza `now` un segundo por iteracion,
asi que el TTL por si solo ya vaciaria el dict; el valor del test esta en el par — el
segundo, sin `prune()`, es el que demuestra que `_enforce_cap()` se llama tambien desde
`analyze()`.
  </action>

  <verify>
    <automated>F:/Documentos/IA/Proyecto_Camara/.venv/Scripts/python.exe -m pytest tests/test_memory_bounds.py -q</automated>
  </verify>

  <acceptance_criteria>
    - `pytest tests/test_memory_bounds.py -k object_analyzer -q` recoge 3 tests y sale verde.
    - `pytest tests/test_memory_bounds.py -q` sigue verde entero (sin regresion en los tests de las Fases 22-26).
    - `grep -n "_ignored" tests/test_memory_bounds.py` acierta.
  </acceptance_criteria>

  <done>El estado del `ObjectAnalyzer` esta acotado con y sin `prune()`, incluido el diccionario de ids ignorados.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| detecciones del modelo -> analizador de dominio | Posiciones y clases derivadas de un frame no confiable (escena real, ruido de bbox, oclusiones) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-01 | Denial of Service | `ObjectAnalyzer._aggs` / `_ignored` | mitigate | Doble guarda: TTL `state_ttl` en `prune()` + cota dura LRU `max_tracks` en `_enforce_cap()`, invocada TAMBIEN desde `analyze()`. Verificado por el par de `tests/test_memory_bounds.py` |
| T-27-02 | Denial of Service | rafaga de `OBJECT_LEFT` (`Severity.WARNING` -> subida de clips a Drive) | mitigate | Latch `left_latched` por episodio con re-armado solo al moverse: un objeto quieto 10 min produce 1 evento, no ~4.800 a 8 FPS. Verificado por `TEST_object_left_latched` |
| T-27-03 | Spoofing | mobiliario fijo confundido con objeto abandonado | mitigate | Guarda (a) de warmup (`object_warmup_secs`) + guarda (b) de zona `exclude_objects`, ambas en memoria y sin estado persistente (27-RESEARCH Q2). Verificado por `TEST_warmup_furniture` y `TEST_excluded_zone` |
| T-27-04 | Repudiation | `OBJECT_REMOVED` por oclusion de un frame | mitigate | La decision vive en `prune()` con `object_gone_secs` de gracia, nunca en `analyze()`. Verificado por `TEST_occlusion_grace` |
</threat_model>

<verification>
```bash
F:/Documentos/IA/Proyecto_Camara/.venv/Scripts/python.exe -m pytest tests/test_object_analyzer.py tests/test_memory_bounds.py -q
```
</verification>

<success_criteria>
- `backend/perception/objects.py` es dominio puro: sin `time`, sin `numpy`, sin imports de `backend.*`.
- `analyze()` emite un unico `OBJECT_LEFT` por episodio y ninguno con una persona dentro del radio.
- `prune()` devuelve `OBJECT_REMOVED` solo si el objeto era estable y hubo una persona cerca dentro de la ventana.
- El estado esta acotado con y sin `prune()`.
- `backend/perception/behavior.py` sin cambios.
</success_criteria>

<output>
Al terminar, crear `.planning/phases/27-multi-clase-y-contexto-de-escena/27-01-SUMMARY.md`.
</output>
