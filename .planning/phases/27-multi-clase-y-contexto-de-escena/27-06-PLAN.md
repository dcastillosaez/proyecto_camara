---
phase: 27-multi-clase-y-contexto-de-escena
plan: 06
type: execute
wave: 3
depends_on: [27-01, 27-03, 27-04, 27-05]
files_modified:
  - backend/pipeline/detection.py
  - backend/pipeline/manager.py
  - backend/main.py
  - tests/test_detection_worker.py
autonomous: true
requirements: [BEH-06, BEH-07]

must_haves:
  truths:
    - "Un objeto inmovil sin persona cerca produce un OBJECT_LEFT real por el camino del worker"
    - "Un fallo del ObjectAnalyzer incrementa _exceptions y no mata el hilo de deteccion"
    - "El ObjectAnalyzer y el ObjectTracker sobreviven a un reinicio del DetectionWorker por el supervisor"
    - "Con objects_enabled=False el pipeline arranca sin analizador ni tracker de objetos"
    - "Las zonas con kind == 'exclude_objects' llegan al worker y suprimen los candidatos que caen dentro"
    - "CameraPipeline.set_detection_classes aplica el cambio al detector y al reparto sin reiniciar ningun worker"
  artifacts:
    - path: "backend/pipeline/detection.py"
      provides: "_analyze_objects + propagacion de kind a _zone_states"
      contains: "def _analyze_objects"
    - path: "backend/pipeline/manager.py"
      provides: "ObjectAnalyzer y ObjectTracker construidos FUERA de la factoria + fachada set_detection_classes"
      contains: "def set_detection_classes"
    - path: "tests/test_detection_worker.py"
      provides: "Tests de cableado, aislamiento de fallos y supervivencia a reinicio"
      contains: "TEST_object_analyzer_survives_worker_restart"
  key_links:
    - from: "backend/pipeline/manager.py"
      to: "ObjectAnalyzer / ObjectTracker"
      via: "construidos en CameraPipeline.__init__, nunca dentro de _make_detection"
      pattern: "self\\.objects = ObjectAnalyzer\\("
    - from: "backend/pipeline/detection.py"
      to: "backend/events/engine.py"
      via: "emit_object por cada finding, fuera del try"
      pattern: "self\\._event_engine\\.emit_object\\("
---

<objective>
Cablear el analizador de objetos al hilo de deteccion: alimentarlo con las anclas de
objetos y personas del frame, recoger sus veredictos (incluidos los de `prune`), traducirlos
a eventos y construirlo — junto al `ObjectTracker` — **fuera** de la factoria del
`WorkerSupervisor`.

Purpose: es el plan con mas riesgo operativo de la fase despues de 27-03. Si el analizador
o el tracker se construyeran dentro de `_make_detection`, cada reinicio del worker
reiniciaria los `track_id` de objeto y reabriria la ventana de warmup: todo el mobiliario
fijo "apareceria" de nuevo y a los 60 s se emitiria una rafaga de `OBJECT_LEFT` que, al ser
`WARNING`, **subiria un clip a Google Drive por cada mueble**. El repo ya resolvio este
mismo problema tres veces (FSM de identidad en la Fase 24, galeria ReID en la 25,
`BehaviorAnalyzer` en la 26) y hay test para ello.
Output: `_analyze_objects` + construccion fuera de la factoria + fachada + 7 tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/27-multi-clase-y-contexto-de-escena/27-PATTERNS.md
@.planning/phases/27-multi-clase-y-contexto-de-escena/27-01-SUMMARY.md
@.planning/phases/27-multi-clase-y-contexto-de-escena/27-03-SUMMARY.md
@.planning/phases/27-multi-clase-y-contexto-de-escena/27-05-SUMMARY.md
@backend/pipeline/detection.py
@backend/pipeline/manager.py

<interfaces>
<!-- CONTRATO LOCKED. Lo consumen 27-07 (PUT de clases), 27-08 (overlay) y 27-09 (contexto). -->

```python
# backend/pipeline/detection.py — DetectionWorker
def _analyze_objects(self, obj_tracked, tracked, captured_at: float, processed_at: float) -> None: ...
# constructor: nuevo parametro al final, con default
        objects: "ObjectAnalyzer | None" = None,

# backend/pipeline/manager.py — CameraPipeline
self.objects: "ObjectAnalyzer | None" = None
self.object_tracker: "ObjectTracker | None" = None

def set_detection_classes(self, classes: list[int]) -> None: ...
def get_object_stats(self) -> list[dict]: ...
def get_object_boxes(self) -> list[dict]: ...
```

Parametros nuevos de `CameraPipeline.__init__` (bloque por fase al final de la firma;
convencion de las Fases 24/25/26: los nombres sueltan el prefijo `object_` solo cuando
`config.py` tampoco lo lleva — aqui **se conservan tal cual** para que `main.py` sea
`nombre=settings.nombre` linea a linea):

```python
        objects_enabled: bool = True,
        object_class_ids: list[int] | None = None,
        object_left_secs: float = 60.0,
        object_still_radius_px: float = 20.0,
        object_person_radius_px: float = 150.0,
        object_person_radius_ratio: float = 0.5,
        object_warmup_secs: float = 10.0,
        object_gone_secs: float = 3.0,
        object_person_window_secs: float = 10.0,
        object_max_tracks: int = 256,
```

Mapeo a los parametros del analizador (que sueltan el prefijo, ver 27-01):
`object_left_secs -> left_secs`, `object_still_radius_px -> still_radius_px`,
`object_person_radius_px -> person_radius_px`, `object_person_radius_ratio -> person_radius_ratio`,
`object_warmup_secs -> warmup_secs`, `object_gone_secs -> gone_secs`,
`object_person_window_secs -> person_window_secs`, `object_max_tracks -> max_tracks`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: DetectionWorker._analyze_objects + zonas de exclusion</name>

  <read_first>
    - `backend/pipeline/detection.py:191-227` — `_analyze_behavior` ENTERO: es el molde
      literal (guardas de `None`, `try` que envuelve analyze **y** prune juntos, `except`
      con `self._exceptions += 1` + `logger.exception` + `return`, y el bucle de emision
      **fuera** del `try` con `wall_now` calculado una sola vez).
    - `backend/pipeline/detection.py:229-238` — `_zone_membership_snapshot`: reutiliza
      `st["inside"]` en vez de recalcular la geometria.
    - `backend/pipeline/detection.py:289-300` — el bucle de `st["zone"].trigger(tracked)`
      y `np.flatnonzero(mask)`: el molde para calcular la pertenencia de los OBJETOS.
    - `backend/pipeline/detection.py:312` — `tracked.get_anchors_coordinates(sv.Position.BOTTOM_CENTER)`,
      ya usado en este mismo fichero para el heatmap.
    - `backend/pipeline/detection.py:316-338` — `_rebuild_zone_states`: donde se añade la
      clave `kind` al dict de estado, igual que `id` y `name`.
    - `backend/pipeline/detection.py:164-189` — el punto exacto donde encaja la llamada,
      con el estilo `# NUEVO (Fase N)` que introdujo la Fase 26.
  </read_first>

  <files>backend/pipeline/detection.py</files>

  <action>
**1. Constructor:** añadir `objects: "ObjectAnalyzer | None" = None` al final de la firma
(despues de `behavior`), guardar `self._objects = objects`, e importar `ObjectAnalyzer`
bajo `TYPE_CHECKING`.

**2. `kind` en `_zone_states`:** en `_rebuild_zone_states` (`detection.py:328-336`), añadir
`"kind": z.get("kind"),` al dict, igual que `id` y `name`. El dict `z` ya lo trae desde
27-04.

**3. Llamada en el bucle caliente**, justo despues de `self._analyze_behavior(...)`
(`detection.py:186`), con el mismo estilo:
```python
            self._analyze_objects(obj_tracked, tracked, frame.captured_at, now)  # NUEVO (Fase 27)
```

**4. `_analyze_objects`**, calcado de `_analyze_behavior`:

```python
    def _analyze_objects(self, obj_tracked, tracked, captured_at, processed_at) -> None:
        """Analisis de objetos abandonados/retirados del frame (Fase 27, BEH-07).

        Mismo patron de aislamiento de fallos que _analyze_behavior (que a su vez viene de
        RecognitionWorker._sync_identity, recognition.py:366-374): un fallo del analizador
        nunca mata el hilo de deteccion, solo incrementa el contador de excepciones que ya
        expone /api/v2/cameras/{id}/health.

        Las distancias se miden entre anclas BOTTOM_CENTER, no entre centroides: una
        mochila en el suelo junto a una persona de pie tiene el centroide desplazado media
        altura de persona aunque se esten tocando (27-RESEARCH.md Q1). Es la misma
        convencion que ya usan el LineZone (tracker.py:41) y el heatmap (linea 312).
        """
        if self._objects is None or self._event_engine is None or obj_tracked is None:
            return
        obj_ids = obj_tracked.tracker_id
        if obj_ids is None:
            return
        try:
            excluded = self._excluded_object_ids(obj_tracked)
            zone_of = self._object_zone_ids(obj_tracked)
            observations = [...]          # ObjectObservation por objeto trackeado
            persons = [...]               # PersonObservation por persona de `tracked`
            findings = self._objects.analyze(observations, persons, processed_at)
            findings += self._objects.prune(processed_at, {int(t) for t in obj_ids})
        except Exception:
            self._exceptions += 1
            logger.exception("DetectionWorker: analisis de objetos fallo")
            return
        wall_now = datetime.datetime.now()
        for f in findings:
            self._event_engine.emit_object(f, wall_now, captured_at, processed_at)
```

Tres diferencias con el molde que hay que respetar:
- **Dos entradas**: los objetos (`obj_tracked`) y las personas (`tracked`), estas ultimas
  para el radio "persona cerca" y para el alto de su bbox.
- **`findings += prune(...)`**: a diferencia de `BehaviorAnalyzer.prune`, el de objetos
  DEVUELVE veredictos (`OBJECT_REMOVED` se decide al expirar). Ignorar el retorno seria
  perder la mitad del requisito. El precedente de consumo es
  `transitions += self._fsm.on_tick(now)` (`recognition.py:640`).
- Las anclas salen de `get_anchors_coordinates(sv.Position.BOTTOM_CENTER)` para **ambos**
  conjuntos; el alto de persona es `y2 - y1` de su `xyxy`.

**5. Dos helpers privados**, ambos bajo el mismo `self._lock` y reutilizando el estado que
ya calculo `_update_zones_and_heat`:

```python
    def _excluded_object_ids(self, obj_tracked) -> set[int]:
        """Ids de objeto que caen dentro de una zona kind == "exclude_objects".

        Se usa sv.PolygonZone.trigger() sobre los mismos _zone_states que ya existen en
        vez de escribir un test punto-en-poligono propio: recalcularlo duplicaria la
        inferencia geometrica y podria divergir del conteo de get_zone_stats()
        (mismo motivo que _zone_membership_snapshot, linea 229).
        """

    def _object_zone_ids(self, obj_tracked) -> dict[int, str | None]:
        """Zona en la que cae cada objeto, para el zone_id del evento. None = escena."""
```

El bucle a imitar es el de `detection.py:289-300` (`st["zone"].trigger(...)` +
`np.flatnonzero(mask)`), filtrando por `st["kind"] == "exclude_objects"` en el primero.

**Lo que NO cambia:** `_update_zones_and_heat` sigue alimentandose solo de `tracked`
(zonas y heatmap son de personas), `self._rate.observe()` sigue midiendo solo la via de
personas, y `TrackRegistry` sigue sin ver objetos.
  </action>

  <verify>
    <automated>F:/Documentos/IA/Proyecto_Camara/.venv/Scripts/python.exe -m pytest tests/test_detection_worker.py tests/test_architecture.py tests/test_workers_isolation.py -q</automated>
  </verify>

  <acceptance_criteria>
    - `grep -n "def _analyze_objects" backend/pipeline/detection.py` acierta.
    - `grep -n "findings += self._objects.prune" backend/pipeline/detection.py` acierta (el retorno del prune NO se ignora).
    - `grep -n "BOTTOM_CENTER" backend/pipeline/detection.py` acierta al menos 2 veces (heatmap + objetos).
    - `grep -n "exclude_objects" backend/pipeline/detection.py` acierta.
    - `grep -n '"kind": z' backend/pipeline/detection.py` acierta dentro de `_rebuild_zone_states`.
    - `grep -n "emit_object" backend/pipeline/detection.py` acierta FUERA del bloque `try` (bucle de emision al final del metodo).
    - `pytest tests/test_architecture.py -q` verde.
  </acceptance_criteria>

  <done>El hilo de deteccion alimenta al `ObjectAnalyzer` con anclas y zonas de exclusion, recoge tambien los veredictos del `prune` y emite eventos con aislamiento de fallos.</done>
</task>

<task type="auto">
  <name>Task 2: manager.py — construccion FUERA de la factoria + fachada; main.py — propagacion</name>

  <read_first>
    - `backend/pipeline/manager.py:98-124` — la declaracion de los atributos y el bloque de
      la Fase 26 (`BehaviorAnalyzer` construido fuera de la factoria, con el comentario que
      explica el porque). Los otros dos precedentes: `165-178` (FSM, Fase 24) y `180-192`
      (ReID, Fase 25).
    - `backend/pipeline/manager.py:126-140` — `_make_detection`: la factoria que el
      supervisor SI re-ejecuta en cada reinicio, y donde entran los kwargs.
    - `backend/pipeline/manager.py:72-81` — el bloque de parametros por fase al final de la
      firma de `__init__`.
    - `backend/pipeline/manager.py:282-287` — `set_zones` / `get_zone_stats`: el molde de
      la fachada (guarda `if self.detection:` y delegacion).
    - `backend/pipeline/manager.py:301-308` — `set_process_size`: **el contra-ejemplo**.
      Ahi si se reinicia un worker, pero es el `CaptureWorker` y fuera del supervisor. El
      `DetectionWorker` no se reinicia nunca para aplicar configuracion.
    - `backend/pipeline/supervisor.py:153-181` — `_check()`: por que un `stop()` cuenta como
      caida y tres en 60 s dejan el worker en `FAILED`.
    - `backend/main.py:413-454` — el bloque de `camera_manager.add(...)`; las ultimas 10
      lineas son el bloque de la Fase 26, uno por linea y sin logica.
  </read_first>

  <files>backend/pipeline/manager.py, backend/main.py</files>

  <action>
**A. `backend/pipeline/manager.py`** — los tres pasos de la convencion, en orden:

1. **Declarar los atributos** junto a los demas (`manager.py:98-105`):
```python
        self.objects: "ObjectAnalyzer | None" = None
        self.object_tracker: "ObjectTracker | None" = None
```
Guardar tambien `self.detector = detector` si no esta ya disponible como atributo: la
fachada `set_detection_classes` lo necesita.

2. **Construir ANTES de `_make_detection`**, gateado por `if objects_enabled:`, con el
comentario que copia el de la Fase 26 **y le añade el agravante propio de esta fase**:

```python
        if objects_enabled:
            # Analizador y tracker viven FUERA de la factoria: WorkerSupervisor la
            # re-ejecuta en cada reinicio del DetectionWorker, y construirlos dentro
            # borraria las anclas, los latches, la marca de arranque y el contador de ids
            # de objeto. El agravante de esta fase respecto a la 26: reconstruirlos reabre
            # la ventana de warmup y reinicia los track_id, asi que TODO el mobiliario fijo
            # volveria a "aparecer" y a los 60 s se emitiria una rafaga de OBJECT_LEFT —
            # que es Severity.WARNING (types.py:55) y por tanto SUBE UN CLIP A GOOGLE DRIVE
            # POR CADA MUEBLE. Mismo motivo que la FSM de identidad (Fase 24), la galeria
            # de apariencia (Fase 25) y el BehaviorAnalyzer (Fase 26).
            self.object_tracker = ObjectTracker(frame_rate=int(target))
            self.objects = ObjectAnalyzer(
                left_secs=object_left_secs,
                still_radius_px=object_still_radius_px,
                person_radius_px=object_person_radius_px,
                person_radius_ratio=object_person_radius_ratio,
                warmup_secs=object_warmup_secs,
                gone_secs=object_gone_secs,
                person_window_secs=object_person_window_secs,
                max_tracks=object_max_tracks,
            )
```

3. **Pasarlos como kwargs dentro de la factoria** (`_make_detection`):
`objects=self.objects, object_tracker=self.object_tracker,
object_class_ids=set(object_class_ids or []),`.

4. **Fachada**, junto a `set_zones` (`manager.py:282`):
```python
    def set_detection_classes(self, classes: list[int]) -> None:
        """Aplica las clases activas al detector y al reparto persona/objeto.

        NO reinicia el DetectionWorker, a diferencia de set_process_size (que si reinicia
        el CaptureWorker): WorkerSupervisor._check() cuenta cualquier parada como caida y
        tres cambios de configuracion en 60 s lo marcarian FAILED de forma permanente,
        con el pipeline en modo degradado (supervisor.py:166-173). El detector lee
        self._classes en cada inferencia, asi que basta con mutarlo.
        """
        if self.detector is not None:
            self.detector.set_classes(classes)
        if self.detection is not None:
            self.detection.set_object_classes({c for c in classes if c != 0})

    def get_object_stats(self) -> list[dict]:
        return self.detection.get_object_stats() if self.detection else []

    def get_object_boxes(self) -> list[dict]:
        return self.detection.get_object_boxes() if self.detection else []
```

5. **Parametros nuevos** en la firma de `__init__`, en un bloque al final, exactamente los
del bloque `<interfaces>`. `CameraManager.add` (`manager.py:331-334`) **no se toca**:
reenvia `**kwargs`.

**B. `backend/main.py`** — solo propagacion, una linea por parametro, al final del bloque
de `camera_manager.add(...)` (tras las 10 lineas de la Fase 26):

```python
        objects_enabled=settings.objects_enabled,
        object_class_ids=settings.object_class_ids,
        object_left_secs=settings.object_left_secs,
        object_still_radius_px=settings.object_still_radius_px,
        object_person_radius_px=settings.object_person_radius_px,
        object_person_radius_ratio=settings.object_person_radius_ratio,
        object_warmup_secs=settings.object_warmup_secs,
        object_gone_secs=settings.object_gone_secs,
        object_person_window_secs=settings.object_person_window_secs,
        object_max_tracks=settings.object_max_tracks,
```

**Nada mas en `main.py` en este plan**: la lectura de `app_config`, los `include_router` y
los `configure()` son de 27-07 y 27-09.
  </action>

  <verify>
    <automated>F:/Documentos/IA/Proyecto_Camara/.venv/Scripts/python.exe -c "import inspect; from backend.pipeline.manager import CameraPipeline; src=inspect.getsource(CameraPipeline); import re; fac=src[src.index('def _make_detection'):src.index('def _make_detection')+1200] if 'def _make_detection' in src else ''; assert 'ObjectAnalyzer(' not in fac and 'ObjectTracker(' not in fac, 'construidos DENTRO de la factoria'; assert 'def set_detection_classes' in src; from unittest.mock import MagicMock; p=CameraPipeline('cam1','rtsp://fake',detector=MagicMock(),tracker=MagicMock()); assert p.objects is not None and p.object_tracker is not None; p2=CameraPipeline('cam1','rtsp://fake',detector=MagicMock(),tracker=MagicMock(),objects_enabled=False); assert p2.objects is None and p2.object_tracker is None; print('OK')"</automated>
  </verify>

  <acceptance_criteria>
    - `grep -n "self.objects = ObjectAnalyzer(\|self.object_tracker = ObjectTracker(" backend/pipeline/manager.py` acierta y **fuera** de `_make_detection` (comprobado por el `<verify>`).
    - `grep -n "SUBE UN CLIP A GOOGLE DRIVE POR CADA MUEBLE\|POR CADA MUEBLE" backend/pipeline/manager.py` acierta (el agravante esta escrito).
    - `grep -n "def set_detection_classes" backend/pipeline/manager.py` acierta y su docstring cita `supervisor.py`.
    - `grep -c "object_" backend/main.py` devuelve >= 10 (las 10 lineas de propagacion).
    - `grep -n "\.stop()" backend/pipeline/manager.py` no aparece dentro de `set_detection_classes`.
    - El comando de `<verify>` imprime `OK`.
  </acceptance_criteria>

  <done>`ObjectAnalyzer` y `ObjectTracker` se construyen una sola vez por pipeline, sobreviven a los reinicios del worker, y hay una fachada que cambia las clases sin parar nada.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Tests de cableado y de supervivencia a reinicio</name>

  <read_first>
    - `tests/test_detection_worker.py:329-346` — el molde de "construir el worker con la
      dependencia inyectada y llamar directamente al metodo privado".
    - `tests/test_detection_worker.py:363-378` — el molde del test de aislamiento de fallos
      (`analyze.side_effect = RuntimeError("boom")`, `_exceptions == 1`,
      `emit_*.assert_not_called()`).
    - `tests/test_detection_worker.py:400-438` — `TEST_behavior_analyzer_survives_worker_restart`
      y `TEST_behavior_disabled_leaves_pipeline_without_analyzer`: el molde LITERAL,
      incluido el acceso a `pipeline.supervisor._entries["detector"].factory` (convencion
      aceptada en este fichero).
    - `tests/test_detection_worker.py` — el helper `_tracked_cls` añadido en 27-03.
  </read_first>

  <files>tests/test_detection_worker.py</files>

  <behavior>
    - `TEST_object_left_emitted_from_worker`: un `DetectionWorker` con un `ObjectAnalyzer` real (`left_secs` bajo, `warmup_secs=0`) al que se le llama `_analyze_objects` en bucle con un objeto quieto y sin personas -> `event_engine.emit_object` se llama y el finding es `ObjectKind.LEFT`.
    - `TEST_object_analysis_failure_does_not_kill_thread`: `objects.analyze.side_effect = RuntimeError("boom")` -> `worker._exceptions == 1` y `emit_object` no se llama.
    - `TEST_object_prune_findings_are_emitted`: `objects.prune` devuelve un finding y `analyze` ninguno -> `emit_object` se llama igual. Es el test que protege contra ignorar el retorno de `prune`.
    - `TEST_object_analyzer_survives_worker_restart`: invocar dos veces la factoria del supervisor -> `pipeline.objects` es la MISMA instancia y ambos workers la comparten.
    - `TEST_object_tracker_survives_worker_restart`: idem para `pipeline.object_tracker` — si se reconstruyera, los `track_id` de objeto volverian a 1 y todo el mobiliario "apareceria" otra vez.
    - `TEST_objects_disabled_leaves_pipeline_without_analyzer`: con `objects_enabled=False`, `pipeline.objects is None` y `pipeline.object_tracker is None`.
    - `TEST_excluded_zone_suppresses_object_candidate`: con un `_zone_states` que contiene una zona `kind="exclude_objects"` que cubre al objeto, el `ObjectObservation` que llega al analizador sale con `excluded=True`.
    - `TEST_set_detection_classes_does_not_restart_worker`: `pipeline.set_detection_classes([0, 24])` llama a `detector.set_classes` y a `detection.set_object_classes({24})`, y **no** llama a `stop()` en ningun worker.
  </behavior>

  <action>
Añadir los ocho tests, agrupados bajo dos bandas de comentario.

Banda 1, antes de los tests de cableado:
```python
# ─── Cableado del ObjectAnalyzer en el hilo de deteccion (Fase 27, BEH-07) ───
```

Banda 2, antes de los de supervivencia, con el motivo completo (es la que justifica el
patron y hay que dejarla escrita):
```python
# ─── ObjectAnalyzer y ObjectTracker sobreviven a un reinicio del worker ──────
# Se construyen FUERA de _make_detection (manager.py): el supervisor re-ejecuta
# la factoria en cada reinicio y construirlos dentro borraria anclas, latches, la
# marca de arranque y el contador de ids de objeto. Consecuencia concreta: la
# ventana de warmup se reabriria, todo el mobiliario fijo volveria a "aparecer" y
# a los 60 s habria una rafaga de OBJECT_LEFT — que es WARNING y por tanto sube
# un clip a Google Drive por cada mueble. Mismo motivo que la FSM de identidad
# (Fase 24), la galeria ReID (Fase 25) y el BehaviorAnalyzer (Fase 26).
# ─────────────────────────────────────────────────────────────────────────────
```

Notas de implementacion:
- Reutilizar `_tracked_cls` de 27-03 para construir `obj_tracked` y `tracked`; no escribir
  otro helper.
- En `TEST_object_left_emitted_from_worker`, usar `ObjectAnalyzer(left_secs=1.0,
  warmup_secs=0.0, gone_secs=0.5)` y avanzar `processed_at` manualmente: el reloj es
  inyectado, asi que el test no espera ni un milisegundo real.
- En `TEST_set_detection_classes_does_not_restart_worker`, afirmar explicitamente
  `pipeline.detection.stop.assert_not_called()` (o el equivalente con `MagicMock`): es el
  aserto que impide que alguien "arregle" el cambio en caliente reiniciando el worker y
  reintroduzca el fallo del supervisor.
- Nombres `TEST_*`, todos con "object" para que `pytest -k object` los recoja.
  </action>

  <verify>
    <automated>F:/Documentos/IA/Proyecto_Camara/.venv/Scripts/python.exe -m pytest tests/test_detection_worker.py -q</automated>
  </verify>

  <acceptance_criteria>
    - `pytest tests/test_detection_worker.py -k object -q` recoge >= 13 tests (los 7 de 27-03 mas los 8 de aqui) y sale verde.
    - `pytest tests/test_detection_worker.py -q` verde entero.
    - `grep -n "supervisor._entries\[\"detector\"\].factory" tests/test_detection_worker.py` acierta al menos 2 veces mas que antes de este plan (analizador y tracker).
    - `grep -n "stop.assert_not_called" tests/test_detection_worker.py` acierta.
    - `grep -n "POR CADA MUEBLE\|por cada mueble" tests/test_detection_worker.py` acierta.
  </acceptance_criteria>

  <done>El cableado esta probado en sus cinco modos de fallo: emision real, fallo aislado, retorno de `prune` ignorado, reconstruccion en la factoria y reinicio del worker para aplicar configuracion.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| supervisor -> factoria del worker | Un reinicio automatico re-ejecuta codigo de construccion con estado sensible |
| event loop -> hilo de deteccion | La fachada `set_detection_classes` escribe estado que lee el bucle caliente |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-22 | Denial of Service | rafaga de `OBJECT_LEFT` tras un reinicio del worker -> subida masiva a Drive | mitigate | `ObjectAnalyzer` y `ObjectTracker` se construyen en `CameraPipeline.__init__`, fuera de la factoria. Verificado por los dos tests de supervivencia con comparacion de identidad de instancia |
| T-27-23 | Denial of Service | `DetectionWorker` marcado `FAILED` por cambios de configuracion | mitigate | `set_detection_classes` muta detector y reparto sin parar ningun worker; test explicito de `stop.assert_not_called()` (`supervisor.py:166-173`) |
| T-27-24 | Denial of Service | un fallo del analizador matando el hilo de deteccion | mitigate | `try` que envuelve `analyze` **y** `prune`, `except Exception` con `self._exceptions += 1` + `logger.exception` + `return`; el contador ya se expone en `/api/v2/cameras/{id}/health` |
| T-27-25 | Repudiation | veredictos de `prune` perdidos en silencio | mitigate | `findings += self._objects.prune(...)` con test dedicado: sin el, `OBJECT_REMOVED` no se emitiria nunca y el criterio 3 fallaria sin ningun error visible |
| T-27-26 | Information Disclosure | logs del hilo de deteccion | accept | `logger.exception` no incluye la URL RTSP ni identidades; se mantiene el mensaje generico del patron de la Fase 26 |
</threat_model>

<verification>
```bash
F:/Documentos/IA/Proyecto_Camara/.venv/Scripts/python.exe -m pytest tests/test_detection_worker.py tests/test_architecture.py tests/test_workers_isolation.py -q
```
</verification>

<success_criteria>
- `_analyze_objects` alimenta al analizador con anclas `BOTTOM_CENTER`, zonas de exclusion y personas, y emite tambien los veredictos del `prune`.
- Un fallo del analizador incrementa `_exceptions` y no mata el hilo.
- `ObjectAnalyzer` y `ObjectTracker` se construyen fuera de la factoria y sobreviven a un reinicio.
- `set_detection_classes` aplica el cambio sin parar ningun worker.
- `backend/pipeline/tracking.py`, `PersonTracker` y `backend/perception/behavior.py` sin cambios.
</success_criteria>

<output>
Al terminar, crear `.planning/phases/27-multi-clase-y-contexto-de-escena/27-06-SUMMARY.md`.
</output>
