---
phase: 27-multi-clase-y-contexto-de-escena
plan: 11
type: execute
wave: 6
depends_on: [27-08, 27-09, 27-10]
files_modified:
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
autonomous: false
requirements: [BEH-06, BEH-07, BEH-08, BEH-09]

must_haves:
  truths:
    - "La suite completa esta verde con la linea base de 454 (previa a la Fase 27) mas los tests nuevos de los 10 planes anteriores"
    - "Los 6 criterios de exito del ROADMAP estan trazados a un comando pytest -k que pasa"
    - "BEH-06, BEH-07, BEH-08 y BEH-09 quedan marcados [x] en REQUIREMENTS.md"
    - "ROADMAP.md y STATE.md reflejan la Fase 27 completa: 11/11 planes"
    - "El checkpoint de calibracion de object_person_radius_px y de la tasa de falsos positivos de OBJECT_LEFT queda aprobado o explicitamente diferido, sin bloquear el avance a la Fase 28"
  artifacts:
    - path: ".planning/phases/27-multi-clase-y-contexto-de-escena/27-11-SUMMARY.md"
      provides: "Tabla criterio -> comando -> resultado + estado del checkpoint (aprobado o diferido)"
  key_links:
    - from: ".planning/REQUIREMENTS.md"
      to: "ROADMAP.md § Phase 27"
      via: "BEH-06..BEH-09 marcados [x] solo si la suite completa esta verde"
      pattern: "BEH-0[6-9]"
---

<objective>
Puerta de fase: ejecutar la suite completa, trazar los 6 criterios de éxito del ROADMAP a
comandos `pytest -k` que pasan, marcar BEH-06..BEH-09 en `REQUIREMENTS.md`, cerrar la Fase 27
en `ROADMAP.md`/`STATE.md` (11/11 planes), y dejar resuelto o explícitamente diferido el
checkpoint manual de calibración con cámara real — el noveno de la serie que arrancó en la
Fase 19.

Purpose: los 10 planes anteriores (01-10) ya dejaron cada comportamiento probado por
separado (dominio puro, cableado, endpoints, overlay, frontend). Lo que falta es la
verificación conjunta —nada se ha roto entre plan y plan— y la trazabilidad explícita de
"criterio del ROADMAP -> comando que lo demuestra", que es lo que la Fase 26 estableció como
patrón de cierre (`26-05-PLAN.md`).
Output: suite verde + `REQUIREMENTS.md`/`ROADMAP.md`/`STATE.md` al día + `27-11-SUMMARY.md`
con la tabla de trazabilidad + checkpoint aprobado o diferido.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/27-multi-clase-y-contexto-de-escena/27-VALIDATION.md
@.planning/phases/26-an-lisis-de-comportamiento/26-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Suite completa + trazabilidad de los 6 criterios de éxito</name>

  <read_first>
    - `.planning/ROADMAP.md` § Phase 27 — los 6 criterios verbatim.
    - `.planning/phases/27-multi-clase-y-contexto-de-escena/27-VALIDATION.md` § "Criterios
      de éxito del ROADMAP → comando" — los comandos base, algunos ajustados abajo con los
      nombres reales de test que fijaron los planes 01-10.
    - `.planning/phases/26-an-lisis-de-comportamiento/26-05-SUMMARY.md` — el formato de la
      tabla criterio -> comando -> resultado de la fase anterior; replicar la forma.
  </read_first>

  <files>ninguno (solo lectura y ejecución; el resultado se registra en el SUMMARY)</files>

  <action>
**1. Suite completa** (obligatorio: la fase toca pipeline, eventos, configuración y API —
CLAUDE.md § Tests punto 2 lo exige):

```
F:/Documentos/IA/Proyecto_Camara/.venv/Scripts/python.exe -m pytest tests/ -q
```

Línea base antes de la Fase 27: 454/454 (`26-05-SUMMARY.md`). Si algo falla, CORREGIRLO —
no ocultarlo, no marcarlo `skip`. Un fallo en esta puerta es una señal de que dos de los 10
planes anteriores se pisaron (p.ej. un cambio de firma no propagado); diagnosticar contra
los `SUMMARY.md` de cada plan antes de tocar código nuevo.

**2. Trazabilidad.** Ejecutar los 6 comandos y registrar en `27-11-SUMMARY.md` el resultado
real (número de tests recogidos y código de salida):

| # | Criterio | Comando |
|---|---|---|
| 1 | Clases configurables desde la UI | `pytest tests/test_detection_config_api.py -q` (backend) + checkpoint manual del Task 2 (evidencia visual: marcar una clase y verla en el MJPEG, cableado en `27-08`/`27-10`) |
| 2 | `OBJECT_LEFT` tras 60 s inmóvil sin persona cerca | `pytest tests/test_object_analyzer.py -k TEST_object_left -q` |
| 3 | `OBJECT_REMOVED` al desaparecer con persona cerca | `pytest tests/test_object_analyzer.py -k TEST_object_removed -q` |
| 4 | `/api/v2/analytics/context` con media móvil de 7 días | `pytest tests/test_scene_context.py -q` |
| 5 | Un único `OBJECT_LEFT`; con persona presente, ninguno | `pytest tests/test_object_analyzer.py -k "TEST_object_left_latched or TEST_no_left_with_person" -q` |
| 6 | 6 clases no suben la latencia > 15 % | `pytest tests/test_detector.py -k TEST_multiclass_latency -q` |
| — | Regresión ByteTrack (riesgo de primer orden de la fase, `27-03`) | `pytest tests/test_detection_worker.py -k "object_class_does_not_reach_line_zone or objects_not_in_registry or bytetrack_ids_do_not_migrate" -q` |
| — | Sin regresión | `pytest tests/ -q` |

Si el nombre real de algún test difiere del listado aquí (los planes 01-10 pudieron ajustar
un nombre durante su propia ejecución), usar el nombre real que aparezca en el `SUMMARY.md`
del plan correspondiente y dejarlo anotado en `27-11-SUMMARY.md` — no inventar un comando
que no exista.

**3. `REQUIREMENTS.md`** — marcar `[x]` BEH-06, BEH-07, BEH-08 y BEH-09 (sección
"Comprensión de escena (BEH)"), **solo si** el paso 1 salió verde. No tocar ninguna otra
sección.

**4. `ROADMAP.md`** — dos cambios en la entrada de la Fase 27:
   - En la lista corta del bloque ("Bloque B — Inteligencia artificial"): pasar
     `- [ ] **Phase 27: ...**` a `- [x] **Phase 27: Multi-clase y contexto de escena** —
     Objetos abandonados/retirados + estado agregado de la escena (completed <fecha>;
     checkpoint de calibración de `object_person_radius_px` y tasa de falsos positivos de
     `OBJECT_LEFT` pendiente de cámara real)`, con la misma forma que las Fases 20-23/25.
   - En el detalle de la Fase 27 (§ "Phase Details v2.0"), añadir justo antes de
     `**Spec**: SPEC_v2.md Phase 27`:
     ```
     **Plans**: 11/11 plans complete (6 waves)
     Plans:
     - [x] 27-01-PLAN.md — ObjectAnalyzer (dominio puro): OBJECT_LEFT/OBJECT_REMOVED con doble guarda de expiración
     - [x] 27-02-PLAN.md — D-03 (yolo26n.pt) + 14 parámetros object_*/context_* + PersonDetector.set_classes()
     - [x] 27-03-PLAN.md — ObjectTracker + partición por clase antes del tracker (riesgo ByteTrack)
     - [x] 27-04-PLAN.md — DetectionStatRepo.hourly_baseline() + kind en zonas legacy
     - [x] 27-05-PLAN.md — EventEngine.emit_object() + config_changed()
     - [x] 27-06-PLAN.md — Cableado del ObjectAnalyzer en DetectionWorker + construcción fuera de la factoría
     - [x] 27-07-PLAN.md — GET/PUT /api/v2/detection/classes con persistencia en app_config
     - [x] 27-08-PLAN.md — Overlay de objetos en el feed MJPEG
     - [x] 27-09-PLAN.md — GET /api/v2/analytics/context (BEH-08/BEH-09)
     - [x] 27-10-PLAN.md — Panel de clases activas en el dashboard
     - [x] 27-11-PLAN.md — Puerta de fase: trazabilidad de los 6 criterios + checkpoint de calibración
     ```

**5. `STATE.md`** — actualizar: `stopped_at`, `last_updated`, `completed_phases` (10 -> 11),
   `completed_plans` (+11), `total_plans` (+11), `percent`, la fila/párrafo de "Current
   Position" con la Fase 27 completa, y las decisiones acumuladas de esta fase (al menos):
   - `ObjectAnalyzer`/`ObjectTracker` son análogos por sustracción de `behavior.py`/
     `PersonTracker`: mismo patrón de reloj inyectado, latch por episodio y doble guarda de
     expiración, pero `prune()` **devuelve findings** (`OBJECT_REMOVED` se decide al
     expirar) — la única diferencia estructural del molde.
   - `sv.ByteTrack` es class-agnostic (reproducido en `27-RESEARCH.md` Q4): la partición por
     clase ANTES del tracker y un `ObjectTracker` dedicado son obligatorios, no una
     optimización — sin ellos un track de objeto puede transferir su id a una persona
     solapada y contaminar el `LineZone` de la Fase 4.
   - `OBJECT_LEFT` se mantiene en `Severity.WARNING` (decisión del usuario): cruza
     `upload_min_severity="warning"` y sube clips a Drive desde el primer evento — es
     intencional, pero exige calibrar `object_person_radius_px` con cámara real antes de
     operar desatendido (checkpoint de este plan).
   - La BD (`app_config`) gana sobre `YOLO_CLASSES` al arrancar; una fila `[]` se trata como
     ausente para no dejar el sistema ciego en silencio.
   - El nivel de actividad de BEH-09 se normaliza a tasa por minuto en ambos lados
     (baseline y "ahora") para no sesgar "low" al principio de cada hora, y cae a
     `"unknown"` con menos de `context_min_sample_days` de historial.
   Añadir el checkpoint del Task 2 a la lista de checkpoints manuales abiertos (pasa a ser
   el **9º**) y dejar como siguiente paso `/gsd:plan-phase 28`.
  </action>

  <verify>
    <automated>F:/Documentos/IA/Proyecto_Camara/.venv/Scripts/python.exe -m pytest tests/ -q</automated>
  </verify>

  <acceptance_criteria>
    - `pytest tests/ -q` sale con código 0 y un total >= 454 + los tests nuevos de los planes 01-10, sin `skip` nuevos.
    - `grep -c "^- \[x\] \*\*BEH-0[6-9]\*\*" .planning/REQUIREMENTS.md` devuelve 4.
    - `grep -n "Phase 27: Multi-clase" .planning/ROADMAP.md` muestra la entrada marcada `[x]`.
    - `grep -n "11/11 plans complete" .planning/ROADMAP.md` acierta.
    - `grep -n "completed_phases: 11" .planning/STATE.md` acierta (o el valor equivalente si el conteo base difiere).
    - `27-11-SUMMARY.md` contiene una tabla con los 6 criterios, su comando y el resultado real.
  </acceptance_criteria>

  <done>Suite completa verde, los 6 criterios trazados a comandos que pasan, BEH-06..BEH-09 marcados y ROADMAP/STATE al día.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Calibración con cámara real — radio "persona cerca" y falsos positivos de OBJECT_LEFT (9º checkpoint manual)</name>

  <what-built>
La fase completa y automatizada: clases de detección configurables desde la UI con persistencia
en BD (criterio 1), `OBJECT_LEFT`/`OBJECT_REMOVED` con las 9 guardas de comportamiento
verificadas con trayectorias sintéticas (criterios 2, 3, 5), `/api/v2/analytics/context` con
media móvil de 7 días (criterio 4), y el criterio 6 (latencia) ya cumplido con margen 3x
(+4,7 % medido en `27-RESEARCH.md` Q8, muy por debajo del 15 % exigido). El overlay de objetos
en el MJPEG (27-08) y el panel de checkboxes (27-10) dan evidencia visual inmediata del
criterio 1.

Lo que NO se puede verificar sin cámara: `object_person_radius_px = 150.0` es una distancia
razonada (1,9 × `loiter_radius_px`, ~media altura de persona a media distancia en un frame de
1280×720 — `27-RESEARCH.md` Assumption A1/A7), no una medición contra una escena real. Lo mismo
aplica a `object_warmup_secs=10.0` (A2) y `object_gone_secs=3.0` (A3). Y como `OBJECT_LEFT` es
`Severity.WARNING` (decisión del usuario, mantiene el contrato del catálogo), cada falso
positivo sube automáticamente un clip a Google Drive y consume cuota — es el riesgo operativo
más concreto de toda la fase (Pitfall 12 de `27-RESEARCH.md`).
  </what-built>

  <how-to-verify>
1. Arrancar el sistema con la cámara real:
   `F:/Documentos/IA/Proyecto_Camara/.venv/Scripts/python.exe -m uvicorn backend.main:app --host 0.0.0.0 --port 8000`
2. Abrir el dashboard y marcar al menos una clase de objeto (p.ej. "Mochila") en el panel
   "Clases detectadas" — confirmar que el checkbox de "Persona" sigue marcado y deshabilitado.
3. Colocar un objeto de esa clase en la escena (una mochila, una maleta) y comprobar:
   a. **Overlay**: la caja del objeto aparece en el feed MJPEG en color distinto al de las
      personas (magenta).
   b. **OBJECT_LEFT**: dejar el objeto quieto y alejarse — a los 60 s debe llegar exactamente
      un evento `OBJECT_LEFT` por el WebSocket, y el clip correspondiente debe empezar a
      subirse a Google Drive (verificar en `/api/v2/recordings` o en la carpeta de Drive
      configurada).
   c. **Falsos positivos**: dejar que una persona pase cerca del objeto sin tocarlo, a
      distintas distancias — anotar la distancia (en píxeles del frame, o estimada en el
      mundo real) a la que el sistema deja de emitir `OBJECT_LEFT`. Si el radio real de
      "cerca" resulta muy distinto de 150 px, ajustar `OBJECT_PERSON_RADIUS_PX` en `.env`.
   d. **OBJECT_REMOVED**: recoger el objeto con la persona al lado — debe llegar un
      `OBJECT_REMOVED` una vez transcurrida la gracia de `object_gone_secs` (3 s).
   e. **Tasa de falsos positivos**: durante ~15-20 minutos de actividad normal (gente
      pasando, sin dejar nada abandonado), anotar cuántos `OBJECT_LEFT`/`OBJECT_REMOVED`
      espurios aparecen. Si la tasa es alta, subir `OBJECT_PERSON_RADIUS_PX` (el lado seguro
      para `OBJECT_LEFT`, ver el comentario de `27-01-PLAN.md` sobre la asimetría del radio)
      o, como válvula de escape sin tocar código, subir `UPLOAD_MIN_SEVERITY` a `critical`
      para dejar de subir clips por `WARNING` mientras se recalibra.
4. Anotar los valores finales de los umbrales (`OBJECT_PERSON_RADIUS_PX`,
   `OBJECT_WARMUP_SECS`, `OBJECT_GONE_SECS` si se ajustaron) en `.env` y en el
   `27-11-SUMMARY.md`.

**Este checkpoint NO bloquea avanzar a la Fase 28**: los umbrales son configurables en
caliente por `.env`, todos los criterios deterministas ya están verdes con trayectorias
sintéticas, y `objects_enabled=False` desactiva la vía entera si molestara operativamente. Si
no hay cámara disponible en esta sesión, diferirlo explícitamente en `STATE.md` como se hizo
con los 8 checkpoints anteriores (26-05 Task 3 fue el último).
  </how-to-verify>

  <resume-signal>Escribe "aprobado" con los valores de umbral finales, o "diferido" si no hay cámara disponible en esta sesión.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| documentación de planificación -> estado del proyecto | `ROADMAP.md`/`REQUIREMENTS.md`/`STATE.md` son la fuente de verdad de qué está completo; un cierre incorrecto de fase desincroniza la planificación del código real |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-42 | Repudiation | marcar BEH-06..BEH-09 `[x]` sin suite verde | mitigate | El Task 1 condiciona explícitamente el paso 3 (marcar REQUIREMENTS.md) al resultado verde del paso 1; el `acceptance_criteria` lo exige como precondición |
| T-27-43 | Denial of Service | umbrales de `OBJECT_LEFT` sin calibrar operando desatendido | accept | Documentado como 9º checkpoint manual (Task 2); `objects_enabled=False` y `UPLOAD_MIN_SEVERITY=critical` quedan como válvulas de escape sin tocar código |
</threat_model>

<verification>
```bash
F:/Documentos/IA/Proyecto_Camara/.venv/Scripts/python.exe -m pytest tests/ -q
```
</verification>

<success_criteria>
- Suite completa verde con los 6 criterios de éxito del ROADMAP trazados a comandos que pasan.
- BEH-06..BEH-09 marcados `[x]`; ROADMAP y STATE reflejan la Fase 27 completa (11/11 planes).
- El checkpoint de calibración con cámara real queda aprobado o explícitamente diferido, sin bloquear el avance a la Fase 28.
</success_criteria>

<output>
Al terminar, crear `.planning/phases/27-multi-clase-y-contexto-de-escena/27-11-SUMMARY.md`.
</output>
