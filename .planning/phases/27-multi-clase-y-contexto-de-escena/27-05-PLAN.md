---
phase: 27-multi-clase-y-contexto-de-escena
plan: 05
type: execute
wave: 2
depends_on: [27-01]
files_modified:
  - backend/events/engine.py
  - tests/test_event_engine.py
autonomous: true
requirements: [BEH-06, BEH-07]

must_haves:
  truths:
    - "emit_object traduce ObjectKind.LEFT a EventType.OBJECT_LEFT y REMOVED a OBJECT_REMOVED"
    - "OBJECT_LEFT sale con Severity.WARNING y OBJECT_REMOVED con INFO sin pasar severity= explicita"
    - "El payload lleva duration_s con ese nombre literal y ningun valor None"
    - "La bbox del objeto viaja como campo de primer nivel del Event, no dentro del payload"
    - "config_changed() publica un CONFIG_CHANGED con el detalle del cambio de clases"
  artifacts:
    - path: "backend/events/engine.py"
      provides: "_OBJECT_EVENT_TYPE + emit_object() + config_changed()"
      contains: "def emit_object"
    - path: "tests/test_event_engine.py"
      provides: "TEST_emit_object_* — traduccion, payload y severidad"
      contains: "TEST_emit_object"
  key_links:
    - from: "backend/events/engine.py"
      to: "backend/perception/objects.py"
      via: "import de ObjectFinding/ObjectKind — direccion events/ -> perception/, nunca al reves"
      pattern: "from backend.perception.objects import"
    - from: "emit_object"
      to: "DEFAULT_SEVERITY del catalogo"
      via: "no se pasa severity=, asi que aplica el default de types.py:49-57"
      pattern: "payload=finding.magnitudes\\(\\)"
---

<objective>
Traducir los veredictos del dominio (`ObjectFinding`) a eventos tipados del catalogo, y
darle a `EventEngine` el metodo publico que falta para dejar rastro de un cambio de
configuracion de deteccion (`CONFIG_CHANGED`).

Purpose: `perception/` no conoce `camera_id` ni el reloj de pared — por eso `ObjectAnalyzer`
devuelve `ObjectFinding` y no `Event`, exactamente como la Fase 26 con `BehaviorFinding`.
Este plan es el unico punto de la fase que toca `events/engine.py`, y va en wave 2 porque
necesita los tipos de 27-01. `EventType.OBJECT_LEFT`/`OBJECT_REMOVED` y `CONFIG_CHANGED` ya
estan en el catalogo desde la Fase 19/26 (`types.py:36-38,45`) y `tests/test_event_types.py:12`
congela la lista: **no hay que añadir ningun tipo nuevo**.
Output: `emit_object()` + `config_changed()` + 4 tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/27-multi-clase-y-contexto-de-escena/27-PATTERNS.md
@.planning/phases/27-multi-clase-y-contexto-de-escena/27-01-SUMMARY.md
@backend/events/engine.py
@backend/perception/objects.py

<interfaces>
<!-- CONTRATO LOCKED. Lo consumen 27-06 (_analyze_objects) y 27-07 (PUT de clases). -->

```python
# backend/events/engine.py
_OBJECT_EVENT_TYPE: dict[ObjectKind, EventType] = {
    ObjectKind.LEFT:    EventType.OBJECT_LEFT,      # Severity.WARNING por catalogo
    ObjectKind.REMOVED: EventType.OBJECT_REMOVED,   # Severity.INFO por fallback
}

def emit_object(
    self,
    finding: ObjectFinding,
    now: datetime.datetime,
    captured_at: float | None = None,
    processed_at: float | None = None,
) -> None: ...

def config_changed(self, now: datetime.datetime, **detail: Any) -> None: ...
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: _OBJECT_EVENT_TYPE + emit_object() + config_changed()</name>

  <read_first>
    - `backend/events/engine.py:21` — el import de `BehaviorFinding`/`BehaviorKind`: la
      direccion de la dependencia es `events/ -> perception/`, **nunca** al reves. Es lo
      que hace que `ObjectFinding` no pueda ser un `Event`.
    - `backend/events/engine.py:29-34` — `_BEHAVIOR_EVENT_TYPE`: tabla de traduccion a
      nivel de MODULO (la Fase 26 la saco fuera de la clase porque el mapeo es 1:1 y sin
      logica). El molde exacto.
    - `backend/events/engine.py:56-80` — `_publish` y su `**fields`: acepta `bbox` sin
      cambios.
    - `backend/events/engine.py:238-267` — `emit_behavior` entero, incluida la banda de
      comentario de seccion y el docstring que explica DONDE vive la idempotencia.
    - `backend/events/engine.py:163-181` — `camera_offline` / `degraded_mode`: el molde de
      un metodo publico sencillo que traduce una situacion a un evento. `camera_offline`
      es ademas el UNICO sitio del fichero que pasa `severity=` explicita.
    - `backend/events/types.py:49-57` — `DEFAULT_SEVERITY`: `OBJECT_LEFT` -> `WARNING`,
      `OBJECT_REMOVED` no aparece -> `INFO`.
  </read_first>

  <files>backend/events/engine.py</files>

  <action>
**1. Import**, junto al de `behavior` (`engine.py:21`):
```python
from backend.perception.objects import ObjectFinding, ObjectKind
```

**2. Tabla de traduccion** a nivel de modulo, junto a `_BEHAVIOR_EVENT_TYPE`:
```python
_OBJECT_EVENT_TYPE: dict[ObjectKind, EventType] = {
    ObjectKind.LEFT: EventType.OBJECT_LEFT,
    ObjectKind.REMOVED: EventType.OBJECT_REMOVED,
}
```

**3. `emit_object`**, con su banda de comentario de seccion, calcado de `emit_behavior`:

```python
    # ------------------------------------------------------------------
    # Objetos (Fase 27)
    # ------------------------------------------------------------------

    def emit_object(
        self,
        finding: ObjectFinding,
        now: datetime.datetime,
        captured_at: float | None = None,
        processed_at: float | None = None,
    ) -> None:
        """Publica el evento de objeto correspondiente a *finding*, si lo hay.

        La guarda de idempotencia NO esta aqui: vive en el latch por episodio de
        ObjectAnalyzer (criterio 5 del ROADMAP: una mochila abandonada emite UN unico
        OBJECT_LEFT), igual que `emits` vive en la FSM para emit_identity y el latch en el
        analizador para emit_behavior.

        NO se pasa `severity=` a proposito: asi aplica el default del catalogo
        (types.py:49-57), es decir OBJECT_LEFT -> WARNING y OBJECT_REMOVED -> INFO. La
        consecuencia es deliberada y esta decidida con el usuario: WARNING cruza
        upload_min_severity="warning" (config.py:115 -> recording.py:309) y por tanto
        OBJECT_LEFT SUBE EL CLIP A GOOGLE DRIVE. Un objeto abandonado es exactamente lo
        que quieres tener grabado; lo que hay que vigilar es la tasa de falsos positivos
        (checkpoint manual de 27-11).
        """
        event_type = _OBJECT_EVENT_TYPE.get(finding.kind)
        if event_type is None:
            return
        self._publish(
            event_type,
            ts=now,
            captured_at=captured_at,
            processed_at=processed_at,
            track_id=finding.track_id,
            zone_id=finding.zone_id,
            bbox=finding.bbox,
            payload=finding.magnitudes(),
        )
```

`bbox` va como campo de primer nivel (los eventos de objeto llevan caja, los de
comportamiento no); `_publish` ya lo acepta por `**fields`. El payload lo compone el
dominio (`finding.magnitudes()`), **nunca** `EventEngine`.

**4. `config_changed`**, junto a `degraded_mode`, siguiendo su forma:

```python
    def config_changed(self, now: datetime.datetime, **detail: Any) -> None:
        """Deja rastro en el historico de un cambio de configuracion en caliente.

        EventType.CONFIG_CHANGED existe en el catalogo desde la Fase 19 y hasta ahora
        nadie lo emitia. La Fase 27 lo estrena desde el PUT de clases activas: sin roles
        en el sistema (la auth es todo-o-nada), la trazabilidad de quien/cuando cambio la
        configuracion de deteccion es la unica mitigacion disponible (ASVS V4).
        Sin severity= explicita: cae en INFO por el catalogo.
        """
        self._publish(EventType.CONFIG_CHANGED, ts=now, payload=dict(detail))
```

**NO tocar `backend/events/types.py`** (los dos tipos ya existen; `test_event_types.py:12`
congela la lista de nombres).
  </action>

  <verify>
    <automated>F:/Documentos/IA/Proyecto_Camara/.venv/Scripts/python.exe -c "import inspect; from backend.events import engine as e; assert hasattr(e.EventEngine,'emit_object') and hasattr(e.EventEngine,'config_changed'); src=inspect.getsource(e.EventEngine.emit_object); assert 'severity=' not in src, 'emit_object no debe pasar severity= explicita'; assert 'bbox=finding.bbox' in src; assert set(e._OBJECT_EVENT_TYPE.values())=={e.EventType.OBJECT_LEFT, e.EventType.OBJECT_REMOVED}; print('OK')"</automated>
  </verify>

  <acceptance_criteria>
    - `grep -n "_OBJECT_EVENT_TYPE" backend/events/engine.py` acierta a nivel de modulo (no dentro de la clase).
    - `grep -n "def emit_object\|def config_changed" backend/events/engine.py` devuelve 2 resultados.
    - `grep -n "from backend.perception.objects import" backend/events/engine.py` acierta.
    - `grep -n "severity=" backend/events/engine.py` sigue apareciendo solo en `camera_offline`.
    - `git diff --stat backend/events/types.py backend/perception/objects.py` esta vacio.
    - El comando de `<verify>` imprime `OK`.
  </acceptance_criteria>

  <done>`EventEngine` traduce `ObjectFinding` a `Event` sin componer el payload ni forzar la severidad, y sabe emitir `CONFIG_CHANGED`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: TEST_emit_object_* en tests/test_event_engine.py</name>

  <read_first>
    - `tests/test_event_engine.py:290-360` — los tres `TEST_emit_behavior_*`: traduccion de
      kinds (aserto sobre el conjunto `{e.type for e in received}`), payload
      (`all(v is not None for v in event.payload.values())`) y severidad. Son el molde
      literal; el tercero se INVIERTE en esta fase.
    - `tests/test_event_engine.py` — `make_engine()` / `wait_until()` del principio del
      fichero.
    - `backend/perception/objects.py` — `ObjectFinding.magnitudes()`.
  </read_first>

  <files>tests/test_event_engine.py</files>

  <behavior>
    - `TEST_emit_object_translates_both_kinds`: emitir un LEFT y un REMOVED -> `{e.type for e in received} == {EventType.OBJECT_LEFT, EventType.OBJECT_REMOVED}`.
    - `TEST_emit_object_payload_carries_magnitudes`: un LEFT con `duration_s=61.0`, `class_name="backpack"` y `net_displacement_px=12.0` -> `event.payload["duration_s"] == 61.0`, `event.payload["class_name"] == "backpack"`, `"person_distance_px" not in event.payload` (los `None` se omiten) y `all(v is not None for v in event.payload.values())`.
    - `TEST_emit_object_severity_comes_from_catalog`: **el test invertido respecto a la Fase 26** — el `OBJECT_LEFT` sale con `Severity.WARNING` y el `OBJECT_REMOVED` con `Severity.INFO`, sin que `emit_object` pase `severity=`. Este test es el que documenta que la subida a Drive es intencional.
    - `TEST_emit_object_carries_bbox_as_first_class_field`: la bbox esta en el `Event` y no dentro del `payload`.
    - `TEST_config_changed_is_emitted_with_detail`: `config_changed(now, classes=[0, 24])` publica un `CONFIG_CHANGED` cuyo payload contiene `classes`.
  </behavior>

  <action>
Añadir los cinco tests al final del fichero, con una banda de comentario que explique el
punto de la inversion:

```python
# ─── emit_object: la severidad la pone el CATALOGO, no el emisor (Fase 27) ───
# Al reves que los cuatro comportamientos de la Fase 26, que se quedaron en INFO
# a proposito para no disparar subidas a Drive, aqui OBJECT_LEFT es WARNING
# (types.py:55) y por tanto cruza upload_min_severity="warning"
# (config.py:115 -> recording.py:309): CADA OBJECT_LEFT SUBE UN CLIP A GOOGLE
# DRIVE. Es una decision tomada con el usuario (se mantiene el contrato ya
# publicado del catalogo) y este test es el sitio donde queda documentada.
# ─────────────────────────────────────────────────────────────────────────────
```

Detalles obligatorios:
- Construir los `ObjectFinding` a mano, sin pasar por el analizador: este fichero prueba la
  traduccion, no el dominio.
- Usar `f.kind is ObjectKind.LEFT` / `e.severity is Severity.WARNING` (identidad, no `==`).
- El aserto de `TEST_emit_object_payload_carries_magnitudes` sobre la ausencia de las claves
  con valor `None` es el que protege el contrato de `magnitudes()`; sin el, un `None` en el
  payload llegaria a SQLite y al WebSocket.
- Nombres `TEST_*`, todos con "object" o "config_changed" para que
  `pytest -k emit_object` los recoja.
  </action>

  <verify>
    <automated>F:/Documentos/IA/Proyecto_Camara/.venv/Scripts/python.exe -m pytest tests/test_event_engine.py tests/test_event_types.py -q</automated>
  </verify>

  <acceptance_criteria>
    - `pytest tests/test_event_engine.py -k "emit_object or config_changed" -q` recoge 5 tests y sale verde.
    - `pytest tests/test_event_engine.py -q` verde entero (sin regresion en los `TEST_emit_behavior_*`).
    - `pytest tests/test_event_types.py -q` verde (la lista del catalogo no ha cambiado).
    - `grep -n "Severity.WARNING" tests/test_event_engine.py` acierta dentro del test de severidad de objeto.
    - `grep -n "SUBE UN CLIP A GOOGLE" tests/test_event_engine.py` acierta.
  </acceptance_criteria>

  <done>La traduccion, el payload y la severidad de los dos eventos de objeto estan probados, y queda escrito en el test por que `OBJECT_LEFT` sube clips a Drive.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dominio (`perception/`) -> bus de eventos | Veredictos que se convierten en eventos persistidos, difundidos por WebSocket y evaluados por reglas |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-18 | Denial of Service | rafaga de eventos de objeto -> subida masiva a Drive | mitigate | La idempotencia vive en el latch del `ObjectAnalyzer`, no aqui; `emit_object` es un traductor sin estado. `debounce_secs` de `rules.yaml` queda como segunda capa configurable |
| T-27-19 | Elevation of Privilege | severidad forzada desde el emisor | mitigate | `emit_object` NO pasa `severity=`: la decide el catalogo (`types.py:49-57`), que es el contrato publicado. Verificado por `TEST_emit_object_severity_comes_from_catalog` |
| T-27-20 | Repudiation | cambio de configuracion de deteccion sin rastro | mitigate | `config_changed()` publica `CONFIG_CHANGED` con el detalle; el evento se persiste como cualquier otro. Es la unica mitigacion disponible al no haber roles (ASVS V4) |
| T-27-21 | Tampering | payload con claves inventadas | mitigate | `duration_s` es nombre obligatorio porque `rules.py:88-91` lo lee literalmente para `duration_gte`; cualquier otro nombre romperia las reglas YAML en silencio. Verificado por el test de payload |
</threat_model>

<verification>
```bash
F:/Documentos/IA/Proyecto_Camara/.venv/Scripts/python.exe -m pytest tests/test_event_engine.py tests/test_event_types.py tests/test_rule_engine.py -q
```
</verification>

<success_criteria>
- `emit_object()` traduce los dos `ObjectKind` y pasa `bbox` como campo de primer nivel.
- La severidad sale del catalogo: `OBJECT_LEFT` en `WARNING`, `OBJECT_REMOVED` en `INFO`.
- `magnitudes()` omite los `None` y el payload lleva `duration_s` con ese nombre literal.
- `config_changed()` publica `CONFIG_CHANGED` con el detalle del cambio.
- `backend/events/types.py` y `backend/perception/objects.py` sin cambios.
</success_criteria>

<output>
Al terminar, crear `.planning/phases/27-multi-clase-y-contexto-de-escena/27-05-SUMMARY.md`.
</output>
