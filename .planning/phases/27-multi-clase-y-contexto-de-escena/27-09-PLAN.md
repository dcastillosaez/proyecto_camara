---
phase: 27-multi-clase-y-contexto-de-escena
plan: 09
type: execute
wave: 5
depends_on: [27-04, 27-06, 27-07]
files_modified:
  - backend/api/v2/context.py
  - backend/main.py
  - tests/test_scene_context.py
autonomous: true
requirements: [BEH-08, BEH-09]

must_haves:
  truths:
    - "GET /api/v2/analytics/context devuelve hora, personas totales/conocidas/desconocidas, zonas y nivel de actividad en una sola llamada"
    - "known cuenta solo identity_state CONFIRMED, nunca CANDIDATE ni TEMPORARILY_LOST"
    - "Con menos de context_min_sample_days de historial el nivel de actividad es unknown, nunca un valor inventado"
    - "Dentro de los primeros 5 minutos de una hora el nivel de actividad es unknown, no low por sesgo de hora parcial"
    - "El endpoint nunca devuelve person_id ni nombres de persona, solo recuentos"
  artifacts:
    - path: "backend/api/v2/context.py"
      provides: "Router GET /api/v2/analytics/context + _person_counts + _classify_activity, ambas puras y testables"
      contains: "def _classify_activity"
    - path: "tests/test_scene_context.py"
      provides: "TEST_context_shape, TEST_known_requires_confirmed, TEST_insufficient_history, TEST_partial_hour_normalised"
      contains: "TEST_known_requires_confirmed"
  key_links:
    - from: "backend/api/v2/context.py"
      to: "backend/storage/repositories.py (DetectionStatRepo.hourly_baseline, 27-04)"
      via: "dos llamadas: baseline (until=inicio de hora) y ahora (since=inicio de hora)"
      pattern: "hourly_baseline\\("
    - from: "backend/api/v2/context.py"
      to: "backend/pipeline/tracking.py (TrackRegistry)"
      via: "frame_ids() + snapshot(), nunca active_ids() (arrastra 30s de TTL)"
      pattern: "registry\\.frame_ids\\(\\)"
---

<objective>
Exponer `GET /api/v2/analytics/context`: el criterio 4 del ROADMAP, y el requisito de esta
fase con mas superficie sin cerrar todavia. Devuelve hora, zona, personas
totales/conocidas/desconocidas y nivel de actividad calculado contra la media movil de 7
dias, en una sola llamada.

Purpose: el endpoint no duplica logica de ninguna fase anterior — combina estado ya vivo
(`TrackRegistry`, `CameraPipeline.get_zone_stats/get_object_stats`) con una query nueva
(`DetectionStatRepo.hourly_baseline`, 27-04) y la fachada de config de clases (27-07). Va en
wave 5 porque toca `main.py`, que 27-06 y 27-07 ya modificaron en waves anteriores — mismo
fichero, mismo orden de escritura secuencial que el resto de la fase.
Output: `backend/api/v2/context.py` + arranque cableado en `main.py` + 6 tests, dos de ellos
sobre funciones puras sin BD ni HTTP.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/27-multi-clase-y-contexto-de-escena/27-PATTERNS.md
@.planning/phases/27-multi-clase-y-contexto-de-escena/27-04-SUMMARY.md
@.planning/phases/27-multi-clase-y-contexto-de-escena/27-06-SUMMARY.md
@.planning/phases/27-multi-clase-y-contexto-de-escena/27-07-SUMMARY.md
@backend/api/v2/metrics.py
@backend/api/v2/recordings.py
@backend/pipeline/tracking.py

<interfaces>
<!-- CONTRATO LOCKED. Consume el hourly_baseline() de 27-04 y el configure() de main.py
     de 27-06/27-07. Nadie mas depende de este fichero dentro de la fase. -->

```python
# backend/api/v2/context.py
router = APIRouter(prefix="/api/v2/analytics", tags=["analytics"])

def configure(camera_manager: Any) -> None: ...

# Funciones puras, testables sin BD ni HTTP:
def _person_counts(registry: "TrackRegistry") -> dict[str, int]: ...
def _classify_activity(
    baseline_entry: dict[str, float] | None,
    now_entry: dict[str, float] | None,
    minutes_elapsed: float,
    settings: "Settings",
) -> dict[str, Any]: ...

# GET /api/v2/analytics/context?camera_id=cam1&days=7   (days opcional, ge=1, le=90)
{
  "timestamp": "2026-08-16T14:23:07.123456",
  "camera_id": "cam1",
  "hour": 14,
  "persons": {"total": 3, "known": 1, "unknown": 1, "pending": 1},
  "zones": [{"id": "entrada", "name": "Entrada", "current": 2, "entries": 15}],
  "objects": [{"class_name": "backpack", "count": 1}],
  "activity": {
    "level": "normal",
    "current": 2.4,
    "baseline": 2.1,
    "ratio": 1.14,
    "sample_days": 5
  }
}
```

Niveles de `activity.level`: `"low" | "normal" | "high" | "unknown"`. `"unknown"` cuando
`sample_days < settings.context_min_sample_days` (default 3) o cuando han pasado menos de 5
minutos de la hora en curso (Pitfall 7 de 27-RESEARCH.md).

**Seguridad — no negociable:** el endpoint NUNCA devuelve `person_id` ni nombres de persona,
solo recuentos. `days` va como `Query(default=None, ge=1, le=90)`, nunca interpolado en SQL.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: backend/api/v2/context.py — router + funciones puras `_person_counts`/`_classify_activity`</name>

  <read_first>
    - `backend/api/v2/metrics.py:1-33` — patron `configure()` con global de modulo: la
      unica forma limpia de inyectar la referencia viva sin ciclo de imports (`main.py`
      importa el router, no al reves).
    - `backend/api/v2/recordings.py:1-26` — docstring de auth heredada, `prefix`, factoria
      privada de repo.
    - `backend/api/v2/deps.py` — `limiter`, `V2_RATE_LIMIT`.
    - `backend/pipeline/tracking.py:91-115` — `TrackRegistry.snapshot()` y `.frame_ids()`
      (NO `active_ids()`, que arrastra hasta 30 s de TTL de `prune()`).
    - `backend/perception/face/identity.py` — los 4 valores de `IdentityState`:
      `UNKNOWN`, `CANDIDATE`, `CONFIRMED`, `TEMPORARILY_LOST`.
    - `backend/storage/repositories.py` (tras 27-04) — `DetectionStatRepo.hourly_baseline(
      camera_id, since, until=None) -> dict[str, dict[str, float]]`, clave de hora string
      de dos digitos (`"09"`), valores `avg_total`/`sample_days`/`mins`/`avg_per_minute`.
    - `backend/config.py` (tras 27-02) — `context_baseline_days`, `context_min_sample_days`,
      `context_low_ratio`, `context_high_ratio`.
    - `backend/pipeline/manager.py` (tras 27-06) — `CameraPipeline.get_zone_stats()` (ya
      existia) y `CameraPipeline.get_object_stats()` (nuevo en 27-06).
  </read_first>

  <files>backend/api/v2/context.py</files>

  <action>
Fichero nuevo. Docstring de modulo copiando el parrafo de auth de `recordings.py:3-6` y
añadiendo el aviso de que este endpoint es de solo lectura y de recuentos:

```python
"""API v2 — contexto de escena agregado: hora, zona, personas, nivel de actividad
(Fase 27, BEH-08/BEH-09).

Auth y rate limiting: la app aplica auth globalmente
(FastAPI(dependencies=[Depends(verify)])), asi que los routers incluidos con
app.include_router() la heredan automaticamente. El rate limit (SEC-16) usa el
limiter/valor compartidos de backend/api/v2/deps.py.

Este endpoint es de SOLO RECUENTOS: nunca devuelve person_id ni nombres de persona. Un
endpoint de analitica de escena no debe convertirse en un endpoint de identidad
(27-RESEARCH.md § Security Domain).
"""
```

**1. Router + configure()**, molde `metrics.py`:

```python
router = APIRouter(prefix="/api/v2/analytics", tags=["analytics"])

_camera_manager: Any = None


def configure(camera_manager: Any) -> None:
    """Wire the live CameraManager instance. Called once from main.py's lifespan."""
    global _camera_manager
    _camera_manager = camera_manager


def _stat_repo() -> DetectionStatRepo:
    return DetectionStatRepo(get_session_factory())
```

**2. `_person_counts(registry)`** — funcion pura, sin `self`, sin async, para poder
probarla sin BD ni HTTP (es donde vive el criterio "known cuenta solo CONFIRMED"):

```python
def _person_counts(registry: TrackRegistry) -> dict[str, int]:
    """Recuento de personas visibles AHORA MISMO, por estado de identidad.

    frame_ids(), no active_ids(): active_ids() arrastra hasta 30s de TTL de prune()
    (tracking.py:130), asi que un track que acaba de perderse seguiria contando como
    "presente" hasta 30s despues de salir del frame. frame_ids() es exacto e inmediato
    (27-RESEARCH.md Q5, mismo criterio que usa RecognitionWorker._sync_identity).

    "Conocida" es identity_state is CONFIRMED, NO person_id is not None: set_identity()
    escribe person_id en cuanto hay un match, incluso antes de que la votacion temporal
    confirme (tracking.py:117-122). Usar person_id contaria como "conocida" a alguien
    todavia en CANDIDATE, contradiciendo la semantica de FACE-08.
    """
    visible = registry.frame_ids()
    states = [ts for ts in registry.snapshot().values() if ts.track_id in visible]
    known = sum(1 for ts in states if ts.identity_state is IdentityState.CONFIRMED)
    pending = sum(
        1 for ts in states
        if ts.identity_state in (IdentityState.CANDIDATE, IdentityState.TEMPORARILY_LOST)
    )
    total = len(visible)
    return {"total": total, "known": known, "unknown": total - known - pending, "pending": pending}
```

**3. `_classify_activity(...)`** — funcion pura, el nucleo de BEH-09 (Q7 de
27-RESEARCH.md). Recibe los dos resultados de `hourly_baseline()` ya calculados (no hace
queries): el `baseline_entry` (ventana `[hoy - N dias, inicio de la hora en curso)`) y el
`now_entry` (ventana `[inicio de la hora en curso, ahora)`), mas `minutes_elapsed` (minutos
de reloj transcurridos de la hora en curso — NO los minutos que `now_entry["mins"]` reporta,
porque el minuto en curso puede no estar volcado todavia, 27-RESEARCH.md H-5):

```python
def _classify_activity(
    baseline_entry: dict[str, float] | None,
    now_entry: dict[str, float] | None,
    minutes_elapsed: float,
    settings: Settings,
) -> dict[str, Any]:
    """Nivel de actividad de la franja horaria actual contra su media movil (BEH-09).

    Normalizado a TASA POR MINUTO en los dos lados: comparar el total acumulado de una
    hora completa (baseline) contra el de una hora que solo lleva unos minutos (ahora)
    sesgaria "low" cada hora en punto y subiria hasta el minuto 59 (Pitfall 7). "unknown"
    con pocos dias de historial (Pitfall 8) o dentro de los primeros 5 minutos de la
    hora: en ambos casos el sistema debe decir "no se todavia" en vez de inventar un
    veredicto, mismo principio que reid_inherit_identity=False al arrancar (Fase 25).
    """
    sample_days = baseline_entry["sample_days"] if baseline_entry else 0
    if baseline_entry is None or sample_days < settings.context_min_sample_days or minutes_elapsed < 5.0:
        return {"level": "unknown", "current": None, "baseline": None, "ratio": None,
                "sample_days": sample_days}

    baseline_rate = baseline_entry["avg_per_minute"]
    current_total = now_entry["avg_total"] if now_entry else 0.0
    current_rate = current_total / minutes_elapsed

    if baseline_rate <= 0:
        return {"level": "unknown", "current": round(current_rate, 3), "baseline": 0.0,
                "ratio": None, "sample_days": sample_days}

    ratio = current_rate / baseline_rate
    if ratio < settings.context_low_ratio:
        level = "low"
    elif ratio > settings.context_high_ratio:
        level = "high"
    else:
        level = "normal"
    return {
        "level": level, "current": round(current_rate, 3), "baseline": round(baseline_rate, 3),
        "ratio": round(ratio, 3), "sample_days": sample_days,
    }
```

**4. `GET /context`**, con `@limiter.limit(V2_RATE_LIMIT)` y `request: Request`:

```python
@router.get("/context")
@limiter.limit(V2_RATE_LIMIT)
async def get_context(
    request: Request,
    camera_id: str = Query(default="cam1"),
    days: int | None = Query(default=None, ge=1, le=90),
) -> dict[str, Any]:
    settings = get_settings()
    now = datetime.datetime.now()
    hour_start = now.replace(minute=0, second=0, microsecond=0)
    minutes_elapsed = (now - hour_start).total_seconds() / 60.0
    hour_key = f"{now.hour:02d}"
    window_days = days if days is not None else settings.context_baseline_days

    repo = _stat_repo()
    baseline_map = await repo.hourly_baseline(
        camera_id, since=now - datetime.timedelta(days=window_days), until=hour_start,
    )
    now_map = await repo.hourly_baseline(camera_id, since=hour_start)
    activity = _classify_activity(
        baseline_map.get(hour_key), now_map.get(hour_key), minutes_elapsed, settings,
    )

    pipeline = _camera_manager.get(camera_id) if _camera_manager is not None else None
    if pipeline is not None:
        persons = _person_counts(pipeline.registry)
        zones = pipeline.get_zone_stats()
        objects = pipeline.get_object_stats()
    else:
        persons = {"total": 0, "known": 0, "unknown": 0, "pending": 0}
        zones, objects = [], []

    return {
        "timestamp": now.isoformat(),
        "camera_id": camera_id,
        "hour": now.hour,
        "persons": persons,
        "zones": zones,
        "objects": objects,
        "activity": activity,
    }
```

`hourly_baseline` se llama DOS veces con el contrato exacto de 27-04: `until=hour_start`
para el baseline (excluye la hora en curso, evita que se contamine a si misma) y
`since=hour_start` sin `until` para el "ahora" (esa llamada solo devuelve, como mucho, la
entrada de `hour_key`, con `avg_total` = total acumulado hoy en esa hora y `sample_days=1`).

**Nunca** añadir `person_id`, `person_name` ni nada de `registry.snapshot()` mas alla de
`identity_state` al payload. **Nunca** interpolar `camera_id`/`days` en SQL crudo — ya van
como parametros de `hourly_baseline` (ligados por SQLAlchemy desde 27-04).
  </action>

  <verify>
    <automated>F:/Documentos/IA/Proyecto_Camara/.venv/Scripts/python.exe -c "from backend.api.v2.context import router, configure, _person_counts, _classify_activity; paths={r.path for r in router.routes}; assert paths=={'/api/v2/analytics/context'}, paths; from backend.pipeline.tracking import TrackRegistry; r=TrackRegistry(); assert _person_counts(r)=={'total':0,'known':0,'unknown':0,'pending':0}; from backend.config import Settings; s=Settings(); out=_classify_activity(None, None, 10.0, s); assert out['level']=='unknown'; import inspect; src=inspect.getsource(__import__('backend.api.v2.context', fromlist=['x'])); assert 'person_id' not in src and 'person_name' not in src; print('OK')"</automated>
  </verify>

  <acceptance_criteria>
    - `grep -n 'APIRouter(prefix="/api/v2/analytics"' backend/api/v2/context.py` acierta.
    - `grep -n "def _person_counts\|def _classify_activity" backend/api/v2/context.py` devuelve 2 resultados, ambos sin `async`.
    - `grep -n "frame_ids()" backend/api/v2/context.py` acierta y `grep -n "active_ids()" backend/api/v2/context.py` NO acierta.
    - `grep -n "person_id\|person_name" backend/api/v2/context.py` NO acierta.
    - `grep -c "hourly_baseline(" backend/api/v2/context.py` devuelve 2 (baseline + ahora).
    - `grep -n "@limiter.limit(V2_RATE_LIMIT)" backend/api/v2/context.py` acierta.
    - El comando de `<verify>` imprime `OK`.
  </acceptance_criteria>

  <done>El router expone `GET /api/v2/analytics/context` con los 6 bloques del criterio 4, y las dos funciones que deciden "conocida" y "nivel de actividad" son puras y testables sin BD ni HTTP.</done>
</task>

<task type="auto">
  <name>Task 2: main.py — configure() + include_router</name>

  <read_first>
    - `backend/main.py:245-246` — `metrics_v2_module.configure(latency_tracker)`, llamado
      justo tras crear `latency_tracker`, ANTES de que exista `camera_manager` (linea 412).
      `context.configure(camera_manager)` necesita `camera_manager`, asi que va DESPUES de
      la linea 412, no junto al de metrics.
    - `backend/main.py:412-454` — creacion de `camera_manager` y `camera_manager.add(...)`.
    - `backend/main.py:557-561` — bloque de `include_router` a nivel de modulo (tras el de
      `detection_v2_router`, añadido por 27-07).
  </read_first>

  <files>backend/main.py</files>

  <action>
Dos cambios, ninguno con logica nueva.

**1. `configure()`**, justo despues de `camera_manager = CameraManager()` (linea 412) o
justo despues de `camera_manager.add(...)` (linea 454) — cualquiera de los dos sirve,
porque el router solo usa `_camera_manager.get(camera_id)` en tiempo de peticion, no en
tiempo de arranque:

```python
    from backend.api.v2 import context as context_v2_module
    context_v2_module.configure(camera_manager)
```

**2. `include_router`**, a nivel de modulo, junto al de `detection_v2_router` (27-07):

```python
from backend.api.v2.context import router as context_v2_router
app.include_router(context_v2_router)
```

**Nada mas en `main.py` en este plan**: `ConfigRepo`/precedencia de clases es de 27-07 y no
se toca aqui.
  </action>

  <verify>
    <automated>F:/Documentos/IA/Proyecto_Camara/.venv/Scripts/python.exe -c "import backend.main as m; paths={getattr(r,'path','') for r in m.app.routes}; assert '/api/v2/analytics/context' in paths, sorted(p for p in paths if 'v2' in p); src=open('backend/main.py',encoding='utf-8').read(); assert 'context_v2_module.configure' in src; print('OK')"</automated>
  </verify>

  <acceptance_criteria>
    - `grep -n "context_v2_module.configure(camera_manager)" backend/main.py` acierta despues de `camera_manager = CameraManager()`.
    - `grep -n "from backend.api.v2.context import router" backend/main.py` acierta.
    - `pytest tests/test_security_regression.py -q` verde (endpoint v2 nuevo con rate limit).
  </acceptance_criteria>

  <done>El router de contexto queda registrado en la app y cableado a la referencia viva de `camera_manager`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: tests/test_scene_context.py</name>

  <read_first>
    - `tests/test_repositories.py:20-31` — fixture `db` (BD in-memory por test, camara
      `cam1` sembrada).
    - `tests/test_security_regression.py:27-28` — `_client()` (ASGI sin levantar servidor).
    - `backend/pipeline/tracking.py` — `TrackRegistry.update_from_detections`,
      `set_frame_ids`, `set_identity_state`.
  </read_first>

  <files>tests/test_scene_context.py</files>

  <behavior>
    - `TEST_known_requires_confirmed`: un `TrackRegistry` con dos tracks visibles, uno `CONFIRMED` y otro `CANDIDATE` -> `_person_counts(registry)` da `known=1`, `pending=1`, `unknown=0`, `total=2`. Sin BD, sin HTTP.
    - `TEST_person_counts_uses_frame_ids_not_active_ids`: un track en el registry que NO esta en `frame_ids()` (simulando que salio del frame pero sigue dentro del TTL de `active_ids()`) no cuenta en `total`.
    - `TEST_insufficient_history`: `_classify_activity(baseline_entry={"avg_total": 10.0, "sample_days": 1, "avg_per_minute": 0.16}, now_entry=..., minutes_elapsed=30.0, settings=Settings())` con `sample_days=1 < context_min_sample_days=3` -> `level == "unknown"`.
    - `TEST_partial_hour_normalised`: mismo `baseline_entry` con `sample_days=5` (suficiente) pero `minutes_elapsed=3.0` -> `level == "unknown"` (Pitfall 7), y con `minutes_elapsed=30.0` y una tasa "ahora" igual a la del baseline -> `level == "normal"`.
    - `TEST_activity_ratio_thresholds`: tres casos con el mismo baseline y `now_entry` variando -> `ratio < 0.5` da `"low"`, `ratio` en `[0.5, 1.5]` da `"normal"`, `ratio > 1.5` da `"high"` — usando los defaults reales de `Settings().context_low_ratio`/`context_high_ratio`, no numeros inventados.
    - `TEST_context_shape`: via ASGI (`httpx` + `ASGITransport`), con `context_module.configure(mock_manager)` y `monkeypatch.setattr(context_module, "get_session_factory", lambda: sf)` sobre una BD sembrada con `DetectionStatRepo.upsert_minute` (>= 3 dias de historia en la hora actual) -> `200`, y el JSON tiene exactamente las claves `timestamp, camera_id, hour, persons, zones, objects, activity`, con `persons` conteniendo `total/known/unknown/pending`.
    - `TEST_context_never_leaks_person_identity`: en el mismo escenario del test anterior, el cuerpo de la respuesta (texto crudo) no contiene las subcadenas `"person_id"` ni `"person_name"`.
  </behavior>

  <action>
Fichero nuevo. Docstring citando el criterio 4 del ROADMAP y BEH-08/BEH-09.

**Parte 1 — funciones puras** (`TEST_known_requires_confirmed`,
`TEST_person_counts_uses_frame_ids_not_active_ids`, `TEST_insufficient_history`,
`TEST_partial_hour_normalised`, `TEST_activity_ratio_thresholds`): construir
`TrackRegistry()` real y poblarlo con `update_from_detections(_tracked_cls(...), now)` +
`set_frame_ids({...})` + `set_identity_state(tid, IdentityState.CONFIRMED)`, o construir
los dicts de `baseline_entry`/`now_entry` a mano. **Sin BD, sin async, sin ASGI** — son las
mas baratas de escribir y las que fijan el contrato con mas precision.

Para `TEST_person_counts_uses_frame_ids_not_active_ids`: llamar
`registry.update_from_detections(...)` (que añade el track a `_tracks`) pero NO llamar
`registry.set_frame_ids({ese_id})` — asi el track esta en `active_ids()` pero no en
`frame_ids()`. Confirma la eleccion documentada en el docstring de `_person_counts`.

Para `TEST_activity_ratio_thresholds`, usar `Settings()` real (no hardcodear `0.5`/`1.5`)
para que el test seguiria siendo correcto si alguien recalibra los defaults en `config.py`.

**Parte 2 — integracion ASGI** (`TEST_context_shape`,
`TEST_context_never_leaks_person_identity`): patron `_client()` de
`test_security_regression.py`. Sembrar la BD de prueba con
`DetectionStatRepo(sf).upsert_minute(...)` para >= 3 dias en la hora actual
(`datetime.datetime.now()` calculado UNA vez al principio del test, reutilizado para
sembrar y para razonar sobre `hour_key`). Construir un `mock_manager` con `.get(camera_id)`
devolviendo un objeto con `.registry` (un `TrackRegistry()` real, puede estar vacio),
`.get_zone_stats()` y `.get_object_stats()` mockeados (`MagicMock(return_value=[...])`).
Llamar `context_module.configure(mock_manager)` y restaurar `configure(None)` al final del
test (fixture o `try/finally`) para no contaminar otros ficheros de test.

No afirmar sobre el VALOR de `activity.level` en `TEST_context_shape` (depende del minuto
real de reloj en el que corra el test, ver Parte 1 para eso) — solo sobre la FORMA del
JSON y los codigos de estado.

Nombres `TEST_*`.
  </action>

  <verify>
    <automated>F:/Documentos/IA/Proyecto_Camara/.venv/Scripts/python.exe -m pytest tests/test_scene_context.py -q</automated>
  </verify>

  <acceptance_criteria>
    - `pytest tests/test_scene_context.py -q` recoge >= 7 tests y sale verde.
    - `pytest tests/test_scene_context.py -k "TEST_known_requires_confirmed or TEST_insufficient_history or TEST_partial_hour_normalised" -q` sale verde.
    - `grep -n "person_id\|person_name" tests/test_scene_context.py` acierta SOLO dentro de `TEST_context_never_leaks_person_identity` (como cadena buscada, no como campo esperado).
    - `grep -n "active_ids" tests/test_scene_context.py` acierta dentro de `TEST_person_counts_uses_frame_ids_not_active_ids`.
    - `pytest tests/ -q` sin regresion.
  </acceptance_criteria>

  <done>Los 6 bloques del criterio 4 estan probados, `known` distingue `CONFIRMED` de `CANDIDATE`, el sesgo de hora parcial y el minimo de dias estan cubiertos, y el endpoint nunca filtra identidad.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cliente HTTP -> API v2 | `camera_id`/`days` no confiables que acaban en una query de agregacion |
| `TrackRegistry` (estado de identidad) -> endpoint de analitica | Frontera critica: un descuido aqui convertiria un endpoint de metricas en uno de identidad |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-36 | Information Disclosure | fuga de `person_id`/nombre a traves del endpoint de contexto | mitigate | `_person_counts` solo lee `identity_state` de cada `TrackState`, nunca `person_id`/`person_name`. Verificado por `TEST_context_never_leaks_person_identity` |
| T-27-37 | Tampering | inyeccion SQL via `camera_id`/`days` | mitigate | Ambos llegan tipados (`str`, `int` con `Query(ge=1, le=90)`) y se pasan como parametros ligados a `hourly_baseline` (27-04); nunca interpolados en el `text()` del `group_by` |
| T-27-38 | Repudiation | nivel de actividad inventado con poco historial | mitigate | `sample_days < context_min_sample_days` -> `"unknown"` explicito, nunca un `AVG` sobre una muestra insuficiente disfrazado de veredicto |
| T-27-39 | Denial of Service | martilleo del endpoint (dos queries de agregacion por peticion) | mitigate | `@limiter.limit(V2_RATE_LIMIT)` (60/min), mismo limite compartido que el resto de la superficie v2; cada query mide 11 ms sobre un año de datos (27-RESEARCH.md Q7) |
</threat_model>

<verification>
```bash
F:/Documentos/IA/Proyecto_Camara/.venv/Scripts/python.exe -m pytest tests/test_scene_context.py tests/test_security_regression.py tests/test_repositories.py -q
```
</verification>

<success_criteria>
- `GET /api/v2/analytics/context` devuelve los 6 bloques del criterio 4 en una sola llamada.
- `_person_counts` cuenta `known` solo por `identity_state is CONFIRMED`.
- `_classify_activity` devuelve `"unknown"` con pocos dias de historial o dentro de los primeros 5 minutos de la hora, y clasifica correctamente con los umbrales de `Settings`.
- El endpoint nunca devuelve `person_id` ni nombres.
- `tests/test_security_regression.py` sigue verde.
</success_criteria>

<output>
Al terminar, crear `.planning/phases/27-multi-clase-y-contexto-de-escena/27-09-SUMMARY.md`.
</output>
