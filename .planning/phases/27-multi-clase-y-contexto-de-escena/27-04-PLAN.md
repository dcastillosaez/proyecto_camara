---
phase: 27-multi-clase-y-contexto-de-escena
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/storage/repositories.py
  - backend/database.py
  - tests/test_repositories.py
  - tests/test_database.py
autonomous: true
requirements: [BEH-06, BEH-07, BEH-09]

must_haves:
  truths:
    - "hourly_baseline() suma unique_tracks por (dia, hora) y solo despues promedia entre dias"
    - "hourly_baseline() devuelve sample_days para que el endpoint pueda decir 'unknown' con poca historia"
    - "La ventana se puede acotar por arriba (until) para que la hora en curso no contamine el baseline"
    - "get_zones() del ORM legacy devuelve kind, que es lo que habilita las zonas de exclusion de objetos"
    - "ConfigRepo.get/set hacen roundtrip de una list[int] en la columna JSON de app_config"
  artifacts:
    - path: "backend/storage/repositories.py"
      provides: "DetectionStatRepo.hourly_baseline() — media movil por franja horaria (BEH-09, D-02)"
      contains: "async def hourly_baseline"
    - path: "backend/database.py"
      provides: "kind en el Zone legacy y en get_zones()"
      contains: "kind = Column(String(30), nullable=True)"
    - path: "tests/test_repositories.py"
      provides: "TEST_hourly_baseline_* y TEST_config_repo_*"
      contains: "TEST_hourly_baseline"
  key_links:
    - from: "backend/storage/repositories.py"
      to: "models.DetectionStat.unique_tracks"
      via: "doble GROUP BY: sum por (dia, hora), avg entre dias"
      pattern: "func\\.avg\\("
    - from: "backend/database.py"
      to: "backend/pipeline/detection.py (_rebuild_zone_states)"
      via: "el dict de get_zones() lleva kind"
      pattern: "\"kind\": z\\.kind"
---

<objective>
Poner las dos piezas de datos que la fase necesita y que hoy no existen: la query de media
movil por franja horaria (BEH-09) y la columna `kind` de zonas en el ORM legacy que
alimenta al pipeline (guarda (b) de exclusion de mobiliario fijo, BEH-07). De paso,
estrenar `ConfigRepo` con tests, que es la persistencia de las clases activas (BEH-06).

Purpose: los tres cambios son de datos puros, no importan nada de la fase y no los importa
nadie todavia, asi que van en wave 1. `zones.kind` **ya existe fisicamente** en la tabla
(`migrations.py:103-108` lo garantiza en cualquier instalacion) y `ZoneRepo` v2 ya lo
devuelve: lo que se quedo atras es el `Zone` legacy de `backend/database.py`, que es
justo el que `main.py:468` usa para alimentar al `DetectionWorker`. Son dos lineas, no una
migracion.
Output: `hourly_baseline()` + 2 lineas en `database.py` + los tests de ambos y de `ConfigRepo`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/27-multi-clase-y-contexto-de-escena/27-RESEARCH.md
@.planning/phases/27-multi-clase-y-contexto-de-escena/27-PATTERNS.md
@backend/storage/repositories.py
@backend/database.py

<interfaces>
<!-- CONTRATO LOCKED. Lo consume 27-09 (endpoint de contexto), que llama al metodo DOS
     veces: una para el baseline historico y otra, con since=inicio de la hora en curso,
     para el "ahora". Por eso hace falta `until`. -->

```python
# backend/storage/repositories.py — dentro de DetectionStatRepo, justo tras recent()
async def hourly_baseline(
    self,
    camera_id: str,
    since: datetime.datetime,
    until: datetime.datetime | None = None,
) -> dict[str, dict[str, float]]:
    """{"09": {"avg_total": 12.5, "sample_days": 4, "mins": 240, "avg_per_minute": 0.208}, ...}

    La clave de hora es un STRING de dos digitos ("09"), porque viene de
    strftime("%H", ...) — misma convencion que EventRepo.hourly_counts.
    """
```

```python
# backend/database.py — Zone legacy
kind = Column(String(30), nullable=True)      # copiado caracter a caracter de models.py:168
# y en el dict de get_zones():
"kind": z.kind,
```

Convencion de `kind` que establece esta fase: `kind == "exclude_objects"` marca una zona
en la que los objetos nunca son candidatos a `OBJECT_LEFT`. `None` es lo normal.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: DetectionStatRepo.hourly_baseline() — media movil por franja horaria</name>

  <read_first>
    - `backend/storage/repositories.py:1` — el docstring del modulo: "every SQL query in
      the project lives here". Por eso la query va aqui y no en el router.
    - `backend/storage/repositories.py:153-169` — `EventRepo.hourly_counts`: el precedente
      literal de `func.strftime` + `.group_by(text("hour"))` y de que el repo asume SQLite.
    - `backend/storage/repositories.py:198-261` — `DetectionStatRepo` completo:
      `upsert_minute` (que escribe `unique_tracks`) y `recent()` (la forma del metodo de
      lectura: `async with self._sf() as session`, sin `session.begin()`).
    - `backend/storage/models.py:112-128` — `DetectionStat`, sus columnas y sus dos
      indices (`idx_detstats_minute` y el unique `(camera_id, minute)`).
    - `backend/events/engine.py:273-286` — por que `detections` NO sirve de baseline.
  </read_first>

  <files>backend/storage/repositories.py</files>

  <action>
Añadir `hourly_baseline` dentro de `DetectionStatRepo`, justo **despues** de `recent()`
(`repositories.py:261`). `text` y `func` ya estan importados (`repositories.py:15`).

Copiar la query de 27-RESEARCH Q7 tal cual, incluido su docstring de tres parrafos, y
añadir el parametro `until`:

```python
    async def hourly_baseline(
        self,
        camera_id: str,
        since: datetime.datetime,
        until: datetime.datetime | None = None,
    ) -> dict[str, dict[str, float]]:
        """Media movil por franja horaria sobre los ultimos N dias (BEH-09, D-02).

        Dos niveles de agregacion, y el orden importa: primero se SUMA por (dia, hora) y
        solo despues se PROMEDIA entre dias. Promediar directamente sobre las filas de
        minuto daria "media por minuto", no "media por hora", y ademas ponderaria mas los
        dias con mas minutos registrados.

        Se agrupa sobre `unique_tracks`, no sobre `detections`: detections acumula
        len(active_track_ids) UNA VEZ POR FRAME PROCESADO (engine.py:281), asi que depende
        del FPS efectivo que AdaptiveRate haya elegido y no es comparable entre dias con
        distinta carga de CPU. `unique_tracks` mide flujo de personas distintas, que es la
        semantica de "nivel de actividad" acordada con el usuario.

        `until` acota la ventana por arriba para que la hora EN CURSO (parcial) no
        contamine su propio baseline. El endpoint llama a este metodo dos veces: una con
        (now - N dias, inicio de la hora en curso) para el baseline y otra con
        (inicio de la hora en curso, None) para el "ahora".

        strftime() es especifico de SQLite; ya hay precedente en EventRepo.hourly_counts
        (repositories.py:162). El proyecto es SQLite-only. Coste medido en 27-RESEARCH Q7:
        p50 de 11,2 ms sobre 525.600 filas (un año) usando el indice unico
        (camera_id, minute) que ya existe — NO hace falta indice nuevo, el WHERE se
        resuelve con un range scan y los strftime se evaluan solo sobre las filas ya
        filtradas.
        """
```

Cuerpo: la subconsulta `per_day` (GROUP BY dia, hora; `sum(unique_tracks)` y `count()` de
minutos) y el `select` externo (GROUP BY hora; `avg(total)`, `count()` como `sample_days`,
`sum(mins)`), exactamente como el research. Añadir la condicion de `until` a la lista de
`where` solo si no es `None`.

El dict devuelto por fila:
```python
            row.hour: {
                "avg_total": float(row.avg_total),
                "sample_days": int(row.sample_days),
                "mins": int(row.mins),
                "avg_per_minute": float(row.avg_total) / max(1.0, row.mins / row.sample_days),
            }
```

**No crear ningun indice**, no tocar `models.py`, no tocar `ConfigRepo` (ya esta completo:
`get`/`set`/`get_all`, `repositories.py:551-574`). Esta fase es su primer usuario, pero el
codigo no necesita cambios.

**Seguridad:** `camera_id`, `since` y `until` van como parametros ligados de SQLAlchemy.
Nunca interpolar nada dentro del `text()` del `group_by` — ahi solo van los alias
literales `"day"` y `"hour"`.
  </action>

  <verify>
    <automated>F:/Documentos/IA/Proyecto_Camara/.venv/Scripts/python.exe -m pytest tests/test_repositories.py -q</automated>
  </verify>

  <acceptance_criteria>
    - `grep -n "async def hourly_baseline" backend/storage/repositories.py` acierta y esta dentro de `DetectionStatRepo`, despues de `recent`.
    - `grep -n "unique_tracks" backend/storage/repositories.py` acierta dentro de `hourly_baseline` y `grep -n "func.sum(models.DetectionStat.detections)" backend/storage/repositories.py` NO acierta.
    - `grep -n "sample_days" backend/storage/repositories.py` acierta.
    - `git diff --stat backend/storage/models.py` esta vacio (sin indice nuevo, sin columna nueva).
    - `git diff backend/storage/repositories.py | grep "^-" | grep -v "^---"` esta vacio (solo se añade codigo).
  </acceptance_criteria>

  <done>`DetectionStatRepo.hourly_baseline()` existe, agrega en dos niveles sobre `unique_tracks` y acepta una ventana acotada por los dos extremos.</done>
</task>

<task type="auto">
  <name>Task 2: kind en el Zone legacy de backend/database.py (2 lineas)</name>

  <read_first>
    - `backend/storage/models.py:168` — `kind = Column(String(30), nullable=True)`: hay que
      copiarlo **caracter a caracter** para que ambos ORM mapeen la misma columna fisica.
    - `backend/storage/migrations.py:103-108` — `_add_missing_columns(conn, "zones",
      models.Zone)`: garantiza que la columna existe en cualquier instalacion, nueva o
      migrada. Por eso esto NO es una migracion.
    - `backend/database.py:37-44` — la clase `Zone` legacy, que declara 5 columnas.
    - `backend/database.py:295-309` — `get_zones()`, la funcion que `main.py:468` usa para
      alimentar al `DetectionWorker`.
    - `backend/storage/repositories.py:500-506` — `ZoneRepo._to_dict`, que ya devuelve
      `kind`: el v2 ya lo hace bien, el legacy se quedo atras.
  </read_first>

  <files>backend/database.py</files>

  <action>
Exactamente dos lineas de codigo, mas un comentario.

1. En la clase `Zone` (`database.py:37-44`), añadir tras `enabled`:
```python
    # Copiada caracter a caracter de storage/models.py:168 para que ambos ORM mapeen la
    # MISMA columna fisica. La columna ya existe en toda instalacion gracias a
    # _add_missing_columns (migrations.py:103-108): esto es codigo, no migracion.
    # Convencion de la Fase 27: kind == "exclude_objects" marca una zona en la que los
    # objetos nunca son candidatos a OBJECT_LEFT (mobiliario fijo revelado despues del
    # arranque — guarda (b) de 27-RESEARCH.md Q2).
    kind = Column(String(30), nullable=True)
```

2. En el dict de `get_zones()` (`database.py:295-309`), añadir `"kind": z.kind,` junto a
`"enabled"`.

**No tocar** `upsert_zone` ni el endpoint `/api/zones` de `main.py:908-931`: crear zonas de
exclusion desde la UI no esta en el alcance de esta fase (el operador puede fijar el `kind`
por el `ZoneRepo` v2 o directamente en BD). Lo que esta fase necesita es **leerlo**.
El consumidor (`_rebuild_zone_states`, `detection.py:316-338`) ya recibe el dict `z`
completo, asi que a partir de aqui el `kind` viaja solo — cablearlo es trabajo de 27-06.
  </action>

  <verify>
    <automated>F:/Documentos/IA/Proyecto_Camara/.venv/Scripts/python.exe -m pytest tests/test_database.py tests/test_migrations.py -q</automated>
  </verify>

  <acceptance_criteria>
    - `grep -n "kind = Column(String(30), nullable=True)" backend/database.py` acierta.
    - `grep -n '"kind": z.kind' backend/database.py` acierta.
    - `git diff --stat backend/storage/models.py backend/storage/migrations.py` esta vacio.
    - `pytest tests/test_migrations.py -q` verde (la columna sigue siendo la misma).
    - `git diff backend/database.py --stat` muestra 2 lineas de codigo añadidas (mas comentarios).
  </acceptance_criteria>

  <done>`get_zones()` devuelve `kind`, que es lo unico que le falta al pipeline para poder excluir objetos por zona.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Tests del baseline, del kind y del ConfigRepo</name>

  <read_first>
    - `tests/test_repositories.py:20-31` — la fixture `db` (BD in-memory por test con
      `models.Base.metadata.create_all` y una camara sembrada).
    - `tests/test_repositories.py:92-119` — `TEST_detection_stats_upsert` y
      `TEST_count_since_and_hourly_counts`: sembrar con el propio repo, fechas fijas y
      explicitas, y afirmar sobre el dict COMPLETO (`==`, no `in`). La clave de hora es
      string de dos digitos (`"09"`).
    - `tests/test_database.py` — el estilo de los tests del ORM legacy y su fixture de BD.
  </read_first>

  <files>tests/test_repositories.py, tests/test_database.py</files>

  <behavior>
    - `TEST_hourly_baseline_averages_across_days`: sembrar 3 dias con la hora 09 a `unique_tracks` 10, 20 y 30 (repartidos en varios minutos) -> `baseline["09"]["avg_total"] == 20.0` y `sample_days == 3`. Demuestra el doble GROUP BY: promediar las filas de minuto directamente daria otra cifra.
    - `TEST_hourly_baseline_reports_sample_days`: con un solo dia sembrado, `sample_days == 1` — es lo que permite al endpoint devolver `"unknown"` en vez de inventarse un veredicto (Pitfall 8).
    - `TEST_hourly_baseline_respects_until`: con `until` en el inicio de la ultima hora sembrada, esa hora NO aparece en el resultado. Es la guarda contra el sesgo de hora parcial (Pitfall 7).
    - `TEST_hourly_baseline_filters_by_camera`: filas de `cam2` no contaminan el baseline de `cam1`.
    - `TEST_hourly_baseline_empty_window`: ventana sin datos -> `{}`, sin excepcion.
    - `TEST_config_repo_roundtrip_list`: `set("yolo_classes", [0, 24, 28])` + `get("yolo_classes")` devuelve la lista intacta (la columna es `JSON`, no hay que serializar a mano); un `set` posterior sobrescribe (upsert) y `get` de una clave inexistente devuelve el `default`.
    - `TEST_get_zones_returns_kind` (en `tests/test_database.py`): una zona persistida con `kind="exclude_objects"` sale con ese `kind` en el dict de `get_zones()`, y una zona sin `kind` sale con `None`.
  </behavior>

  <action>
**A. `tests/test_repositories.py`** — los 6 primeros tests del bloque `<behavior>`, con la
fixture `db` existente. Sembrar SIEMPRE con `repo.upsert_minute(...)`, nunca con SQL
directo, y con fechas fijas y explicitas (`datetime.datetime(2026, 1, 1, 9, 5, 0)`).

Banda de comentario obligatoria antes del bloque del baseline:

```python
# ─── hourly_baseline: media movil por franja horaria (Fase 27, BEH-09) ───────
# El orden de agregacion es lo que se esta probando: primero SUM por (dia, hora)
# y solo despues AVG entre dias. Promediar las filas de minuto daria "media por
# minuto" y ponderaria mas los dias con mas minutos registrados. Se agrega sobre
# unique_tracks y no sobre detections porque detections depende del FPS que
# AdaptiveRate haya elegido (engine.py:281) y no es comparable entre dias.
# ─────────────────────────────────────────────────────────────────────────────
```

En `TEST_hourly_baseline_averages_across_days`, repartir los `unique_tracks` de cada dia
en **varios minutos distintos** (p. ej. 3 filas de 09:05, 09:20 y 09:40): si cada dia
tuviera una sola fila, el test pasaria igual con la implementacion ingenua y no probaria
nada.

**B. `tests/test_database.py`** — `TEST_get_zones_returns_kind`, con la fixture de BD de ese
fichero. Comentario citando que la columna ya existia fisicamente y que el que se habia
quedado atras era el ORM legacy, que es justo el que alimenta al pipeline (`main.py:468`).

Todos los nombres en `TEST_*`.
  </action>

  <verify>
    <automated>F:/Documentos/IA/Proyecto_Camara/.venv/Scripts/python.exe -m pytest tests/test_repositories.py tests/test_database.py -q</automated>
  </verify>

  <acceptance_criteria>
    - `pytest tests/test_repositories.py -k "hourly_baseline or config_repo" -q` recoge >= 6 tests y sale verde.
    - `pytest tests/test_database.py -k kind -q` recoge 1 test y sale verde.
    - `pytest tests/test_repositories.py tests/test_database.py -q` verdes enteros, sin regresion.
    - `grep -n '"09"' tests/test_repositories.py` acierta (clave de hora como string de dos digitos).
    - `grep -n "upsert_minute" tests/test_repositories.py` acierta dentro de los tests nuevos (siembra por el repo, no SQL crudo).
  </acceptance_criteria>

  <done>El baseline esta probado en sus cuatro modos de fallo (orden de agregacion, pocos dias, ventana acotada, camara equivocada), `ConfigRepo` tiene su primer test y `get_zones()` devuelve `kind`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| parametros del endpoint -> SQL | `camera_id` y la ventana temporal llegan de una peticion HTTP y acaban en una query |
| tabla `app_config` -> proceso | Valor JSON escrito por el operador desde la UI que condiciona que detecta el sistema |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-14 | Tampering | inyeccion SQL via `camera_id`/ventana en `hourly_baseline` | mitigate | Todo va como parametro ligado de SQLAlchemy; dentro del `text()` del `group_by` solo hay alias literales (`"day"`, `"hour"`), nunca datos de la peticion (ASVS V5) |
| T-27-15 | Denial of Service | query de baseline sobre una tabla sin retencion | accept | `detection_stats` crece 525.600 filas/año (80 MB medidos) y no tiene purga hoy, pero el coste de la query no crece con la tabla: range scan sobre el indice existente, p50 11,2 ms a un año de datos (27-RESEARCH Q7). La retencion queda fuera de alcance por decision del research |
| T-27-16 | Information Disclosure | el baseline devuelve conteos, nunca identidades | mitigate | `hourly_baseline` agrega `unique_tracks`; no toca `persons` ni `person_id`. El endpoint que lo consume (27-09) tiene la misma restriccion |
| T-27-17 | Tampering | `kind` de zona como vector de configuracion | accept | `kind` solo puede suprimir eventos de objeto, nunca crearlos; y el CRUD de zonas ya esta autenticado globalmente. Esta fase solo lo lee |
</threat_model>

<verification>
```bash
F:/Documentos/IA/Proyecto_Camara/.venv/Scripts/python.exe -m pytest tests/test_repositories.py tests/test_database.py tests/test_migrations.py -q
```
</verification>

<success_criteria>
- `hourly_baseline()` agrega en dos niveles sobre `unique_tracks`, acepta `until` y devuelve `sample_days`.
- No hay indice nuevo ni cambios en `models.py`/`migrations.py`.
- `get_zones()` del ORM legacy devuelve `kind`.
- `ConfigRepo` tiene su primer test de roundtrip con una `list[int]`.
- Los tres ficheros de test quedan verdes sin regresion.
</success_criteria>

<output>
Al terminar, crear `.planning/phases/27-multi-clase-y-contexto-de-escena/27-04-SUMMARY.md`.
</output>
