---
phase: 24-identidad-temporal-votaci-n-y-m-quina-de-estados
plan: 03
type: execute
wave: 3
depends_on: ["24-01", "24-02"]
files_modified:
  - backend/recognizer.py
  - tests/test_recognizer_orchestration.py
  - tests/test_memory_bounds.py
  - tests/test_architecture.py
autonomous: true
requirements: [FACE-07]
tags: [recognizer, arcface, refactor, memory-bounds]

must_haves:
  truths:
    - "El reconocimiento devuelve el score de similitud del match, no solo el person_id"
    - "Ya no existe una segunda capa de votación dentro de PersonRecognizer: los parámetros configurados de TemporalVoter son los efectivos"
    - "La firma pública process_crop(crop_bgr, tracker_id) -> (person_id, name, is_new) sigue funcionando igual para quien la use"
    - "TemporalVoter e IdentityStateMachine tienen cota de memoria demostrada tras 10.000 tracks"
  artifacts:
    - path: "backend/recognizer.py"
      provides: "FaceResult(person_id, name, is_new, score, ambiguous) y process_crop_scored()"
      contains: "def process_crop_scored"
    - path: "tests/test_memory_bounds.py"
      provides: "Cota de TemporalVoter._votes y de IdentityStateMachine._states"
      contains: "TEST_temporal_voter"
    - path: "tests/test_architecture.py"
      provides: "process_crop_scored dentro del set INFERENCE_CALLS"
      contains: "process_crop_scored"
  key_links:
    - from: "backend/recognizer.py::process_crop_scored"
      to: "backend/recognizer.py::_best_match"
      via: "el score (best_sim) deja de descartarse y sube hasta el retorno"
      pattern: "score"
    - from: "backend/recognizer.py::process_crop"
      to: "backend/recognizer.py::process_crop_scored"
      via: "wrapper de compatibilidad, misma firma y mismo retorno de 3 elementos"
      pattern: "return .*person_id.*name.*is_new"
---

<objective>
Adaptar `backend/recognizer.py` a la Fase 24: exponer el score de similitud del match
(que hoy se calcula y se tira) y **retirar la votación por mayoría interna**
(`_votes`, `VOTE_WINDOW`), que quedaría encadenada delante de `TemporalVoter`. Actualizar
en el mismo commit los tres ficheros de test que dependen de lo retirado, y añadir los
dos tests de cota de memoria que exige el invariante de la Fase 22.

Purpose: sin el score, `IdentityStateMachine.on_face_result(track_id, person_id, score)`
no puede existir. Sin retirar `_votes`, los votos que llegan a `TemporalVoter` ya vienen
suavizados por una mayoría de 5 y el criterio 3 de la fase pasaría "demasiado fácil"
(Pitfall 1 del RESEARCH).

Output: `FaceResult` + `process_crop_scored()`, `process_crop()` intacta como wrapper,
y 4 ficheros de test coherentes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/24-identidad-temporal-votaci-n-y-m-quina-de-estados/24-RESEARCH.md
@.planning/phases/24-identidad-temporal-votaci-n-y-m-quina-de-estados/24-PATTERNS.md
@.planning/phases/24-identidad-temporal-votaci-n-y-m-quina-de-estados/24-02-SUMMARY.md
@CLAUDE.md

**Intérprete:** `F:/Documentos/IA/Proyecto_Camara/.venv/Scripts/python.exe`.
**Convención de tests:** funciones nuevas `TEST_*` obligatorio.

**Decisión de diseño ya tomada (opción 2 del Mismatch 2 del RESEARCH):** se añade
`process_crop_scored()` y `process_crop()` queda como wrapper. NO se cambia la aridad
de la tupla de `process_crop`. Motivo: `tests/test_recognizer_orchestration.py:92`
congela esa firma por introspección y hay ~15 llamadas que hacen unpacking de 3.

<interfaces>
Firma actual a preservar — `backend/recognizer.py:152-154`:
```python
def process_crop(
    self, crop_bgr: np.ndarray, tracker_id: int
) -> tuple[int | None, str | None, bool]:
```

Privada, a ampliar con el score — `backend/recognizer.py:424`:
```python
def _best_match(self, enc: np.ndarray) -> tuple[int | None, str | None, bool]:
    ...
    best_pid, best_sim = ranked[0]
    if best_sim < self._match_threshold:
        return None, None, False        # best_sim se descarta
    if len(ranked) > 1 and (best_sim - ranked[1][1]) < self.MATCH_MARGIN:
        return None, None, True         # best_sim se descarta
    return best_pid, self._name_of(best_pid), False   # best_sim se descarta
```

Bloque a ELIMINAR — `backend/recognizer.py:200-215`:
```python
with self._lock:
    cached = self._cache.get(tracker_id)
    pid, name, ambiguous = self._best_match(enc)
    if pid is not None:
        # Majority vote over the last VOTE_WINDOW decisive matches:
        # the winner — not necessarily this sample — is the identity.
        votes = self._votes.setdefault(tracker_id, deque(maxlen=self.VOTE_WINDOW))
        votes.append(pid)
        winner = Counter(votes).most_common(1)[0][0]
        winner_name = name if winner == pid else self._name_of(winner)
        if cached is not None and cached[0] != winner:
            logger.info(
                "PersonRecognizer: re-verify corrected tracker %d: "
                "person %d → %d", tracker_id, cached[0], winner,
            )
        self._touch(winner)
        self._cache[tracker_id] = (winner, winner_name)
        self._pending.pop(tracker_id, None)
        return winner, winner_name, False
```

Set que congela las rutas de inferencia — `tests/test_architecture.py:15-17`:
```python
INFERENCE_CALLS = {
    "detect_sv", "detect", "embed", "process_crop", "identify_or_register",
}
```
</interfaces>

<known_breakages>
Puntos exactos que rompen y hay que arreglar **en el mismo commit**:

| Fichero:línea | Qué asume hoy | Acción |
|---|---|---|
| `tests/test_memory_bounds.py:82` | `r._votes[tid] = None` | Eliminar la línea |
| `tests/test_memory_bounds.py:89` | `assert len(r._votes) == 10` | Eliminar la línea |
| `tests/test_memory_bounds.py:72` | comentario que menciona `_votes` | Reescribir el comentario |
| `tests/test_memory_bounds.py:233` | `recognizer._pending, recognizer._votes = {}, {}` | Dejar solo `recognizer._pending = {}` |
| `tests/test_recognizer_orchestration.py:92` | firma congelada de `process_crop` | **No cambia** (por eso se elige el wrapper) — verificar que sigue verde |
| `tests/test_recognizer_orchestration.py:247-269` | `TEST_reverify_majority_vote_corrects_identity` afirma el voto interno | Reescribir: ahora el match es por frame y la corrección la hace `IdentityStateMachine` |
| `tests/test_architecture.py:15-17` | `INFERENCE_CALLS` sin `process_crop_scored` | Añadir `"process_crop_scored"` |
</known_breakages>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Exponer el score y retirar la votación interna de PersonRecognizer</name>
  <read_first>
    - `backend/recognizer.py` líneas 1-90 (docstring del módulo, constantes de clase incluyendo `VOTE_WINDOW = 5` en la 49, `__init__` con `_votes` en la 75 y el comentario "reserved for Fase 24's TemporalVoter" en la 78).
    - `backend/recognizer.py` líneas 145-260 (`process_crop` completo, con su docstring que describe la votación y el bloque a retirar).
    - `backend/recognizer.py` líneas 335-355 (`prune`) y 420-460 (`_best_match`).
    - `backend/perception/face/quality.py` líneas 27-40 — patrón de dataclass de resultado con comentario del dominio de valores.
  </read_first>
  <action>
En `backend/recognizer.py`:

1. **Dataclass del resultado.** Junto a los imports/constantes de módulo:
   ```python
   @dataclass
   class FaceResult:
       """Resultado de una pasada de reconocimiento sobre un crop (Fase 24).

       `score` es la similitud coseno del mejor match (0.0 si no hubo match). La
       IdentityStateMachine lo necesita para votar; antes de la Fase 24 se calculaba
       en _best_match y se descartaba.
       """

       person_id: int | None
       name: str | None
       is_new: bool
       score: float = 0.0
       ambiguous: bool = False
   ```
   Añadir `from dataclasses import dataclass` al bloque de imports.

2. **`_best_match` devuelve 4 elementos**: `tuple[int | None, str | None, bool, float]`
   → `(person_id, name, ambiguous, score)`. En los tres `return` actuales:
   - por debajo del umbral: `return None, None, False, float(best_sim)`
   - ambiguo por `MATCH_MARGIN`: `return None, None, True, float(best_sim)`
   - match: `return best_pid, self._name_of(best_pid), False, float(best_sim)`
   Si `ranked` está vacío, devolver `None, None, False, 0.0`.

3. **Renombrar el cuerpo a `process_crop_scored`**, con la firma
   `def process_crop_scored(self, crop_bgr: np.ndarray, tracker_id: int) -> FaceResult:`
   y todos los `return None, None, False` convertidos en
   `return FaceResult(None, None, False, score, ambiguous)` con los valores que
   correspondan (score 0.0 donde no se llegó a hacer match).

4. **Eliminar la votación interna** dentro de `process_crop_scored`:
   - Borrar la constante `VOTE_WINDOW = 5` (línea 49) y su comentario.
   - Borrar `self._votes: dict[int, deque[int]] = {}` (línea 75) y su comentario (74).
   - Sustituir el bloque de las líneas 202-215 por:
     ```python
     with self._lock:
         cached = self._cache.get(tracker_id)
         pid, name, ambiguous, score = self._best_match(enc)
         if pid is not None:
             # Sin voto por mayoria aqui: el match es por frame y la evidencia
             # temporal la acumula TemporalVoter (Fase 24, FACE-07). Encadenar dos
             # votaciones haria que los parametros configurados no fueran los
             # efectivos.
             self._touch(pid)
             self._cache[tracker_id] = (pid, name)
             self._pending.pop(tracker_id, None)
             return FaceResult(pid, name, False, score, ambiguous)
     ```
   - Quitar `Counter` y `deque` del import de la línea 15 **solo si** dejan de usarse
     en el fichero (comprobar con grep antes de tocar el import).
   - En `prune` (línea 348), quitar `self._votes` de la tupla y actualizar el docstring
     (línea 344) para que ya no lo mencione.
   - Línea 78: cambiar el comentario `# reserved for Fase 24's TemporalVoter` por
     `# umbral de confianza de identidad usado por IdentityStateMachine (Fase 24)`.

5. **`process_crop` como wrapper de compatibilidad**, con exactamente la misma firma
   e idéntico tipo de retorno:
   ```python
   def process_crop(
       self, crop_bgr: np.ndarray, tracker_id: int
   ) -> tuple[int | None, str | None, bool]:
       """Compatibilidad: process_crop_scored() sin el score ni el flag de ambiguedad.

       El pipeline (RecognitionWorker) usa process_crop_scored desde la Fase 24; esta
       forma se conserva para los llamadores que no necesitan el score.
       """
       r = self.process_crop_scored(crop_bgr, tracker_id)
       return r.person_id, r.name, r.is_new
   ```
   La firma debe seguir siendo exactamente `(crop_bgr, tracker_id)` — hay un test que la
   congela por introspección.

6. **Reescribir el docstring de `process_crop_scored`** (hoy en las líneas 173-177 de
   `process_crop`): eliminar los párrafos que describen la re-verificación por mayoría
   de `VOTE_WINDOW` y sustituirlos por una nota de que la evidencia temporal la acumula
   `TemporalVoter` fuera de esta clase. No dejar el docstring mintiendo.

En `tests/test_architecture.py`, añadir `"process_crop_scored"` al set `INFERENCE_CALLS`
(línea 15-17): es una ruta que ejecuta inferencia y el invariante debe seguir
protegiéndola.
  </action>
  <verify>
    <automated>F:/Documentos/IA/Proyecto_Camara/.venv/Scripts/python.exe -m pytest tests/test_architecture.py tests/test_face_engine.py tests/test_identity_index.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "VOTE_WINDOW\|_votes" backend/recognizer.py` no devuelve nada.
    - `grep -n "def process_crop_scored" backend/recognizer.py` encuentra la función.
    - `grep -n "class FaceResult" backend/recognizer.py` encuentra el dataclass.
    - `grep -n "def process_crop" backend/recognizer.py` sigue encontrando `process_crop` (el wrapper).
    - `grep -n "process_crop_scored" tests/test_architecture.py` encuentra la entrada en `INFERENCE_CALLS`.
    - `python -c "import inspect; from backend.recognizer import PersonRecognizer as P; print(list(inspect.signature(P.process_crop).parameters))"` imprime `['self', 'crop_bgr', 'tracker_id']`.
    - `pytest tests/test_architecture.py -q` sale con código 0.
  </acceptance_criteria>
  <done>`process_crop_scored` devuelve `FaceResult` con el score real, la votación por mayoría interna ha desaparecido, `process_crop` mantiene su contrato y el test de arquitectura protege la nueva ruta de inferencia.</done>
</task>

<task type="auto">
  <name>Task 2: Reparar los tests que dependían de _votes y del voto interno</name>
  <read_first>
    - `tests/test_recognizer_orchestration.py` líneas 85-100 (firma congelada) y 246-290 (`TEST_reverify_majority_vote_corrects_identity` y `TEST_unknown_face_during_reverify_does_not_register`).
    - `tests/test_memory_bounds.py` líneas 68-95 (`TEST_recognizer_cache_bounded`) y 215-250 (el test de crecimiento con tracemalloc).
    - `24-RESEARCH.md` § "Pitfall 1: Doble votación encadenada" — el porqué de la retirada, para dejarlo escrito en los comentarios de los tests.
  </read_first>
  <action>
En `tests/test_recognizer_orchestration.py`:

1. Sustituir `TEST_reverify_majority_vote_corrects_identity` (líneas ~247-269) por
   `TEST_match_is_per_frame_without_internal_vote`, que afirma el comportamiento nuevo:
   con la persona A registrada y luego un embedding de la persona B, la **segunda**
   llamada ya devuelve `pid_b` (antes devolvía `pid_a` por el empate `[A, B]`) y
   `get_cached(1) == (pid_b, None)`. Comentario de bloque explicando el porqué:
   ```python
   # ─── El match es por frame: la evidencia temporal vive fuera del recognizer ───
   # Hasta la Fase 23 PersonRecognizer votaba por mayoria sobre las ultimas
   # VOTE_WINDOW=5 coincidencias. La Fase 24 retira esa capa: encadenarla delante de
   # TemporalVoter haria que los parametros configurados (window/min_votes/min_ratio)
   # no fueran los efectivos y el criterio 3 de la fase pasaria por accidente.
   # La correccion de una identidad equivocada la hace ahora IdentityStateMachine.
   # ─────────────────────────────────────────────────────────────────────────────
   ```
2. Añadir `TEST_process_crop_scored_returns_similarity`: con un embedding conocido
   registrado, `process_crop_scored(_FAKE_FRAME, tracker_id=1)` devuelve un `FaceResult`
   con `person_id` correcto y `0.0 < score <= 1.0`; y que
   `process_crop(...) == (r.person_id, r.name, r.is_new)` (el wrapper es coherente).
3. Verificar que `TEST_public_contract_unchanged` (línea 90-95) sigue en verde sin
   tocarlo. Si falla, es que el wrapper cambió la firma — arreglar el wrapper, no el test.
4. `TEST_unknown_face_during_reverify_does_not_register` no debe cambiar: la guarda
   `cached is not None` sigue existiendo.

En `tests/test_memory_bounds.py`:

5. En `TEST_recognizer_cache_bounded` (líneas 68-89): eliminar `r._votes[tid] = None`
   (línea 82) y `assert len(r._votes) == 10` (línea 89), y actualizar el comentario de
   cabecera (línea 72) para que enumere solo `_cache`, `_last_attempt` y `_pending`.
6. En el test de crecimiento con tracemalloc (línea 233): cambiar
   `recognizer._pending, recognizer._votes = {}, {}` por `recognizer._pending = {}`.
7. Añadir dos tests nuevos de cota (invariante de la Fase 22, siguiendo el patrón de
   reloj sintético de las líneas 46-53):
   ```python
   # ─── TemporalVoter no acumula votos de tracks muertos ────────────────────────
   # ByteTrack asigna ids monotonamente crecientes y nunca los reutiliza: sin
   # prune(active_ids), _votes crece sin cota en un proceso 24/7.
   # ─────────────────────────────────────────────────────────────────────────────
   def TEST_temporal_voter_bounded():
       voter = TemporalVoter(window=8)
       for tid in range(10_000):
           voter.vote(tid, 1, 0.9)
           voter.prune(set(range(max(0, tid - 5), tid + 1)))
       assert len(voter._votes) <= 6

   # ─── IdentityStateMachine expira sus estados por tiempo, no por active_ids ───
   # TEMPORARILY_LOST existe para sobrevivir a que el track desaparezca, asi que no
   # se puede podar contra active_ids; la cota la dan lost_ttl y el TTL de estados
   # rancios de on_tick().
   # ─────────────────────────────────────────────────────────────────────────────
   def TEST_identity_state_machine_bounded():
       fsm = IdentityStateMachine(TemporalVoter(window=8), lost_ttl=30.0,
                                  revalidate_after=120.0)
       for tid in range(10_000):
           now = float(tid)
           fsm.on_face_result(tid, 1, 0.9, now)
           fsm.on_active_tracks({tid}, now)
           fsm.on_tick(now)
       assert len(fsm._states) <= 500
   ```
   Ajustar la cota superior del segundo test a lo que salga realmente (debe ser un número
   fijo y pequeño frente a 10.000, derivado de
   `lost_ttl + revalidate_after * MAX_FAILED_REVALIDATIONS = 390` unidades de reloj):
   ejecutar, leer el valor y fijar el `assert` con margen, dejando escrito en el
   comentario de dónde sale el número. **No** relajar la cota hasta que pase; si el
   número es del orden de 10.000, la expiración no funciona y hay que arreglar la FSM.
   Añadir los imports de `TemporalVoter` e `IdentityStateMachine` en la cabecera.
  </action>
  <verify>
    <automated>F:/Documentos/IA/Proyecto_Camara/.venv/Scripts/python.exe -m pytest tests/test_recognizer_orchestration.py tests/test_memory_bounds.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "_votes" tests/test_memory_bounds.py` solo devuelve líneas referidas a `TemporalVoter._votes` (ninguna sobre `PersonRecognizer`).
    - `grep -n "TEST_reverify_majority_vote_corrects_identity" tests/test_recognizer_orchestration.py` no devuelve nada.
    - `grep -c "def TEST_temporal_voter_bounded\|def TEST_identity_state_machine_bounded" tests/test_memory_bounds.py` devuelve 2.
    - `pytest tests/test_memory_bounds.py -k "voter or state_machine" -q` sale con código 0.
    - `pytest tests/test_recognizer_orchestration.py tests/test_memory_bounds.py -q` sale con código 0.
    - `pytest tests/test_phase9.py -q` sale con código 0 (usa `identify_or_register`, que llega a `process_crop`).
  </acceptance_criteria>
  <done>Los 4 puntos de rotura conocidos están reparados, la capacidad de re-verificación queda documentada como responsabilidad de la FSM, y `TemporalVoter`/`IdentityStateMachine` tienen cota de memoria demostrada con 10.000 tracks.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Embedding facial → `_best_match` | Dato biométrico derivado entra en la decisión de identidad |
| `PersonRecognizer` → caché por `tracker_id` | Estado acumulativo por track en un proceso 24/7 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-09 | Spoofing | `_best_match` | mitigate | Se conservan intactos `_match_threshold` y el ratio test `MATCH_MARGIN`: retirar `_votes` no relaja ninguna de las dos guardas por frame, solo mueve la agregación temporal a `TemporalVoter` |
| T-24-10 | Denial of Service | `_cache`/`_pending`/`_last_attempt` | mitigate | `prune(active_tracker_ids)` sigue existiendo; `TEST_recognizer_cache_bounded` se actualiza pero mantiene la aserción de cota sobre los tres diccionarios restantes |
| T-24-11 | Information Disclosure | log de re-verificación | mitigate | El `logger.info` que imprimía la corrección de identidad desaparece con el bloque de votación. La FSM no loguea nombres; los ids que sí se logueen seguirán siendo enteros (ASVS V8) |
| T-24-12 | Tampering | `process_crop` (wrapper) | accept | Se conserva la firma pública por compatibilidad; `TEST_public_contract_unchanged` la congela por introspección y detectaría cualquier cambio accidental |
</threat_model>

<verification>
```bash
F:/Documentos/IA/Proyecto_Camara/.venv/Scripts/python.exe -m pytest tests/test_recognizer_orchestration.py tests/test_memory_bounds.py tests/test_architecture.py tests/test_phase9.py tests/test_temporal_voting.py tests/test_identity_state_machine.py -q
```
Todos en verde. `tests/test_recognition_worker.py` seguirá rojo hasta 24-05 (usa
`process_crop`, que sigue funcionando, así que en realidad debería seguir verde — si
falla aquí, hay una regresión real que arreglar ahora).
</verification>

<success_criteria>
- `grep -n "VOTE_WINDOW\|_votes" backend/recognizer.py` no devuelve nada.
- `process_crop_scored` devuelve el score de similitud real y `process_crop` mantiene su contrato de 3 elementos.
- `INFERENCE_CALLS` incluye `process_crop_scored`.
- Los 4 puntos de rotura documentados están reparados en el mismo commit que el cambio.
- Existen tests de cota para `TemporalVoter` e `IdentityStateMachine` con 10.000 tracks.
- `pytest tests/test_recognizer_orchestration.py tests/test_memory_bounds.py tests/test_architecture.py tests/test_phase9.py -q` sale con código 0.
</success_criteria>

<output>
Al terminar, crear `.planning/phases/24-identidad-temporal-votaci-n-y-m-quina-de-estados/24-03-SUMMARY.md`.
</output>
