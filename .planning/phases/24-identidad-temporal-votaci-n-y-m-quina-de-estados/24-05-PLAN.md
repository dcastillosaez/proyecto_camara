---
phase: 24-identidad-temporal-votaci-n-y-m-quina-de-estados
plan: 05
type: execute
wave: 4
depends_on: ["24-01", "24-02", "24-03", "24-04"]
files_modified:
  - backend/pipeline/recognition.py
  - backend/pipeline/manager.py
  - backend/main.py
  - tests/test_recognition_worker.py
autonomous: true
requirements: [FACE-08, FACE-09, FACE-10, FACE-11]
tags: [pipeline, recognition-worker, wiring, inference-budget]

must_haves:
  truths:
    - "El reconocimiento se dispara por evento (track nuevo, votación en curso, confianza de identidad baja, revalidación vencida), no ciegamente cada N frames"
    - "Con un track estático que nunca llega a identificarse, las inferencias faciales bajan al menos un 70% respecto al comportamiento de la Fase 23"
    - "Los eventos de identidad llegan al EventBus desde el hilo del worker, sin await y sin construir Event a mano"
    - "El estado de identidad de cada track queda escrito en el TrackRegistry"
    - "Un reinicio del worker por el supervisor no borra la identidad ya confirmada"
  artifacts:
    - path: "backend/pipeline/recognition.py"
      provides: "RecognitionWorker con identity_fsm + event_engine, gate FACE-11 y contador face_inferences"
      contains: "needs_recognition"
    - path: "backend/pipeline/manager.py"
      provides: "IdentityStateMachine construida FUERA de _make_recognition + event_engine cableado"
      contains: "self.identity_fsm"
    - path: "tests/test_recognition_worker.py"
      provides: "Presupuesto de inferencias (criterio 6) y disparador por confianza de identidad"
      contains: "TEST_inference_budget"
  key_links:
    - from: "backend/pipeline/recognition.py::_loop"
      to: "IdentityStateMachine.on_face_result / on_tick / on_active_tracks"
      via: "llamada síncrona desde el hilo del worker, reloj monotónico inyectado"
      pattern: "self\\._fsm\\.on_"
    - from: "backend/pipeline/recognition.py"
      to: "EventEngine.emit_identity"
      via: "publicación de la transición devuelta por la FSM"
      pattern: "emit_identity\\("
    - from: "backend/pipeline/manager.py"
      to: "RecognitionWorker"
      via: "identity_fsm=self.identity_fsm construido fuera de la factoría del supervisor"
      pattern: "identity_fsm=self\\.identity_fsm"
---

<objective>
Cablear la máquina de estados al pipeline: `RecognitionWorker` pasa a ser el dueño del
ciclo de vida de la FSM, sustituye su gate ciego por `needs_recognition()` (FACE-11),
escribe `identity_state` en el registry (FACE-08) y publica los eventos de identidad
vía `EventEngine` (FACE-09/FACE-10). `manager.py` construye la FSM **fuera** de la
factoría del supervisor y le pasa el `event_engine` que hoy no recibe.

Purpose: es el único sitio donde FACE-11 puede cumplirse. SPEC §9 no lista estos
ficheros; el RESEARCH corrigió esa lista (Mismatch 1) — `perception/face/engine.py` es
un adaptador de 106 líneas de insightface que no conoce tracks ni tiempo.

Output: pipeline funcional de identidad temporal, con el criterio 6 medido de forma
automatizada.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/24-identidad-temporal-votaci-n-y-m-quina-de-estados/24-RESEARCH.md
@.planning/phases/24-identidad-temporal-votaci-n-y-m-quina-de-estados/24-PATTERNS.md
@.planning/phases/24-identidad-temporal-votaci-n-y-m-quina-de-estados/24-03-SUMMARY.md
@.planning/phases/24-identidad-temporal-votaci-n-y-m-quina-de-estados/24-04-SUMMARY.md
@CLAUDE.md

**Intérprete:** `F:/Documentos/IA/Proyecto_Camara/.venv/Scripts/python.exe`.
**Convención de tests:** las funciones NUEVAS deben llamarse `TEST_*`. Las existentes de
`test_recognition_worker.py` están en minúscula (`def test_*`) y no se recogen en el CI
Linux; no hace falta renombrarlas en esta fase, pero las nuevas sí van en `TEST_*`.

<interfaces>
Disponible desde 24-02 (`backend/perception/face/identity.py`):
```python
class IdentityStateMachine:
    MAX_FAILED_REVALIDATIONS = 3
    def __init__(self, voter=None, lost_ttl=30.0, revalidate_after=120.0,
                 low_confidence=0.55) -> None: ...
    def on_face_result(self, track_id, person_id, score, now) -> IdentityTransition | None
    def on_track_lost(self, track_id, now) -> IdentityTransition | None
    def on_active_tracks(self, active_ids: set[int], now: float) -> list[IdentityTransition]
    def on_tick(self, now: float) -> list[IdentityTransition]
    def state_of(self, track_id: int) -> IdentityState
    def identity_of(self, track_id: int) -> tuple[int | None, float]
    def needs_recognition(self, track_id: int, now: float) -> bool
```

Disponible desde 24-03 (`backend/recognizer.py`):
```python
@dataclass
class FaceResult:
    person_id: int | None
    name: str | None
    is_new: bool
    score: float = 0.0
    ambiguous: bool = False

def process_crop_scored(self, crop_bgr, tracker_id) -> FaceResult: ...
def process_crop(self, crop_bgr, tracker_id) -> tuple[int | None, str | None, bool]: ...
```

Disponible desde 24-04:
```python
TrackRegistry.set_identity_state(track_id: int, state: IdentityState) -> None
EventEngine.emit_identity(transition, now: datetime.datetime, person_name=None,
                          bbox=None, captured_at=None, processed_at=None) -> None
```

Bucle actual a modificar — `backend/pipeline/recognition.py:99-142`:
```python
def _loop(self) -> None:
    available = getattr(self._recognizer, "available", False)
    while self._running:
        frame = self._sub.get(timeout=1.0)
        if frame is None: continue
        if not available: continue
        now = time.monotonic()
        self._maybe_prune(now)
        if not self._rate.should_process(now): continue
        target = self._next_candidate(now)
        if target is None: continue
        crop = self._crop_for(frame.image, target.bbox)
        if crop is None: continue
        t0 = time.monotonic()
        try:
            pid, name, _ = self._recognizer.process_crop(crop, target.track_id)
        except Exception:
            self._exceptions += 1
            logger.exception("RecognitionWorker: fallo de reconocimiento (track %d)", target.track_id)
            continue
        face_latency = time.monotonic() - t0
        self._rate.observe(face_latency)
        _metrics.inference_latency_seconds.labels(stage="face").observe(face_latency)
        if pid is None: continue
        self._registry.set_identity(target.track_id, pid, name)
        self._identified += 1
        if self._on_identified is not None:
            try: self._on_identified(crop, pid)
            except Exception: logger.exception("RecognitionWorker: on_identified fallo")
```

Gate actual a sustituir — `backend/pipeline/recognition.py:144-152`:
```python
def _next_candidate(self, now: float):
    """Track mas antiguo sin identidad y con edad suficiente, o None."""
    candidates = [
        ts for ts in self._registry.snapshot().values()
        if ts.person_id is None and (now - ts.first_seen) >= self._min_track_age
    ]
    if not candidates: return None
    return min(candidates, key=lambda ts: ts.first_seen)
```

Patrón de emisión desde un hilo — `backend/pipeline/detection.py:187-195`:
```python
def _emit_track_lifecycle(self, tracked, captured_at, processed_at) -> None:
    if self._event_engine is None:
        return
    wall_now = datetime.datetime.now()
    ...
    self._event_engine.process_tracks(active_ids, wall_now, captured_at, processed_at)
```

Factoría del supervisor a modificar — `backend/pipeline/manager.py:114-125`:
```python
if recognizer is not None and getattr(recognizer, "available", False):
    def _make_recognition() -> RecognitionWorker:
        self.recognition = RecognitionWorker(
            self.broker.subscribe("recognition", replace=True),
            self.registry, recognizer,
            AdaptiveRate(target_fps=recognition_fps,
                         min_fps=recognition_fps, max_fps=recognition_fps),
            on_identified=on_identified,
        )
        return self.recognition
    self.supervisor.register("recognition", _make_recognition)
```

Precedente de estado rescatado entre reinicios — `backend/pipeline/manager.py:91-99`
(`_make_streaming` recupera `clients` de la instancia anterior).
</interfaces>

<locked_user_decisions>
- **D-01**: el criterio 6 (≥70% menos inferencias) se mide sobre un track **NO
  confirmado**. Sobre un track ya identificado el filtro actual ya hace 0 inferencias y
  medir ahí no significa nada; además esta fase *añade* la revalidación cada 120 s.
  El SUMMARY debe documentar el escenario exacto medido, no solo el porcentaje.
- **D-03**: el disparador "confianza baja" usa `TemporalVoter.verdict()[1]` (ya
  encapsulado en `needs_recognition`), NO `TrackState.confidence`.
</locked_user_decisions>

<known_breakages>
`recognizer.process_crop.return_value = (None, None, False)` y variantes aparecen en
`tests/test_recognition_worker.py` líneas **54, 77, 97, 130 y 152** (5 sitios, no 4).
Al pasar el worker a `process_crop_scored`, un `MagicMock` devolverá un `MagicMock` y
las aserciones sobre `process_crop.call_count` (líneas 65, 87) dejarán de medir nada.
Hay que actualizar los 5 en la Task 1, en el mismo commit.
</known_breakages>
</context>

<tasks>

<task type="auto">
  <name>Task 1: RecognitionWorker toma posesión de la FSM y del gate FACE-11</name>
  <read_first>
    - `backend/pipeline/recognition.py` completo (177 líneas) — especialmente el docstring del módulo (líneas 1-11, que dice "la revalidacion temporal llega en la Fase 24" y hay que reescribir), el constructor (38-60), `stats` (87-93), `_loop` (99-142), `_next_candidate` (144-152) y `_maybe_prune` (161-177).
    - `backend/pipeline/detection.py` líneas 26-70 (import bajo `TYPE_CHECKING`, `event_engine` opcional en el constructor) y 178-196 (emisión desde el hilo con `datetime.datetime.now()`).
    - `tests/test_recognition_worker.py` completo (189 líneas) — los 5 puntos de mock y las aserciones sobre `call_count`.
  </read_first>
  <action>
En `backend/pipeline/recognition.py`:

1. **Reescribir el docstring del módulo** (líneas 1-11). Los párrafos "Los tracks ya
   identificados se saltan — la revalidacion temporal llega en la Fase 24" ya no son
   ciertos. Explicar: consume del broker, elige a quién inferir preguntando a
   `IdentityStateMachine.needs_recognition()` (FACE-11), es el único escritor de
   `person_id`/`person_name`/`identity_state` en el registry, y publica los eventos de
   identidad vía `EventEngine`. Mencionar que sin `identity_fsm` conserva el
   comportamiento de la Fase 23 (útil para medir el baseline del criterio 6).

2. **Imports**: `datetime` a nivel de módulo; bajo `TYPE_CHECKING` añadir
   `from backend.events.engine import EventEngine`. `IdentityStateMachine` e
   `IdentityState` se importan en runtime desde `backend.perception.face.identity`
   (se usan valores, no solo anotaciones).

3. **Constructor**: dos parámetros nuevos, ambos con default `None` para no romper a
   los llamadores existentes:
   ```python
   identity_fsm: IdentityStateMachine | None = None,
   event_engine: EventEngine | None = None,
   ```
   Guardarlos en `self._fsm` y `self._event_engine`. Añadir `self._face_inferences = 0`.

4. **`stats`**: añadir `"face_inferences": self._face_inferences` al diccionario. Es el
   contador con el que se mide el criterio 6 (`face_fps` del sampler NO sirve: viene de
   `AdaptiveRate.stats` y marca el FPS objetivo aunque no se ejecute ni una inferencia
   — Mismatch 5 del RESEARCH).

5. **`_next_candidate(now)`**: sustituir el filtro `ts.person_id is None` por la
   pregunta a la FSM, conservando el filtro de edad mínima:
   ```python
   def _next_candidate(self, now: float):
       """Track mas antiguo que merece una inferencia facial ahora, o None.

       Con FSM (Fase 24, FACE-11) el criterio es needs_recognition(): track nuevo,
       votacion en curso, identidad temporalmente perdida, confianza de identidad
       baja o revalidacion vencida. Sin FSM se conserva el criterio de la Fase 23
       (`person_id is None`), que reintentaba indefinidamente sobre los tracks que
       nunca llegan a identificarse.
       """
       tracks = [
           ts for ts in self._registry.snapshot().values()
           if (now - ts.first_seen) >= self._min_track_age
       ]
       if self._fsm is None:
           tracks = [ts for ts in tracks if ts.person_id is None]
       else:
           tracks = [ts for ts in tracks if self._fsm.needs_recognition(ts.track_id, now)]
       if not tracks:
           return None
       return min(tracks, key=lambda ts: ts.first_seen)
   ```

6. **`_loop`**: insertar el mantenimiento de la FSM justo después de `_maybe_prune(now)`
   y **antes** de `self._rate.should_process(now)` (el ciclo de vida de la identidad no
   debe depender del ritmo de inferencia):
   ```python
   self._maybe_prune(now)
   self._sync_identity(now)
   if not self._rate.should_process(now):
       continue
   ```

7. **`_sync_identity(now)`** (método nuevo, con el manejo de errores del bucle caliente:
   nunca deja morir el hilo):
   ```python
   def _sync_identity(self, now: float) -> None:
       """Tracks caidos + expiraciones de lost_ttl. Un solo hilo, sin lock."""
       if self._fsm is None:
           return
       try:
           transitions = self._fsm.on_active_tracks(self._registry.active_ids(), now)
           transitions += self._fsm.on_tick(now)
       except Exception:
           self._exceptions += 1
           logger.exception("RecognitionWorker: mantenimiento de la FSM de identidad fallo")
           return
       for t in transitions:
           self._emit_identity(t)
   ```

8. **`_emit_identity(transition, person_name=None, bbox=None, captured_at=None)`**
   (método nuevo, patrón de `detection.py:187-195`):
   ```python
   def _emit_identity(self, transition, person_name=None, bbox=None,
                      captured_at=None, processed_at=None) -> None:
       if self._event_engine is None or transition is None:
           return
       try:
           self._event_engine.emit_identity(
               transition, datetime.datetime.now(), person_name=person_name,
               bbox=bbox, captured_at=captured_at, processed_at=processed_at,
           )
       except Exception:
           logger.exception("RecognitionWorker: emision de evento de identidad fallo")
   ```

9. **Cuerpo del reconocimiento** dentro de `_loop`: cambiar la llamada a
   `process_crop_scored` y contar la inferencia:
   ```python
   t0 = time.monotonic()
   try:
       result = self._recognizer.process_crop_scored(crop, target.track_id)
   except Exception:
       self._exceptions += 1
       logger.exception("RecognitionWorker: fallo de reconocimiento (track %d)", target.track_id)
       continue
   self._face_inferences += 1
   face_latency = time.monotonic() - t0
   self._rate.observe(face_latency)
   _metrics.inference_latency_seconds.labels(stage="face").observe(face_latency)
   ```
   Contar `_face_inferences` **después** del `try` para que solo cuente inferencias
   completadas, igual que la métrica de latencia.

   Y bifurcar el tratamiento del resultado:
   ```python
   if self._fsm is None:
       # Comportamiento Fase 23 — baseline del criterio 6
       if result.person_id is None:
           continue
       self._registry.set_identity(target.track_id, result.person_id, result.name)
       self._identified += 1
       self._notify_identified(crop, result.person_id)
       continue

   now2 = time.monotonic()
   transition = self._fsm.on_face_result(
       target.track_id, result.person_id, result.score, now2
   )
   self._registry.set_identity_state(target.track_id, self._fsm.state_of(target.track_id))
   pid, _conf = self._fsm.identity_of(target.track_id)
   if pid is not None:
       # El nombre solo es fiable si corresponde al pid que la FSM ha fijado:
       # el ganador de la votacion puede no ser el match de ESTE frame.
       name = result.name if result.person_id == pid else None
       self._registry.set_identity(target.track_id, pid, name)
   if transition is not None:
       self._emit_identity(
           transition,
           person_name=result.name if result.person_id == transition.person_id else None,
           bbox=target.bbox,
           captured_at=frame.captured_at,
           processed_at=now2,
       )
   if transition is not None and transition.to_state is IdentityState.CONFIRMED:
       self._identified += 1
       self._notify_identified(crop, pid)
   ```
   Extraer el callback existente a `_notify_identified(crop, person_id)` conservando
   su manejo de errores actual (`logger.exception("RecognitionWorker: on_identified fallo")`,
   sin incrementar `_exceptions`). Con FSM, la galería se alimenta **solo al confirmar**,
   no en cada frame identificado.

10. **`_maybe_prune`**: no tocar. El voter se poda dentro de `on_active_tracks`
    (Pitfall 2: la FSM NO se poda por `active_ids`).

En `tests/test_recognition_worker.py`, actualizar los 5 puntos de mock (líneas 54, 77,
97, 130, 152) para que el `MagicMock` cubra el método nuevo. Definir un helper local:
```python
def _face(person_id=None, name=None, is_new=False, score=0.0, ambiguous=False):
    return FaceResult(person_id, name, is_new, score, ambiguous)
```
y usar `recognizer.process_crop_scored.return_value = _face()` /
`_face(42, "David", score=0.8)` según el caso, cambiando también las aserciones de
`recognizer.process_crop.call_count` por `recognizer.process_crop_scored.call_count`
(líneas 65, 87) y el `side_effect` de la línea 130.
  </action>
  <verify>
    <automated>F:/Documentos/IA/Proyecto_Camara/.venv/Scripts/python.exe -m pytest tests/test_recognition_worker.py tests/test_architecture.py tests/test_workers_isolation.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "Fase 24" backend/pipeline/recognition.py` no contiene ya la frase "la revalidacion temporal llega en la Fase 24".
    - `grep -c "self._fsm\." backend/pipeline/recognition.py` devuelve >= 6 (`needs_recognition`, `on_active_tracks`, `on_tick`, `on_face_result`, `state_of`, `identity_of`).
    - `grep -n "process_crop_scored" backend/pipeline/recognition.py` encuentra la llamada.
    - `grep -n "face_inferences" backend/pipeline/recognition.py` aparece en `__init__`, en el bucle y en `stats`.
    - `grep -n "await\|async def" backend/pipeline/recognition.py` no devuelve nada (invariante: ningún hilo hace `await`).
    - `pytest tests/test_recognition_worker.py -q` sale con código 0.
    - `pytest tests/test_architecture.py -q` sale con código 0.
  </acceptance_criteria>
  <done>`RecognitionWorker` decide a quién inferir preguntando a la FSM, escribe `identity_state`, publica los eventos de identidad sin `await` y expone `face_inferences`; los 5 mocks del fichero de test están actualizados y verdes.</done>
</task>

<task type="auto">
  <name>Task 2: Cableado en manager.py y main.py, con la FSM fuera de la factoría</name>
  <read_first>
    - `backend/pipeline/manager.py` líneas 36-130 — firma de `CameraPipeline.__init__` (39-54, con `event_engine` en la 47 y `recognition_fps: float = 2.0` en la 53 como precedente de escalar con default), `_make_detection` (77-89, el único worker que ya recibe `event_engine`), `_make_streaming` (91-99, el precedente de estado rescatado entre reinicios) y `_make_recognition` (114-125).
    - `backend/main.py` líneas 296-310 (construcción de `PersonRecognizer` desde `settings`) y 405-435 (llamada a `camera_manager.add(...)` con `recognition_fps=settings.recognition_target_fps`).
    - `backend/config.py` — el bloque `# --- Identidad temporal (Fase 24 ...)` añadido en 24-01.
  </read_first>
  <action>
En `backend/pipeline/manager.py`:

1. Imports: `from backend.perception.face.identity import IdentityStateMachine, TemporalVoter`.

2. Ampliar la firma de `CameraPipeline.__init__` con 6 escalares nuevos, al lado de
   `recognition_fps` (línea 53):
   ```python
   identity_vote_window: int = 8,
   identity_min_votes: int = 3,
   identity_min_ratio: float = 0.6,
   identity_lost_ttl: float = 30.0,
   identity_revalidate_after: float = 120.0,
   identity_low_confidence: float = 0.55,
   ```

3. Declarar el atributo junto a los demás workers (línea ~73):
   `self.identity_fsm: IdentityStateMachine | None = None`.

4. Modificar el bloque de la línea 114-125. La FSM se construye **fuera** de
   `_make_recognition`, con un comentario que explique el porqué:
   ```python
   if recognizer is not None and getattr(recognizer, "available", False):
       # La FSM vive FUERA de la factoria: WorkerSupervisor la re-ejecuta en cada
       # reinicio del worker, y construirla dentro perderia toda la identidad ya
       # confirmada. Mismo motivo por el que _make_streaming rescata `clients`.
       self.identity_fsm = IdentityStateMachine(
           TemporalVoter(
               window=identity_vote_window,
               min_votes=identity_min_votes,
               min_ratio=identity_min_ratio,
           ),
           lost_ttl=identity_lost_ttl,
           revalidate_after=identity_revalidate_after,
           low_confidence=identity_low_confidence,
       )

       def _make_recognition() -> RecognitionWorker:
           self.recognition = RecognitionWorker(
               self.broker.subscribe("recognition", replace=True),
               self.registry, recognizer,
               AdaptiveRate(target_fps=recognition_fps,
                            min_fps=recognition_fps, max_fps=recognition_fps),
               identity_fsm=self.identity_fsm,
               event_engine=event_engine,
               on_identified=on_identified,
           )
           return self.recognition

       self.supervisor.register("recognition", _make_recognition)
   ```

En `backend/main.py`, en la llamada a `camera_manager.add(...)` (línea ~414-430),
después de `recognition_fps=settings.recognition_target_fps`, añadir:
```python
identity_vote_window=settings.identity_vote_window,
identity_min_votes=settings.identity_min_votes,
identity_min_ratio=settings.identity_min_ratio,
identity_lost_ttl=settings.identity_lost_ttl_secs,
identity_revalidate_after=settings.identity_revalidate_after_secs,
identity_low_confidence=settings.face_confirm_threshold,
```
`face_confirm_threshold` (0.55, Fase 23) se reutiliza como umbral de confianza de
identidad baja — es el parámetro que `recognizer.py:78` marcaba como "reserved for
Fase 24's TemporalVoter". No se importa `get_settings()` dentro de `backend/pipeline/`:
la configuración se inyecta desde `main.py`, como el resto.

En `tests/test_recognition_worker.py`, añadir `TEST_fsm_survives_worker_restart`:
construir un `CameraPipeline` con un `recognizer` `MagicMock` (`available=True`),
guardar `id(pipeline.identity_fsm)`, invocar dos veces la factoría registrada en el
supervisor (o `pipeline.supervisor` según su API real — leerla antes) y afirmar que
`pipeline.identity_fsm` es **el mismo objeto** mientras `pipeline.recognition` es uno
nuevo. Si construir el pipeline completo resulta caro en el test, basta con afirmar que
`RecognitionWorker` recibe la misma instancia de FSM en dos llamadas consecutivas a la
factoría.
  </action>
  <verify>
    <automated>F:/Documentos/IA/Proyecto_Camara/.venv/Scripts/python.exe -m pytest tests/test_recognition_worker.py tests/test_workers_isolation.py tests/test_config.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "self.identity_fsm = IdentityStateMachine" backend/pipeline/manager.py` encuentra la construcción, y está **antes** de `def _make_recognition`.
    - `grep -n "identity_fsm=self.identity_fsm" backend/pipeline/manager.py` encuentra el paso al worker.
    - `grep -n "event_engine=event_engine" backend/pipeline/manager.py` aparece 2 veces (detection y recognition).
    - `grep -c "identity_" backend/main.py` devuelve >= 6.
    - `grep -n "get_settings" backend/pipeline/manager.py` no devuelve nada.
    - `grep -n "def TEST_fsm_survives_worker_restart" tests/test_recognition_worker.py` encuentra el test.
    - `pytest tests/test_recognition_worker.py -k restart -q` sale con código 0.
    - `python -c "import backend.main"` no lanza (arranque de imports intacto).
  </acceptance_criteria>
  <done>La configuración llega de `Settings` al `TemporalVoter`/`IdentityStateMachine` por inyección, `RecognitionWorker` recibe el `event_engine` que hasta ahora no tenía, y un reinicio del worker no pierde la identidad confirmada.</done>
</task>

<task type="auto">
  <name>Task 3: Medir el criterio 6 — presupuesto de inferencias con track no confirmado</name>
  <read_first>
    - `tests/test_recognition_worker.py` líneas 18-90 — fixture `broker`, `_make_frame`, `_FakeTracked`, `_publish_for` y el patrón de aserción sobre `call_count`. Es literalmente el test del criterio 6 con otros números.
    - `24-RESEARCH.md` § "Pitfall 4: Medir el 70% sobre el baseline equivocado" — por qué el escenario debe ser un track NO confirmado.
    - `backend/pipeline/recognition.py` tras la Task 1 — `stats["face_inferences"]` y el parámetro `identity_fsm`.
  </read_first>
  <action>
Añadir a `tests/test_recognition_worker.py` (nombres `TEST_*`):

1. **`TEST_inference_budget_drops_on_unconfirmed_track`** — criterio 6. El escenario está
   fijado por decisión del usuario (D-01): **track estático que NUNCA llega a
   identificarse**. Es el caso patológico que FACE-11 arregla; sobre un track ya
   confirmado el filtro de la Fase 23 ya hacía 0 inferencias y esta fase *añade*
   revalidación, así que medir ahí no significa nada.

   Estructura: ejecutar **dos veces** el mismo worker sobre la misma carga, cambiando
   solo la presencia de la FSM.
   ```python
   # ─── Criterio 6: presupuesto de inferencias sobre un track no confirmado ─────
   # Escenario medido (24-CONTEXT.md, decision del usuario): UNA persona estatica
   # cuyo reconocimiento NUNCA tiene exito (cara no detectable / calidad
   # insuficiente / match ambiguo). En la Fase 23, _next_candidate la devolvia en
   # cada ciclo y se gastaban ~120 inferencias/min a 2 FPS, indefinidamente. Con la
   # FSM, tras llenar la ventana de votos sin ningun match el track entra en backoff
   # hasta que vence revalidate_after.
   #
   # Escalado del reloj: los 120 s reales de produccion se comprimen a `revalidate`
   # segundos de test manteniendo la proporcion ventana/backoff.
   # ─────────────────────────────────────────────────────────────────────────────
   def TEST_inference_budget_drops_on_unconfirmed_track(broker):
       def _run(fsm) -> int:
           registry = TrackRegistry()
           registry.update_from_detections(_FakeTracked([1]), now=time.monotonic() - 10)
           recognizer = MagicMock()
           recognizer.available = True
           recognizer.process_crop_scored.return_value = _face()   # nunca identifica
           sub = broker.subscribe("recognition")
           rate = AdaptiveRate(target_fps=20.0, min_fps=20.0, max_fps=20.0)
           worker = RecognitionWorker(sub, registry, recognizer, rate,
                                      min_track_age=0.0, identity_fsm=fsm)
           worker.start()
           _publish_for(broker, seconds=1.0)
           worker.stop()
           return recognizer.process_crop_scored.call_count

       baseline = _run(None)                                   # comportamiento Fase 23
       fsm = IdentityStateMachine(TemporalVoter(window=2, min_votes=2),
                                  lost_ttl=30.0, revalidate_after=10.0)
       with_fsm = _run(fsm)

       assert baseline >= 8, f"baseline degenerado ({baseline}): el test no mide nada"
       assert with_fsm <= baseline * 0.30, (
           f"reduccion insuficiente: {baseline} -> {with_fsm} "
           f"({100 * (1 - with_fsm / baseline):.0f}%, se exige >= 70%)"
       )
   ```
   Notas para el ejecutor:
   - Usar `broker.subscribe` con nombres distintos en cada `_run` si el broker exige
     unicidad, o crear un `FrameBroker` por ejecución.
   - Si el `baseline` sale por debajo de 8, subir `target_fps` o `seconds`; **no**
     relajar el umbral del 30%.
   - `window=2` con `revalidate_after=10.0` mantiene la misma forma que la producción
     (`window=8`, `revalidate_after=120.0`): unas pocas inferencias iniciales y luego
     backoff.

2. **`TEST_low_identity_confidence_retriggers_recognition`** (D-03): con un track
   CONFIRMED en la FSM cuya `confidence` está por debajo de `low_confidence`, el worker
   vuelve a hacerle inferencia dentro de la ventana de revalidación; con la confianza
   por encima, no. Puede construirse llamando a `fsm.on_face_result(...)` directamente
   para llegar al estado deseado antes de arrancar el worker.

3. **`TEST_confirmed_track_emits_person_recognized_once`** (FACE-09 end-to-end): worker
   con FSM y un `EventEngine` real sobre un `EventBus` (patrón `make_engine` de
   `tests/test_event_engine.py`), `process_crop_scored` devolviendo siempre
   `_face(42, "David", score=0.8)`; tras publicar frames durante ~1 s con
   `min_votes=3`, exactamente **un** evento `PERSON_RECOGNIZED` en la lista recibida,
   y `registry.snapshot()[1].identity_state == IdentityState.CONFIRMED`.
   Ojo: el bus necesita un event loop; si el test es síncrono, usar un `EventBus` con
   `loop` propio o marcar el test como `async def` (`asyncio_mode = auto`, sin decorador).
  </action>
  <verify>
    <automated>F:/Documentos/IA/Proyecto_Camara/.venv/Scripts/python.exe -m pytest tests/test_recognition_worker.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "def TEST_inference_budget_drops_on_unconfirmed_track" tests/test_recognition_worker.py` encuentra el test.
    - `grep -n "baseline \* 0.30" tests/test_recognition_worker.py` encuentra el umbral del 70%.
    - `grep -n "def TEST_low_identity_confidence_retriggers_recognition" tests/test_recognition_worker.py` encuentra el test.
    - `grep -n "def TEST_confirmed_track_emits_person_recognized_once" tests/test_recognition_worker.py` encuentra el test.
    - `pytest tests/test_recognition_worker.py -k inference_budget -q` sale con código 0.
    - `pytest tests/test_recognition_worker.py -q` sale con código 0.
    - El SUMMARY del plan recoge las dos cifras reales (`baseline` y `with_fsm`) y el escenario medido.
  </acceptance_criteria>
  <done>El criterio 6 está verificado por un test automatizado sobre el escenario que el usuario fijó (track no confirmado), con el baseline medido en la misma ejecución para que la comparación sea honesta.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Hilo `recognition-worker` → `EventBus` (event loop) | Cruce hilo/corrutina vía `publish_threadsafe` |
| `RecognitionWorker` → `TrackRegistry` | Escritura de estado compartido entre workers |
| Frames RTSP → inferencia facial | Entrada no confiable que consume CPU |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-17 | Denial of Service | `_next_candidate` | mitigate | `needs_recognition()` corta el reintento indefinido sobre tracks que nunca se identifican: es a la vez FACE-11 y la mitigación del agotamiento de CPU. Verificado por `TEST_inference_budget_drops_on_unconfirmed_track` |
| T-24-18 | Tampering | `TrackRegistry.identity_state` | mitigate | `RecognitionWorker` es el único escritor de `person_id`/`person_name`/`identity_state`; el invariante está escrito en el docstring de `TrackRegistry` y `tests/test_workers_isolation.py` protege el acoplamiento entre workers |
| T-24-19 | Information Disclosure | logs del worker | mitigate | Los `logger.exception` del bucle usan `track_id` (entero), nunca `person_name`. El nombre solo viaja dentro del `Event` (ASVS V8) |
| T-24-20 | Denial of Service | `_loop` | mitigate | Toda llamada nueva (FSM, `emit_identity`) va dentro de `try/except` con `logger.exception` + `continue`: el hilo del worker nunca muere por una excepción de identidad |
| T-24-21 | Repudiation | reinicio del supervisor | mitigate | La FSM se construye fuera de `_make_recognition`, así que un reinicio del worker no borra la identidad confirmada ni provoca un segundo `PERSON_RECOGNIZED` espurio |
</threat_model>

<verification>
```bash
F:/Documentos/IA/Proyecto_Camara/.venv/Scripts/python.exe -m pytest tests/test_recognition_worker.py tests/test_workers_isolation.py tests/test_architecture.py tests/test_track_registry.py tests/test_event_engine.py tests/test_detection_worker.py -q
```
Todos en verde. `test_architecture.py` confirma que el cableado nuevo no mete `await`
en el hilo del worker ni inferencia en una corrutina.
</verification>

<success_criteria>
- El gate de reconocimiento pregunta a `needs_recognition()` y ya no filtra por `person_id is None` cuando hay FSM.
- `stats["face_inferences"]` cuenta inferencias reales, no el FPS objetivo.
- Los eventos de identidad se publican vía `EventEngine.emit_identity` desde el hilo del worker, sin `await`.
- `identity_state` se escribe en el registry en cada resultado facial.
- La FSM se construye fuera de la factoría del supervisor y sobrevive a un reinicio del worker.
- El criterio 6 está medido en test con baseline en la misma ejecución (≥70% de reducción sobre un track NO confirmado).
- `pytest tests/test_recognition_worker.py tests/test_architecture.py -q` sale con código 0.
</success_criteria>

<output>
Al terminar, crear `.planning/phases/24-identidad-temporal-votaci-n-y-m-quina-de-estados/24-05-SUMMARY.md`,
documentando explícitamente el escenario del criterio 6 (D-01) y las cifras medidas.
</output>
