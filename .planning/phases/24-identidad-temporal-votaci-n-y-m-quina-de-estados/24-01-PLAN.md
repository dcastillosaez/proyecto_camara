---
phase: 24-identidad-temporal-votaci-n-y-m-quina-de-estados
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/config.py
  - backend/perception/face/identity.py
  - tests/test_config.py
  - tests/test_temporal_voting.py
autonomous: true
requirements: [FACE-07]
tags: [identity, voting, config, pydantic-settings]

must_haves:
  truths:
    - "Una identidad solo gana la votación si acumula min_votes coherentes dentro de la ventana y su proporción sobre el total de votos alcanza min_ratio"
    - "Con votos alternando dos identidades (A,B,A,B,...) el veredicto es (None, 0.0): ninguna identidad se impone"
    - "Los 5 parámetros de la fase (window, min_votes, min_ratio, lost_ttl, revalidate_after) se leen de Settings y no están hardcodeados"
    - "Una configuración imposible (min_votes > window, min_ratio fuera de (0,1]) aborta el arranque con mensaje claro"
  artifacts:
    - path: "backend/perception/face/identity.py"
      provides: "IdentityState (4 estados), IdentityTransition (dataclass), TemporalVoter"
      contains: "class TemporalVoter"
      min_lines: 90
    - path: "backend/config.py"
      provides: "identity_vote_window, identity_min_votes, identity_min_ratio, identity_lost_ttl_secs, identity_revalidate_after_secs"
      contains: "identity_vote_window"
    - path: "tests/test_temporal_voting.py"
      provides: "Cobertura de FACE-07 y del criterio 3 de la fase"
      contains: "def TEST_"
  key_links:
    - from: "backend/perception/face/identity.py"
      to: "collections.deque / collections.Counter"
      via: "ventana deslizante acotada por maxlen + recuento de mayoría"
      pattern: "deque\\(maxlen="
    - from: "tests/test_temporal_voting.py"
      to: "backend.perception.face.identity.TemporalVoter"
      via: "import directo, sin mocks (lógica pura)"
      pattern: "from backend.perception.face.identity import"
---

<objective>
Crear el módulo de dominio puro `backend/perception/face/identity.py` con el enum
`IdentityState`, el dataclass `IdentityTransition` y la clase `TemporalVoter`
(ventana deslizante de votos por track), más los 5 parámetros de configuración de
la fase en `backend/config.py`.

Purpose: FACE-07 exige que una identidad se confirme por evidencia acumulada, no
por un frame. `TemporalVoter` es la pieza que acumula esa evidencia y el criterio 3
de la fase (embeddings ruidosos alternando dos identidades → no confirma ninguna)
se decide íntegramente aquí.

Output: `identity.py` (sin `IdentityStateMachine` todavía — llega en 24-02),
`Settings` ampliado con validación de rangos, y `tests/test_temporal_voting.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/24-identidad-temporal-votaci-n-y-m-quina-de-estados/24-CONTEXT.md
@.planning/phases/24-identidad-temporal-votaci-n-y-m-quina-de-estados/24-PATTERNS.md
@CLAUDE.md

**Intérprete:** el worktree no tiene `.venv` propio. Usar siempre
`F:/Documentos/IA/Proyecto_Camara/.venv/Scripts/python.exe`.

**Convención de tests obligatoria:** `pytest.ini` fija `python_functions = TEST_*`.
Los tests nombrados `def test_*` NO se recogen en el CI (Linux, case-sensitive).
Toda función de test nueva DEBE llamarse `TEST_*`.

<interfaces>
<!-- Contratos que el ejecutor necesita. Extraídos del código real (commit 7262644). -->

Contrato locked de SPEC_v2.md §5.5 (NO cambiar las firmas públicas):
```python
class TemporalVoter:
    """Ventana deslizante de votos por track."""
    def __init__(self, window: int = 8, min_votes: int = 3,
                 min_ratio: float = 0.6): ...
    def vote(self, track_id: int, person_id: int | None, score: float) -> None: ...
    def verdict(self, track_id: int) -> tuple[int | None, float]:
        """(person_id ganador, confianza agregada) o (None, 0.0)"""
```

Patrón de enum del repo — `backend/events/types.py:13-17`:
```python
class Severity(str, Enum):
    INFO = "info"
    WARNING = "warning"
    CRITICAL = "critical"
```

Patrón de deque + Counter que esta fase sustituye — `backend/recognizer.py:204-210`:
```python
votes = self._votes.setdefault(tracker_id, deque(maxlen=self.VOTE_WINDOW))
votes.append(pid)
winner = Counter(votes).most_common(1)[0][0]
```

Patrón de prune por conjunto de activos — `backend/recognizer.py:340-351`
(copiar la forma del bucle, NO el `with self._lock:` — `identity.py` es de un solo hilo):
```python
def prune(self, active_tracker_ids: set[int]) -> None:
    for d in (self._cache, self._last_attempt, self._pending, self._votes):
        for tid in list(d):
            if tid not in active_tracker_ids:
                del d[tid]
```

Bloque de config de la Fase 23 al que hay que añadir el nuevo — `backend/config.py:128-135`:
```python
    # --- Reconocimiento facial ArcFace (Fase 23 — FACE-01..03) ---
    face_min_size_px: int = 60
    face_max_blur: float = 100.0
    face_max_yaw_deg: float = 40.0
    face_match_threshold: float = 0.45
    face_confirm_threshold: float = 0.55
```

Import actual de pydantic en `backend/config.py:6` (hay que ampliarlo):
```python
from pydantic import field_validator
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Añadir los 5 parámetros de identidad a Settings con validación de rangos</name>
  <read_first>
    - `backend/config.py` líneas 1-60 (imports, `_MODEL_PATH_ALLOWED_SUFFIXES`, el `@field_validator("yolo_model_path")` de la línea 39 como patrón de validador con mensaje claro) y 115-140 (bloques de Fase 21/22/23, donde se inserta el nuevo bloque).
    - `tests/test_config.py` completo (es corto) — para copiar el estilo de los tests de validación existentes.
  </read_first>
  <action>
En `backend/config.py`:

1. Ampliar el import de la línea 6 a:
   ```python
   from pydantic import field_validator, model_validator
   ```

2. Inmediatamente **después** de `face_confirm_threshold: float = 0.55` (línea 135), insertar:
   ```python
   # --- Identidad temporal (Fase 24 — FACE-07..FACE-11) ---
   # Defaults de SPEC_v2.md §5.5. `face_confirm_threshold` (arriba, Fase 23) se
   # reutiliza como umbral de "confianza de identidad baja": por debajo de el, un
   # track CONFIRMED vuelve a pasar por reconocimiento sin esperar a la
   # revalidacion periodica (FACE-11). Es la confianza agregada del
   # TemporalVoter, no la confianza de deteccion de YOLO.
   identity_vote_window: int = 8
   identity_min_votes: int = 3
   identity_min_ratio: float = 0.6
   identity_lost_ttl_secs: float = 30.0
   identity_revalidate_after_secs: float = 120.0
   ```

3. Añadir un validador cruzado justo después del bloque (o junto a los validadores
   existentes, al estilo del de la línea 39). Una configuración imposible debe abortar
   el arranque, no degradar en silencio (V5 de ASVS, ver `<threat_model>`):
   ```python
   @model_validator(mode="after")
   def validate_identity_params(self) -> "Settings":
       if self.identity_min_votes < 1:
           raise ValueError("identity_min_votes debe ser >= 1")
       if self.identity_vote_window < self.identity_min_votes:
           raise ValueError(
               f"identity_vote_window ({self.identity_vote_window}) no puede ser menor "
               f"que identity_min_votes ({self.identity_min_votes}): la votacion nunca "
               f"alcanzaria el minimo"
           )
       if not 0.0 < self.identity_min_ratio <= 1.0:
           raise ValueError("identity_min_ratio debe estar en (0, 1]")
       if self.identity_lost_ttl_secs <= 0:
           raise ValueError("identity_lost_ttl_secs debe ser > 0")
       if self.identity_revalidate_after_secs <= 0:
           raise ValueError("identity_revalidate_after_secs debe ser > 0")
       return self
   ```
   Ojo: el nombre del método NO debe empezar por `model_` ni chocar con campos.

En `tests/test_config.py`, añadir tests `TEST_*` (nombre en MAYÚSCULAS, obligatorio):
   - `TEST_identity_defaults_match_spec`: instanciar `Settings()` y afirmar
     `identity_vote_window == 8`, `identity_min_votes == 3`,
     `identity_min_ratio == 0.6`, `identity_lost_ttl_secs == 30.0`,
     `identity_revalidate_after_secs == 120.0`.
   - `TEST_identity_window_smaller_than_min_votes_rejected`: `pytest.raises(ValueError)`
     con `Settings(identity_vote_window=2, identity_min_votes=3)`.
   - `TEST_identity_ratio_out_of_range_rejected`: `pytest.raises(ValueError)` con
     `identity_min_ratio=0.0` y con `identity_min_ratio=1.5`.

   Si `Settings()` lee `.env` y eso interfiere, usar el mismo mecanismo que ya emplean
   los tests existentes de `tests/test_config.py` (leerlo antes de escribir).
  </action>
  <verify>
    <automated>F:/Documentos/IA/Proyecto_Camara/.venv/Scripts/python.exe -m pytest tests/test_config.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "identity_vote_window\|identity_min_votes\|identity_min_ratio\|identity_lost_ttl_secs\|identity_revalidate_after_secs" backend/config.py` devuelve >= 10 (5 definiciones + 5+ usos en el validador).
    - `grep -n "model_validator" backend/config.py` encuentra el import y el decorador.
    - `pytest tests/test_config.py -q` sale con código 0.
    - `grep -c "def TEST_identity" tests/test_config.py` devuelve 3.
    - `grep -n "def test_identity" tests/test_config.py` no devuelve nada (nombres en minúscula prohibidos).
  </acceptance_criteria>
  <done>`Settings` expone los 5 parámetros con los defaults del SPEC, rechaza configuraciones imposibles con `ValueError` y 3 tests `TEST_*` lo verifican.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Crear identity.py con IdentityState, IdentityTransition y TemporalVoter</name>
  <read_first>
    - `backend/perception/face/quality.py` líneas 1-48 — patrón exacto de cabecera de módulo (docstring que declara qué fija el SPEC y qué queda fuera), dataclass plano y clase con umbrales por constructor.
    - `backend/pipeline/rate.py` líneas 1-30 y 50-65 — `AdaptiveRate`: el módulo NO importa `time`, `should_process(now)` recibe el reloj por parámetro. Es el patrón de determinismo que `identity.py` debe seguir.
    - `backend/recognizer.py` líneas 196-215 — el bloque de votación por mayoría que esta fase reemplaza (deque + Counter).
    - `backend/events/types.py` líneas 13-17 — patrón de enum `str, Enum`.
    - `tests/test_recognizer_orchestration.py` líneas 1-20 y 60-80 — estilo de cabecera y separadores `─── título ───`.
  </read_first>
  <behavior>
Tests que deben existir en `tests/test_temporal_voting.py` (todos `TEST_*`):

  - `TEST_verdict_empty_track_returns_none`: `verdict(1)` sobre un track sin votos → `(None, 0.0)`.
  - `TEST_verdict_needs_min_votes`: con `min_votes=3`, tras 2 votos a la persona 7 → `(None, 0.0)`; tras el 3.º → `(7, score>0)`.
  - `TEST_verdict_aggregates_confidence`: 3 votos a la persona 7 con scores 0.6/0.8/0.7 → `verdict()[1] == pytest.approx(0.7)` (media de los scores del ganador).
  - `TEST_alternating_identities_have_no_winner` (**criterio 3 de la fase**): con
    `window=8, min_votes=3, min_ratio=0.6`, votar A,B,A,B,A,B,A,B → `verdict()` es
    `(None, 0.0)` porque `4/8 = 0.5 < 0.6`. Afirmar además `matched_votes(tid) == 8`.
  - `TEST_ratio_counts_unmatched_votes`: 3 votos a la persona 7 y 5 votos `None`
    (frames sin cara utilizable) → `3/8 = 0.375 < 0.6` → `(None, 0.0)`.
  - `TEST_window_is_sliding`: con `window=8`, votar 20 veces a la persona 7 →
    `len(voter._votes[tid]) == 8` (la cota la impone `maxlen`, no código propio).
  - `TEST_reset_clears_track`: tras `reset(tid)`, `verdict(tid) == (None, 0.0)` y
    `votes_for(tid) == 0`.
  - `TEST_prune_drops_inactive_tracks`: votar en los tracks 1..100, `prune({98, 99})`
    → `len(voter._votes) == 2`.
  </behavior>
  <action>
Crear `backend/perception/face/identity.py`. Módulo de **dominio puro**: no importa
`time`, no arranca hilos, no hace I/O, no conoce `Event` ni `camera_id`, no usa locks
(vive en un solo hilo, `RecognitionWorker._loop`).

Cabecera del módulo, al estilo de `quality.py:1-14`:
```python
"""IdentityState / IdentityTransition / TemporalVoter — identidad temporal (Fase 24).

SPEC_v2.md §5.5 fija el contrato de TemporalVoter (window/min_votes/min_ratio) y
los 4 estados de identidad. Este modulo es dominio puro: no importa `time`, no
arranca hilos, no hace I/O y no construye eventos. Todos los metodos que dependen
del reloj lo reciben como parametro `now: float` (monotonico), igual que
AdaptiveRate.should_process(now) en backend/pipeline/rate.py.

Fuera de alcance aqui: publicar eventos (lo hace EventEngine traduciendo
IdentityTransition), persistir el estado y la re-identificacion por apariencia
sin cara visible (Fase 25).
"""

from __future__ import annotations

from collections import Counter, deque
from dataclasses import dataclass
from enum import Enum
```

Contenido:

```python
class IdentityState(str, Enum):
    UNKNOWN = "UNKNOWN"
    CANDIDATE = "CANDIDATE"
    CONFIRMED = "CONFIRMED"
    TEMPORARILY_LOST = "TEMPORARILY_LOST"


@dataclass
class IdentityTransition:
    """Cambio de estado de identidad de un track.

    La FSM devuelve esto, NO un Event: perception/ no conoce camera_id ni el reloj
    de pared. EventEngine lo traduce a Event (o lo descarta si `emits` es False o si
    la transicion no tiene tipo de evento en el catalogo de SPEC_v2.md §6.1).
    """

    track_id: int
    from_state: IdentityState
    to_state: IdentityState
    person_id: int | None = None
    confidence: float = 0.0
    votes: int = 0
    window: int = 0
    emits: bool = True   # False = cambio de estado silencioso (misma visita)


class TemporalVoter:
    """Ventana deslizante de votos por track (SPEC_v2.md §5.5, FACE-07)."""

    def __init__(self, window: int = 8, min_votes: int = 3, min_ratio: float = 0.6) -> None:
        self._window = window
        self._min_votes = min_votes
        self._min_ratio = min_ratio
        # track_id -> ultimos `window` resultados (person_id, score). `None` es un
        # voto valido: significa "frame sin match" y penaliza el ratio, que es
        # justo lo que exige "evidencia coherente".
        self._votes: dict[int, deque[tuple[int | None, float]]] = {}

    @property
    def window(self) -> int: ...
    @property
    def min_votes(self) -> int: ...

    def vote(self, track_id: int, person_id: int | None, score: float) -> None:
        votes = self._votes.setdefault(track_id, deque(maxlen=self._window))
        votes.append((person_id, score))

    def verdict(self, track_id: int) -> tuple[int | None, float]:
        """(person_id ganador, confianza agregada) o (None, 0.0)."""
        votes = self._votes.get(track_id)
        if not votes:
            return None, 0.0
        matched = [pid for pid, _ in votes if pid is not None]
        if len(matched) < self._min_votes:
            return None, 0.0
        winner, count = Counter(matched).most_common(1)[0]
        if count < self._min_votes:
            return None, 0.0
        if count / len(votes) < self._min_ratio:
            return None, 0.0
        scores = [s for pid, s in votes if pid == winner]
        return winner, sum(scores) / len(scores)

    def votes_for(self, track_id: int) -> int:
        """Numero de votos acumulados en la ventana (con o sin match)."""

    def matched_votes(self, track_id: int) -> int:
        """Numero de votos CON match en la ventana. Distingue 'identidades en
        conflicto' (sigue habiendo caras) de 'se acabaron los matches'."""

    def reset(self, track_id: int) -> None:
        self._votes.pop(track_id, None)

    def prune(self, active_track_ids: set[int]) -> None:
        """ByteTrack nunca reutiliza track_ids: sin prune, _votes crece sin cota en
        un proceso 24/7 (invariante de la Fase 22). Sin lock: un solo hilo."""
        for tid in list(self._votes):
            if tid not in active_track_ids:
                del self._votes[tid]
```

Detalles no negociables:
- El denominador del ratio es `len(votes)` (todos los votos de la ventana, incluidos
  los `None`), no `len(matched)`. Con esto, A,B,A,B,A,B,A,B da `4/8 = 0.5 < 0.6` y el
  criterio 3 se cumple.
- La confianza agregada es la **media de los scores del ganador**, no el máximo: un
  único frame afortunado no debe inflarla (es literalmente el problema que la fase cierra).
- Nada de `threading.RLock` (PATTERNS § "No Analog Found": el lock de `tracking.py`
  NO se copia aquí).

Escribir `tests/test_temporal_voting.py` con los 8 tests del bloque `<behavior>`,
todos `TEST_*`, con cabecera de módulo explicando que la clase es pura y no necesita
mocks, y separadores `# ─── titulo ───`.
  </action>
  <verify>
    <automated>F:/Documentos/IA/Proyecto_Camara/.venv/Scripts/python.exe -m pytest tests/test_temporal_voting.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `backend/perception/face/identity.py` existe y contiene `class IdentityState(str, Enum)`, `class IdentityTransition`, `class TemporalVoter`.
    - `grep -n "^import time\|^from time\|import threading" backend/perception/face/identity.py` no devuelve nada (módulo puro, sin reloj propio ni locks).
    - `grep -n "deque(maxlen=self._window)" backend/perception/face/identity.py` encuentra 1 línea.
    - `grep -c "def TEST_" tests/test_temporal_voting.py` devuelve >= 8.
    - `grep -n "def test_" tests/test_temporal_voting.py` no devuelve nada.
    - `pytest tests/test_temporal_voting.py -q` sale con código 0 y recoge >= 8 tests.
    - `pytest tests/test_temporal_voting.py -k alternating -q` sale con código 0 (criterio 3).
    - `pytest tests/test_architecture.py -q` sigue en verde.
  </acceptance_criteria>
  <done>`TemporalVoter` acumula, agrega y poda votos con los parámetros de SPEC §5.5; el criterio 3 (identidades alternadas sin ganador) está probado; `identity.py` no importa `time` ni `threading`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `.env` / entorno → `Settings` | Valores de configuración no confiables cruzan aquí al arrancar el proceso |
| Resultado de inferencia → `TemporalVoter` | Los `person_id`/`score` que entran a votar provienen del pipeline de visión |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-01 | Tampering | `Settings.identity_*` | mitigate | `@model_validator(mode="after")` rechaza `min_votes > window`, `min_ratio` fuera de `(0,1]` y TTLs no positivos: una config imposible aborta el arranque en vez de dejar la FSM sin confirmar nunca (ASVS V5) |
| T-24-02 | Denial of Service | `TemporalVoter._votes` | mitigate | `deque(maxlen=window)` acota cada track y `prune(active_track_ids)` elimina los tracks muertos; ByteTrack nunca reutiliza ids (invariante de la Fase 22). Test de cota en 24-03 |
| T-24-03 | Spoofing | `TemporalVoter.verdict` | mitigate | `min_votes` + `min_ratio` sobre el total de votos (incluidos los `None`) impiden que un solo frame afortunado asigne identidad |
| T-24-04 | Information Disclosure | `identity.py` | accept | El módulo maneja `person_id` (entero), nunca nombres, y no escribe logs. No hay superficie de fuga en esta capa |
</threat_model>

<verification>
```bash
F:/Documentos/IA/Proyecto_Camara/.venv/Scripts/python.exe -m pytest tests/test_temporal_voting.py tests/test_config.py tests/test_architecture.py -q
```
Los tres deben salir en verde. `test_architecture.py` confirma que `identity.py` no
introduce `await` en hilos ni inferencia en corrutinas.
</verification>

<success_criteria>
- `backend/perception/face/identity.py` existe con `IdentityState`, `IdentityTransition` y `TemporalVoter`, sin importar `time` ni `threading`.
- `Settings` expone los 5 parámetros de SPEC §5.5 con validación de rangos.
- `pytest tests/test_temporal_voting.py tests/test_config.py -q` sale con código 0.
- El criterio 3 de la fase (identidades alternadas → sin confirmación) está cubierto por un test automatizado.
- Todas las funciones de test nuevas se llaman `TEST_*`.
</success_criteria>

<output>
Al terminar, crear `.planning/phases/24-identidad-temporal-votaci-n-y-m-quina-de-estados/24-01-SUMMARY.md`.
</output>
