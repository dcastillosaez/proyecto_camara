---
phase: 24-identidad-temporal-votaci-n-y-m-quina-de-estados
plan: 06
type: execute
wave: 5
depends_on: ["24-01", "24-02", "24-03", "24-04", "24-05"]
files_modified:
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [FACE-07, FACE-08, FACE-09, FACE-10, FACE-11]
tags: [phase-gate, regression, measurement]

must_haves:
  truths:
    - "La suite completa está verde: la fase toca pipeline, eventos y configuración, y CLAUDE.md lo exige"
    - "Los 6 criterios de éxito del ROADMAP tienen un comando automatizado que los demuestra"
    - "El escenario exacto en el que se midió el criterio 6 está documentado, no solo el porcentaje"
    - "FACE-07..FACE-11 quedan marcados como completos en REQUIREMENTS.md"
  artifacts:
    - path: ".planning/phases/24-identidad-temporal-votaci-n-y-m-quina-de-estados/24-06-SUMMARY.md"
      provides: "Tabla criterio → comando → resultado, y el escenario del criterio 6"
    - path: ".planning/REQUIREMENTS.md"
      provides: "FACE-07..FACE-11 marcados [x]"
      contains: "FACE-11"
  key_links:
    - from: "24-06-SUMMARY.md"
      to: "los 6 criterios de éxito de ROADMAP § Phase 24"
      via: "tabla de trazabilidad criterio → test → resultado"
      pattern: "criterio"
---

<objective>
Puerta de fase: dejar la suite completa verde y producir la evidencia de que los 6
criterios de éxito del ROADMAP se cumplen, con el comando concreto que lo demuestra
para cada uno.

Purpose: la fase toca `backend/pipeline/`, `backend/events/`, `backend/config.py` y
`backend/main.py`. CLAUDE.md § Tests exige la suite entera en ese caso, no solo los
ficheros afectados. Y la decisión D-01 del usuario obliga a documentar el escenario en
el que se midió el criterio 6, porque el porcentaje solo, sin escenario, no significa
nada.

Output: suite completa verde y `24-06-SUMMARY.md` con la trazabilidad criterio→comando.
Este plan **no** añade funcionalidad; si aparece una regresión, se arregla en el fichero
que la causa (en la wave 5 no hay planes en paralelo, así que no hay conflicto de
propiedad).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/24-identidad-temporal-votaci-n-y-m-quina-de-estados/24-CONTEXT.md
@.planning/phases/24-identidad-temporal-votaci-n-y-m-quina-de-estados/24-VALIDATION.md
@.planning/phases/24-identidad-temporal-votaci-n-y-m-quina-de-estados/24-05-SUMMARY.md
@CLAUDE.md

**Intérprete:** `F:/Documentos/IA/Proyecto_Camara/.venv/Scripts/python.exe`.
**Línea base de la suite antes de la fase:** 326/326 (`.planning/STATE.md`).

<criterios_verbatim>
Los 6 criterios de éxito de `ROADMAP.md § Phase 24`:

1. Los 4 estados (UNKNOWN, CANDIDATE, CONFIRMED, TEMPORARILY_LOST) y sus transiciones
   están testeados uno a uno.
2. Una secuencia de 200 frames de una persona conocida emite exactamente un
   PERSON_RECOGNIZED.
3. Con embeddings ruidosos alternando dos identidades, el track permanece en CANDIDATE
   y no confirma ninguna.
4. Cero identidades duplicadas tras pérdida y recuperación de track.
5. La revalidación tras 120 s funciona y tres fallos consecutivos emiten IDENTITY_LOST.
6. Las inferencias faciales por minuto con una persona estática bajan al menos un 70%
   respecto a la Phase 23.
</criterios_verbatim>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Suite completa verde y trazabilidad de los 6 criterios</name>
  <read_first>
    - `.planning/phases/24-identidad-temporal-votaci-n-y-m-quina-de-estados/24-VALIDATION.md` § "Per-Task Verification Map" — el mapa requisito→comando que este plan cierra.
    - Los SUMMARY de 24-01 a 24-05 — para recoger las cifras y nombres de test reales, no los previstos.
  </read_first>
  <action>
1. Ejecutar la suite completa:
   ```bash
   F:/Documentos/IA/Proyecto_Camara/.venv/Scripts/python.exe -m pytest tests/ -q
   ```
   (~90 s). Si hay fallos, arreglarlos en el fichero que los causa. **No** ocultar
   fallos ni marcar tests como `skip`/`xfail` para pasar la puerta.

2. Verificar que los tests nuevos se recogerían en el CI Linux. `pytest.ini` fija
   `python_functions = TEST_*` y en Windows el emparejamiento es insensible a
   mayúsculas, así que un `def test_*` nuevo pasaría desapercibido localmente y
   **no se ejecutaría en el CI**. Comprobar con:
   ```bash
   grep -n "^def test_\|^async def test_" tests/test_temporal_voting.py \
       tests/test_identity_state_machine.py
   ```
   No debe devolver nada. Y en los ficheros extendidos, comprobar que las funciones
   **añadidas por esta fase** están en `TEST_*` (las preexistentes de
   `tests/test_recognition_worker.py` en minúscula no se renombran en esta fase).

3. Ejecutar uno a uno los comandos de cada criterio y anotar el resultado:
   | # | Criterio | Comando |
   |---|----------|---------|
   | 1 | 4 estados y 6 transiciones, una por test | `pytest tests/test_identity_state_machine.py -q` |
   | 2 | 200 frames → un solo PERSON_RECOGNIZED | `pytest tests/test_identity_state_machine.py -k single_recognition -q` |
   | 3 | Identidades alternadas → sigue en CANDIDATE | `pytest tests/test_temporal_voting.py tests/test_identity_state_machine.py -k alternating -q` |
   | 4 | Cero identidades duplicadas tras pérdida/recuperación | `pytest tests/test_identity_state_machine.py -k track_recovery -q` |
   | 5 | Revalidación a 120 s y 3 ciclos fallidos → IDENTITY_LOST | `pytest tests/test_identity_state_machine.py -k revalidat -q` |
   | 6 | ≥70% menos inferencias con track no confirmado | `pytest tests/test_recognition_worker.py -k inference_budget -q` |

   Si algún `-k` no selecciona ningún test (pytest sale con código 5), corregir el
   patrón o el nombre del test — un criterio sin comando que lo seleccione no está
   verificado.

4. Marcar en `.planning/REQUIREMENTS.md` § "Reconocimiento facial (FACE)" los requisitos
   FACE-07, FACE-08, FACE-09, FACE-10 y FACE-11 como `- [x]`, respetando el texto exacto
   de cada uno (líneas 216-220). No tocar ningún otro requisito.
  </action>
  <verify>
    <automated>F:/Documentos/IA/Proyecto_Camara/.venv/Scripts/python.exe -m pytest tests/ -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/ -q` sale con código 0 y el número de tests recogidos es >= 326 + los añadidos por la fase.
    - `pytest tests/ -q` no reporta ningún `skipped` ni `xfailed` nuevo respecto a la línea base.
    - `grep -n "^def test_\|^async def test_" tests/test_temporal_voting.py tests/test_identity_state_machine.py` no devuelve nada.
    - Los 6 comandos de la tabla salen con código 0 (ninguno con código 5 = "no tests collected").
    - `grep -n "FACE-07\|FACE-08\|FACE-09\|FACE-10\|FACE-11" .planning/REQUIREMENTS.md` muestra los 5 con `- [x]`.
  </acceptance_criteria>
  <done>Suite completa verde, cada criterio de éxito tiene un comando que lo selecciona y lo prueba, y FACE-07..FACE-11 están marcados como completos.</done>
</task>

<task type="auto">
  <name>Task 2: Documentar el escenario del criterio 6 y las consecuencias abiertas</name>
  <read_first>
    - `.planning/phases/24-identidad-temporal-votaci-n-y-m-quina-de-estados/24-05-SUMMARY.md` — las cifras `baseline` / `with_fsm` reales medidas.
    - `24-CONTEXT.md` § "Decisiones resueltas tras el research" — el texto de D-01, que exige documentar el escenario.
    - `24-RESEARCH.md` § "Mismatch 5" — por qué `face_fps` del sampler no sirve para medir esto.
  </read_first>
  <action>
Escribir en `24-06-SUMMARY.md` (además de lo que pida la plantilla de SUMMARY):

1. **Tabla de trazabilidad** con las 6 filas: criterio verbatim, comando ejecutado,
   nombre del test que lo cubre, resultado.

2. **Sección "Criterio 6 — escenario medido"**, exigida por D-01. Debe decir, sin
   ambigüedad:
   - Qué escenario se midió: **un track estático que nunca llega a identificarse**
     (cara no detectable, calidad insuficiente o match ambiguo). Es el caso patológico
     de la Fase 23: `_next_candidate` lo devolvía en cada ciclo y se gastaban
     ~120 inferencias/min a 2 FPS, indefinidamente.
   - Qué escenario **no** se midió y por qué: sobre un track ya confirmado, el filtro
     `person_id is None` de la Fase 23 ya hacía 0 inferencias; medir ahí daría 0% o
     negativo, porque esta fase **añade** la revalidación cada 120 s.
   - El coste nuevo aceptado: en régimen permanente, un track CONFIRMED consume
     0,5 inferencias/min (una cada `revalidate_after=120 s`) donde antes consumía 0.
     Es el precio de poder detectar que la identidad dejó de ser correcta.
   - Las cifras reales de la ejecución: `baseline`, `with_fsm` y el porcentaje.
   - Con qué se midió: el contador `RecognitionWorker.stats["face_inferences"]` /
     `process_crop_scored.call_count`, **no** la métrica `face_fps` del sampler, que
     viene de `AdaptiveRate.stats` y marca el FPS objetivo aunque no se ejecute ni una
     inferencia.

3. **Sección "Consecuencias abiertas"** — anotar sin actuar sobre ellas:
   - `config/rules.yaml` tiene la regla `persona_desconocida` basada en
     `event: LINE_CROSSED` + `person: unknown`. Ahora que `UNKNOWN_PERSON` se emite de
     verdad, añadir una regla para ese tipo sin retirar la otra duplicaría
     notificaciones.
   - La columna `tracks.identity_state` (`backend/storage/models.py:78`) sigue sin
     escribirse: la fase produce el valor en `TrackRegistry`, pero persistirlo quedó
     explícitamente fuera de alcance.
   - `should_attempt`, `identify_or_register`, `REVERIFY_INTERVAL` y `get_cached` de
     `backend/recognizer.py` siguen sin llamadas en producción (solo los usan
     `tests/test_phase9.py` y `tests/test_recognizer_orchestration.py`). Su retirada
     quedó fuera del alcance de la Fase 24.
   - `.github/workflows/tests.yml` tiene `continue-on-error: true` y `pytest.ini` fija
     `python_functions = TEST_*`: los ~69 tests del repo escritos en minúscula no se
     ejecutan en el CI Linux y un fallo no bloquea el check del PR. Arreglar ese
     desajuste es trabajo aparte.
   - Si la fase ha dejado algún checkpoint de cámara real pendiente, enumerarlo. En
     principio **ninguno**: `24-VALIDATION.md` § "Manual-Only Verifications" está vacío
     porque todos los criterios son verificables con tests sintéticos.
  </action>
  <verify>
    <automated>F:/Documentos/IA/Proyecto_Camara/.venv/Scripts/python.exe -m pytest tests/test_temporal_voting.py tests/test_identity_state_machine.py tests/test_recognition_worker.py tests/test_event_engine.py tests/test_track_registry.py tests/test_memory_bounds.py tests/test_recognizer_orchestration.py tests/test_architecture.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `24-06-SUMMARY.md` existe y contiene una tabla con las 6 filas de criterios y su comando.
    - `grep -c "criterio" 24-06-SUMMARY.md` devuelve >= 6.
    - `grep -n "no confirmado\|NO confirmado" 24-06-SUMMARY.md` encuentra la descripción del escenario del criterio 6.
    - `grep -n "face_inferences\|process_crop_scored.call_count" 24-06-SUMMARY.md` documenta con qué se midió.
    - `grep -n "persona_desconocida" 24-06-SUMMARY.md` recoge la consecuencia abierta sobre `config/rules.yaml`.
    - El comando de verificación sale con código 0.
  </acceptance_criteria>
  <done>El SUMMARY deja escrito qué se midió exactamente, con qué contador, qué coste nuevo se acepta y qué queda abierto — de modo que el criterio 6 sea reproducible y auditable sin volver a leer los planes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI (Ubuntu) → suite de tests | Los tests que no se recogen dan una falsa sensación de cobertura |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-22 | Repudiation | cobertura del CI | mitigate | Se verifica con grep que ninguna función de test nueva usa `def test_*`: en Linux `python_functions = TEST_*` es sensible a mayúsculas y esos tests no se recogerían |
| T-24-23 | Tampering | puerta de fase | mitigate | Prohibido marcar `skip`/`xfail` para pasar la puerta; el criterio de aceptación compara el recuento de saltados contra la línea base de 326 tests |
| T-24-24 | Information Disclosure | documentación de la fase | accept | El SUMMARY registra `person_id` sintéticos de test, nunca identidades reales de `data/persons.db` |
</threat_model>

<verification>
```bash
F:/Documentos/IA/Proyecto_Camara/.venv/Scripts/python.exe -m pytest tests/ -q
```
Código de salida 0, sin saltados nuevos, con al menos los 326 tests de la línea base más
los añadidos por la fase.
</verification>

<success_criteria>
- `pytest tests/ -q` sale con código 0.
- Los 6 criterios de éxito del ROADMAP tienen un comando que los selecciona y pasa.
- Ninguna función de test nueva se llama `test_*` (se perdería en el CI Linux).
- El escenario del criterio 6, el contador usado y el coste nuevo aceptado están documentados en el SUMMARY (D-01).
- FACE-07..FACE-11 marcados `[x]` en `.planning/REQUIREMENTS.md`.
- Las consecuencias abiertas (regla `persona_desconocida`, columna `identity_state` sin persistir, código muerto de `recognizer.py`, desajuste del CI) quedan anotadas.
</success_criteria>

<output>
Al terminar, crear `.planning/phases/24-identidad-temporal-votaci-n-y-m-quina-de-estados/24-06-SUMMARY.md`.
</output>
