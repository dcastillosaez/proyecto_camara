---
phase: 24-identidad-temporal-votaci-n-y-m-quina-de-estados
plan: 04
type: execute
wave: 3
depends_on: ["24-02"]
files_modified:
  - backend/pipeline/tracking.py
  - backend/events/engine.py
  - tests/test_track_registry.py
  - tests/test_event_engine.py
autonomous: true
requirements: [FACE-08, FACE-09]
tags: [tracking, events, event-engine, shared-state]

must_haves:
  truths:
    - "Cada track del TrackRegistry expone su estado de identidad explícito, legible por streaming, sampler y API"
    - "El EventEngine sabe emitir PERSON_RECOGNIZED, UNKNOWN_PERSON e IDENTITY_LOST desde una IdentityTransition"
    - "Una transición marcada como silenciosa (misma visita) no publica ningún evento"
    - "UNKNOWN_PERSON conserva su severidad WARNING por defecto del catálogo"
  artifacts:
    - path: "backend/pipeline/tracking.py"
      provides: "TrackState.identity_state + TrackRegistry.set_identity_state"
      contains: "identity_state"
    - path: "backend/events/engine.py"
      provides: "EventEngine.emit_identity(transition, ...)"
      contains: "def emit_identity"
    - path: "tests/test_event_engine.py"
      provides: "Los 3 eventos de identidad con su payload, sobre un EventBus real"
      contains: "TEST_identity"
  key_links:
    - from: "backend/events/engine.py::emit_identity"
      to: "backend/events/engine.py::_publish"
      via: "delegación — no se construye Event a mano"
      pattern: "self\\._publish\\("
    - from: "backend/pipeline/tracking.py"
      to: "backend.perception.face.identity.IdentityState"
      via: "import del enum para tipar el campo del dataclass"
      pattern: "from backend.perception.face.identity import"
---

<objective>
Dar salida al resultado de la máquina de estados: `TrackState` gana el campo
`identity_state` (FACE-08) y `EventEngine` gana `emit_identity()`, que traduce una
`IdentityTransition` a uno de los tres tipos de evento del catálogo (FACE-09).

Purpose: hoy `EventType.PERSON_RECOGNIZED`, `UNKNOWN_PERSON` e `IDENTITY_LOST` existen
en `events/types.py` desde la Fase 19 pero **ningún código los emite** (verificado por
grep en el RESEARCH). Y la columna `tracks.identity_state` existe en el esquema desde la
Fase 19 sin que nadie produzca el valor. Este plan cierra las dos puntas sueltas.

Output: campo + setter en el registry, método de emisión en el EventEngine, y sus tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/24-identidad-temporal-votaci-n-y-m-quina-de-estados/24-PATTERNS.md
@.planning/phases/24-identidad-temporal-votaci-n-y-m-quina-de-estados/24-02-SUMMARY.md
@CLAUDE.md

**Intérprete:** `F:/Documentos/IA/Proyecto_Camara/.venv/Scripts/python.exe`.
**Convención de tests:** funciones `TEST_*` obligatorio.

<interfaces>
Disponible desde 24-02 (`backend/perception/face/identity.py`):
```python
class IdentityState(str, Enum):
    UNKNOWN = "UNKNOWN"; CANDIDATE = "CANDIDATE"
    CONFIRMED = "CONFIRMED"; TEMPORARILY_LOST = "TEMPORARILY_LOST"

@dataclass
class IdentityTransition:
    track_id: int
    from_state: IdentityState
    to_state: IdentityState
    person_id: int | None = None
    confidence: float = 0.0
    votes: int = 0
    window: int = 0
    emits: bool = True
```

Campo a añadir en `backend/pipeline/tracking.py:16-29` (junto a `person_name`):
```python
@dataclass
class TrackState:
    track_id: int
    first_seen: float
    last_seen: float
    bbox: tuple[int, int, int, int]
    confidence: float
    centroid_history: deque = field(default_factory=lambda: deque(maxlen=150))
    zones: set[str] = field(default_factory=set)
    zone_entry_times: dict[str, float] = field(default_factory=dict)
    person_id: int | None = None
    person_name: str | None = None
```

Setter análogo a copiar — `backend/pipeline/tracking.py:91-96`:
```python
def set_identity(self, track_id: int, person_id: int, name: str | None) -> None:
    with self._lock:
        ts = self._tracks.get(track_id)
        if ts is not None:
            ts.person_id = person_id
            ts.person_name = name
```

Publicador a delegar — `backend/events/engine.py:45-65`:
```python
def _publish(self, event_type, ts, captured_at=None, processed_at=None, **fields) -> None:
    payload = dict(fields.pop("payload", None) or {})
    payload["_emitted_at"] = time.monotonic()
    if captured_at is not None:
        payload["_captured_at"] = captured_at
    event = Event(type=event_type, camera_id=self._camera_id, ts=ts, payload=payload, **fields)
    self._bus.publish_threadsafe(event)
    ...
```

Campos disponibles en `Event` sin inventar nada — `backend/events/types.py:60-74`:
`track_id`, `person_id`, `person_name`, `confidence`, `bbox`, `payload`.

Severidades por defecto ya asignadas — `backend/events/types.py:49-57`:
`UNKNOWN_PERSON → WARNING`, el resto `INFO`.
</interfaces>

<hard_constraints>
- **NO pasar `severity=` en `emit_identity`.** El validador `_apply_default_severity`
  (`types.py:76-80`) solo actúa si `"severity" not in model_fields_set`; pasarlo
  explícitamente pierde el `WARNING` por defecto de `UNKNOWN_PERSON`.
- **NO construir `Event(...)` a mano** en `emit_identity`: delegar en `_publish`, que es
  quien añade `_emitted_at`, `_captured_at`, `camera_id` y llama a `publish_threadsafe`.
- **Único escritor.** El docstring de `TrackRegistry` (`tracking.py:33-39`) prohíbe que
  dos workers escriban el mismo campo. `identity_state` lo escribe solo
  `RecognitionWorker`, igual que `person_id`/`person_name`. Documentarlo en el docstring.
</hard_constraints>
</context>

<tasks>

<task type="auto">
  <name>Task 1: identity_state en TrackState y set_identity_state en TrackRegistry</name>
  <read_first>
    - `backend/pipeline/tracking.py` completo (106 líneas) — dataclass, docstring de invariante de escritores, `set_identity`, `prune`.
    - `tests/test_track_registry.py` completo — estilo y helpers existentes (`_fake_tracked`).
    - `backend/pipeline/streaming.py` y `backend/observability/sampler.py` líneas 100-125 — comprobar con grep si alguno itera los campos de `TrackState` de forma que un campo nuevo rompa algo.
  </read_first>
  <action>
En `backend/pipeline/tracking.py`:

1. Añadir el import del enum (dirección `pipeline → perception`, que ya existe vía
   `recognition.py → recognizer.py → perception/face/*` y que ningún test de
   arquitectura prohíbe):
   ```python
   from backend.perception.face.identity import IdentityState
   ```
2. Añadir el campo al final de `TrackState`, después de `person_name`:
   ```python
   # Estado de identidad de la FSM (Fase 24, FACE-08). Escritor unico:
   # RecognitionWorker, igual que person_id/person_name.
   identity_state: IdentityState = IdentityState.UNKNOWN
   ```
3. Añadir el setter justo después de `set_identity` (línea 96), con el mismo lock:
   ```python
   def set_identity_state(self, track_id: int, state: IdentityState) -> None:
       with self._lock:
           ts = self._tracks.get(track_id)
           if ts is not None:
               ts.identity_state = state
   ```
4. Ampliar el docstring de la clase (líneas 33-39) para incluir `identity_state` en la
   lista de campos cuyo escritor único es `RecognitionWorker`.

Comprobar con grep que ningún consumidor de `snapshot()` se rompe por el campo nuevo
(`grep -rn "snapshot()" backend/`). `IdentityState` hereda de `str`, así que serializa
como cadena en JSON sin conversión adicional.

En `tests/test_track_registry.py`, añadir (nombres `TEST_*`):
   - `TEST_identity_state_defaults_to_unknown`: tras `update_from_detections`, el track
     nuevo tiene `identity_state is IdentityState.UNKNOWN`.
   - `TEST_set_identity_state_updates_track`: `set_identity_state(1, IdentityState.CONFIRMED)`
     → `snapshot()[1].identity_state is IdentityState.CONFIRMED`.
   - `TEST_set_identity_state_on_missing_track_is_noop`: llamar con un `track_id`
     inexistente no lanza y no crea la entrada.
   - `TEST_identity_state_serialises_as_string`: `str(IdentityState.CONFIRMED) `/
     `IdentityState.CONFIRMED == "CONFIRMED"` es `True` (el enum hereda de `str`).
  </action>
  <verify>
    <automated>F:/Documentos/IA/Proyecto_Camara/.venv/Scripts/python.exe -m pytest tests/test_track_registry.py tests/test_streaming_worker.py tests/test_metrics_sampler.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "identity_state" backend/pipeline/tracking.py` devuelve >= 3 líneas (campo, setter, docstring).
    - `grep -n "def set_identity_state" backend/pipeline/tracking.py` encuentra el setter.
    - `grep -n "from backend.perception.face.identity import IdentityState" backend/pipeline/tracking.py` encuentra el import.
    - `grep -c "def TEST_identity_state\|def TEST_set_identity_state" tests/test_track_registry.py` devuelve 4.
    - `pytest tests/test_track_registry.py -k identity_state -q` sale con código 0.
    - `pytest tests/test_streaming_worker.py tests/test_metrics_sampler.py -q` sale con código 0 (el campo nuevo no rompe a los lectores).
    - `pytest tests/test_architecture.py -q` sale con código 0.
  </acceptance_criteria>
  <done>FACE-08 tiene soporte en el estado compartido: cada track expone su `identity_state`, con escritor único y sin romper a los lectores existentes.</done>
</task>

<task type="auto">
  <name>Task 2: EventEngine.emit_identity — los 3 eventos de identidad</name>
  <read_first>
    - `backend/events/engine.py` completo (190 líneas) — `_publish` (45-65), `emit_line_crossing` (71-84) como plantilla de método público, y `camera_offline`/`camera_recovered` (143-153) como patrón de emisión solo en transición.
    - `backend/events/types.py` líneas 19-80 — catálogo `EventType`, `DEFAULT_SEVERITY` y campos de `Event`.
    - `tests/test_event_engine.py` líneas 1-70 — helpers `wait_until` y `make_engine`, y `TEST_line_crossing_emits_event` como plantilla.
  </read_first>
  <action>
En `backend/events/engine.py`:

1. Importar el enum y el dataclass (import normal; `perception/face/identity.py` no
   importa nada de `backend/`, así que no hay ciclo):
   ```python
   from backend.perception.face.identity import IdentityState, IdentityTransition
   ```

2. Añadir el traductor y el método público, en una sección nueva con separador al estilo
   del fichero (`# --- Identidad (Fase 24) ---`):
   ```python
   @staticmethod
   def _identity_event_type(transition: IdentityTransition) -> EventType | None:
       """Traduce una transicion de la FSM al catalogo de SPEC_v2.md §6.1.

       No hay tipo de evento para CANDIDATE ni para TEMPORARILY_LOST: son estados
       intermedios que la UI leera del TrackRegistry (bloque C), no eventos.
       """
       if transition.to_state is IdentityState.CONFIRMED:
           return EventType.PERSON_RECOGNIZED
       if transition.to_state is IdentityState.UNKNOWN:
           if transition.from_state is IdentityState.CANDIDATE:
               return EventType.UNKNOWN_PERSON
           if transition.from_state in (IdentityState.CONFIRMED,
                                        IdentityState.TEMPORARILY_LOST):
               return EventType.IDENTITY_LOST
       return None

   def emit_identity(
       self,
       transition: IdentityTransition,
       now: datetime.datetime,
       person_name: str | None = None,
       bbox: tuple[int, int, int, int] | None = None,
       captured_at: float | None = None,
       processed_at: float | None = None,
   ) -> None:
       """Publica el evento de identidad correspondiente a *transition*, si lo hay.

       FACE-09: una visita genera un unico evento de reconocimiento. La guarda de
       idempotencia vive en la FSM, que marca `emits=False` cuando el cambio de estado
       es continuacion de la misma visita (recuperacion de un track dentro de
       lost_ttl) o cuando el evento ya se emitio para ese track.
       """
       if not transition.emits:
           return
       event_type = self._identity_event_type(transition)
       if event_type is None:
           return
       self._publish(
           event_type,
           ts=now,
           captured_at=captured_at,
           processed_at=processed_at,
           track_id=transition.track_id,
           person_id=transition.person_id,
           person_name=person_name,
           confidence=transition.confidence or None,
           bbox=bbox,
           payload={
               "state": transition.to_state.value,
               "previous_state": transition.from_state.value,
               "votes": transition.votes,
               "window": transition.window,
           },
       )
   ```
   No pasar `severity=`: `UNKNOWN_PERSON` debe conservar su `WARNING` por defecto.

En `tests/test_event_engine.py`, reutilizando `make_engine()` y `wait_until()`
(`asyncio_mode = auto`, sin decorador), añadir tests `TEST_*`:
   - `TEST_identity_confirmed_emits_person_recognized`: transición
     `CANDIDATE → CONFIRMED` con `person_id=7, confidence=0.82, votes=3, window=8`;
     afirmar `type == EventType.PERSON_RECOGNIZED`, `track_id == 1`, `person_id == 7`,
     `person_name == "David"` (pasado por parámetro), `payload["state"] == "CONFIRMED"`,
     `payload["votes"] == 3`, `severity == Severity.INFO`.
   - `TEST_identity_candidate_to_unknown_emits_unknown_person`: transición
     `CANDIDATE → UNKNOWN` → `type == EventType.UNKNOWN_PERSON` y
     `severity == Severity.WARNING` (default del catálogo, no explícita).
   - `TEST_identity_confirmed_to_unknown_emits_identity_lost`: transición
     `CONFIRMED → UNKNOWN` → `EventType.IDENTITY_LOST`.
   - `TEST_identity_temporarily_lost_to_unknown_emits_identity_lost`: transición
     `TEMPORARILY_LOST → UNKNOWN` → `EventType.IDENTITY_LOST`.
   - `TEST_identity_silent_transition_emits_nothing`: transición con `emits=False`
     → `received` sigue vacío tras 0.2 s (usar un `await asyncio.sleep(0.2)` corto en
     lugar de `wait_until`, que espera a que ocurra algo).
   - `TEST_identity_intermediate_states_emit_nothing`: transiciones
     `UNKNOWN → CANDIDATE` y `CONFIRMED → TEMPORARILY_LOST` (con `emits=True`) no
     publican nada — `_identity_event_type` devuelve `None`.
  </action>
  <verify>
    <automated>F:/Documentos/IA/Proyecto_Camara/.venv/Scripts/python.exe -m pytest tests/test_event_engine.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "def emit_identity" backend/events/engine.py` encuentra el método.
    - `grep -n "def _identity_event_type" backend/events/engine.py` encuentra el traductor.
    - `grep -n "severity=" backend/events/engine.py` NO muestra ninguna línea dentro de `emit_identity` (se conserva el default WARNING de `UNKNOWN_PERSON`).
    - `grep -n "Event(" backend/events/engine.py` sigue apareciendo solo dentro de `_publish`.
    - `grep -c "def TEST_identity" tests/test_event_engine.py` devuelve 6.
    - `pytest tests/test_event_engine.py -k identity -q` sale con código 0 y recoge 6 tests.
    - `pytest tests/test_event_engine.py tests/test_event_types.py tests/test_rule_engine.py -q` sale con código 0.
  </acceptance_criteria>
  <done>`EventEngine` sabe publicar los tres eventos de identidad con payload trazable (estado, estado previo, votos, ventana), respeta la severidad por defecto del catálogo e ignora las transiciones silenciosas e intermedias.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Hilo del worker → event loop asyncio | `publish_threadsafe` cruza el límite hilo/corrutina |
| Evento de identidad → WebSocket / reglas / BD | `person_name` (dato biométricamente derivado) sale del proceso |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-13 | Information Disclosure | `emit_identity` | mitigate | `person_name` viaja solo dentro del `Event` (canal ya autenticado del dashboard), nunca en un `logger`. `emit_identity` no escribe logs (ASVS V8) |
| T-24-14 | Tampering | severidad del evento | mitigate | No se pasa `severity=`, así que `UNKNOWN_PERSON` conserva su `WARNING` del catálogo; un test lo verifica explícitamente |
| T-24-15 | Repudiation | payload del evento | mitigate | El payload incluye `previous_state`, `votes` y `window`: cada evento de identidad lleva la evidencia con la que se tomó la decisión, auditable a posteriori |
| T-24-16 | Denial of Service | `TrackState.identity_state` | accept | Campo escalar de tamaño fijo por track; el registry ya tiene su propio `prune(now, ttl)` |
</threat_model>

<verification>
```bash
F:/Documentos/IA/Proyecto_Camara/.venv/Scripts/python.exe -m pytest tests/test_track_registry.py tests/test_event_engine.py tests/test_event_types.py tests/test_rule_engine.py tests/test_streaming_worker.py tests/test_metrics_sampler.py tests/test_architecture.py -q
```

**Nota lateral documentada en el RESEARCH (no se actúa en esta fase):**
`config/rules.yaml` tiene una regla `persona_desconocida` basada en
`event: LINE_CROSSED` + `person: unknown`. Al empezar a emitir `UNKNOWN_PERSON` de
verdad, añadir una regla para ese tipo sin retirar la otra duplicaría notificaciones.
Anotarlo en el SUMMARY; no tocar `config/rules.yaml` aquí.
</verification>

<success_criteria>
- `TrackState` expone `identity_state` con default `UNKNOWN` y `TrackRegistry` tiene `set_identity_state`.
- `EventEngine.emit_identity()` publica `PERSON_RECOGNIZED`, `UNKNOWN_PERSON` e `IDENTITY_LOST` delegando en `_publish`.
- Las transiciones silenciosas (`emits=False`) y las intermedias (CANDIDATE, TEMPORARILY_LOST) no publican nada.
- `UNKNOWN_PERSON` conserva severidad `WARNING` sin pasarla explícitamente.
- `pytest tests/test_track_registry.py tests/test_event_engine.py -q` sale con código 0.
</success_criteria>

<output>
Al terminar, crear `.planning/phases/24-identidad-temporal-votaci-n-y-m-quina-de-estados/24-04-SUMMARY.md`.
</output>
