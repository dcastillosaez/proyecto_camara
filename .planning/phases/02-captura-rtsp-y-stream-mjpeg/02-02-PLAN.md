---
phase: 02-captura-rtsp-y-stream-mjpeg
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - backend/main.py
  - tests/test_stream.py
autonomous: false
requirements:
  - CAP-03

must_haves:
  truths:
    - "GET /video_feed retorna StreamingResponse con content-type multipart/x-mixed-replace"
    - "El generador MJPEG codifica frames con JPEG quality 80 y los sirve con boundary=frame"
    - "El generador incluye await asyncio.sleep() en cada iteracion para permitir cancelacion"
    - "RTSPStream se inicia en lifespan startup y se detiene en lifespan shutdown"
    - "El usuario ve video en directo en el navegador al visitar http://localhost:8000/video_feed"
  artifacts:
    - path: "backend/main.py"
      provides: "FastAPI app con lifespan y endpoint /video_feed"
      exports: ["app"]
      min_lines: 30
  key_links:
    - from: "backend/main.py"
      to: "backend/stream.py"
      via: "import RTSPStream, instancia global, lifespan start/stop"
      pattern: "from backend\\.stream import RTSPStream"
    - from: "backend/main.py"
      to: "backend/config.py"
      via: "import get_settings para camera_url"
      pattern: "from backend\\.config import get_settings"
    - from: "backend/main.py"
      to: "/video_feed endpoint"
      via: "StreamingResponse con mjpeg_generator"
      pattern: "StreamingResponse.*mjpeg_generator"
---

<objective>
Implementar la app FastAPI en backend/main.py con el endpoint /video_feed que sirve el stream MJPEG. Conecta RTSPStream (del plan 02-01) con el navegador via un generador async que codifica frames como JPEG y los envia con formato multipart.

Purpose: Este endpoint es lo que el usuario abre en su navegador para ver el video en directo.
Output: backend/main.py funcional con test de integracion y verificacion manual en navegador.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-captura-rtsp-y-stream-mjpeg/02-RESEARCH.md
@.planning/phases/02-captura-rtsp-y-stream-mjpeg/02-01-SUMMARY.md

@backend/config.py
@backend/stream.py
</context>

<interfaces>
<!-- RTSPStream del plan 02-01 -->
```python
from backend.stream import RTSPStream

class RTSPStream:
    def __init__(self, url: str) -> None: ...
    def start(self) -> None: ...
    def stop(self) -> None: ...
    def get_frame(self) -> np.ndarray | None: ...
```

<!-- Settings del config existente -->
```python
from backend.config import get_settings

# get_settings().camera_url -> str (URL RTSP)
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implementar main.py con FastAPI, lifespan y endpoint /video_feed</name>
  <files>backend/main.py, tests/test_stream.py</files>
  <read_first>backend/stream.py, backend/config.py, .planning/phases/02-captura-rtsp-y-stream-mjpeg/02-RESEARCH.md</read_first>
  <behavior>
    - test_video_feed_returns_mjpeg_content_type: GET /video_feed retorna status 200 con header content-type que contiene "multipart/x-mixed-replace".
    - test_video_feed_contains_jpeg_boundary: Los primeros bytes del body contienen b"--frame" y b"Content-Type: image/jpeg".
    - test_app_creates_rtsp_stream_on_startup: Al iniciar la app, RTSPStream.start() se llama una vez.
  </behavior>
  <action>
Primero, agregar 3 tests de integracion al final de tests/test_stream.py (no en archivo separado, para mantener contexto):

```python
import pytest
from unittest.mock import patch, MagicMock
import numpy as np

@pytest.mark.asyncio
async def test_video_feed_returns_mjpeg_content_type():
    """GET /video_feed returns multipart/x-mixed-replace content type."""
    # Mock RTSPStream.get_frame para retornar un frame sintetico
    # Usar httpx.AsyncClient con ASGITransport
    ...

async def test_video_feed_contains_jpeg_boundary():
    """Response body starts with MJPEG boundary marker."""
    ...

async def test_app_creates_rtsp_stream_on_startup():
    """Lifespan calls RTSPStream.start() on startup."""
    ...
```

Instalar pytest-asyncio si no esta en requirements.txt (verificar primero; si falta, agregarlo).

Luego, reemplazar el placeholder de backend/main.py con:

```python
"""FastAPI application — MJPEG video feed from RTSP camera."""

import asyncio
import logging
from contextlib import asynccontextmanager

import cv2
from fastapi import FastAPI
from fastapi.responses import StreamingResponse

from backend.config import get_settings
from backend.stream import RTSPStream

logger = logging.getLogger(__name__)

rtsp_stream: RTSPStream | None = None


@asynccontextmanager
async def lifespan(app: FastAPI):
    global rtsp_stream
    settings = get_settings()
    rtsp_stream = RTSPStream(settings.camera_url)
    rtsp_stream.start()
    logger.info("RTSP stream started: %s", settings.camera_url)
    yield
    rtsp_stream.stop()
    logger.info("RTSP stream stopped")


app = FastAPI(lifespan=lifespan)


async def mjpeg_generator():
    """Yield JPEG frames in MJPEG multipart format."""
    try:
        while True:
            frame = rtsp_stream.get_frame()
            if frame is None:
                await asyncio.sleep(0.1)
                continue
            _, jpeg = cv2.imencode(
                ".jpg", frame, [cv2.IMWRITE_JPEG_QUALITY, 80]
            )
            yield (
                b"--frame\r\n"
                b"Content-Type: image/jpeg\r\n"
                b"Content-Length: " + str(len(jpeg)).encode() + b"\r\n\r\n"
                + jpeg.tobytes() + b"\r\n"
            )
            await asyncio.sleep(0.033)  # ~30 fps cap
    except asyncio.CancelledError:
        pass


@app.get("/video_feed")
async def video_feed():
    return StreamingResponse(
        mjpeg_generator(),
        media_type="multipart/x-mixed-replace; boundary=frame",
    )
```

Ejecutar: `.venv/Scripts/python.exe -m pytest tests/test_stream.py -v -x` — todos los tests (6 del plan 01 + 3 nuevos) deben pasar.
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/test_stream.py -v -x</automated>
  </verify>
  <acceptance_criteria>
    - backend/main.py exporta `app` (FastAPI instance)
    - app usa lifespan que crea RTSPStream con get_settings().camera_url
    - Endpoint GET /video_feed retorna StreamingResponse con media_type "multipart/x-mixed-replace; boundary=frame"
    - mjpeg_generator usa await asyncio.sleep(0.033) en cada iteracion
    - mjpeg_generator captura asyncio.CancelledError para limpieza
    - cv2.imencode usa IMWRITE_JPEG_QUALITY=80
    - 9 tests pasan (6 unitarios + 3 integracion)
  </acceptance_criteria>
  <done>main.py funcional con /video_feed, lifespan correcto, 9 tests GREEN.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verificar stream en vivo en navegador</name>
  <files>backend/main.py, backend/stream.py</files>
  <action>
Arrancar el servidor y presentar al usuario las instrucciones de verificacion manual.

Ejecutar: `.venv/Scripts/python.exe -m uvicorn backend.main:app --host 0.0.0.0 --port 8000`

Indicar al usuario que verifique:
1. Asegurar que la camara Tapo C220 esta encendida y conectada a la red local
2. Abrir http://localhost:8000/video_feed en Chrome
3. Verificar que se ve el video en directo con latencia menor a 2 segundos
4. Dejar correr 1-2 minutos — la latencia no debe aumentar (drain thread funciona)
5. Cerrar la pestana y verificar que no hay errores en la terminal
6. Ctrl+C para detener — debe cerrarse limpiamente

Esperar a que el usuario escriba "aprobado" o describa problemas.
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/test_stream.py -v --tb=short</automated>
  </verify>
  <done>El usuario confirma que ve video en directo en el navegador con latencia aceptable y cierre limpio.</done>
</task>

</tasks>

<verification>
.venv/Scripts/python.exe -m pytest tests/test_stream.py -v --tb=short
Todos los tests GREEN (unitarios + integracion).
Verificacion manual: video en directo visible en http://localhost:8000/video_feed.
</verification>

<success_criteria>
- GET /video_feed sirve stream MJPEG con video en directo de la camara
- Latencia inferior a 2 segundos en LAN
- Cerrar navegador no deja procesos zombi
- Ctrl+C detiene servidor limpiamente (lifespan shutdown)
- 9 tests pasan (6 unitarios stream + 3 integracion endpoint)
</success_criteria>

<output>
After completion, create `.planning/phases/02-captura-rtsp-y-stream-mjpeg/02-02-SUMMARY.md`
</output>
