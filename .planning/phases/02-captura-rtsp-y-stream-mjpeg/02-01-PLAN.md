---
phase: 02-captura-rtsp-y-stream-mjpeg
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_stream.py
  - tests/conftest.py
  - backend/stream.py
autonomous: true
requirements:
  - CAP-01
  - CAP-02

must_haves:
  truths:
    - "RTSPStream drena frames continuamente y solo expone el ultimo via get_frame()"
    - "Tras fallo de cap.read(), RTSPStream libera el VideoCapture y reconecta con backoff exponencial (1s a 30s)"
    - "get_frame() devuelve una copia del frame, no la referencia interna"
    - "stop() detiene el hilo daemon y libera el VideoCapture"
  artifacts:
    - path: "backend/stream.py"
      provides: "RTSPStream class con drain thread y reconexion"
      exports: ["RTSPStream"]
      min_lines: 60
    - path: "tests/test_stream.py"
      provides: "Tests unitarios para drain, reconexion y get_frame"
      min_lines: 40
    - path: "tests/conftest.py"
      provides: "Fixture fake_frame y mock de VideoCapture"
      min_lines: 10
  key_links:
    - from: "backend/stream.py"
      to: "backend/config.py"
      via: "import get_settings para camera_url"
      pattern: "from backend\\.config import"
---

<objective>
Implementar la clase RTSPStream en backend/stream.py con el patron drain thread: un hilo daemon que lee frames RTSP en bucle continuo, descarta todos menos el ultimo, y reconecta automaticamente con backoff exponencial tras fallos.

Purpose: Sin esta clase no hay frames disponibles para servir via MJPEG. Es la base de toda la cadena de video.
Output: backend/stream.py funcional con tests unitarios que verifican drain, reconexion y thread safety.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-captura-rtsp-y-stream-mjpeg/02-RESEARCH.md

@backend/config.py
</context>

<interfaces>
<!-- Contrato existente que stream.py debe consumir -->

From backend/config.py:
```python
class Settings(BaseSettings):
    camera_url: str = "rtsp://192.168.1.132:554/stream1"
    # ...

@lru_cache
def get_settings() -> Settings:
    """Return cached Settings singleton."""
    return Settings()
```

<!-- Contrato que stream.py debe exponer (para plan 02-02) -->
```python
class RTSPStream:
    def __init__(self, url: str) -> None: ...
    def start(self) -> None: ...
    def stop(self) -> None: ...
    def get_frame(self) -> np.ndarray | None: ...
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Tests unitarios y fixtures para RTSPStream</name>
  <files>tests/conftest.py, tests/test_stream.py</files>
  <read_first>backend/config.py, tests/__init__.py, .planning/phases/02-captura-rtsp-y-stream-mjpeg/02-RESEARCH.md</read_first>
  <behavior>
    - test_drain_keeps_latest_frame: Al crear RTSPStream con un mock de cv2.VideoCapture que retorna 5 frames distintos, get_frame() devuelve el ultimo frame leido (no uno intermedio ni None).
    - test_get_frame_returns_copy: El ndarray retornado por get_frame() no es el mismo objeto que el interno (_frame). Modificar el retornado no afecta al interno.
    - test_reconnect_on_failure: Si cap.read() retorna (False, None), RTSPStream hace cap.release() y crea un nuevo VideoCapture. Verificar que cv2.VideoCapture se llama al menos 2 veces.
    - test_backoff_increases: Durante reconexion, el delay entre intentos crece exponencialmente. Verificar con mock de time.sleep que los delays son 1, 2, 4... hasta max 30.
    - test_stop_releases_capture: Tras stop(), el VideoCapture se libera (cap.release() llamado).
    - test_get_frame_none_before_start: Antes de que llegue el primer frame, get_frame() retorna None.
  </behavior>
  <action>
Crear tests/conftest.py con:
- Fixture `fake_frame` que retorna `np.zeros((720, 1280, 3), dtype=np.uint8)`.
- Fixture `mock_video_capture` que parchea `cv2.VideoCapture` con un MagicMock configurable. El mock por defecto retorna `isOpened()=True` y `read()=(True, fake_frame)`.

Crear tests/test_stream.py con los 6 tests descritos en `<behavior>`. Todos usan mocks de cv2 (nunca abren una conexion RTSP real). Importar RTSPStream desde `backend.stream`.

Para test_drain_keeps_latest_frame: configurar el mock para retornar frames con valores pixel distintos (0, 1, 2, 3, 4), arrancar el stream, esperar 0.3s con `time.sleep(0.3)`, verificar que `get_frame()` retorna el frame con valor 4.

Para test_reconnect_on_failure: hacer que `read()` retorne `(False, None)` las 2 primeras veces, luego `(True, frame)`. Verificar que `cv2.VideoCapture` se instancio mas de 1 vez (hubo reconexion). Parchear `time.sleep` para que los delays no bloqueen.

Para test_backoff_increases: parchear `time.sleep` y capturar los argumentos. Hacer que el mock de `VideoCapture` falle 5 veces seguidas y luego funcione. Verificar que los delays son aproximadamente [1, 2, 4, 8, 16].

IMPORTANTE: Los tests DEBEN fallar al principio (RED). stream.py solo tiene el docstring placeholder.

Ejecutar: `.venv/Scripts/python.exe -m pytest tests/test_stream.py -v -x` — debe fallar con ImportError o AssertionError.
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/test_stream.py -v -x 2>&amp;1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - tests/conftest.py existe con fixtures fake_frame y mock_video_capture
    - tests/test_stream.py existe con 6 funciones test_* que importan RTSPStream
    - Todos los tests fallan (RED) porque backend/stream.py no tiene implementacion
  </acceptance_criteria>
  <done>6 tests escritos, todos fallan con ImportError al importar RTSPStream desde backend.stream</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implementar RTSPStream con drain thread y reconexion</name>
  <files>backend/stream.py</files>
  <read_first>tests/test_stream.py, tests/conftest.py, backend/config.py, .planning/phases/02-captura-rtsp-y-stream-mjpeg/02-RESEARCH.md</read_first>
  <behavior>
    - Los 6 tests del Task 1 pasan (GREEN).
    - RTSPStream.__init__ acepta url: str, inicializa _frame=None, _lock=threading.Lock(), _running=False, _cap=None.
    - start() lanza un threading.Thread daemon que ejecuta _capture_loop().
    - _capture_loop() lee frames en bucle, guarda solo el ultimo en _frame con lock.
    - Si cap.read() falla, llama a _reconnect() que hace release + nuevo VideoCapture con backoff exponencial.
    - get_frame() retorna _frame.copy() bajo lock, o None si no hay frame.
    - stop() pone _running=False y libera cap si existe.
  </behavior>
  <action>
Reemplazar el placeholder de backend/stream.py con la clase RTSPStream completa:

```python
"""RTSP stream capture with drain thread and auto-reconnection."""

import logging
import threading
import time

import cv2
import numpy as np

logger = logging.getLogger(__name__)


class RTSPStream:
    """Captures RTSP frames in a daemon thread, keeping only the latest."""

    def __init__(self, url: str) -> None:
        self._url = url
        self._frame: np.ndarray | None = None
        self._lock = threading.Lock()
        self._running = False
        self._cap: cv2.VideoCapture | None = None

    def start(self) -> None:
        self._running = True
        self._thread = threading.Thread(target=self._capture_loop, daemon=True)
        self._thread.start()

    def _capture_loop(self) -> None:
        self._cap = self._create_capture()
        while self._running:
            if self._cap is None or not self._cap.isOpened():
                self._reconnect()
                continue
            ret, frame = self._cap.read()
            if not ret:
                self._reconnect()
                continue
            with self._lock:
                self._frame = frame

    def _create_capture(self) -> cv2.VideoCapture:
        cap = cv2.VideoCapture(self._url, cv2.CAP_FFMPEG)
        cap.set(cv2.CAP_PROP_BUFFERSIZE, 1)
        return cap

    def _reconnect(self) -> None:
        if self._cap is not None:
            self._cap.release()
            self._cap = None

        delay = 1.0
        max_delay = 30.0

        while self._running:
            logger.warning("Reconnecting RTSP in %.1fs...", delay)
            time.sleep(delay)
            cap = self._create_capture()
            if cap.isOpened():
                ret, _ = cap.read()
                if ret:
                    self._cap = cap
                    logger.info("RTSP reconnection successful")
                    return
            cap.release()
            delay = min(delay * 2, max_delay)

    def get_frame(self) -> np.ndarray | None:
        with self._lock:
            return self._frame.copy() if self._frame is not None else None

    def stop(self) -> None:
        self._running = False
        if self._cap is not None:
            self._cap.release()
            self._cap = None
```

Usar esta implementacion como referencia. El executor puede ajustar detalles menores para pasar los tests.

Ejecutar: `.venv/Scripts/python.exe -m pytest tests/test_stream.py -v -x` — los 6 tests deben pasar (GREEN).

Si algun test falla, ajustar la implementacion hasta que pase. No modificar los tests excepto si hay un bug claro en el test mismo.
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/test_stream.py -v -x</automated>
  </verify>
  <acceptance_criteria>
    - backend/stream.py exporta la clase RTSPStream
    - RTSPStream tiene metodos start(), stop(), get_frame(), _reconnect(), _capture_loop()
    - _reconnect() usa backoff exponencial con delay entre 1.0 y 30.0
    - get_frame() usa threading.Lock y retorna .copy()
    - _create_capture() usa cv2.CAP_FFMPEG y CAP_PROP_BUFFERSIZE=1
    - Los 6 tests de tests/test_stream.py pasan
  </acceptance_criteria>
  <done>6 tests GREEN. RTSPStream con drain thread, reconexion con backoff, y thread safety implementado.</done>
</task>

</tasks>

<verification>
.venv/Scripts/python.exe -m pytest tests/test_stream.py -v --tb=short
Todos los tests GREEN, sin warnings criticos.
</verification>

<success_criteria>
- backend/stream.py contiene RTSPStream con drain thread pattern
- Reconexion automatica con backoff exponencial (1s a 30s max)
- get_frame() retorna copia thread-safe del ultimo frame
- 6 tests unitarios pasan sin camara real (todo mockeado)
</success_criteria>

<output>
After completion, create `.planning/phases/02-captura-rtsp-y-stream-mjpeg/02-01-SUMMARY.md`
</output>
