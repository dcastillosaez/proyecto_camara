---
phase: 32-vista-de-c-mara-y-configuraci-n-visual
plan: 07
type: execute
wave: 6
depends_on: ["32-04", "32-06"]
files_modified:
  - frontend/js/nav.js
  - frontend/index.html
  - frontend/js/app.js
  - tests/test_frontend_modules.py
autonomous: true
requirements: [OPS-16, OPS-17, OPS-18]

must_haves:
  truths:
    - "El tablist de vistas pasa de 2 a 4 pestañas (Operaciones, Analítica, Cámara, Ajustes) sin tocar ningún panel ni id de Operaciones"
    - "#camera-feed arranca sin src y solo lo recibe la primera activación de la pestaña Cámara"
    - "Los 8 módulos nuevos de la fase están en LOCKED_JS y ninguno supera 300 líneas"
    - "Si nav.js no existe al ejecutar este plan, el plan se detiene con instrucciones explícitas de ejecutar la Fase 31 primero, sin construir un tablist propio"
  artifacts:
    - path: "frontend/js/nav.js"
      provides: "2 entradas más (camara, ajustes) en el registro de vistas de la Fase 31, sin reescritura"
    - path: "frontend/index.html"
      provides: "tabpanel Cámara (video grande + rtsp-card + teselas + ajustes rápidos) y tabpanel Ajustes (árbol + panel + savebar), ~25 líneas de armazón, cero lógica inline"
  key_links:
    - from: "frontend/js/app.js"
      to: "frontend/js/views/camera.js + settings.js"
      via: "initCamera()/initCameraQuick()/initSettings() llamadas en DOMContentLoaded"
      pattern: "initCamera\\(\\)|initSettings\\(\\)"
---

<objective>
Cerrar la fase conectando todo lo construido en waves 1-5 a la navegación real: extender
`frontend/js/nav.js` (de la Fase 31) con las pestañas "Cámara" y "Ajustes", añadir el armazón
HTML mínimo de ambos `tabpanel` en `index.html` (contenedores con `id`, cero lógica), y llamar
`initCamera()`/`initCameraQuick()`/`initSettings()` desde `app.js`. Este es el ÚNICO plan de la
fase que toca `nav.js` — por diseño, para que el bloqueo de la Fase 31 (si no se ha ejecutado
todavía) afecte solo a este plan y no a los 6 anteriores.

**Precondición dura, verificada ANTES de cualquier otra acción de este plan**: `32-RESEARCH.md`
Pitfall 5 y el hallazgo de `32-01-PLAN.md` Task 0 documentan que, en el momento de planificar
esta fase, la Fase 31 solo tenía 11 `PLAN.md` sin ejecutar — `frontend/js/nav.js` no existía. Si
sigue sin existir en el momento de EJECUTAR este plan, la Task 1 de abajo se detiene con
instrucciones explícitas; no se construye un `nav.js`/`tablist` propio (eso es responsabilidad
de la Fase 31, no de la 32 — replicarlo aquí duplicaría el mecanismo de navegación y
contradiría D-01 del CONTEXT, que exige heredar literalmente el de la Fase 31).

Purpose: sin este plan, ninguna de las dos vistas es alcanzable desde el navegador — todo el
trabajo de las waves 1-5 queda construido pero inerte.
Output: 4 pestañas de vista, ambos `tabpanel` montados, `app.js` arrancando los 3 módulos
nuevos, `LOCKED_JS` con los 8 ficheros de la fase.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/32-vista-de-c-mara-y-configuraci-n-visual/32-UI-SPEC.md
@frontend/js/app.js
@frontend/index.html
@tests/test_frontend_modules.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Precondición — verificar nav.js real (bloqueante) antes de tocarlo</name>
  <read_first>
    - frontend/js/nav.js (puede no existir todavía)
    - .planning/phases/32-vista-de-c-mara-y-configuraci-n-visual/32-01-SUMMARY.md sección "Estado de la dependencia Fase 31" (verificación temprana de wave 1 — releer su resultado, no repetir el mismo hallazgo desde cero si sigue siendo válido)
  </read_first>
  <action>
    ```bash
    test -f frontend/js/nav.js && echo "EXISTE" || echo "NO_EXISTE"
    ```
    Si "NO_EXISTE": **detener este plan aquí**. No modificar `nav.js`, `index.html` ni `app.js`
    en absoluto — dejar Task 2 y Task 3 sin ejecutar. Escribir el SUMMARY de este plan con el
    texto: "Bloqueado: `frontend/js/nav.js` no existe todavía. Ejecutar `/gsd-execute-phase 31`
    (código real, no solo planificación) y volver a lanzar `/gsd-execute-phase 32` — este plan
    (32-07) es el único punto de la Fase 32 que depende de ese armazón; las waves 1-5 (backend
    completo, CSS, camera.js/camera-quick.js, settings-field.js/settings-save.js,
    settings.js/settings-section.js) ya están construidas y no necesitan reejecutarse." Devolver
    el control al orquestador con este estado — NO intentar continuar con Task 2/3 en modo
    degradado ni construir un tablist sustituto.
    Si "EXISTE": leer el fichero completo, extraer con
    `grep -n "export function\|export const" frontend/js/nav.js` el contrato real (nombres de
    función para registrar una vista/pestaña nueva, forma del hash routing), y usarlo
    literalmente en Task 2 — el UI-SPEC describe el mecanismo esperado (`role="tablist"`,
    `history.replaceState`, activación con flechas) pero el código real de la Fase 31, una vez
    exista, es la fuente de verdad sobre nombres de función exactos, no el UI-SPEC.
  </action>
  <files>none — verificación de solo lectura; el resultado (bloqueo o contrato extraído) se anota en SUMMARY.md, no se crea ni modifica ningún fichero de código</files>
  <verify>
    <automated>test -f frontend/js/nav.js; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>Si nav.js no existe: SUMMARY documenta el bloqueo literal y Task 2/3 no se ejecutan. Si existe: su contrato real queda extraído y citado en el SUMMARY para Task 2.</acceptance_criteria>
  <done>Si nav.js no existe: SUMMARY documenta el bloqueo literal y Task 2/3 no se ejecutan. Si existe: su contrato real queda extraído y citado en el SUMMARY para Task 2.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extender nav.js (2 pestañas) + armazón HTML de ambos tabpanel</name>
  <read_first>
    - frontend/js/nav.js (contrato real, extraído en Task 1 — solo ejecutar si Task 1 confirmó EXISTE)
    - frontend/index.html (estructura real del tablist y de los tabpanel existentes tras la Fase 31)
    - .planning/phases/32-vista-de-c-mara-y-configuraci-n-visual/32-UI-SPEC.md secciones "Decisión estructural 1" y "Decisión estructural 2" (contrato exacto de las 4 pestañas y del doble `<img>`)
  </read_first>
  <behavior>
    - El `tablist` pasa de 2 a 4 `role="tab"`: Operaciones, Analítica, Cámara, Ajustes, en ese
      orden; los dos primeros (heredados de Fase 29/31) no cambian ni de posición ni de
      `aria-controls`.
    - `#camara` y `#ajustes` (y `#ajustes/{sección}`) se resuelven con el mismo mecanismo de
      hash que ya usan `#operaciones`/`#analitica` — ni un segundo router, ni un segundo
      listener de `hashchange`.
    - Activar la pestaña Cámara por primera vez llama `activateCameraFeed()` de `32-04` (deferred
      activation del `<img>`, D-02/"Decisión estructural 2" del UI-SPEC) — activaciones
      posteriores no reasignan `src`.
    - `index.html` gana el armazón de ambos `tabpanel` (contenedores con `id`, sin lógica): el
      de Cámara con `<img id="camera-feed">` (sin `src`), el bloque `#camera-offline` (mismo
      copy/patrón que el ya existente en Operaciones, con ids propios), los 3 contenedores de la
      tarjeta RTSP (`#rtsp-dot`, `#rtsp-status-text`, `#rtsp-last-frame`, `#rtsp-reconnects`,
      `#rtsp-resolution`, `#rtsp-url`), las 6 teselas de métrica (`#cam-capture-fps`,
      `#cam-detection-fps`, `#cam-latency`, `#cam-cpu`, `#cam-ram`, `#cam-dropped`), la barra de
      ajustes rápidos (contenedores para los 4 controles) y `#camera-updated`; el de Ajustes con
      `#settings-tree`, `#settings-panel`, y el contenedor del popover de restaurar. Ningún
      `<script>`/`<style>` inline — `TEST_no_inline_logic` sigue en verde.
  </behavior>
  <action>
    Extender `frontend/js/nav.js` con dos entradas más al registro de vistas existente (usar el
    contrato real extraído en Task 1 — la forma exacta depende de cómo la Fase 31 implementó su
    registro; no inventar una API distinta a la ya usada por `operaciones`/`analitica`).

    Añadir a `index.html`, dentro de `<nav role="tablist">`, dos `<button role="tab">` más
    (Cámara, Ajustes) con el mismo patrón de clases/atributos que los dos ya existentes, y dos
    `<div role="tabpanel" hidden>` con el armazón descrito en `<behavior>` — todo con Tailwind
    utility classes inline en el marcado (no CSS nuevo aquí, ya está en `32-03`), sin interpolar
    ningún dato dinámico en el HTML estático (todo dato real lo pintan los módulos JS en
    runtime).
  </action>
  <verify>
    <automated>grep -c 'role="tab"' frontend/index.html; grep -c "camera-feed" frontend/index.html</automated>
  </verify>
  <acceptance_criteria>4 pestañas en el tablist, ambos tabpanel montados con los ids exactos que camera.js/camera-quick.js/settings.js/settings-section.js ya esperaban (waves 4-5), cero script/style inline nuevo.</acceptance_criteria>
  <done>4 pestañas en el tablist, ambos tabpanel montados con los ids exactos que camera.js/camera-quick.js/settings.js/settings-section.js ya esperaban (waves 4-5), cero script/style inline nuevo.</done>
</task>

<task type="auto">
  <name>Task 3: Wiring en app.js + LOCKED_JS de los 8 módulos de la fase</name>
  <read_first>
    - frontend/js/app.js (58 líneas — orden de wiring existente, punto de inserción)
    - tests/test_frontend_modules.py (LOCKED_JS — lista actual tras la Fase 31, si ya se extendió)
  </read_first>
  <action>
    En `frontend/js/app.js`, añadir los imports de los 3 puntos de entrada de esta fase:
    ```javascript
    import { initCamera } from './views/camera.js';
    import { initCameraQuick } from './views/camera-quick.js';
    import { initSettings } from './views/settings.js';
    ```
    Y, dentro de `document.addEventListener('DOMContentLoaded', ...)`, tras el bloque existente
    de `loadObservability()`/`setInterval(loadObservability, 5000)` (línea ~57), añadir:
    ```javascript
    initCamera();
    initCameraQuick();
    initSettings();
    ```
    (Los tres son seguros de llamar aunque su pestaña no esté activa todavía — `initCamera`
    solo arma el timer de la tarjeta RTSP y NO activa `#camera-feed` hasta que `nav.js` llame
    `activateCameraFeed()` al abrir la pestaña por primera vez; `initSettings` carga el esquema
    una vez y lo deja en memoria, coste bajo incluso si el operador nunca abre Ajustes en esa
    sesión.)

    Añadir a `LOCKED_JS` en `tests/test_frontend_modules.py` (tras la última entrada existente,
    sin reordenar las anteriores):
    ```python
    "views/camera.js",
    "views/camera-quick.js",
    "views/settings.js",
    "views/settings-section.js",
    "views/settings-field.js",
    "views/settings-save.js",
    ```
    (`nav.js` y `dashboard-observability.js` ya deberían estar en `LOCKED_JS` desde la Fase
    28/31 — si `dashboard-observability.js` no está, añadirlo también, ya que `32-04` lo
    extendió.)
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/test_frontend_modules.py -q</automated>
  </verify>
  <acceptance_criteria>app.js llama a los 3 initX() nuevos; los 6-8 módulos de la fase están en LOCKED_JS; TEST_line_limit y TEST_locked_files_exist en verde.</acceptance_criteria>
  <done>app.js llama a los 3 initX() nuevos; los 6-8 módulos de la fase están en LOCKED_JS; TEST_line_limit y TEST_locked_files_exist en verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Ninguna nueva — este plan es cableado de UI sobre superficies ya aseguradas en waves previas
(auth global heredada, `PUT /api/v2/config` ya validado en `32-02`).

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-21 | N/A | Wiring de `app.js`/`nav.js`/`index.html` | accept | Marcado estático + llamadas a funciones ya auditadas en planes anteriores; no introduce superficie nueva |

</threat_model>

<verification>
```bash
.venv/Scripts/python.exe -m pytest tests/test_frontend_modules.py -q
.venv/Scripts/python.exe -m pytest tests/ -q
```
</verification>

<success_criteria>
- Si `nav.js` no existía al ejecutar: el plan se detiene documentando el bloqueo, sin tocar ningún fichero, y las waves 1-5 quedan intactas y reutilizables cuando la Fase 31 se ejecute.
- Si `nav.js` existía: 4 pestañas en el tablist, ambos tabpanel montados con los ids exactos esperados por los módulos de waves 4-5, `app.js` arranca `initCamera`/`initCameraQuick`/`initSettings`, `LOCKED_JS` cubre los 8 módulos de la fase, suite completa en verde.
</success_criteria>

<output>
After completion, create `.planning/phases/32-vista-de-c-mara-y-configuraci-n-visual/32-07-SUMMARY.md`
</output>
