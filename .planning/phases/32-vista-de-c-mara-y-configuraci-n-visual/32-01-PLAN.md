---
phase: 32-vista-de-c-mara-y-configuraci-n-visual
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/storage/repositories.py
  - backend/api/v2/config_schema.py
  - tests/test_repositories.py
  - tests/test_config_schema.py
autonomous: true
requirements: [SET-01, SET-02, SET-03]

must_haves:
  truths:
    - "ConfigRepo puede borrar una fila de app_config por clave (necesario para 'Restaurar valores por defecto', OPS-20)"
    - "Existe un registro declarativo (FIELD_SCHEMA) con los 112 parámetros reales de Settings, agrupados en las 8 secciones fijas y sus subsecciones"
    - "Cada FieldDef con validador cruzado o de rango en backend/config.py cita ese rango exacto, no un rango inventado"
    - "resolve_origin() distingue runtime/env/default para escalares y para listas sin falsos positivos"
  artifacts:
    - path: "backend/api/v2/config_schema.py"
      provides: "FieldDef, Section, Group, ALL_SECTIONS (112 campos), resolve_origin(), build_candidate_settings()"
      min_lines: 400
    - path: "backend/storage/repositories.py"
      provides: "ConfigRepo.delete(key)"
    - path: "tests/test_config_schema.py"
      provides: "cobertura de resolve_origin (3 estados, listas) y de que ALL_SECTIONS cubre exactamente los campos de Settings"
  key_links:
    - from: "backend/api/v2/config_schema.py"
      to: "backend/config.py"
      via: "FieldDef.min/max citan field_validator/model_validator existentes"
      pattern: "identity_vote_window|reid_similarity_threshold|run_window_secs|context_low_ratio|snapshot_max_width"
---

<objective>
Sentar la base declarativa de toda la fase: un módulo de esquema (`config_schema.py`) que
describe los 112 parámetros reales de `backend/config.py` (label, hint, tipo, rango, sección,
aplicación en caliente, secreto), más el método `ConfigRepo.delete()` que falta para poder
borrar overrides. Ningún endpoint HTTP se crea en este plan — es contrato puro, consumido por
`32-02` (router) y por las vistas de frontend de waves posteriores.

Esta fase (`OPS-16..OPS-20`, `SET-01..SET-04`) construye la vista Cámara y la vista Ajustes.
El `32-UI-SPEC.md` fija los 8 nombres de sección como decisión de producto **fija, no
renegociable**: "Cámara" · "Detección" · "Tracking" · "Reconocimiento" · "Zonas" · "Reglas" ·
"Alertas" · "Almacenamiento". Este plan asigna cada uno de los 112 campos reales de `Settings`
a exactamente una de esas 8 secciones (con subsecciones, que no están fijadas por el UI-SPEC y
son responsabilidad de este plan, per `32-CONTEXT.md` § Claude's Discretion).

Purpose: sin este esquema en servidor, ningún endpoint ni ninguna vista puede existir sin
inventar rangos o duplicar la fuente de verdad (D-04: "el cliente no mantiene copia de rangos
ni traducciones de nombres de campo").
Output: `backend/api/v2/config_schema.py`, `ConfigRepo.delete()`, tests de ambos.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/32-vista-de-c-mara-y-configuraci-n-visual/32-CONTEXT.md
@.planning/phases/32-vista-de-c-mara-y-configuraci-n-visual/32-RESEARCH.md
@.planning/phases/32-vista-de-c-mara-y-configuraci-n-visual/32-UI-SPEC.md
@backend/config.py
@backend/storage/repositories.py
@backend/api/v2/detection.py
</context>

<interfaces>
<!-- Contratos que este plan CREA. 32-02 y todas las vistas de frontend de waves
posteriores los consumen literalmente — no hay que re-derivarlos. -->

```python
# backend/api/v2/config_schema.py — contrato final de este plan

from __future__ import annotations
from dataclasses import dataclass, field
from typing import Any

@dataclass(frozen=True)
class FieldDef:
    key: str                      # nombre exacto en Settings y clave en app_config
    env: str                      # nombre de la env var (mayúsculas)
    label: str                    # español llano, nunca el identificador Python
    hint: str = ""                # pista en prosa, opcional
    type: str = "str"             # "bool"|"int"|"float"|"enum"|"time"|"list_int"|"list_str"|"secret"|"readonly"
    default: Any = None
    min: float | None = None
    max: float | None = None
    step: float | None = None
    enum_values: tuple[str, ...] | None = None
    applies: str = "restart_camera"   # "hot" | "restart_camera" | "restart_server"
    secret: bool = False
    readonly: bool = False
    max_length: int | None = None     # solo para type="str"/"list_str" sin rango numérico

@dataclass(frozen=True)
class Group:
    key: str
    label: str
    fields: tuple[FieldDef, ...] = ()
    # Grupos de solo lectura no respaldados por Settings (zonas definidas, reglas
    # cargadas) usan fields=() y se marcan con external_source:
    external_source: str | None = None   # "/api/zones" | "/api/v2/rules" | None

@dataclass(frozen=True)
class Section:
    key: str
    label: str
    groups: tuple[Group, ...] = ()

ALL_SECTIONS: tuple[Section, ...] = (...)   # las 8 secciones, ver Task 2

def all_fields() -> list[FieldDef]:
    """Aplana ALL_SECTIONS a una lista de 112 FieldDef, para lookup por key."""

def field_by_key(key: str) -> FieldDef | None: ...

def resolve_origin(field: FieldDef, overrides: dict[str, Any], settings: Any) -> tuple[Any, str]:
    """Devuelve (valor_efectivo, origin) con origin en {"runtime","env","default"}.
    Ver Task 1 para el algoritmo exacto (incluye el caso de listas, Pitfall 4)."""

def build_candidate_settings(settings: Any, overrides: dict[str, Any], changes: dict[str, Any]):
    """Construye un Settings candidato = overrides + changes aplicados sobre defaults,
    usando el CONSTRUCTOR completo (no model_copy — ver Task 1, pydantic 2.13.1 instalado
    no revalida con model_copy). Lanza pydantic.ValidationError si algún model_validator
    de Settings lo rechaza. 32-02 la usa para SET-03 sin reimplementar los validadores."""
```

Estas firmas son el contrato que `32-02-PLAN.md` importa literalmente. No renombrar ni
cambiar la forma de ningún campo del dataclass sin razón — downstream depende de esto.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 0: Verificar el andamiaje de navegación de la Fase 31 (informe temprano, no bloqueante para este plan)</name>
  <read_first>
    - frontend/js/nav.js (puede no existir)
    - frontend/index.html
  </read_first>
  <action>
    Este plan (backend puro) NO depende de `nav.js` ni del `role="tablist"` de la Fase 31 — es
    trabajo independiente y puede avanzar exista o no ese armazón. Pero `32-RESEARCH.md`
    Pitfall 5 exige una verificación explícita y temprana en la fase para que el riesgo quede
    documentado antes de que ninguna otra ola avance asumiendo lo contrario.

    Ejecutar y registrar el resultado literal en el SUMMARY de este plan (sección "Estado de
    la dependencia Fase 31"):
    ```bash
    test -f frontend/js/nav.js && echo "nav.js EXISTE" || echo "nav.js NO EXISTE"
    grep -c 'role="tablist"' frontend/index.html 2>/dev/null || echo "0 (index.html sin tablist o inexistente)"
    ```
    Si "nav.js NO EXISTE" (esperado en esta sesión, verificado 2026-08-23: Fase 31 solo tiene
    11 PLAN.md sin ejecutar): escribir en el SUMMARY, textualmente, que **la Fase 31 debe
    ejecutarse (código real, `/gsd-execute-phase 31`) antes de que `32-07-PLAN.md` (wave 6,
    integración de navegación) pueda ejecutarse**, y que el resto de planes de esta fase
    (32-01..32-06, backend completo y los seis módulos de vista) no dependen de ese armazón y
    pueden construirse y verificarse igual mientras tanto. No crear ni modificar `nav.js` ni
    `index.html` en este task — verificar y reportar es lo único que corresponde aquí; construir
    el tablist es responsabilidad de la Fase 31, no de esta.
    Si "nav.js EXISTE" (la Fase 31 se ejecutó entre la investigación y la ejecución de este
    plan): registrar en el SUMMARY las funciones exportadas de `nav.js` (grep `export function`)
    para que `32-07-PLAN.md` tenga el contrato real, no el asumido por el UI-SPEC.
  </action>
  <verify>
    <automated>test -f frontend/js/nav.js; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>SUMMARY de este plan contiene la sección "Estado de la dependencia Fase 31" con el resultado literal de ambos comandos y, si falta, la instrucción de bloqueo para 32-07.</acceptance_criteria>
  <done>SUMMARY de este plan contiene la sección "Estado de la dependencia Fase 31" con el resultado literal de ambos comandos y, si falta, la instrucción de bloqueo para 32-07.</done>
</task>

<task type="auto">
  <name>Task 1: ConfigRepo.delete() + dataclasses FieldDef/Group/Section + resolvers</name>
  <read_first>
    - backend/storage/repositories.py (líneas 735-818: ZoneRepo.delete, RuleRepo.delete, ConfigRepo completo)
    - backend/config.py (Settings completo — ya leído en investigación, releer solo si hace falta confirmar una línea exacta)
    - tests/test_repositories.py (para el estilo de test existente de ConfigRepo, si lo hay)
  </read_first>
  <action>
    **1. `ConfigRepo.delete()`** en `backend/storage/repositories.py`, dentro de la clase
    `ConfigRepo` ya existente (junto a `get`/`set`/`get_all`), copiando exactamente la forma de
    `RuleRepo.delete()` (líneas 778-785):
    ```python
    async def delete(self, key: str) -> bool:
        async with self._sf() as session:
            async with session.begin():
                row = await session.get(models.AppConfig, key)
                if row:
                    await session.delete(row)
                    return True
        return False
    ```
    Ni prefijos de clave ni tabla nueva (RESEARCH Anti-Patterns) — es una fila más de
    `app_config`, idéntico al resto de operaciones ya existentes en esta clase.

    **2. Crear `backend/api/v2/config_schema.py`** con el módulo de cabecera explicando su
    propósito (molde: docstring de `detection.py` líneas 1-18) y las dataclasses `FieldDef`,
    `Group`, `Section` exactamente como se definen en `<interfaces>` arriba (con
    `frozen=True`, `from __future__ import annotations`).

    **3. `all_fields()` / `field_by_key()`**: recorren `ALL_SECTIONS` (definido en Task 2) y
    devuelven, respectivamente, la lista plana de los 112 `FieldDef` y un lookup por `key`
    (usar un `dict` construido una vez a nivel de módulo, `_FIELDS_BY_KEY: dict[str, FieldDef]`,
    no recalcular en cada llamada).

    **4. `resolve_origin(field, overrides, settings)`** — tres vías, exactamente:
    ```python
    def resolve_origin(field: FieldDef, overrides: dict[str, Any], settings: Any) -> tuple[Any, str]:
        if field.key in overrides:
            return overrides[field.key], "runtime"
        current = getattr(settings, field.key, field.default)
        if current != field.default:
            return current, "env"
        return current, "default"
    ```
    Caso especial `camera_url`: el valor devuelto (en cualquiera de las tres vías) SIEMPRE pasa
    por `mask_rtsp_url()` (`backend/config.py:429`) antes de salir de `resolve_origin` — nunca
    se devuelve la URL cruda, ni siquiera para decidir el `origin`. Aplicar la máscara
    DESPUÉS de calcular `origin` sobre el valor sin enmascarar (enmascarar no debe afectar la
    comparación `!=` contra el default, que también está sin credenciales embebidas per el
    comentario de `config.py:19`).
    Test explícito de Pitfall 4 (listas): `yolo_classes=[0,24]` vía `.env` (distinto del
    default `[0]`) debe resolver `origin="env"`; con `.env` ausente (== default `[0]`), debe
    resolver `origin="default"`. Cubrir también `object_class_ids` y `schedule_days`.

    **5. `build_candidate_settings(settings, overrides, changes)`** — resuelve la Assumption A1
    de `32-RESEARCH.md`: en pydantic 2.13.1 (instalado, verificado en esta sesión),
    `model_copy(update=...)` **NO** re-ejecuta `model_validator(mode="after")` (es una copia
    superficial por diseño). Usar el **constructor completo**:
    ```python
    def build_candidate_settings(settings: Any, overrides: dict[str, Any], changes: dict[str, Any]) -> Any:
        from backend.config import Settings
        base = settings.model_dump()
        base.update(overrides)      # runtime ya persistido
        base.update(changes)        # cambios propuestos en este PUT
        return Settings(**base)     # constructor completo -> SÍ re-ejecuta field_validator y model_validator
    ```
    Esto lanza `pydantic.ValidationError` si algún `model_validator` (identidad, reid,
    behavior, object, snapshot — `backend/config.py:309-407`) lo rechaza; `32-02` la envuelve
    en un `try/except` para mapear cada error a su fila (SET-03, D-10).
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/test_repositories.py -k ConfigRepo -q</automated>
  </verify>
  <acceptance_criteria>ConfigRepo.delete() devuelve True/False correctamente en test; resolve_origin y build_candidate_settings existen e importan sin error.</acceptance_criteria>
  <done>ConfigRepo.delete() devuelve True/False correctamente en test; resolve_origin y build_candidate_settings existen e importan sin error.</done>
</task>

<task type="auto">
  <name>Task 2: Poblar ALL_SECTIONS con los 112 campos reales de Settings, en las 8 secciones fijas</name>
  <read_first>
    - backend/config.py (fuente de defaults/validators — ya en contexto de Task 1, no releer completo, solo confirmar líneas concretas si hace falta)
    - backend/api/v2/detection.py líneas 46-53 (AVAILABLE_CLASSES/LOCKED_CLASS_IDS — reutilizar tal cual para el tipo list_int de yolo_classes/object_class_ids)
  </read_first>
  <action>
    Poblar `ALL_SECTIONS` en `backend/api/v2/config_schema.py` con **exactamente** estos 112
    campos, agrupados en las 8 secciones fijas del UI-SPEC (`camara`, `deteccion`, `tracking`,
    `reconocimiento`, `zonas`, `reglas`, `alertas`, `almacenamiento`) y las subsecciones
    indicadas. Esta es la asignación completa y definitiva — no hay que re-derivarla desde
    `config.py`; los pocos campos sin sección obvia en el UI-SPEC (host/port/cors/ssl/dashboard
    auth) se resuelven aquí extendiendo "Cámara" con una tercera subsección "Servidor" (el
    UI-SPEC fija los 8 NOMBRES de sección, no las subsecciones — discreción del `32-CONTEXT.md`).

    Para cada campo: `type` "secret" cuando así se indica (nunca aparece su valor en el GET,
    ver `32-02`); `readonly` solo para `camera_url` (masked, ver Task 1); `applies="hot"`
    ÚNICAMENTE para `yolo_classes`, `process_width`, `process_height` (las tres únicas rutas
    reales — RESEARCH Pitfall 1); todo lo demás `"restart_camera"` si lo consume
    `CameraPipeline`/`DetectionWorker`/`RecordingWorker` o `"restart_server"` si lo consume el
    proceso completo (host/port/ssl/dashboard/cors/db_path/retención/alertas/metrics/gdrive).

    **Sección `camara`** (16 campos):
    - Grupo `captura`: `camera_url` (readonly, env=CAMERA_URL, label="URL RTSP de la cámara",
      applies=restart_camera) · `camera_driver` (enum tapo/generic, env=CAMERA_DRIVER,
      label="Tipo de cámara", default="tapo", applies=restart_server) · `rtsp_user` (secret,
      env=RTSP_USER, label="Usuario RTSP") · `rtsp_pass` (secret, env=RTSP_PASS, label="Contraseña RTSP")
      · `tapo_host` (str, env=TAPO_HOST, label="IP de la cámara (PTZ)", default="192.168.1.132",
      applies=restart_camera) · `tapo_user` (secret, env=TAPO_USER, label="Usuario Tapo (PTZ)")
      · `tapo_pass` (secret, env=TAPO_PASS, label="Contraseña Tapo (PTZ)")
    - Grupo `procesado`: `process_width` (int, env=PROCESS_WIDTH, label="Ancho de procesado",
      default=1280, min=320, max=1920, step=16, applies=hot) · `process_height` (int,
      env=PROCESS_HEIGHT, label="Alto de procesado", default=720, min=180, max=1080, step=16,
      applies=hot)
    - Grupo `servidor`: `host` (str, env=HOST, label="Dirección de escucha", default="0.0.0.0",
      applies=restart_server) · `port` (int, env=PORT, label="Puerto", default=8000, min=1,
      max=65535, applies=restart_server) · `cors_origins` (list_str, env=CORS_ORIGINS,
      label="Orígenes CORS permitidos", default=[], applies=restart_server) · `ssl_certfile`
      (secret, env=SSL_CERTFILE, label="Certificado SSL") · `ssl_keyfile` (secret,
      env=SSL_KEYFILE, label="Clave privada SSL") · `dashboard_user` (secret,
      env=DASHBOARD_USER, label="Usuario del dashboard") · `dashboard_pass` (secret,
      env=DASHBOARD_PASS, label="Contraseña del dashboard")

    **Sección `deteccion`** (19 campos):
    - Grupo `personas`: `yolo_model_path` (str, env=YOLO_MODEL_PATH,
      label="Modelo YOLO", default="yolo26n.pt", hint cita el validador de extensión+contención
      de config.py:42-54, applies=restart_camera) · `yolo_confidence` (float, env=YOLO_CONFIDENCE,
      label="Confianza mínima de detección", default=0.45, min=0.05, max=0.95, step=0.05,
      applies=restart_camera — NO "hot" pese a la tentación, RESEARCH Pitfall 1) · `yolo_classes`
      (list_int, env=YOLO_CLASSES, label="Clases detectadas", default=[0], applies=hot,
      reutilizar `AVAILABLE_CLASSES`/`LOCKED_CLASS_IDS` de `detection.py` como catálogo/lista
      bloqueada) · `detection_label` (str, env=DETECTION_LABEL, label="Etiqueta en el overlay",
      default="person", max_length=40, applies=restart_camera) · `yolo_imgsz` (int,
      env=YOLO_IMGSZ, label="Tamaño de inferencia (imgsz)", default=640, min=320, max=1280,
      step=32, applies=restart_camera)
    - Grupo `objetos`: `objects_enabled` (bool, env=OBJECTS_ENABLED, label="Detectar objetos",
      default=true, applies=restart_camera) · `object_class_ids` (list_int, env=OBJECT_CLASS_IDS,
      label="Clases de objeto vigiladas", default=[1,2,3,24,28], hint="ids COCO 0-79, sin la
      clase 0 (persona)", applies=restart_camera) · `object_left_secs` (float>0,
      env=OBJECT_LEFT_SECS, label="Segundos inmóvil para 'abandonado'", default=60.0,
      applies=restart_camera) · `object_still_radius_px` (float>0, env=OBJECT_STILL_RADIUS_PX,
      label="Radio de quietud (px)", default=20.0, applies=restart_camera) ·
      `object_person_radius_px` (float>0, env=OBJECT_PERSON_RADIUS_PX,
      label="Radio de proximidad a persona (px)", default=150.0, applies=restart_camera) ·
      `object_person_radius_ratio` (float [0,2], env=OBJECT_PERSON_RADIUS_RATIO,
      label="Ratio de proximidad sobre alto de persona", default=0.5, applies=restart_camera) ·
      `object_warmup_secs` (float>0, env=OBJECT_WARMUP_SECS,
      label="Segundos de calentamiento del tracker", default=10.0, applies=restart_camera) ·
      `object_gone_secs` (float>0, env=OBJECT_GONE_SECS,
      label="Segundos de gracia antes de 'retirado'", default=3.0, applies=restart_camera) ·
      `object_person_window_secs` (float>0, env=OBJECT_PERSON_WINDOW_SECS,
      label="Ventana de persona cercana (s)", default=10.0, applies=restart_camera) ·
      `object_max_tracks` (int>=1, env=OBJECT_MAX_TRACKS,
      label="Máximo de objetos en seguimiento", default=256, applies=restart_camera)
    - Grupo `contexto`: `context_baseline_days` (int [1,90], env=CONTEXT_BASELINE_DAYS,
      label="Días de referencia", default=7, applies=restart_camera) ·
      `context_min_sample_days` (int>=1, env=CONTEXT_MIN_SAMPLE_DAYS,
      label="Días mínimos de muestra", default=3, applies=restart_camera) · `context_low_ratio`
      (float, env=CONTEXT_LOW_RATIO, label="Umbral de actividad baja", default=0.5,
      hint="debe ser menor que el umbral alto", applies=restart_camera) · `context_high_ratio`
      (float, env=CONTEXT_HIGH_RATIO, label="Umbral de actividad alta", default=1.5,
      hint="debe ser mayor que el umbral bajo", applies=restart_camera)

    **Sección `tracking`** (16 campos):
    - Grupo `seguimiento`: `tracker_frame_rate` (int [1,60], env=TRACKER_FRAME_RATE,
      label="FPS inicial de seguimiento", default=15, applies=restart_camera) ·
      `detection_target_fps` (float [1,30], env=DETECTION_TARGET_FPS,
      label="FPS objetivo de detección", default=8.0, applies=restart_camera) ·
      `detection_min_fps` (float [1,30], env=DETECTION_MIN_FPS, label="FPS mínimo de detección",
      default=3.0, applies=restart_camera) · `detection_max_fps` (float [1,30],
      env=DETECTION_MAX_FPS, label="FPS máximo de detección", default=12.0,
      applies=restart_camera) · `recognition_target_fps` (float [0.5,10],
      env=RECOGNITION_TARGET_FPS, label="FPS objetivo de reconocimiento", default=2.0,
      applies=restart_camera) · `housekeeping_secs` (float [1,3600], env=HOUSEKEEPING_SECS,
      label="Intervalo de limpieza de tracks (s)", default=60.0, applies=restart_camera)
    - Grupo `comportamiento`: `behavior_enabled` (bool, env=BEHAVIOR_ENABLED,
      label="Detectar comportamientos", default=true, applies=restart_camera) · `loiter_secs`
      (float>0, env=LOITER_SECS, label="Segundos para 'merodeo'", default=120.0,
      applies=restart_camera) · `loiter_radius_px` (float>0, env=LOITER_RADIUS_PX,
      label="Radio de merodeo (px)", default=80.0, applies=restart_camera) ·
      `loiter_require_zone` (bool, env=LOITER_REQUIRE_ZONE, label="Exigir zona para merodeo",
      default=false, applies=restart_camera) · `run_speed_px_s` (float>0, env=RUN_SPEED_PX_S,
      label="Velocidad mínima de carrera (px/s)", default=350.0, applies=restart_camera) ·
      `run_window_secs` (float (0,12], env=RUN_WINDOW_SECS,
      label="Ventana de cálculo de velocidad (s)", default=1.0, max=12.0,
      hint="no puede superar 12.0 s — cota real del historial de tracking, config.py:359-365",
      applies=restart_camera) · `immobile_secs` (float>0, env=IMMOBILE_SECS,
      label="Segundos para 'inmóvil'", default=60.0, applies=restart_camera) ·
      `immobile_radius_px` (float>0, env=IMMOBILE_RADIUS_PX, label="Radio de inmovilidad (px)",
      default=20.0, applies=restart_camera) · `crowd_threshold` (int>=1, env=CROWD_THRESHOLD,
      label="Personas para 'aglomeración'", default=5, applies=restart_camera) ·
      `behavior_max_tracks` (int>=1, env=BEHAVIOR_MAX_TRACKS,
      label="Máximo de tracks con historial de comportamiento", default=256,
      applies=restart_camera)

    **Sección `reconocimiento`** (17 campos):
    - Grupo `rostro`: `face_min_size_px` (int [1,1000], env=FACE_MIN_SIZE_PX,
      label="Tamaño mínimo de rostro (px)", default=60, applies=restart_camera) ·
      `face_max_blur` (float [0,1000], env=FACE_MAX_BLUR, label="Desenfoque máximo admitido",
      default=100.0, applies=restart_camera) · `face_max_yaw_deg` (float [0,90],
      env=FACE_MAX_YAW_DEG, label="Ángulo de perfil máximo (°)", default=40.0,
      applies=restart_camera) · `face_match_threshold` (float (0,1], env=FACE_MATCH_THRESHOLD,
      label="Umbral de coincidencia facial", default=0.45, applies=restart_camera) ·
      `face_confirm_threshold` (float (0,1], env=FACE_CONFIRM_THRESHOLD,
      label="Umbral de confirmación facial", default=0.55, applies=restart_camera)
    - Grupo `identidad`: `identity_vote_window` (int, env=IDENTITY_VOTE_WINDOW,
      label="Ventana de votación (frames)", default=8,
      hint="debe ser >= votos mínimos para confirmar — config.py:313-318",
      applies=restart_camera) · `identity_min_votes` (int>=1, env=IDENTITY_MIN_VOTES,
      label="Votos mínimos para confirmar", default=3, applies=restart_camera) ·
      `identity_min_ratio` (float (0,1], env=IDENTITY_MIN_RATIO,
      label="Proporción mínima de votos", default=0.6, applies=restart_camera) ·
      `identity_lost_ttl_secs` (float>0, env=IDENTITY_LOST_TTL_SECS,
      label="Segundos antes de dar por perdida la identidad", default=30.0,
      applies=restart_camera) · `identity_revalidate_after_secs` (float>0,
      env=IDENTITY_REVALIDATE_AFTER_SECS, label="Segundos entre revalidaciones", default=120.0,
      applies=restart_camera)
    - Grupo `reid`: `reid_enabled` (bool, env=REID_ENABLED, label="Activar re-identificación",
      default=true, applies=restart_camera) · `reid_model_path` (str, env=REID_MODEL_PATH,
      label="Modelo de re-identificación", default="models/reid/osnet_x0_25_msmt17_dyn.onnx",
      hint cita el validador config.py:237-249, applies=restart_camera) ·
      `reid_inherit_window_secs` (float>0, env=REID_INHERIT_WINDOW_SECS,
      label="Ventana de herencia (s)", default=15.0, applies=restart_camera) ·
      `reid_similarity_threshold` (float (0,1], env=REID_SIMILARITY_THRESHOLD,
      label="Umbral de similitud", default=0.7, applies=restart_camera) ·
      `reid_interval_secs` (float>0, env=REID_INTERVAL_SECS,
      label="Intervalo mínimo entre inferencias (s)", default=2.0, applies=restart_camera) ·
      `reid_inherit_identity` (bool, env=REID_INHERIT_IDENTITY,
      label="Aplicar herencia automáticamente", default=false, applies=restart_camera) ·
      `reid_max_gallery_entries` (int>=1, env=REID_MAX_GALLERY_ENTRIES,
      label="Máximo de entradas en galería", default=256, applies=restart_camera)

    **Sección `zonas`** (4 campos):
    - Grupo `linea` (label "Línea de conteo"): `line_start_x_frac`, `line_start_y_frac`,
      `line_end_x_frac`, `line_end_y_frac` — los 4, float [0.0,1.0] step 0.01, envs
      LINE_START_X_FRAC/LINE_START_Y_FRAC/LINE_END_X_FRAC/LINE_END_Y_FRAC, labels
      "Inicio de línea — X/Y" y "Fin de línea — X/Y", defaults 0.0/0.5/1.0/0.5,
      applies=restart_camera (NO hot: la línea de conteo no está entre las 3 rutas reales,
      solo `set_zones()` de polígonos lo está).
    - Grupo `zonas_definidas` (label "Zonas definidas", solo lectura): `fields=()`,
      `external_source="/api/zones"` — sin FieldDef, el frontend (`32-06`) lo resuelve
      consultando ese endpoint ya existente al abrir esta subsección (Open Question 3 de
      RESEARCH). No construir aquí ninguna lógica de zonas nueva.

    **Sección `reglas`** (4 campos):
    - Grupo `horario` (label "Horario de acceso"): `schedule_enabled` (bool,
      env=SCHEDULE_ENABLED, label="Aplicar horario de acceso", default=false,
      applies=restart_camera) · `schedule_start` (time HH:MM, env=SCHEDULE_START,
      label="Hora de inicio", default="08:00", applies=restart_camera) · `schedule_end` (time
      HH:MM, env=SCHEDULE_END, label="Hora de fin", default="22:00", applies=restart_camera) ·
      `schedule_days` (list_int [0-6], env=SCHEDULE_DAYS, label="Días activos",
      default=[0,1,2,3,4], hint="0=lunes … 6=domingo", applies=restart_camera)
    - Grupo `reglas_cargadas` (label "Reglas cargadas", solo lectura): `fields=()`,
      `external_source="/api/v2/rules"` — igual que zonas_definidas, sin FieldDef.

    **Sección `alertas`** (8 campos):
    - Grupo `canales`: `alert_webhook_url` (secret, env=ALERT_WEBHOOK_URL,
      label="URL de webhook") · `alert_telegram_token` (secret, env=ALERT_TELEGRAM_TOKEN,
      label="Token de bot de Telegram") · `alert_telegram_chat_id` (str,
      env=ALERT_TELEGRAM_CHAT_ID, label="Chat ID de Telegram", default="",
      applies=restart_server — NO secret: identifica un chat, no autentica nada)
    - Grupo `disparadores`: `alert_on_intrusion` (bool, env=ALERT_ON_INTRUSION,
      label="Avisar en intrusión", default=true, applies=restart_server) ·
      `alert_on_unknown` (bool, env=ALERT_ON_UNKNOWN, label="Avisar en desconocido",
      default=true, applies=restart_server) · `alert_on_detection` (bool,
      env=ALERT_ON_DETECTION, label="Avisar en cada detección", default=false,
      applies=restart_server) · `alert_cooldown_secs` (float [0,3600],
      env=ALERT_COOLDOWN_SECS, label="Segundos entre avisos repetidos", default=60.0,
      applies=restart_server) · `alert_count_threshold` (int [0,1000],
      env=ALERT_COUNT_THRESHOLD, label="Umbral de conteo (0 = desactivado)", default=0,
      applies=restart_server)

    **Sección `almacenamiento`** (28 campos):
    - Grupo `grabacion` (label "Grabación"): `db_path` (str, env=DB_PATH,
      label="Ruta de la base de datos", default="data/events.db", applies=restart_server) ·
      `clips_dir` (str, env=CLIPS_DIR, label="Carpeta de clips", default="data/clips",
      applies=restart_camera) · `recording_fps` (float [1,30], env=RECORDING_FPS,
      label="FPS de grabación", default=15.0, applies=restart_camera) ·
      `recording_tail_secs` (float [0,60], env=RECORDING_TAIL_SECS,
      label="Cola tras última detección (s)", default=5.0, applies=restart_camera) ·
      `recording_codec` (enum mp4v/avc1, env=RECORDING_CODEC, label="Códec de vídeo",
      default="mp4v", applies=restart_camera) · `gdrive_folder_id` (str,
      env=GDRIVE_FOLDER_ID, label="ID de carpeta de Google Drive",
      default="1OJTWvYoHCDU28ZyzwlpOlongxs8lqWir", applies=restart_server) ·
      `gdrive_credentials_path` (secret, env=GDRIVE_CREDENTIALS_PATH,
      label="Credenciales de Google Drive") · `gdrive_token_path` (secret,
      env=GDRIVE_TOKEN_PATH, label="Token de Google Drive") · `max_upload_attempts`
      (int [1,20], env=MAX_UPLOAD_ATTEMPTS, label="Intentos máximos de subida", default=5,
      applies=restart_server) · `upload_poll_secs` (float [1,300], env=UPLOAD_POLL_SECS,
      label="Intervalo de sondeo de subida (s)", default=30.0, applies=restart_server)
    - Grupo `buffer`: `pre_buffer_secs` (float [0,60], env=PRE_BUFFER_SECS,
      label="Segundos de pre-buffer", default=10.0, applies=restart_camera) ·
      `post_buffer_secs` (float [0,60], env=POST_BUFFER_SECS,
      label="Segundos de post-buffer", default=10.0, applies=restart_camera) ·
      `pre_buffer_max_mb` (int [1,512], env=PRE_BUFFER_MAX_MB,
      label="Tamaño máximo del pre-buffer (MB)", default=48, applies=restart_camera) ·
      `pre_buffer_jpeg_quality` (int [1,100], env=PRE_BUFFER_JPEG_QUALITY,
      label="Calidad JPEG del pre-buffer", default=85, applies=restart_camera)
    - Grupo `retencion` (label "Retención"): `local_retention_days` (int [0,3650],
      env=LOCAL_RETENTION_DAYS, label="Retención local de clips (días)", default=7,
      applies=restart_server) · `events_retention_days` (int [0,3650],
      env=EVENTS_RETENTION_DAYS, label="Retención de eventos (días, 0=desactivado)",
      default=30, applies=restart_server) · `recordings_retention_days` (int [0,3650],
      env=RECORDINGS_RETENTION_DAYS, label="Retención de grabaciones (días, 0=desactivado)",
      default=30, applies=restart_server) · `persons_retention_days` (int [0,3650],
      env=PERSONS_RETENTION_DAYS, label="Retención de personas anónimas (días)", default=30,
      applies=restart_server) · `upload_min_severity` (enum info/warning/critical,
      env=UPLOAD_MIN_SEVERITY, label="Subir clips a Drive desde severidad",
      default="warning", applies=restart_camera — es el ajuste rápido #4 de `32-04`)
    - Grupo `capturas`: `gallery_dir` (str, env=GALLERY_DIR,
      label="Carpeta de galería de rostros", default="data/gallery",
      applies=restart_camera) · `gallery_throttle_secs` (float [0,3600],
      env=GALLERY_THROTTLE_SECS, label="Intervalo entre capturas de galería (s)",
      default=30.0, applies=restart_camera) · `snapshot_enabled` (bool,
      env=SNAPSHOT_ENABLED, label="Guardar snapshot de evento", default=true,
      applies=restart_camera) · `snapshot_dir` (str, env=SNAPSHOT_DIR,
      label="Carpeta de snapshots", default="data/snapshots", hint cita el validador de
      contención config.py:273-285, applies=restart_camera) · `snapshot_max_width`
      (int [64,1920], env=SNAPSHOT_MAX_WIDTH, label="Ancho máximo del snapshot (px)",
      default=320, applies=restart_camera) · `snapshot_min_interval_secs` (float [0,3600],
      env=SNAPSHOT_MIN_INTERVAL_SECS, label="Intervalo mínimo entre snapshots (s)",
      default=5.0, applies=restart_camera) · `snapshot_retention_days` (int [0,3650],
      env=SNAPSHOT_RETENTION_DAYS, label="Retención de snapshots (días, 0=sin purga)",
      default=30, applies=restart_camera)
    - Grupo `metricas` (label "Métricas"): `metrics_enabled` (bool, env=METRICS_ENABLED,
      label="Activar métricas Prometheus", default=true, applies=restart_server) ·
      `metrics_sample_secs` (float [1,60], env=METRICS_SAMPLE_SECS,
      label="Intervalo de muestreo (s)", default=5.0, applies=restart_server)

    Todos los `hint` no citados explícitamente arriba: frase corta en español llano derivada
    del comentario de `config.py` junto al campo (ya leído en este plan) — no dejar `hint=""`
    en ningún campo salvo que el propio código no tenga comentario alguno.
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -c "from backend.api.v2.config_schema import all_fields; f=all_fields(); print(len(f)); import collections; d=collections.Counter(x.key for x in f); print([k for k,v in d.items() if v>1])"</automated>
  </verify>
  <acceptance_criteria>
    `all_fields()` devuelve exactamente 112 `FieldDef` sin claves duplicadas. Cada `key`
    corresponde a un atributo real de `backend.config.Settings` (ninguna clave inventada, ninguna
    olvidada) — verificar con
    `.venv/Scripts/python.exe -c "from backend.config import Settings; from backend.api.v2.config_schema import all_fields; s=set(Settings.model_fields); c=set(f.key for f in all_fields()); print('faltan en schema:', s-c); print('sobran en schema:', c-s)"`
    y ambos conjuntos deben salir vacíos.
  </acceptance_criteria>
  <done>
    `all_fields()` devuelve exactamente 112 `FieldDef` sin claves duplicadas. Cada `key`
    corresponde a un atributo real de `backend.config.Settings` (ninguna clave inventada, ninguna
    olvidada) — verificar con
    `.venv/Scripts/python.exe -c "from backend.config import Settings; from backend.api.v2.config_schema import all_fields; s=set(Settings.model_fields); c=set(f.key for f in all_fields()); print('faltan en schema:', s-c); print('sobran en schema:', c-s)"`
    y ambos conjuntos deben salir vacíos.
  </done>
</task>

<task type="auto">
  <name>Task 3: Tests de config_schema.py y de ConfigRepo.delete()</name>
  <read_first>
    - tests/test_repositories.py (estilo de test existente sobre ConfigRepo/ZoneRepo/RuleRepo)
    - tests/test_detection_config_api.py (estilo de fixture y de assert usado en la Fase 27)
  </read_first>
  <action>
    Añadir a `tests/test_repositories.py` (o crear `tests/test_config_schema.py` si el fichero
    de repositorios no tiene ya una sección de `ConfigRepo` — usar
    `grep -n "class TestConfigRepo\|ConfigRepo" tests/test_repositories.py` para decidir):
    - `TEST_config_repo_delete_removes_existing_key`: `set()` una clave, `delete()` devuelve
      `True`, `get()` posterior devuelve el `default` pasado.
    - `TEST_config_repo_delete_missing_key_returns_false`: `delete()` sobre una clave nunca
      escrita devuelve `False` sin lanzar excepción.

    Crear `tests/test_config_schema.py`:
    - `TEST_all_fields_covers_every_settings_attribute`: el conjunto de `key` de `all_fields()`
      es exactamente `set(Settings.model_fields)` (ni de más ni de menos) — este es el test que
      hace irrefutable el criterio de "cobertura completa" de Task 2, no una aserción manual.
    - `TEST_all_fields_no_duplicate_keys`.
    - `TEST_resolve_origin_runtime_wins_over_env_and_default`: overrides con la clave presente
      -> `origin == "runtime"`.
    - `TEST_resolve_origin_env_when_differs_from_default`: `Settings` construido con un valor
      distinto del default (vía kwarg directo, no `.env` real) y overrides vacío ->
      `origin == "env"`.
    - `TEST_resolve_origin_default_when_matches`: `origin == "default"`.
    - `TEST_resolve_origin_list_field_env_vs_default` (Pitfall 4): repetir los tres casos
      anteriores para `yolo_classes` (list[int]) y `schedule_days`, comparando por contenido,
      no por identidad de objeto.
    - `TEST_resolve_origin_camera_url_always_masked`: `resolve_origin` para el campo
      `camera_url` con un `Settings` que tenga `rtsp_user`/`rtsp_pass` no vacíos en la URL
      construida NO devuelve credenciales en claro (usar `mask_rtsp_url` como oráculo de
      comparación, no una regex propia).
    - `TEST_build_candidate_settings_rejects_invalid_cross_field`: `changes={"identity_min_votes": 10, "identity_vote_window": 2}`
      debe lanzar `pydantic.ValidationError` (reproduce `config.py:313-318`).
    - `TEST_build_candidate_settings_accepts_valid_change`: un cambio válido no lanza y el
      candidato resultante tiene el valor nuevo.
    - `TEST_field_by_key_returns_none_for_unknown_key`.

    Cada `hint` del esquema no debe quedar vacío: `TEST_no_field_has_empty_hint` recorre
    `all_fields()` y falla si `field.hint == ""` para más de 5 campos (margen para los que
    genuinamente no tengan comentario en `config.py`, sin exigir el 100% literal).
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/test_config_schema.py tests/test_repositories.py -q</automated>
  </verify>
  <acceptance_criteria>Todos los tests nuevos pasan; ningún test existente de tests/test_repositories.py se rompe.</acceptance_criteria>
  <done>Todos los tests nuevos pasan; ningún test existente de tests/test_repositories.py se rompe.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| `config_schema.py` → futuros consumidores (32-02 router) | El esquema declara qué campos son `secret`/`readonly`; es la única fuente de verdad que decide qué puede salir del servidor. Un error aquí (marcar un campo secreto como editable/no-secreto) se propaga a todos los planes posteriores sin que ellos puedan detectarlo por sí solos. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-01 | Information Disclosure | `FieldDef` de `rtsp_pass`/`tapo_pass`/`dashboard_pass`/`ssl_certfile`/`ssl_keyfile`/`alert_webhook_url`/`alert_telegram_token`/`gdrive_credentials_path`/`gdrive_token_path` | mitigate | Los 9 campos llevan `secret=True` explícito en Task 2; `TEST_all_fields_covers_every_settings_attribute` (Task 3) + una aserción adicional que recorre `all_fields()` y falla si algún `key` que contenga "pass"/"token"/"secret"/"certfile"/"keyfile"/"credentials" no tiene `secret=True`, como red de seguridad mecánica contra un olvido futuro cuando `Settings` gane un campo nuevo |
| T-32-02 | Tampering | `build_candidate_settings` | mitigate | Reutiliza el constructor real de `Settings` (no reimplementa los `model_validator`) — imposible que el esquema acepte una combinación que el arranque del proceso rechazaría, por construcción |
| T-32-03 | Information Disclosure | `resolve_origin` para `camera_url` | mitigate | Enmascarado obligatorio vía `mask_rtsp_url()` antes de devolver el valor en cualquiera de las tres vías de origen, cubierto por `TEST_resolve_origin_camera_url_always_masked` |
| T-32-04 | Tampering | Rango inventado para campos sin `field_validator`/`model_validator` (A2) | accept | Riesgo bajo y explícitamente delegado por `32-CONTEXT.md` § Claude's Discretion; los rangos elegidos en Task 2 son conservadores (mismo orden de magnitud que los defaults) y no bloquean valores operativos razonables |

</threat_model>

<verification>
```bash
.venv/Scripts/python.exe -m pytest tests/test_config_schema.py tests/test_repositories.py -q
.venv/Scripts/python.exe -c "from backend.api.v2.config_schema import all_fields; from backend.config import Settings; s=set(Settings.model_fields); c=set(f.key for f in all_fields()); assert not (s-c) and not (c-s), (s-c, c-s); print('OK: 112 campos cubiertos 1:1')"
```
</verification>

<success_criteria>
- `ConfigRepo.delete()` existe, sigue el molde de `RuleRepo.delete()`/`ZoneRepo.delete()`, con dos tests verdes.
- `backend/api/v2/config_schema.py` existe con `FieldDef`/`Group`/`Section`/`ALL_SECTIONS`/`all_fields()`/`field_by_key()`/`resolve_origin()`/`build_candidate_settings()`.
- `all_fields()` cubre exactamente los 112 atributos de `Settings`, sin huecos ni duplicados, verificado por test (no por inspección manual).
- Todo campo `secret` real (9 campos) lleva `secret=True`.
- `applies="hot"` solo en `yolo_classes`, `process_width`, `process_height`.
- `resolve_origin` resuelve correctamente los tres estados para escalares y para listas (Pitfall 4 cerrado con test).
- `build_candidate_settings` usa el constructor de `Settings`, no `model_copy` (Assumption A1 resuelta).
- SUMMARY documenta el estado de la dependencia de la Fase 31 (Task 0).
</success_criteria>

<output>
After completion, create `.planning/phases/32-vista-de-c-mara-y-configuraci-n-visual/32-01-SUMMARY.md`
</output>
