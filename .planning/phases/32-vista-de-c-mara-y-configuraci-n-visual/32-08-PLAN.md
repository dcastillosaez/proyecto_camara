---
phase: 32-vista-de-c-mara-y-configuraci-n-visual
plan: 08
type: execute
wave: 7
depends_on: ["32-07"]
files_modified:
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
autonomous: false
requirements: [OPS-16, OPS-17, OPS-18, OPS-19, OPS-20, SET-01, SET-02, SET-03, SET-04]

must_haves:
  truths:
    - "Los 7 criterios de éxito del ROADMAP para la Fase 32 están trazados a un comando pytest -k que pasa, o a un paso del checkpoint visual"
    - "La suite completa del proyecto pasa en verde"
    - "El checkpoint visual confirma con navegador real las piezas que no tienen automatización posible (secretos nunca visibles, badges correctos, popover de restaurar, 422 legible)"
  artifacts: []
  key_links: []
---

<objective>
Puerta de fase: verificar que los 7 criterios de éxito del ROADMAP para la Fase 32 están
cerrados con evidencia (comando + resultado), que la suite completa sigue en verde tras las 7
waves anteriores, y levantar el checkpoint visual manual para las piezas que ningún test
automatizado de este proyecto puede cubrir (no hay runner JS — mismo patrón que las Fases
29/30/31). Marca `OPS-16..OPS-20`/`SET-01..04` en `REQUIREMENTS.md` y cierra la Fase 32 en
`ROADMAP.md`/`STATE.md`.

Purpose: cerrar la fase con la misma disciplina de trazabilidad criterio→test que las Fases
24-27/30 ya establecieron, sin dar nada por completo sin evidencia.
Output: `32-08-SUMMARY.md` con la tabla de trazabilidad, checkpoint visual firmado o
explícitamente diferido, `REQUIREMENTS.md`/`ROADMAP.md`/`STATE.md` actualizados.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/32-vista-de-c-mara-y-configuraci-n-visual/32-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Suite completa + trazabilidad criterio→test</name>
  <read_first>
    - .planning/ROADMAP.md sección "Phase 32" (los 7 criterios de éxito exactos)
    - .planning/REQUIREMENTS.md líneas 258-273 (OPS-16..20, SET-01..04)
  </read_first>
  <action>
    Ejecutar la suite completa: `.venv/Scripts/python.exe -m pytest tests/ -q`. Si algo falla,
    corregirlo antes de continuar (no ocultar fallos, per CLAUDE.md § Tests).

    Construir la tabla de trazabilidad de los 7 criterios del ROADMAP (Fase 32) a comandos
    `pytest -k` reales que pasan, o al paso del checkpoint visual de Task 2 cuando no exista
    automatización posible (secretos, popover, badges — verificación puramente visual):

    | # | Criterio (ROADMAP) | Evidencia |
    |---|---|---|
    | 1 | Vista Cámara con live view, FPS, latencia e2e, CPU, RAM, FPS detector, estado RTSP | `pytest tests/test_config_api.py tests/test_pipeline_v2.py -k health -q` (datos) + checkpoint Task 2 (visual) |
    | 2 | Árbol de configuración completo (8 secciones) | `pytest tests/test_config_api.py -k get_config -q` (112 campos, 8 secciones) |
    | 3 | Cambios persisten en app_config y se aplican en caliente cuando procede | `pytest tests/test_config_api.py -k "put_config or persists" -q` |
    | 4 | Valor fuera de rango -> 422 legible junto al campo | `pytest tests/test_config_api.py -k batch_validation_errors -q` + checkpoint Task 2 |
    | 5 | "Restaurar valores por defecto" por sección | `pytest tests/test_config_api.py -k restore_section -q` + checkpoint Task 2 |
    | 6 | Cada cambio genera CONFIG_CHANGED con el diff | `pytest tests/test_config_api.py -k config_changed_emits_diff -q` |
    | 7 | Precedencia app_config > .env > default, documentada y testeada | `pytest tests/test_config_schema.py -k resolve_origin -q` + esta misma tabla como documentación |

    Marcar en `.planning/REQUIREMENTS.md` `[x]` en OPS-16, OPS-17, OPS-18, OPS-19, OPS-20,
    SET-01, SET-02, SET-03, SET-04 (líneas 258-273) — solo si la evidencia de la tabla anterior
    y el checkpoint de Task 2 lo respaldan; si alguno queda pendiente de checkpoint diferido,
    dejarlo sin marcar y documentarlo explícitamente en el SUMMARY, mismo patrón que los 9
    checkpoints manuales previos del proyecto (nunca marcar un requisito sin evidencia real).
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/ -q</automated>
  </verify>
  <acceptance_criteria>Suite completa en verde; tabla de trazabilidad de 7 filas con comando real por fila; REQUIREMENTS.md refleja exactamente lo que tiene evidencia, ni más ni menos.</acceptance_criteria>
  <done>Suite completa en verde; tabla de trazabilidad de 7 filas con comando real por fila; REQUIREMENTS.md refleja exactamente lo que tiene evidencia, ni más ni menos.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Checkpoint visual de la vista Camara y Ajustes</name>
  <action>Seguir los pasos de how-to-verify con el servidor real y un navegador, y reportar el resultado en resume-signal.</action>
  <what-built>
    La vista Cámara (live view grande, tarjeta de estado RTSP con URL siempre enmascarada,
    6 teselas de métrica, barra de 4 ajustes rápidos) y la vista Ajustes (árbol de 8 secciones,
    ~112 filas de configuración con badges de origen/aplicación, guardado por sección con 422
    legible, restaurar por sección con popover, secretos nunca visibles).
  </what-built>
  <how-to-verify>
    Arrancar el servidor real: `.venv/Scripts/python.exe -m uvicorn backend.main:app --host 0.0.0.0 --port 8000`
    y abrir `http://localhost:8000/` con Basic Auth si está configurada.

    1. Pestaña "Cámara": el vídeo grande carga (o muestra "Cámara sin señal" con backoff activo
       si no hay cámara real accesible); las 6 teselas muestran valores o "–", nunca vacío; la
       tarjeta RTSP muestra la URL enmascarada (`***:***@...`), nunca usuario/contraseña en
       claro; cambiar el selector de resolución o el deslizador de confianza actualiza sin
       botón de guardar y sin recargar la página.
    2. Pestaña "Ajustes": el árbol muestra las 8 secciones; abrir "Reconocimiento" muestra tres
       `<fieldset>` (Rostro/Identidad temporal/Re-identificación); cada fila muestra label,
       variable de entorno en monoespaciada, rango, badge de origen y badge de aplicación.
    3. Abrir la subsección "Cámara → Captura": el campo `Usuario RTSP`/`Contraseña RTSP` muestra
       "Configurado"/"Sin configurar", nunca un valor ni asteriscos con longitud reveladora.
    4. Cambiar `Confianza mínima de detección` a un valor fuera de [0.05, 0.95] (forzando desde
       el campo numérico) y guardar: aparece el mensaje 422 literal bajo el control, el campo
       válido de al lado (si se tocó otro a la vez) sigue marcado como pendiente.
    5. Pulsar "Restaurar valores por defecto" en una sección con al menos un valor `runtime`:
       aparece el popover con el recuento exacto, nunca un `confirm()` del navegador; confirmar
       y comprobar que el badge de origen de esos campos vuelve a "Del fichero .env" o
       "Valor por defecto".
    6. Consola del navegador sin errores JS en ninguna de las dos pestañas.
    7. Layout de la vista Cámara sin scroll a 1366×768 (vídeo + ajustes rápidos completos, PTZ
       puede quedar bajo el pliegue).
  </how-to-verify>
  <resume-signal>Escribe "aprobado" o describe qué falló (con el paso exacto de la lista de arriba).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Ninguna nueva — puerta de fase, sin código de producción propio.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-22 | N/A | Puerta de fase | accept | No introduce superficie nueva; verifica lo ya mitigado en 32-01..32-07 |

</threat_model>

<verification>
```bash
.venv/Scripts/python.exe -m pytest tests/ -q
```
</verification>

<success_criteria>
- Suite completa en verde.
- Tabla de trazabilidad de 7 criterios con comando real por fila.
- Checkpoint visual firmado (o explícitamente diferido con motivo, mismo patrón que los 9 checkpoints previos del proyecto).
- `REQUIREMENTS.md`/`ROADMAP.md`/`STATE.md` reflejan el estado real, sin marcar nada sin evidencia.
</success_criteria>

<output>
After completion, create `.planning/phases/32-vista-de-c-mara-y-configuraci-n-visual/32-08-SUMMARY.md`
</output>
