---
phase: 32-vista-de-c-mara-y-configuraci-n-visual
plan: 03
type: execute
wave: 3
depends_on: ["32-02"]
files_modified:
  - frontend/css/components.css
  - tests/test_frontend_modules.py
autonomous: true
requirements: [OPS-16, OPS-17, OPS-18]

must_haves:
  truths:
    - "Existen las clases CSS .metric-tile, .rtsp-card, .cfg-tree, .cfg-node, .cfg-row, .cfg-badge, .cfg-applies, .cfg-savebar antes de que ningún módulo de vista las use"
    - "El interruptor .cam-toggle tiene un área de pulsación real de 44x44 (WCAG 2.2 AA 2.5.8), corrigiendo los 4 interruptores heredados de la Fase 11 de paso"
    - "components.css no supera el tope de 300 líneas de TEST_line_limit tras la extensión de esta fase y de la Fase 31"
  artifacts:
    - path: "frontend/css/components.css"
      provides: ".metric-tile .rtsp-card .cfg-tree .cfg-node .cfg-row .cfg-badge .cfg-applies .cfg-savebar, .cam-toggle::before de 44x44"
  key_links:
    - from: "frontend/js/views/camera.js (32-04)"
      to: "frontend/css/components.css"
      via: "classList con .metric-tile/.rtsp-card"
      pattern: "metric-tile|rtsp-card"
    - from: "frontend/js/views/settings-field.js (32-05)"
      to: "frontend/css/components.css"
      via: "classList con .cfg-row/.cfg-badge/.cfg-applies"
      pattern: "cfg-row|cfg-badge|cfg-applies"
---

<objective>
Añadir las 8 clases CSS nuevas que necesitan la vista Cámara (`.metric-tile`, `.rtsp-card`) y
la vista Ajustes (`.cfg-tree`, `.cfg-node`, `.cfg-row`, `.cfg-badge`, `.cfg-applies`,
`.cfg-savebar`) ANTES de que ningún módulo de vista las consuma — es la capa de contrato visual
que `32-04` y `32-05` importan como dado, mismo principio "interfaces primero" que los
contratos de backend de `32-01`. De paso corrige el área de pulsación del interruptor
`.cam-toggle` (20px de alto, por debajo de WCAG 2.2 AA 2.5.8) para los 4 interruptores que ya
existen desde la Fase 11 y para los nuevos de esta fase.

Purpose: `frontend/css/components.css` es un fichero COMPARTIDO por todas las vistas
(`LOCKED_CSS`, Fase 28) con un tope duro de 300 líneas (`TEST_line_limit`). Medía 163 líneas en
la investigación de esta fase (2026-08-23), pero la Fase 31 (que se ejecuta antes, según el
orden del roadmap) también escribe en el mismo fichero — el margen real solo se conoce en el
momento de ejecutar este plan, no en el momento de planificarlo (32-RESEARCH.md Pitfall 6).
Output: `components.css` extendido (o un fichero nuevo + `LOCKED_CSS` actualizado, si el margen
no alcanza), con las 8 clases del UI-SPEC listas para usar.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/32-vista-de-c-mara-y-configuraci-n-visual/32-UI-SPEC.md
@frontend/css/components.css
@tests/test_frontend_modules.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Medir margen real y añadir las 8 clases nuevas (.metric-tile, .rtsp-card, .cfg-*)</name>
  <read_first>
    - frontend/css/components.css (fichero completo — medir su longitud real primero, no asumir 163)
    - tests/test_frontend_modules.py (LOCKED_CSS, LINE_LIMIT=300, TEST_line_limit)
    - .planning/phases/32-vista-de-c-mara-y-configuraci-n-visual/32-UI-SPEC.md sección "Spacing Scale" y "Color" (medidas exactas de cada clase)
  </read_first>
  <action>
    Primero, medir el estado real del fichero en el momento de ejecutar (la Fase 31 puede haber
    añadido sus propias clases entre la planificación y la ejecución):
    ```bash
    wc -l frontend/css/components.css
    ```
    Si `163 + (líneas estimadas de las 8 clases nuevas, ~90-110) <= 300`: extender
    `components.css` tal cual (caso esperado). Si el margen no alcanza (p. ej. la Fase 31 ya
    dejó el fichero por encima de 190 líneas): crear `frontend/css/config.css` con las 8 clases
    nuevas, añadirlo a `LOCKED_CSS` en `tests/test_frontend_modules.py`, y añadir
    `<link rel="stylesheet" href="/static/css/config.css">` junto a los otros 3 `<link>` de
    `index.html` (mismo patrón de carga que `base.css`/`layout.css`/`components.css` — sin
    bundler, sin build step). Documentar en el SUMMARY cuál de las dos rutas se tomó y por qué,
    con la medición exacta.

    Añadir las 8 clases con las medidas EXACTAS del UI-SPEC (sección "Spacing Scale" / "Color" /
    Layout & Component Contracts), sin inventar valores:
    - `.metric-tile`: `padding: 12px` (`p-3`, NO los 10px/`p-2.5` heredados de "Salud del
      sistema" — UI-SPEC dice explícitamente no copiar ese legado), fondo `#1e293b`, sin color
      por métrica, sin umbral coloreado.
    - `.rtsp-card`: contenedor de la tarjeta de estado RTSP — fondo `#0f172a`, borde `#1e293b`,
      punto de estado de 8px (reutilizar los 3 colores del semáforo ya existente:
      `#4ade80`/`#f59e0b`/`#ef4444`, NO definir un cuarto estado).
    - `.cfg-tree`: contenedor `position: sticky; top: 112px`, ancho mínimo 200px, máximo 260px.
    - `.cfg-node`: caja de 32px, padding `8px 12px`, mismo patrón que `.nav-tab` de la Fase 31
      (si `.nav-tab` no existe aún en `components.css` en el momento de ejecutar — Fase 31 sin
      ejecutar — replicar las medidas del UI-SPEC directamente, sin depender de que exista
      `.nav-tab`); subnivel con `padding-left` +16px; nodo activo en azul acento
      `#3b82f6`/`#2563eb`; punto de 6px azul para "cambios sin guardar".
    - `.cfg-row`: altura MÍNIMA 56px (no fija — es la única fila del proyecto con altura
      variable, UI-SPEC lo señala explícitamente), `padding: 8px 0`, separador inferior
      `1px solid #1e293b`; modificador `.cfg-row.dirty` con barra izquierda azul de 2px y fondo
      `rgba(59,130,246,0.04)`; modificador `.cfg-row.error` con borde de control `#ef4444`.
    - `.cfg-badge`: badge de 20px de alto, 3 variantes de origen (`.cfg-badge--runtime` azul
      `#3b82f6` sobre `rgba(59,130,246,0.10)`, `.cfg-badge--env` slate `#64748b` sobre
      `#1e293b`, `.cfg-badge--default` slate `#475569` sin fondo).
    - `.cfg-applies`: mismo alto de 20px, 2 variantes (`.cfg-applies--hot` slate `#64748b`
      sobre `#1e293b`, `.cfg-applies--restart` ámbar `#f59e0b` sobre `rgba(245,158,11,0.10)`
      — reutilizar el mismo estilo visual para "Requiere reinicio" y "Reinicio del servidor",
      solo cambia el texto que pinta `settings-field.js`, no la clase).
    - `.cfg-savebar`: 56px de alto, `position: sticky; bottom: 0`,
      `backdrop-filter: blur(6px)` sobre `rgba(15,23,42,0.92)`.

    Además, en el mismo fichero, corregir `.cam-toggle` (definido ya en `components.css`,
    localizarlo con `grep -n "cam-toggle" frontend/css/components.css`): añadir un pseudo-elemento
    `::before` transparente de 44×44 centrado sobre la pista de 40×20 existente
    (`content: ''; position: absolute; inset: -12px -2px;` o equivalente que resulte en un área
    real de 44×44 centrada — ajustar los valores exactos al viewport real de `.cam-toggle`, no
    copiar ciegamente los números de ejemplo si la caja base no mide exactamente 40×20). No
    cambiar el tamaño visual de la pista (sigue siendo 40×20px), solo el área de pulsación.
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/test_frontend_modules.py -k line_limit -q</automated>
  </verify>
  <acceptance_criteria>Las 8 clases existen con las medidas exactas del UI-SPEC; `.cam-toggle::before` da un área real de 44x44 sin cambiar el tamaño visual de la pista; `TEST_line_limit` sigue en verde (o el nuevo fichero está en LOCKED_CSS si se tomó esa vía).</acceptance_criteria>
  <done>Las 8 clases existen con las medidas exactas del UI-SPEC; `.cam-toggle::before` da un área real de 44x44 sin cambiar el tamaño visual de la pista; `TEST_line_limit` sigue en verde (o el nuevo fichero está en LOCKED_CSS si se tomó esa vía).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Ninguna — este plan es CSS puro, sin datos de servidor, sin lógica, sin superficie de ataque
nueva.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-11 | N/A | `components.css` / `config.css` | accept | CSS estático sin datos de usuario ni de servidor; no aplica ninguna categoría STRIDE |

</threat_model>

<verification>
```bash
.venv/Scripts/python.exe -m pytest tests/test_frontend_modules.py -q
wc -l frontend/css/components.css
```
</verification>

<success_criteria>
- Las 8 clases `.metric-tile`/`.rtsp-card`/`.cfg-tree`/`.cfg-node`/`.cfg-row`/`.cfg-badge`/`.cfg-applies`/`.cfg-savebar` existen con las medidas exactas del UI-SPEC.
- `.cam-toggle` tiene área de pulsación real de 44x44 sin cambiar su tamaño visual.
- `TEST_line_limit` en verde para todo fichero de `LOCKED_CSS` (incluido uno nuevo si hizo falta).
- SUMMARY documenta la medición real de líneas y la decisión extender-vs-nuevo-fichero.
</success_criteria>

<output>
After completion, create `.planning/phases/32-vista-de-c-mara-y-configuraci-n-visual/32-03-SUMMARY.md`
</output>
