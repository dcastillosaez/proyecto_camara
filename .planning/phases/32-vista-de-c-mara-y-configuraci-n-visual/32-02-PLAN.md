---
phase: 32-vista-de-c-mara-y-configuraci-n-visual
plan: 02
type: execute
wave: 2
depends_on: ["32-01"]
files_modified:
  - backend/api/v2/config.py
  - backend/main.py
  - tests/test_config_api.py
autonomous: true
requirements: [OPS-18, OPS-19, OPS-20, SET-01, SET-02, SET-03, SET-04]

must_haves:
  truths:
    - "GET /api/v2/config devuelve las 8 secciones con label/hint/rango/origin/applies por campo, sin exponer ningún valor secret"
    - "PUT /api/v2/config valida el lote completo (rango por campo + invariantes cruzados de Settings) y devuelve todos los errores 422 a la vez, no el primero"
    - "PUT persiste antes de propagar y solo aplica en caliente yolo_classes/process_width+height; el resto queda marcado applies y persiste igual"
    - "Cada PUT o restore con éxito emite exactamente un CONFIG_CHANGED por sección, con diff, nunca con campos secret"
    - "POST /api/v2/config/{section}/restore borra solo las filas runtime de esa sección"
  artifacts:
    - path: "backend/api/v2/config.py"
      provides: "GET/PUT /api/v2/config, POST /api/v2/config/{section}/restore, configure()"
      exports: ["router", "configure"]
    - path: "tests/test_config_api.py"
      provides: "cobertura de GET, PUT (feliz + 422 batch + hot-apply + secret), restore, orden persistir-antes-de-propagar"
  key_links:
    - from: "backend/api/v2/config.py"
      to: "backend/api/v2/config_schema.py"
      via: "resolve_origin(), build_candidate_settings(), all_fields()/field_by_key()"
      pattern: "from backend.api.v2.config_schema import"
    - from: "backend/api/v2/config.py"
      to: "backend/events/engine.py"
      via: "EventEngine.config_changed(now, section=..., diff=...)"
      pattern: "config_changed\\("
    - from: "backend/main.py"
      to: "backend/api/v2/config.py"
      via: "configure(camera_manager, event_engine) + include_router en el lifespan"
      pattern: "config_v2_module.configure"
---

<objective>
Construir el único endpoint HTTP nuevo de la fase: `backend/api/v2/config.py`, con GET (lee
el esquema de `32-01` y resuelve valor/origin/máscara por campo), PUT (valida el lote completo,
persiste antes de propagar, aplica en caliente las 3 rutas reales, audita con `CONFIG_CHANGED`)
y `POST /{section}/restore` (borra overrides de una sección). Es el corazón de OPS-18, OPS-19,
OPS-20 y SET-01..04 — todas las vistas de frontend de waves posteriores son clientes puros de
este router, sin lógica de validación propia (D-04).

Purpose: sin este router, ninguna de las dos vistas (Cámara, Ajustes) tiene con qué hablar.
Output: `backend/api/v2/config.py` registrado en el lifespan de `main.py`, con tests de
integración que cubren los 10 comportamientos del mapa de tests de `32-RESEARCH.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/32-vista-de-c-mara-y-configuraci-n-visual/32-CONTEXT.md
@.planning/phases/32-vista-de-c-mara-y-configuraci-n-visual/32-RESEARCH.md
@backend/api/v2/config_schema.py
@backend/api/v2/detection.py
@backend/api/v2/alerts.py
@backend/pipeline/manager.py
@tests/test_detection_config_api.py
</context>

<interfaces>
<!-- Contrato que 32-04/32-05/32-06 (frontend) consumen literalmente. -->

```jsonc
// GET /api/v2/config  ->  200
{
  "sections": [
    {
      "key": "camara", "label": "Cámara",
      "groups": [
        {
          "key": "captura", "label": "Captura",
          "external_source": null,
          "fields": [
            {
              "key": "yolo_confidence", "env": "YOLO_CONFIDENCE",
              "label": "Confianza mínima de detección",
              "hint": "Por debajo de este valor YOLO descarta la detección.",
              "type": "float", "value": 0.45, "default": 0.45,
              "min": 0.05, "max": 0.95, "step": 0.05, "enum_values": null,
              "origin": "runtime", "applies": "restart_camera",
              "secret": false, "readonly": false, "configured": true
            }
          ]
        },
        { "key": "zonas_definidas", "label": "Zonas definidas", "external_source": "/api/zones", "fields": [] }
      ]
    }
  ]
}
```
Para `type="secret"`: el objeto de campo NO lleva `value` (clave ausente, nunca `null` que
podría confundirse con "vacío pero visible") — lleva `configured: bool(valor_no_vacío)` en su
lugar. `configured` está presente en TODOS los campos (secret o no) por uniformidad de
`settings-field.js`, pero solo importa cuando `secret == true`.

```jsonc
// PUT /api/v2/config  ->  body
{ "section": "deteccion", "changes": { "yolo_confidence": 0.5, "yolo_classes": [0, 24] } }

// 200 -> mismo shape que un grupo de GET, solo los campos de esa sección, valores frescos
{ "section": "deteccion", "fields": [ /* FieldValue[] de toda la sección, no solo los tocados */ ],
  "requires_restart": ["yolo_confidence"] }

// 422 -> TODOS los errores del lote a la vez
{ "errors": [
    { "field": "identity_min_votes", "message": "identity_vote_window (2) no puede ser menor que identity_min_votes (10): la votacion nunca alcanzaria el minimo" },
    { "field": "yolo_confidence", "message": "Debe estar entre 0.05 y 0.95." }
] }
```

```jsonc
// POST /api/v2/config/{section}/restore  ->  200
{ "section": "deteccion", "restored_count": 3, "fields": [ /* FieldValue[] frescos, sección completa */ ] }
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: GET /api/v2/config — esquema resuelto con origin/applies/secreto</name>
  <read_first>
    - backend/api/v2/config_schema.py (Task 2 de 32-01 — FieldDef/Section/Group/all_fields/resolve_origin)
    - backend/api/v2/detection.py (molde de router: imports, configure(), _config_repo(), decorador de rate limit)
    - backend/api/v2/deps.py (limiter, V2_RATE_LIMIT)
  </read_first>
  <behavior>
    - Con `app_config` vacío: cada campo resuelve `origin="default"` (o `"env"` si `Settings`
      difiere del default de fábrica) y `value` == valor efectivo de `Settings`.
    - Con una fila en `app_config` (`ConfigRepo.get_all()` devuelve `{"yolo_confidence": 0.6}`):
      ese campo resuelve `origin="runtime"`, `value=0.6`; el resto sigue por env/default.
    - Un campo `secret=True` (p. ej. `rtsp_pass`) nunca lleva la clave `value` en el JSON de
      salida, en ningún `origin`; lleva `configured: true` si el valor efectivo no está vacío.
    - `camera_url` sale siempre con `mask_rtsp_url()` aplicado, cualquiera que sea su `origin`.
    - Los grupos con `external_source` no nulo (`zonas_definidas`, `reglas_cargadas`) salen con
      `"fields": []` y su `external_source` intacto — el GET no consulta zonas ni reglas.
    - Response HTTP 200 con Content-Type JSON; rate-limited igual que el resto de `/api/v2/*`.
  </behavior>
  <action>
    Crear `backend/api/v2/config.py` con la cabecera de módulo explicando el propósito (molde:
    docstring de `detection.py`), imports:
    ```python
    from __future__ import annotations
    import datetime
    from typing import Any
    from fastapi import APIRouter, HTTPException, Request
    from pydantic import BaseModel, ValidationError
    from backend.api.v2.deps import V2_RATE_LIMIT, limiter
    from backend.api.v2.config_schema import (
        ALL_SECTIONS, all_fields, field_by_key, resolve_origin, build_candidate_settings,
    )
    from backend.config import get_settings, mask_rtsp_url
    from backend.database import get_session_factory
    from backend.storage.repositories import ConfigRepo

    router = APIRouter(prefix="/api/v2/config", tags=["config"])
    _camera_manager: Any = None
    _event_engine: Any = None

    def configure(camera_manager: Any, event_engine: Any = None) -> None:
        global _camera_manager, _event_engine
        _camera_manager = camera_manager
        _event_engine = event_engine

    def _config_repo() -> ConfigRepo:
        return ConfigRepo(get_session_factory())
    ```
    (Exactamente el molde de `detection.py:33-58`, sin desviarse — es el seam que los tests
    parchean con `patch.object(config_module, "_config_repo", return_value=fake_repo)`.)

    `_field_payload(f: FieldDef, overrides: dict, settings) -> dict`: aplica
    `resolve_origin(f, overrides, settings)`, y construye el dict del contrato de arriba. Si
    `f.secret`: omite `value`, añade `configured = bool(valor_efectivo)` sin volcarlo. Si
    `f.key == "camera_url"`: `value = mask_rtsp_url(valor_efectivo)`.

    `GET /api/v2/config`: una sola llamada a `_config_repo().get_all()`, una a `get_settings()`,
    recorre `ALL_SECTIONS` construyendo el JSON del contrato de arriba (grupos con
    `external_source` no nulo devuelven `fields: []`).
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/test_config_api.py -k get_config -q</automated>
  </verify>
  <acceptance_criteria>GET expone las 8 secciones, ningún campo secret lleva `value`, camera_url siempre enmascarada, origin correcto en los 3 escenarios del behavior.</acceptance_criteria>
  <done>GET expone las 8 secciones, ningún campo secret lleva `value`, camera_url siempre enmascarada, origin correcto en los 3 escenarios del behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PUT /api/v2/config — validación por lote, persistir-antes-de-propagar, hot-apply, auditoría</name>
  <read_first>
    - backend/api/v2/detection.py líneas 84-118 (put_classes — molde exacto de "persistir antes de propagar")
    - backend/pipeline/manager.py líneas 322-386 (set_zones/set_detection_classes/set_process_size — únicas 3 rutas hot)
    - backend/events/engine.py líneas 315-324 (config_changed)
  </read_first>
  <behavior>
    - `changes` con una clave que no existe en `all_fields()` -> 422, `{"field": key, "message": "Campo desconocido."}`.
    - `changes` con una clave `secret=True` o `readonly=True` -> 422, `{"field": key, "message": "Este campo no es editable desde la interfaz."}`.
    - `changes={"yolo_confidence": 1.5}` (fuera de [0.05, 0.95]) -> 422 con mensaje que cita el rango exacto del esquema.
    - `changes={"identity_min_votes": 10, "identity_vote_window": 2}` (viola el cruce de
      `config.py:313-318`) -> 422 con el mensaje LITERAL de `ValidationError` de `Settings`
      (D-10: "el cliente no traduce ni reescribe el mensaje" se aplica también aquí, aunque
      lo consuma settings-save.js, no un humano directo).
    - Lote con 2 errores (uno de rango, uno cruzado) -> el 422 trae los DOS en `errors`, no solo el primero.
    - `changes={"yolo_classes": [24]}` (sin la clase 0 bloqueada) -> 422 (reutilizar la MISMA regla que `detection.py:99-105`, no una nueva).
    - Camino feliz con `yolo_classes` -> `ConfigRepo.set` se llama ANTES que
      `pipeline.set_detection_classes` en TODAS las pipelines de `_camera_manager.all()`
      (test de orden con `parent.attach_mock`, molde `tests/test_detection_config_api.py:126-146`).
    - Camino feliz con `process_width` (sin `process_height` en el mismo PUT) -> hot-apply llama
      `pipeline.set_process_size(nuevo_width, alto_efectivo_actual)`, nunca `set_process_size(w, 0)`.
    - Camino feliz con un campo `applies="restart_camera"` -> se persiste iguialmente (D-08), NO
      se llama a ningún método de `CameraPipeline`, y la respuesta incluye la clave en
      `requires_restart`.
    - Éxito -> exactamente UNA llamada a `_event_engine.config_changed(now, section=..., diff={key: {"before":..., "after":...}})`, nunca una por campo.
    - Ningún campo `secret` puede llegar al `diff` (defensa en profundidad: ya es imposible por
      el rechazo 422 de arriba, pero el test lo verifica explícitamente).
  </behavior>
  <action>
    `class ConfigPutIn(BaseModel): section: str; changes: dict[str, Any]`.

    Algoritmo exacto de `PUT /api/v2/config` (un solo pase, sin cortocircuitar en el primer
    error — D-10):
    ```python
    @router.put("")
    @limiter.limit(V2_RATE_LIMIT)
    async def put_config(request: Request, body: ConfigPutIn) -> dict[str, Any]:
        section = next((s for s in ALL_SECTIONS if s.key == body.section), None)
        if section is None:
            raise HTTPException(404, detail=f"Sección desconocida: {body.section}")

        errors: list[dict[str, str]] = []
        valid_changes: dict[str, Any] = {}
        for key, value in body.changes.items():
            f = field_by_key(key)
            if f is None:
                errors.append({"field": key, "message": "Campo desconocido."}); continue
            if f.secret or f.readonly:
                errors.append({"field": key, "message": "Este campo no es editable desde la interfaz."}); continue
            per_field_error = _validate_range(f, value)   # min/max/enum_values/type — ver abajo
            if per_field_error:
                errors.append({"field": key, "message": per_field_error}); continue
            valid_changes[key] = value

        # Regla especial yolo_classes: reutilizar LITERALMENTE las 4 comprobaciones de
        # detection.py:88-105 (vacía, rango COCO, duplicados, clase 0 obligatoria) si
        # "yolo_classes" está en valid_changes -- no reimplementarlas con otra redaccion.

        settings = get_settings()
        overrides = await _config_repo().get_all()
        if valid_changes:
            try:
                build_candidate_settings(settings, overrides, valid_changes)
            except ValidationError as e:
                for err in e.errors():
                    fkey = str(err["loc"][0]) if err["loc"] else body.section
                    msg = err["msg"]
                    if not any(x["field"] == fkey for x in errors):
                        errors.append({"field": fkey, "message": msg})
                    # si el campo cruzado NO estaba en valid_changes (p.ej. el otro lado del
                    # par identity_vote_window/identity_min_votes no se tocó en este PUT),
                    # sacarlo de valid_changes igualmente para no persistir una mitad invalida
                    valid_changes.pop(fkey, None)

        if errors:
            raise HTTPException(422, detail={"errors": errors})

        diff: dict[str, dict[str, Any]] = {}
        requires_restart: list[str] = []
        for key, value in valid_changes.items():
            f = field_by_key(key)
            before = overrides[key] if key in overrides else getattr(settings, key)
            await _config_repo().set(key, value)     # PERSISTIR ANTES DE PROPAGAR
            diff[key] = {"before": before, "after": value}
            if f.applies != "hot":
                requires_restart.append(key)

        if _camera_manager is not None:
            for pipeline in _camera_manager.all():
                if "yolo_classes" in valid_changes:
                    pipeline.set_detection_classes(list(valid_changes["yolo_classes"]))
                if "process_width" in valid_changes or "process_height" in valid_changes:
                    w = valid_changes.get("process_width", overrides.get("process_width", settings.process_width))
                    h = valid_changes.get("process_height", overrides.get("process_height", settings.process_height))
                    pipeline.set_process_size(int(w), int(h))

        if _event_engine is not None and diff:
            _event_engine.config_changed(datetime.datetime.now(), section=body.section, diff=diff)

        fresh_overrides = await _config_repo().get_all()
        fields = [_field_payload(f, fresh_overrides, get_settings()) for grp in section.groups for f in grp.fields]
        return {"section": body.section, "fields": fields, "requires_restart": requires_restart}
    ```
    `_validate_range(field, value)` — devuelve `str | None`: comprueba tipo (`bool`/`int`/
    `float`/`str`), `min`/`max` si presentes, `enum_values` si `type=="enum"`, longitud de lista
    para `list_int`/`list_str`, sin reinventar rangos — usa exactamente `field.min`/`field.max`
    del esquema de `32-01`.
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/test_config_api.py -k "put_config" -q</automated>
  </verify>
  <acceptance_criteria>Los 10 comportamientos del <behavior> pasan; orden persistir-antes-de-propagar verificado con `attach_mock`; ningún secret llega al diff.</acceptance_criteria>
  <done>Los 10 comportamientos del <behavior> pasan; orden persistir-antes-de-propagar verificado con `attach_mock`; ningún secret llega al diff.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: POST /{section}/restore + wiring en main.py</name>
  <read_first>
    - backend/main.py líneas 580-773 (bloque de configure()/include_router del lifespan — insertar junto a detection_v2_module/alerts_v2_module)
    - backend/api/v2/config_schema.py (Group.external_source para excluir grupos de solo lectura del recuento)
  </read_first>
  <behavior>
    - Sección con 3 campos en `app_config` (origin=runtime) -> `restore` borra esas 3 filas,
      devuelve `restored_count: 3`, y un `CONFIG_CHANGED` con `restored=True`.
    - Sección sin ningún campo `origin=runtime` -> `restore` devuelve `restored_count: 0` sin
      llamar a `ConfigRepo.delete` ni a `config_changed` (nada que auditar).
    - `restore` sobre una sección desconocida -> 404.
    - Tras `restore`, el GET siguiente para esos campos vuelve a `origin="env"` o `"default"`
      según corresponda (no queda un valor congelado).
  </behavior>
  <action>
    ```python
    @router.post("/{section_key}/restore")
    @limiter.limit(V2_RATE_LIMIT)
    async def restore_section(request: Request, section_key: str) -> dict[str, Any]:
        section = next((s for s in ALL_SECTIONS if s.key == section_key), None)
        if section is None:
            raise HTTPException(404, detail=f"Sección desconocida: {section_key}")
        overrides = await _config_repo().get_all()
        keys = [f.key for grp in section.groups for f in grp.fields if f.key in overrides]
        for key in keys:
            await _config_repo().delete(key)
        if keys and _event_engine is not None:
            diff = {k: {"before": overrides[k], "after": None} for k in keys}
            _event_engine.config_changed(datetime.datetime.now(), section=section_key, diff=diff, restored=True)
        fresh_overrides = await _config_repo().get_all()
        fields = [_field_payload(f, fresh_overrides, get_settings()) for grp in section.groups for f in grp.fields]
        return {"section": section_key, "restored_count": len(keys), "fields": fields}
    ```

    Wiring en `backend/main.py`, dentro del bloque de lifespan que ya construye
    `camera_manager` (junto a las líneas 586-593):
    ```python
    from backend.api.v2 import config as config_v2_module
    config_v2_module.configure(camera_manager, event_engine)
    ```
    Y junto al bloque de `include_router` (líneas 762-772):
    ```python
    from backend.api.v2.config import router as config_v2_router
    app.include_router(config_v2_router)
    ```
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/test_config_api.py -q &amp;&amp; .venv/Scripts/python.exe -c "import backend.main"
</automated>
  </verify>
  <acceptance_criteria>Restore borra solo runtime, respeta secciones desconocidas con 404, `main.py` importa sin error tras registrar el router (arranque no roto).</acceptance_criteria>
  <done>Restore borra solo runtime, respeta secciones desconocidas con 404, `main.py` importa sin error tras registrar el router (arranque no roto).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Cliente HTTP autenticado -> `PUT/POST /api/v2/config` | Cualquier credencial Basic Auth válida puede reescribir 103 de los 112 parámetros del sistema (todos salvo los 9 `secret`); no hay roles (documentado también en `detection.py`). La única mitigación es auditoría + rate limit + validación estricta. |
| `PUT` body -> `Settings` candidato | El body es JSON arbitrario del cliente; solo debe alcanzar `Settings(**...)` tras pasar por `field_by_key`/`_validate_range`, nunca antes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-05 | Information Disclosure | `GET /api/v2/config` | mitigate | `_field_payload` omite `value` para todo campo `secret=True` (verificado por test, Task 1); `camera_url` siempre pasa por `mask_rtsp_url()` |
| T-32-06 | Elevation of Privilege | `PUT /api/v2/config` sobre un campo `secret`/`readonly` (p. ej. `dashboard_pass`, `ssl_certfile`) | mitigate | Rechazo explícito 422 antes de tocar `Settings`/`ConfigRepo` — ningún campo `secret`/`readonly` llega nunca a `valid_changes` (Task 2, cubierto por test) |
| T-32-07 | Tampering | Combinación de campos que el 422 por-campo aceptaría pero que `Settings` rechazaría al reiniciar | mitigate | `build_candidate_settings` reutiliza el constructor real de `Settings`, que re-ejecuta los `model_validator` — imposible persistir una combinación que el arranque rechazaría (RESEARCH Pattern 3, Pitfall 3) |
| T-32-08 | Repudiation | Cambios de configuración sin rastro | mitigate | Un `CONFIG_CHANGED` por `PUT`/`restore` con éxito, con diff completo salvo campos secret (que nunca llegan aquí, T-32-06) — única mitigación de repudio disponible sin roles, mismo criterio que la Fase 27 (T-27-20) |
| T-32-09 | Denial of Service | `PUT` masivo repetido contra 112 campos | accept | Rate limit compartido (`V2_RATE_LIMIT`, 60/min) ya aplicado a todo `/api/v2/*`; el payload está acotado por el número finito de campos del esquema |
| T-32-10 | Tampering | `yolo_classes` vacío o sin la clase 0 vía este router (bypass del router dedicado de la Fase 27) | mitigate | Reutiliza literalmente las 4 comprobaciones de `detection.py:88-105`, no una redacción paralela que pudiera divergir |

</threat_model>

<verification>
```bash
.venv/Scripts/python.exe -m pytest tests/test_config_api.py -q
.venv/Scripts/python.exe -m pytest tests/ -q
```
</verification>

<success_criteria>
- `GET /api/v2/config` devuelve las 8 secciones con origin/applies/secreto resueltos correctamente, sin fugas.
- `PUT /api/v2/config` valida el lote completo (rango + cruzado), persiste antes de propagar, aplica en caliente solo las 3 rutas reales, emite un único `CONFIG_CHANGED` por sección.
- `POST /{section}/restore` borra solo filas `runtime` de la sección indicada.
- Ningún valor `secret` sale nunca del servidor, ni en GET, ni en el diff de auditoría.
- `backend/main.py` importa y arranca sin error con el router nuevo registrado.
- Suite completa en verde.
</success_criteria>

<output>
After completion, create `.planning/phases/32-vista-de-c-mara-y-configuraci-n-visual/32-02-SUMMARY.md`
</output>
