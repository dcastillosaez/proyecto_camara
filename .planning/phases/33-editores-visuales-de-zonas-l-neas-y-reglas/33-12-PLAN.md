---
phase: 33-editores-visuales-de-zonas-l-neas-y-reglas
plan: 12
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/js/views/rules-form.js
  - frontend/js/views/rules-editor.js
autonomous: true
requirements: [OPS-24, RULE-05]

must_haves:
  truths:
    - "El operador compone una regla completa (condición + acciones) rellenando un formulario, sin editar YAML"
    - "El servidor valida la regla antes de guardarla; los errores 422 se muestran junto al campo que falló"
    - "El operador puede pulsar 'Probar regla' y ver cuántos de los últimos 500 eventos habrían disparado, sin guardar nada nuevo"
  artifacts:
    - path: "frontend/js/views/rules-form.js"
      provides: "renderWhenFields/renderActionFields — un renderer por campo de When/Action"
    - path: "frontend/js/views/rules-editor.js"
      provides: "initRulesEditor(), lista de reglas, guardar/borrar/probar contra /api/v2/rules"
  key_links:
    - from: "frontend/js/views/rules-editor.js"
      to: "frontend/js/views/rules-form.js"
      via: "import { renderWhenFields, renderActionFields }"
      pattern: "from '\\./rules-form\\.js'"
    - from: "frontend/js/views/rules-editor.js"
      to: "/api/v2/rules"
      via: "fetch GET/POST/DELETE + POST /{id}/test"
      pattern: "fetch\\('/api/v2/rules"
---

<objective>
Editor de reglas por formularios (OPS-24): `rules-form.js` (renderers de campo para
`When`/`Action`, imitando el patrón ya establecido de `settings-field.js` sin
importarlo — contexto DOM distinto) y `rules-editor.js` (orquestador: lista de reglas,
CRUD contra `/api/v2/rules`, botón "Probar regla" que llama `POST /{id}/test` y muestra
`would_fire/total_checked` — RULE-05).

Purpose: cierra OPS-24/RULE-05 por el lado del cliente. `config/rules.yaml` deja de ser
la única forma de crear una regla; el operador nunca necesita tocarlo tras esta fase
(aunque sigue existiendo como semilla inicial vía `scripts/generate_initial_rules.py`,
sin cambios en este plan). Sin dependencia de otros plans de esta fase: el contrato de
`/api/v2/rules` (Plan 33-06) ya está fijado por 33-RESEARCH.md/33-PATTERNS.md antes de
planificar, así que este módulo se construye contra ese contrato documentado —
verificable en el Plan 33-13 cuando ambos existan, sin bloquear su propia escritura.
Output: dos módulos nuevos (~250 + ~200 líneas), sin modal — panel inline dentro de la
vista Cámara (decisión documentada: evita gastar presupuesto de CSS en `.modal` cuando
el patrón de panel colapsable ya usado por `#add-zone-panel`/`#quick-classes` cubre la
necesidad sin CSS nueva).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-editores-visuales-de-zonas-l-neas-y-reglas/33-CONTEXT.md
@.planning/phases/33-editores-visuales-de-zonas-l-neas-y-reglas/33-RESEARCH.md
@.planning/phases/33-editores-visuales-de-zonas-l-neas-y-reglas/33-PATTERNS.md

<interfaces>
Renderers de campo a imitar (patrón, NO import — `settings-field.js` vive en otro
contexto DOM con `trackChange(sectionKey, field.key, value)`; este editor tiene su propio
estado local de formulario) — `frontend/js/views/settings-field.js:139-189` (código real):
```javascript
function _renderEnum(field, controlWrap, sectionKey) {
  const select = document.createElement('select');
  select.className = 'filter-input filter-select w-[140px]';
  for (const v of field.enum_values ?? []) {
    const opt = document.createElement('option');
    opt.value = v; opt.textContent = v;
    if (v === field.value) opt.selected = true;
    select.appendChild(opt);
  }
  select.addEventListener('change', () => trackChange(sectionKey, field.key, select.value));
  controlWrap.appendChild(select);
}
function _renderTime(field, controlWrap, sectionKey) {
  const input = document.createElement('input');
  input.type = 'time'; input.className = 'filter-input w-[96px]';
  input.value = field.value ?? '';
  input.addEventListener('change', () => trackChange(sectionKey, field.key, input.value));
  controlWrap.appendChild(input);
}
function _renderListInt(field, controlWrap, sectionKey) {
  const wrap = document.createElement('div');
  wrap.className = 'flex flex-wrap gap-1 justify-end';
  const active = new Set(field.value ?? []);
  ...
  for (const [id, label] of entries) {
    const chip = document.createElement('button');
    chip.type = 'button'; chip.className = 'filter-chip'; chip.textContent = label;
    ...
  }
}
```
Anti-XSS (cita textual del docstring de `settings-field.js`, aplicar idéntico aquí):
"createElement/textContent siempre, nunca innerHTML interpolando datos de servidor
(label/hint/env/mensaje 422)".

Modelo `When`/`Action` — `backend/events/rules.py` (sin cambios, solo consumo — mismos
campos que el Plan 33-06 valida en servidor):
```python
class When(BaseModel):
    event: EventType            # enum -> <select>
    zone: str | None = None     # <select> poblado desde GET /api/v2/zones (Plan 33-03)
    camera: str = "*"           # <input type=text>, "*" por defecto
    time_range: str | None = None   # dos <input type=time>
    days: list[int] | None = None   # chips 0-6, mismo patrón que schedule_days
    min_confidence: float | None = None   # <input type=number step=0.05 min=0 max=1>
    duration_gte: float | None = None     # <input type=number step=0.5 min=0>
    person: str | None = None             # <input type=text>, placeholder "unknown o nombre"
    payload: dict[str, Any] | None = None # fuera de alcance del formulario visual (Claude's
                                           # Discretion, 33-CONTEXT.md) — se omite del formulario,
                                           # el servidor lo acepta null

class Action(BaseModel):
    type: Literal["record","snapshot","notify","telegram","webhook","log","upload_drive","set_flag"]
    pre_secs: float | None = None
    post_secs: float | None = None
    template: str | None = None
    url_ref: str | None = None
```
`EventType` — importar sus valores reales del enum consultando `backend/events/types.py`
al implementar (no adivinar la lista; el formulario de `event` debe listar EXACTAMENTE
los valores que el backend acepta, o el 422 sería inevitable para cualquier valor no
listado).

Contrato del router — Plan 33-06 (se ejecuta en Wave 2, después de este plan; este
módulo se escribe contra el contrato ya fijado en 33-RESEARCH.md/33-PATTERNS.md, sin
dependencia real de ficheros entre ambos planes — se integran en el Plan 33-13):
`POST /api/v2/rules` body `{id, name, enabled, when: {...}, debounce_secs, actions:
[{...}]}`. Respuesta `{"rules": [...]}`. 422 shape: `{"detail": {"errors": [{"field":
"when.time_range", "message": "..."}]}}` según validación Pydantic anidada (path con
puntos). `DELETE /api/v2/rules/{id}`. `POST /api/v2/rules/{id}/test` → `{"would_fire": N,
"total_checked": M}`.

Patrón de mapeo de error 422 a fila — `frontend/js/views/settings-save.js:85-99` (código
real, mismo principio a aplicar aquí con selector propio, ej.
`[data-when-field="time_range"]` en vez de `.cfg-row[data-field-key=...]`):
```javascript
if (res.status === 422) {
  const body = await res.json().catch(() => ({}));
  const errors = body.detail?.errors ?? body.errors ?? [];
  for (const err of errors) {
    const row = panel?.querySelector(`.cfg-row[data-field-key="${err.field}"]`);
    if (!row) continue;
    setFieldError(row, err.message);
  }
}
```
Import de toasts: `import { showToast } from './dashboard.js';` (mismo directorio,
`views/`, a diferencia de `components/*.js` que suben un nivel).

Mount point real (lo prepara este plan, lo conecta el Plan 33-13): panel inline dentro
de Cámara — sin `.modal`. Ids a definir en cabecera: `#rules-panel`, `#rules-list`,
`#rule-new-btn`, `#rule-form`, `#rule-test-btn`, `#rule-test-result`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: rules-form.js — renderers de campo para When/Action</name>
  <files>frontend/js/views/rules-form.js</files>
  <read_first>
    frontend/js/views/settings-field.js completo (298 líneas — patrón de renderers a
    imitar, ya extractado en `<interfaces>`; releer completo para capturar también el
    manejo de `aria-invalid`/`aria-describedby` en estado de error 422, que este fichero
    debe replicar)
    backend/events/types.py (EventType enum — listar sus valores reales, no adivinar)
    backend/events/rules.py:24-43 (When/Action, ya extractado)
  </read_first>
  <behavior>
    Sin framework de test JS — verificación mecánica.
  </behavior>
  <action>
    Crear `frontend/js/views/rules-form.js` con `export function renderWhenFields(when,
    zoneOptions, onChange)` que construye y devuelve un `DocumentFragment` con un control
    por campo de `When` (select de `event` con los valores reales de `EventType`, select
    de `zone` poblado desde `zoneOptions` —lista que `rules-editor.js` obtiene de
    `GET /api/v2/zones`—, input de `camera`, dos `<input type=time>` para `time_range`
    —unidos al guardar como `"HH:MM-HH:MM"`—, chips 0-6 para `days`, dos `<input
    type=number>` para `min_confidence`/`duration_gte`, input de `person`), cada control
    con `data-when-field="<nombre>"` para que `rules-editor.js` pueda mapear errores 422
    por campo. `onChange(fieldName, value)` se llama en cada `change`/`input`, sin estado
    propio del módulo (el estado del formulario vive en `rules-editor.js`, este fichero
    es puramente de renderizado — mismo principio de separación que
    `settings-field.js`/`settings-save.js`).

    `export function renderActionFields(actions, onChange)`: lista editable de acciones
    (añadir/quitar), cada una con un `<select>` de `type` (8 valores `Literal`) y los
    campos condicionales que ese tipo usa (`pre_secs`/`post_secs` para `record`,
    `template` para `notify`/`telegram`, `url_ref` para `webhook`) — mostrar/ocultar
    campos según el `type` seleccionado, no renderizar los 4 siempre.

    Anti-XSS: `createElement`/`textContent` en todo el fichero, sin excepción.
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/test_frontend_modules.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "export function renderWhenFields" frontend/js/views/rules-form.js` la encuentra.
    - `grep -n "export function renderActionFields" frontend/js/views/rules-form.js` la encuentra.
    - `grep -n "data-when-field" frontend/js/views/rules-form.js` confirma el contrato de marcado para mapeo de errores 422.
    - `grep -n "innerHTML" frontend/js/views/rules-form.js` NO produce coincidencias.
    - `wc -l frontend/js/views/rules-form.js` reporta ≤300.
  </acceptance_criteria>
  <done>Renderers de campo completos para When/Action, con el mismo rigor anti-XSS que settings-field.js, sin importarlo.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: rules-editor.js — CRUD + Probar regla contra /api/v2/rules</name>
  <files>frontend/js/views/rules-editor.js</files>
  <read_first>
    frontend/js/views/settings-save.js completo (180 líneas — patrón de fetch/422-mapping/
    toast a replicar con selector propio, ya extractado en `<interfaces>`)
    frontend/js/views/rules-form.js de la Task 1 (firmas exactas de renderWhenFields/renderActionFields)
  </read_first>
  <behavior>
    - `loadRules()` pinta `#rules-list` desde `GET /api/v2/rules`, nombre/estado por `textContent`.
    - `openRuleForm(rule?)` monta `renderWhenFields`/`renderActionFields` en `#rule-form`
      con el estado inicial de la regla (o vacío para una nueva).
    - `saveRule()` reúne el estado del formulario, hace `POST /api/v2/rules`; en 422,
      mapea cada `{field,message}` al control `[data-when-field="<field sin prefijo
      'when.'>"]` más cercano y muestra el mensaje (mismo principio que
      `setFieldError` de `settings-field.js`, reimplementado localmente sin importar ese
      módulo — contexto DOM distinto); en éxito, toast + `loadRules()` + cierra el formulario.
    - `testRule(ruleId)` hace `POST /api/v2/rules/{id}/test`, muestra
      `"N de M eventos recientes habrían disparado esta regla"` en `#rule-test-result`
      (textContent), sin guardar nada ni cerrar el formulario.
    - `deleteRule(id, name)` pide `confirm()`, `DELETE /api/v2/rules/{id}`, repinta.
    - `initRulesEditor()` es el punto de entrada único (mismo patrón lazy que
      `initZoneEditor()`/`initLineEditor()`): cachea la lista de zonas (`GET
      /api/v2/zones`, una sola vez) para poblar el `<select>` de `zone` en
      `renderWhenFields`, sin refetch en cada apertura de formulario.
  </behavior>
  <action>
    Implementar según el `<behavior>` de arriba, mismo estilo de código que
    `settings-save.js` (try/fetch/status-check/toast), importando
    `{ renderWhenFields, renderActionFields }` de `./rules-form.js` y `{ showToast }` de
    `./dashboard.js`.
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/test_frontend_modules.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "export function initRulesEditor" frontend/js/views/rules-editor.js` la encuentra.
    - `grep -n "fetch('/api/v2/rules" frontend/js/views/rules-editor.js` confirma el endpoint base.
    - `grep -n "/test" frontend/js/views/rules-editor.js` confirma la llamada a `POST /{id}/test`.
    - `grep -n "from './rules-form.js'" frontend/js/views/rules-editor.js` confirma la separación de módulos.
    - `wc -l frontend/js/views/rules-editor.js` reporta ≤300.
    - `pytest tests/test_frontend_modules.py -q` termina en verde.
  </acceptance_criteria>
  <done>Editor de reglas funcional: crear/editar/borrar/probar contra /api/v2/rules, con mapeo de errores 422 por campo.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Servidor → DOM (nombre de regla, mensajes 422, resultado de /test) | Debe entrar siempre por `textContent`, nunca `innerHTML` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-15 | Tampering (XSS reflejado) | Nombre de regla o mensaje de error 422 con caracteres HTML | mitigate | `createElement`+`textContent` en ambos módulos |
| T-33-16 | Denial of Service (cliente) | Pulsar "Probar regla" repetidamente | accept | El servidor ya limita con `V2_RATE_LIMIT` (Plan 33-06); el cliente no necesita debounce adicional para una acción manual de un solo operador |
</threat_model>

<verification>
```bash
.venv/Scripts/python.exe -m pytest tests/test_frontend_modules.py -q
```
Sin framework de test JS — el comportamiento real contra `/api/v2/rules` (que existe
desde el Plan 33-06, Wave 2) se verifica en el checkpoint manual del Plan 33-14.
</verification>

<success_criteria>
- Composición completa de regla por formulario, sin YAML.
- Errores 422 visibles junto al campo exacto.
- "Probar regla" muestra el conteo real sin persistir nada nuevo ni alterar el debounce de producción.
</success_criteria>

<output>
Al terminar, crear `.planning/phases/33-editores-visuales-de-zonas-l-neas-y-reglas/33-12-SUMMARY.md`
</output>
