---
phase: 33-editores-visuales-de-zonas-l-neas-y-reglas
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tracker.py
  - backend/events/engine.py
  - tests/test_tracker.py
  - tests/test_phase9.py
  - tests/test_detection_worker.py
autonomous: true
requirements: [OPS-22]

must_haves:
  truths:
    - "PersonTracker puede llevar el conteo de cruce de N líneas independientes, no solo una"
    - "Cada cruce emitido identifica de qué línea proviene (line_id/line_name), no solo la dirección"
    - "Sustituir la lista de líneas en caliente no pierde los IDs de tracking activos (ByteTrack se comparte, no se recrea)"
  artifacts:
    - path: "backend/tracker.py"
      provides: "PersonTracker con lista de LineZone (reconfigure_lines), get_counts() por línea"
    - path: "backend/events/engine.py"
      provides: "emit_line_crossing incluye line_id/line_name en el payload"
  key_links:
    - from: "backend/tracker.py:PersonTracker.update"
      to: "backend/tracker.py:PersonTracker._lines (list[dict])"
      via: "bucle sobre self._lines en vez de un unico self._line_zone"
      pattern: "for .* in self\\._lines"
    - from: "backend/events/engine.py:emit_line_crossing"
      to: "payload\\[.line_id.\\]"
      via: "crossing dict ya trae line_id desde PersonTracker.update"
      pattern: "line_id"
---

<objective>
Refactorizar `PersonTracker` (`backend/tracker.py`) de una única `sv.LineZone` fija a una
lista de líneas independientes con conteo propio por línea (D-01, OPS-22 — "líneas" en
plural). ByteTrack y el suavizado de detecciones se mantienen COMPARTIDOS (una persona
cruza varias líneas con la misma identidad); solo el `LineZone` + contadores se vuelven
por-línea, siguiendo el mismo molde de lista-de-estado que `_zone_states` ya usa en
`backend/pipeline/detection.py` para zonas.

Purpose: es el cambio de forma que D-01/Pitfall 4 de 33-RESEARCH.md exige antes de que
cualquier CRUD de líneas (Plan 33-07) tenga sentido — sin esto, el editor visual solo
podría dibujar una línea, incumpliendo el plural de OPS-22.
Output: `PersonTracker.reconfigure_lines(lines: list[dict])` (reemplaza
`reconfigure_line(start, end)`), `get_counts()` devuelve un dict por línea, crossings
incluyen `line_id`/`line_name`, tests existentes migrados + tests nuevos de N líneas.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-editores-visuales-de-zonas-l-neas-y-reglas/33-CONTEXT.md
@.planning/phases/33-editores-visuales-de-zonas-l-neas-y-reglas/33-RESEARCH.md
@.planning/phases/33-editores-visuales-de-zonas-l-neas-y-reglas/33-PATTERNS.md

<interfaces>
`backend/tracker.py` completo (204 líneas, código real ACTUAL — ver `<context>` de
33-RESEARCH.md para el resto del fichero, `ObjectTracker` NO se toca):
```python
class PersonTracker:
    CROSSING_THRESHOLD = 2
    LOST_TRACK_BUFFER = 60

    def __init__(self, start: sv.Point, end: sv.Point, frame_rate: int = 15) -> None:
        self._byte_tracker = sv.ByteTrack(lost_track_buffer=self.LOST_TRACK_BUFFER, frame_rate=frame_rate)
        self._smoother = sv.DetectionsSmoother(length=5)
        self._line_zone = sv.LineZone(start=start, end=end,
            triggering_anchors=[sv.Position.BOTTOM_CENTER],
            minimum_crossing_threshold=self.CROSSING_THRESHOLD)
        self._box_annotator = sv.BoxAnnotator(thickness=1)
        self._label_annotator = sv.LabelAnnotator(text_scale=0.5, text_thickness=1)
        self._trace_annotator = sv.TraceAnnotator(trace_length=30, thickness=1)
        self._in_count = 0
        self._out_count = 0
        self._crossed_ids: set[int] = set()
        self._lock = threading.Lock()

    def update(self, detections: sv.Detections) -> tuple[sv.Detections, list[dict[str, Any]]]:
        tracked = self._byte_tracker.update_with_detections(detections)
        if tracked.tracker_id is not None:
            tracked = self._smoother.update_with_detections(tracked)
        crossed_in, crossed_out = self._line_zone.trigger(tracked)
        ids = tracked.tracker_id if tracked.tracker_id is not None else []
        crossings: list[dict[str, Any]] = []
        now = datetime.datetime.now()
        with self._lock:
            for i, tid in enumerate(ids):
                if crossed_in[i]:
                    self._in_count += 1
                    self._crossed_ids.add(int(tid))
                    crossings.append({"direction": "in", "timestamp": now, "tracker_id": int(tid)})
                elif crossed_out[i]:
                    self._out_count += 1
                    self._crossed_ids.add(int(tid))
                    crossings.append({"direction": "out", "timestamp": now, "tracker_id": int(tid)})
        return tracked, crossings

    def get_counts(self) -> dict[str, int]:
        with self._lock:
            return {"in": self._in_count, "out": self._out_count, "total": len(self._crossed_ids)}

    def reconfigure_line(self, start: sv.Point, end: sv.Point) -> None:
        with self._lock:
            self._line_zone = sv.LineZone(start=start, end=end,
                triggering_anchors=[sv.Position.BOTTOM_CENTER],
                minimum_crossing_threshold=self.CROSSING_THRESHOLD)
```

`emit_line_crossing` — `backend/events/engine.py:88-101` (código real):
```python
def emit_line_crossing(self, crossing: dict[str, Any], captured_at=None, processed_at=None) -> None:
    self._publish(
        EventType.LINE_CROSSED, ts=crossing["timestamp"],
        captured_at=captured_at, processed_at=processed_at,
        track_id=crossing.get("tracker_id"),
        payload={"direction": crossing["direction"], "is_intrusion": bool(crossing.get("is_intrusion", False))},
    )
```

Consumidor en `backend/pipeline/detection.py:471-478` (NO se toca en este plan — solo
referencia; el Plan 33-05 lo extiende para el hot-reload de líneas):
```python
def _emit_crossings(self, crossings: list[dict], captured_at: float, processed_at: float) -> None:
    if not crossings or self._event_engine is None:
        return
    is_intrusion = bool(self._is_intrusion()) if self._is_intrusion else False
    for c in crossings:
        self._event_engine.emit_line_crossing({**c, "is_intrusion": is_intrusion}, captured_at, processed_at)
```
`{**c, ...}` ya propaga cualquier clave nueva que `PersonTracker.update()` añada al dict
de crossing — no hace falta tocar `_emit_crossings`, solo `emit_line_crossing` para que
lea `line_id`/`line_name` del dict y los meta en el payload.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PersonTracker — de una línea a N líneas con conteo independiente</name>
  <files>backend/tracker.py, tests/test_tracker.py, tests/test_phase9.py</files>
  <read_first>
    backend/tracker.py completo (204 líneas, ya extractado en `<interfaces>` — releer
    igualmente `ObjectTracker`, líneas 155-205, para NO tocarlo ni copiar su patrón por error)
    tests/test_tracker.py completo (patrón de test existente: `PersonTracker(start=sv.Point(0,360), end=sv.Point(1280,360))`)
    tests/test_phase9.py:1-60 (mismo patrón de construcción, fixture `tracker()`)
    backend/pipeline/detection.py:34,55-93 (cómo DetectionWorker consume tracker.update(),
    NO se toca en esta tarea — solo para entender el contrato de retorno que no puede romperse)
  </read_first>
  <behavior>
    - `PersonTracker(frame_rate=15)` sin líneas iniciales: `update()` no lanza excepción,
      `get_counts()` devuelve `{}` (sin líneas configuradas).
    - `PersonTracker(frame_rate=15, lines=[{"id":"l1","name":"Entrada","start":sv.Point(0,300),"end":sv.Point(640,300)}])`:
      `get_counts()` devuelve `{"l1": {"name": "Entrada", "in": 0, "out": 0, "total": 0}}`.
    - Dos líneas configuradas, una persona cruza solo la línea `l1`: el crossing dict
      resultante trae `"line_id": "l1", "line_name": "Entrada"`; `get_counts()["l2"]` sigue
      en `{"in":0,"out":0,"total":0}`.
    - Una persona cruza dos líneas distintas en el mismo frame: `update()` devuelve DOS
      entradas en `crossings` (una por línea), con el MISMO `tracker_id` en ambas (ByteTrack
      compartido, identidad no se duplica).
    - `reconfigure_lines([...])` sustituye la lista completa de líneas SIN recrear
      `self._byte_tracker` (un track activo antes de la llamada sigue vivo después,
      verificable llamando `update()` con las mismas detecciones antes/después y
      comprobando que `tracker_id` no cambia).
    - `annotate()` sigue dibujando sin excepción con 0, 1 o N líneas configuradas.
  </behavior>
  <action>
    Modificar `PersonTracker.__init__` para aceptar `lines: list[dict[str, Any]] | None =
    None` en vez de `start`/`end` obligatorios (parámetro nuevo, sin valor por defecto
    posicional que rompa `frame_rate` — mantener `frame_rate` como kwarg con su default
    actual). Cada dict de línea: `{"id": str, "name": str, "start": sv.Point, "end":
    sv.Point}`. Construir `self._lines: list[dict[str, Any]]` donde cada entrada añade
    `"zone": sv.LineZone(start=..., end=..., triggering_anchors=[sv.Position.BOTTOM_CENTER],
    minimum_crossing_threshold=self.CROSSING_THRESHOLD)`, `"in_count": 0`, `"out_count": 0`,
    `"crossed_ids": set()`. Si `lines` es `None`, `self._lines = []`.

    `reconfigure_lines(self, lines: list[dict[str, Any]]) -> None`: bajo `self._lock`,
    reconstruye `self._lines` desde cero con el mismo shape que `__init__`, CONSERVANDO
    `in_count`/`out_count`/`crossed_ids` de cada línea cuyo `id` ya existiera en
    `self._lines` (para no perder el conteo acumulado al mover un vértice de esa línea o
    al reordenar la lista) — solo resetea contadores para líneas realmente nuevas.
    `self._byte_tracker`/`self._smoother` NO se tocan (se mantiene el comentario ya
    existente sobre por qué recrear ByteTrack perdería tracks).

    `update()`: sustituir la única llamada `self._line_zone.trigger(tracked)` por un
    bucle `for line in self._lines: crossed_in, crossed_out = line["zone"].trigger(tracked)`
    acumulando en `crossings` una entrada por cada cruce CON `"line_id": line["id"],
    "line_name": line["name"]` añadidos a cada dict de crossing (además de
    `direction`/`timestamp`/`tracker_id` que ya existían). Los contadores `in_count`/
    `out_count`/`crossed_ids` pasan a vivir dentro de cada dict de `self._lines`, no como
    atributos sueltos de la instancia.

    `get_counts()`: devuelve `{line["id"]: {"name": line["name"], "in": line["in_count"],
    "out": line["out_count"], "total": len(line["crossed_ids"])} for line in self._lines}`
    bajo `self._lock`.

    `annotate()`: el bucle que dibuja la línea (buscar la línea existente que dibuja
    `self._line_zone` en el frame — probablemente vía un `LineZoneAnnotator` de
    supervision si existe, o dibujo manual; verificar en el fichero real qué anotador usa
    hoy) pasa a iterar `self._lines` y dibujar cada una. Si no existe ya un
    `sv.LineZoneAnnotator`, no añadirlo en este plan (fuera de alcance de este task —
    el indicador visual de dirección para el editor vive en el CANVAS del frontend,
    Plan 33-11, no en el overlay MJPEG del backend).

    Eliminar `reconfigure_line` (singular) — ya no tiene sentido con N líneas. Buscar y
    actualizar su único llamador real, `backend/camera.py` (NO tocarlo en este plan — el
    Plan 33-05 lo actualiza; si `reconfigure_line` desaparece antes de que 33-05 corra,
    dejar temporalmente una nota `# TODO(33-05)` no es aceptable — en su lugar, mantener
    `reconfigure_line(start, end)` como wrapper de compatibilidad que llama
    `self.reconfigure_lines([{"id": "_legacy", "name": "Linea", "start": start, "end":
    end}])` SOLO hasta que 33-05 lo retire explícitamente, documentado con un comentario
    corto).

    Actualizar `tests/test_tracker.py` y `tests/test_phase9.py`: cambiar toda construcción
    `PersonTracker(start=sv.Point(...), end=sv.Point(...))` a
    `PersonTracker(lines=[{"id": "l1", "name": "Linea 1", "start": sv.Point(...), "end":
    sv.Point(...)}])`, y cualquier assert sobre `tracker.get_counts()["in"]` a
    `tracker.get_counts()["l1"]["in"]`. Añadir los tests nuevos del `<behavior>` (N líneas,
    conservación de conteo en reconfigure_lines, tracker_id compartido).
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/test_tracker.py tests/test_phase9.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "self._lines" backend/tracker.py` confirma la lista, no `self._line_zone` como único estado (puede seguir existiendo como wrapper de compatibilidad documentado).
    - `grep -n "def reconfigure_lines" backend/tracker.py` confirma el método plural nuevo.
    - `grep -n "line_id" backend/tracker.py` confirma que los crossings incluyen la clave.
    - `pytest tests/test_tracker.py tests/test_phase9.py -q` termina en verde.
  </acceptance_criteria>
  <done>PersonTracker soporta N líneas con conteo independiente, conserva ByteTrack compartido, y los tests existentes están migrados sin perder cobertura.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: emit_line_crossing propaga line_id/line_name + migracion de PersonTracker(...) en test_detection_worker.py</name>
  <files>backend/events/engine.py, tests/test_detection_worker.py</files>
  <read_first>
    backend/events/engine.py:1-102 (emit_line_crossing completo, ya extractado en `<interfaces>`)
    tests/test_event_engine.py (test existente de emit_line_crossing — releer para no
    romper su assert sobre las claves YA existentes del payload, `direction`/`is_intrusion`)
    tests/test_detection_worker.py completo (grep `PersonTracker(` primero — IMPORTANTE:
    la Task 1 de este plan SOLO migra `tests/test_tracker.py` y `tests/test_phase9.py`; NO
    toca este fichero. Este fichero tiene 4 construcciones posicionales sin migrar,
    localizables por grep: `PersonTracker(line_start, line_end, frame_rate=15)` (dos veces,
    en `TEST_object_class_does_not_reach_line_zone`) y `PersonTracker(sv.Point(...),
    sv.Point(...), frame_rate=15)` (en `TEST_objects_not_in_registry` y
    `TEST_bytetrack_ids_do_not_migrate_between_classes`) — con la nueva firma
    `__init__(self, lines: list[dict] | None = None, frame_rate: int = 15)` estas 4
    llamadas fallan con `TypeError` por exceso de argumentos posicionales; esta tarea DEBE
    migrarlas, no es opcional)
  </read_first>
  <behavior>
    - `emit_line_crossing({"direction":"in","timestamp":now,"tracker_id":7,"line_id":"l1","line_name":"Entrada","is_intrusion":False})` publica un evento cuyo `payload` incluye `"line_id":"l1"` y `"line_name":"Entrada"`, además de `direction`/`is_intrusion` (sin regresión).
    - `emit_line_crossing({"direction":"in","timestamp":now,"tracker_id":7,"is_intrusion":False})` (SIN line_id, compatibilidad con el wrapper legacy de la Task 1) sigue funcionando: `payload.get("line_id")` es `None`, sin excepción.
    - Las 4 construcciones `PersonTracker(...)` posicionales de `tests/test_detection_worker.py`
      (líneas ~529, 539, 552, 616) migradas a `PersonTracker(lines=[{"id": "l1", "name":
      "Linea 1", "start": <start>, "end": <end>}], frame_rate=15)` siguen pasando sin
      cambiar el comportamiento que verifican (cruce de línea con clase `person` filtrada,
      aislamiento de ByteTrack entre `ObjectTracker`/`PersonTracker`).
    - El assert `tracker_a.get_counts()["total"] >= 1` de
      `TEST_object_class_does_not_reach_line_zone` migrado a
      `tracker_a.get_counts()["l1"]["total"] >= 1` (el `get_counts()` de la Task 1 ya no
      devuelve claves planas `in`/`out`/`total`, sino un dict por `line_id`).
  </behavior>
  <action>
    En `emit_line_crossing`, añadir al dict `payload={...}`:
    `"line_id": crossing.get("line_id"), "line_name": crossing.get("line_name")` — usar
    `.get()` (nunca acceso directo `[...]`) para tolerar crossings sin esas claves (el
    wrapper de compatibilidad `reconfigure_line` de la Task 1 las omite).

    Además (responsabilidad de ESTA tarea, no de la Task 1): en
    `tests/test_detection_worker.py`, migrar las 4 construcciones `PersonTracker(start,
    end, frame_rate=...)` posicionales a la forma `PersonTracker(lines=[{"id": "l1",
    "name": "Linea 1", "start": start, "end": end}], frame_rate=...)` — mismo shape de
    línea que usa la migración de `tests/test_tracker.py`/`tests/test_phase9.py` en la
    Task 1. Actualizar también el único assert que lee `get_counts()` con claves planas
    (`tracker_a.get_counts()["total"]` → `tracker_a.get_counts()["l1"]["total"]` en
    `TEST_object_class_does_not_reach_line_zone`). El resto de asserts de ese fichero
    (sobre `tracked`, `registry`, `object_tracker`) no cambian.
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/test_event_engine.py tests/test_detection_worker.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "line_id" backend/events/engine.py` confirma la propagación al payload.
    - `grep -n "PersonTracker(sv.Point\|PersonTracker(line_start" tests/test_detection_worker.py`
      NO debe producir coincidencias posicionales (todas migradas a `lines=[...]`) —
      `grep -n "PersonTracker(lines=" tests/test_detection_worker.py` sí debe encontrar las 4.
    - `pytest tests/test_event_engine.py tests/test_detection_worker.py -q` termina en verde,
      incluido el test existente que no pasa `line_id` y los 4 tests migrados de
      `test_detection_worker.py`.
  </acceptance_criteria>
  <done>emit_line_crossing propaga line_id/line_name cuando existen, sin romper el contrato previo del payload.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DetectionWorker (interno) → PersonTracker | Sin entrada externa; `lines` llega ya validada por el router del Plan 33-07/33-05, este plan solo consume la forma pixel-space que el pipeline construye |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-07 | Denial of Service | N líneas sin límite superior en `reconfigure_lines` | accept | El límite práctico lo impone el operador (interfaz manual de un solo usuario, sin API pública de creación masiva); el router del Plan 33-07 puede añadir un tope si el checkpoint manual (Plan 33-14) detecta degradación de FPS con muchas líneas — no se fija un número arbitrario sin evidencia |
</threat_model>

<verification>
```bash
.venv/Scripts/python.exe -m pytest tests/test_tracker.py tests/test_phase9.py tests/test_event_engine.py tests/test_detection_worker.py -q
```
No se relanza la suite completa: cambio contenido en tracker.py/engine.py con tests
dirigidos; la suite completa corre en la integración final (Plan 33-08) que reconecta
todo el pipeline de arranque.
</verification>

<success_criteria>
- PersonTracker soporta N líneas independientes con conteo propio.
- ByteTrack compartido entre líneas (una identidad, N cruces posibles).
- Crossings identifican la línea de origen.
</success_criteria>

<output>
Al terminar, crear `.planning/phases/33-editores-visuales-de-zonas-l-neas-y-reglas/33-04-SUMMARY.md`
</output>
