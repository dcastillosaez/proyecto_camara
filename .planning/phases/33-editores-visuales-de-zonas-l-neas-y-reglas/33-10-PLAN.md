---
phase: 33-editores-visuales-de-zonas-l-neas-y-reglas
plan: 10
type: execute
wave: 2
depends_on: ["33-09"]
files_modified:
  - frontend/js/components/zoneEditor.js
autonomous: true
requirements: [OPS-21, OPS-23]

must_haves:
  truths:
    - "El operador puede dibujar un polígono haciendo click sobre el vídeo, moverlo, editar sus vértices y borrarlo, sin escribir JSON a mano"
    - "El operador puede fijar el tipo de zona (conteo/restringida/exclusión) y un horario propio desde controles de formulario"
    - "Las coordenadas que se envían al servidor son siempre fracciones [0,1], nunca píxeles absolutos"
  artifacts:
    - path: "frontend/js/components/zoneEditor.js"
      provides: "initZoneEditor()/drawZone/CRUD contra /api/v2/zones — canvas + formulario de tipo/horario"
  key_links:
    - from: "frontend/js/components/zoneEditor.js"
      to: "/api/v2/zones"
      via: "fetch POST/DELETE"
      pattern: "fetch\\('/api/v2/zones"
    - from: "frontend/js/components/zoneEditor.js"
      to: "frontend/js/components/videoCanvas.js:canvasClickToFrac"
      via: "import"
      pattern: "canvasClickToFrac"
---

<objective>
Reescribir `frontend/js/components/zoneEditor.js` (hoy: formulario de texto JSON contra
`/api/zones` v1, Fase 13) como editor visual de zonas sobre `<canvas>`: click para añadir
vértices, arrastrar para mover un vértice o el polígono completo, borrar zona, selector de
tipo (`counting`/`restricted`/`exclusion`, con `exclude_objects` mostrado como etiqueta
de solo lectura si la zona ya lo trae — Pitfall 2 de 33-RESEARCH.md) y horario propio
(días + rango horario, mismo patrón de chips/inputs que `settings-field.js`). CRUD contra
`/api/v2/zones` (D-02), coordenadas siempre en fracción `[0,1]`.

Purpose: es la pieza visual de OPS-21/OPS-23 que D-03 sitúa dentro de la vista Cámara —
el Plan 33-13 la monta en el DOM real; este plan construye el módulo en sí, autocontenido
y verificable por sus criterios mecánicos (línea límite, ausencia de `innerHTML` con
datos de servidor).
Output: `zoneEditor.js` reescrito (~250 líneas), sin tocar el HTML real todavía.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-editores-visuales-de-zonas-l-neas-y-reglas/33-CONTEXT.md
@.planning/phases/33-editores-visuales-de-zonas-l-neas-y-reglas/33-RESEARCH.md
@.planning/phases/33-editores-visuales-de-zonas-l-neas-y-reglas/33-PATTERNS.md

<interfaces>
`canvasClickToFrac`/`syncCanvasToImage` — `frontend/js/components/videoCanvas.js` (Plan
33-09, ya cerrado, Wave 1):
```javascript
export function canvasClickToFrac(clickX, clickY, img, canvas) { ... }  // -> {x_frac, y_frac}
export function syncCanvasToImage(canvas, img) { ... }  // canvas.width/height = img.getBoundingClientRect()
```

Convención de toast + confirm-antes-de-borrar — `frontend/js/components/zoneEditor.js`
actual (104 líneas, código real, ESTE es el fichero que se reescribe — conservar el
IMPORT y el patrón de UX, no la implementación de formulario JSON):
```javascript
import { showToast } from '../views/dashboard.js';
...
if (!confirm(`¿Eliminar la zona "${z.name}"?`)) return;
try {
  const r = await fetch(`/api/zones/${encodeURIComponent(z.id)}`, { method: 'DELETE' });
  if (r.ok) { showToast(`Zona "${z.name}" eliminada`, 'info'); loadZones(); }
  else showToast('Error al eliminar zona', 'error');
} catch { showToast('Sin respuesta', 'error'); }
```
Este plan cambia el endpoint a `/api/v2/zones/{id}` (DELETE) y `/api/v2/zones` (POST,
upsert) — mismo patrón de try/catch/toast, mismo `confirm()` nativo (D-07 de la Fase 30
solo prohíbe `confirm()` para acciones NUEVAS que ya tienen precedente de popover
—delete de zona ya usaba `confirm()` en producción, se conserva).

Contrato del router — Plan 33-03 (Wave 1, ya cerrado): `POST /api/v2/zones` body
`{id, camera_id?, name, polygon: [[x_frac,y_frac],...], kind?, schedule?, enabled?}`,
`kind` uno de `counting|restricted|exclusion|exclude_objects`, `schedule`
`{"time_range": "HH:MM-HH:MM", "days": [0-6]}` o `null`. Respuesta siempre
`{"zones": [...]}` con la lista completa tras la mutación. `GET /api/v2/zones` sin
argumentos devuelve todas.

Renderers de campo a imitar (NO importar, son de otra vista con otro contexto DOM) —
`frontend/js/views/settings-field.js:139-189` (patrón de `<select>` para kind, chips
`.filter-chip` con `aria-pressed` para días, `<input type="time">` para el rango):
```javascript
// enum
const select = document.createElement('select');
select.className = 'filter-input filter-select w-[140px]';
for (const v of field.enum_values ?? []) { const opt = document.createElement('option'); opt.value = v; opt.textContent = v; select.appendChild(opt); }
// time
const input = document.createElement('input'); input.type = 'time'; input.className = 'filter-input w-[96px]';
// chips de días (list_int)
const chip = document.createElement('button'); chip.type = 'button'; chip.className = 'filter-chip'; chip.textContent = label;
```

Anti-XSS obligatorio (convención de todo el frontend del proyecto, citada explícitamente
en `settings-field.js`): `createElement`/`textContent` siempre para datos de servidor
(nombre de zona, mensajes de error 422) — nunca `innerHTML` interpolando esos valores (el
`zoneEditor.js` ACTUAL sí usa `row.innerHTML = ...` con `${z.name}` interpolado — es la
única desviación del patrón anti-XSS del resto del proyecto y este plan la corrige).

Mount point real (lo prepara este plan pensando en él, lo conecta el Plan 33-13):
`#camera-feed` (`<img>`, Fase 32) es la imagen fuente; el editor necesita un `<canvas>`
propio superpuesto (id a definir por este plan, ej. `#zone-line-canvas`) MÁS un panel de
lista/formulario (ids a definir, ej. `#zone-editor-panel`) — ninguno de esos ids existe
todavía en `index.html`. Este plan escribe el módulo asumiendo que esos elementos
existirán (documentar los ids exactos elegidos en la cabecera del fichero, mismo patrón
que `camera-quick.js` documentó su contrato de ids para que `32-07` lo consumiera
literalmente — aquí el consumidor es el Plan 33-13).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Motor de canvas — dibujo, arrastre de vértices, hit-testing</name>
  <files>frontend/js/components/zoneEditor.js</files>
  <read_first>
    frontend/js/components/videoCanvas.js completo tras el Plan 33-09 (canvasClickToFrac/syncCanvasToImage exportadas)
    frontend/js/components/zoneEditor.js actual completo (104 líneas, ya extractado arriba — se sustituye por completo, pero conservar el import de showToast y el estilo de UX)
    frontend/css/components.css (para reutilizar clases Tailwind/custom existentes, no inventar nuevas si no hace falta)
  </read_first>
  <behavior>
    Sin framework de test JS (documentado en `tests/test_frontend_modules.py`) — la
    verificación de este plan es mecánica (línea límite, módulo importable, sin
    `innerHTML` con datos de servidor) más el checkpoint manual del Plan 33-14 para el
    comportamiento real de dibujo.
  </behavior>
  <action>
    Definir en la cabecera del fichero (comentario), el contrato de ids que el Plan 33-13
    debe crear en `index.html`, dentro del tabpanel Cámara: `#zone-line-canvas` (canvas
    superpuesto a `#camera-feed`), `#zone-mode-toggle` (botón para entrar/salir de modo
    edición de zonas — el canvas solo captura clicks en modo edición, para no interferir
    con el resto de la vista Cámara), `#zone-list` (lista de zonas existentes),
    `#zone-form-kind` (`<select>`), `#zone-form-days` (contenedor de chips 0-6),
    `#zone-form-time-start`/`#zone-form-time-end` (`<input type="time">`), `#zone-save-btn`,
    `#zone-cancel-btn`, `#zone-new-btn`.

    Implementar el motor de dibujo con Canvas 2D nativo (sin librería, per
    33-RESEARCH.md Standard Stack): `_polygonPoints: {x_frac,y_frac}[]` como estado de
    edición en curso; `mousedown` sobre el canvas en modo edición añade un vértice (click
    simple) O selecciona un vértice existente para arrastrarlo si el click cae dentro de
    un radio de ~8px de un vértice ya dibujado (hit-testing por distancia euclídea en
    píxeles de canvas, no en fracción); `mousemove` con vértice seleccionado lo reposiciona
    (recalculando su fracción con `canvasClickToFrac`); `mouseup` suelta la selección.
    Doble click o un botón "Cerrar polígono" cierra el trazado (mínimo 3 puntos,
    igual que la validación ya existente client-side del fichero actual: "Puntos
    inválidos... mín. 3 puntos").

    Redibujo: `ctx.clearRect` + `ctx.beginPath/moveTo/lineTo/closePath/stroke` para el
    contorno, más un `ctx.arc(...)` de radio ~5px por vértice (relleno distinto si está
    siendo arrastrado). Usar `syncCanvasToImage(canvas, img)` + `ResizeObserver` PROPIO
    (no compartir `_tracksResizeObserver` de `videoCanvas.js`, que es privado y apunta a
    otro `<img>`/`<canvas>`) para mantener el canvas alineado al redimensionar
    `#camera-feed`.

    Exportar `initZoneEditor()` como punto de entrada único (llamado por el Plan 33-13
    desde `camera.js`, mismo patrón que `initCamera()`/`initSettings()` en `app.js`):
    engancha los listeners del canvas y del panel, pero NO dispara ninguna petición de red
    hasta que el operador entre en modo edición o pida la lista (lazy, mismo criterio que
    `initCamera()` no activa el feed hasta el primer cambio de pestaña).
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/test_frontend_modules.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "export function initZoneEditor" frontend/js/components/zoneEditor.js` la encuentra.
    - `grep -n "canvasClickToFrac" frontend/js/components/zoneEditor.js` confirma el reuso (sin fórmula de letterboxing propia).
    - `grep -n "innerHTML" frontend/js/components/zoneEditor.js` NO produce coincidencias (anti-XSS, corrige la desviación del fichero actual).
    - `wc -l frontend/js/components/zoneEditor.js` reporta ≤300.
  </acceptance_criteria>
  <done>Motor de dibujo/arrastre de polígonos sobre canvas, sin dependencias nuevas, reutilizando la matemática compartida del Plan 33-09.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CRUD de zona (kind/schedule) contra /api/v2/zones</name>
  <files>frontend/js/components/zoneEditor.js</files>
  <read_first>
    backend/api/v2/zones.py tras el Plan 33-03 (contrato exacto de request/response, ya extractado en `<interfaces>`)
    frontend/js/views/settings-field.js:139-189 (renderers de enum/time/chips a imitar, no importar)
  </read_first>
  <behavior>
    - `loadZones()` pinta `#zone-list` desde `GET /api/v2/zones`, con nombre por
      `textContent` (nunca interpolado), y el `kind` visible por fila.
    - Guardar una zona nueva con polígono válido, kind y schedule opcionales hace
      `POST /api/v2/zones` con el body exacto `{id, name, polygon, kind, schedule,
      enabled: true}` (kind/schedule `null` si el operador no los fijó — el servidor los
      trata como "sin restricción").
    - Un 422 del servidor (ej. punto fuera de rango) se muestra en un mensaje de error
      visible, sin perder el trazado en curso del canvas (el operador puede corregir y
      reintentar sin volver a dibujar desde cero).
    - Borrar una zona pide confirmación nativa (`confirm()`), hace
      `DELETE /api/v2/zones/{id}` y repinta la lista.
    - Una zona con `kind: "exclude_objects"` se muestra en el formulario de edición con el
      selector de tipo deshabilitado y una etiqueta explicando el origen ("Exclusión de
      objetos — heredado de la Fase 27"), en vez de forzarla a uno de los 3 valores del
      selector.
  </action>
  <action>
    Añadir al mismo fichero: `loadZones()` (fetch `GET /api/v2/zones`, pinta `#zone-list`
    con `createElement`/`textContent` para nombre/kind, botón de borrar por fila con
    `confirm()` + `DELETE` + toast, mismo patrón que el fichero actual pero contra el
    endpoint v2). `renderZoneForm(zone?)` construye el `<select>` de kind (3 opciones, o
    texto deshabilitado si `zone.kind === "exclude_objects"`), chips de días 0-6
    (`aria-pressed`, mismo patrón que `_renderListInt` de `settings-field.js`) y dos
    `<input type="time">` para `schedule.time_range`. `saveZone()` reúne
    `_polygonPoints`/kind/schedule del formulario en curso, valida client-side (≥3 puntos,
    igual que el fichero actual), hace `fetch('/api/v2/zones', {method:'POST', ...})`,
    mapea 422 a un mensaje visible: `res.status === 422` → `const d = await res.json();`.
    El router de zonas (Plan 33-03) usa un body param Pydantic tipado, así que
    `d.detail` es el shape NATIVO de FastAPI (una LISTA de `{loc, msg, type}`), NO el
    shape custom `{errors:[...]}` de `config.py`/`rules.py` — renderizar
    `Array.isArray(d.detail) ? d.detail.map(e => e.msg).join(', ') : String(d.detail)`
    por `textContent`, nunca `d.detail` directamente (un array de objetos interpolado
    produciría "[object Object]"). En éxito `showToast(...)` + `loadZones()` +
    limpia el trazado en curso. `deleteZone(id, name)` mismo patrón que el fichero actual,
    apuntando a `/api/v2/zones/{id}`.
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/test_frontend_modules.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "fetch('/api/v2/zones" frontend/js/components/zoneEditor.js` confirma el endpoint nuevo (y ausencia de `/api/zones` v1: `grep -n "fetch('/api/zones" frontend/js/components/zoneEditor.js` no debe producir coincidencias).
    - `grep -n "exclude_objects" frontend/js/components/zoneEditor.js` confirma el manejo especial del kind heredado.
    - `grep -n "confirm(" frontend/js/components/zoneEditor.js` confirma que se conserva la confirmación nativa de borrado.
    - `wc -l frontend/js/components/zoneEditor.js` reporta ≤300 (suma con la Task 1).
    - `pytest tests/test_frontend_modules.py -q` termina en verde.
  </acceptance_criteria>
  <done>CRUD completo de zona contra /api/v2/zones, con tipo/horario editables y el caso especial de exclude_objects manejado sin forzar un valor incorrecto.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Servidor → DOM (nombre de zona, mensajes 422) | Datos que ya pasaron por el servidor pero se renderizan en el navegador — deben entrar siempre por `textContent`, nunca `innerHTML` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-13 | Tampering (XSS reflejado) | Nombre de zona con caracteres HTML, mostrado en `#zone-list` | mitigate | `createElement`+`textContent` en todo el módulo, sin `innerHTML` interpolado — corrige la única desviación del patrón anti-XSS que el fichero actual tenía |
</threat_model>

<verification>
```bash
.venv/Scripts/python.exe -m pytest tests/test_frontend_modules.py -q
```
Sin framework de test JS — la verificación funcional real (dibujar, arrastrar, guardar
contra un servidor vivo) es el checkpoint manual del Plan 33-14.
</verification>

<success_criteria>
- Editor visual de zonas funcional, sin JSON manual.
- CRUD contra /api/v2/zones exclusivamente (D-02).
- Anti-XSS conforme al resto del proyecto.
</success_criteria>

<output>
Al terminar, crear `.planning/phases/33-editores-visuales-de-zonas-l-neas-y-reglas/33-10-SUMMARY.md`
</output>
