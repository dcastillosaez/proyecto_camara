---
phase: 33-editores-visuales-de-zonas-l-neas-y-reglas
plan: 09
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/js/components/videoCanvas.js
  - frontend/css/components.css
autonomous: true
requirements: [OPS-21, OPS-22]

must_haves:
  truths:
    - "Un click sobre el canvas superpuesto al vídeo se puede convertir en una fracción [0,1] del frame fuente, deshaciendo el letterboxing de object-fit:cover"
    - "El editor de zonas y el de líneas comparten la misma matemática de canvas, sin reimplementarla cada uno por su lado"
  artifacts:
    - path: "frontend/js/components/videoCanvas.js"
      provides: "canvasClickToFrac(clickX, clickY, img, canvas) exportada, inversa de normalizedBoxToCanvasRect"
  key_links:
    - from: "frontend/js/components/zoneEditor.js (Plan 33-10)"
      to: "frontend/js/components/videoCanvas.js:canvasClickToFrac"
      via: "import { canvasClickToFrac, syncCanvasToImage } from '../components/videoCanvas.js'"
      pattern: "canvasClickToFrac"
    - from: "frontend/js/components/lineEditor.js (Plan 33-11)"
      to: "frontend/js/components/videoCanvas.js:canvasClickToFrac"
      via: "mismo import"
      pattern: "canvasClickToFrac"
---

<objective>
Extraer a `videoCanvas.js` (módulo canónico de matemática canvas/MJPEG del proyecto) la
función inversa que el editor de zonas y el de líneas necesitan por igual: convertir un
click en píxeles de canvas mostrados a una fracción `[0,1]` del frame fuente, deshaciendo
el recorte de `object-fit:cover` — exactamente la operación inversa de
`normalizedBoxToCanvasRect`, que ya existe en este mismo fichero.

Purpose: evita que los Planes 33-10 (zonas) y 33-11 (líneas) reimplementen cada uno su
propia versión de esta matemática (CLAUDE.md: "sin lógica duplicada"; 33-RESEARCH.md
Anti-patterns lo señala explícitamente: "Reinferir zoom/pan/object-fit con matemáticas
nuevas... cualquier desviación es una fuente de bugs de desalineación").
Output: `canvasClickToFrac` exportada desde `videoCanvas.js`, más el mínimo de CSS nuevo
que ambos editores necesitarán (cursor, contenedor del canvas de edición) — sin gastar
el presupuesto de 300 líneas de `components.css` en clases que Tailwind ya cubre.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-editores-visuales-de-zonas-l-neas-y-reglas/33-CONTEXT.md
@.planning/phases/33-editores-visuales-de-zonas-l-neas-y-reglas/33-RESEARCH.md
@.planning/phases/33-editores-visuales-de-zonas-l-neas-y-reglas/33-PATTERNS.md

<interfaces>
`frontend/js/components/videoCanvas.js` completo (137 líneas, código real — funciones a
NO reescribir, solo extender):
```javascript
function normalizedBoxToCanvasRect(box, img, canvas) {
  const iw = img.naturalWidth, ih = img.naturalHeight;
  const cw = canvas.width, ch = canvas.height;
  if (!iw || !ih || !cw || !ch) return null;
  const scale = Math.max(cw / iw, ch / ih);
  const drawW = iw * scale, drawH = ih * scale;
  const offsetX = (cw - drawW) / 2;
  const offsetY = (ch - drawH) / 2;
  const [x1, y1, x2, y2] = box;
  return {
    x: offsetX + x1 * iw * scale, y: offsetY + y1 * ih * scale,
    w: (x2 - x1) * iw * scale, h: (y2 - y1) * ih * scale,
  };
}

function syncCanvasToImage(canvas, img) {
  const rect = img.getBoundingClientRect();
  canvas.width = rect.width;
  canvas.height = rect.height;
}

let _tracksResizeObserver = null;
export function initTracksOverlay() {
  const canvas = document.getElementById('tracks-overlay');
  const img = document.getElementById('video-feed');
  if (!canvas || !img || _tracksResizeObserver) return;
  syncCanvasToImage(canvas, img);
  _tracksResizeObserver = new ResizeObserver(() => syncCanvasToImage(canvas, img));
  _tracksResizeObserver.observe(img);
}
```
`_tracksResizeObserver` es un singleton PRIVADO del módulo, ligado a `#tracks-overlay`/
`#video-feed` (Operaciones) — el nuevo canvas de edición vive sobre `#camera-feed`
(Cámara, Fase 32) y necesita su PROPIO `ResizeObserver`, no compartir ese singleton.

Fórmula inversa ya redactada en 33-RESEARCH.md (mismas variables, mismo criterio de
`naturalWidth`/`naturalHeight`, nunca `width`/`height`):
```javascript
function canvasClickToFrac(clickX, clickY, img, canvas) {
  const iw = img.naturalWidth, ih = img.naturalHeight;
  const cw = canvas.width, ch = canvas.height;
  const scale = Math.max(cw / iw, ch / ih);
  const drawW = iw * scale, drawH = ih * scale;
  const offsetX = (cw - drawW) / 2, offsetY = (ch - drawH) / 2;
  return { x_frac: (clickX - offsetX) / drawW, y_frac: (clickY - offsetY) / drawH };
}
```

`frontend/css/components.css`: 237 líneas actuales, tope 300 (`TEST_line_limit`,
`tests/test_frontend_modules.py:81-88`) — margen real de 63 líneas. Convención
establecida: Tailwind utility-first, custom classes solo para lo que Tailwind no cubre
(`.metric-tile`, `.cfg-row`, `.filter-chip`, etc. — releer el fichero completo para no
duplicar un nombre de clase ya usado).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: canvasClickToFrac exportada + syncCanvasToImage reutilizable + cursor de edición</name>
  <files>frontend/js/components/videoCanvas.js, frontend/css/components.css</files>
  <read_first>
    frontend/js/components/videoCanvas.js completo (137 líneas, ya extractado arriba —
    releer igualmente las ~35 líneas no citadas para ubicar el punto de inserción exacto
    y confirmar qué otras funciones ya exporta `loadResolutions`/etc., sin colisionar nombres)
    frontend/css/components.css completo (237 líneas — para verificar presupuesto real y
    no duplicar ninguna clase existente)
  </read_first>
  <behavior>
    No hay framework de test JS en el repo (`tests/test_frontend_modules.py` lo documenta
    explícitamente) — la verificación de este task es mecánica: existencia de la función
    exportada, límite de líneas, y ausencia de `<style>`/lógica inline.
  </behavior>
  <action>
    En `frontend/js/components/videoCanvas.js`, añadir junto a `normalizedBoxToCanvasRect`
    (misma sección del fichero, con comentario que las presenta como par
    inversa/directa):
    ```javascript
    // Inversa de normalizedBoxToCanvasRect — click en canvas (px mostrados) -> fraccion
    // del frame fuente. Compartida por zoneEditor.js/lineEditor.js (Fase 33): NO
    // reimplementar esta formula en ningun editor, D-06/Anti-patron de 33-RESEARCH.md.
    export function canvasClickToFrac(clickX, clickY, img, canvas) {
      const iw = img.naturalWidth, ih = img.naturalHeight;
      const cw = canvas.width, ch = canvas.height;
      if (!iw || !ih || !cw || !ch) return null;
      const scale = Math.max(cw / iw, ch / ih);
      const drawW = iw * scale, drawH = ih * scale;
      const offsetX = (cw - drawW) / 2, offsetY = (ch - drawH) / 2;
      return { x_frac: (clickX - offsetX) / drawW, y_frac: (clickY - offsetY) / drawH };
    }
    ```
    Exportar también `syncCanvasToImage` (hoy sin `export`, uso interno de
    `initTracksOverlay`) añadiendo la palabra clave `export` delante de su declaración —
    los Planes 33-10/33-11 la necesitan para su PROPIO `ResizeObserver` sobre
    `#camera-feed` (no tocar `initTracksOverlay`/`_tracksResizeObserver`, siguen privados
    y ligados a `#tracks-overlay`).

    En `frontend/css/components.css`, añadir SOLO si no hay utilidad Tailwind directa:
    una clase `.zone-editor-canvas` con `cursor: crosshair;` (el único estilo que Tailwind
    no ofrece como utilidad de una palabra para "cursor de precisión de dibujo" sin
    arbitrary values) y `touch-action: none;` (evita scroll táctil accidental mientras se
    arrastra un vértice). Máximo 6 líneas nuevas — si al escribir la regla el fichero
    seguiría teniendo margen sobrado (>50 líneas libres), no añadir nada más por
    anticipación: los Planes 33-10/33-11/33-12 añaden sus propias clases SOLO si de verdad
    las necesitan, cada uno documentando el motivo.
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/test_frontend_modules.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "export function canvasClickToFrac" frontend/js/components/videoCanvas.js` la encuentra.
    - `grep -n "export function syncCanvasToImage" frontend/js/components/videoCanvas.js` confirma que ya no es privada.
    - `wc -l frontend/js/components/videoCanvas.js` reporta ≤300.
    - `wc -l frontend/css/components.css` reporta ≤300.
    - `pytest tests/test_frontend_modules.py -q` termina en verde (TEST_line_limit, TEST_css_modules_exist, TEST_js_modules_exist incluidos).
  </acceptance_criteria>
  <done>canvasClickToFrac y syncCanvasToImage exportadas y listas para que 33-10/33-11 las importen sin reimplementar matemática de canvas.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Cambio puramente de matemática de presentación en cliente, sin entrada de red ni datos de servidor nuevos |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-08 | N/A | — | accept | Sin superficie de amenaza nueva: funciones puras de geometría 2D sobre valores ya presentes en el DOM del propio navegador |
</threat_model>

<verification>
```bash
.venv/Scripts/python.exe -m pytest tests/test_frontend_modules.py -q
```
</verification>

<success_criteria>
- canvasClickToFrac disponible y reutilizable por ambos editores.
- Presupuesto de líneas de components.css preservado para los planes siguientes.
</success_criteria>

<output>
Al terminar, crear `.planning/phases/33-editores-visuales-de-zonas-l-neas-y-reglas/33-09-SUMMARY.md`
</output>
