---
phase: 33-editores-visuales-de-zonas-l-neas-y-reglas
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/storage/migrations.py
  - tests/test_migrations.py
autonomous: true
requirements: [OPS-21, OPS-22, OPS-23]

must_haves:
  truths:
    - "Una zona creada con el endpoint v1 legacy (polygon_json) sigue siendo visible y editable por el editor v2 nuevo tras la migración, sin perder su polígono"
    - "Una instalación existente con la línea de conteo única configurada por .env conserva esa línea como fila real en la tabla lines tras la migración, en vez de perderla silenciosamente"
  artifacts:
    - path: "backend/storage/migrations.py"
      provides: "_migrate_v4_to_v5: backfill zones.polygon desde polygon_json + seed de una Line por defecto desde Settings.line_*_frac"
  key_links:
    - from: "backend/storage/migrations.py:MIGRATIONS"
      to: "backend/storage/migrations.py:_migrate_v4_to_v5"
      via: "tupla (5, nombre, fn) añadida a la lista"
      pattern: "\\(5, .*_migrate_v4_to_v5\\)"
---

<objective>
Cerrar el Pitfall 1 de 33-RESEARCH.md (dos ORMs sobre la misma tabla `zones`,
`polygon_json` string vs `polygon` JSON) y proteger la continuidad de D-01 (N líneas):
una migración v4→v5 idempotente que (a) rellena la columna v2 `zones.polygon` a partir
de `polygon_json` para toda fila donde `polygon IS NULL`, y (b) siembra una fila en
`lines` con la línea única existente (`Settings.line_start_x_frac` etc.) SI la tabla
`lines` está vacía para `cam1` — así una instalación que actualiza a esta fase no pierde
su línea de conteo configurada.

Purpose: el Plan 33-08 migrará el arranque del pipeline para leer zonas y líneas
EXCLUSIVAMENTE de `ZoneRepo`/`LineRepo` (D-02) — esta migración es lo que garantiza que
esa lectura no empiece vacía para instalaciones ya en producción.
Output: migración `_migrate_v4_to_v5` registrada en `MIGRATIONS`, `SCHEMA_VERSION = 5`,
tests que verifican backfill + seed sobre una BD v4 sintética con datos reales.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-editores-visuales-de-zonas-l-neas-y-reglas/33-CONTEXT.md
@.planning/phases/33-editores-visuales-de-zonas-l-neas-y-reglas/33-RESEARCH.md

<interfaces>
De `backend/storage/migrations.py` (fichero completo, patrón exacto a extender —
NO reescribir `_migrate_v1_to_v2`/`_migrate_v2_to_v3`/`_migrate_v3_to_v4`, solo añadir
una cuarta función y su entrada en `MIGRATIONS`):
```python
SCHEMA_VERSION = 4   # -> pasa a 5 en este plan

def _migrate_v3_to_v4(conn: Connection) -> None:
    conn.execute(text(
        "CREATE INDEX IF NOT EXISTS idx_events_analytics "
        "ON events (camera_id, ts, person_id, zone_id, track_id)"))
    _record_version(conn, 4)

MIGRATIONS: list[tuple[int, str, Callable[[Connection], None]]] = [
    (2, "esquema v2 completo", _migrate_v1_to_v2),
    (3, "indice compuesto de la linea temporal", _migrate_v2_to_v3),
    (4, "indice compuesto de analitica", _migrate_v3_to_v4),
]
```
`_record_version(conn, version)` ya existe — cada migración registra SU propia versión
objetivo, nunca `SCHEMA_VERSION` directamente (comentario explícito en el fichero).

Columnas reales de la tabla física `zones` (confirmado por `_migrate_v1_to_v2` +
`storage/models.py:168-179`): `id, name, polygon_json, enabled, kind, created_at`
(v1) más `camera_id, polygon, schedule` (v2, añadidas por `_add_missing_columns`).
Backfill objetivo: `UPDATE zones SET polygon = polygon_json WHERE polygon IS NULL AND
polygon_json IS NOT NULL` — SQLite no tiene una función nativa de "TEXT JSON string ->
JSON column" con transformación; la columna `polygon` es tipo `JSON` (SQLAlchemy la
serializa/deserializa como texto igualmente en SQLite, así que copiar el TEXTO crudo de
`polygon_json` en `polygon` funciona porque ambos son JSON válido serializado como texto
plano — verificar con `json.loads` en el test, no asumir).

`backend.config.Settings` (fichero completo, NO se toca en este plan — solo se LEE para
construir los valores por defecto del seed): `line_start_x_frac=0.0,
line_start_y_frac=0.5, line_end_x_frac=1.0, line_end_y_frac=0.5` son los defaults si el
`.env` no los sobreescribe. La migración corre en `run_migrations(engine)` (síncrono,
antes de que la app async arranque) y NO tiene acceso directo a `get_settings()` en ese
punto sin importar `backend.config` — importarlo es seguro (sin ciclo: `migrations.py`
ya no importa nada de `backend.config` hoy, y `config.py` no importa `migrations.py`).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migración v4→v5 — backfill de zonas.polygon + seed de línea por defecto</name>
  <files>backend/storage/migrations.py, tests/test_migrations.py</files>
  <read_first>
    backend/storage/migrations.py completo (216 líneas — ya extractado en `<interfaces>`,
    pero releer la función `_migrate_v3_to_v4` completa como plantilla exacta de forma:
    guard de tabla, `conn.execute(text(...))`, `_record_version`)
    tests/test_migrations.py:1-70 (`make_v1_db`/`make_v2_db`, patrón de construcción de BD
    sintética con `create_engine`+`text()` — releer también las funciones equivalentes
    `make_v3_db`/`make_v4_db` más abajo en el fichero, mismo patrón, para clonar el estilo
    exacto al escribir `make_v4_db_with_legacy_zone`)
    backend/config.py:82-88 (line_start_x_frac/line_start_y_frac/line_end_x_frac/line_end_y_frac,
    valores por defecto — NO se eliminan en este plan, eso es Plan 33-08)
  </read_first>
  <behavior>
    - Sobre una BD v4 con una fila en `zones` con `polygon_json='[[0,0],[1,0],[1,1]]'` y
      `polygon IS NULL`: tras la migración, `SELECT polygon FROM zones WHERE id=...`
      deserializa (`json.loads`) al mismo array `[[0,0],[1,0],[1,1]]`.
    - Sobre una BD v4 con una fila en `zones` con `polygon` YA poblado (no NULL): la
      migración NO la sobreescribe (idempotencia — nunca pisa un valor v2 ya editado por
      el nuevo editor).
    - Sobre una BD v4 sin ninguna fila en `lines` para `camera_id='cam1'`: tras la
      migración existe exactamente una fila en `lines` con
      `start_x_frac=0.0, start_y_frac=0.5, end_x_frac=1.0, end_y_frac=0.5` (valores
      default de Settings) y `camera_id='cam1'`.
    - Sobre una BD v4 con al menos una fila YA existente en `lines` para `cam1`: la
      migración NO añade ninguna fila nueva (idempotencia — no duplica el seed).
    - Re-ejecutar `run_migrations(engine)` dos veces seguidas sobre la misma BD no falla
      y no duplica nada (mismo criterio que las migraciones anteriores).
  </behavior>
  <action>
    Añadir `SCHEMA_VERSION = 5` (sustituye el `4` actual).

    Añadir función, con la misma forma que `_migrate_v3_to_v4`:
    ```python
    def _migrate_v4_to_v5(conn: Connection) -> None:
        """Unifica el modelo de Zone (D-02, 33-CONTEXT.md) y siembra la linea de conteo
        unica existente como primera fila real de `lines` (D-01) — sin esto, una
        instalacion que actualiza a esta fase pierde su linea configurada por .env en
        cuanto el pipeline empiece a leer solo de LineRepo (Plan 33-08).

        Backfill de zones.polygon: solo rellena filas donde polygon es NULL (v1 puro,
        nunca escritas por ZoneRepo) — nunca pisa un polygon ya editado por el nuevo
        editor v2, ni siquiera si polygon_json tambien cambio despues (no deberia, D-02
        deja polygon_json de solo lectura de compatibilidad).
        """
        if _table_exists(conn, "zones"):
            conn.execute(text(
                "UPDATE zones SET polygon = polygon_json "
                "WHERE polygon IS NULL AND polygon_json IS NOT NULL"
            ))
        if _table_exists(conn, "lines"):
            existing = conn.execute(
                text("SELECT COUNT(*) FROM lines WHERE camera_id = 'cam1'")
            ).scalar()
            if not existing:
                from backend.config import get_settings
                s = get_settings()
                conn.execute(
                    text(
                        "INSERT INTO lines (id, camera_id, name, start_x_frac, "
                        "start_y_frac, end_x_frac, end_y_frac, enabled) VALUES "
                        "('linea-1', 'cam1', 'Linea de conteo', :sx, :sy, :ex, :ey, 1)"
                    ),
                    {
                        "sx": s.line_start_x_frac, "sy": s.line_start_y_frac,
                        "ex": s.line_end_x_frac, "ey": s.line_end_y_frac,
                    },
                )
                logger.info("Sembrada linea de conteo por defecto desde Settings (D-01)")
        _record_version(conn, 5)
    ```
    Añadir `(5, "unificacion de zonas + siembra de linea de conteo", _migrate_v4_to_v5),`
    a la lista `MIGRATIONS`.

    En `tests/test_migrations.py`, añadir `make_v4_db_with_legacy_zone(path)` (clon de
    `make_v4_db` existente, más abajo en el fichero — releerlo antes de escribir la nueva
    función) que además inserta una fila en `zones` con `polygon_json` poblado y
    `polygon` NULL, y tests que cubran los 5 casos del `<behavior>` de arriba usando
    `run_migrations(create_engine(...))` + `text()` para leer el resultado, mismo estilo
    que el resto del fichero.
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/test_migrations.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "SCHEMA_VERSION = 5" backend/storage/migrations.py` lo confirma.
    - `grep -n "_migrate_v4_to_v5" backend/storage/migrations.py` aparece tanto en la definición de función como en la tupla de `MIGRATIONS`.
    - `pytest tests/test_migrations.py -q` termina en verde, suite completa del fichero (no rompe los tests v1→v4 existentes).
    - Ejecutar `run_migrations(engine)` dos veces sobre la misma BD sintética no lanza excepción ni duplica filas en `lines` (test explícito de idempotencia).
  </acceptance_criteria>
  <done>Migración v4→v5 registrada, idempotente, con tests que demuestran backfill de polygon y seed de línea sin duplicar ni pisar datos existentes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Migración → BD en disco | Se ejecuta con privilegios completos sobre `data/events.db`; ya protegida por `_backup_db()` (copia antes de cualquier migración destructiva) — sin cambios de superficie aquí |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-02 | Tampering | `_migrate_v4_to_v5` re-ejecutada sobre una BD parcialmente migrada | mitigate | Guard `WHERE polygon IS NULL` / `SELECT COUNT(*)` antes de sembrar — mismo patrón `INSERT OR IGNORE`/guard condicional que `_migrate_v1_to_v2` ya usa para `crossing_events` |
</threat_model>

<verification>
```bash
.venv/Scripts/python.exe -m pytest tests/test_migrations.py -q
```
Fichero de migraciones, sin tocar pipeline/API en caliente: no se relanza la suite completa en este plan (se relanzará en la integración final, Plan 33-08).
</verification>

<success_criteria>
- Zonas v1 preexistentes (`polygon_json`) legibles por el editor v2 nuevo sin migración manual.
- Instalaciones con línea única configurada por `.env` conservan esa línea como fila real en `lines` tras actualizar.
- Migración idempotente, con backup automático ya cubierto por `run_migrations()`.
</success_criteria>

<output>
Al terminar, crear `.planning/phases/33-editores-visuales-de-zonas-l-neas-y-reglas/33-02-SUMMARY.md`
</output>
