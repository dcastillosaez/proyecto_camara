---
phase: 33-editores-visuales-de-zonas-l-neas-y-reglas
plan: 05
type: execute
wave: 2
depends_on: ["33-01", "33-04"]
files_modified:
  - backend/pipeline/detection.py
  - backend/pipeline/manager.py
  - backend/camera.py
  - tests/test_pipeline_lines.py
autonomous: true
requirements: [OPS-21, OPS-22, OPS-23]

must_haves:
  truths:
    - "Cambiar las líneas de conteo de una cámara recarga el pipeline en menos de 1s sin reiniciar, igual que las zonas"
    - "Una zona dibujada a 720p sigue siendo correcta al cambiar a 1080p, sin recalcular nada manualmente"
    - "Una zona con horario propio deja de contar entradas fuera de su ventana horaria, sin necesidad de borrarla ni deshabilitarla a mano"
  artifacts:
    - path: "backend/pipeline/detection.py"
      provides: "set_lines()/_lines_dirty + gating de schedule en _update_zones_and_heat + consumo de ZoneRepo.polygon (lista) en vez de polygon_json"
    - path: "backend/pipeline/manager.py"
      provides: "CameraPipeline.set_lines(lines)"
  key_links:
    - from: "backend/api/v2/lines.py (Plan 33-07)"
      to: "backend/pipeline/manager.py:CameraPipeline.set_lines"
      via: "camera_manager.all() tras persistir"
      pattern: "\\.set_lines\\("
    - from: "backend/pipeline/detection.py:DetectionWorker.set_lines"
      to: "backend/tracker.py:PersonTracker.reconfigure_lines"
      via: "conversion fraccion->pixel en el rebuild, igual que zonas"
      pattern: "reconfigure_lines"
    - from: "backend/pipeline/detection.py:_update_zones_and_heat"
      to: "backend/events/rules.py:is_schedule_active"
      via: "gating por zona antes de contar entradas"
      pattern: "is_schedule_active"
---

<objective>
Cerrar tres huecos reales del pipeline detectados en 33-RESEARCH.md, los tres en el mismo
fichero (`backend/pipeline/detection.py`) por lo que se agrupan en un único plan: (1)
hot-reload de líneas — `DetectionWorker.set_lines()`/`CameraPipeline.set_lines()` con el
mismo patrón dirty-flag que zonas, forwarding fracción→píxel a
`PersonTracker.reconfigure_lines()` (Plan 33-04); (2) `_rebuild_zone_states` deja de leer
`z["polygon_json"]` (string legacy) y pasa a leer `z["polygon"]` (lista, ya viene de
`ZoneRepo` — D-02); (3) horario propio de zona (OPS-23) deja de ser un dato inerte:
`_update_zones_and_heat` usa `is_schedule_active()` (Plan 33-01) para saltarse el
`trigger()` de una zona fuera de su ventana horaria. De paso, `backend/camera.py` pierde
la recalculación manual de línea única en `/resolution` — ya no aplica, las líneas se
recalculan solas en el siguiente frame como las zonas.

Purpose: sin esto, D-01 (N líneas) queda a medio construir (Plan 33-04 cambia
`PersonTracker` pero nadie lo alimenta en caliente) y el criterio de éxito 6/7 del
ROADMAP no se cumple para líneas ni para el horario de zonas.
Output: `set_lines`/`set_zones` simétricos en `DetectionWorker`/`CameraPipeline`, zonas
leyendo `polygon` (lista), horario de zona funcionalmente activo, `camera.py` sin código
muerto.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-editores-visuales-de-zonas-l-neas-y-reglas/33-CONTEXT.md
@.planning/phases/33-editores-visuales-de-zonas-l-neas-y-reglas/33-RESEARCH.md
@.planning/phases/33-editores-visuales-de-zonas-l-neas-y-reglas/33-PATTERNS.md

<interfaces>
Patrón dirty-flag de zonas — `backend/pipeline/detection.py:135-139,480-544` (código real,
NO se reescribe, se CLONA para líneas conservando el original para zonas):
```python
def set_zones(self, zones: list[dict]) -> None:
    with self._lock:
        self._zones = list(zones)
        self._zones_dirty = True

def _update_zones_and_heat(self, tracked, shape, captured_at=0.0, processed_at=0.0) -> None:
    fh, fw = shape[:2]
    with self._lock:
        dirty = self._zones_dirty
        self._zones_dirty = False
        zones_snap = list(self._zones)
    if dirty or self._zone_frame_size != (fw, fh):
        self._zone_frame_size = (fw, fh)
        self._rebuild_zone_states(zones_snap, fw, fh)
    ids = tracked.tracker_id
    for st in self._zone_states:
        mask = st["zone"].trigger(tracked)
        inside = {int(ids[i]) for i in np.flatnonzero(mask)} if ids is not None and len(mask) else set()
        with self._lock:
            st["entries"] += len(inside - st["inside"])
            st["inside"] = inside
            st["current"] = len(inside)
        if self._event_engine is not None:
            self._event_engine.process_zone(st["id"], inside, datetime.datetime.now(), captured_at, processed_at, now_monotonic=processed_at)
    ...

def _rebuild_zone_states(self, zones: list[dict], fw: int, fh: int) -> None:
    states: list[dict] = []
    for z in zones:
        if not z.get("enabled", True):
            continue
        try:
            pts = np.array([[int(p[0] * fw), int(p[1] * fh)] for p in json.loads(z["polygon_json"])], dtype=np.int64)
            if len(pts) < 3:
                continue
            states.append({"id": z["id"], "name": z["name"], "kind": z.get("kind"), "polygon": pts,
                "zone": sv.PolygonZone(polygon=pts), "inside": set(), "entries": 0, "current": 0})
        except Exception:
            logger.warning("DetectionWorker: zone %s invalid polygon_json, skipped", z.get("id"))
    with self._lock:
        ...  # (releer el resto exacto del método al abrir el fichero)
```
`self._zones: list[dict]` llega con la forma de `ZoneRepo._to_dict()` tras el Plan 33-08
(`polygon` ya es una LISTA de puntos, no una string JSON) — el `json.loads(z["polygon_json"])`
deja de ser válido y debe sustituirse por `z["polygon"]` directamente. Mientras el
Plan 33-08 no haya corrido, el llamador real (`main.py`) sigue usando la forma v1 legacy
(`get_zones()`, con `polygon_json`) — este plan, al cambiar la clave leída, ROMPE ese
llamador hasta que 33-08 lo actualice. Es una dependencia de orden intencionada: 33-08
depende explícitamente de este plan (Wave 4 tras Wave 2) y es quien actualiza `main.py`
para pasar zonas con forma `ZoneRepo` desde el arranque. No añadir aquí un fallback
`z.get("polygon") or json.loads(z.get("polygon_json", "[]"))` — duplicaría lógica sin
necesidad real (CLAUDE.md); el cambio de contrato se cierra atómicamente por la cadena de
dependencias del plan, no por compatibilidad dual permanente.

Constructor de `PersonTracker` tras el Plan 33-04 (ya cerrado, Wave 1):
`reconfigure_lines(self, lines: list[dict[str, Any]]) -> None` — cada dict
`{"id": str, "name": str, "start": sv.Point, "end": sv.Point}` (píxeles, no fracciones —
la conversión fracción→píxel es responsabilidad de QUIEN llama, igual que
`_rebuild_zone_states` hace `int(p[0]*fw)` para zonas).

`CameraPipeline.set_zones`/`CameraManager.all` — `backend/pipeline/manager.py:322-327,432-433`
(molde exacto para `set_lines`):
```python
def set_zones(self, zones: list[dict]) -> None:
    if self.detection:
        self.detection.set_zones(zones)
```
`CameraPipeline` guarda el tracker en `self.tracker` (kwarg `tracker=tracker` pasado a
`CameraManager.add()` en `main.py`) — pero el hot-reload de líneas debe pasar por
`DetectionWorker` (que ya tiene `self._tracker`), NO llamar `self.tracker.reconfigure_lines()`
directamente desde `CameraPipeline`: el dirty-flag y la conversión fracción→píxel viven
en `DetectionWorker`, igual que zonas.

`backend/camera.py:173-198` (código real, a recortar — el bloque completo del endpoint):
```python
@router.post("/resolution")
async def set_resolution(req: ResolutionRequest):
    if _stream_ref is None or _tracker_ref is None:
        raise HTTPException(status_code=503, detail="Stream not ready")
    s = get_settings()
    target_w = req.width if req.width > 0 else None
    target_h = req.height if req.height > 0 else None
    if target_w and target_h:
        frame_w, frame_h = target_w, target_h
    else:
        nw, nh = _stream_ref.get_native_resolution()
        frame_w, frame_h = (nw or 1280), (nh or 720)
    new_start = sv.Point(int(s.line_start_x_frac * frame_w), int(s.line_start_y_frac * frame_h))
    new_end   = sv.Point(int(s.line_end_x_frac   * frame_w), int(s.line_end_y_frac   * frame_h))
    _tracker_ref.reconfigure_line(new_start, new_end)
    _stream_ref.set_process_size(req.width, req.height)
    label = f"{req.width}×{req.height}" if req.width > 0 else "Nativa"
    return {"resolution": label, "line_start": [new_start.x, new_start.y], "line_end": [new_end.x, new_end.y]}
```
Este endpoint (`POST /camera/resolution`, activamente usado por
`frontend/js/components/videoCanvas.js:44`, un control DISTINTO al `quick-resolution` de
la Fase 32) no recalcula zonas manualmente hoy — confía en que
`_zone_frame_size != (fw, fh)` dispare el rebuild automático en el siguiente frame. Tras
este plan, las líneas deben comportarse IGUAL: `set_resolution` deja de tocar el tracker
directamente.

`is_schedule_active(schedule, now=None)` — `backend/events/rules.py` (Plan 33-01, Wave 1,
ya cerrado): `True` si `schedule` es `None` o si la hora/día actuales caen dentro.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: DetectionWorker.set_lines() + zonas leen polygon (lista) + gating de horario</name>
  <files>backend/pipeline/detection.py, tests/test_pipeline_lines.py</files>
  <read_first>
    backend/pipeline/detection.py completo (545 líneas — ya extractados los métodos
    relevantes en `<interfaces>`, releer igualmente el `__init__` completo, líneas 55-103,
    para añadir el estado `_lines`/`_lines_dirty`/`_line_frame_size` en el mismo bloque
    donde vive `_zones`/`_zones_dirty`/`_zone_frame_size`)
    backend/tracker.py (tras el Plan 33-04 — `reconfigure_lines`, forma exacta del dict de línea)
    backend/events/rules.py (tras el Plan 33-01 — `is_schedule_active`)
    tests/test_detection_worker.py (localizar con grep las 3 construcciones `set_zones([...
    "polygon_json": ...}])` — actualizarlas a `"polygon": [[...]]` en esta tarea, ya que
    `_rebuild_zone_states` deja de aceptar la forma string)
  </read_first>
  <behavior>
    - `worker.set_lines([{"id":"l1","name":"Entrada","start_x_frac":0.0,"start_y_frac":0.5,"end_x_frac":1.0,"end_y_frac":0.5}])`
      seguido de un frame procesado: `worker._tracker.get_counts()` incluye `"l1"` con los
      píxeles correctos para el `shape` del frame (`int(0.0*fw)`, `int(0.5*fh)`, etc.).
    - Cambiar el tamaño de frame entre dos llamadas (720p → 1080p) sin llamar `set_lines`
      de nuevo: la línea se recalcula igualmente en el frame de la nueva resolución (mismo
      criterio 7 que zonas — gate por `_line_frame_size != (fw, fh)`).
    - `worker.set_zones([{"id":"z1","name":"Z1","enabled":True,"polygon":[[0.0,0.0],[0.5,0.0],[0.5,1.0],[0.0,1.0]]}])`
      sigue construyendo el `PolygonZone` correctamente (forma lista, no string).
    - Una zona con `"schedule": {"days": [0], "time_range": "00:00-23:59"}` evaluada un
      día distinto de lunes: `st["current"]`/`st["entries"]` no se incrementan aunque haya
      tracks dentro del polígono (gating aplicado, `trigger()` ni se llama para esa zona).
    - Una zona SIN `schedule` (o `schedule: None`) se comporta exactamente igual que antes
      de este plan (sin regresión — gating es no-op cuando no hay horario).
  </behavior>
  <action>
    En `DetectionWorker.__init__`, junto al bloque `self._zones`/`self._zones_dirty`/
    `self._zone_frame_size`, añadir `self._lines: list[dict] = []`,
    `self._lines_dirty: bool = True`, `self._line_frame_size: tuple[int, int] = (0, 0)`.

    Añadir método `set_lines(self, lines: list[dict]) -> None` (mismo molde exacto que
    `set_zones`, bajo `self._lock`, marca `self._lines_dirty = True`).

    Añadir método `_update_lines(self, shape: tuple) -> None`, llamado desde `_loop()`
    justo antes o después de `self._update_zones_and_heat(...)` (mismo punto del ciclo):
    ```python
    def _update_lines(self, shape: tuple) -> None:
        fh, fw = shape[:2]
        with self._lock:
            dirty = self._lines_dirty
            self._lines_dirty = False
            lines_snap = list(self._lines)
        if not (dirty or self._line_frame_size != (fw, fh)):
            return
        self._line_frame_size = (fw, fh)
        pixel_lines = [
            {
                "id": ln["id"], "name": ln["name"],
                "start": sv.Point(int(ln["start_x_frac"] * fw), int(ln["start_y_frac"] * fh)),
                "end": sv.Point(int(ln["end_x_frac"] * fw), int(ln["end_y_frac"] * fh)),
            }
            for ln in lines_snap if ln.get("enabled", True)
        ]
        self._tracker.reconfigure_lines(pixel_lines)
    ```
    Llamar `self._update_lines(frame.image.shape)` en `_loop()` junto a la línea existente
    `self._update_zones_and_heat(tracked, frame.image.shape, ...)`.

    En `_rebuild_zone_states`, sustituir
    `json.loads(z["polygon_json"])` por `z["polygon"]` directamente (ya es una lista de
    `[x_frac, y_frac]`) — el resto de la función (conversión a píxeles, `len(pts)<3`,
    `sv.PolygonZone`) no cambia.

    En `_update_zones_and_heat`, antes de `mask = st["zone"].trigger(tracked)`, añadir el
    gating de horario: `if not is_schedule_active(st.get("schedule")): continue` (import
    `is_schedule_active` desde `backend.events.rules` al principio del fichero). Guardar
    `schedule` en el dict de estado dentro de `_rebuild_zone_states` (`"schedule":
    z.get("schedule")`, junto a `"kind": z.get("kind")`).

    Actualizar las 3 construcciones de test en `tests/test_detection_worker.py` (líneas
    ~285-290, ~437-442, ~759-762 del fichero previo a este plan — confirmar posición real
    con grep antes de editar, puede haber cambiado tras el Plan 33-04) de
    `"polygon_json": json.dumps([...])` a `"polygon": [...]` directamente.
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/test_pipeline_lines.py tests/test_detection_worker.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "def set_lines" backend/pipeline/detection.py` la encuentra en `DetectionWorker`.
    - `grep -n "polygon_json" backend/pipeline/detection.py` NO produce coincidencias (migración completa a `polygon`).
    - `grep -n "is_schedule_active" backend/pipeline/detection.py` confirma el import y el uso en el gating.
    - `pytest tests/test_pipeline_lines.py tests/test_detection_worker.py -q` termina en verde.
  </acceptance_criteria>
  <done>Líneas con hot-reload simétrico a zonas, zonas leyendo `polygon` (lista), horario de zona funcionalmente activo.</done>
</task>

<task type="auto">
  <name>Task 2: CameraPipeline.set_lines() + retirar recalculo manual en camera.py</name>
  <files>backend/pipeline/manager.py, backend/camera.py</files>
  <read_first>
    backend/pipeline/manager.py:290-330 (CameraPipeline.set_zones/get_zone_stats, ya extractado)
    backend/camera.py:150-198 completo (endpoint /resolution, ya extractado en `<interfaces>`)
  </read_first>
  <acceptance_criteria>
    - `grep -n "def set_lines" backend/pipeline/manager.py` confirma el método facade en `CameraPipeline`.
    - `grep -n "reconfigure_line\|_tracker_ref.reconfigure" backend/camera.py` NO produce coincidencias (bloque retirado).
    - `grep -n "new_start\|new_end" backend/camera.py` NO produce coincidencias.
    - `pytest tests/ -k camera -q` termina en verde (o el fichero de test específico de `backend/camera.py` si existe uno dedicado — localizar con `Glob tests/test_camera*.py` antes de decidir el comando exacto).
  </acceptance_criteria>
  <action>
    En `backend/pipeline/manager.py`, añadir junto a `set_zones`:
    ```python
    def set_lines(self, lines: list[dict]) -> None:
        if self.detection:
            self.detection.set_lines(lines)
    ```
    En `backend/camera.py`, en `set_resolution`, eliminar el bloque de cálculo de
    `new_start`/`new_end` y la llamada `_tracker_ref.reconfigure_line(new_start, new_end)`
    — las líneas ahora se recalculan solas en el siguiente frame procesado, vía el gate
    `_line_frame_size != (fw, fh)` de la Task 1, exactamente como ya ocurre con zonas (que
    este mismo endpoint NUNCA ha recalculado a mano). Ajustar la respuesta del endpoint:
    ya no puede devolver `line_start`/`line_end` (esos valores vivían del cálculo
    eliminado) — devolver solo `{"resolution": label}`. Verificar con grep si algún
    consumidor de frontend (`videoCanvas.js` u otro) lee `line_start`/`line_end` de la
    respuesta de este endpoint antes de quitarlos; si algo los usa, documentarlo como
    hallazgo en el SUMMARY en vez de romperlo en silencio.
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/ -k "camera and not test_camera_module" -q</automated>
  </verify>
  <done>CameraPipeline.set_lines existe; camera.py ya no calcula píxeles de línea a mano, delega en el rebuild automático del pipeline.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A (interno) | Sin entrada de red nueva en este plan — las líneas/zonas ya fueron validadas por sus routers (Planes 33-03/33-07) antes de llegar aquí |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-09 | Information Disclosure | Ninguna | accept | Sin cambio de superficie: mismo dato (zonas/líneas) que ya circulaba, solo cambia su forma interna |
</threat_model>

<verification>
```bash
.venv/Scripts/python.exe -m pytest tests/test_pipeline_lines.py tests/test_detection_worker.py -q
```
No se relanza la suite completa: el arranque real (`main.py`) que conecta este código con
`ZoneRepo`/`LineRepo` llega en el Plan 33-08 (Wave 4), que sí relanza la suite completa.
</verification>

<success_criteria>
- Líneas con hot-reload <1s sin reiniciar (criterio 6).
- Zonas correctas al cambiar resolución (criterio 7), ahora también sin cálculo manual redundante.
- Horario de zona funcionalmente activo (criterio 3/OPS-23), no solo almacenado.
</success_criteria>

<output>
Al terminar, crear `.planning/phases/33-editores-visuales-de-zonas-l-neas-y-reglas/33-05-SUMMARY.md`
</output>
