---
phase: 33-editores-visuales-de-zonas-l-neas-y-reglas
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/storage/repositories.py
  - backend/events/rules.py
  - tests/test_repositories.py
  - tests/test_rule_engine.py
autonomous: true
requirements: [OPS-22, OPS-23, RULE-05]

must_haves:
  truths:
    - "El repositorio de líneas (LineRepo) puede listar/crear/actualizar/borrar líneas de conteo por cámara, igual que ZoneRepo/RuleRepo"
    - "Una regla puede probarse sin mutar su estado de debounce (would_match es puro)"
    - "Una zona con horario propio puede consultarse para saber si está activa ahora mismo"
  artifacts:
    - path: "backend/storage/repositories.py"
      provides: "LineRepo (list/upsert/delete/_to_dict) + RuleRepo.get(rule_id)"
    - path: "backend/events/rules.py"
      provides: "RuleEngine.would_match(when, event) público + is_schedule_active(schedule, now=None)"
  key_links:
    - from: "backend/events/rules.py:RuleEngine.would_match"
      to: "backend/events/rules.py:_matches"
      via: "llamada directa, sin tocar self._last_fired"
      pattern: "def would_match"
    - from: "backend/storage/repositories.py:LineRepo"
      to: "backend/storage/models.py:Line"
      via: "SQLAlchemy select/get/delete"
      pattern: "class LineRepo"
---

<objective>
Añadir las tres piezas de datos/motor puras que el resto de la Fase 33 necesita como
cimiento: `LineRepo` (CRUD de líneas de conteo, tabla `lines` ya modelada pero sin
repositorio — D-01), `RuleRepo.get(rule_id)` (falta para `POST /rules/{id}/test`, Plan
33-06) y dos funciones puras en `backend/events/rules.py`: `RuleEngine.would_match()`
(evaluación de reglas sin efectos secundarios, RULE-05) e `is_schedule_active()`
(horario propio de zona, OPS-23, consumido por el rebuild de zonas en el Plan 33-05).

Purpose: desbloquear en paralelo los routers `/api/v2/lines` (33-07) y
`/api/v2/rules` (33-06), y el gating de horario de zonas (33-05), sin que ninguno
tenga que reinventar su propia versión de estas piezas.
Output: `LineRepo`, `RuleRepo.get()`, `RuleEngine.would_match()`, `is_schedule_active()`,
con tests unitarios verdes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-editores-visuales-de-zonas-l-neas-y-reglas/33-CONTEXT.md
@.planning/phases/33-editores-visuales-de-zonas-l-neas-y-reglas/33-RESEARCH.md
@.planning/phases/33-editores-visuales-de-zonas-l-neas-y-reglas/33-PATTERNS.md

<interfaces>
De `backend/storage/models.py:182-192` (ya existe, no se toca en este plan):
```python
class Line(Base):
    __tablename__ = "lines"
    id = Column(String(50), primary_key=True)
    camera_id = Column(String(50), ForeignKey("cameras.id"), nullable=False, server_default="cam1")
    name = Column(String(100), nullable=False)
    start_x_frac = Column(Float, nullable=False)
    start_y_frac = Column(Float, nullable=False)
    end_x_frac = Column(Float, nullable=False)
    end_y_frac = Column(Float, nullable=False)
    enabled = Column(Boolean, nullable=False, default=True)
```

De `backend/storage/repositories.py:704-793` (ZoneRepo/RuleRepo ya existen — molde exacto a copiar):
```python
class ZoneRepo:
    def __init__(self, session_factory) -> None:
        self._sf = session_factory

    async def list(self, camera_id: str | None = None) -> list[dict[str, Any]]:
        q = select(models.Zone)
        if camera_id is not None:
            q = q.where(models.Zone.camera_id == camera_id)
        async with self._sf() as session:
            result = await session.execute(q)
            return [self._to_dict(z) for z in result.scalars().all()]

    async def upsert(self, zone_id, camera_id, name, polygon, kind=None, schedule=None, enabled=True) -> None:
        async with self._sf() as session:
            async with session.begin():
                existing = await session.get(models.Zone, zone_id)
                if existing:
                    existing.camera_id = camera_id; existing.name = name
                    existing.polygon = polygon; existing.kind = kind
                    existing.schedule = schedule; existing.enabled = enabled
                else:
                    session.add(models.Zone(id=zone_id, camera_id=camera_id, name=name,
                        polygon=polygon, kind=kind, schedule=schedule, enabled=enabled))

    async def delete(self, zone_id: str) -> bool:
        async with self._sf() as session:
            async with session.begin():
                z = await session.get(models.Zone, zone_id)
                if z:
                    await session.delete(z)
                    return True
        return False

class RuleRepo:
    def __init__(self, session_factory) -> None:
        self._sf = session_factory

    async def list(self) -> list[dict[str, Any]]:
        async with self._sf() as session:
            result = await session.execute(select(models.Rule))
            return [self._to_dict(r) for r in result.scalars().all()]

    async def upsert(self, rule_id, name, enabled, definition: dict) -> None: ...
    async def delete(self, rule_id: str) -> bool: ...
```

De `backend/events/rules.py:72-102,169-188` (código real — base para `would_match`):
```python
def _matches(when: When, event: Event) -> bool: ...   # función pura de módulo, privada

class RuleEngine:
    def match(self, event: Event) -> list[str]:
        self._purge_stale(event.ts)
        fired: list[str] = []
        for rule in self._rules:
            if not rule.enabled: continue
            if not _matches(rule.when, event): continue
            if self._is_debounced(rule, event): continue
            self._last_fired[self._debounce_key(rule, event)] = event.ts
            fired.append(rule.name)
        return fired
```

`_parse_time_range(spec: str) -> tuple[datetime.time, datetime.time]` y
`_time_in_range(t, start, end) -> bool` ya existen en el mismo módulo
(`backend/events/rules.py:60-69`) — `is_schedule_active` debe reutilizarlas
directamente (mismo módulo, sin problema de visibilidad).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: LineRepo + RuleRepo.get(rule_id)</name>
  <files>backend/storage/repositories.py, tests/test_repositories.py</files>
  <read_first>
    backend/storage/repositories.py (clases ZoneRepo/RuleRepo completas, líneas 704-793, y
    el resto del fichero para import style/convenciones de `models`/`select`/`Any`)
    backend/storage/models.py:182-192 (Line, columnas exactas)
    tests/test_repositories.py (patrón de test existente para ZoneRepo/RuleRepo — copiar
    el mismo estilo de fixture de sesión in-memory)
  </read_first>
  <behavior>
    - `LineRepo.list(camera_id=None)` devuelve `[]` en BD vacía; con camera_id filtra por columna.
    - `LineRepo.upsert(...)` crea si no existe, actualiza todos los campos si existe (mismo id).
    - `LineRepo.delete(line_id)` devuelve True si borró, False si no existía.
    - `LineRepo._to_dict(row)` expone exactamente `{"id","camera_id","name","start_x_frac","start_y_frac","end_x_frac","end_y_frac","enabled"}`.
    - `RuleRepo.get(rule_id)` devuelve el dict de una regla (mismo shape que `_to_dict` de la lista) o `None` si no existe.
  </behavior>
  <action>
    Añadir `class LineRepo` inmediatamente después de `RuleRepo` (o antes de `ZoneRepo`,
    agrupado con los repos de configuración de cámara) copiando el molde exacto de
    `ZoneRepo`: `list(camera_id: str | None = None)`, `upsert(line_id: str, camera_id: str,
    name: str, start_x_frac: float, start_y_frac: float, end_x_frac: float,
    end_y_frac: float, enabled: bool = True)`, `delete(line_id: str) -> bool`,
    `_to_dict(line: models.Line) -> dict[str, Any]` con las 8 claves del modelo
    (`id, camera_id, name, start_x_frac, start_y_frac, end_x_frac, end_y_frac, enabled`).
    `models.Line` no tiene `updated_at` — no inventar la columna (ya advertido en
    33-PATTERNS.md).

    Añadir a `RuleRepo` un método `async def get(self, rule_id: str) -> dict[str, Any] | None:`
    que hace `await session.get(models.Rule, rule_id)` y devuelve `self._to_dict(r)` o `None`
    — mismo patrón de sesión de solo lectura que `list()`, sin `session.begin()` (no muta).
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/test_repositories.py -k "Line or rule_get" -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "class LineRepo" backend/storage/repositories.py` encuentra la clase.
    - `grep -n "async def get" backend/storage/repositories.py` encuentra el nuevo método en RuleRepo (verificar por contexto: línea inmediatamente bajo `class RuleRepo`).
    - `pytest tests/test_repositories.py -q` termina en verde, incluyendo los tests nuevos para LineRepo (list/upsert/update/delete) y RuleRepo.get (hit y miss).
  </acceptance_criteria>
  <done>LineRepo y RuleRepo.get() existen, con tests unitarios verdes contra una BD SQLite en memoria (mismo fixture que el resto del fichero).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RuleEngine.would_match() público + is_schedule_active()</name>
  <files>backend/events/rules.py, tests/test_rule_engine.py</files>
  <read_first>
    backend/events/rules.py completo (210 líneas — ya en contexto arriba, pero releer para
    ubicar el punto de inserción exacto: `would_match` como método de `RuleEngine` junto a
    `match()`/`run_actions()`; `is_schedule_active` como función de módulo junto a
    `_parse_time_range`/`_time_in_range`)
    tests/test_rule_engine.py (estilo de test existente para `match()`/`_matches()`, copiar
    construcción de `Event`/`When` de fixture)
  </read_first>
  <behavior>
    - `RuleEngine.would_match(when, event)` devuelve lo mismo que `_matches(when, event)`
      para los mismos argumentos, y NO modifica `self._last_fired` (llamar dos veces con
      un evento que debounce-ría en `match()` no cambia el resultado de `would_match`).
    - `is_schedule_active(None)` → `True` (sin horario = siempre activo).
    - `is_schedule_active({"days": [0]}, now=<un martes>)` → `False`.
    - `is_schedule_active({"time_range": "23:00-06:00"}, now=<00:30>)` → `True` (cruce de medianoche).
    - `is_schedule_active({"time_range": "09:00-17:00", "days": [0,1,2,3,4]}, now=<sábado 10:00>)` → `False`.
  </behavior>
  <action>
    En `class RuleEngine`, añadir método público:
    ```python
    def would_match(self, when: When, event: Event) -> bool:
        """Envoltorio publico y sin efectos de _matches() — para POST /rules/{id}/test
        (RULE-05). A diferencia de match(), NO purga ni actualiza self._last_fired: probar
        una regla contra el historico nunca debe alterar su debounce en produccion."""
        return _matches(when, event)
    ```
    Como función de módulo (junto a `_time_in_range`, antes de `load_rules`), añadir:
    ```python
    def is_schedule_active(schedule: dict[str, Any] | None, now: datetime.datetime | None = None) -> bool:
        """Horario propio de zona (OPS-23). None = sin restriccion, siempre activa.
        Reutiliza _parse_time_range/_time_in_range (mismo modulo, misma logica que When.time_range/When.days) —
        no reimplementar el parseo de rango horario."""
        if not schedule:
            return True
        now = now or datetime.datetime.now()
        days = schedule.get("days")
        if days is not None and now.weekday() not in days:
            return False
        time_range = schedule.get("time_range")
        if time_range:
            start, end = _parse_time_range(time_range)
            if not _time_in_range(now.time(), start, end):
                return False
        return True
    ```
    Necesita `from typing import Any` — ya importado en el fichero (`Awaitable, Callable,
    Literal, Protocol` en la misma línea de `typing`); añadir `Any` a esa importación.
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/test_rule_engine.py -k "would_match or schedule_active" -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "def would_match" backend/events/rules.py` lo encuentra dentro de `class RuleEngine`.
    - `grep -n "def is_schedule_active" backend/events/rules.py` lo encuentra como función de módulo.
    - Test que llama `engine.match(event)` seguido de `engine.would_match(rule.when, event)` sobre una regla con `debounce_secs > 0` demuestra que `would_match` no queda afectado por el debounce ya registrado por `match()`.
    - `pytest tests/test_rule_engine.py -q` termina en verde (suite completa del fichero, no solo los tests nuevos).
  </acceptance_criteria>
  <done>would_match() e is_schedule_active() existen, documentados, con tests que cubren cruce de medianoche, filtro de días y el caso sin horario.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB → RuleEngine.would_match | La `definition` de una regla persistida se re-valida como `Rule` Pydantic antes de llegar aquí (Plan 33-06) — este plan no valida entrada, es lógica pura sobre tipos ya validados |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-01 | Tampering | `is_schedule_active` con `time_range` malformado | accept | La función solo se invoca en este plan desde tests; el Plan 33-05 (consumidor real) valida el `schedule` en persistencia (Plan 33-03/33-08), así que un `time_range` corrupto nunca llega vivo al pipeline — documentado, no mitigado aquí porque este plan no persiste datos |
</threat_model>

<verification>
```bash
.venv/Scripts/python.exe -m pytest tests/test_repositories.py tests/test_rule_engine.py -q
```
Ambos ficheros en verde. No se relanza la suite completa: plan de solo dos ficheros nuevos/puros, sin tocar pipeline/API/configuración.
</verification>

<success_criteria>
- `LineRepo` disponible con la misma forma que `ZoneRepo`/`RuleRepo`.
- `RuleRepo.get(rule_id)` disponible.
- `RuleEngine.would_match()` público y sin efectos secundarios.
- `is_schedule_active()` pública, reutilizando el parseo de rango horario ya existente.
- Tests unitarios de ambos ficheros en verde.
</success_criteria>

<output>
Al terminar, crear `.planning/phases/33-editores-visuales-de-zonas-l-neas-y-reglas/33-01-SUMMARY.md`
</output>
