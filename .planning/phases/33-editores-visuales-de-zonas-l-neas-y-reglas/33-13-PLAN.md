---
phase: 33-editores-visuales-de-zonas-l-neas-y-reglas
plan: 13
type: execute
wave: 5
depends_on: ["33-08", "33-10", "33-11", "33-12"]
files_modified:
  - frontend/js/views/camera.js
  - frontend/index.html
  - frontend/js/app.js
  - frontend/js/views/settings-section.js
  - tests/test_frontend_modules.py
autonomous: true
requirements: [OPS-21, OPS-22, OPS-23, OPS-24, RULE-05]

must_haves:
  truths:
    - "Desde la vista Cámara, el operador puede abrir el editor de zonas/líneas sobre el vídeo en vivo y el editor de reglas, sin navegar a otra pestaña"
    - "Las subsecciones de solo lectura de Ajustes (zonas/reglas/líneas) enlazan al editor real en Cámara"
    - "La tarjeta 'Zonas de interés' de Operaciones (Fase 13, formulario JSON manual) deja de existir — el editor visual de Cámara es la única superficie de edición"
  artifacts:
    - path: "frontend/index.html"
      provides: "armazón HTML de los tres paneles (zonas/líneas/reglas) dentro de #view-camara, con los ids exactos que 33-10/33-11/33-12 ya esperaban; tarjeta legacy de zonas retirada de #view-operaciones"
    - path: "frontend/js/views/camera.js"
      provides: "llamada a initZoneEditor()/initLineEditor()/initRulesEditor() en initCamera()"
  key_links:
    - from: "frontend/js/app.js"
      to: "frontend/js/views/camera.js:initCamera"
      via: "ya existente desde la Fase 32, sin cambios de orden"
      pattern: "initCamera\\(\\)"
    - from: "frontend/index.html:#view-camara"
      to: "frontend/js/components/zoneEditor.js"
      via: "ids #zone-line-canvas/#zone-list/... consumidos literalmente"
      pattern: "zone-line-canvas"
---

<objective>
Cierra la fase por el lado del frontend: monta en el DOM real (`index.html`, dentro del
tabpanel Cámara ya existente desde la Fase 32) los tres paneles que los Planes
33-10/33-11/33-12 construyeron contra un contrato de ids documentado pero sin marcado
real. Llama `initZoneEditor()`/`initLineEditor()`/`initRulesEditor()` desde `camera.js`
(mismo patrón que `initCameraQuick()`/`initSettings()` en la Fase 32). Actualiza las
subsecciones de solo lectura de Ajustes (`zonas_definidas`/`reglas_cargadas`/
`lineas_definidas`) para enlazar al editor real en Cámara (D-03: "no se duplican como
formularios completos en Ajustes"). Retira la tarjeta "Zonas de interés" de Operaciones
(Fase 13) — el editor visual de Cámara es ahora la única superficie de edición (D-02).
Actualiza `LOCKED_JS`/`LOCKED_CSS` con los módulos nuevos de la fase.

Purpose: sin este plan, todo el trabajo de los Planes 33-01..33-12 es código correcto
pero invisible — es el mismo rol que cumplió 32-07 para la Fase 32.
Output: vista Cámara con los tres editores funcionales sobre datos reales, Operaciones
sin el formulario JSON legacy, Ajustes enlazando al lugar correcto.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-editores-visuales-de-zonas-l-neas-y-reglas/33-CONTEXT.md
@.planning/phases/33-editores-visuales-de-zonas-l-neas-y-reglas/33-RESEARCH.md
@.planning/phases/33-editores-visuales-de-zonas-l-neas-y-reglas/33-PATTERNS.md

<interfaces>
Contrato de ids del editor de zonas/líneas — cabecera de `frontend/js/components/zoneEditor.js`
y `frontend/js/components/lineEditor.js` tras los Planes 33-10/33-11 (releer ambas
cabeceras completas antes de escribir el HTML — son la fuente de verdad de los ids
exactos, este resumen puede quedar desactualizado si esos planes ajustaron algo):
`#zone-line-canvas` (canvas compartido, modo zona/línea), `#zone-mode-toggle`,
`#zone-list`, `#zone-form-kind`, `#zone-form-days`, `#zone-form-time-start`,
`#zone-form-time-end`, `#zone-save-btn`, `#zone-cancel-btn`, `#zone-new-btn`,
`#line-list`, `#line-form-name`, `#line-save-btn`, `#line-cancel-btn`, `#line-new-btn`.

Contrato de ids del editor de reglas — cabecera de `frontend/js/views/rules-editor.js`
tras el Plan 33-12: `#rules-panel`, `#rules-list`, `#rule-new-btn`, `#rule-form`,
`#rule-test-btn`, `#rule-test-result`.

Patrón de montaje de tabpanel Cámara (Fase 32, ya en producción) —
`frontend/index.html:850-989` (bloque completo `<section id="view-camara" ...>`, ya
citado en detalle en 33-RESEARCH.md/STATE.md — releer el fichero real, no confiar en la
cita, puede haber cambiado). Añadir los tres paneles nuevos como secciones adicionales
DENTRO de `#view-camara`, después del bloque de "Ajustes rápidos" (línea ~929) y antes
de la sección derecha de RTSP/teselas, o como una fila nueva a lo ancho — decisión de
maquetación de este plan, con el único requisito duro de que el `<canvas>` del editor se
superponga a `#camera-feed` (mismo contenedor `.card` que ya envuelve la imagen, líneas
856-873) sin romper el `aspect-ratio:16/9` ya fijado.

Patrón de wiring en `camera.js` — `frontend/js/views/camera.js` tras la Fase 32 (119
líneas, `initCamera()` como único punto de entrada llamado desde `app.js`):
```javascript
export function initCamera() {
  loadRtspCard();
  setInterval(tickCameraFooter, 5000);
  // ... resto de la inicialización existente
}
```
Añadir las tres llamadas nuevas al final de `initCamera()`:
`initZoneEditor(); initLineEditor(); initRulesEditor();` — mismo criterio "seguro de
llamar aunque la pestaña no esté activa" que ya documenta el comentario de `app.js:68-71`
sobre `initCamera`/`initCameraQuick`/`initSettings` (los tres editores nuevos deben
seguir el mismo principio: armar listeners sin disparar peticiones de red hasta que el
operador interactúe, tal como ya especificaron los Planes 33-10/33-11/33-12).

`frontend/js/app.js` — imports nuevos junto a los ya existentes de `camera.js`/
`camera-quick.js`/`settings.js` (líneas 17-19):
```javascript
import { initCamera } from './views/camera.js';
import { initCameraQuick } from './views/camera-quick.js';
import { initSettings } from './views/settings.js';
```
Si `initZoneEditor`/`initLineEditor`/`initRulesEditor` se llaman DESDE `camera.js` (no
directamente desde `app.js`), `app.js` no necesita imports nuevos — decisión de este
plan: llamarlos desde `camera.js` (cohesión con D-03, que sitúa el editor dentro de la
vista Cámara) en vez de añadir tres imports más a `app.js`.

Subsecciones de solo lectura de Ajustes — `frontend/js/views/settings-section.js:71-80`
(código real, ya usado por el Plan 33-08 para el `external_source`; este plan añade el
ENLACE visual, no cambia el fetch):
```javascript
const isZones = group.key === 'zonas_definidas';
const items = isZones ? (data.zones ?? []) : (data.rules ?? []);
if (items.length === 0) {
  const [title, hint] = isZones
    ? ['Sin zonas definidas', 'Las zonas se crean y se borran desde el panel «Zonas de interés» de Operaciones.']
    : ['Sin reglas cargadas', 'Las reglas se definen en el fichero config/rules.yaml.'];
  ...
}
```
Los DOS textos de `hint` están obsoletos tras esta fase: "panel «Zonas de interés» de
Operaciones" ya no existe (este mismo plan lo retira) y "se definen en el fichero
config/rules.yaml" ya no es cierto (Plan 33-06/33-08, ahora es la tabla `rules` vía
formulario). Actualizar ambos textos para que apunten a "la pestaña Cámara" (sin enlace
`<a>` clicable obligatorio — un texto que diga "Se crean y editan desde la pestaña
Cámara → Zonas/Líneas" o "→ Reglas" es suficiente y consistente con el resto de textos
de esta función, que ya son solo informativos). `group.key === 'lineas_definidas'`
necesita su propia rama en el `isZones ? ... : ...` — pasa de un ternario de 2 vías a
gestionar 3 tipos de grupo (`zonas_definidas`/`lineas_definidas`/`reglas_cargadas`);
reescribir la selección de `items`/`title`/`hint` con un pequeño `switch(group.key)` o
un objeto de lookup en vez de anidar ternarios, para no degradar la legibilidad ya
establecida.

Tarjeta "Zonas de interés" a retirar — `frontend/index.html:629-671` (código real, bloque
completo `<!-- INTEREST ZONES -->`, ya citado en la sesión de investigación de este
mismo plan — releer el fichero real antes de borrar, puede haberse desplazado). El
`import { loadZones, bindZoneForm } from './components/zoneEditor.js'` y las llamadas
`bindZoneForm(); ... loadZones();` en `frontend/js/app.js` (líneas 8,26,57) deben
retirarse junto con el HTML — `zoneEditor.js` (Plan 33-10) ya NO exporta
`loadZones`/`bindZoneForm` con esa firma legacy (exporta `initZoneEditor` en su lugar),
así que dejar esas líneas en `app.js` rompería la carga del módulo por completo, no solo
seguiría un patrón obsoleto.

`LOCKED_JS`/`LOCKED_CSS` — `tests/test_frontend_modules.py:34-68` (lista real, ya en
contexto de sesiones previas): añadir `"components/lineEditor.js"`,
`"views/rules-form.js"`, `"views/rules-editor.js"` a `LOCKED_JS` (agrupados bajo un
comentario `# Fase 33: editores visuales de zonas/lineas/reglas`, mismo estilo que los
comentarios de fase existentes). `components/zoneEditor.js` YA está en `LOCKED_JS`
(Fase 28) — no duplicar la entrada.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Armazón HTML de los tres editores en #view-camara + retirada de la tarjeta legacy</name>
  <files>frontend/index.html</files>
  <read_first>
    frontend/index.html completo (2000+ líneas — usar Grep para localizar
    `id="view-camara"`, `<!-- INTEREST ZONES -->`, y el bloque de "Ajustes rápidos" antes
    de editar, no asumir números de línea de este PLAN.md como definitivos)
    frontend/js/components/zoneEditor.js tras el Plan 33-10 (cabecera con el contrato de ids)
    frontend/js/components/lineEditor.js tras el Plan 33-11 (cabecera con el contrato de ids)
    frontend/js/views/rules-editor.js tras el Plan 33-12 (cabecera con el contrato de ids)
  </read_first>
  <acceptance_criteria>
    - `grep -n "zone-line-canvas" frontend/index.html` la encuentra dentro de `#view-camara`.
    - `grep -n "rules-panel" frontend/index.html` la encuentra dentro de `#view-camara`.
    - `grep -n "add-zone-panel\|zone-points-input\|zones-list" frontend/index.html` NO produce coincidencias (tarjeta legacy retirada por completo).
    - Todos los ids listados en la cabecera de `zoneEditor.js`/`lineEditor.js`/`rules-editor.js` existen literalmente en `frontend/index.html` (verificar uno por uno con grep).
  </acceptance_criteria>
  <action>
    Dentro de `#view-camara`, añadir tres bloques nuevos con los ids exactos documentados
    por los Planes 33-10/33-11/33-12 (releídos en `<read_first>`): un `<canvas
    id="zone-line-canvas">` superpuesto al contenedor `.card` que envuelve `#camera-feed`
    (mismo patrón `position:relative` en el padre + `position:absolute;inset:0` en el
    canvas que ya usaría cualquier overlay sobre `<img>`), un toggle de modo
    (`#zone-mode-toggle`), listas/formularios de zona y línea, y un panel de reglas
    (`#rules-panel`) como sección adicional dentro del mismo tabpanel — Tailwind
    utility-first, sin CSS nueva salvo que algo documentado por 33-09/33-10/33-11/33-12
    ya la requiera.

    Retirar por completo el bloque `<!-- INTEREST ZONES -->` de `#view-operaciones`
    (`#btn-add-zone`, `#add-zone-panel`, `#zone-id-input`, `#zone-name-input`,
    `#zone-points-input`, `#zone-cancel-btn`, `#zone-save-btn`, `#zone-msg`,
    `#zones-list`, `#zones-empty` — todo el `<div class="card">` que los contiene).
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/test_frontend_modules.py -k "no_inline_logic" -q</automated>
  </verify>
  <done>Armazón HTML de los tres editores montado en Cámara con los ids exactos esperados; tarjeta legacy de Operaciones retirada.</done>
</task>

<task type="auto">
  <name>Task 2: Wiring en camera.js/app.js + enlaces de Ajustes actualizados</name>
  <files>frontend/js/views/camera.js, frontend/js/app.js, frontend/js/views/settings-section.js</files>
  <read_first>
    frontend/js/views/camera.js completo (119 líneas, ya extractado el patrón `initCamera()`)
    frontend/js/app.js completo (75 líneas, imports/llamadas de `bindZoneForm`/`loadZones` a retirar, ya extractado)
    frontend/js/views/settings-section.js:60-100 (bloque `_loadExternalGroup`, ya extractado)
  </read_first>
  <acceptance_criteria>
    - `grep -n "initZoneEditor\|initLineEditor\|initRulesEditor" frontend/js/views/camera.js` confirma las tres llamadas dentro de `initCamera()`.
    - `grep -n "bindZoneForm\|loadZones" frontend/js/app.js` NO produce coincidencias (import y llamadas legacy retiradas).
    - `grep -n "lineas_definidas" frontend/js/views/settings-section.js` confirma la tercera rama del selector de grupo externo.
    - `grep -n "config/rules.yaml" frontend/js/views/settings-section.js` NO produce coincidencias (texto de hint actualizado).
    - `pytest tests/test_frontend_modules.py -q` termina en verde.
  </acceptance_criteria>
  <action>
    En `frontend/js/views/camera.js`, importar `{ initZoneEditor }` de
    `../components/zoneEditor.js`, `{ initLineEditor }` de `../components/lineEditor.js`,
    `{ initRulesEditor }` de `./rules-editor.js`, y llamarlas al final de `initCamera()`.

    En `frontend/js/app.js`, eliminar la línea de import
    `import { loadZones, bindZoneForm } from './components/zoneEditor.js';` y las dos
    llamadas `bindZoneForm();`/`loadZones();` en el cuerpo de `DOMContentLoaded`.

    En `frontend/js/views/settings-section.js`, `_loadExternalGroup`: sustituir el
    ternario `isZones ? ... : ...` por un `switch (group.key)` (o lookup object) con tres
    ramas (`zonas_definidas`, `lineas_definidas`, `reglas_cargadas`), cada una resolviendo
    `items` desde `data.zones`/`data.lines`/`data.rules` según corresponda, y actualizando
    los dos textos de hint obsoletos citados en `<interfaces>` para referenciar la
    pestaña Cámara en vez de Operaciones/YAML.
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/test_frontend_modules.py -q</automated>
  </verify>
  <done>Los tres editores se inicializan desde camera.js, app.js sin referencias al módulo legacy retirado, y Ajustes enlaza al lugar correcto con textos actualizados.</done>
</task>

<task type="auto">
  <name>Task 3: LOCKED_JS/LOCKED_CSS + checkpoint de arranque con servidor real</name>
  <files>tests/test_frontend_modules.py</files>
  <read_first>
    tests/test_frontend_modules.py:34-68 (LOCKED_JS completo, ya extractado)
  </read_first>
  <acceptance_criteria>
    - `grep -n "components/lineEditor.js" tests/test_frontend_modules.py` confirma la entrada nueva.
    - `grep -n "views/rules-editor.js" tests/test_frontend_modules.py` confirma la entrada nueva.
    - `grep -n "views/rules-form.js" tests/test_frontend_modules.py` confirma la entrada nueva.
    - `pytest tests/test_frontend_modules.py -q` termina en verde (TEST_js_modules_exist incluido).
  </acceptance_criteria>
  <action>
    Añadir a `LOCKED_JS`, agrupadas bajo un comentario `# Fase 33: editores visuales de
    zonas/lineas/reglas`: `"components/lineEditor.js"`, `"views/rules-form.js"`,
    `"views/rules-editor.js"`. No añadir nada a `LOCKED_CSS` (sin fichero CSS nuevo en
    esta fase — el Plan 33-09 solo extendió `components.css`, ya locked desde la Fase 28).
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/ -q</automated>
  </verify>
  <done>LOCKED_JS incorpora los tres módulos nuevos de la fase; suite completa relanzada (toca navegación/wiring compartido).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Plan de wiring/maquetación, sin superficie de amenaza nueva — hereda los threat models de los planes que integra |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-20 | N/A | — | accept | Sin cambio de superficie de ataque: solo maquetación y wiring de módulos ya validados individualmente |
</threat_model>

<verification>
```bash
.venv/Scripts/python.exe -m pytest tests/ -q
```
Suite completa — toca navegación/wiring compartido, mismo criterio que 32-07.
</verification>

<success_criteria>
- Los tres editores visibles y funcionales dentro de la vista Cámara.
- Sin superficie de edición duplicada (Operaciones/Ajustes ya no editan, solo enlazan/leen).
- LOCKED_JS actualizado.
</success_criteria>

<output>
Al terminar, crear `.planning/phases/33-editores-visuales-de-zonas-l-neas-y-reglas/33-13-SUMMARY.md`
</output>
