---
phase: 33-editores-visuales-de-zonas-l-neas-y-reglas
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/api/v2/zones.py
  - tests/test_zones_api.py
autonomous: true
requirements: [OPS-21, OPS-23]

must_haves:
  truths:
    - "Se puede crear/actualizar una zona por HTTP con polígono normalizado, tipo y horario, y el servidor rechaza polígonos con menos de 3 puntos o coordenadas fuera de [0,1]"
    - "Al guardar o borrar una zona, el cambio se empuja de inmediato a los pipelines de cámara activos (hot-reload, sin reiniciar)"
    - "Las zonas heredadas de la Fase 27 (kind=exclude_objects) se pueden leer y escribir sin que el servidor las rechace"
  artifacts:
    - path: "backend/api/v2/zones.py"
      provides: "APIRouter prefix=/api/v2/zones — GET, POST (upsert), DELETE /{zone_id}"
    - path: "tests/test_zones_api.py"
      provides: "cobertura de validación de polígono/rango/kind/schedule + hot-reload push"
  key_links:
    - from: "backend/api/v2/zones.py"
      to: "backend/storage/repositories.py:ZoneRepo"
      via: "_zone_repo() factory, mismo molde que _event_repo() de events.py"
      pattern: "ZoneRepo\\(get_session_factory\\(\\)\\)"
    - from: "backend/api/v2/zones.py"
      to: "backend/pipeline/manager.py:CameraPipeline.set_zones"
      via: "camera_manager.all() tras persistir"
      pattern: "\\.set_zones\\("
---

<objective>
Nuevo router `/api/v2/zones` (GET/POST-upsert/DELETE) sobre `ZoneRepo` (esquema v2,
D-02): valida polígono (≥3 puntos, fracciones en [0,1]), tipo (`kind` — OPS-23, con
`exclude_objects` conservado como cuarto valor heredado de la Fase 27, no remapeado) y
horario (`schedule`), persiste, y empuja el cambio a todos los pipelines de cámara vivos
para cumplir el criterio 6 (<1s, sin reiniciar) — reutilizando `CameraPipeline.set_zones`,
que ya existe y ya es thread-safe.

Purpose: es el único endpoint HTTP que el editor visual de zonas (Plan 33-10) necesita
para CRUD real; sustituye a `/api/zones` (v1) que se retira en el Plan 33-08 de
integración final, una vez todo lo que lo usa (frontend, `config_schema.py`) apunte aquí.
Output: `backend/api/v2/zones.py` con `router` y `configure(camera_manager)`, tests
unitarios contra una app FastAPI local (sin tocar `backend.main`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-editores-visuales-de-zonas-l-neas-y-reglas/33-CONTEXT.md
@.planning/phases/33-editores-visuales-de-zonas-l-neas-y-reglas/33-RESEARCH.md
@.planning/phases/33-editores-visuales-de-zonas-l-neas-y-reglas/33-PATTERNS.md

<interfaces>
Plantilla de router v2 completa — `backend/api/v2/events.py:1-39` (patrón de
auth/rate-limit heredados, factory de repo):
```python
from fastapi import APIRouter, Body, HTTPException, Query, Request
from backend.api.v2.deps import V2_RATE_LIMIT, limiter, pagination_limit, snapshot_url
from backend.database import get_session_factory
from backend.storage.repositories import EventRepo, RecordingRepo

router = APIRouter(prefix="/api/v2/events", tags=["events"])

def _event_repo() -> EventRepo:
    return EventRepo(get_session_factory())

@router.get("")
@limiter.limit(V2_RATE_LIMIT)
async def list_events(request: Request, ...) -> dict[str, Any]:
    ...
```

Plantilla de wiring `configure()` — `backend/api/v2/config.py:43-58`:
```python
_camera_manager: Any = None
_event_engine: Any = None

def configure(camera_manager: Any, event_engine: Any = None) -> None:
    """Wire the live CameraManager/EventEngine instances. Called once from main.py's lifespan."""
    global _camera_manager, _event_engine
    _camera_manager = camera_manager
    _event_engine = event_engine
```

Validación de polígono (patrón v1 a replicar, `backend/main.py:1107-1130`):
```python
zone_id = str(body.get("id", "")).strip()
name = str(body.get("name", "")).strip()
if not zone_id or not name:
    raise HTTPException(status_code=400, detail="id and name are required")
if len(zone_id) > 50 or len(name) > 100:
    raise HTTPException(status_code=400, detail="id/name too long")
pts = body.get("polygon")
if not isinstance(pts, list) or len(pts) < 3:
    raise HTTPException(status_code=422, detail="polygon must have >=3 points")
# v2 AÑADE lo que v1 nunca validó: rango [0,1] por punto (33-RESEARCH.md Anti-patterns)
for p in pts:
    if not (isinstance(p, list) and len(p) == 2 and all(0.0 <= float(c) <= 1.0 for c in p)):
        raise HTTPException(status_code=422, detail=f"point out of [0,1] range: {p}")
```

`ZoneRepo` completo — `backend/storage/repositories.py:704-751` (ya en el repo, sin
cambios en este plan): `list(camera_id=None)`, `upsert(zone_id, camera_id, name,
polygon, kind=None, schedule=None, enabled=True)`, `delete(zone_id) -> bool`,
`_to_dict(z) -> {"id","camera_id","name","polygon","kind","schedule","enabled"}`.

`CameraPipeline.set_zones` y `CameraManager.all()` — `backend/pipeline/manager.py:322-327,432-433`:
```python
def set_zones(self, zones: list[dict]) -> None:
    if self.detection:
        self.detection.set_zones(zones)

def all(self) -> list[CameraPipeline]:
    return list(self._pipelines.values())
```
Cada `CameraPipeline` tiene atributo `.camera_id` (asignado en `CameraManager.add(camera_id, ...)`,
`backend/pipeline/manager.py`). El hot-reload debe filtrar por cámara:
`await zone_repo.list(camera_id=pipeline.camera_id)` — NO enviar todas las zonas a
todos los pipelines (el proyecto corre 1 cámara hoy, pero el router es multi-cámara-shaped).

`is_schedule_active` — `backend/events/rules.py` (Plan 33-01, ya cerrado en Wave 1;
usarlo aquí SOLO para validar que el `schedule` recibido no lanza excepción al parsear,
envuelto en try/except → 422 si el formato es inválido; NO se usa para lógica de
negocio en este router, eso vive en el pipeline, Plan 33-05).

Test-app local — `tests/test_config_api.py:31-54` (patrón exacto a clonar, sustituyendo
`config_module` por el módulo de este router):
```python
def _local_app() -> FastAPI:
    app = FastAPI()
    app.state.limiter = limiter
    app.add_exception_handler(RateLimitExceeded, _rate_limit_exceeded_handler)
    app.include_router(zones_module.router)
    return app

async def _client() -> httpx.AsyncClient:
    return httpx.AsyncClient(transport=ASGITransport(app=_local_app()), base_url="http://test")
```
Mockear `ZoneRepo`/`CameraManager` con `unittest.mock.MagicMock`/`AsyncMock` vía
`patch.object(zones_module, "_zone_repo", return_value=fake_repo)`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Router /api/v2/zones — CRUD, validación, hot-reload</name>
  <files>backend/api/v2/zones.py, tests/test_zones_api.py</files>
  <read_first>
    backend/api/v2/events.py completo (165 líneas — forma de router v2, auth/rate-limit heredados)
    backend/api/v2/config.py:1-58 (patrón `configure()` con globals de módulo)
    backend/main.py:1097-1150 (los tres endpoints v1 a superar: GET/POST/DELETE/stats — leer
    completo, incluida `/api/zones/stats`, para no perder ninguna validación existente)
    backend/storage/repositories.py:704-751 (ZoneRepo, sin cambios — solo consumo)
    backend/pipeline/manager.py:300-330,418-434 (CameraPipeline.set_zones/get_zone_stats, CameraManager.all/get)
    backend/events/rules.py (is_schedule_active, del Plan 33-01 — ya cerrado)
    tests/test_config_api.py:1-70 (patrón de test-app local + mocking con AsyncMock)
  </read_first>
  <behavior>
    - `GET /api/v2/zones` sin query → `{"zones": [...]}` (todas, cualquier cámara).
    - `GET /api/v2/zones?camera_id=cam1` → filtra por cámara.
    - `POST /api/v2/zones` con `{"id","name","polygon"}` válido y sin `camera_id` → usa
      `camera_id="cam1"` por defecto, persiste, responde `{"zones": [...]}` (lista completa
      actualizada, mismo contrato que el v1 legacy) y llama `pipeline.set_zones(...)` en
      cada pipeline de `camera_manager.all()` cuyo `camera_id` coincide.
    - `POST` con `polygon` de 2 puntos → 422, detalle menciona `>=3 points`.
    - `POST` con un punto `[1.5, 0.2]` (fuera de rango) → 422, detalle menciona el punto exacto.
    - `POST` con `kind="exclude_objects"` → 200, se persiste sin rechazo (compatibilidad Fase 27).
    - `POST` con `kind="invalido"` → 422.
    - `POST` con `schedule={"time_range": "25:00-08:00"}` (hora inválida) → 422.
    - `DELETE /api/v2/zones/{id}` inexistente → 404. Existente → 200, hot-reload push, `{"zones": [...]}` restante.
    - Sin `camera_manager` configurado (`configure()` nunca llamado) → CRUD sigue funcionando (persistencia OK), simplemente no hay push de hot-reload (no debe lanzar excepción).
  </behavior>
  <action>
    Crear `backend/api/v2/zones.py` con docstring de cabecera (mismo estilo que
    `config.py:1-21`: qué hace el router, referencia a D-02/OPS-21/OPS-23, nota de
    auth/rate-limit heredados). Definir:
    - `router = APIRouter(prefix="/api/v2/zones", tags=["zones"])`.
    - `_camera_manager: Any = None` + `def configure(camera_manager: Any) -> None`.
    - `_zone_repo() -> ZoneRepo: return ZoneRepo(get_session_factory())`.
    - Un modelo Pydantic local `ZoneIn` (`id: str, camera_id: str = "cam1", name: str,
      polygon: list[list[float]], kind: str | None = None, schedule: dict[str, Any] | None
      = None, enabled: bool = True`) — NO reutilizar `ZoneBody(dict)` del v1 (ese patrón
      era deliberadamente débil; aquí Pydantic ya da 422 estructurado gratis para los tipos
      básicos, y las validaciones semánticas van en un `@field_validator`).
    - `_KIND_VALUES = {"counting", "restricted", "exclusion", "exclude_objects"}` (constante
      de módulo, con comentario citando 33-RESEARCH.md Pitfall 2: `exclude_objects` es
      heredado de la Fase 27 y se conserva, no se remapea).
    - Validadores en `ZoneIn`: `len(id)<=50`, `len(name)<=100`, `len(polygon)>=3`, cada
      punto `len==2` y `0.0<=x<=1.0`/`0.0<=y<=1.0`, `kind in _KIND_VALUES` si no es None,
      y si `schedule` no es None, `is_schedule_active(schedule)` envuelto en
      `try/except Exception: raise ValueError("schedule invalido")` (dry-run de parseo).
    - `async def _push_hot_reload(zone_repo: ZoneRepo) -> None:` que, si
      `_camera_manager is not None`, itera `_camera_manager.all()` y llama
      `pipeline.set_zones(await zone_repo.list(camera_id=pipeline.camera_id))` por cada
      pipeline — mismo patrón que `main.py:1129`/`1141` pero multi-cámara.
    - `@router.get("")` `@limiter.limit(V2_RATE_LIMIT)` `list_zones(request, camera_id:
      str | None = None)`.
    - `@router.post("")` `@limiter.limit(V2_RATE_LIMIT)` `upsert_zone(request, body:
      ZoneIn)`: llama `zone_repo.upsert(...)`, luego `_push_hot_reload`, devuelve
      `{"zones": await zone_repo.list()}`.
    - `@router.delete("/{zone_id}")` `@limiter.limit(V2_RATE_LIMIT)` `delete_zone(request,
      zone_id: str)`: 404 si `not await zone_repo.delete(zone_id)`, si no
      `_push_hot_reload` + `{"zones": await zone_repo.list()}`.
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/test_zones_api.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "prefix=\"/api/v2/zones\"" backend/api/v2/zones.py` confirma el router.
    - `grep -n "exclude_objects" backend/api/v2/zones.py` confirma el valor heredado tolerado.
    - `grep -n "0.0 <= " backend/api/v2/zones.py` o equivalente confirma la validación de rango por punto (ausente en el v1 legacy, exigida por 33-RESEARCH.md).
    - `pytest tests/test_zones_api.py -q` termina en verde cubriendo los 10 casos del `<behavior>`.
    - `grep -n "def configure" backend/api/v2/zones.py` confirma el punto de wiring para el Plan 33-08.
  </acceptance_criteria>
  <done>Router /api/v2/zones completo, con validación de polígono/rango/kind/schedule, hot-reload multi-cámara, y tests unitarios en verde contra una app FastAPI local.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Cliente (navegador) → POST/DELETE /api/v2/zones | Entrada no confiable: polígono, kind, schedule, id/name — cualquier credencial Basic Auth válida puede escribir (sin roles, mismo modelo que el resto de /api/v2, ya documentado en config.py) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-03 | Tampering | `polygon` con coordenadas fuera de `[0,1]` o degenerado (<3 puntos) | mitigate | Validación Pydantic por punto en `ZoneIn`, 422 con el punto exacto — extiende la validación v1 que solo comprobaba `len(pts)>=3` |
| T-33-04 | Tampering | `kind` arbitrario no reconocido | mitigate | `_KIND_VALUES` cerrado (4 valores), 422 en cualquier otro |
| T-33-05 | Tampering | `schedule` con `time_range`/`days` malformado que rompería el gating del pipeline en producción (Plan 33-05) | mitigate | Dry-run de `is_schedule_active(schedule)` en el validator, 422 antes de persistir |
| T-33-06 | Denial of Service | `POST`/`DELETE` repetidos forzando rebuilds de zona en cada pipeline | mitigate | `@limiter.limit(V2_RATE_LIMIT)` (60/min) heredado del resto de `/api/v2`, mismo patrón que `config.py` |
</threat_model>

<verification>
```bash
.venv/Scripts/python.exe -m pytest tests/test_zones_api.py -q
```
Router probado con app FastAPI local (no `backend.main.app`) — el wiring real a
`camera_manager` llega en el Plan 33-08, que añade su propia comprobación de integración.
No se relanza la suite completa en este plan.
</verification>

<success_criteria>
- CRUD completo de zonas vía `/api/v2/zones`, con validación estricta de entrada.
- Hot-reload multi-cámara tras cada mutación.
- `exclude_objects` (Fase 27) preservado sin romper.
</success_criteria>

<output>
Al terminar, crear `.planning/phases/33-editores-visuales-de-zonas-l-neas-y-reglas/33-03-SUMMARY.md`
</output>
