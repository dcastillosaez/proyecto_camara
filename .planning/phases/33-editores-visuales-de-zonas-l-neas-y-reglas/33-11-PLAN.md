---
phase: 33-editores-visuales-de-zonas-l-neas-y-reglas
plan: 11
type: execute
wave: 2
depends_on: ["33-09"]
files_modified:
  - frontend/js/components/lineEditor.js
autonomous: true
requirements: [OPS-22]

must_haves:
  truths:
    - "El operador puede dibujar múltiples líneas de conteo sobre el vídeo, cada una con su propio nombre"
    - "Cada línea muestra un indicador visual de dirección (qué lado cuenta como entrada)"
    - "Las coordenadas de línea se envían al servidor siempre como fracciones [0,1], nunca píxeles absolutos"
  artifacts:
    - path: "frontend/js/components/lineEditor.js"
      provides: "initLineEditor()/drawLine/CRUD contra /api/v2/lines — canvas + lista de líneas"
  key_links:
    - from: "frontend/js/components/lineEditor.js"
      to: "/api/v2/lines"
      via: "fetch POST/DELETE"
      pattern: "fetch\\('/api/v2/lines"
    - from: "frontend/js/components/lineEditor.js"
      to: "frontend/js/components/videoCanvas.js:canvasClickToFrac"
      via: "import"
      pattern: "canvasClickToFrac"
---

<objective>
Nuevo módulo `frontend/js/components/lineEditor.js`: editor visual de líneas de conteo
sobre `<canvas>` (D-01, OPS-22 — N líneas por cámara, no solo la línea única heredada).
Dos clicks trazan una línea (inicio/fin); un pequeño triángulo perpendicular al punto
medio indica la dirección "entrada" (in) — la misma convención que ya usa
`sv.LineZone`/`PersonTracker` internamente (`crossed_in`/`crossed_out`), traducida a una
flecha visible. CRUD contra `/api/v2/lines` (Plan 33-07).

Purpose: es la pieza visual de OPS-22 que D-03 sitúa en la vista Cámara junto al editor
de zonas (Plan 33-10) — ambos comparten canvas math (Plan 33-09) pero viven en módulos
separados porque dibujan primitivas distintas (polígono cerrado vs. segmento con
dirección) y así cada fichero se mantiene bajo el límite de 300 líneas.
Output: `lineEditor.js` nuevo (~200 líneas).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-editores-visuales-de-zonas-l-neas-y-reglas/33-CONTEXT.md
@.planning/phases/33-editores-visuales-de-zonas-l-neas-y-reglas/33-RESEARCH.md
@.planning/phases/33-editores-visuales-de-zonas-l-neas-y-reglas/33-PATTERNS.md

<interfaces>
`canvasClickToFrac`/`syncCanvasToImage` — `frontend/js/components/videoCanvas.js` (Plan
33-09, ya cerrado): mismas firmas que en el Plan 33-10 (releer ese PLAN.md o
directamente el fichero fuente tras 33-09 — no reimplementar).

Contrato del router — Plan 33-07 (Wave 3, se ejecuta DESPUÉS de este plan; este módulo se
escribe contra el contrato ya fijado en 33-RESEARCH.md/33-PATTERNS.md, verificable en el
Plan 33-13 cuando ambos existan): `POST /api/v2/lines` body `{id, camera_id?, name,
start_x_frac, start_y_frac, end_x_frac, end_y_frac, enabled?}`. Respuesta
`{"lines": [...]}` (mismo molde que zonas). `GET /api/v2/lines` sin argumentos devuelve
todas. `DELETE /api/v2/lines/{id}`.

Dirección de cruce en `sv.LineZone` (referencia conceptual, `backend/tracker.py`, ya
refactorizado por el Plan 33-04): la dirección "in"/"out" la decide `supervision` según
el sentido del vector `start→end` (regla de la mano derecha del segmento) — el frontend
NO necesita replicar esa matemática, solo dibujar una flecha perpendicular al segmento
en su punto medio, apuntando hacia el lado que la convención del proyecto llama
"entrada" (documentar en el fichero, con un comentario, que la flecha es orientativa
para el operador y no se valida contra el servidor — es una ayuda visual, no un campo
persistido nuevo).

Import de toasts — mismo patrón que el resto del frontend:
`import { showToast } from '../views/dashboard.js';`

Anti-XSS: `createElement`/`textContent` para nombre de línea, igual que el Plan 33-10.

Mount point real (lo prepara este plan, lo conecta el Plan 33-13): mismo `<canvas>` que
el editor de zonas puede reutilizarse (`#zone-line-canvas`, definido por el Plan 33-10) o
uno propio — decisión de este plan, documentada en la cabecera del fichero. Recomendado:
REUTILIZAR el mismo canvas que 33-10 (un solo `<canvas>` superpuesto a `#camera-feed`,
con un selector de modo "Zonas" / "Líneas" que decide qué maneja el click, evitando dos
canvas apilados con eventos de ratón compitiendo) — documentar esta decisión explícitamente
para que el Plan 33-13 la implemente sin ambigüedad.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Motor de canvas para líneas + indicador de dirección</name>
  <files>frontend/js/components/lineEditor.js</files>
  <read_first>
    frontend/js/components/videoCanvas.js tras el Plan 33-09 (canvasClickToFrac/syncCanvasToImage)
    frontend/js/components/zoneEditor.js tras el Plan 33-10 (para decidir con precisión
    cómo comparte el mismo `<canvas>`/modo de edición — releer su contrato de ids documentado
    en cabecera, y replicar el mismo mecanismo de "modo activo" en vez de inventar uno propio)
  </read_first>
  <behavior>
    Sin framework de test JS — verificación mecánica (línea límite, import correcto, sin
    `innerHTML`), comportamiento real verificado en el checkpoint manual del Plan 33-14.
  </behavior>
  <action>
    Documentar en cabecera el contrato de ids: reutiliza `#zone-line-canvas` (Plan 33-10)
    en modo `"line"` (vs. `"zone"`), más `#line-list`, `#line-form-name`,
    `#line-save-btn`, `#line-cancel-btn`, `#line-new-btn`. Un primer click sobre el canvas
    en modo línea fija el punto de inicio; el segundo fija el punto de fin y completa el
    trazado (a diferencia de zonas, que acumulan N vértices). Mientras se traza, redibujar
    en cada `mousemove` un segmento "fantasma" desde el punto de inicio hasta la posición
    actual del ratón, para que el operador vea la línea antes de confirmar el segundo
    click. Tras el segundo click: calcular el punto medio, y dibujar un pequeño triángulo
    (3 líneas `ctx.lineTo`) perpendicular al segmento apuntando hacia un lado fijo por
    convención (documentar en comentario qué lado — p.ej. "a la derecha del vector
    start→end mirando desde start hacia end" — de forma determinista y reproducible).
    Exportar `initLineEditor()` como punto de entrada (mismo patrón lazy que
    `initZoneEditor()`).
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/test_frontend_modules.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "export function initLineEditor" frontend/js/components/lineEditor.js` la encuentra.
    - `grep -n "canvasClickToFrac" frontend/js/components/lineEditor.js` confirma el reuso de la matemática compartida.
    - `wc -l frontend/js/components/lineEditor.js` reporta ≤300.
  </acceptance_criteria>
  <done>Trazado de línea de dos clicks con indicador de dirección visible, compartiendo canvas math con el editor de zonas.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CRUD de línea contra /api/v2/lines</name>
  <files>frontend/js/components/lineEditor.js</files>
  <read_first>
    33-PATTERNS.md sección "backend/api/v2/zones.py / backend/api/v2/lines.py" (contrato
    de request/response ya documentado — el router real lo construye el Plan 33-07,
    Wave 3, pero el contrato de campos es estable desde la investigación)
  </read_first>
  <behavior>
    - `loadLines()` pinta `#line-list` desde `GET /api/v2/lines`, nombre por `textContent`.
    - Guardar una línea nueva hace `POST /api/v2/lines` con
      `{id, name, start_x_frac, start_y_frac, end_x_frac, end_y_frac, enabled: true}`.
    - Un 422 (línea degenerada, longitud ~0) se muestra como mensaje de error visible sin
      perder el trazado en curso.
    - Borrar pide `confirm()`, hace `DELETE /api/v2/lines/{id}`, repinta la lista.
  </behavior>
  <action>
    Añadir `loadLines()`, `saveLine()`, `deleteLine(id, name)` con el mismo patrón
    try/fetch/toast/422-mapping que `zoneEditor.js` (Plan 33-10) — mismo estilo de código,
    mismo import de `showToast`. El router de líneas (Plan 33-07) usa igual que zonas un
    body param Pydantic tipado, así que un 422 llega como `d.detail` en el shape NATIVO de
    FastAPI (lista de `{loc, msg, type}`) — renderizar el mensaje visible con
    `Array.isArray(d.detail) ? d.detail.map(e => e.msg).join(', ') : String(d.detail)` por
    `textContent`, NUNCA `d.detail` directamente (mismo fix que 33-10, evita
    "[object Object]"). Sin duplicar lógica de validación de rango `[0,1]`
    client-side más allá de lo que ya hace el trazado por canvas (las fracciones siempre
    vienen de `canvasClickToFrac`, que ya está acotada por construcción al área del
    canvas — no puede producir valores fuera de `[0,1]` salvo que el operador arrastre
    fuera del área visible, caso que el servidor rechaza con 422 de todos modos).
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/test_frontend_modules.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "fetch('/api/v2/lines" frontend/js/components/lineEditor.js` confirma el endpoint.
    - `grep -n "confirm(" frontend/js/components/lineEditor.js` confirma la confirmación nativa de borrado.
    - `grep -n "innerHTML" frontend/js/components/lineEditor.js` NO produce coincidencias (anti-XSS).
    - `wc -l frontend/js/components/lineEditor.js` reporta ≤300 (suma con la Task 1).
    - `pytest tests/test_frontend_modules.py -q` termina en verde.
  </acceptance_criteria>
  <done>CRUD completo de línea contra /api/v2/lines, mismo estilo de manejo de error y confirmación que el editor de zonas.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Servidor → DOM (nombre de línea, mensajes 422) | Igual que el Plan 33-10: debe entrar siempre por `textContent` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-14 | Tampering (XSS reflejado) | Nombre de línea con caracteres HTML, mostrado en `#line-list` | mitigate | `createElement`+`textContent` en todo el módulo, mismo patrón que 33-10 |
</threat_model>

<verification>
```bash
.venv/Scripts/python.exe -m pytest tests/test_frontend_modules.py -q
```
Sin framework de test JS — el comportamiento real (trazar, arrastrar, guardar contra
`/api/v2/lines` una vez el Plan 33-07 exista) se verifica en el checkpoint manual del
Plan 33-14.
</verification>

<success_criteria>
- Editor visual de N líneas con indicador de dirección.
- CRUD contra /api/v2/lines exclusivamente.
- Comparte canvas math y modo de edición con el editor de zonas, sin duplicar lógica.
</success_criteria>

<output>
Al terminar, crear `.planning/phases/33-editores-visuales-de-zonas-l-neas-y-reglas/33-11-SUMMARY.md`
</output>
