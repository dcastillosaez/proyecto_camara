---
phase: 33-editores-visuales-de-zonas-l-neas-y-reglas
plan: 07
type: execute
wave: 3
depends_on: ["33-01", "33-05"]
files_modified:
  - backend/api/v2/lines.py
  - tests/test_lines_api.py
autonomous: true
requirements: [OPS-22]

must_haves:
  truths:
    - "Se puede crear/actualizar/borrar una línea de conteo por HTTP con coordenadas normalizadas, y el servidor rechaza líneas degeneradas (longitud ~0) o fuera de [0,1]"
    - "Al guardar o borrar una línea, el cambio se empuja de inmediato a los pipelines de cámara activos (hot-reload, sin reiniciar)"
  artifacts:
    - path: "backend/api/v2/lines.py"
      provides: "APIRouter prefix=/api/v2/lines — GET, POST (upsert), DELETE /{line_id}"
    - path: "tests/test_lines_api.py"
      provides: "cobertura de validación de rango/longitud + hot-reload push"
  key_links:
    - from: "backend/api/v2/lines.py"
      to: "backend/storage/repositories.py:LineRepo"
      via: "_line_repo() factory"
      pattern: "LineRepo\\(get_session_factory\\(\\)\\)"
    - from: "backend/api/v2/lines.py"
      to: "backend/pipeline/manager.py:CameraPipeline.set_lines"
      via: "camera_manager.all() tras persistir"
      pattern: "\\.set_lines\\("
---

<objective>
Nuevo router `/api/v2/lines` (GET/POST-upsert/DELETE) sobre `LineRepo` (Plan 33-01):
valida rango `[0,1]` por coordenada y longitud mínima de línea (rechaza líneas
degeneradas, punto-a-punto), persiste, y empuja el cambio a todos los pipelines de
cámara vivos vía `CameraPipeline.set_lines` (Plan 33-05) — mismo criterio 6 que zonas
(<1s, sin reiniciar).

Purpose: es el único endpoint HTTP que el editor visual de líneas (Plan 33-11) necesita
para CRUD real. Depende explícitamente de que `CameraPipeline.set_lines` ya exista (Plan
33-05, Wave 2) — es el último eslabón backend antes de la integración final (Plan 33-08).
Output: `backend/api/v2/lines.py` con `router` y `configure(camera_manager)`, tests
unitarios contra una app FastAPI local.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-editores-visuales-de-zonas-l-neas-y-reglas/33-CONTEXT.md
@.planning/phases/33-editores-visuales-de-zonas-l-neas-y-reglas/33-RESEARCH.md
@.planning/phases/33-editores-visuales-de-zonas-l-neas-y-reglas/33-PATTERNS.md

<interfaces>
`backend/api/v2/zones.py` (Plan 33-03, ya cerrado — MISMO fichero, molde a clonar casi
literal, cambiando `ZoneRepo`→`LineRepo`, `polygon`→`start_x_frac/start_y_frac/
end_x_frac/end_y_frac`, sin `kind`/`schedule`): releer ese fichero completo antes de
escribir este router — es el analog más cercano posible, escrito en este mismo repo, en
este mismo estilo, dos plans antes.

`LineRepo` — `backend/storage/repositories.py` (Plan 33-01, ya cerrado):
```python
async def list(self, camera_id: str | None = None) -> list[dict[str, Any]]: ...
async def upsert(self, line_id, camera_id, name, start_x_frac, start_y_frac, end_x_frac, end_y_frac, enabled=True) -> None: ...
async def delete(self, line_id: str) -> bool: ...
async def _to_dict(line) -> dict  # {"id","camera_id","name","start_x_frac","start_y_frac","end_x_frac","end_y_frac","enabled"}
```

`CameraPipeline.set_lines` — `backend/pipeline/manager.py` (Plan 33-05, ya cerrado):
```python
def set_lines(self, lines: list[dict]) -> None:
    if self.detection:
        self.detection.set_lines(lines)
```
`DetectionWorker.set_lines(lines)` (Plan 33-05) espera exactamente la forma de
`LineRepo._to_dict()` (claves `start_x_frac`/`start_y_frac`/`end_x_frac`/`end_y_frac`/
`id`/`name`/`enabled`) — no transformar la forma en el router, pasar los dicts tal cual
vienen de `line_repo.list(camera_id=...)`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Router /api/v2/lines — CRUD, validación, hot-reload</name>
  <files>backend/api/v2/lines.py, tests/test_lines_api.py</files>
  <read_first>
    backend/api/v2/zones.py completo tras el Plan 33-03 (molde exacto a clonar)
    backend/storage/repositories.py (LineRepo tras el Plan 33-01)
    backend/pipeline/manager.py (CameraPipeline.set_lines tras el Plan 33-05)
    tests/test_zones_api.py completo tras el Plan 33-03 (patrón de test exacto a clonar)
  </read_first>
  <behavior>
    - `GET /api/v2/lines` sin query → `{"lines": [...]}` (todas). Con `camera_id` → filtra.
    - `POST /api/v2/lines` con `{"id","name","start_x_frac":0.0,"start_y_frac":0.5,"end_x_frac":1.0,"end_y_frac":0.5}`
      válido → 200, persiste, hot-reload push a cada pipeline cuyo `camera_id` coincide, responde `{"lines": [...]}`.
    - `POST` con una coordenada fuera de `[0,1]` (ej. `end_x_frac=1.5`) → 422.
    - `POST` con línea degenerada (`start` y `end` a distancia euclídea < 0.001 en fracción) → 422, detalle menciona "línea degenerada" o equivalente.
    - `DELETE /api/v2/lines/{id}` inexistente → 404. Existente → 200, hot-reload push, `{"lines": [...]}` restante.
    - Sin `camera_manager` configurado → CRUD sigue funcionando, sin excepción por ausencia de push.
  </behavior>
  <action>
    Crear `backend/api/v2/lines.py` clonando la forma de `backend/api/v2/zones.py`
    (docstring de cabecera citando D-01/OPS-22, `router = APIRouter(prefix="/api/v2/lines",
    tags=["lines"])`, `_camera_manager`/`configure(camera_manager)`, `_line_repo()`).
    Modelo Pydantic local `LineIn(id: str, camera_id: str = "cam1", name: str,
    start_x_frac: float, start_y_frac: float, end_x_frac: float, end_y_frac: float,
    enabled: bool = True)` con validadores: cada frac `0.0<=v<=1.0`, `len(id)<=50`,
    `len(name)<=100`, y un `@model_validator(mode="after")` que calcula
    `((end_x_frac-start_x_frac)**2 + (end_y_frac-start_y_frac)**2)**0.5` y rechaza si
    `< 0.001` ("línea degenerada: inicio y fin son el mismo punto").
    `_push_hot_reload(line_repo)` idéntico en forma a `_push_hot_reload` de
    `zones.py`, sustituyendo `pipeline.set_zones` por `pipeline.set_lines`.
    Tres rutas: `GET ""`, `POST ""` (upsert), `DELETE "/{line_id}"` — mismo molde exacto
    que `zones.py`.
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/test_lines_api.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "prefix=\"/api/v2/lines\"" backend/api/v2/lines.py` confirma el router.
    - `grep -n "degenerad" backend/api/v2/lines.py` confirma la validación de longitud mínima.
    - `grep -n "def configure" backend/api/v2/lines.py` confirma el punto de wiring para el Plan 33-08.
    - `pytest tests/test_lines_api.py -q` termina en verde cubriendo los 6 casos del `<behavior>`.
  </acceptance_criteria>
  <done>Router /api/v2/lines completo, con validación de rango/longitud, hot-reload multi-cámara, y tests unitarios en verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Cliente (navegador) → POST/DELETE /api/v2/lines | Entrada no confiable: coordenadas, id/name — mismo modelo de auth todo-o-nada que el resto de /api/v2 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-17 | Tampering | Coordenadas fuera de `[0,1]` o línea degenerada (longitud 0) | mitigate | Validación Pydantic con `@model_validator`, 422 antes de persistir — mismo patrón exacto que T-33-03 en el Plan 33-03 |
| T-33-18 | Denial of Service | `POST`/`DELETE` repetidos forzando rebuilds en cada pipeline | mitigate | `@limiter.limit(V2_RATE_LIMIT)` heredado |
</threat_model>

<verification>
```bash
.venv/Scripts/python.exe -m pytest tests/test_lines_api.py -q
```
Router probado con app FastAPI local. El wiring real a `camera_manager` llega en el Plan
33-08. No se relanza la suite completa en este plan.
</verification>

<success_criteria>
- CRUD completo de líneas vía /api/v2/lines, con validación estricta.
- Hot-reload multi-cámara tras cada mutación, simétrico a zonas.
</success_criteria>

<output>
Al terminar, crear `.planning/phases/33-editores-visuales-de-zonas-l-neas-y-reglas/33-07-SUMMARY.md`
</output>
