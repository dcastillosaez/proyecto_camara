---
phase: 33-editores-visuales-de-zonas-l-neas-y-reglas
plan: 06
type: execute
wave: 2
depends_on: ["33-01"]
files_modified:
  - backend/api/v2/rules.py
  - tests/test_rules_api.py
autonomous: true
requirements: [OPS-24, RULE-05]

must_haves:
  truths:
    - "Una regla compuesta por formulario (event/zone/camera/time_range/days/min_confidence/duration_gte/person/payload + acciones) se valida en servidor antes de guardar, con los mismos modelos que ya usa el motor de reglas en producción"
    - "Una regla mal formada nunca llega a persistirse ni a recargar el motor de reglas — el servidor la rechaza con 422 antes de tocar la base de datos"
    - "Se puede saber cuántos de los últimos 500 eventos habrían disparado una regla, sin alterar su debounce real"
  artifacts:
    - path: "backend/api/v2/rules.py"
      provides: "APIRouter prefix=/api/v2/rules — GET, POST (upsert), DELETE /{rule_id}, POST /{rule_id}/test"
    - path: "tests/test_rules_api.py"
      provides: "cobertura de validación Rule/When/Action + reload del motor + /test contra eventos sembrados"
  key_links:
    - from: "backend/api/v2/rules.py"
      to: "backend/events/rules.py:Rule/When/Action"
      via: "Rule.model_validate(body) antes de persistir"
      pattern: "Rule\\.model_validate"
    - from: "backend/api/v2/rules.py"
      to: "backend/storage/repositories.py:RuleRepo"
      via: "_rule_repo() factory"
      pattern: "RuleRepo\\(get_session_factory\\(\\)\\)"
    - from: "backend/api/v2/rules.py:/{rule_id}/test"
      to: "backend/events/rules.py:RuleEngine.would_match"
      via: "bucle sobre EventRepo.query(limit=500)"
      pattern: "would_match"
---

<objective>
Nuevo router `/api/v2/rules`: CRUD completo sobre `RuleRepo` (D-02 análogo para reglas —
fuente de verdad pasa de `config/rules.yaml` a la tabla `rules`), validando cada regla
contra los modelos Pydantic `Rule`/`When`/`Action` que el motor de reglas YA usa en
producción (`backend/events/rules.py`) — nunca una validación paralela. Incluye
`POST /{rule_id}/test` (RULE-05): evalúa la regla contra los últimos 500 eventos vía
`EventRepo.query(limit=500)` + `RuleEngine.would_match()` (Plan 33-01), puro, sin tocar
debounce.

Purpose: es el endpoint que el editor de reglas por formularios (Plan 33-12) necesita
para OPS-24 y RULE-05; sustituye al `GET /api/v2/rules` de solo lectura que hoy vive
suelto en `main.py:998-1007` (se retira en el Plan 33-08 de integración final).
Output: `backend/api/v2/rules.py` con `router` y `configure(rule_engine)`, tests
unitarios contra una app FastAPI local con `RuleRepo`/`EventRepo`/`RuleEngine` mockeados.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-editores-visuales-de-zonas-l-neas-y-reglas/33-CONTEXT.md
@.planning/phases/33-editores-visuales-de-zonas-l-neas-y-reglas/33-RESEARCH.md
@.planning/phases/33-editores-visuales-de-zonas-l-neas-y-reglas/33-PATTERNS.md

<interfaces>
Modelos ya existentes, sin cambios — `backend/events/rules.py` (Plan 33-01 ya cerró
`would_match`, no se vuelve a tocar aquí salvo lectura):
```python
class When(BaseModel):
    event: EventType
    zone: str | None = None
    camera: str = "*"
    time_range: str | None = None
    days: list[int] | None = None
    min_confidence: float | None = None
    duration_gte: float | None = None
    person: str | None = None
    payload: dict[str, Any] | None = None

class Action(BaseModel):
    type: Literal["record","snapshot","notify","telegram","webhook","log","upload_drive","set_flag"]
    pre_secs: float | None = None
    post_secs: float | None = None
    template: str | None = None
    url_ref: str | None = None

class Rule(BaseModel):
    name: str
    enabled: bool = True
    when: When
    debounce_secs: float = 0.0
    actions: list[Action]

class RuleEngine:
    def would_match(self, when: When, event: Event) -> bool: ...   # Plan 33-01
    def reload(self, rules: list[Rule], invalid=None) -> None: ...  # ya existente
```

`RuleRepo` — `backend/storage/repositories.py:754-793` + `get()` añadido por el Plan
33-01 (ya cerrado):
```python
async def list(self) -> list[dict[str, Any]]: ...   # [{"id","name","enabled","definition","updated_at"}, ...]
async def get(self, rule_id: str) -> dict[str, Any] | None: ...
async def upsert(self, rule_id: str, name: str, enabled: bool, definition: dict) -> None: ...
async def delete(self, rule_id: str) -> bool: ...
```
`definition` es el JSON crudo persistido — este plan define su forma exacta:
`{"when": {...}, "debounce_secs": 0.0, "actions": [...]}` (todo lo de `Rule` MENOS
`name`/`enabled`, que ya son columnas propias de la tabla). Necesita una función
`rule_from_db_dict(row: dict) -> Rule` que reconstruya el objeto Pydantic completo:
`Rule.model_validate({"name": row["name"], "enabled": row["enabled"], **row["definition"]})`
— Pitfall 3 de 33-RESEARCH.md: NO asumir que `definition` ya es un `Rule` válido sin
pasar por esta validación.

Endpoint viejo a retirar (lo hace el Plan 33-08, este plan solo debe REPLICAR su forma de
respuesta para no romper el consumidor de frontend que ya existe) —
`backend/main.py:998-1007`:
```python
@app.get("/api/v2/rules")
async def api_v2_rules(request: Request):
    return {
        "rules": [json.loads(r.model_dump_json()) for r in rule_engine.rules],
        "invalid": [{"name": name, "reason": reason} for name, reason in rule_engine.invalid_rules],
    }
```
`frontend/js/views/settings-section.js:71-80` consume `data.rules` (array con al menos
`.name`/`.enabled`) para la subsección de solo lectura "reglas_cargadas" — el nuevo GET
puede omitir `invalid` (con reglas validadas en escritura, la tabla `rules` no puede
contener una regla inválida, a diferencia del YAML editado a mano) pero DEBE conservar
`rules` con `name`/`enabled` legibles.

`EventRepo.query(limit=500)` — `backend/storage/repositories.py:152-192` (código real, ya
en contexto de sesiones previas de este plan): devuelve `tuple[list[EventDTO], str |
None]` donde `EventDTO` ES `Event` de `backend.events.types` (mismo tipo que
`When`/`_matches` esperan — sin conversión necesaria).

Patrón de test-app local + mocking — `tests/test_config_api.py:31-54` (clonar, ver Plan
33-03 para la misma cita completa). `scripts/seed_events.py` (108 líneas, ya existe,
usado en la Fase 30/31 para sembrar eventos de prueba) — usar como referencia de forma de
`Event` si el test necesita sembrar eventos reales contra una BD SQLite en memoria en vez
de mockear `EventRepo.query` directamente (decisión de la tarea: mockear es más simple y
suficiente para este router, sembrar de verdad pertenece más al checkpoint manual del
Plan 33-14 — no dupliques ambos enfoques).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Router /api/v2/rules — CRUD validado + POST /{id}/test</name>
  <files>backend/api/v2/rules.py, tests/test_rules_api.py</files>
  <read_first>
    backend/api/v2/events.py completo (forma de router v2, ya usado como plantilla por 33-03)
    backend/events/rules.py completo (Rule/When/Action/RuleEngine, ya extractado arriba)
    backend/storage/repositories.py (RuleRepo tras el Plan 33-01, y EventRepo.query completo, líneas 152-192)
    backend/main.py:998-1007 (endpoint a replicar en forma de respuesta del GET)
    frontend/js/views/settings-section.js:60-80 (consumidor real de `data.rules`, para no romper su contrato)
    scripts/seed_events.py (si la tarea decide sembrar eventos reales en vez de mockear — leer antes de decidir)
    tests/test_config_api.py:1-70 (patrón de test-app local + AsyncMock, ya usado por 33-03)
  </read_first>
  <behavior>
    - `GET /api/v2/rules` → `{"rules": [{"id","name","enabled",...}]}` desde `RuleRepo.list()`.
    - `POST /api/v2/rules` con `{"id","name","enabled","when":{"event":"LINE_CROSSED"},"actions":[{"type":"log"}]}`
      válido → 200, persiste vía `RuleRepo.upsert(id, name, enabled, {"when":...,"debounce_secs":0.0,"actions":[...]})`,
      recarga `rule_engine.reload(...)` con todas las reglas actuales convertidas por
      `rule_from_db_dict`, responde `{"rules": [...]}`.
    - `POST` con `when.event` no perteneciente a `EventType` → 422 con detalle que incluye
      la ruta del campo (`when.event`), igual que Pydantic lo reporta nativamente.
    - `POST` con `actions=[]` (lista vacía) → 422 (`Action` list no puede estar vacía —
      confirmar si `Rule.actions: list[Action]` permite lista vacía hoy; si el modelo
      actual lo permite, añadir esa restricción aquí vía validación explícita del router,
      documentando que es una regla de negocio de la Fase 33, no del modelo compartido).
    - `POST` con `actions=[{"type":"invalido"}]` → 422 (`Literal` de `Action.type` ya lo rechaza).
    - `DELETE /api/v2/rules/{id}` inexistente → 404. Existente → 200, recarga el motor,
      `{"rules": [...]}` restante.
    - `POST /api/v2/rules/{id}/test` con id inexistente → 404.
    - `POST /api/v2/rules/{id}/test` con id existente → `{"would_fire": N, "total_checked": M}`
      donde `N <= M <= 500`, calculado con `rule_engine.would_match(rule.when, event)` para
      cada evento de `EventRepo.query(limit=500)` — verificar con un mock de 3 eventos donde
      2 casan y 1 no, `would_fire == 2`.
    - Llamar `/test` dos veces seguidas sobre la misma regla con eventos que activarían
      debounce en producción da el MISMO resultado ambas veces (would_match no muta estado).
  </behavior>
  <action>
    Crear `backend/api/v2/rules.py` (docstring de cabecera igual estilo que `config.py`,
    citando OPS-24/RULE-05/D-02). Definir:
    - `router = APIRouter(prefix="/api/v2/rules", tags=["rules"])`.
    - `_rule_engine: Any = None` + `def configure(rule_engine: Any) -> None`.
    - `_rule_repo() -> RuleRepo` y `_event_repo() -> EventRepo` (factories, mismo molde).
    - `def rule_from_db_dict(row: dict[str, Any]) -> Rule:` — `Rule.model_validate({"name":
      row["name"], "enabled": row["enabled"], **row["definition"]})`.
    - `async def _reload_engine(rule_repo: RuleRepo) -> None:` — si `_rule_engine is not
      None`: `rows = await rule_repo.list()`; convierte cada uno con `rule_from_db_dict`
      (capturando `ValidationError` por fila para no tirar abajo el reload entero por una
      fila corrupta — mismo espíritu defensivo que `load_rules()` original, aunque en
      teoría no debería ocurrir porque este router valida en escritura); llama
      `_rule_engine.reload(valid_rules, invalid=invalid_pairs)`.
    - Modelo Pydantic local `RuleIn(id: str, name: str, enabled: bool = True, when: When,
      debounce_secs: float = 0.0, actions: list[Action])` con
      `@field_validator("actions") ... if not v: raise ValueError("at least one action required")`.
    - `@router.get("")` → `{"rules": await rule_repo.list()}`.
    - `@router.post("")` `@limiter.limit(V2_RATE_LIMIT)` `upsert_rule(request, body: RuleIn)`:
      construye `definition = {"when": body.when.model_dump(), "debounce_secs":
      body.debounce_secs, "actions": [a.model_dump() for a in body.actions]}`, llama
      `rule_repo.upsert(body.id, body.name, body.enabled, definition)`, `_reload_engine`,
      devuelve `{"rules": await rule_repo.list()}`.
    - `@router.delete("/{rule_id}")` `@limiter.limit(V2_RATE_LIMIT)`: 404 si no existía,
      si no `_reload_engine` + `{"rules": [...]}`.
    - `@router.post("/{rule_id}/test")` `@limiter.limit(V2_RATE_LIMIT)`
      `test_rule(request, rule_id: str)`: `row = await rule_repo.get(rule_id)`; 404 si
      `None`; `rule = rule_from_db_dict(row)`; `items, _ = await event_repo.query(limit=500)`;
      `would_fire = sum(1 for e in items if _rule_engine.would_match(rule.when, e))` (503
      si `_rule_engine is None`); devuelve `{"would_fire": would_fire, "total_checked":
      len(items)}`.
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/test_rules_api.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "prefix=\"/api/v2/rules\"" backend/api/v2/rules.py` confirma el router.
    - `grep -n "def rule_from_db_dict" backend/api/v2/rules.py` confirma la conversión pública (Pitfall 3 cerrado, sin importar `_matches` con guion bajo — grep negativo: `grep -n "_matches" backend/api/v2/rules.py` no debe aparecer, solo `would_match`).
    - `grep -n "would_match" backend/api/v2/rules.py` confirma el uso en `/test`.
    - `pytest tests/test_rules_api.py -q` termina en verde cubriendo los 9 casos del `<behavior>`.
  </acceptance_criteria>
  <done>Router /api/v2/rules completo: CRUD validado contra los modelos Pydantic reales del motor de reglas, recarga del motor en cada mutación, y POST /{id}/test puro sobre los últimos 500 eventos.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Cliente (navegador) → POST/DELETE /api/v2/rules | Entrada no confiable: when/actions/payload — validados por los mismos modelos Pydantic que el motor de reglas ya usa en producción, sin `eval()` (mismo principio que `load_rules()` documenta en su docstring) |
| Cliente → POST /{id}/test | Coste no trivial (evalúa hasta 500 eventos por petición) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-10 | Tampering | `payload` arbitrario en `When.payload` intentando forzar claves no esperadas | mitigate | `When`/`Action` ya son modelos Pydantic cerrados (sin `extra="allow"`); el router no añade ningún bypass |
| T-33-11 | Denial of Service | `POST /{id}/test` invocado en bucle | mitigate | `@limiter.limit(V2_RATE_LIMIT)` (60/min) heredado, mismo patrón que el resto de `/api/v2` |
| T-33-12 | Tampering | `actions=[]` dejando una regla sin efecto observable pero "activa" | mitigate | Validación explícita `at least one action required` en `RuleIn`, no delegada solo al modelo compartido |
</threat_model>

<verification>
```bash
.venv/Scripts/python.exe -m pytest tests/test_rules_api.py -q
```
Router probado con app FastAPI local — el wiring real a `rule_engine` llega en el Plan
33-08. No se relanza la suite completa en este plan.
</verification>

<success_criteria>
- CRUD de reglas vía formulario, validado contra los modelos reales del motor.
- POST /{id}/test puro, sin efectos sobre debounce en producción.
- Contrato de GET compatible con el consumidor existente en settings-section.js.
</success_criteria>

<output>
Al terminar, crear `.planning/phases/33-editores-visuales-de-zonas-l-neas-y-reglas/33-06-SUMMARY.md`
</output>
