---
phase: 33-editores-visuales-de-zonas-l-neas-y-reglas
plan: 14
type: execute
wave: 6
depends_on: ["33-13"]
files_modified: []
autonomous: false
requirements: [OPS-21, OPS-22, OPS-23, OPS-24, RULE-05]

must_haves:
  truths:
    - "Los 7 criterios de éxito del ROADMAP para la Fase 33 están verificados con evidencia real, no solo con tests unitarios mockeados"
    - "El operador puede dibujar una zona, una línea y probar una regla contra eventos reales sin errores de consola"
  artifacts: []
  key_links: []
---

<objective>
Puerta de fase: trazabilidad de los 7 criterios de éxito de `ROADMAP.md` para la Fase 33
+ checkpoint visual con servidor real, mismo patrón que 27-10/28-09/30-12/31-11/32-08.
Sin código nuevo — este plan verifica lo que los Planes 33-01..33-13 ya construyeron.

Purpose: cerrar formalmente OPS-21..OPS-24/RULE-05, que hasta este punto "avanzan" pero
no se marcan como completos (mismo patrón que toda fase de frontend anterior — la
interfaz visible es la única prueba real de que el editor funciona con datos reales,
no con mocks).
Output: `33-14-SUMMARY.md` con la traza de los 7 criterios y el veredicto del checkpoint
manual (aprobado / lista de issues).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-editores-visuales-de-zonas-l-neas-y-reglas/33-CONTEXT.md
@.planning/phases/33-editores-visuales-de-zonas-l-neas-y-reglas/33-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Trazabilidad de los 7 criterios de éxito + suite completa</name>
  <files></files>
  <read_first>
    .planning/ROADMAP.md líneas 637-650 (los 7 criterios de éxito exactos de la Fase 33)
    tests/test_zones_api.py, tests/test_lines_api.py, tests/test_rules_api.py,
    tests/test_pipeline_lines.py, tests/test_migrations.py, tests/test_tracker.py
    (recorrer los nombres de test de cada fichero para mapear cuál cubre cada criterio)
  </read_first>
  <acceptance_criteria>
    - Cada uno de los 7 criterios de éxito del ROADMAP tiene al menos un test automatizado citado por nombre de fichero::función en el SUMMARY, o está marcado explícitamente como "solo verificable manualmente" con la razón.
    - `pytest tests/ -q` reporta 0 failed.
  </acceptance_criteria>
  <action>
    Construir la tabla de trazabilidad criterio→test para los 7 criterios de
    `ROADMAP.md` (dibujar/mover/editar/borrar polígonos con coordenadas normalizadas;
    líneas con indicador de dirección; tipo+horario de zona; formulario de reglas
    validado en servidor; `/rules/{id}/test` contra 500 eventos; hot-reload <1s;
    720p→1080p correcto), citando el test real que lo cubre. Ejecutar la suite completa
    y confirmar 0 failed antes de pasar a la Task 2 (checkpoint manual) — un checkpoint
    manual sobre una suite en rojo no es válido.
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/ -q</automated>
  </verify>
  <done>Tabla de trazabilidad completa, suite completa en verde.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Checkpoint visual de los editores de zonas, líneas y reglas</name>
  <action>Seguir los pasos de how-to-verify con el servidor real y un navegador, y reportar el resultado en resume-signal.</action>
  <what-built>
    Editor visual de zonas y líneas de conteo sobre el vídeo en vivo (vista Cámara),
    editor de reglas por formularios con "Probar regla" contra el histórico reciente, y
    hot-reload de zonas/líneas sin reiniciar el pipeline.
  </what-built>
  <how-to-verify>
    Arrancar el servidor real: `.venv/Scripts/python.exe -m uvicorn backend.main:app --host 0.0.0.0 --port 8000`
    y abrir `http://localhost:8000/#camara` en el navegador.

    1. Entrar en modo edición de zonas, dibujar un polígono de al menos 3 puntos sobre
       el vídeo, fijar tipo "Restringida" y un horario, guardar. Confirmar que aparece
       en la lista y que no hay errores en la consola del navegador.
    2. Arrastrar un vértice de esa zona y confirmar que el polígono se actualiza
       visualmente y persiste al recargar la página.
    3. Cambiar a modo línea, trazar una línea de conteo con dos clicks, confirmar que se
       ve el indicador de dirección, guardar. Repetir para una segunda línea (verificar
       que ambas coexisten — D-01, N líneas).
    4. Abrir el editor de reglas, componer una regla simple (ej. `event=LINE_CROSSED`),
       guardar, pulsar "Probar regla" y confirmar que el resultado muestra un número de
       "habría disparado" razonable (0 si no hay histórico suficiente — en ese caso,
       repetir tras poblar eventos con `scripts/seed_events.py`, per 33-CONTEXT.md
       Claude's Discretion).
    5. Verificar el criterio 6 (hot-reload <1s): tras guardar un cambio de zona/línea,
       confirmar que el pipeline sigue sirviendo vídeo sin ningún parón visible ni
       reconexión de `#camera-feed`.
    6. Verificar que la tarjeta "Zonas de interés" de Operaciones (Fase 13) ya no existe,
       y que las subsecciones "Zonas definidas"/"Líneas definidas"/"Reglas cargadas" de
       Ajustes muestran los datos reales creados en los pasos 1-4.
  </how-to-verify>
  <resume-signal>Escribe "aprobado" o describe los problemas encontrados (con el paso exacto de la lista de arriba)</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Plan de verificación, sin código de producción nuevo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-21 | N/A | — | accept | Sin superficie de amenaza nueva |
</threat_model>

<verification>
```bash
.venv/Scripts/python.exe -m pytest tests/ -q
```
Más el checkpoint manual de la Task 2, con servidor real.
</verification>

<success_criteria>
- Los 7 criterios de éxito de la Fase 33 trazados con evidencia (test o verificación manual explícita).
- Checkpoint manual aprobado por el usuario.
- OPS-21..OPS-24/RULE-05 cerrados formalmente.
</success_criteria>

<output>
Al terminar, crear `.planning/phases/33-editores-visuales-de-zonas-l-neas-y-reglas/33-14-SUMMARY.md`
</output>
