---
phase: 33-editores-visuales-de-zonas-l-neas-y-reglas
plan: 08
type: execute
wave: 4
depends_on: ["33-02", "33-03", "33-05", "33-06", "33-07"]
files_modified:
  - backend/main.py
  - backend/config.py
  - backend/api/v2/config_schema.py
  - tests/test_config_api.py
  - tests/test_config_schema.py
  - tests/test_zones_api.py
  - tests/test_lines_api.py
  - tests/test_rules_api.py
autonomous: true
requirements: [OPS-21, OPS-22, OPS-24]

must_haves:
  truths:
    - "Al arrancar, el pipeline carga sus zonas y líneas desde ZoneRepo/LineRepo (v2), no desde el modelo v1 legacy ni desde Settings.line_*_frac"
    - "Al arrancar, el motor de reglas carga sus reglas desde la tabla rules (v2), no desde config/rules.yaml"
    - "/api/zones (v1) y el GET /api/v2/rules suelto ya no existen; todo pasa por los routers nuevos"
  artifacts:
    - path: "backend/main.py"
      provides: "wiring de zones_v2/lines_v2/rules_v2, arranque desde ZoneRepo/LineRepo/RuleRepo, retirada de endpoints v1"
  key_links:
    - from: "backend/main.py:lifespan"
      to: "backend/api/v2/zones.py:configure"
      via: "zones_v2_module.configure(camera_manager)"
      pattern: "zones_v2_module\\.configure"
    - from: "backend/main.py:lifespan"
      to: "backend/api/v2/rules.py:configure"
      via: "rules_v2_module.configure(rule_engine)"
      pattern: "rules_v2_module\\.configure"
    - from: "backend/api/v2/config_schema.py"
      to: "/api/v2/zones"
      via: "external_source actualizado (era /api/zones)"
      pattern: "external_source=\"/api/v2/zones\""
---

<objective>
Integración final del backend: conectar los tres routers nuevos (zonas/líneas/reglas,
Planes 33-03/33-06/33-07) al `lifespan` de `main.py`, migrar el arranque del pipeline
para que zonas/líneas/reglas se carguen SIEMPRE desde sus repositorios v2 (D-02), y
retirar el código v1 que queda huérfano: `/api/zones` (GET/POST/DELETE/stats),
`GET /api/v2/rules` suelto, y los 4 campos `line_*_frac` de `Settings`/`config_schema.py`
(obsoletos tras el Plan 33-05 — las líneas ya no se leen de `.env`, se leen de la tabla
`lines`, con el seed de compatibilidad que el Plan 33-02 ya garantizó).

Purpose: es el único plan que toca `main.py`/`config_schema.py` en toda la fase —
concentrar aquí todas las ediciones de estos ficheros compartidos evita conflictos de
wave entre los 7 plans backend anteriores (mismo patrón que 32-07 cerró la Fase 32).
Output: pipeline arrancando 100% desde v2 para zonas/líneas/reglas, endpoints v1
retirados, `config_schema.py` sin campos muertos, suite completa en verde.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-editores-visuales-de-zonas-l-neas-y-reglas/33-CONTEXT.md
@.planning/phases/33-editores-visuales-de-zonas-l-neas-y-reglas/33-RESEARCH.md
@.planning/phases/33-editores-visuales-de-zonas-l-neas-y-reglas/33-PATTERNS.md

<interfaces>
Wiring existente de routers v2 — `backend/main.py:584-599,762-778` (código real, patrón a
replicar tres veces más):
```python
camera_manager = CameraManager()
from backend.api.v2 import context as context_v2_module
context_v2_module.configure(camera_manager)
from backend.api.v2 import config as config_v2_module
config_v2_module.configure(camera_manager, event_engine)
...
from backend.api.v2.config import router as config_v2_router
app.include_router(config_v2_router)
```

Construcción actual de `tracker`/carga de zonas — `backend/main.py:460-469,671` (código
real, a modificar):
```python
tracker = PersonTracker(
    start=sv.Point(int(settings.line_start_x_frac * settings.process_width), int(settings.line_start_y_frac * settings.process_height)),
    end=sv.Point(int(settings.line_end_x_frac * settings.process_width), int(settings.line_end_y_frac * settings.process_height)),
    frame_rate=settings.tracker_frame_rate,
)
...
pipeline.set_zones(await get_zones())   # get_zones() = backend.database (v1 legacy)
```
Tras el Plan 33-04, `PersonTracker(lines=None, frame_rate=...)` acepta lista de líneas
opcional en vez de `start`/`end` obligatorios — construir sin líneas iniciales
(`PersonTracker(frame_rate=settings.tracker_frame_rate)`) y cargar las reales DESPUÉS de
`pipeline.start()`, mismo punto donde hoy se cargan las zonas (línea 671), con el mismo
patrón: `pipeline.set_lines(await LineRepo(get_session_factory()).list(camera_id="cam1"))`.
`pipeline.set_zones(await get_zones())` pasa a `pipeline.set_zones(await
ZoneRepo(get_session_factory()).list(camera_id="cam1"))`.

Reglas: `backend/main.py:426-434` (código real, a modificar):
```python
rules_path = Path("config/rules.yaml")
if rules_path.exists():
    loaded_rules, rule_errors = load_rules(str(rules_path))
    ...
else:
    loaded_rules, rule_errors = [], []
rule_engine = RuleEngine(loaded_rules, registry=event_actions.ACTIONS, invalid=rule_errors)
```
Pasa a cargar desde `RuleRepo` (vía `rule_from_db_dict` del Plan 33-06 — importar esa
función desde `backend.api.v2.rules`, o duplicar la línea de conversión si importar desde
un router a `main.py` genera un ciclo; verificar antes de decidir). Si `RuleRepo.list()`
devuelve vacío (instalación nueva o recién migrada, tabla `rules` sin filas), mantener el
fallback a `config/rules.yaml` EXACTAMENTE como hoy (para no dejar el sistema sin
reglas la primera vez que arranca tras esta fase) — documentar en comentario que este
fallback es de arranque único, no de sincronización continua: una vez el operador cree su
primera regla vía `/api/v2/rules`, la tabla deja de estar vacía y el YAML deja de leerse
en los arranques siguientes.

Endpoints a retirar — `backend/main.py:998-1007` (`GET /api/v2/rules` suelto) y
`backend/main.py:1097-1150` (`/api/zones` GET/POST/DELETE + `/api/zones/stats` — leer el
bloque completo, incluida `/api/zones/stats`, ANTES de decidir qué hacer con ella: no la
sustituye ningún router nuevo de esta fase, así que debe seguir sirviendo pero apuntando
a la fuente v2, o retirarse si nada la consume — verificar consumidores con grep antes de
tocarla, `frontend/js/**` y `tests/**`).

Consumidores de `/api/zones` a actualizar tras retirar el endpoint v1 (ya identificados
en 33-RESEARCH.md/33-PATTERNS.md): `frontend/js/views/timeline.js:148` (fetch para
resolver nombre de zona en el filtro) y `backend/api/v2/config_schema.py:613`
(`external_source="/api/zones"` del grupo "zonas_definidas" en Ajustes) — ambos deben
apuntar a `/api/v2/zones`, cuya respuesta (`{"zones": [...]}` con `id`/`name`) es
compatible con lo que ambos ya consumen (`data.zones`, `.id`, `.name`).

`config_schema.py` — grupo "Línea de conteo" a retirar (4 `FieldDef` con
`applies="restart_camera"`, obsoletas tras el Plan 33-05) — `backend/api/v2/config_schema.py:568-616`
(código real, sección "zonas" completa):
```python
Section(key="zonas", label="Zonas", groups=(
    Group(key="linea", label="Línea de conteo", fields=(
        FieldDef(key="line_start_x_frac", ...), FieldDef(key="line_start_y_frac", ...),
        FieldDef(key="line_end_x_frac", ...), FieldDef(key="line_end_y_frac", ...),
    )),
    Group(key="zonas_definidas", label="Zonas definidas", external_source="/api/zones"),
)),
```
Sustituir el grupo `"linea"` (4 campos) por un grupo `external_source` análogo a
`zonas_definidas`: `Group(key="lineas_definidas", label="Líneas definidas",
external_source="/api/v2/lines")` — mismo patrón de solo-lectura + enlace al editor real
(Plan 33-13 conecta el enlace visual). `zonas_definidas` cambia su `external_source` a
`/api/v2/zones`.

`backend/config.py:82-88` — eliminar las 4 líneas `line_start_x_frac`/etc. de `Settings`
(ya no las consume nada tras este plan).

Test de paridad campo↔Settings — `tests/test_config_schema.py:25` (`keys = {f.key for f
in all_fields()}` comparado contra `set(Settings.model_fields)`) — este test FALLARÁ si
se retiran los 4 `FieldDef` sin retirar también las 4 columnas de `Settings`, o viceversa;
ambos cambios deben ir en el mismo commit de esta tarea.

`tests/test_config_api.py:130` — `assert zonas_definidas["external_source"] ==
"/api/zones"` debe actualizarse a `"/api/v2/zones"`.

Patrón de test de wiring real contra `backend.main.app` — `tests/test_config_api.py:433-438`
(código real, único precedente en el repo — clonar tres veces, una por router nuevo):
```python
def TEST_main_imports_with_config_router_registered():
    import backend.main as main_module
    paths = {getattr(r, "path", None) for r in main_module.app.routes}
    assert "/api/v2/config" in paths
    assert "/api/v2/config/{section_key}/restore" in paths
```
No existe `tests/test_main_wiring.py` en el repo — este patrón vive DENTRO del fichero de
test de cada router (`test_config_api.py` lo hace así), así que la comprobación de
wiring de zonas/líneas/reglas contra `backend.main.app` va en
`tests/test_zones_api.py`/`tests/test_lines_api.py`/`tests/test_rules_api.py`
respectivamente (una función `TEST_main_imports_with_*_router_registered` por fichero),
no en un fichero nuevo.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wiring de routers v2 (zonas/líneas/reglas) + arranque desde repositorios v2</name>
  <files>backend/main.py, tests/test_zones_api.py, tests/test_lines_api.py, tests/test_rules_api.py</files>
  <read_first>
    backend/main.py:400-480 (lifespan, construcción de tracker/rule_engine, ya extractado)
    backend/main.py:580-680 (wiring de routers v2 existentes + carga de zonas al arrancar, ya extractado)
    backend/main.py:755-780 (bloque de include_router, ya extractado)
    backend/api/v2/zones.py, backend/api/v2/lines.py, backend/api/v2/rules.py completos
    (los tres routers ya cerrados en Waves 1-3 — confirmar la firma exacta de cada `configure()`)
    backend/storage/repositories.py (ZoneRepo/LineRepo/RuleRepo, confirmar firmas de `.list()`)
    tests/test_config_api.py:430-439 (patrón de test de wiring real contra backend.main.app, ya extractado)
  </read_first>
  <acceptance_criteria>
    - `grep -n "from backend.api.v2 import zones as zones_v2_module" backend/main.py` confirma el import.
    - `grep -n "from backend.api.v2 import lines as lines_v2_module" backend/main.py` confirma el import.
    - `grep -n "from backend.api.v2 import rules as rules_v2_module" backend/main.py` confirma el import.
    - `grep -n "zones_v2_module.configure\|lines_v2_module.configure\|rules_v2_module.configure" backend/main.py` confirma los tres wirings.
    - `grep -n "PersonTracker(frame_rate=settings.tracker_frame_rate)" backend/main.py` confirma que el tracker ya no construye con start/end.
    - `grep -n "ZoneRepo(get_session_factory()).list\|LineRepo(get_session_factory()).list" backend/main.py` confirma el arranque v2.
    - `grep -n "get_zones()" backend/main.py` NO produce coincidencias (v1 legacy retirado del arranque).
    - `pytest tests/test_zones_api.py tests/test_lines_api.py tests/test_rules_api.py -k "wiring or registered" -q` termina en verde.
  </acceptance_criteria>
  <action>
    En el `lifespan`, tras `camera_manager = CameraManager()` y junto a los `configure()`
    ya existentes, añadir:
    ```python
    from backend.api.v2 import zones as zones_v2_module
    zones_v2_module.configure(camera_manager)
    from backend.api.v2 import lines as lines_v2_module
    lines_v2_module.configure(camera_manager)
    from backend.api.v2 import rules as rules_v2_module
    rules_v2_module.configure(rule_engine)
    ```
    Sustituir la construcción de `tracker` (líneas 460-469) por
    `tracker = PersonTracker(frame_rate=settings.tracker_frame_rate)` (sin `lines=`, lista
    vacía por defecto — las líneas reales llegan tras `pipeline.start()`, igual que zonas).

    Sustituir la carga de reglas (líneas 426-434): construir `rule_repo =
    RuleRepo(get_session_factory())` (import ya existente de `ConfigRepo` en el mismo
    bloque — añadir `RuleRepo` a esa importación), `rule_rows = await rule_repo.list()`;
    si `rule_rows` no está vacío, convertir cada fila con la función de conversión del
    Plan 33-06 (importar `rule_from_db_dict` desde `backend.api.v2.rules` — si esto crea
    un ciclo de imports con `main.py`, mover `rule_from_db_dict` a `backend/events/rules.py`
    como función de módulo pública en su lugar, documentando el cambio de ubicación en el
    SUMMARY) capturando `ValidationError` por fila (log + acumular en `rule_errors`, mismo
    patrón defensivo que `load_rules()`); si `rule_rows` ESTÁ vacío, mantener el fallback
    exacto a `config/rules.yaml` que ya existe hoy (sin cambios en ese `else`).

    Sustituir `pipeline.set_zones(await get_zones())` (línea 671) por
    `pipeline.set_zones(await ZoneRepo(get_session_factory()).list(camera_id="cam1"))` y
    añadir justo después `pipeline.set_lines(await
    LineRepo(get_session_factory()).list(camera_id="cam1"))` (import `ZoneRepo`/`LineRepo`
    junto al resto de repos ya importados en el fichero).

    En el bloque de `include_router` (líneas 762-778), añadir los tres nuevos junto a
    `config_v2_router`:
    ```python
    from backend.api.v2.zones import router as zones_v2_router
    app.include_router(zones_v2_router)
    from backend.api.v2.lines import router as lines_v2_router
    app.include_router(lines_v2_router)
    from backend.api.v2.rules import router as rules_v2_router
    app.include_router(rules_v2_router)
    ```

    Eliminar `GET /api/v2/rules` suelto (líneas 998-1007, sustituido por el router nuevo)
    y los cuatro endpoints `/api/zones`/`/api/zones/stats` (líneas 1097-1150) —
    `/api/zones/stats` (estadísticas en vivo de ocupación por zona, `rtsp_stream.get_zone_stats()`)
    no tiene equivalente v2 en esta fase: recrearla como `GET /api/v2/zones/stats` en el
    router del Plan 33-03 excede el alcance de ESTE plan (tocaría `zones.py`, ya cerrado
    en Wave 1) — en su lugar, conservar el endpoint TAL CUAL bajo su ruta actual
    `/api/zones/stats` (no lo retira este plan, solo se retiran GET/POST/DELETE de
    `/api/zones`), documentando en el SUMMARY que queda como único resto v1 activo,
    consumidor real a verificar con grep antes de decidir si merece su propio plan de
    gap-closure.

    Añadir a `tests/test_zones_api.py`, `tests/test_lines_api.py` y `tests/test_rules_api.py`
    (uno cada uno, mismo patrón que `tests/test_config_api.py:433-438`) una función
    `TEST_main_imports_with_<recurso>_router_registered()` que importa `backend.main` y
    verifica que `/api/v2/zones`, `/api/v2/lines` y `/api/v2/rules` respectivamente
    aparecen en `{getattr(r, "path", None) for r in main_module.app.routes}`.
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/test_zones_api.py tests/test_lines_api.py tests/test_rules_api.py -q</automated>
  </verify>
  <done>main.py arranca el pipeline y el motor de reglas desde los repositorios v2, con los tres routers nuevos servidos (verificado contra backend.main.app real) y los endpoints v1 huérfanos retirados (salvo /api/zones/stats, documentado).</done>
</task>

<task type="auto">
  <name>Task 2: config_schema.py — retirar línea única, apuntar external_source a v2</name>
  <files>backend/api/v2/config_schema.py, backend/config.py, tests/test_config_schema.py, tests/test_config_api.py</files>
  <read_first>
    backend/api/v2/config_schema.py:568-616 (sección "zonas" completa, ya extractada)
    backend/config.py:80-90 (line_*_frac, ya extractado)
    tests/test_config_schema.py completo (test de paridad campo↔Settings, ya extractado en `<interfaces>`)
    tests/test_config_api.py:120-135 (assert sobre external_source de zonas_definidas, ya extractado)
    frontend/js/views/timeline.js:140-155 (consumidor de /api/zones a migrar a v2)
  </read_first>
  <acceptance_criteria>
    - `grep -n "line_start_x_frac\|line_start_y_frac\|line_end_x_frac\|line_end_y_frac" backend/config.py` NO produce coincidencias.
    - `grep -n "line_start_x_frac" backend/api/v2/config_schema.py` NO produce coincidencias.
    - `grep -n "external_source=\"/api/v2/zones\"" backend/api/v2/config_schema.py` confirma la migración.
    - `grep -n "external_source=\"/api/v2/lines\"" backend/api/v2/config_schema.py` confirma el grupo nuevo "líneas definidas".
    - `grep -n "fetch('/api/zones')" frontend/js/views/timeline.js` NO produce coincidencias (migrado a v2).
    - `pytest tests/test_config_schema.py tests/test_config_api.py -q` termina en verde.
  </acceptance_criteria>
  <action>
    En `backend/config.py`, eliminar las 4 líneas de `Settings` (`line_start_x_frac`,
    `line_start_y_frac`, `line_end_x_frac`, `line_end_y_frac`) y su comentario asociado.

    En `backend/api/v2/config_schema.py`, dentro de `Section(key="zonas", ...)`,
    sustituir el `Group(key="linea", ...)` completo (4 `FieldDef`) por
    `Group(key="lineas_definidas", label="Líneas definidas", external_source="/api/v2/lines")`
    y cambiar `Group(key="zonas_definidas", ..., external_source="/api/zones")` a
    `external_source="/api/v2/zones"`.

    En `tests/test_config_api.py`, actualizar el assert de `external_source` de zonas a
    `"/api/v2/zones"` y añadir el equivalente para `lineas_definidas` si el test recorre
    todos los grupos de la sección "zonas" (releer el test completo antes de decidir el
    alcance exacto del cambio).

    En `frontend/js/views/timeline.js`, cambiar `fetch('/api/zones')` a
    `fetch('/api/v2/zones')` — la forma de la respuesta (`{"zones": [...]}` con
    `id`/`name`) es compatible, sin más cambios en ese fichero.
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/test_config_schema.py tests/test_config_api.py -q</automated>
  </verify>
  <done>config_schema.py sin campos de línea única obsoletos, external_source de zonas/líneas apuntando a v2, timeline.js migrado.</done>
</task>

<task type="auto">
  <name>Task 3: Suite completa + verificación de arranque real</name>
  <files>backend/main.py</files>
  <read_first>
    (sin lectura nueva — esta tarea ejecuta y corrige lo que la suite completa señale
    tras las Tasks 1-2 de este mismo plan)
  </read_first>
  <acceptance_criteria>
    - `pytest tests/ -q` reporta 0 failed (skips permitidos si ya eran preexistentes, comparar con el conteo del cierre de la Fase 32 en STATE.md).
    - `grep -rn "reconfigure_line(" backend/` NO produce coincidencias fuera de `backend/tracker.py` (el wrapper de compatibilidad documentado en el Plan 33-04, si sigue existiendo, es el único sitio permitido — verificar si ya se retiró; si sigue vivo y ya no lo llama nadie tras este plan, retirarlo aquí y documentarlo).
    - `python -c "from backend.main import app"` no lanza excepción de import (humo mínimo de que el wiring no rompe la carga del módulo).
  </acceptance_criteria>
  <action>
    Ejecutar la suite completa. Corregir cualquier fallo derivado de las Tasks 1-2 (no
    ocultar fallos preexistentes ajenos a esta fase — si aparece alguno, documentarlo en
    el SUMMARY igual que hizo 30-12 con el bug de `/health` con `inf`, sin intentar
    arreglarlo si es ajeno). Si `PersonTracker.reconfigure_line` (wrapper de
    compatibilidad del Plan 33-04) ya no tiene ningún llamador real tras este plan
    (confirmado por el Plan 33-05, que debía retirar su único uso en `camera.py`),
    eliminarlo de `backend/tracker.py` en esta tarea — es el cierre natural de esa
    compatibilidad temporal, y este plan es el primero que puede confirmar con certeza
    que ya no hace falta (todo lo que construye `PersonTracker` pasa por este fichero).
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/ -q</automated>
  </verify>
  <done>Suite completa en verde, arranque de la app verificado por import, wrapper de compatibilidad retirado si ya no tiene llamador.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Arranque del proceso → carga de configuración | Sin entrada de red; superficie ya cubierta por los threat models de los Planes 33-03/33-06/33-07 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-19 | Tampering | `config/rules.yaml` con una regla inválida, en el camino de fallback de arranque cuando `rules` está vacía | accept | Comportamiento IDÉNTICO al preexistente (`load_rules()` ya captura errores por regla) — este plan no cambia esa ruta, solo la evita cuando la tabla ya tiene filas |
</threat_model>

<verification>
```bash
.venv/Scripts/python.exe -m pytest tests/ -q
```
Suite completa — este plan toca arranque/wiring compartido, exige el mismo criterio que
30-12/31-11/32-07: relanzar todo antes de cerrar.
</verification>

<success_criteria>
- Pipeline y motor de reglas arrancan 100% desde v2 (ZoneRepo/LineRepo/RuleRepo).
- Endpoints v1 huérfanos retirados (salvo /api/zones/stats, documentado explícitamente).
- config_schema.py sin campos muertos, external_source correctos.
- Suite completa en verde.
</success_criteria>

<output>
Al terminar, crear `.planning/phases/33-editores-visuales-de-zonas-l-neas-y-reglas/33-08-SUMMARY.md`
</output>
