---
phase: 25-re-identificaci-n-de-personas-reid
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/fetch_models.py
  - .gitignore
  - requirements.txt
  - backend/perception/reid/__init__.py
  - backend/perception/reid/engine.py
  - tests/test_reid_engine.py
autonomous: true
requirements: [REID-01]

must_haves:
  truths:
    - "El fichero models/reid/osnet_x0_25_msmt17_dyn.onnx existe tras ejecutar scripts/fetch_models.py, con sha256 verificado y eje de batch dinamico"
    - "ReIDEngine.embed(crop) devuelve un vector 512D float32 con norma L2 = 1"
    - "La mediana (p50) de ReIDEngine.embed() sobre un crop real es menor de 20 ms en CPU (criterio 1)"
    - "Sin el fichero de modelo, ReIDEngine.available es False y embed() devuelve None sin lanzar excepcion"
    - "Un modelo con eje de batch fijo distinto de 1 deja ReIDEngine.available a False (no se arrastran 84 ms por inferencia)"
  artifacts:
    - path: "scripts/fetch_models.py"
      provides: "Descarga idempotente + verificacion sha256 + reescritura del eje batch a dinamico"
      contains: "e78604f4ccda49b8f41cd0f8f7303800ce75d2361895ebb0729513c1bf53d277"
    - path: "backend/perception/reid/engine.py"
      provides: "ReIDEngine — adaptador ONNXRuntime con degradacion graciosa"
      exports: ["ReIDEngine"]
      min_lines: 70
    - path: "backend/perception/reid/__init__.py"
      provides: "Paquete backend.perception.reid"
    - path: "tests/test_reid_engine.py"
      provides: "Criterio 1 (512D, norma L2, p50 < 20 ms) + degradacion + guarda de batch fijo"
      min_lines: 60
    - path: ".gitignore"
      contains: "models/"
    - path: "requirements.txt"
      contains: "onnx>="
  key_links:
    - from: "scripts/fetch_models.py"
      to: "models/reid/osnet_x0_25_msmt17_dyn.onnx"
      via: "descarga HTTPS + onnx.save tras reescribir dim0"
      pattern: "onnx\\.save"
    - from: "backend/perception/reid/engine.py"
      to: "onnxruntime.InferenceSession"
      via: "carga del modelo en __init__ con CPUExecutionProvider"
      pattern: "InferenceSession"
    - from: "backend/perception/reid/engine.py"
      to: "np.linalg.norm"
      via: "L2-normalizacion de la salida cruda (norma ~52, el modelo NO normaliza)"
      pattern: "np\\.linalg\\.norm"
---

<objective>
Producir el fichero ONNX de OSNet listo para batch 1 y el `ReIDEngine` que lo consume,
ambos verificables en aislamiento sin tocar el pipeline.

Purpose: es el prerrequisito Wave 0 de toda la fase — sin modelo con eje de batch
dinamico, el criterio 1 falla por 4x (84,5 ms vs 20 ms exigidos) y `tests/test_reid_engine.py`
no puede existir. El RESEARCH ya descargo, hasheo, reescribio y cronometro este modelo en
este mismo equipo; esta tarea reproduce ese spike **dentro del repo**.

Output: `scripts/fetch_models.py`, `backend/perception/reid/{__init__,engine}.py`,
`tests/test_reid_engine.py`, `models/reid/osnet_x0_25_msmt17_dyn.onnx` (gitignored).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/25-re-identificaci-n-de-personas-reid/25-RESEARCH.md
@.planning/phases/25-re-identificaci-n-de-personas-reid/25-PATTERNS.md
@backend/perception/face/engine.py
@scripts/migrate_embeddings.py
@tests/test_face_engine.py

<interfaces>
<!-- Contratos que el ejecutor necesita. Extraidos del repo y del RESEARCH.
     No hace falta explorar el codebase. -->

Contrato SPEC_v2.md §5.6 (locked, ampliado por D-3/D-4 del RESEARCH):
```python
class ReIDEngine:
    def embed(self, person_crop: np.ndarray) -> np.ndarray | None:   # 512D float32 L2-normalizado
```

Datos verificados del modelo (RESEARCH § Blocker resuelto):
```
url primaria : https://huggingface.co/kornia/osnet/resolve/main/osnet_x0_25_msmt17.onnx
espejo       : https://huggingface.co/anriha/osnet_x0_25_msmt17/resolve/main/osnet_x0_25_msmt17.onnx
sha256       : e78604f4ccda49b8f41cd0f8f7303800ce75d2361895ebb0729513c1bf53d277
bytes        : 907169
input  'input'  [16, 3, 256, 128]  float32   <-- dim_value FIJO 16, no simbolico
output 'output' [16, 512]          float32
norma L2 de la salida cruda: ~52.4  (el modelo NO devuelve embeddings normalizados)
```

Entorno (ya instalado, no hay nada que instalar):
```
onnxruntime 1.28.0   (requirements.txt: onnxruntime>=1.19)
onnx        1.22.0   (hoy solo transitiva de insightface -> hay que declararla)
scikit-image         (requirements.txt: scikit-image>=0.24)
python: F:/Documentos/IA/Proyecto_Camara/.venv/Scripts/python.exe   (el worktree no tiene .venv propio)
```

pytest.ini fija `python_functions = TEST_*`: todo test nuevo DEBE llamarse `TEST_*`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: scripts/fetch_models.py — descarga verificada + reescritura del eje batch</name>
  <read_first>
    - `scripts/migrate_embeddings.py` (fichero completo, 124 lineas) — patron de script CLI del repo:
      docstring con `Usage:`, idempotencia declarada, `argparse`, funcion que **devuelve** el exit
      code (no llama a `sys.exit` por dentro), `_report()` alineado al final, y el bloque
      `if __name__ == "__main__": ... sys.exit(migrate(args.db_path))` (lineas 1-27, 53-58, 104-123).
    - `.planning/phases/25-re-identificaci-n-de-personas-reid/25-RESEARCH.md` § "Code Examples"
      (bloque `ModelSpec` + `_to_dynamic_batch`) y § "Blocker resuelto en research".
    - `.gitignore` (fichero completo) — estilo de bloque con comentario, p. ej.
      `# Clip thumbnails (Fase 20) — generated at runtime from the trigger frame` + `data/thumbnails/`.
    - `requirements.txt` (fichero completo) — usa `>=` sin pins; `onnxruntime>=1.19` esta en el
      bloque de la Fase 23.
  </read_first>
  <files>scripts/fetch_models.py, .gitignore, requirements.txt</files>
  <action>
Crear `scripts/fetch_models.py` con solo biblioteca estandar + `onnx` (nada de `requests`,
nada de `huggingface_hub`). Estructura exacta:

```python
"""Descarga y prepara los modelos ONNX que el repo no autodescarga (SPEC_v2.md §4.3).

Hoy solo OSNet (ReID, Fase 25): insightface autodescarga los suyos, este no.
Idempotente: si el destino ya existe y tiene el eje de batch dinamico, no
descarga nada y sale 0. Verifica sha256 y tamano exacto antes de escribir:
un ONNX es un grafo ejecutable, descargarlo de un tercero sin comprobar el
hash es una cadena de suministro sin control (ASVS V6).

Usage:
    .venv/Scripts/python.exe scripts/fetch_models.py [--force]
"""

from __future__ import annotations

import argparse
import hashlib
import shutil
import sys
import tempfile
import urllib.request
from dataclasses import dataclass
from pathlib import Path

_PROJECT_ROOT = Path(__file__).resolve().parent.parent


@dataclass(frozen=True)
class ModelSpec:
    name: str
    url: str
    mirror: str
    sha256: str
    size: int
    dest: Path


OSNET = ModelSpec(
    name="osnet_x0_25_msmt17",
    url="https://huggingface.co/kornia/osnet/resolve/main/osnet_x0_25_msmt17.onnx",
    mirror=(
        "https://huggingface.co/anriha/osnet_x0_25_msmt17/resolve/main/"
        "osnet_x0_25_msmt17.onnx"
    ),
    # sha256 identico en las dos fuentes (verificado en el research de la Fase 25).
    sha256="e78604f4ccda49b8f41cd0f8f7303800ce75d2361895ebb0729513c1bf53d277",
    size=907_169,
    dest=_PROJECT_ROOT / "models" / "reid" / "osnet_x0_25_msmt17_dyn.onnx",
)

MODELS = [OSNET]
```

Funciones (todas devuelven valores, ninguna llama a `sys.exit`):

1. `_download(url: str, dst: Path) -> None` — `urllib.request.urlopen(url, timeout=60)` +
   `shutil.copyfileobj` al fichero temporal. Sin verificacion aqui.
2. `_sha256(path: Path) -> str` — lectura por bloques de 1 MiB con `hashlib.sha256`.
3. `_to_dynamic_batch(src: Path, dst: Path) -> None` — **copiar tal cual** del RESEARCH:
   ```python
   def _to_dynamic_batch(src: Path, dst: Path) -> None:
       """El export publico fija dim0=16 en entrada y salida. ORT rechaza batch 1
       y una llamada rellena a 16 cuesta 84,5 ms en vez de 4,97 ms (medido en el
       research de la Fase 25). Reescribir el eje da un grafo bit-identico
       (verificado: max|delta| = 0.0) con batch dinamico."""
       import onnx

       m = onnx.load(str(src))
       for t in list(m.graph.input) + list(m.graph.output):
           d = t.type.tensor_type.shape.dim[0]
           d.ClearField("dim_value")
           d.dim_param = "batch"
       dst.parent.mkdir(parents=True, exist_ok=True)
       onnx.save(m, str(dst))
   ```
4. `_batch_dim_is_dynamic(path: Path) -> bool` — `onnx.load` + devolver
   `graph.input[0].type.tensor_type.shape.dim[0].dim_param != ""`. Es la comprobacion de
   idempotencia: si el destino ya existe y esto es `True`, no se descarga nada.
5. `fetch(spec: ModelSpec, force: bool = False) -> int` — orquesta y devuelve el exit code:
   - Si `spec.dest.exists()` y `_batch_dim_is_dynamic(spec.dest)` y no `force`:
     imprimir `f"[skip] {spec.name}: ya presente en {spec.dest} con batch dinamico"`, devolver 0.
   - Descargar a `Path(tempfile.mkdtemp()) / "model.onnx"`, primero `spec.url`; si lanza
     cualquier `Exception`, avisar por stdout y reintentar con `spec.mirror`. Si ambas fallan,
     imprimir el error y devolver 1.
   - Verificar `tmp.stat().st_size == spec.size`; si no coincide, imprimir esperado/obtenido y
     devolver 1 **sin escribir en `models/`**.
   - Verificar `_sha256(tmp) == spec.sha256`; si no, mismo tratamiento (devolver 1).
   - `_to_dynamic_batch(tmp, spec.dest)`.
   - Imprimir el reporte alineado (estilo `_report()` de `migrate_embeddings.py`):
     destino, bytes descargados, `sha256 OK`, `batch: 16 -> dinamico`.
   - Devolver 0.
6. `main(force: bool) -> int` — recorre `MODELS`, acumula `max(codes)`, devuelve el peor.
7. Bloque `if __name__ == "__main__":` con `argparse` (`--force`, `action="store_true"`,
   help "vuelve a descargar aunque el destino ya exista") y `sys.exit(main(args.force))`.

Reglas no negociables:
- La escritura solo ocurre bajo `_PROJECT_ROOT / "models"`, con ruta derivada de la constante
  `OSNET.dest`, nunca de entrada de usuario (ASVS V12).
- **Nada de esto puede vivir en `ReIDEngine.__init__`**: un arranque del servidor no depende de
  la red (RESEARCH § Don't Hand-Roll).

Ademas:
- `.gitignore`: anadir al final un bloque nuevo con comentario, siguiendo el estilo del fichero:
  ```
  # Modelos ONNX descargados por scripts/fetch_models.py (Fase 25) — ~900 KB de binario
  models/
  ```
- `requirements.txt`: anadir `onnx>=1.16` justo despues de `onnxruntime>=1.19`. Hoy `onnx` esta
  en el venv solo como transitiva de `insightface`; `fetch_models.py` la usa directamente, asi
  que debe declararse (regla de dependencias explicitas del repo).

Finalmente, **ejecutar el script** para dejar el modelo en su sitio:
`F:/Documentos/IA/Proyecto_Camara/.venv/Scripts/python.exe scripts/fetch_models.py`
  </action>
  <verify>
    <automated>F:/Documentos/IA/Proyecto_Camara/.venv/Scripts/python.exe scripts/fetch_models.py &amp;&amp; F:/Documentos/IA/Proyecto_Camara/.venv/Scripts/python.exe scripts/fetch_models.py &amp;&amp; F:/Documentos/IA/Proyecto_Camara/.venv/Scripts/python.exe -c "import onnx,sys; m=onnx.load('models/reid/osnet_x0_25_msmt17_dyn.onnx'); d=m.graph.input[0].type.tensor_type.shape.dim[0]; print('dim_param=',repr(d.dim_param)); sys.exit(0 if d.dim_param else 1)"</automated>
  </verify>
  <acceptance_criteria>
    - `models/reid/osnet_x0_25_msmt17_dyn.onnx` existe y `onnx.load(...).graph.input[0].type.tensor_type.shape.dim[0].dim_param == "batch"` (el comando de verify sale con codigo 0).
    - Segunda ejecucion consecutiva de `scripts/fetch_models.py` imprime una linea que contiene `[skip]` y sale con codigo 0 (idempotencia).
    - `grep -n "e78604f4ccda49b8f41cd0f8f7303800ce75d2361895ebb0729513c1bf53d277" scripts/fetch_models.py` devuelve exactamente 1 linea.
    - `grep -n "907_169\|907169" scripts/fetch_models.py` devuelve al menos 1 linea.
    - `grep -n "anriha" scripts/fetch_models.py` devuelve al menos 1 linea (espejo declarado).
    - `grep -n "^models/$" .gitignore` devuelve 1 linea.
    - `grep -n "^onnx>=1.16$" requirements.txt` devuelve 1 linea.
    - `grep -n "sys.exit" scripts/fetch_models.py` solo aparece dentro del bloque `if __name__ == "__main__":` (las funciones devuelven exit codes).
    - `git status --short models/` no lista ningun fichero (el modelo esta ignorado).
  </acceptance_criteria>
  <done>El modelo con batch dinamico esta en `models/reid/`, el script es idempotente y verifica hash+tamano antes de escribir, `models/` esta gitignored y `onnx>=1.16` declarado.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ReIDEngine — adaptador ONNXRuntime con degradacion graciosa</name>
  <read_first>
    - `backend/perception/face/engine.py` lineas 1-106 (fichero completo) — el analog exacto:
      docstring de "adaptador fino", `self._available = False` primero, `try/except` con
      `logger.exception`, `return` temprano, `available` como `@property`, constante de clase con
      comentario que justifica la decision con la medicion que la respalda (`_ALLOWED_MODULES`,
      lineas 73-78), y `embed()` devolviendo `None` en vez de lanzar (lineas 111-124 de PATTERNS).
    - `backend/perception/face/__init__.py` (1 linea) — el `__init__.py` del paquete hermano.
    - `.planning/phases/25-re-identificaci-n-de-personas-reid/25-RESEARCH.md` § "Pattern 1"
      (implementacion completa de `ReIDEngine`, copiarla).
  </read_first>
  <files>backend/perception/reid/__init__.py, backend/perception/reid/engine.py</files>
  <behavior>
    - `ReIDEngine("models/reid/osnet_x0_25_msmt17_dyn.onnx").embed(crop_bgr)` devuelve `np.ndarray` de shape `(512,)`, dtype `float32`, con `abs(norm - 1.0) < 1e-3`.
    - `ReIDEngine("no/such.onnx").available is False` y `embed(cualquier_cosa) is None`, sin lanzar.
    - Si `sess.get_inputs()[0].shape[0]` es un `int` distinto de 1 (modelo con batch fijo), `available is False` y se loguea un `logger.error` que menciona `scripts/fetch_models.py`.
    - `embed(None)` y `embed(np.zeros((0,0,3)))` devuelven `None`.
  </behavior>
  <action>
Crear `backend/perception/reid/__init__.py` con **una sola linea** de docstring, igual de escueto
que `backend/perception/face/__init__.py`:

```python
"""Re-identificacion de personas por apariencia (OSNet / ONNXRuntime, Fase 25)."""
```

Crear `backend/perception/reid/engine.py` copiando **literalmente** el bloque `Pattern 1` del
RESEARCH (§ "Architecture Patterns / Pattern 1"), con `from __future__ import annotations`,
`logger = logging.getLogger(__name__)` a nivel de modulo, e imports directos de `cv2`, `numpy as np`
y `onnxruntime as ort` (a diferencia de `FaceEngine`, aqui la libreria es dependencia dura desde la
Fase 23; lo que degrada es **el fichero del modelo**, no el import):

```python
class ReIDEngine:
    """Embeddings de apariencia 512D con osnet_x0_25 (MSMT17) sobre ONNXRuntime CPU.

    Degrada como FaceEngine: sin modelo, available=False y embed() devuelve None.
    """

    INPUT_HW = (256, 128)                                   # H, W del export ONNX
    # Valores de preprocesado de la implementacion de referencia (boxmot
    # reid/core/preprocessing.py): resize a 128x256 INTER_LINEAR, BGR2RGB,
    # /255, media/desviacion de ImageNet. Usar otra normalizacion no falla,
    # solo degrada la calidad de los embeddings en silencio.
    _MEAN = np.array([0.485, 0.456, 0.406], np.float32).reshape(3, 1, 1)
    _STD = np.array([0.229, 0.224, 0.225], np.float32).reshape(3, 1, 1)

    def __init__(self, model_path: str, intra_op_threads: int = 1) -> None:
        self._available = False
        self._sess = None
        try:
            so = ort.SessionOptions()
            so.graph_optimization_level = ort.GraphOptimizationLevel.ORT_ENABLE_ALL
            # 1 hilo: el worker comparte CPU con YOLO. Medido en el research de
            # la Fase 25: 12,16 ms p50 con 1 hilo vs 4,97 ms sin limite -- sigue
            # muy por debajo de los 20 ms del criterio 1 y no le roba cores al
            # DetectionWorker.
            so.intra_op_num_threads = intra_op_threads
            so.inter_op_num_threads = intra_op_threads
            self._sess = ort.InferenceSession(
                model_path, sess_options=so, providers=["CPUExecutionProvider"]
            )
            self._in = self._sess.get_inputs()[0].name
            self._out = self._sess.get_outputs()[0].name
            batch_dim = self._sess.get_inputs()[0].shape[0]
            if isinstance(batch_dim, int) and batch_dim != 1:
                # El export publico viene con batch FIJO 16: una inferencia
                # suelta costaria 84,5 ms en vez de 4,97 ms y el criterio 1
                # fallaria por 4x. scripts/fetch_models.py reescribe ese eje;
                # si no se ejecuto, mejor deshabilitar que arrastrar 84 ms.
                logger.error(
                    "ReIDEngine: modelo con batch fijo %s — ejecutar "
                    "scripts/fetch_models.py", batch_dim
                )
                return
            self._available = True
        except Exception:
            logger.exception("ReIDEngine: fallo al cargar %s", model_path)

    @property
    def available(self) -> bool:
        return self._available

    def embed(self, person_crop: np.ndarray | None) -> np.ndarray | None:
        """512D L2-normalizado (float32). None si el motor no esta o el crop es vacio."""
        if not self._available or person_crop is None or person_crop.size == 0:
            return None
        h, w = self.INPUT_HW
        x = cv2.resize(person_crop, (w, h), interpolation=cv2.INTER_LINEAR)
        x = cv2.cvtColor(x, cv2.COLOR_BGR2RGB).astype(np.float32) / 255.0
        x = (np.transpose(x, (2, 0, 1)) - self._MEAN) / self._STD
        raw = self._sess.run([self._out], {self._in: x[None]})[0][0]
        # El modelo NO normaliza: norma cruda medida ~52,4. SPEC §5.6 exige 512D
        # normalizado y el coseno de TrackGallery lo da por hecho. float32, no
        # float64: la galeria guarda 2 KB por entrada, no 4 KB.
        return (raw / np.linalg.norm(raw)).astype(np.float32)
```

Envolver `self._sess.run(...)` en un `try/except Exception` que loguee con `logger.exception` y
devuelva `None` — el hilo del worker no debe recibir excepciones (regla "degradacion graciosa" de
PATTERNS § Shared Patterns).

**No** anadir descarga, cache, ni `os.path.exists` con fallback de red: eso vive en
`scripts/fetch_models.py`.
  </action>
  <verify>
    <automated>F:/Documentos/IA/Proyecto_Camara/.venv/Scripts/python.exe -c "import numpy as np; from backend.perception.reid.engine import ReIDEngine; e=ReIDEngine('models/reid/osnet_x0_25_msmt17_dyn.onnx'); print('available', e.available); v=e.embed(np.zeros((200,80,3), dtype=np.uint8)); print(v.shape, v.dtype, float(np.linalg.norm(v))); b=ReIDEngine('no/such.onnx'); assert b.available is False and b.embed(np.zeros((10,10,3),dtype=np.uint8)) is None"</automated>
  </verify>
  <acceptance_criteria>
    - El comando de verify sale con codigo 0 e imprime `available True`, `(512,) float32` y una norma en `1.0 ± 0.001`.
    - `grep -n "np.linalg.norm" backend/perception/reid/engine.py` devuelve al menos 1 linea.
    - `grep -n "astype(np.float32)" backend/perception/reid/engine.py` devuelve al menos 1 linea.
    - `grep -n "fetch_models.py" backend/perception/reid/engine.py` devuelve al menos 1 linea (mensaje de la guarda de batch fijo).
    - `grep -nE "urllib|requests|download|hf_hub" backend/perception/reid/engine.py` no devuelve nada (cero red en runtime).
    - `grep -c "def " backend/perception/reid/engine.py` <= 4 (`__init__`, `available`, `embed`, y como mucho un helper).
    - `F:/Documentos/IA/Proyecto_Camara/.venv/Scripts/python.exe -m pytest tests/test_architecture.py -q` sigue verde.
  </acceptance_criteria>
  <done>`ReIDEngine` carga el modelo, produce 512D float32 L2-normalizado, se deshabilita ante modelo ausente o batch fijo != 1, y nunca lanza hacia el llamante.</done>
</task>

<task type="auto">
  <name>Task 3: tests/test_reid_engine.py — criterio 1 y guardas del motor</name>
  <read_first>
    - `tests/test_face_engine.py` (fichero completo, 75 lineas) — el analog exacto: docstring que
      justifica usar `skimage.data.astronaut()` como fixture real "fetched at test time rather than
      stored as a repo asset", fixtures `scope="module"`, assert de 512D + norma L2 (lineas 55-61)
      y degradacion graciosa via `monkeypatch` (lineas 64-75).
    - `pytest.ini` — `python_functions = TEST_*` (los tests DEBEN llamarse `TEST_*`).
    - `.planning/phases/25-re-identificaci-n-de-personas-reid/25-VALIDATION.md` § Per-Task
      Verification Map, filas de REID-01 (nombres exactos de los 4 tests con `-k`).
  </read_first>
  <files>tests/test_reid_engine.py</files>
  <action>
Crear `tests/test_reid_engine.py` con docstring de cabecera que explique las dos decisiones no
obvias: (a) imagen real de `skimage.data`, nunca ruido aleatorio — con ruido el coseno entre dos
embeddings independientes de OSNet sale 0,991 (colapso fuera de distribucion, medido en el
research); (b) `pytest.skip` si el modelo no esta, porque a diferencia de `insightface` aqui no hay
autodescarga.

Fixtures (`scope="module"`):

```python
MODEL_PATH = Path("models/reid/osnet_x0_25_msmt17_dyn.onnx")


@pytest.fixture(scope="module")
def engine() -> ReIDEngine:
    if not MODEL_PATH.exists():
        pytest.skip(
            f"{MODEL_PATH} ausente — ejecutar scripts/fetch_models.py. "
            "Sin autodescarga (a diferencia de insightface), skip, nunca fallo."
        )
    e = ReIDEngine(str(MODEL_PATH))
    if not e.available:
        pytest.skip("ReIDEngine no disponible con el modelo presente")
    return e


@pytest.fixture(scope="module")
def person_crop_bgr() -> np.ndarray:
    import skimage.data as data
    return cv2.cvtColor(data.astronaut(), cv2.COLOR_RGB2BGR)
```

Los 4 tests (nombres exactos — `25-VALIDATION.md` los selecciona con `-k`):

1. `TEST_reid_embedding_is_512d_l2_normalized(engine, person_crop_bgr)` — criterio 1, parte de
   forma: `emb.shape == (512,)`, `emb.dtype == np.float32`, `abs(float(np.linalg.norm(emb)) - 1.0) < 1e-3`.

2. `TEST_reid_latency_under_20ms(engine, person_crop_bgr)` — criterio 1, parte de latencia.
   **Medir p50, no un maximo y no una sola llamada**: el p95 medido en el research sube a ~30 ms
   por jitter del planificador de Windows en esta maquina compartida, asi que un assert sobre una
   sola llamada seria flaky. Estructura obligatoria:
   ```python
   for _ in range(5):                       # warmup: la primera inferencia paga la
       engine.embed(person_crop_bgr)        # inicializacion de los kernels de ORT
   samples = []
   for _ in range(30):                      # >= 30 iteraciones
       t0 = time.perf_counter()
       engine.embed(person_crop_bgr)
       samples.append(time.perf_counter() - t0)
   p50 = statistics.median(samples)
   assert p50 < 0.020, (
       f"criterio 1: p50 de embed() = {p50 * 1000:.2f} ms, se exige < 20 ms "
       f"(medido en el research: 5,50 ms; 84,5 ms si el eje batch sigue fijo a 16)"
   )
   ```

3. `TEST_reid_engine_degrades_gracefully()` — sin fixture (no necesita modelo):
   `broken = ReIDEngine("no/such/path.onnx")`, `assert broken.available is False`,
   `assert broken.embed(np.zeros((10, 10, 3), dtype=np.uint8)) is None`, y que `embed(None)`
   tampoco lanza.

4. `TEST_reid_engine_rejects_fixed_batch(monkeypatch)` — Pitfall 1. No hace falta fabricar un ONNX:
   monkeypatchear `ort.InferenceSession` en el modulo del motor para que devuelva un doble cuyo
   `get_inputs()[0].shape == [16, 3, 256, 128]`:
   ```python
   import backend.perception.reid.engine as engine_module

   class _FakeIO:
       def __init__(self, name, shape):
           self.name = name
           self.shape = shape

   class _FakeSession:
       def __init__(self, *a, **kw): ...
       def get_inputs(self):  return [_FakeIO("input", [16, 3, 256, 128])]
       def get_outputs(self): return [_FakeIO("output", [16, 512])]

   monkeypatch.setattr(engine_module.ort, "InferenceSession", _FakeSession)
   assert ReIDEngine("cualquier.onnx").available is False
   ```
   Con comentario de cabecera explicando la consecuencia de no tener esta guarda: 84,5 ms por
   inferencia en vez de 4,97 ms, criterio 1 fallado por 4x.

Imports necesarios: `statistics`, `time`, `pathlib.Path`, `cv2`, `numpy as np`, `pytest`,
`from backend.perception.reid.engine import ReIDEngine`.
  </action>
  <verify>
    <automated>F:/Documentos/IA/Proyecto_Camara/.venv/Scripts/python.exe -m pytest tests/test_reid_engine.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_reid_engine.py -q` recoge exactamente 4 tests y todos pasan (0 skipped, porque Task 1 dejo el modelo en su sitio).
    - `pytest tests/test_reid_engine.py -k embedding_is_512d_l2_normalized -q` selecciona 1 test y pasa (no "no tests ran").
    - `pytest tests/test_reid_engine.py -k latency_under_20ms -q` selecciona 1 test y pasa.
    - `pytest tests/test_reid_engine.py -k degrades_gracefully -q` selecciona 1 test y pasa.
    - `pytest tests/test_reid_engine.py -k rejects_fixed_batch -q` selecciona 1 test y pasa.
    - `grep -c "def TEST_" tests/test_reid_engine.py` devuelve 4 (ningun `def test_`: `grep -c "def test_" tests/test_reid_engine.py` devuelve 0).
    - `grep -n "statistics.median\|median(" tests/test_reid_engine.py` devuelve al menos 1 linea (p50, no maximo).
    - `grep -n "pytest.skip" tests/test_reid_engine.py` devuelve al menos 1 linea.
    - `grep -n "np.random" tests/test_reid_engine.py` no devuelve nada (Pitfall 3).
  </acceptance_criteria>
  <done>Los 4 tests de REID-01 pasan; el criterio 1 se mide como p50 con warmup de >=30 iteraciones, y el fichero hace skip (no fallo) si el modelo falta.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| huggingface.co -> `models/reid/` | Artefacto ejecutable (grafo ONNX) de un tercero entra en el disco del proyecto |
| `.env` (`REID_MODEL_PATH`) -> `ReIDEngine` | Ruta de fichero controlada por configuracion (se valida en el plan 25-05) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-01 | Tampering | `scripts/fetch_models.py` | mitigate | sha256 `e78604f4...` + tamano exacto 907169 verificados **antes** de escribir en `models/`; espejo secundario con el mismo hash |
| T-25-02 | Tampering / DoS | `ReIDEngine.__init__` | mitigate | Cero red en runtime: `grep -E "urllib|requests"` sobre `engine.py` debe salir vacio; la descarga vive solo en `scripts/` |
| T-25-03 | Denial of Service | Carga del modelo | mitigate | `try/except` + `available=False`: sin modelo el sistema arranca exactamente igual que hoy |
| T-25-04 | Tampering | Deserializacion del modelo | accept | ONNX es protobuf, no pickle; `test_no_pickle_in_backend` (test_architecture.py:117-124) sigue cubriendo el invariante |
</threat_model>

<verification>
```bash
F:/Documentos/IA/Proyecto_Camara/.venv/Scripts/python.exe scripts/fetch_models.py
F:/Documentos/IA/Proyecto_Camara/.venv/Scripts/python.exe -m pytest tests/test_reid_engine.py tests/test_architecture.py -q
git status --short models/   # debe salir vacio
```
</verification>

<success_criteria>
- `models/reid/osnet_x0_25_msmt17_dyn.onnx` existe con eje de batch dinamico y sha256 verificado.
- `scripts/fetch_models.py` es idempotente (2a ejecucion: `[skip]`, exit 0) y no escribe nada si el hash o el tamano no cuadran.
- `ReIDEngine.embed()` devuelve 512D float32 con norma L2 = 1 en p50 < 20 ms (criterio 1 del ROADMAP).
- Sin modelo o con batch fijo != 1, `available is False` y nada lanza.
- `models/` gitignored, `onnx>=1.16` en `requirements.txt`.
- `tests/test_reid_engine.py`: 4 tests `TEST_*` verdes; `tests/test_architecture.py` sigue verde.
</success_criteria>

<output>
Al terminar, crear `.planning/phases/25-re-identificaci-n-de-personas-reid/25-01-SUMMARY.md`.
</output>
