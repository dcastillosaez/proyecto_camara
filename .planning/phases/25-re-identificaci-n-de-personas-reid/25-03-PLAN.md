---
phase: 25-re-identificaci-n-de-personas-reid
plan: 03
type: execute
wave: 2
depends_on: ["25-01"]
files_modified:
  - backend/perception/reid/gallery.py
  - tests/test_track_gallery.py
  - tests/test_memory_bounds.py
autonomous: true
requirements: [REID-02, REID-04]

must_haves:
  truths:
    - "TrackGallery.resolve() devuelve el person_id de un track cerrado hace menos de 15 s cuando la similitud coseno supera 0.7 (criterio 2)"
    - "TrackGallery.resolve() NO hereda si la identidad candidata esta en un track visible en el frame actual (conflicto, criterio 2)"
    - "TrackGallery.resolve() NO hereda si la similitud es 0.7 o menor, ni si la entrada tiene mas de 15 s (criterio 4: dos apariencias distintas no se fusionan)"
    - "TrackGallery.resolve() devuelve SIEMPRE (candidato, similitud) reales: el modo solo-observacion no vive aqui"
    - "TrackGallery.needs_embedding(track_id, now) devuelve False antes de que pase reid_interval_secs desde la ultima llamada a update() de ese track (criterio 5)"
    - "TrackGallery nunca supera max_entries entradas ni acumula entradas mas viejas que la ventana de herencia"
  artifacts:
    - path: "backend/perception/reid/gallery.py"
      provides: "TrackGallery — memoria de embeddings de tracks recientes, dominio puro con reloj inyectado"
      exports: ["TrackGallery"]
      min_lines: 80
    - path: "tests/test_track_gallery.py"
      provides: "Criterios 2 y 4 con vectores 512D sinteticos de coseno controlado"
      min_lines: 90
    - path: "tests/test_memory_bounds.py"
      provides: "Cota de memoria de TrackGallery (invariante de la Fase 22)"
      contains: "TEST_track_gallery_bounded"
  key_links:
    - from: "TrackGallery.resolve"
      to: "producto escalar de embeddings normalizados"
      via: "coseno directo emb @ e.emb (ADR-03: np.dot, sin base vectorial)"
      pattern: "emb @ |np\\.dot"
    - from: "TrackGallery.resolve"
      to: "active_identities / frame_ids"
      via: "comprobacion de conflicto exigida por REID-02"
      pattern: "active_identities"
    - from: "TrackGallery.prune"
      to: "self._entries"
      via: "doble guarda TTL + cota dura LRU por last_seen"
      pattern: "max_entries|_max_entries"
---

<objective>
Construir `TrackGallery`: la memoria de embeddings de apariencia de los tracks recientes y la
resolucion del candidato a heredar identidad. Dominio puro, sin reloj propio, sin hilos, sin I/O.

Purpose: es donde viven los criterios 2, 4 y 5 del ROADMAP, y es testeable en aislamiento
completo — sin modelo ONNX, sin pipeline y sin imagenes. El RESEARCH midio que dos entradas de
**ruido aleatorio** dan coseno 0,991 con OSNet (colapso fuera de distribucion), asi que este plan
usa vectores 512D construidos a mano con coseno conocido: es la unica forma de que el umbral 0,7
sea una frontera real en los tests.

Output: `backend/perception/reid/gallery.py`, `tests/test_track_gallery.py` y el test de cota en
`tests/test_memory_bounds.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/25-re-identificaci-n-de-personas-reid/25-RESEARCH.md
@.planning/phases/25-re-identificaci-n-de-personas-reid/25-PATTERNS.md
@backend/perception/face/identity.py
@tests/test_identity_state_machine.py
@tests/test_memory_bounds.py

<interfaces>
<!-- Contrato final de TrackGallery. SPEC §5.6 omitia `now` y `active_identities`;
     D-3/D-4 del RESEARCH los anaden por la regla de reloj inyectado que la Fase 24
     ya establecio (SPEC §5.5 mostraba on_face_result(track_id, person_id, score) y
     el codigo real es on_face_result(track_id, person_id, score, now)). -->

```python
@dataclass
class _GalleryEntry:
    emb: np.ndarray                  # float32, 512D, L2-normalizado
    person_id: int | None
    last_seen: float
    last_embedded_at: float


class TrackGallery:
    def __init__(self, inherit_window: float = 15.0, similarity_threshold: float = 0.7,
                 interval: float = 2.0, max_entries: int = 256) -> None: ...

    def needs_embedding(self, track_id: int, now: float) -> bool: ...
    def update(self, track_id: int, emb: np.ndarray, identity: int | None, now: float) -> None: ...
    def resolve(self, track_id: int, emb: np.ndarray, now: float,
                active_identities: set[int]) -> tuple[int | None, float]: ...
    def prune(self, now: float, frame_ids: set[int]) -> None: ...
```

Parametros locked (CONTEXT §Parametros por defecto):
`reid_inherit_window_secs = 15.0`, `reid_similarity_threshold = 0.7`,
`reid_interval_secs = 2.0`, `max_entries = 256` (RESEARCH §Q6).

Aritmetica de memoria (RESEARCH §Q6): 512 x float32 = 2 KB/entrada; numpy por defecto usaria
float64 = 4 KB. Techo: 256 x 2 KB = 512 KB.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: backend/perception/reid/gallery.py — TrackGallery</name>
  <read_first>
    - `backend/perception/face/identity.py` lineas 1-12 (docstring de dominio puro y reloj inyectado)
      y 100-148 (`TemporalVoter.prune` con la justificacion de ByteTrack, `_TrackIdentity` con
      comentario por campo, docstring de clase + `__init__` con parametros de politica).
    - `backend/perception/face/identity.py` lineas 425-457 (`on_tick`) — el patron de doble guarda
      TTL + "seguro de vida", incluida la nota de iterar sobre `list(self._states)` y no sobre el
      dict vivo.
    - `.planning/phases/25-re-identificaci-n-de-personas-reid/25-RESEARCH.md` § "Pattern 2"
      (esqueleto de `TrackGallery` y las 4 reglas de `resolve()` en orden) y §Q4 y §Q6.
    - `.planning/phases/25-re-identificaci-n-de-personas-reid/25-PATTERNS.md` § 3 (traduccion del
      patron de expiracion a `TrackGallery`).
  </read_first>
  <files>backend/perception/reid/gallery.py</files>
  <behavior>
    - `needs_embedding(1, now=0.0)` con la galeria vacia ⇒ `True` (track nunca embebido).
    - Tras `update(1, emb, None, now=0.0)`: `needs_embedding(1, 1.9)` ⇒ `False`; `needs_embedding(1, 2.0)` ⇒ `True` (comparacion `>=`).
    - `resolve(2, emb_b, now=5.0, active_identities=set())` con una entrada `(track 1, person 7, last_seen=0.0)` cuyo coseno con `emb_b` es 0.95 ⇒ `(7, ~0.95)`.
    - Mismo caso con coseno 0.65 ⇒ `(None, ~0.65)`: devuelve el candidato descartado con su similitud real, para poder auditar.
    - Mismo caso con `now=16.0` (fuera de la ventana de 15 s) ⇒ `(None, 0.0)`.
    - Mismo caso con `active_identities={7}` ⇒ `(None, ~0.95)`: conflicto, la persona 7 sigue visible en otro track.
    - Una entrada con `person_id is None` nunca es candidata.
    - `resolve(1, ...)` nunca se resuelve consigo mismo (`tid != track_id`).
    - `prune(now=20.0, frame_ids=set())` borra las entradas con `now - last_seen > 15.0`.
    - Tras 10.000 `update()` con ids crecientes, `len(gallery._entries) <= max_entries`.
  </behavior>
  <action>
Crear `backend/perception/reid/gallery.py`. Docstring de modulo en el estilo de `identity.py:1-12`,
declarando explicitamente la regla de dominio puro:

```python
"""TrackGallery — continuidad de identidad por apariencia (SPEC_v2.md §5.6, REID-02/03/04).

Dominio puro: no importa `time`, no arranca hilos, no hace I/O y no construye
eventos. Todos los metodos que dependen del reloj lo reciben como parametro
`now: float` (monotonico), igual que IdentityStateMachine y AdaptiveRate.
La firma de SPEC §5.6 omitia `now` y `active_identities`; sin el primero no se
puede aplicar la ventana de 15 s y sin el segundo no se puede detectar el
conflicto que exige REID-02 (misma correccion que la Fase 24 hizo con
on_face_result).

Fuera de alcance aqui: decidir si la herencia se aplica (eso es el flag
reid_inherit_identity, que vive en RecognitionWorker) y transicionar el estado
de identidad (eso es IdentityStateMachine.on_reid_result).
"""
```

`_GalleryEntry` como `@dataclass`, con comentario en linea en cada campo cuya semantica no sea
obvia (patron de `_TrackIdentity`, identity.py:112-124):

```python
@dataclass
class _GalleryEntry:
    """Ultimo embedding conocido de un track (patron `_TrackIdentity` de identity.py)."""

    emb: np.ndarray                # float32 512D L2-normalizado (2 KB; float64 costaria 4 KB)
    person_id: int | None          # identidad CONFIRMED del track cuando se embebio
    last_seen: float               # ultima vez que el track se refresco: base del TTL
    last_embedded_at: float        # ultima inferencia ReID de este track: base del criterio 5
```

Clase con docstring que declare el single-thread sin lock (mismo argumento que
`identity.py:129-130`):

```python
class TrackGallery:
    """Memoria de apariencia de los tracks recientes (SPEC_v2.md §5.6, REID-02/04).

    Reloj inyectado: ningun metodo llama a time.monotonic(). Un solo hilo
    (RecognitionWorker._loop), por eso no hay lock — igual que IdentityStateMachine.

    No aplica politica de producto: resolve() calcula SIEMPRE el candidato real y
    devuelve (person_id, similitud). Que la herencia se aplique o solo se registre
    lo decide el flag reid_inherit_identity en el worker; si resolve() devolviera
    None en modo observacion se perderia justo el dato que se quiere auditar.
    """

    def __init__(
        self,
        inherit_window: float = 15.0,
        similarity_threshold: float = 0.7,
        interval: float = 2.0,
        max_entries: int = 256,
    ) -> None:
        self._inherit_window = inherit_window
        self._threshold = similarity_threshold
        self._interval = interval
        self._max_entries = max_entries
        self._entries: dict[int, _GalleryEntry] = {}
```

Metodos:

**`needs_embedding(self, track_id: int, now: float) -> bool`** — copiar del RESEARCH §Q4, con
docstring que explique el coste que evita (patron de `needs_recognition`, identity.py:381-404):
```python
    def needs_embedding(self, track_id: int, now: float) -> bool:
        """A que track toca re-embeber ahora mismo (criterio 5: max 1 cada reid_interval_secs).

        Espejo exacto de IdentityStateMachine.needs_recognition (FACE-11), pero con
        su propio reloj: reutilizar `last_face_at` mezclaria la cadencia facial
        (revalidate_after = 120 s) con la de apariencia (2 s) en el mismo campo.
        """
        e = self._entries.get(track_id)
        return e is None or (now - e.last_embedded_at) >= self._interval
```

**`update(self, track_id, emb, identity, now) -> None`** — crea o refresca la entrada. Forzar
`emb.astype(np.float32, copy=False)` con el comentario de los 2 KB/entrada. Fija `last_seen = now`
y `last_embedded_at = now`. Tras insertar, aplicar la cota dura (ver `prune`).

**`resolve(self, track_id, emb, now, active_identities) -> tuple[int | None, float]`** — las 4
reglas de REID-02 en **este orden exacto** (RESEARCH § Pattern 2):
1. Candidatos = entradas con `person_id is not None` y `tid != track_id`.
2. Frescura: `now - e.last_seen <= self._inherit_window`.
3. Similitud: `sim = float(emb @ e.emb)` (coseno directo, ambos ya normalizados — ADR-03:
   `np.dot`, sin base vectorial). Quedarse con el **maximo**.
4. Si no hay mejor candidato ⇒ `(None, 0.0)`.
   Si `best_sim <= self._threshold` ⇒ `(None, best_sim)` — devolver la similitud real aunque no
   se herede, es el dato de auditoria del criterio 4.
   Si `best_person_id in active_identities` ⇒ `(None, best_sim)` con un comentario que explique
   que esto es lo que impide fusionar dos personas cuando la "perdida" sigue en pantalla.
   En otro caso ⇒ `(best_person_id, best_sim)`.

   Nota obligatoria en el docstring: el umbral es **estricto** (`sim > threshold`), coherente con
   el criterio 2 del ROADMAP ("similitud > 0.7").

**`prune(self, now: float, frame_ids: set[int]) -> None`** — doble guarda calcada de
`on_tick` (identity.py:425-457), iterando siempre sobre `list(self._entries)`:
- Guarda 1 (TTL): borrar entradas con `now - e.last_seen > self._inherit_window`. Un track visible
  refresca `last_seen` en cada `update()` (<= 2 s), asi que nunca se cae estando en pantalla.
- Refrescar `last_seen = now` de las entradas cuyo `tid in frame_ids` **antes** de aplicar el TTL:
  un track visible al que aun no le toca re-embeber no debe caducar.
- Guarda 2 (cota dura, "seguro de vida" de la Fase 22): si `len(self._entries) > self._max_entries`,
  borrar las mas antiguas por `last_seen` (LRU) hasta la cota, con el comentario que explique que
  actua **aunque nadie llame a `prune()` a tiempo`** — mismo razonamiento que identity.py:450-453.
  Extraer esa guarda a un helper privado `_enforce_cap()` y llamarlo **tambien desde `update()`**,
  para que la cota se cumpla sin depender del mantenimiento.

**Prohibiciones** (verificables por grep):
- No `import time`, no `import threading`, no `import cv2`, no imports de `backend/pipeline/`.
- No ramas de politica de producto (`reid_inherit_identity` no aparece en este fichero).
- No FAISS/hnswlib/kd-tree: producto escalar directo (ADR-03, N < 20.000).
  </action>
  <verify>
    <automated>F:/Documentos/IA/Proyecto_Camara/.venv/Scripts/python.exe -c "import numpy as np; from backend.perception.reid.gallery import TrackGallery; g=TrackGallery(); a=np.zeros(512,dtype=np.float32); a[0]=1.0; b=np.zeros(512,dtype=np.float32); b[0]=0.95; b[1]=(1-0.95**2)**0.5; g.update(1,a,7,now=0.0); print(g.resolve(2,b,now=5.0,active_identities=set())); print(g.resolve(2,b,now=5.0,active_identities={7})); print(g.resolve(2,b,now=16.0,active_identities=set())); print(g.needs_embedding(1,1.9), g.needs_embedding(1,2.0))"</automated>
  </verify>
  <acceptance_criteria>
    - El comando de verify imprime, en este orden: `(7, 0.95...)`, `(None, 0.95...)`, `(None, 0.0)`, `False True`.
    - `grep -n "def needs_embedding\|def update\|def resolve\|def prune" backend/perception/reid/gallery.py` devuelve 4 lineas.
    - `grep -n "active_identities" backend/perception/reid/gallery.py` devuelve al menos 2 lineas (firma + comprobacion de conflicto).
    - `grep -n "astype(np.float32" backend/perception/reid/gallery.py` devuelve al menos 1 linea.
    - `grep -n "_max_entries" backend/perception/reid/gallery.py` devuelve al menos 3 lineas (init, `_enforce_cap`, llamada).
    - `grep -nE "^import time|^import threading|^import cv2|from backend.pipeline" backend/perception/reid/gallery.py` no devuelve nada.
    - `grep -n "reid_inherit_identity\|inherit_identity" backend/perception/reid/gallery.py` no devuelve nada (la politica vive en el worker).
    - `grep -nE "faiss|hnswlib|KDTree|cdist" backend/perception/reid/gallery.py` no devuelve nada.
    - `F:/Documentos/IA/Proyecto_Camara/.venv/Scripts/python.exe -m pytest tests/test_architecture.py -q` verde.
  </acceptance_criteria>
  <done>`TrackGallery` implementa las 4 reglas de `resolve()` en orden, el gate `needs_embedding` del criterio 5 y la doble guarda de expiracion, sin reloj propio ni politica de producto.</done>
</task>

<task type="auto">
  <name>Task 2: tests/test_track_gallery.py — criterios 2 y 4 con vectores sinteticos</name>
  <read_first>
    - `tests/test_identity_state_machine.py` lineas 1-35 (docstring + helper `_fsm()` con defaults
      sobreescribibles) y 306-350 (estilo de separadores de seccion, `now=` explicito en cada
      llamada, aserciones con mensaje).
    - `.planning/phases/25-re-identificaci-n-de-personas-reid/25-RESEARCH.md` § Pitfall 3
      (la trampa medida: coseno 0,991 entre dos ruidos independientes) y §Q5 (que patron de test
      usar en cada sitio).
    - `pytest.ini` — `python_functions = TEST_*`.
    - `.planning/phases/25-re-identificaci-n-de-personas-reid/25-VALIDATION.md` filas de REID-02 y
      criterio 4 (los `-k` que deben seleccionar: `inherit`, `conflict`, `does_not_merge`).
  </read_first>
  <files>tests/test_track_gallery.py</files>
  <action>
Crear `tests/test_track_gallery.py`. Docstring de cabecera con la advertencia medida — es la
decision de diseño mas importante del fichero:

```python
"""Tests para TrackGallery (Fase 25, REID-02/REID-04).

La galeria es dominio puro: no hay hilos, no hay reloj real, no hay ONNX. El
reloj se inyecta como float sintetico, igual que en test_identity_state_machine.py.

NUNCA usar np.random como "embedding de persona": el research de la Fase 25 midio
que el coseno entre dos embeddings de OSNet alimentados con ruido independiente
sale 0,991 (colapso fuera de distribucion). Un test asi "demostraria" que todo se
parece a todo y no probaria nada sobre el umbral de 0,7. Aqui los vectores se
construyen a mano con coseno EXACTO y controlado.
"""
```

Helpers:

```python
DIM = 512


def _gallery(inherit_window=15.0, similarity_threshold=0.7, interval=2.0,
             max_entries=256) -> TrackGallery:
    return TrackGallery(inherit_window=inherit_window,
                        similarity_threshold=similarity_threshold,
                        interval=interval, max_entries=max_entries)


def _vec(i: int) -> np.ndarray:
    """Vector base ortonormal i: coseno 0 con cualquier _vec(j != i)."""
    v = np.zeros(DIM, dtype=np.float32)
    v[i] = 1.0
    return v


def _vec_at_cosine(base_index: int, cos: float, other_index: int = 511) -> np.ndarray:
    """Vector unitario cuyo coseno con _vec(base_index) es EXACTAMENTE `cos`.

    cos * e_base + sqrt(1 - cos^2) * e_other, con e_base ⟂ e_other.
    """
    v = np.zeros(DIM, dtype=np.float32)
    v[base_index] = cos
    v[other_index] = float(np.sqrt(1.0 - cos * cos))
    return v
```

Tests (nombres exactos, todos `TEST_*`):

1. `TEST_gallery_inherits_identity_within_window_above_threshold()` — criterio 2, caso positivo:
   `update(1, _vec(0), 7, now=0.0)`; `resolve(2, _vec_at_cosine(0, 0.95), now=5.0, active_identities=set())`
   ⇒ `pid == 7` y `sim == pytest.approx(0.95, abs=1e-4)`.

2. `TEST_gallery_does_not_inherit_below_threshold()` — coseno 0.65 ⇒ `pid is None` y
   `sim == approx(0.65)`. Comentario: la similitud se devuelve **igualmente** porque es el dato de
   auditoria del criterio 4.

3. `TEST_gallery_does_not_inherit_at_exactly_threshold()` — coseno 0.70 exacto ⇒ `pid is None`
   (el criterio 2 dice "similitud > 0.7", umbral estricto).

4. `TEST_gallery_does_not_inherit_outside_window()` — criterio 2, ventana: coseno 0.95 pero
   `now=16.0` con `inherit_window=15.0` ⇒ `pid is None`.

5. `TEST_gallery_conflict_with_active_identity_blocks_inherit()` — criterio 2, conflicto:
   coseno 0.95, dentro de ventana, pero `active_identities={7}` ⇒ `pid is None` y `sim` sigue
   siendo ~0.95. Comentario obligatorio: esto es lo que impide fusionar dos personas cuando la
   "perdida" en realidad sigue en pantalla.

6. `TEST_gallery_does_not_merge_two_different_appearances()` — criterio 4 con vectores
   deterministas: registrar `update(1, _vec(0), 7, now=0.0)` y `update(2, _vec(1), 9, now=0.0)`;
   `resolve(3, _vec_at_cosine(0, 0.98), now=1.0, set())` ⇒ `7` (no `9`), y
   `resolve(4, _vec_at_cosine(1, 0.30), now=1.0, set())` ⇒ `None`. Aserción con mensaje que cite
   el criterio 4 ("dos personas distintas con ropa similar no se fusionan").

7. `TEST_gallery_ignores_entries_without_identity()` — `update(1, _vec(0), None, now=0.0)`;
   `resolve(2, _vec_at_cosine(0, 0.99), now=1.0, set())` ⇒ `(None, 0.0)`.

8. `TEST_gallery_does_not_resolve_against_itself()` — `update(1, _vec(0), 7, now=0.0)`;
   `resolve(1, _vec(0), now=1.0, set())` ⇒ `(None, 0.0)`.

9. `TEST_gallery_needs_embedding_respects_interval()` — criterio 5 en aislamiento:
   `needs_embedding(1, 0.0) is True`; `update(1, _vec(0), None, now=0.0)`;
   `needs_embedding(1, 1.9) is False`; `needs_embedding(1, 2.0) is True`.

10. `TEST_gallery_prune_drops_entries_older_than_window()` — tres entradas con `last_seen`
    escalonados; `prune(now=20.0, frame_ids=set())` deja solo las de dentro de la ventana.

11. `TEST_gallery_prune_keeps_tracks_present_in_frame()` — una entrada vieja cuyo `tid` esta en
    `frame_ids` no se borra (el track sigue delante de la camara aunque aun no le toque re-embeber).

12. `TEST_gallery_embeddings_are_float32()` — `update(1, np.ones(512, dtype=np.float64) / np.sqrt(512), 7, now=0.0)`
    y comprobar `gallery._entries[1].emb.dtype == np.float32` (2 KB/entrada, no 4 KB).
  </action>
  <verify>
    <automated>F:/Documentos/IA/Proyecto_Camara/.venv/Scripts/python.exe -m pytest tests/test_track_gallery.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_track_gallery.py -q` recoge 12 tests y todos pasan.
    - `pytest tests/test_track_gallery.py -k inherit -q` selecciona >= 4 tests y todos pasan.
    - `pytest tests/test_track_gallery.py -k conflict -q` selecciona exactamente 1 test y pasa.
    - `pytest tests/test_track_gallery.py -k does_not_merge -q` selecciona exactamente 1 test y pasa.
    - `grep -c "def TEST_" tests/test_track_gallery.py` devuelve 12; `grep -c "def test_" tests/test_track_gallery.py` devuelve 0.
    - `grep -n "np.random" tests/test_track_gallery.py` no devuelve nada (Pitfall 3).
    - `grep -n "skimage\|cv2\|onnx" tests/test_track_gallery.py` no devuelve nada (sin imagenes, sin modelo).
    - `grep -n "def _vec_at_cosine" tests/test_track_gallery.py` devuelve 1 linea.
  </acceptance_criteria>
  <done>12 tests verdes cubriendo umbral (por encima, exacto y por debajo), ventana, conflicto, no-fusion, entradas sin identidad, auto-resolucion, intervalo y prune — todo con vectores de coseno exacto.</done>
</task>

<task type="auto">
  <name>Task 3: cota de memoria de TrackGallery en tests/test_memory_bounds.py</name>
  <read_first>
    - `tests/test_memory_bounds.py` lineas 23-30 (bloque de imports, ordenados alfabeticamente por
      modulo backend) y 91-120 (`TEST_temporal_voter_bounded` y `TEST_identity_state_machine_bounded`
      — la plantilla exacta: separador de seccion con comentario que explica por que la estructura
      podria crecer sin cota, bucle de 10.000 track_ids, assert sobre `len(...)`, y la variante con
      cota holgada explicando que el numero exacto no importa).
    - `.planning/phases/25-re-identificaci-n-de-personas-reid/25-RESEARCH.md` §Q6 (aritmetica de
      memoria: 2 KB/entrada, techo 512 KB).
  </read_first>
  <files>tests/test_memory_bounds.py</files>
  <action>
Anadir el import de `TrackGallery` en el bloque de imports existente (linea ~23-30), respetando el
orden alfabetico por modulo backend (`backend.perception.face.identity` va antes que
`backend.perception.reid.gallery`).

Anadir al final del fichero, con separador de seccion en el estilo del propio fichero:

```python
# ─── TrackGallery no acumula embeddings de tracks muertos ─────────────────────
# ByteTrack asigna ids monotonamente crecientes y nunca los reutiliza. Cada
# entrada son 512 float32 = 2 KB; sin doble guarda (TTL de inherit_window +
# cota dura max_entries) un proceso 24/7 la haria crecer sin limite. La cota
# dura es el "seguro de vida" de la Fase 22: actua aunque nadie llame a prune()
# a tiempo. Techo: 256 x 2 KB = 512 KB.
# ─────────────────────────────────────────────────────────────────────────────
def TEST_track_gallery_bounded():
    gallery = TrackGallery(inherit_window=15.0, similarity_threshold=0.7,
                           interval=2.0, max_entries=256)
    emb = np.zeros(512, dtype=np.float32)
    emb[0] = 1.0
    for tid in range(10_000):
        now = float(tid)
        gallery.update(tid, emb, tid % 7, now)
        gallery.prune(now, frame_ids={tid})
    assert len(gallery._entries) <= 256
```

Anadir un segundo test que verifique que la cota se respeta **sin llamar a `prune()`** (es lo que
demuestra que `_enforce_cap()` se aplica tambien desde `update()`):

```python
def TEST_track_gallery_bounded_without_prune():
    """La cota dura actua aunque el mantenimiento periodico nunca se ejecute."""
    gallery = TrackGallery(max_entries=256)
    emb = np.zeros(512, dtype=np.float32)
    emb[0] = 1.0
    for tid in range(10_000):
        gallery.update(tid, emb, 1, now=float(tid))
    assert len(gallery._entries) <= 256
```

No tocar ningun test existente del fichero.
  </action>
  <verify>
    <automated>F:/Documentos/IA/Proyecto_Camara/.venv/Scripts/python.exe -m pytest tests/test_memory_bounds.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_memory_bounds.py -k track_gallery_bounded -q` selecciona 2 tests y ambos pasan.
    - `pytest tests/test_memory_bounds.py -q` verde en su totalidad (los tests previos intactos).
    - `grep -n "from backend.perception.reid.gallery import TrackGallery" tests/test_memory_bounds.py` devuelve 1 linea.
    - `grep -n "10_000" tests/test_memory_bounds.py` devuelve al menos 4 lineas (los 2 previos + los 2 nuevos).
    - `git diff tests/test_memory_bounds.py | grep "^-" | grep -v "^---"` no devuelve nada (solo inserciones).
  </acceptance_criteria>
  <done>La cota de `TrackGallery` esta verificada con y sin `prune()`, con el invariante de la Fase 22 documentado en el comentario de seccion.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Embeddings de tracks -> atribucion de identidad | Un candidato mal resuelto atribuye a la persona A la actividad de la persona B |
| Flujo continuo de track_ids nuevos -> memoria del proceso | ByteTrack genera ids sin reutilizarlos en un proceso 24/7 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-08 | Spoofing / Repudiation | `TrackGallery.resolve` | mitigate | Umbral estricto `sim > 0.7` + ventana de 15 s (mas corta que los 30 s faciales, la apariencia es menos fiable) + comprobacion de conflicto contra `active_identities` |
| T-25-09 | Denial of Service | `TrackGallery._entries` | mitigate | Doble guarda: TTL por `inherit_window` en `prune()` + cota dura `max_entries=256` aplicada tambien desde `update()`; verificado con 10.000 ids en `test_memory_bounds.py` |
| T-25-10 | Information Disclosure | Embeddings en memoria | accept | Los embeddings no salen del proceso, no se persisten (SPEC §7 contempla `reid_embedding` en `tracks`, pero ningun requisito REID-01..04 lo pide) y caducan en 15 s |
</threat_model>

<verification>
```bash
F:/Documentos/IA/Proyecto_Camara/.venv/Scripts/python.exe -m pytest tests/test_track_gallery.py tests/test_memory_bounds.py tests/test_architecture.py -q
```
</verification>

<success_criteria>
- `TrackGallery` cumple las 4 reglas de `resolve()` (candidatos, frescura, similitud maxima,
  conflicto) y devuelve siempre la similitud real aunque no herede.
- `needs_embedding()` implementa el criterio 5 por track, con su propio reloj.
- Doble guarda de expiracion verificada con 10.000 track_ids, con y sin `prune()`.
- 12 tests en `tests/test_track_gallery.py` + 2 en `tests/test_memory_bounds.py`, todos verdes.
- Cero `np.random`, cero imagenes, cero ONNX en los tests de la galeria.
</success_criteria>

<output>
Al terminar, crear `.planning/phases/25-re-identificaci-n-de-personas-reid/25-03-SUMMARY.md`.
</output>
