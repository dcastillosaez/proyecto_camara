---
phase: 25-re-identificaci-n-de-personas-reid
plan: 04
type: execute
wave: 3
depends_on: ["25-01", "25-02", "25-03"]
files_modified:
  - backend/pipeline/recognition.py
  - tests/test_recognition_worker.py
autonomous: true
requirements: [REID-01, REID-02, REID-03, REID-04]

must_haves:
  truths:
    - "Una persona identificada que deja de mostrar la cara y reaparece con un track_id nuevo conserva su person_id, con exactamente 1 PERSON_RECOGNIZED y 0 UNKNOWN_PERSON (criterio 3)"
    - "El worker llama a ReIDEngine.embed como maximo una vez cada reid_interval_secs por track (criterio 5)"
    - "Con reid_inherit_identity=False, resolve() se ejecuta y los contadores suben, pero la FSM no cambia de estado (modo solo-observacion)"
    - "Cada match de ReID queda logueado a nivel INFO con track_id, person_id y similitud, para el audit de falsos positivos del criterio 4"
    - "Los contadores reid_inferences, reid_matches, reid_inherited y reid_conflicts salen por RecognitionWorker.stats"
    - "Sin ReIDEngine o sin TrackGallery, el worker se comporta exactamente como en la Fase 24"
    - "La latencia de ReID NO entra en AdaptiveRate: avg_latency en /api/v2/cameras/{id}/health sigue significando latencia facial"
  artifacts:
    - path: "backend/pipeline/recognition.py"
      provides: "Via ReID dentro de _loop: seleccion propia de candidato, embed, update, resolve, herencia condicional y prune"
      contains: "_next_reid_candidate"
    - path: "tests/test_recognition_worker.py"
      provides: "Criterio 3 end-to-end, criterio 5 (presupuesto de inferencias) y modo solo-observacion"
      contains: "TEST_reid_recovers_identity_without_face"
  key_links:
    - from: "RecognitionWorker._loop"
      to: "ReIDEngine.embed"
      via: "inferencia de apariencia sobre el crop del candidato ReID, en el hilo del worker"
      pattern: "self\\._reid_engine\\.embed\\("
    - from: "RecognitionWorker._loop"
      to: "TrackGallery.resolve"
      via: "resolucion del candidato con active_identities derivadas de registry.frame_ids()"
      pattern: "self\\._gallery\\.resolve\\("
    - from: "RecognitionWorker._loop"
      to: "IdentityStateMachine.on_reid_result"
      via: "herencia solo si reid_inherit_identity esta activo"
      pattern: "on_reid_result\\("
    - from: "RecognitionWorker._sync_identity"
      to: "TrackGallery.prune"
      via: "unico punto de mantenimiento periodico, ya recibe now y frame_ids"
      pattern: "self\\._gallery\\.prune\\("
    - from: "RecognitionWorker"
      to: "_metrics.inference_latency_seconds"
      via: "instrumentacion con stage='reid' (NUNCA self._rate.observe)"
      pattern: "stage=\"reid\""
---

<objective>
Cablear `ReIDEngine` + `TrackGallery` + `on_reid_result()` dentro de `RecognitionWorker`: la via
de apariencia corre en el mismo hilo y el mismo tick que la via facial, sin tocarla.

Purpose: aqui se cierran los criterios 3 y 5 del ROADMAP end-to-end, y aqui vive el flag
`reid_inherit_identity` que hace posible auditar la tasa de falsos positivos (criterio 4) antes de
activar la herencia en produccion. El RESEARCH §Q3 descarto crear un worker nuevo: la FSM esta
documentada como single-thread sin lock (identity.py:130) y un segundo hilo escribiendo en
`_states` obligaria a anadirle un lock, es decir, a reescribirla — justo lo que CONTEXT prohibe.
El coste anadido es de ~11 ms/s a 2 FPS, ~1,1 % de un core.

Output: `backend/pipeline/recognition.py` con la via ReID, y 4 tests nuevos en
`tests/test_recognition_worker.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/25-re-identificaci-n-de-personas-reid/25-RESEARCH.md
@.planning/phases/25-re-identificaci-n-de-personas-reid/25-PATTERNS.md
@.planning/phases/25-re-identificaci-n-de-personas-reid/25-02-SUMMARY.md
@.planning/phases/25-re-identificaci-n-de-personas-reid/25-03-SUMMARY.md
@backend/pipeline/recognition.py
@tests/test_recognition_worker.py

<interfaces>
<!-- Contratos ya construidos por los planes 25-01/25-02/25-03. -->

```python
# backend/perception/reid/engine.py  (plan 25-01)
class ReIDEngine:
    def __init__(self, model_path: str, intra_op_threads: int = 1) -> None: ...
    @property
    def available(self) -> bool: ...
    def embed(self, person_crop: np.ndarray | None) -> np.ndarray | None: ...   # 512D float32 L2

# backend/perception/reid/gallery.py  (plan 25-03)
class TrackGallery:
    def needs_embedding(self, track_id: int, now: float) -> bool: ...
    def update(self, track_id: int, emb: np.ndarray, identity: int | None, now: float) -> None: ...
    def resolve(self, track_id, emb, now, active_identities: set[int]) -> tuple[int | None, float]: ...
    def prune(self, now: float, frame_ids: set[int]) -> None: ...

# backend/perception/face/identity.py  (plan 25-02)
def on_reid_result(self, track_id: int, person_id: int | None,
                   similarity: float, now: float) -> IdentityTransition | None: ...
```

Estado actual de `RecognitionWorker` (recognition.py, verificado por lectura):

```python
class RecognitionWorker:
    CROP_PAD = ...
    def __init__(self, sub, registry, recognizer, rate, min_track_age=0.5,
                 prune_interval=10.0, on_identified=None, identity_fsm=None,
                 event_engine=None) -> None:
        ...
        self._identified = 0
        self._exceptions = 0
        self._face_inferences = 0          # lineas 47-80

    @property
    def stats(self) -> dict:               # linea 101-108
        return {"identified": ..., "exceptions": ..., "face_inferences": ...,
                **self._rate.stats}

    def _loop(self) -> None:               # linea 114
        available = getattr(self._recognizer, "available", False)
        while self._running:
            frame = self._sub.get(timeout=1.0)
            if frame is None: continue
            if not available: continue
            now = time.monotonic()
            self._maybe_prune(now)
            self._sync_identity(now)
            if not self._rate.should_process(now): continue
            target = self._next_candidate(now)
            if target is None: continue
            crop = self._crop_for(frame.image, target.bbox)
            if crop is None: continue
            t0 = time.monotonic()
            result = self._recognizer.process_crop_scored(crop, target.track_id)
            self._face_inferences += 1
            face_latency = time.monotonic() - t0
            self._rate.observe(face_latency)                                     # <-- ReID NO
            _metrics.inference_latency_seconds.labels(stage="face").observe(face_latency)
            ...
            now2 = time.monotonic()
            transition = self._fsm.on_face_result(target.track_id, result.person_id,
                                                  result.score, now2)
            self._registry.set_identity_state(target.track_id, self._fsm.state_of(target.track_id))
            ...
    def _next_candidate(self, now): ...    # linea 184-203
    def _crop_for(self, image, bbox) -> np.ndarray | None: ...   # 205-210
    def _sync_identity(self, now) -> None: ...                   # 220-238
    def _emit_identity(self, transition, ...) -> None: ...       # 240
    def _maybe_prune(self, now) -> None: ...                     # 253
```

`TrackRegistry` (read-only para ReID; `tracking.py` NO se modifica — D-5):
`snapshot() -> dict[int, TrackState]`, `frame_ids() -> set[int]`,
`set_identity(track_id, person_id, name)`, `set_identity_state(track_id, state)`.

Metricas ya existentes, sin cambios (`backend/observability/metrics.py:76-82`):
`reid_fps` (Gauge, label `camera`) e `inference_latency_seconds` (Histogram, label `stage`).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: via ReID dentro de RecognitionWorker</name>
  <read_first>
    - `backend/pipeline/recognition.py` completo (269 lineas): `__init__` (47-80), `stats` (101-108),
      `_loop` (114-183), `_next_candidate` (184-203), `_crop_for` (205-210), `_sync_identity`
      (220-238), `_emit_identity` (240-252), `_maybe_prune` (253-269).
    - `.planning/phases/25-re-identificaci-n-de-personas-reid/25-RESEARCH.md` §Q3, §Q4, §Q7,
      § "Code Examples" (`_next_reid_candidate` y `_active_identities`, copiarlos) y Pitfalls 6 y 7.
    - `.planning/phases/25-re-identificaci-n-de-personas-reid/25-PATTERNS.md` § 5 (bloque de
      inferencia de `_loop` con la anotacion `<-- ReID NO hace esto` sobre `self._rate.observe`).
    - `backend/observability/metrics.py` lineas 70-85 (gauges y histograma ya definidos).
  </read_first>
  <files>backend/pipeline/recognition.py</files>
  <behavior>
    - Sin `reid_engine` o sin `reid_gallery`, `_loop` se comporta byte a byte como en la Fase 24 (todos los tests previos siguen verdes).
    - Con motor y galeria, en cada tick que pasa `rate.should_process`, se elige **como mucho un** track para ReID y se llama a `embed()` una sola vez.
    - `stats` incluye `reid_inferences`, `reid_matches`, `reid_inherited` y `reid_conflicts`.
    - Con `reid_inherit=False`, `resolve()` se llama y `reid_matches` sube, pero `on_reid_result()` no se invoca y `reid_inherited` sigue a 0.
    - Con `reid_inherit=True` y un candidato valido, `on_reid_result()` devuelve una transicion, se propaga a `registry.set_identity_state`/`set_identity` y se pasa por `_emit_identity`.
    - Una excepcion en `embed()` o en `resolve()` incrementa `_exceptions` y no mata el hilo.
  </behavior>
  <action>
Todos los cambios son **aditivos**; no se modifica ninguna linea de la via facial.

**1. `__init__`** — tres kwargs nuevos al final de la firma, con default que preserva el
comportamiento actual, y contadores en el mismo bloque que `_face_inferences`:

```python
        identity_fsm: IdentityStateMachine | None = None,
        event_engine: EventEngine | None = None,
        reid_engine: "ReIDEngine | None" = None,
        reid_gallery: "TrackGallery | None" = None,
        reid_inherit: bool = False,
    ) -> None:
        ...
        self._reid_engine = reid_engine
        self._gallery = reid_gallery
        # REID_INHERIT_IDENTITY: el flag vive AQUI, no en TrackGallery. resolve()
        # calcula siempre el candidato real; si lo hiciera la galeria devolviendo
        # None en modo observacion se perderia justo el dato que se quiere auditar
        # (que identidad se habria heredado) — RESEARCH §Q7, criterio 4.
        self._reid_inherit = reid_inherit
        ...
        self._face_inferences = 0
        self._reid_inferences = 0
        self._reid_matches = 0
        self._reid_inherited = 0
        self._reid_conflicts = 0
```

Los imports de `ReIDEngine`/`TrackGallery` van bajo `if TYPE_CHECKING:` (junto a `EventEngine`,
`Subscription`, `PersonRecognizer`), para no crear dependencia dura del pipeline con el paquete
de percepcion ReID.

**2. `stats`** — anadir los 4 contadores:
```python
            "face_inferences": self._face_inferences,
            "reid_inferences": self._reid_inferences,
            "reid_matches": self._reid_matches,
            "reid_inherited": self._reid_inherited,
            "reid_conflicts": self._reid_conflicts,
            **self._rate.stats,
```
Salen solos por `manager.py:261` -> `/api/v2/cameras/{id}/health`: es el canal de auditoria del
criterio 4, sin endpoints nuevos.

**3. `_next_reid_candidate(self, now)`** — copiar del RESEARCH § "Code Examples" tal cual,
incluido el docstring que explica por que NO usa `fsm.needs_recognition()`:

```python
    def _next_reid_candidate(self, now: float):
        """Track mas antiguo que toca re-embeber ahora, o None.

        Deliberadamente NO usa fsm.needs_recognition(): el caso que ReID cubre
        (persona de espaldas, TEMPORARILY_LOST, UNKNOWN en backoff de 120 s) es
        justo donde ese gate dice que no. El unico limite es reid_interval_secs
        (criterio 5, dentro de TrackGallery.needs_embedding) y min_track_age.
        """
        tracks = [
            ts for ts in self._registry.snapshot().values()
            if (now - ts.first_seen) >= self._min_track_age
            and self._gallery.needs_embedding(ts.track_id, now)
        ]
        if not tracks:
            return None
        return min(tracks, key=lambda ts: ts.first_seen)
```

Anadir al docstring la nota de la cota agregada (RESEARCH §Q4): **un track por tick**; a 2 FPS y
`reid_interval_secs=2.0` eso sostiene hasta 4 tracks concurrentes a ritmo pleno, y con mas tracks
el intervalo efectivo por track se degrada por encima de 2 s — degradacion segura (menos coste,
nunca mas) que sigue cumpliendo el criterio 5.

**4. `_active_identities(self)`** — copiar del RESEARCH § "Code Examples":

```python
    def _active_identities(self) -> set[int]:
        """Identidades CONFIRMED sobre tracks visibles en el frame actual (REID-02).

        frame_ids(), no active_ids(): active_ids() tarda hasta 30 s (ttl de prune)
        en dejar de ver un track desaparecido — el bug D-05 de la Fase 24.
        identity_of() ya devuelve identidad solo si el track esta CONFIRMED.
        """
        out: set[int] = set()
        for tid in self._registry.frame_ids():
            pid, _ = self._fsm.identity_of(tid)
            if pid is not None:
                out.add(pid)
        return out
```

**5. `_reid_pass(self, frame, now)`** — metodo nuevo con toda la via ReID, invocado desde `_loop`
**despues** del bloque facial completo, dentro del mismo tick. Envolver el cuerpo entero en
`try/except Exception` que incremente `self._exceptions`, loguee con `logger.exception` y haga
`return` sin matar el hilo (patron de `_sync_identity`, recognition.py:220-238):

```python
    def _reid_pass(self, frame, now: float) -> None:
        """Via de apariencia (Fase 25). Corre en el mismo tick que la facial y en
        el mismo hilo: la FSM es single-thread sin lock (identity.py:130)."""
        if self._reid_engine is None or self._gallery is None or self._fsm is None:
            return
        if not getattr(self._reid_engine, "available", False):
            return
        try:
            target = self._next_reid_candidate(now)
            if target is None:
                return
            crop = self._crop_for(frame.image, target.bbox)
            if crop is None:
                return
            t0 = time.monotonic()
            emb = self._reid_engine.embed(crop)
            reid_latency = time.monotonic() - t0
            if emb is None:
                return
            self._reid_inferences += 1
            # Pitfall 6: NUNCA self._rate.observe(reid_latency). Ese AdaptiveRate
            # esta fijado a 2 FPS (min=max) y no cambiaria el ritmo, pero si
            # contaminaria avg_latency en /api/v2/cameras/{id}/health, que
            # significa "latencia facial".
            _metrics.inference_latency_seconds.labels(stage="reid").observe(reid_latency)

            pid_now, _conf = self._fsm.identity_of(target.track_id)
            self._gallery.update(target.track_id, emb, pid_now, now)

            pid, sim = self._gallery.resolve(
                target.track_id, emb, now, self._active_identities()
            )
            if pid is None:
                return
            self._reid_matches += 1
            logger.info(
                "ReID: track %d -> person %d (sim %.3f, inherit=%s)",
                target.track_id, pid, sim, self._reid_inherit,
            )
            if not self._reid_inherit:
                return                      # modo solo-observacion (criterio 4)
            now2 = time.monotonic()
            transition = self._fsm.on_reid_result(target.track_id, pid, sim, now2)
            if transition is None:
                self._reid_conflicts += 1   # la FSM rechazo la herencia
                return
            self._reid_inherited += 1
            self._registry.set_identity_state(
                target.track_id, self._fsm.state_of(target.track_id)
            )
            self._registry.set_identity(target.track_id, pid, None)
            self._emit_identity(
                transition,
                bbox=target.bbox,
                captured_at=frame.captured_at,
                processed_at=now2,
            )
        except Exception:
            self._exceptions += 1
            logger.exception("RecognitionWorker: via ReID fallo")
```

Nota sobre `set_identity(target.track_id, pid, None)`: el nombre se pasa `None` porque la via de
apariencia no sabe el nombre — mismo criterio de la via facial, que solo pasa el nombre si
corresponde al `pid` que la FSM ha fijado (recognition.py:180-186).

Comprobar la firma real de `_emit_identity` antes de llamarla y ajustar los kwargs a los que
acepte (`person_name` es opcional y aqui no se pasa).

**6. `_loop`** — dos cambios minimos:
- Sacar el `continue` que hoy termina la iteracion cuando `_next_candidate` devuelve `None`, de
  forma que la via ReID corra igualmente. La forma mas segura y menos invasiva es **envolver el
  bloque facial en un metodo `_face_pass(frame, now)`** que haga `return` donde hoy hace `continue`,
  y dejar `_loop` como:
  ```python
            if not self._rate.should_process(now):
                continue
            self._face_pass(frame, now)
            self._reid_pass(frame, time.monotonic())
  ```
  Si extraer `_face_pass` resultara arriesgado, la alternativa aceptable es sustituir cada
  `continue` del bloque facial por saltos que caigan en la llamada a `_reid_pass`. Lo no negociable
  es que **la ausencia de candidato facial no impida la pasada ReID** — precisamente el caso que
  ReID cubre (persona de espaldas: `needs_recognition()` dice que no).
- Ojo con `available` (recognition.py:115): hoy es `getattr(self._recognizer, "available", False)`
  y un `False` corta el tick antes de todo. **No cambiar esa guarda**: ReID es una via de
  recuperacion sobre un sistema con reconocimiento facial activo; sin `PersonRecognizer` no hay
  identidades que heredar.

**7. `_sync_identity`** — anadir la poda de la galeria al final, dentro del `try` existente que ya
recibe `now` y ya usa `frame_ids()`:
```python
            if self._gallery is not None:
                self._gallery.prune(now, self._registry.frame_ids())
```
No inventar un segundo bucle de mantenimiento (RESEARCH §Q6).

**Prohibiciones** (verificables por grep):
- `self._rate.observe(` NO puede aparecer dentro de `_reid_pass` (Pitfall 6).
- Cero `await` y cero `async def` en `recognition.py` (invariante del proyecto; `test_architecture.py`
  vigila literalmente el nombre `embed` en `INFERENCE_CALLS`, asi que `embed()` solo puede llamarse
  desde el hilo del worker).
- No tocar `backend/pipeline/tracking.py` (D-5) ni `backend/events/engine.py` (D-6).
  </action>
  <verify>
    <automated>F:/Documentos/IA/Proyecto_Camara/.venv/Scripts/python.exe -m pytest tests/test_recognition_worker.py tests/test_architecture.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "def _next_reid_candidate\|def _active_identities\|def _reid_pass" backend/pipeline/recognition.py` devuelve 3 lineas.
    - `grep -n 'stage="reid"' backend/pipeline/recognition.py` devuelve al menos 1 linea.
    - `sed -n '/def _reid_pass/,/def _sync_identity/p' backend/pipeline/recognition.py | grep -c "_rate.observe("` devuelve 0 (Pitfall 6).
    - `grep -n "reid_inferences\|reid_matches\|reid_inherited\|reid_conflicts" backend/pipeline/recognition.py` devuelve al menos 8 lineas (init + stats + incrementos).
    - `grep -n "self._gallery.prune(" backend/pipeline/recognition.py` devuelve exactamente 1 linea, dentro de `_sync_identity`.
    - `grep -n "needs_recognition" backend/pipeline/recognition.py` sigue apareciendo solo dentro de `_next_candidate` (la via ReID no lo usa): `sed -n '/def _next_reid_candidate/,/def _crop_for/p' backend/pipeline/recognition.py | grep -c needs_recognition` devuelve 0.
    - `grep -cE "async def|await " backend/pipeline/recognition.py` devuelve 0.
    - `git status --short backend/pipeline/tracking.py backend/events/engine.py` no lista nada (D-5, D-6).
    - `pytest tests/test_recognition_worker.py -q` verde (todos los tests de las Fases 23/24 intactos).
    - `pytest tests/test_architecture.py -q` verde.
  </acceptance_criteria>
  <done>La via ReID corre en `RecognitionWorker` con seleccion propia de candidato, instrumentacion `stage="reid"`, contadores en `stats`, poda de la galeria en `_sync_identity` y el flag de herencia en el worker; la via facial queda intacta.</done>
</task>

<task type="auto">
  <name>Task 2: tests de presupuesto (criterio 5) y modo solo-observacion</name>
  <read_first>
    - `tests/test_recognition_worker.py` lineas 24-49 (helpers `_make_frame`, `_FakeTracked`,
      `_publish_for`, `_face`) y 85-87 (patron de mock del motor con `MagicMock`).
    - `tests/test_recognition_worker.py` lineas 318-358 (`TEST_inference_budget_drops_on_unconfirmed_track`)
      — la plantilla exacta del test de presupuesto: funcion interna `_run()` que monta worker +
      registry, publica frames N segundos y **devuelve el `call_count`**, mas el comentario de
      cabecera que documenta el escalado del reloj.
    - `.planning/phases/25-re-identificaci-n-de-personas-reid/25-RESEARCH.md` §Q7 (modo
      solo-observacion) y §Q4 (cota agregada de un track por tick).
  </read_first>
  <files>tests/test_recognition_worker.py</files>
  <action>
Anadir un bloque nuevo al final del fichero con separador de seccion
(`# ─── Fase 25 — via de apariencia (ReID) ───`) y estos helpers locales:

```python
def _emb(i: int = 0) -> np.ndarray:
    """Vector 512D unitario determinista. NUNCA np.random: el research midio
    coseno 0,991 entre dos ruidos independientes con OSNet."""
    v = np.zeros(512, dtype=np.float32)
    v[i] = 1.0
    return v


def _reid_mock(vec: np.ndarray | None = None) -> MagicMock:
    engine = MagicMock()
    engine.available = True
    engine.embed.return_value = _emb(0) if vec is None else vec
    return engine
```

Tests (nombres exactos; `25-VALIDATION.md` los selecciona con `-k`):

1. `TEST_reid_inference_budget(broker)` — **criterio 5**. Estructura calcada de
   `TEST_inference_budget_drops_on_unconfirmed_track`:
   ```python
   # ─── Criterio 5: max 1 inferencia ReID cada reid_interval_secs por track ────
   # Escalado del reloj: los 2 s de produccion se comprimen a 0.5 s en el test,
   # con el tick del worker a 20 FPS para que el limite NO sea el tick sino el
   # gate de TrackGallery.needs_embedding.
   # ───────────────────────────────────────────────────────────────────────────
   registry = TrackRegistry()
   registry.update_from_detections(_FakeTracked([1]), now=time.monotonic() - 10)
   registry.set_frame_ids({1})
   recognizer = MagicMock(); recognizer.available = True
   recognizer.process_crop_scored.return_value = _face()
   engine = _reid_mock()
   gallery = TrackGallery(inherit_window=15.0, similarity_threshold=0.7, interval=0.5)
   fsm = IdentityStateMachine(TemporalVoter(window=2, min_votes=2))
   worker = RecognitionWorker(broker.subscribe("recognition"), registry, recognizer,
                              AdaptiveRate(target_fps=20.0, min_fps=20.0, max_fps=20.0),
                              min_track_age=0.0, identity_fsm=fsm,
                              reid_engine=engine, reid_gallery=gallery, reid_inherit=False)
   worker.start()
   _publish_for(broker, seconds=1.0)
   worker.stop()
   calls = engine.embed.call_count
   assert calls >= 1, "la via ReID no se ejecuto: el test no mide nada"
   assert calls <= 4, (
       f"criterio 5: {calls} inferencias ReID en 1 s con intervalo de 0.5 s "
       f"(se admite hasta 4 por jitter del planificador); a 20 FPS de tick "
       f"habria 20 sin el gate de needs_embedding"
   )
   ```
   El limite superior holgado (4 en vez de 3) es deliberado: el reloj real del worker introduce
   jitter en Windows. Lo que el test demuestra es la diferencia de orden de magnitud (20 -> ~2-3),
   no un numero exacto. Documentarlo en el comentario.

2. `TEST_reid_observation_only(broker)` — modo solo-observacion (`reid_inherit=False`):
   - Preparar el escenario minimo en el que `resolve()` **si** devolveria candidato: confirmar la
     persona 7 sobre el track 1 con la FSM (3 `on_face_result` directos), poblar la galeria con
     `gallery.update(1, _emb(0), 7, now)`, marcar el track 1 como perdido
     (`fsm.on_track_lost(1, now)`), y dejar en el registry solo el track 2 (`_FakeTracked([2])` +
     `set_frame_ids({2})`).
   - Arrancar el worker con `reid_inherit=False`, publicar 1 s de frames, parar.
   - Asserts: `worker.stats["reid_matches"] >= 1` (resolve calculo el candidato y lo registro) y
     `worker.stats["reid_inherited"] == 0` y `fsm.state_of(2) is IdentityState.UNKNOWN`
     (la FSM no cambio de estado). Mensaje de assert citando el criterio 4: el modo observacion
     debe recorrer exactamente el mismo camino hasta el punto de decision, con un solo `if` de
     diferencia — es lo que hace la auditoria creible.

3. `TEST_reid_counters_exposed_in_stats(broker)` — comprobar que `worker.stats` contiene las 4
   claves `reid_inferences`, `reid_matches`, `reid_inherited`, `reid_conflicts` (son el canal de
   auditoria via `/api/v2/cameras/{id}/health`, sin endpoints nuevos).

4. `TEST_worker_without_reid_behaves_as_phase_24(broker)` — construir el worker **sin**
   `reid_engine`/`reid_gallery`, publicar 1 s de frames y comprobar que no lanza, que
   `stats["reid_inferences"] == 0` y que la via facial siguio funcionando
   (`recognizer.process_crop_scored.call_count >= 1`).

Imports nuevos al principio del fichero: `TrackGallery` desde `backend.perception.reid.gallery`
(el motor se mockea, no se importa `ReIDEngine`).
  </action>
  <verify>
    <automated>F:/Documentos/IA/Proyecto_Camara/.venv/Scripts/python.exe -m pytest tests/test_recognition_worker.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_recognition_worker.py -k reid_inference_budget -q` selecciona 1 test y pasa.
    - `pytest tests/test_recognition_worker.py -k observation_only -q` selecciona 1 test y pasa.
    - `pytest tests/test_recognition_worker.py -q` verde en su totalidad.
    - `grep -n "def _emb" tests/test_recognition_worker.py` devuelve 1 linea; `grep -n "np.random" tests/test_recognition_worker.py` no devuelve nada.
    - `grep -n "reid_inherited\"\] == 0" tests/test_recognition_worker.py` devuelve al menos 1 linea.
    - `grep -n "embed.call_count" tests/test_recognition_worker.py` devuelve al menos 1 linea.
  </acceptance_criteria>
  <done>Criterio 5 medido contando llamadas al motor mockeado, modo solo-observacion verificado (calcula y cuenta pero no transiciona), contadores expuestos en `stats` y compatibilidad hacia atras sin ReID.</done>
</task>

<task type="auto">
  <name>Task 3: test end-to-end del criterio 3 — identidad conservada sin cara visible</name>
  <read_first>
    - `tests/test_recognition_worker.py` lineas 230-292
      (`TEST_track_recovery_via_real_path_emits_person_recognized_once`) — la plantilla exacta:
      mock que "solo reconoce al track activo" via `match_track = {"id": 1}` + `side_effect`
      (234-243), la secuencia `set_frame_ids({1})` -> `set_frame_ids(set())` ->
      `registry.prune(now, ttl=0.01)` -> `update_from_detections(_FakeTracked([99]))` +
      `set_frame_ids({99})` (254-275), y `make_engine()` + `wait_until()` +
      `await asyncio.sleep(0.1)` antes de contar eventos (283-292).
    - `.planning/phases/25-re-identificaci-n-de-personas-reid/25-VALIDATION.md` fila de REID-03
      (nombre del test: `reid_recovers_identity_without_face`).
    - `backend/events/engine.py` (solo lectura, NO se modifica): `emit_identity` descarta la
      transicion cuando `emits is False`, que es lo que da el "sin UNKNOWN_PERSON intermedio".
  </read_first>
  <files>tests/test_recognition_worker.py</files>
  <action>
Anadir `TEST_reid_recovers_identity_without_face` — el test que cierra el **criterio 3** del
ROADMAP end-to-end. Escenario, con comentario de cabecera que lo describa en los terminos del
criterio ("una persona identificada que se gira de espaldas 10 s y vuelve conserva su `person_id`
sin `UNKNOWN_PERSON` intermedio"):

1. Montar `EventEngine` real con un bus que acumule eventos en `received` (patron de
   `make_engine()` del test analogo, lineas 283-292).
2. `PersonRecognizer` mockeado con `side_effect` que **solo** reconoce a la persona 7 mientras el
   track activo sea el 1; en cuanto el track pasa a ser el 99 (reaparicion de espaldas) devuelve
   `_face()` — es decir, **la cara ya no esta disponible**. Este es el punto que diferencia este
   test del de la Fase 24, donde el reconocimiento facial si volvia a acertar.
3. `ReIDEngine` mockeado que devuelve **el mismo vector** para cualquier crop (`_emb(0)`): la
   apariencia sigue siendo la misma persona.
4. `TrackGallery` real (no mock) con `inherit_window=15.0`, `similarity_threshold=0.7`,
   `interval=0.1` (para que el test no espere 2 s).
5. `IdentityStateMachine` real con `TemporalVoter(window=2, min_votes=2)` y `lost_ttl=30.0`.
6. Worker con `reid_inherit=True`.
7. Secuencia temporal, copiando la del test analogo:
   - Track 1 visible: `update_from_detections(_FakeTracked([1]))` + `set_frame_ids({1})`,
     publicar frames hasta que `fsm.state_of(1) is IdentityState.CONFIRMED` (`wait_until`).
   - Desaparicion: `set_frame_ids(set())`, `registry.prune(now, ttl=0.01)`, publicar frames para
     que `_sync_identity` lleve el track 1 a `TEMPORARILY_LOST`.
   - Reaparicion con id nuevo: `update_from_detections(_FakeTracked([99]))` + `set_frame_ids({99})`,
     publicar frames.
   - `worker.stop()` + `await asyncio.sleep(0.1)` antes de contar (con el comentario del original
     sobre por que `wait_until` puede salir sin ceder el control al loop).
8. Asserts, cada uno con mensaje que cite el criterio:
   ```python
   assert fsm.identity_of(99)[0] == 7, (
       "criterio 3: el track reaparecido no heredo el person_id por apariencia"
   )
   recognized = [e for e in received if e.type is EventType.PERSON_RECOGNIZED]
   assert len(recognized) == 1, (
       f"se esperaba 1 PERSON_RECOGNIZED, hubo {len(recognized)} -- la herencia "
       "por ReID debe llevar emits=False (misma visita)"
   )
   unknown = [e for e in received if e.type is EventType.UNKNOWN_PERSON]
   assert unknown == [], (
       "criterio 3: hubo UNKNOWN_PERSON intermedio; la persona nunca dejo de estar "
       "identificada"
   )
   assert worker.stats["reid_inherited"] >= 1
   ```

Si el test resulta inestable por tiempos, ajustar **solo** los tiempos de publicacion y los
`wait_until`, nunca relajar los asserts: los tres asserts de arriba SON el criterio 3.
  </action>
  <verify>
    <automated>F:/Documentos/IA/Proyecto_Camara/.venv/Scripts/python.exe -m pytest tests/test_recognition_worker.py -k reid_recovers_identity_without_face -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_recognition_worker.py -k reid_recovers_identity_without_face -q` selecciona exactamente 1 test y pasa (no "no tests ran").
    - El mismo comando repetido 3 veces seguidas pasa las 3 (no flaky).
    - `grep -n "UNKNOWN_PERSON" tests/test_recognition_worker.py` devuelve al menos 1 linea dentro del test nuevo.
    - `grep -n "reid_inherit=True" tests/test_recognition_worker.py` devuelve al menos 1 linea.
    - `pytest tests/test_recognition_worker.py -q` verde en su totalidad.
    - `git status --short backend/events/engine.py backend/pipeline/tracking.py` no lista nada (D-5, D-6: el criterio 3 se cierra sin tocarlos).
  </acceptance_criteria>
  <done>El criterio 3 esta demostrado end-to-end: identidad conservada con `track_id` nuevo y sin cara disponible, 1 solo `PERSON_RECOGNIZED` y 0 `UNKNOWN_PERSON`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Hilo del worker -> estado de la FSM | Un segundo escritor concurrente sobre `_states` obligaria a anadir un lock a `IdentityStateMachine` |
| Decision de herencia -> registro de actividad por persona | Una herencia erronea atribuye la actividad de B a A en SQLite y en las alertas |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-11 | Tampering | Concurrencia sobre `IdentityStateMachine._states` | mitigate | La via ReID corre en `RecognitionWorker._loop`, el mismo unico hilo que ya escribe la FSM; no se crea worker nuevo (RESEARCH §Q3) |
| T-25-12 | Spoofing / Repudiation | Fusion erronea de identidad | mitigate | `reid_inherit=False` por defecto (fail-safe ASVS V14) + log INFO por match con `track_id`/`person_id`/`similarity` + contadores `reid_matches`/`reid_inherited`/`reid_conflicts` en `/api/v2/cameras/{id}/health` |
| T-25-13 | Denial of Service | Coste de inferencia | mitigate | Un track por tick + `TrackGallery.needs_embedding` (criterio 5); medido ~5,5 ms por inferencia, ~11 ms/s a 2 FPS |
| T-25-14 | Denial of Service | Excepcion en la via ReID mata el hilo del worker | mitigate | `_reid_pass` entero envuelto en `try/except` que incrementa `_exceptions` y hace `return` (patron de `_sync_identity`) |
| T-25-15 | Information Disclosure | Log de auditoria | accept | Se loguean `track_id`, `person_id` (entero) y similitud; ningun dato biometrico ni imagen |
</threat_model>

<verification>
```bash
F:/Documentos/IA/Proyecto_Camara/.venv/Scripts/python.exe -m pytest tests/test_recognition_worker.py tests/test_identity_state_machine.py tests/test_track_gallery.py tests/test_architecture.py -q
```
</verification>

<success_criteria>
- Criterio 3 verde end-to-end: `person_id` conservado, 1 `PERSON_RECOGNIZED`, 0 `UNKNOWN_PERSON`.
- Criterio 5 verde: `engine.embed.call_count` acotado por `reid_interval_secs`, no por el tick.
- Modo solo-observacion verificado: calcula y cuenta, no transiciona.
- 4 contadores `reid_*` en `RecognitionWorker.stats`; log INFO por match.
- `self._rate.observe()` no aparece en la via ReID; instrumentacion solo con `stage="reid"`.
- `tracking.py` y `events/engine.py` sin modificar; `test_architecture.py` verde.
</success_criteria>

<output>
Al terminar, crear `.planning/phases/25-re-identificaci-n-de-personas-reid/25-04-SUMMARY.md`.
</output>
