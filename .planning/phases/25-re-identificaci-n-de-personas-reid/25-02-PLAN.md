---
phase: 25-re-identificaci-n-de-personas-reid
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/perception/face/identity.py
  - tests/test_identity_state_machine.py
autonomous: true
requirements: [REID-02, REID-03]

must_haves:
  truths:
    - "Un track UNKNOWN sin evidencia facial que recibe un resultado de ReID coherente con una identidad TEMPORARILY_LOST dentro de lost_ttl pasa a CONFIRMED conservando el person_id"
    - "La transicion devuelta lleva emits=False: no se publica un segundo PERSON_RECOGNIZED ni un UNKNOWN_PERSON intermedio (criterio 3)"
    - "on_reid_result nunca secuestra un track CANDIDATE o CONFIRMED, ni uno que ya tiene votos faciales coincidentes"
    - "on_reid_result no vota en el TemporalVoter: la votacion sigue siendo exclusivamente facial"
    - "Tras heredar por ReID, on_tick() no emite un IDENTITY_LOST espurio para la entrada reclamada"
    - "El track heredado sobrevive al barrido de entradas rancias de on_tick porque last_face_at queda fijado a now"
  artifacts:
    - path: "backend/perception/face/identity.py"
      provides: "IdentityStateMachine.on_reid_result() — segunda via de recuperacion de identidad"
      contains: "def on_reid_result"
    - path: "tests/test_identity_state_machine.py"
      provides: "Cobertura de on_reid_result: herencia, no-interferencia, no-voto, no-IDENTITY_LOST-espurio"
      contains: "TEST_reid"
  key_links:
    - from: "IdentityStateMachine.on_reid_result"
      to: "IdentityStateMachine._claim_lost"
      via: "reutilizacion del unico metodo que borra la entrada TEMPORARILY_LOST de _states"
      pattern: "self\\._claim_lost\\(person_id, now\\)"
    - from: "IdentityStateMachine.on_reid_result"
      to: "TemporalVoter.matched_votes"
      via: "guarda de no-interferencia con la evidencia facial del propio track"
      pattern: "matched_votes\\(track_id\\)"
    - from: "IdentityTransition(emits=False)"
      to: "EventEngine.emit_identity"
      via: "supresion del evento (misma visita) — engine.py ya lo maneja, NO se toca"
      pattern: "emits=False"
---

<objective>
Anadir a `IdentityStateMachine` el metodo publico `on_reid_result()`: la segunda via de
recuperacion de identidad, por apariencia, que actua cuando la cara no esta disponible.

Purpose: el RESEARCH §Q1 demostro trazando el codigo que resolver la herencia **fuera** de la
FSM deja huerfana la entrada `TEMPORARILY_LOST` en `_states`, y 30 s despues `on_tick()` emite un
`IDENTITY_LOST` de una persona que el sistema tiene delante y ya reetiquetada (Pitfall 4). Solo
`_claim_lost()` limpia esa entrada y es privado. La via del worker esta descartada **por el
codigo**, no por gusto. Este metodo es aditivo (D-8): no toca `on_face_result`, ni `_claim_lost`,
ni el `TemporalVoter`.

Output: `on_reid_result()` + tests deterministas con reloj sintetico, sin pipeline y sin ReID real.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/25-re-identificaci-n-de-personas-reid/25-RESEARCH.md
@.planning/phases/25-re-identificaci-n-de-personas-reid/25-PATTERNS.md
@backend/perception/face/identity.py
@tests/test_identity_state_machine.py

<interfaces>
<!-- API real de identity.py tras la Fase 24, verificada por grep en este repo. -->

```python
@dataclass
class IdentityTransition:
    track_id: int
    from_state: IdentityState
    to_state: IdentityState
    person_id: int | None = None
    confidence: float = 0.0
    votes: int = 0
    window: int = 0
    emits: bool = True   # False = cambio de estado silencioso (misma visita)


class TemporalVoter:
    @property
    def window(self) -> int: ...                       # linea 60
    def votes_for(self, track_id: int) -> int: ...     # linea 87
    def matched_votes(self, track_id: int) -> int: ... # linea 92
    def reset(self, track_id: int) -> None: ...
    def prune(self, active_track_ids: set[int]) -> None: ...


@dataclass
class _TrackIdentity:                                  # linea 112
    state: IdentityState = IdentityState.UNKNOWN
    person_id: int | None = None
    confidence: float = 0.0
    last_face_at: float = 0.0
    last_revalidation_at: float = 0.0
    lost_at: float | None = None
    failed_revalidations: int = 0
    recognized_emitted: bool = False
    unknown_emitted: bool = False


class IdentityStateMachine:
    MAX_FAILED_REVALIDATIONS = 3
    def state_of(self, track_id: int) -> IdentityState: ...            # 150
    def identity_of(self, track_id: int) -> tuple[int|None, float]: ...# 154, solo si CONFIRMED
    def _claim_lost(self, person_id: int, now: float) -> bool: ...     # 161-179
    def on_face_result(self, track_id, person_id, score, now): ...     # 181
    def on_track_lost(self, track_id: int, now: float): ...            # 340
    def needs_recognition(self, track_id: int, now: float) -> bool: ...# 381
    def on_active_tracks(self, active_ids: set[int], now: float): ...  # 406
    def on_tick(self, now: float) -> list[IdentityTransition]: ...     # 425
```

`on_tick` (425-457) hace dos cosas: expira `TEMPORARILY_LOST` con
`now - lost_at > lost_ttl` (emitiendo `IDENTITY_LOST`), y borra entradas rancias con
`now - st.last_face_at > stale_ttl`, donde
`stale_ttl = lost_ttl + revalidate_after * MAX_FAILED_REVALIDATIONS`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: IdentityStateMachine.on_reid_result()</name>
  <read_first>
    - `backend/perception/face/identity.py` lineas 1-12 (docstring de modulo: dominio puro, reloj
      inyectado, y la frase final que dice "la re-identificacion por apariencia sin cara visible
      (Fase 25)" como fuera de alcance — hay que actualizarla).
    - `backend/perception/face/identity.py` lineas 150-230 (`state_of`, `identity_of`,
      `_claim_lost` completo y la rama `UNKNOWN` de `on_face_result` con su `emits=False`).
    - `backend/perception/face/identity.py` lineas 381-457 (`needs_recognition`, `on_active_tracks`,
      `on_tick` con las dos guardas y el comentario del "seguro de vida").
    - `.planning/phases/25-re-identificaci-n-de-personas-reid/25-RESEARCH.md` §Q1 (implementacion
      completa de `on_reid_result` — se copia verbatim) y § Pitfall 4 / Pitfall 5.
  </read_first>
  <files>backend/perception/face/identity.py</files>
  <behavior>
    - `on_reid_result(2, 7, 0.85, now)` sobre un track 2 desconocido, con el track 1 en `TEMPORARILY_LOST` con `person_id=7` dentro de `lost_ttl` ⇒ devuelve `IdentityTransition(2, UNKNOWN, CONFIRMED, person_id=7, confidence=0.85, votes=0, emits=False)` y `identity_of(2) == (7, 0.85)`.
    - `on_reid_result(2, None, 0.0, now)` ⇒ `None`, sin efectos.
    - Sin ningun track `TEMPORARILY_LOST` con ese `person_id` ⇒ `None`, y `state_of(2)` sigue `UNKNOWN`.
    - Track ya `CANDIDATE` o `CONFIRMED` ⇒ `None`, estado intacto.
    - Track `UNKNOWN` pero con `matched_votes(track_id) > 0` ⇒ `None` (la cara manda).
    - Tras heredar, `voter.votes_for(track_id) == 0`: `on_reid_result` no vota.
    - Tras heredar, `on_tick(now + lost_ttl + 1)` devuelve `[]` (Pitfall 4) y `state_of(track_id)` sigue `CONFIRMED`.
  </behavior>
  <action>
1. Actualizar el docstring de modulo (`identity.py:1-12`): la ultima frase dice hoy que la
   re-identificacion por apariencia sin cara visible queda "fuera de alcance aqui (Fase 25)".
   Sustituirla por una frase que la incluya, del estilo:
   `"La re-identificacion por apariencia (Fase 25) entra por on_reid_result(), que reutiliza
   _claim_lost() y NO vota en el TemporalVoter: la votacion sigue siendo exclusivamente facial."`

2. Anadir el metodo publico `on_reid_result` **justo despues de `on_face_result`** (para que quede
   junto a la otra via de entrada de evidencia). Copiar la implementacion del RESEARCH §Q1 **tal
   cual, sin rediseñarla**:

```python
    def on_reid_result(
        self, track_id: int, person_id: int | None, similarity: float, now: float
    ) -> IdentityTransition | None:
        """Segunda via de recuperacion de identidad: apariencia, sin cara visible (Fase 25).

        NO vota en el TemporalVoter: la votacion es facial y la Fase 25 no la toca.
        Solo actua sobre tracks sin evidencia facial propia; un track CANDIDATE con
        votacion en curso o CONFIRMED nunca es secuestrado por apariencia.
        """
        if person_id is None:
            return None
        st = self._states.get(track_id)
        if st is not None and st.state is not IdentityState.UNKNOWN:
            return None                       # la cara manda; ReID no interfiere
        if st is not None and self._voter.matched_votes(track_id) > 0:
            return None                       # ya hay evidencia facial de este track
        if not self._claim_lost(person_id, now):
            return None                       # nadie perdido con esa identidad
        st = self._states.setdefault(track_id, _TrackIdentity())
        st.state = IdentityState.CONFIRMED
        st.person_id = person_id
        st.confidence = similarity
        st.failed_revalidations = 0
        st.last_revalidation_at = now
        st.last_face_at = now                 # evita que on_tick lo purgue por rancio
        st.recognized_emitted = True
        return IdentityTransition(
            track_id,
            IdentityState.UNKNOWN,
            IdentityState.CONFIRMED,
            person_id=person_id,
            confidence=similarity,
            votes=0,
            window=self._voter.window,
            emits=False,                      # misma visita: no hay 2o PERSON_RECOGNIZED
        )
```

3. Anadir sobre `st.last_face_at = now` el comentario de Pitfall 5, con la consecuencia deliberada
   escrita (es la palanca que decide cuanto tiempo puede vivir una herencia erronea sin
   corregirse):
   ```
   # Pitfall 5: sin refrescar last_face_at, on_tick borraria este estado por
   # rancio (now - last_face_at > stale_ttl). Consecuencia deliberada: el track
   # heredado por apariencia no revalida con cara hasta revalidate_after (120 s).
   ```

**Prohibiciones explicitas** (verificables por grep en las acceptance criteria):
- NO llamar a `self._voter.vote(...)` dentro de `on_reid_result` — un voto de apariencia
  contaminaria la votacion facial y romperia los parametros medidos de FACE-07.
- NO modificar `on_face_result`, `_claim_lost`, `on_tick`, `on_active_tracks` ni
  `needs_recognition`. El cambio es **aditivo** (D-8).
- NO importar `time` ni nada de `backend/pipeline/` en `identity.py`: sigue siendo dominio puro.
  </action>
  <verify>
    <automated>F:/Documentos/IA/Proyecto_Camara/.venv/Scripts/python.exe -m pytest tests/test_identity_state_machine.py tests/test_architecture.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "def on_reid_result" backend/perception/face/identity.py` devuelve exactamente 1 linea.
    - `grep -n "self._claim_lost(person_id, now)" backend/perception/face/identity.py` devuelve 2 lineas (la de `on_face_result` y la nueva de `on_reid_result`).
    - `grep -n "matched_votes(track_id) > 0" backend/perception/face/identity.py` devuelve al menos 1 linea.
    - `grep -n "last_face_at = now" backend/perception/face/identity.py` devuelve al menos 2 lineas (`on_face_result` y `on_reid_result`).
    - Dentro del cuerpo de `on_reid_result` NO aparece `self._voter.vote(` — comprobar con `sed -n '/def on_reid_result/,/^    def /p' backend/perception/face/identity.py | grep -c "_voter.vote("` que devuelve 0.
    - `git diff --stat backend/perception/face/identity.py` muestra solo inserciones y la modificacion del docstring de modulo; `git diff backend/perception/face/identity.py | grep "^-" | grep -v "^---"` solo contiene lineas del docstring de cabecera.
    - `pytest tests/test_identity_state_machine.py -q` sigue verde (los tests de la Fase 24 no se rompen).
    - `pytest tests/test_architecture.py -q` verde.
  </acceptance_criteria>
  <done>`on_reid_result()` existe, reutiliza `_claim_lost`, devuelve `emits=False`, fija `last_face_at`, no vota, y ningun test previo se rompe.</done>
</task>

<task type="auto">
  <name>Task 2: tests de on_reid_result en tests/test_identity_state_machine.py</name>
  <read_first>
    - `tests/test_identity_state_machine.py` lineas 1-35 (docstring que explica el reloj sintetico
      y el helper `_fsm(window, min_votes, min_ratio, lost_ttl, revalidate_after, low_confidence)`).
    - `tests/test_identity_state_machine.py` lineas 306-350 (`TEST_track_recovery_keeps_same_identity`
      y `TEST_recovery_after_lost_ttl_is_a_new_visit`) — la plantilla exacta de secuencia
      "confirmar → perder → recuperar con id nuevo" con `now=` explicito en cada llamada.
    - `.planning/phases/25-re-identificaci-n-de-personas-reid/25-VALIDATION.md` filas de REID-02
      (nombres de test seleccionables con `-k reid` y `-k reid_no_spurious_identity_lost`).
  </read_first>
  <files>tests/test_identity_state_machine.py</files>
  <action>
Anadir al final del fichero un bloque de tests con separador de seccion en el estilo del repo
(`# ─── Fase 25 — herencia de identidad por apariencia (REID-02/REID-03) ───`), con un comentario
de cabecera que explique **por que** ReID entra por la FSM y no por el worker (el `IDENTITY_LOST`
espurio del Pitfall 4).

Todos los tests reutilizan el helper `_fsm()` ya existente y reloj sintetico (`now=0.0`, `now=1.0`…).
Nombres exactos (deben empezar por `TEST_` y contener `reid` para que `-k reid` los seleccione):

1. `TEST_reid_inherits_identity_from_lost_track()`
   ```python
   fsm = _fsm()
   for t in (0.0, 1.0, 2.0):
       fsm.on_face_result(1, 7, 0.7, now=t)          # track 1 -> CONFIRMED como persona 7
   assert fsm.state_of(1) is IdentityState.CONFIRMED
   fsm.on_track_lost(1, now=10.0)                     # se gira / desaparece
   tr = fsm.on_reid_result(2, 7, 0.85, now=15.0)      # reaparece con track_id NUEVO, sin cara
   assert tr is not None
   assert tr.to_state is IdentityState.CONFIRMED
   assert tr.person_id == 7
   assert tr.emits is False, "criterio 3: misma visita, no un 2o PERSON_RECOGNIZED"
   assert tr.votes == 0
   assert fsm.identity_of(2) == (7, 0.85)
   assert fsm.state_of(1) is IdentityState.UNKNOWN    # la entrada vieja fue reclamada
   ```

2. `TEST_reid_does_not_vote_in_temporal_voter()` — misma preparacion; tras `on_reid_result(2, 7,
   0.9, now=15.0)`, `assert fsm._voter.votes_for(2) == 0` con comentario: un voto de apariencia
   invalidaria los parametros medidos de FACE-07.

3. `TEST_reid_does_not_hijack_confirmed_or_candidate_track()` — dos casos en el mismo test:
   - Track 2 ya `CONFIRMED` como persona 9: `on_reid_result(2, 7, 0.99, now=…)` devuelve `None` y
     `identity_of(2)[0] == 9`.
   - Track 3 en `CANDIDATE` (un solo `on_face_result(3, 5, 0.7, now=…)` con `min_votes=3`):
     `on_reid_result(3, 7, 0.99, now=…)` devuelve `None` y `state_of(3) is IdentityState.CANDIDATE`.

4. `TEST_reid_ignored_when_track_has_face_evidence()` — track 4 en `UNKNOWN` pero con al menos un
   voto facial coincidente (`matched_votes > 0`); `on_reid_result(4, 7, 0.99, now=…)` devuelve
   `None`.

5. `TEST_reid_without_lost_identity_does_nothing()` — sin ningun track `TEMPORARILY_LOST` con ese
   `person_id`: devuelve `None` y `state_of(2) is IdentityState.UNKNOWN`.

6. `TEST_reid_no_spurious_identity_lost()` — **Pitfall 4, el test que justifica todo el diseño**:
   ```python
   fsm = _fsm(lost_ttl=30.0)
   for t in (0.0, 1.0, 2.0):
       fsm.on_face_result(1, 7, 0.7, now=t)
   fsm.on_track_lost(1, now=10.0)
   assert fsm.on_reid_result(2, 7, 0.85, now=15.0) is not None
   transitions = fsm.on_tick(now=10.0 + 30.0 + 1.0)   # lost_ttl + epsilon
   assert transitions == [], (
       "on_tick emitio una transicion tras heredar por ReID: la entrada "
       "TEMPORARILY_LOST quedo huerfana y produciria un IDENTITY_LOST espurio "
       "para alguien que esta delante de la camara (Pitfall 4)"
   )
   assert fsm.state_of(2) is IdentityState.CONFIRMED
   ```

7. `TEST_reid_inherited_track_survives_stale_sweep()` — Pitfall 5: tras heredar en `now=15.0`,
   ejecutar `on_tick(now=15.0 + 1.0)` y comprobar que `state_of(2)` sigue `CONFIRMED` (es decir,
   `last_face_at` se fijo y la entrada no se barrio por rancia).

Todos los asserts con mensaje explicativo cuando la causa del fallo no sea obvia, como en el resto
del fichero.
  </action>
  <verify>
    <automated>F:/Documentos/IA/Proyecto_Camara/.venv/Scripts/python.exe -m pytest tests/test_identity_state_machine.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_identity_state_machine.py -k reid -q` selecciona 7 tests y todos pasan (no "no tests ran").
    - `pytest tests/test_identity_state_machine.py -k reid_no_spurious_identity_lost -q` selecciona exactamente 1 test y pasa.
    - `pytest tests/test_identity_state_machine.py -q` verde en su totalidad (los tests de la Fase 24 incluidos).
    - `grep -c "def TEST_reid" tests/test_identity_state_machine.py` devuelve 7.
    - `grep -n "emits is False" tests/test_identity_state_machine.py` devuelve al menos 1 linea.
    - `grep -n "votes_for(2) == 0" tests/test_identity_state_machine.py` devuelve al menos 1 linea.
    - `grep -n "transitions == \[\]" tests/test_identity_state_machine.py` devuelve al menos 1 linea.
    - `grep -n "import time\|MagicMock\|np.random" tests/test_identity_state_machine.py` no devuelve nada en las lineas nuevas (dominio puro, reloj sintetico).
  </acceptance_criteria>
  <done>7 tests `TEST_reid*` verdes cubriendo herencia, no-voto, no-interferencia, ausencia de identidad perdida, `IDENTITY_LOST` espurio y barrido de rancios.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Evidencia de apariencia -> estado de identidad | Un vector de ropa/silueta puede cambiar a que persona se atribuye la actividad registrada |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-05 | Spoofing | `on_reid_result` | mitigate | La guarda `st.state is not IdentityState.UNKNOWN` + `matched_votes(track_id) > 0` impide que la apariencia secuestre un track con evidencia facial: la cara siempre manda |
| T-25-06 | Repudiation | Herencia erronea de identidad | mitigate | La herencia solo puede reclamar una identidad ya `TEMPORARILY_LOST` dentro de `lost_ttl` (via `_claim_lost`), nunca inventar una identidad nueva |
| T-25-07 | Tampering | Contaminacion de la votacion facial | mitigate | `on_reid_result` no llama a `_voter.vote()`; verificado por grep en las acceptance criteria |
</threat_model>

<verification>
```bash
F:/Documentos/IA/Proyecto_Camara/.venv/Scripts/python.exe -m pytest tests/test_identity_state_machine.py -q
F:/Documentos/IA/Proyecto_Camara/.venv/Scripts/python.exe -m pytest tests/test_architecture.py -q
git diff --stat backend/perception/face/identity.py
```
</verification>

<success_criteria>
- `IdentityStateMachine.on_reid_result()` existe, es aditivo y reutiliza `_claim_lost()`.
- La transicion devuelta lleva `emits=False`, `votes=0` y `confidence=similarity`.
- ReID no vota en el `TemporalVoter` ni interfiere con tracks `CANDIDATE`/`CONFIRMED` ni con
  tracks que ya tienen votos faciales coincidentes.
- `on_tick()` no emite `IDENTITY_LOST` espurio tras una herencia por ReID.
- 7 tests `TEST_reid*` verdes; `tests/test_identity_state_machine.py` y
  `tests/test_architecture.py` completos en verde.
</success_criteria>

<output>
Al terminar, crear `.planning/phases/25-re-identificaci-n-de-personas-reid/25-02-SUMMARY.md`.
</output>
