---
phase: 25-re-identificaci-n-de-personas-reid
plan: 05
type: execute
wave: 4
depends_on: ["25-04"]
files_modified:
  - backend/config.py
  - tests/test_config.py
  - backend/pipeline/manager.py
  - backend/main.py
  - tests/test_recognition_worker.py
autonomous: true
requirements: [REID-01, REID-02, REID-03, REID-04]

must_haves:
  truths:
    - "Settings() expone los 7 parametros reid_* con los defaults locked de CONTEXT, incluido reid_inherit_identity=False"
    - "Un reid_similarity_threshold, reid_inherit_window_secs o reid_interval_secs fuera de rango es rechazado al construir Settings"
    - "reid_model_path solo admite extensiones permitidas y rutas contenidas dentro del proyecto (SEC-16)"
    - "CameraPipeline construye ReIDEngine y TrackGallery FUERA de la factoria _make_recognition: un reinicio del worker no vacia la galeria ni pierde la identidad"
    - "main.py pasa los settings reid_* a camera_manager.add() con el mapeo explicito kwarg <- setting"
    - "Sin models/reid/ el sistema arranca exactamente igual que hoy (ReIDEngine.available=False, ReID no-op)"
  artifacts:
    - path: "backend/config.py"
      provides: "7 parametros reid_* + validador de ruta de modelo + validador de rangos"
      contains: "reid_inherit_identity"
    - path: "backend/pipeline/manager.py"
      provides: "Construccion y propiedad de ReIDEngine/TrackGallery fuera del supervisor + kwargs reid_*"
      contains: "self.reid_gallery"
    - path: "backend/main.py"
      provides: "Paso de settings.reid_* al pipeline"
      contains: "reid_inherit="
    - path: "tests/test_config.py"
      provides: "Defaults y validadores de reid_*"
      contains: "TEST_reid"
  key_links:
    - from: "backend/main.py"
      to: "CameraPipeline.__init__"
      via: "kwargs reid_* de camera_manager.add()"
      pattern: "reid_similarity_threshold=settings\\."
    - from: "CameraPipeline.__init__"
      to: "RecognitionWorker"
      via: "reid_engine / reid_gallery / reid_inherit inyectados dentro de _make_recognition"
      pattern: "reid_gallery=self\\.reid_gallery"
    - from: "backend/config.py"
      to: "_PROJECT_ROOT"
      via: "contencion de reid_model_path dentro del proyecto (SEC-16)"
      pattern: "is_relative_to\\(_PROJECT_ROOT\\)"
---

<objective>
Poner la via ReID bajo configuracion y cablearla en el arranque real: 7 parametros en
`backend/config.py`, construccion de motor y galeria en `CameraPipeline` fuera de la factoria del
supervisor, y paso de settings en `backend/main.py`.

Purpose: hasta aqui la via ReID existe y esta testeada, pero nadie la construye en produccion.
Este plan cierra el cableado y deja `reid_inherit_identity=False` como fail-safe: sin decision
explicita del operador, ReID calcula y registra pero no altera identidades.

Output: `config.py`, `manager.py`, `main.py` y sus tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/25-re-identificaci-n-de-personas-reid/25-RESEARCH.md
@.planning/phases/25-re-identificaci-n-de-personas-reid/25-PATTERNS.md
@.planning/phases/25-re-identificaci-n-de-personas-reid/25-04-SUMMARY.md
@backend/config.py
@backend/pipeline/manager.py

<interfaces>
<!-- Codigo real leido de este repo. -->

`backend/config.py` (cabecera, lineas 1-13):
```python
from pydantic import field_validator, model_validator
from pydantic_settings import BaseSettings

_MODEL_PATH_ALLOWED_SUFFIXES = {".pt", ".onnx"}     # .onnx YA incluido
_PROJECT_ROOT = Path(__file__).resolve().parent.parent
```

Bloque de la Fase 24 (config.py:137-147) — el analog exacto del bloque nuevo:
```python
    # --- Identidad temporal (Fase 24 — FACE-07..FACE-11) ---
    identity_vote_window: int = 8
    identity_min_votes: int = 3
    identity_min_ratio: float = 0.6
    identity_lost_ttl_secs: float = 30.0
    identity_revalidate_after_secs: float = 120.0
```

`validate_identity_params` (config.py:183-199) — analog del validador de rangos, con mensajes en
espanol que explican **que pasaria** si el valor se aceptara.
`validate_yolo_model_path` (config.py:39-51) — analog del validador de ruta, reutilizable casi
literalmente.

`CameraPipeline.__init__` (manager.py:40-62), ultimos kwargs:
```python
        identity_lost_ttl: float = 30.0,
        identity_revalidate_after: float = 120.0,
        identity_low_confidence: float = 0.55,
    ) -> None:
```

Bloque de la FSM fuera de la factoria (manager.py:122-149) — el analog literal, con el comentario
que explica **por que** vive fuera de `_make_recognition`.

`CameraManager.add` (manager.py:271) es `def add(self, camera_id, rtsp_url, **kwargs)` -> los
kwargs llegan intactos a `CameraPipeline`.

`main.py:413-437` — el bloque de kwargs de `camera_manager.add()`, que hoy termina en
`identity_low_confidence=settings.face_confirm_threshold,`. Ojo: el nombre del kwarg **no siempre
coincide** con el del setting (`identity_lost_ttl` <- `identity_lost_ttl_secs`,
`identity_low_confidence` <- `face_confirm_threshold`).

Parametros locked (CONTEXT §Parametros por defecto + RESEARCH § Code Examples):
```python
reid_enabled: bool = True
reid_model_path: str = "models/reid/osnet_x0_25_msmt17_dyn.onnx"
reid_inherit_window_secs: float = 15.0
reid_similarity_threshold: float = 0.7
reid_interval_secs: float = 2.0
reid_inherit_identity: bool = False      # REID_INHERIT_IDENTITY
reid_max_gallery_entries: int = 256
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 7 parametros reid_* + validadores en backend/config.py</name>
  <read_first>
    - `backend/config.py` lineas 1-20 (imports, `_MODEL_PATH_ALLOWED_SUFFIXES`, `_PROJECT_ROOT`),
      39-51 (`validate_yolo_model_path` completo), 128-147 (bloques de la Fase 23 y 24 con su
      comentario de cabecera) y 183-199 (`validate_identity_params`).
    - `tests/test_config.py` lineas 182-217 (`TEST_identity_defaults_match_spec` y
      `TEST_identity_ratio_out_of_range_rejected`) — el estilo: comentario de cabecera que explica
      la consecuencia de aceptar el valor malo, y `pytest.raises((ValidationError, ValueError))`.
    - `.planning/phases/25-re-identificaci-n-de-personas-reid/25-RESEARCH.md` § "Parametros de
      configuracion (patron de la Fase 24)" y § Security Domain (fila V5).
  </read_first>
  <files>backend/config.py, tests/test_config.py</files>
  <behavior>
    - `Settings()` devuelve los 7 defaults exactos, con `reid_inherit_identity is False`.
    - `Settings(reid_similarity_threshold=0.0)` y `Settings(reid_similarity_threshold=1.5)` lanzan.
    - `Settings(reid_inherit_window_secs=0)` y `Settings(reid_interval_secs=-1)` lanzan.
    - `Settings(reid_max_gallery_entries=0)` lanza.
    - `Settings(reid_model_path="models/reid/x.txt")` lanza (extension no permitida).
    - `Settings(reid_model_path="../../etc/passwd.onnx")` lanza (fuera de `_PROJECT_ROOT`, SEC-16).
    - `Settings(reid_model_path="models/reid/otro.onnx")` NO lanza (el fichero no tiene por que existir: la ausencia la maneja `ReIDEngine.available=False`).
  </behavior>
  <action>
**1. Bloque de parametros** — insertar justo despues del bloque `# --- Identidad temporal
(Fase 24 — FACE-07..FACE-11) ---` (config.py:147), con el comentario de cabecera en el estilo del
fichero, explicando de donde salen los defaults y la interaccion no obvia entre ellos:

```python
    # --- Re-identificacion por apariencia (Fase 25 — REID-01..REID-04) ---
    # Defaults de SPEC_v2.md §5.6 / ADR-04. reid_inherit_window_secs es MAS CORTA
    # que identity_lost_ttl_secs (30 s) a proposito: la apariencia es menos fiable
    # que la votacion facial y debe caducar antes. reid_inherit_identity arranca en
    # False (modo solo-observacion): ReID calcula y registra la decision de herencia
    # sin aplicarla, para poder auditar la tasa de falsos positivos con datos reales
    # antes de activarla. El modelo lo produce scripts/fetch_models.py; si falta,
    # ReIDEngine.available queda a False y la via ReID es no-op.
    reid_enabled: bool = True
    reid_model_path: str = "models/reid/osnet_x0_25_msmt17_dyn.onnx"
    reid_inherit_window_secs: float = 15.0
    reid_similarity_threshold: float = 0.7
    reid_interval_secs: float = 2.0
    reid_inherit_identity: bool = False
    reid_max_gallery_entries: int = 256
```

**2. Validador de ruta** — copiar `validate_yolo_model_path` (config.py:39-51) adaptando el nombre
del campo y los mensajes. `_MODEL_PATH_ALLOWED_SUFFIXES` ya incluye `.onnx`, no hay que tocarlo:

```python
    @field_validator("reid_model_path")
    @classmethod
    def validate_reid_model_path(cls, v: str) -> str:
        p = Path(v)
        if p.suffix.lower() not in _MODEL_PATH_ALLOWED_SUFFIXES:
            raise ValueError(
                f"reid_model_path extension {p.suffix!r} not allowed. "
                f"Allowed: {sorted(_MODEL_PATH_ALLOWED_SUFFIXES)}"
            )
        resolved = p.resolve() if p.is_absolute() else (_PROJECT_ROOT / p).resolve()
        if not resolved.is_relative_to(_PROJECT_ROOT):
            raise ValueError(f"reid_model_path must be inside the project directory: {resolved}")
        return v
```
**No** comprobar que el fichero exista: la ausencia del modelo es un caso soportado
(`ReIDEngine.available=False`), no un error de configuracion.

**3. Validador de rangos** — `@model_validator(mode="after")` nuevo llamado `validate_reid_params`,
al lado de `validate_identity_params`, con mensajes que digan **que pasaria** si el valor se
aceptara (estilo del fichero):

```python
    @model_validator(mode="after")
    def validate_reid_params(self) -> "Settings":
        if not 0.0 < self.reid_similarity_threshold <= 1.0:
            raise ValueError(
                "reid_similarity_threshold debe estar en (0, 1]: es un coseno entre "
                "embeddings normalizados; 0.0 heredaria identidad de cualquier "
                "apariencia y valores > 1.0 no heredarian nunca"
            )
        if self.reid_inherit_window_secs <= 0:
            raise ValueError("reid_inherit_window_secs debe ser > 0")
        if self.reid_interval_secs <= 0:
            raise ValueError(
                "reid_interval_secs debe ser > 0: es el minimo entre inferencias "
                "ReID de un mismo track (criterio 5); 0 dejaria correr ReID en cada tick"
            )
        if self.reid_max_gallery_entries < 1:
            raise ValueError("reid_max_gallery_entries debe ser >= 1")
        return self
```
No fusionar con `validate_identity_params`: son bloques independientes y el fichero ya separa un
validador por familia de parametros.

**4. Tests en `tests/test_config.py`** — anadir al final, con separadores de seccion en el estilo
del fichero (comentario que explica la consecuencia de aceptar el valor malo):
- `TEST_reid_defaults_match_spec()` — los 7 valores exactos, con assert explicito de
  `s.reid_inherit_identity is False` y mensaje citando que el modo solo-observacion es el fail-safe
  de la fase (no una omision).
- `TEST_reid_similarity_threshold_out_of_range_rejected()` — `0.0` y `1.5` lanzan.
- `TEST_reid_time_params_must_be_positive()` — `reid_inherit_window_secs=0`,
  `reid_interval_secs=-1` y `reid_max_gallery_entries=0` lanzan.
- `TEST_reid_model_path_rejects_bad_extension_and_traversal()` — `"models/reid/x.txt"` lanza y
  `"../../etc/passwd.onnx"` lanza (SEC-16); `"models/reid/otro.onnx"` NO lanza.
  </action>
  <verify>
    <automated>F:/Documentos/IA/Proyecto_Camara/.venv/Scripts/python.exe -m pytest tests/test_config.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_config.py -k reid -q` selecciona 4 tests y todos pasan.
    - `pytest tests/test_config.py -q` verde en su totalidad.
    - `grep -c "^    reid_" backend/config.py` devuelve 7.
    - `grep -n "def validate_reid_model_path\|def validate_reid_params" backend/config.py` devuelve 2 lineas.
    - `grep -n "is_relative_to(_PROJECT_ROOT)" backend/config.py` devuelve 2 lineas (yolo + reid).
    - `F:/Documentos/IA/Proyecto_Camara/.venv/Scripts/python.exe -c "from backend.config import Settings; s=Settings(); print(s.reid_enabled, s.reid_model_path, s.reid_inherit_window_secs, s.reid_similarity_threshold, s.reid_interval_secs, s.reid_inherit_identity, s.reid_max_gallery_entries)"` imprime `True models/reid/osnet_x0_25_msmt17_dyn.onnx 15.0 0.7 2.0 False 256`.
    - `grep -n "exists()\|is_file()" backend/config.py` no aparece dentro de `validate_reid_model_path`.
  </acceptance_criteria>
  <done>Los 7 parametros existen con los defaults locked, la ruta del modelo esta validada por extension y contencion, y los rangos rechazan valores que romperian los criterios 2 y 5.</done>
</task>

<task type="auto">
  <name>Task 2: CameraPipeline construye ReIDEngine y TrackGallery fuera de la factoria</name>
  <read_first>
    - `backend/pipeline/manager.py` lineas 36-62 (firma de `CameraPipeline.__init__`), 78-82
      (declaracion previa de atributos `self.detection/streaming/recording/recognition/identity_fsm`),
      118-150 (bloque `if recognizer is not None ...` con la FSM fuera de la factoria y el
      comentario que explica por que) y 250-275 (`stats()` y `CameraManager.add`).
    - `tests/test_recognition_worker.py` lineas 296-316 (`TEST_fsm_survives_worker_restart`) — la
      plantilla exacta, incluida la tecnica de sacar la factoria del supervisor
      (`pipeline.supervisor._entries["recognition"].factory`).
    - `.planning/phases/25-re-identificaci-n-de-personas-reid/25-PATTERNS.md` § 6.
  </read_first>
  <files>backend/pipeline/manager.py, tests/test_recognition_worker.py</files>
  <action>
**1. Firma de `CameraPipeline.__init__`** — anadir los kwargs `reid_*` al final, despues de
`identity_low_confidence`, con los mismos defaults que `config.py`:

```python
        identity_low_confidence: float = 0.55,
        reid_enabled: bool = True,
        reid_model_path: str = "models/reid/osnet_x0_25_msmt17_dyn.onnx",
        reid_inherit_window: float = 15.0,
        reid_similarity_threshold: float = 0.7,
        reid_interval: float = 2.0,
        reid_inherit: bool = False,
        reid_max_gallery_entries: int = 256,
    ) -> None:
```

**2. Declaracion previa de atributos** (manager.py:78-82) — dos lineas nuevas junto a
`self.identity_fsm`:
```python
        self.identity_fsm: IdentityStateMachine | None = None
        self.reid_engine: "ReIDEngine | None" = None
        self.reid_gallery: "TrackGallery | None" = None
```

**3. Construccion dentro del `if recognizer is not None and getattr(recognizer, "available", False):`**
— junto a `self.identity_fsm`, **fuera** de `_make_recognition`, con el comentario que explica el
motivo (mismo que el de la FSM):

```python
            if reid_enabled:
                # Motor y galeria viven FUERA de la factoria por el mismo motivo que
                # la FSM: el WorkerSupervisor re-ejecuta la factoria en cada reinicio
                # del worker, y construirlos dentro vaciaria la galeria de apariencia
                # (perdiendo la continuidad de identidad justo tras un reinicio) y
                # recargaria el ONNX cada vez.
                self.reid_engine = ReIDEngine(reid_model_path)
                self.reid_gallery = TrackGallery(
                    inherit_window=reid_inherit_window,
                    similarity_threshold=reid_similarity_threshold,
                    interval=reid_interval,
                    max_entries=reid_max_gallery_entries,
                )
```

Los imports de `ReIDEngine` y `TrackGallery` van arriba, junto a los de
`IdentityStateMachine`/`TemporalVoter`. Si `ReIDEngine` no puede cargar el modelo, su propio
`try/except` deja `available=False` y `_reid_pass` sale inmediatamente — el sistema arranca igual
que hoy.

**4. Inyeccion en la factoria** — dentro de `_make_recognition`, tres kwargs mas:
```python
                    identity_fsm=self.identity_fsm,
                    event_engine=event_engine,
                    on_identified=on_identified,
                    reid_engine=self.reid_engine,
                    reid_gallery=self.reid_gallery,
                    reid_inherit=reid_inherit,
                )
```

**5. `stats()`** — no requiere cambios: `out["recognition"] = self.recognition.stats` ya arrastra
los contadores `reid_*` a `/api/v2/cameras/{id}/health`. Verificarlo, no reescribirlo.

**6. Test** — anadir en `tests/test_recognition_worker.py`, copiando
`TEST_fsm_survives_worker_restart` (lineas 296-316):

```python
def TEST_reid_engine_and_gallery_survive_worker_restart():
    """Motor y galeria se construyen FUERA de _make_recognition (manager.py): un
    reinicio del worker no debe vaciar la galeria de apariencia, igual que no
    debe perder la identidad ya confirmada."""
    from backend.pipeline.manager import CameraPipeline

    recognizer = MagicMock()
    recognizer.available = True
    pipeline = CameraPipeline("cam1", "rtsp://fake", recognizer=recognizer)

    factory = pipeline.supervisor._entries["recognition"].factory
    worker1 = factory()
    gallery = pipeline.reid_gallery
    assert gallery is not None
    assert worker1._gallery is gallery

    worker2 = factory()
    assert worker2 is not worker1
    assert pipeline.reid_gallery is gallery      # misma instancia, no una nueva
    assert worker2._gallery is gallery
```

Anadir tambien `TEST_reid_disabled_leaves_worker_without_gallery()`: construir
`CameraPipeline(..., reid_enabled=False)` y comprobar que `pipeline.reid_gallery is None` y que la
factoria produce un worker funcional (la via ReID queda no-op).
  </action>
  <verify>
    <automated>F:/Documentos/IA/Proyecto_Camara/.venv/Scripts/python.exe -m pytest tests/test_recognition_worker.py tests/test_workers_isolation.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_recognition_worker.py -k survive_worker_restart -q` selecciona 2 tests (el de la FSM y el nuevo de ReID) y ambos pasan.
    - `pytest tests/test_recognition_worker.py -k reid_disabled -q` selecciona 1 test y pasa.
    - `grep -n "self.reid_engine\|self.reid_gallery" backend/pipeline/manager.py` devuelve al menos 5 lineas (declaracion, construccion, inyeccion).
    - `sed -n '/def _make_recognition/,/supervisor.register("recognition"/p' backend/pipeline/manager.py | grep -c "ReIDEngine(\|TrackGallery("` devuelve 0 (se construyen FUERA de la factoria).
    - `grep -c "reid_" backend/pipeline/manager.py` devuelve al menos 14 (7 kwargs + declaraciones + inyecciones).
    - La suite completa de pipeline sigue verde: `pytest tests/ -q -k "manager or recognition or supervisor"`.
  </acceptance_criteria>
  <done>`CameraPipeline` construye motor y galeria una sola vez, fuera del supervisor, y los inyecta en cada reinicio del worker; con `reid_enabled=False` el pipeline queda como en la Fase 24.</done>
</task>

<task type="auto">
  <name>Task 3: backend/main.py pasa settings.reid_* al pipeline</name>
  <read_first>
    - `backend/main.py` lineas 408-440 (el bloque completo de kwargs de `camera_manager.add()`,
      terminando en `identity_low_confidence=settings.face_confirm_threshold,`).
    - `.planning/phases/25-re-identificaci-n-de-personas-reid/25-PATTERNS.md` § 7 (la nota de que
      el nombre del kwarg no siempre coincide con el del setting: el plan debe fijar el mapeo).
    - `backend/config.py` (bloque `reid_*` recien anadido en la Task 1) — nombres exactos.
  </read_first>
  <files>backend/main.py</files>
  <action>
Anadir al bloque de kwargs de `camera_manager.add()` (main.py:413-437), justo despues de
`identity_low_confidence=settings.face_confirm_threshold,`, las 7 lineas con el **mapeo explicito**
kwarg de `CameraPipeline` <- setting de `Settings` (no coinciden todos los nombres, igual que pasa
con `identity_lost_ttl` <- `identity_lost_ttl_secs`):

```python
        identity_low_confidence=settings.face_confirm_threshold,
        reid_enabled=settings.reid_enabled,
        reid_model_path=settings.reid_model_path,
        reid_inherit_window=settings.reid_inherit_window_secs,
        reid_similarity_threshold=settings.reid_similarity_threshold,
        reid_interval=settings.reid_interval_secs,
        reid_inherit=settings.reid_inherit_identity,
        reid_max_gallery_entries=settings.reid_max_gallery_entries,
    )
```

Mapeo de referencia (fijarlo, no improvisarlo):

| kwarg de `CameraPipeline` | setting de `Settings` |
|---|---|
| `reid_enabled` | `reid_enabled` |
| `reid_model_path` | `reid_model_path` |
| `reid_inherit_window` | `reid_inherit_window_secs` |
| `reid_similarity_threshold` | `reid_similarity_threshold` |
| `reid_interval` | `reid_interval_secs` |
| `reid_inherit` | `reid_inherit_identity` |
| `reid_max_gallery_entries` | `reid_max_gallery_entries` |

No tocar nada mas de `main.py`: no hay endpoints nuevos (los contadores `reid_*` salen ya por
`/api/v2/cameras/{id}/health` a traves de `manager.stats()`), no hay tarea de arranque nueva y no
se descarga nada en el arranque.

Verificar despues que la app importa y arranca sin el modelo presente (degradacion graciosa).
  </action>
  <verify>
    <automated>F:/Documentos/IA/Proyecto_Camara/.venv/Scripts/python.exe -c "import backend.main as m; print('import OK')" &amp;&amp; F:/Documentos/IA/Proyecto_Camara/.venv/Scripts/python.exe -m pytest tests/test_architecture.py tests/test_stream.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "reid_" backend/main.py` devuelve 7.
    - `grep -n "reid_inherit=settings.reid_inherit_identity" backend/main.py` devuelve 1 linea.
    - `grep -n "reid_inherit_window=settings.reid_inherit_window_secs" backend/main.py` devuelve 1 linea.
    - `grep -n "reid_interval=settings.reid_interval_secs" backend/main.py` devuelve 1 linea.
    - `F:/Documentos/IA/Proyecto_Camara/.venv/Scripts/python.exe -c "import backend.main"` sale con codigo 0.
    - Con el modelo temporalmente renombrado, el mismo import sigue saliendo con codigo 0 (degradacion graciosa).
    - `pytest tests/test_architecture.py tests/test_stream.py -q` verde.
  </acceptance_criteria>
  <done>`main.py` propaga los 7 settings con el mapeo fijado y la app importa con y sin el fichero de modelo presente.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `.env` / variables de entorno -> `Settings` | `REID_MODEL_PATH` y `REID_INHERIT_IDENTITY` son entrada externa que decide que fichero se ejecuta y si se alteran identidades |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-16 | Tampering | `REID_MODEL_PATH` (path traversal) | mitigate | `validate_reid_model_path`: extension en `_MODEL_PATH_ALLOWED_SUFFIXES` + `resolved.is_relative_to(_PROJECT_ROOT)` (SEC-16), test explicito con `../../etc/passwd.onnx` |
| T-25-17 | Elevation of Privilege | `REID_INHERIT_IDENTITY` | mitigate | Default `False` (fail-safe, ASVS V14): sin decision explicita del operador, ReID no altera identidades |
| T-25-18 | Denial of Service | Umbrales absurdos por configuracion | mitigate | `validate_reid_params` rechaza `reid_interval_secs <= 0` (ReID en cada tick) y `reid_similarity_threshold` fuera de (0, 1] |
| T-25-19 | Denial of Service | Carga del ONNX en el arranque del servidor | accept | La carga es local y de 907 KB; si falla, `available=False` y el arranque continua (verificado en las acceptance criteria renombrando el modelo) |
</threat_model>

<verification>
```bash
F:/Documentos/IA/Proyecto_Camara/.venv/Scripts/python.exe -m pytest tests/test_config.py tests/test_recognition_worker.py tests/test_stream.py tests/test_architecture.py -q
F:/Documentos/IA/Proyecto_Camara/.venv/Scripts/python.exe -c "import backend.main; print('OK')"
```
</verification>

<success_criteria>
- 7 parametros `reid_*` con defaults locked y validadores de rango + de ruta (SEC-16).
- `ReIDEngine`/`TrackGallery` construidos una sola vez, fuera de la factoria del supervisor, y
  sobreviven a un reinicio del worker.
- `reid_enabled=False` deja el pipeline exactamente como en la Fase 24.
- `main.py` propaga los 7 settings con el mapeo explicito.
- La app importa con y sin el modelo presente.
- `test_config.py`, `test_recognition_worker.py`, `test_stream.py` y `test_architecture.py` verdes.
</success_criteria>

<output>
Al terminar, crear `.planning/phases/25-re-identificaci-n-de-personas-reid/25-05-SUMMARY.md`.
</output>
