---
phase: 25-re-identificaci-n-de-personas-reid
plan: 06
type: execute
wave: 5
depends_on: ["25-05"]
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
  - .planning/ROADMAP.md
  - .planning/phases/25-re-identificaci-n-de-personas-reid/25-06-SUMMARY.md
autonomous: false
requirements: [REID-01, REID-02, REID-03, REID-04]

must_haves:
  truths:
    - "La suite completa pasa (>= 377 tests base + los nuevos de la fase), sin tests saltados por ausencia del modelo"
    - "Cada uno de los 5 criterios de exito del ROADMAP tiene un comando pytest -k que lo selecciona y pasa, documentado en el SUMMARY"
    - "REID-01..REID-04 quedan marcados [x] en REQUIREMENTS.md"
    - "STATE.md refleja la Fase 25 como completa y el criterio 4 como checkpoint manual pendiente"
    - "La tasa de falsos positivos del criterio 4 tiene un procedimiento de medida documentado con camara real en modo solo-observacion"
  artifacts:
    - path: ".planning/phases/25-re-identificaci-n-de-personas-reid/25-06-SUMMARY.md"
      provides: "Trazabilidad criterio -> comando -> test para los 5 criterios del ROADMAP"
      min_lines: 60
    - path: ".planning/REQUIREMENTS.md"
      contains: "[x] **REID-04**"
    - path: ".planning/STATE.md"
      provides: "Estado real de la Fase 25 y checkpoint abierto del criterio 4"
  key_links:
    - from: "ROADMAP § Phase 25 criterios 1-5"
      to: "comandos pytest -k"
      via: "tabla de trazabilidad del SUMMARY, cada comando con codigo de salida 0 y sin 'no tests ran'"
      pattern: "pytest .*-k"
---

<objective>
Puerta de fase: verificar que los 5 criterios de exito del ROADMAP estan realmente demostrados por
tests que pasan, cerrar REID-01..REID-04, y abrir formalmente el checkpoint manual del criterio 4
(tasa de falsos positivos con personas reales).

Purpose: la Fase 24 cerro con este mismo formato (`24-06-SUMMARY.md`) y funciono — cada criterio
con su comando `pytest -k` que lo selecciona y pasa, no una afirmacion en prosa. El criterio 4 es
el unico que no se puede cerrar con codigo: el research midio que contenido no relacionado ya roza
0,71 de coseno, y nadie ha medido todavia dos personas reales de esta camara. Ese dato exige
camara real, y por eso `reid_inherit_identity=False` es el default de la fase.

Output: `25-06-SUMMARY.md` con la trazabilidad, `REQUIREMENTS.md`/`STATE.md`/`ROADMAP.md`
actualizados, y el checkpoint del criterio 4 registrado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/25-re-identificaci-n-de-personas-reid/25-VALIDATION.md
@.planning/phases/24-identidad-temporal-votaci-n-y-m-quina-de-estados/24-06-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: suite completa + trazabilidad criterio a criterio</name>
  <read_first>
    - `.planning/phases/24-identidad-temporal-votaci-n-y-m-quina-de-estados/24-06-SUMMARY.md`
      (fichero completo) — el formato exacto de la tabla criterio -> comando -> resultado que la
      Fase 24 uso para cerrar, incluida la forma de documentar el escenario del criterio medido.
    - `.planning/ROADMAP.md` § Phase 25 (los 5 criterios verbatim).
    - `.planning/phases/25-re-identificaci-n-de-personas-reid/25-VALIDATION.md` § Per-Task
      Verification Map (el mapa requisito -> comando ya escrito).
    - Los 5 SUMMARY previos de la fase (`25-01`..`25-05`).
  </read_first>
  <files>.planning/phases/25-re-identificaci-n-de-personas-reid/25-06-SUMMARY.md, .planning/REQUIREMENTS.md, .planning/STATE.md, .planning/ROADMAP.md</files>
  <action>
**1. Suite completa.** Ejecutar
`F:/Documentos/IA/Proyecto_Camara/.venv/Scripts/python.exe -m pytest tests/ -q` (unos 90 s) y
anotar la cifra exacta. Base de la Fase 24: 377/377. Si algo falla, **corregirlo, no ocultarlo**
(CLAUDE.md § Tests). Verificar tambien que `tests/test_reid_engine.py` **no** sale como `skipped`:
si sale, ejecutar `scripts/fetch_models.py` y repetir — un skip aqui significaria que el criterio 1
no esta realmente demostrado.

**2. Trazabilidad de los 5 criterios.** Ejecutar uno por uno estos comandos y registrar codigo de
salida y numero de tests seleccionados. Ninguno puede decir "no tests ran":

| # | Criterio del ROADMAP | Comando |
|---|---|---|
| 1 | `ReIDEngine` produce embeddings 512D con osnet_x0_25 ONNX en menos de 20 ms por crop en CPU | `pytest tests/test_reid_engine.py -k "embedding_is_512d_l2_normalized or latency_under_20ms" -q` |
| 2 | `TrackGallery` hereda de un track cerrado hace < 15 s con similitud > 0.7, y no hereda si hay conflicto con un track activo | `pytest tests/test_track_gallery.py -k "inherit or conflict" -q` |
| 3 | Una persona identificada que se gira de espaldas 10 s y vuelve conserva su `person_id` sin `UNKNOWN_PERSON` intermedio | `pytest tests/test_recognition_worker.py -k reid_recovers_identity_without_face -q` |
| 4 | Dos personas distintas con ropa similar no se fusionan; tasa de falsos positivos documentada | `pytest tests/test_track_gallery.py -k does_not_merge -q` **+ checkpoint manual (Task 2)** |
| 5 | ReID corre como maximo 1 vez cada 2 s por track | `pytest tests/test_track_gallery.py -k needs_embedding_respects_interval -q` y `pytest tests/test_recognition_worker.py -k reid_inference_budget -q` |

Anadir tambien la fila de regresion:
`pytest tests/test_architecture.py tests/test_memory_bounds.py tests/test_identity_state_machine.py -q`.

**3. `25-06-SUMMARY.md`.** Escribirlo con el formato de `24-06-SUMMARY.md`, incluyendo:
- La tabla criterio -> comando -> tests seleccionados -> resultado.
- La cifra real de la suite completa (antes/despues de la fase).
- Los numeros medidos del criterio 1 en **este** equipo (p50 de `embed()`), no los del research —
  el research midio en un scratchpad, aqui se mide dentro del repo.
- El escenario exacto del criterio 3 (que se mockeo, que fue real) y el del criterio 5 (intervalo
  usado en el test, escalado del reloj, cota superior admitida y por que).
- Las **consecuencias abiertas** que la fase deja, con la misma honestidad que `24-06-SUMMARY.md`:
  · el criterio 4 solo esta cerrado en su mitad determinista; la mitad de datos reales es el
    checkpoint de la Task 2;
  · `reid_inherit_identity=False` es el default y activarlo es una decision de producto **fuera**
    del alcance de esta fase (CONTEXT § Deferred Ideas);
  · un track heredado por apariencia no revalida con cara hasta `revalidate_after` (120 s) —
    Pitfall 5, es la palanca que decide cuanto puede vivir una herencia erronea sin corregirse;
  · con mas de 4 tracks concurrentes el intervalo efectivo de ReID por track supera los 2 s
    (degradacion segura: menos coste, nunca mas) — asuncion A3 del research;
  · discrepancias D-1/D-2 de ADR-04/SPEC §4.3 (el modelo son 907 KB y ~0,22M parametros, no 9 MB
    y 2,2M: esas cifras son las de `osnet_x1_0`) — anotarlas para quien reescriba el ADR;
  · `models/` es gitignored: un clon nuevo necesita `scripts/fetch_models.py` antes de que
    `tests/test_reid_engine.py` deje de saltarse.

**4. `REQUIREMENTS.md`** — marcar `[x]` en REID-01, REID-02, REID-03 y REID-04 (lineas 224-227).

**5. `ROADMAP.md` § Phase 25** — marcar los planes como completados en la lista de planes; dejar
el criterio 4 anotado como "verificado en su parte determinista; tasa real pendiente de checkpoint
con camara".

**6. `STATE.md`** — actualizar: `stopped_at`, `last_updated`, la fila de la Fase 25 en la tabla de
las 22 fases (`✓ Completa (codigo)` + pendiente del checkpoint del criterio 4), el contador de
fases completas, la seccion "Siguiente paso" (apuntando a `/gsd:plan-phase 26`), la lista de
checkpoints manuales abiertos (pasan de 6 a 7) y la cifra de Test Coverage. Anadir a
"Accumulated Context / Decisions" las decisiones no obvias de esta fase, en el estilo de las
entradas de la Fase 24:
- Fase 25: el ONNX publico de OSNet trae el eje de batch fijo a 16; `scripts/fetch_models.py` lo
  reescribe a dinamico (bit-identico, `max|Δ|=0.0`) — sin eso, 84,5 ms por inferencia en vez de
  4,97 ms y el criterio 1 falla por 4x.
- Fase 25: ReID entra por la FSM (`on_reid_result`), no por el worker, porque `_claim_lost()` es lo
  unico que borra la entrada `TEMPORARILY_LOST` de `_states`; resolverlo en el worker dejaria la
  entrada huerfana y `on_tick` emitiria un `IDENTITY_LOST` espurio 30 s despues.
- Fase 25: el flag `reid_inherit_identity` vive en el worker, no en `TrackGallery`; `resolve()`
  calcula siempre el candidato real para que el modo solo-observacion pueda auditarse.
- Fase 25: la latencia de ReID no pasa por `AdaptiveRate.observe()` — contaminaria `avg_latency`
  de `/api/v2/cameras/{id}/health`, que significa latencia facial.
- Fase 25: los tests de `TrackGallery` usan vectores 512D de coseno controlado, nunca `np.random`:
  con ruido, el coseno entre dos embeddings independientes de OSNet sale 0,991.
  </action>
  <verify>
    <automated>F:/Documentos/IA/Proyecto_Camara/.venv/Scripts/python.exe -m pytest tests/ -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/ -q` sale con codigo 0 y `0 failed`; la linea de resumen no incluye `skipped` para `test_reid_engine.py`.
    - Los 6 comandos `pytest -k` de la tabla salen con codigo 0 y ninguno imprime "no tests ran".
    - `grep -c "^\- \[x\] \*\*REID-0" .planning/REQUIREMENTS.md` devuelve 4.
    - `.planning/phases/25-re-identificaci-n-de-personas-reid/25-06-SUMMARY.md` existe, tiene al menos 60 lineas y contiene las 5 filas de criterio con su comando (`grep -c "pytest tests/" 25-06-SUMMARY.md` >= 6).
    - `grep -n "Fase 25" .planning/STATE.md` devuelve al menos 6 lineas (tabla de fases, siguiente paso, decisiones).
    - `grep -n "criterio 4" .planning/STATE.md` devuelve al menos 1 linea (checkpoint abierto registrado).
    - `git status --short backend/ scripts/ tests/` no lista nada (la puerta de fase no deberia requerir cambios de codigo; si los requiere, es un fallo real que hay que corregir y documentar en el SUMMARY).
  </acceptance_criteria>
  <done>Suite completa verde, los 5 criterios trazados a comandos que pasan, REID-01..04 cerrados y la documentacion de planificacion al dia.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: checkpoint — tasa de falsos positivos del criterio 4 con camara real</name>
  <read_first>
    - `.planning/phases/25-re-identificaci-n-de-personas-reid/25-RESEARCH.md` § Pitfall 2 (la matriz
      de similitudes medida sobre imagenes reales: misma persona >= 0,93 pero contenido no
      relacionado ya llega a 0,71) y § Open Questions punto 1.
    - `.planning/phases/25-re-identificaci-n-de-personas-reid/25-VALIDATION.md` § Manual-Only
      Verifications.
  </read_first>
  <what-built>
    Todo el mecanismo de ReID esta construido, cableado y testeado de forma determinista:
    `ReIDEngine` (512D, p50 < 20 ms), `TrackGallery` (umbral, ventana y conflicto),
    `IdentityStateMachine.on_reid_result()` (herencia sin evento duplicado) y la via ReID en
    `RecognitionWorker`, con `reid_inherit_identity=False` por defecto.

    Lo que **no** puede construirse: la distribucion real de similitudes entre dos personas
    distintas de esta escena. El research midio que dos apariencias no relacionadas ya alcanzan
    0,71 de coseno, justo por encima del umbral de 0,7. Esa cifra es la que decide si el flag puede
    activarse algun dia, y solo la da la camara.
  </what-built>
  <how-to-verify>
    1. Asegurar que el modelo esta descargado:
       `F:/Documentos/IA/Proyecto_Camara/.venv/Scripts/python.exe scripts/fetch_models.py`
    2. Confirmar que el modo solo-observacion sigue activo: en `.env` **no** debe existir
       `REID_INHERIT_IDENTITY=true` (el default es `false`).
    3. Arrancar el sistema con la camara real:
       `F:/Documentos/IA/Proyecto_Camara/.venv/Scripts/python.exe -m uvicorn backend.main:app --host 0.0.0.0 --port 8000`
    4. Provocar el escenario del criterio 4: dos personas distintas, con ropa de color y tono
       parecidos (el research midio que OSNet es fuertemente cromatico: un desplazamiento de tono
       de 60 grados hunde la similitud a 0,48, asi que el caso dificil es ropa del mismo color),
       entrando y saliendo de encuadre alternadamente durante al menos 15 minutos. Incluir al menos
       un caso de una sola persona que se gira de espaldas y vuelve (el caso que ReID debe acertar).
    5. Recoger las lineas de log de nivel INFO con el prefijo `ReID: track` — cada match trae
       `track_id`, `person_id` y `similarity`.
    6. Consultar los contadores: `curl http://localhost:8000/api/v2/cameras/cam1/health` y anotar
       `reid_inferences`, `reid_matches`, `reid_inherited` (debe ser 0 en modo observacion) y
       `reid_conflicts`.
    7. Construir el histograma de similitudes separando los matches correctos (misma persona) de
       los incorrectos (personas distintas), y calcular la tasa de falsos positivos con el umbral
       de 0,7.

    Resultado esperado: los matches de la misma persona holgadamente por encima de 0,9 y los
    cruces entre personas distintas por debajo de 0,7. Si hay cruces por encima de 0,7, el umbral
    de esta escena debe subir antes de plantearse activar `REID_INHERIT_IDENTITY`.
  </how-to-verify>
  <resume-signal>
    Escribe "aprobado" con las cifras (numero de matches, rango de similitud de la misma persona,
    rango entre personas distintas, tasa de falsos positivos) para que se documenten en
    `25-06-SUMMARY.md`, o describe lo que se observo si no cuadra.

    Si no hay acceso a la camara ahora mismo, escribe "diferir": el checkpoint se registra en
    `STATE.md` junto a los otros 6 checkpoints manuales abiertos de fases anteriores. **No bloquea**
    avanzar a la Fase 26: la parte determinista del criterio 4 ya esta verde y el flag sigue en
    `false`, que es exactamente el estado seguro.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Decision de activar `REID_INHERIT_IDENTITY` -> integridad del registro de actividad por persona | Activar el flag sin la medida real convierte una hipotesis en un cambio de datos |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-20 | Repudiation | Activacion prematura de la herencia | mitigate | Este checkpoint es el requisito previo documentado: sin histograma de similitudes de personas reales, el flag se queda en `false` (CONTEXT § Deferred Ideas lo declara explicitamente fuera del alcance de la fase) |
| T-25-21 | Information Disclosure | Logs de la sesion de auditoria | accept | Los logs contienen `track_id`, `person_id` (entero) y similitud; ninguna imagen ni credencial. No copiar la URL RTSP al chat ni al SUMMARY |
</threat_model>

<verification>
```bash
F:/Documentos/IA/Proyecto_Camara/.venv/Scripts/python.exe -m pytest tests/ -q
F:/Documentos/IA/Proyecto_Camara/.venv/Scripts/python.exe -m pytest tests/test_reid_engine.py -k "embedding_is_512d_l2_normalized or latency_under_20ms" -q
F:/Documentos/IA/Proyecto_Camara/.venv/Scripts/python.exe -m pytest tests/test_track_gallery.py -k "inherit or conflict or does_not_merge" -q
F:/Documentos/IA/Proyecto_Camara/.venv/Scripts/python.exe -m pytest tests/test_recognition_worker.py -k "reid_recovers_identity_without_face or reid_inference_budget" -q
```
</verification>

<success_criteria>
- Suite completa verde, sin skips en `tests/test_reid_engine.py`.
- Los 5 criterios del ROADMAP trazados a comandos `pytest -k` que seleccionan tests y pasan.
- REID-01..REID-04 marcados `[x]`; `STATE.md` y `ROADMAP.md` al dia.
- `25-06-SUMMARY.md` documenta las cifras medidas en este equipo y las consecuencias abiertas.
- El checkpoint del criterio 4 esta ejecutado o formalmente diferido y registrado en `STATE.md`.
</success_criteria>

<output>
Al terminar, crear `.planning/phases/25-re-identificaci-n-de-personas-reid/25-06-SUMMARY.md`.
</output>
