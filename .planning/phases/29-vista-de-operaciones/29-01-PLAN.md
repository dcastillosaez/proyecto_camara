---
phase: 29
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/pipeline/manager.py
  - backend/main.py
  - tests/test_manager.py
  - tests/test_stream.py
autonomous: true
requirements: [OPS-05]

must_haves:
  truths:
    - "El backend expone, sin bloquear ningún worker, los bboxes normalizados de las personas visibles en el frame actual"
    - "El servidor publica esos bboxes por /ws a un ritmo fijo de 2Hz (cada 500ms), independiente del framerate del MJPEG"
    - "Los tracks que ya no están en el frame actual (aunque sigan vivos por el TTL de 30s de TrackRegistry.prune) no se publican"
  artifacts:
    - path: "backend/pipeline/manager.py"
      provides: "CameraPipeline.get_person_boxes() — bboxes normalizados 0-1, filtrados por frame_ids()"
      contains: "def get_person_boxes"
    - path: "backend/main.py"
      provides: "_tracks_broadcast_loop() registrada y cancelada en lifespan()"
      contains: "_tracks_broadcast_loop"
    - path: "tests/test_manager.py"
      provides: "tests unitarios de get_person_boxes (nuevo fichero)"
    - path: "tests/test_stream.py"
      provides: "test de integración del bucle de broadcast (nuevo TEST_ añadido al fichero existente)"
  key_links:
    - from: "backend/pipeline/manager.py::CameraPipeline.get_person_boxes"
      to: "backend/pipeline/tracking.py::TrackRegistry.frame_ids"
      via: "llamada directa, solo lectura bajo el RLock de TrackRegistry"
      pattern: "frame_ids\\(\\)"
    - from: "backend/main.py::_tracks_broadcast_loop"
      to: "backend/main.py::_broadcast"
      via: "await _broadcast({\"type\": \"tracks\", \"camera_id\": ..., \"tracks\": ...})"
      pattern: "\"type\": \"tracks\""
---

<objective>
Exponer los bboxes de personas visibles (OPS-05) como un método de solo lectura en `CameraPipeline` y publicarlos por el WebSocket legacy `/ws` a 2Hz exactos mediante una corrutina asyncio nueva, sin tocar ningún worker del pipeline ni el endpoint `/ws` existente.

Purpose: Es la mitad "productor" del overlay de tracks (OPS-05, criterio de éxito 4 del ROADMAP) — la mitad "consumidor" (canvas en frontend) es 29-02-PLAN.md, que consume literalmente el mismo payload que este plan define.
Output: `CameraPipeline.get_person_boxes()`, `_tracks_broadcast_loop()` registrada en `lifespan()`, y los tests que fijan el contrato del mensaje `{"type": "tracks", "camera_id": ..., "tracks": [...]}`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-vista-de-operaciones/29-CONTEXT.md
@.planning/phases/29-vista-de-operaciones/29-RESEARCH.md
@CLAUDE.md
</context>

<interfaces>
Formato exacto del mensaje que este plan produce y que 29-02-PLAN.md consume literalmente (29-RESEARCH.md Pattern 1/2, Code Examples):

```json
{
  "type": "tracks",
  "camera_id": "cam1",
  "tracks": [
    {
      "track_id": 7,
      "bbox": [0.12, 0.30, 0.28, 0.81],
      "identity_state": "CONFIRMED",
      "person_name": "Ana"
    }
  ]
}
```

`bbox` es `[x1, y1, x2, y2]` normalizado 0-1 respecto al frame de proceso (`get_process_size()`), NUNCA píxeles absolutos — el frontend solo conoce `img.naturalWidth/naturalHeight` (29-02-PLAN.md), no `process_size`. `identity_state` es uno de `"UNKNOWN" | "CANDIDATE" | "CONFIRMED" | "TEMPORARILY_LOST"` (`backend/perception/face/identity.py:22-25`, `IdentityState(str, Enum)`). `person_name` es `str | None`.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CameraPipeline.get_person_boxes() — bboxes normalizados filtrados por frame_ids()</name>
  <files>backend/pipeline/manager.py, tests/test_manager.py</files>
  <read_first>
    - backend/pipeline/manager.py líneas 268-360 (propiedad `health`, `degraded`, `get_object_boxes()` en la línea 346-347 — precedente exacto a replicar, `get_process_size()`/`get_native_resolution()` líneas 355-359)
    - backend/pipeline/tracking.py completo (TrackRegistry: `frame_ids()` líneas 113-115, `snapshot()` líneas 91-94, `TrackState.bbox`/`identity_state`/`person_name` líneas 19-34; el docstring de la clase en líneas 37-45 explica por qué DetectionWorker es el único escritor)
    - backend/pipeline/detection.py líneas 162-165 (formato exacto que devuelve `get_object_boxes()`, mismo patrón dict-por-track a replicar para personas)
    - backend/perception/face/identity.py líneas 22-26 (`IdentityState(str, Enum)`: `UNKNOWN`, `CANDIDATE`, `CONFIRMED`, `TEMPORARILY_LOST` — valores exactos que aparecerán en `identity_state`)
    - tests/test_track_registry.py líneas 14-21 (`_FakeTracked`, patrón para construir un `tracked`-like object que `update_from_detections` consume — replicar la misma clase en el fichero nuevo)
    - .planning/phases/29-vista-de-operaciones/29-RESEARCH.md sección "Pattern 2: Método pull en CameraPipeline, no worker nuevo" y "Pitfall 2: Mostrar boxes fantasma"
  </read_first>
  <behavior>
    - Test 1 (`TEST_get_person_boxes_excludes_stale_tracks`): dos tracks en el registry (ids 1 y 2), pero `set_frame_ids({1})` — solo el track 1 debe aparecer en la salida (Pitfall 2: nunca usar `snapshot()`/`active_ids()` sin filtrar por `frame_ids()`).
    - Test 2 (`TEST_get_person_boxes_normalizes_bbox_0_to_1`): `_process_size=(100, 50)`, bbox absoluto `(10, 10, 20, 20)` tras `update_from_detections` — la salida debe ser `bbox=[0.1, 0.2, 0.2, 0.4]` (x/w, y/h en cada esquina).
    - Test 3 (`TEST_get_person_boxes_returns_empty_without_process_size`): `_process_size=None` y `capture.health.native_resolution=None` (fallback también inválido) — la salida debe ser `[]`, no una excepción.
    - Test 4 (`TEST_get_person_boxes_includes_identity_fields`): tras `registry.set_identity_state(tid, IdentityState.CONFIRMED)` y `registry.set_identity(tid, 1, "Ana")`, el dict de salida contiene `"identity_state": "CONFIRMED"` (string, no el enum) y `"person_name": "Ana"`.
  </behavior>
  <action>
    En `backend/pipeline/manager.py`, insertar el nuevo método inmediatamente después de `get_object_boxes()` (tras la línea 347, antes de `get_heatmap()` en la línea 349), con este cuerpo EXACTO:

    ```python
    def get_person_boxes(self) -> list[dict]:
        """Bboxes de personas normalizados 0-1, solo lectura (OPS-05, 29-RESEARCH.md Pattern 2).

        Filtra por frame_ids() (visibles en el frame actual), no por snapshot()
        completo -- de lo contrario se dibujarian boxes fantasma de tracks que
        llevan hasta 30s sin verse (TrackRegistry.prune ttl, Pitfall 2)."""
        w, h = self.get_process_size()
        if w <= 0 or h <= 0:
            w, h = self.get_native_resolution()
        if w <= 0 or h <= 0:
            return []
        visible = self.registry.frame_ids()
        out: list[dict] = []
        for tid, ts in self.registry.snapshot().items():
            if tid not in visible:
                continue
            x1, y1, x2, y2 = ts.bbox
            out.append({
                "track_id": tid,
                "bbox": [x1 / w, y1 / h, x2 / w, y2 / h],
                "identity_state": ts.identity_state.value,
                "person_name": ts.person_name,
            })
        return out
    ```

    Crear `tests/test_manager.py` (fichero nuevo, no existe hoy — confirmado por `ls tests/`). Cabecera del fichero con docstring explicando el patrón de instanciación (construir `CameraPipeline` sin invocar `__init__` para evitar levantar `CaptureWorker`/RTSP real):

    ```python
    """Tests for backend.pipeline.manager.CameraPipeline — métodos pull de solo lectura.

    CameraPipeline se construye vía object.__new__() para evitar levantar
    CaptureWorker/RTSP real en __init__(); solo se fijan los atributos que
    get_person_boxes() necesita (registry, _process_size, capture.health).
    """
    from __future__ import annotations

    from types import SimpleNamespace

    import numpy as np
    import pytest

    from backend.perception.face.identity import IdentityState
    from backend.pipeline.manager import CameraPipeline
    from backend.pipeline.tracking import TrackRegistry


    class _FakeTracked:
        """Imita la forma de sv.Detections que consume update_from_detections (ver test_track_registry.py)."""

        def __init__(self, ids: list[int], boxes: list[tuple[int, int, int, int]] | None = None):
            self.tracker_id = np.array(ids)
            n = len(ids)
            self.xyxy = np.array(boxes if boxes else [[0, 0, 10, 10]] * n, dtype=float)
            self.confidence = np.full(n, 0.9)


    def _bare_pipeline(process_size: tuple[int, int] | None = (100, 50)) -> CameraPipeline:
        p = object.__new__(CameraPipeline)
        p.registry = TrackRegistry()
        p._process_size = process_size
        p.capture = SimpleNamespace(health=SimpleNamespace(native_resolution=None))
        return p
    ```

    Implementar los 4 tests de `<behavior>` usando `_bare_pipeline()`/`_FakeTracked` y `pipeline.registry.set_frame_ids({...})`/`set_identity_state(...)`/`set_identity(...)`.
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/test_manager.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "def get_person_boxes" backend/pipeline/manager.py` devuelve exactamente 1 línea
    - El cuerpo de `get_person_boxes` contiene la llamada `self.registry.frame_ids()` (nunca `active_ids()` en ese método)
    - El cuerpo normaliza dividiendo por ancho/alto (`x1 / w`, `y1 / h`) — nunca multiplica
    - Cada dict de salida tiene exactamente las claves `track_id`, `bbox`, `identity_state`, `person_name`
    - `tests/test_manager.py` existe y contiene 4 funciones `TEST_*`
    - `.venv/Scripts/python.exe -m pytest tests/test_manager.py -q` termina en verde (4 passed)
  </acceptance_criteria>
  <done>get_person_boxes() filtra por frame_ids(), normaliza bbox a 0-1 y expone identity_state/person_name como strings; 4 tests TEST_* verdes en tests/test_manager.py.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: _tracks_broadcast_loop() — publicador asyncio a 2Hz registrado en lifespan()</name>
  <files>backend/main.py, tests/test_stream.py</files>
  <read_first>
    - backend/main.py líneas 61-90 (`_ws_clients`, `_broadcast()` — reutilizar sin cambios)
    - backend/main.py líneas 176-197 (`_housekeeping_loop` — molde exacto a replicar: `while True: await asyncio.sleep(interval); if camera_manager is None: continue; ...`)
    - backend/main.py líneas 243-269 y 535-551 (`lifespan()`: dónde se crean las tasks con `asyncio.create_task` y dónde se cancelan tras `yield`)
    - backend/main.py líneas 785-803 (endpoint `/ws` — confirmar que NO necesita cambios, el nuevo tipo de mensaje viaja por la conexión ya existente)
    - tests/test_stream.py líneas 90-107 (`TEST_077_lifespan_starts_and_stops_camera_pipeline` — patrón de mock de `CameraManager`/`lifespan` a replicar para el test de este plan)
    - .planning/phases/29-vista-de-operaciones/29-RESEARCH.md sección "Pattern 1: Publicador pull-only a ritmo fijo (backend)" y "Code Examples"
  </read_first>
  <behavior>
    - Test (`TEST_tracks_broadcast_loop_sends_normalized_payload`, añadido al final de `tests/test_stream.py`): con `camera_manager` parcheado a un `MagicMock` cuyo `.all()` devuelve un pipeline `MagicMock` con `camera_id="cam1"` y `get_person_boxes.return_value=[{"track_id": 1, "bbox": [0.1, 0.1, 0.2, 0.2], "identity_state": "CONFIRMED", "person_name": "Ana"}]`, y con `_broadcast` parcheado a un `AsyncMock`, ejecutar `_tracks_broadcast_loop(interval=0.01)` forzando una sola iteración (`asyncio.sleep` parcheado con `side_effect=[None, asyncio.CancelledError()]`) — debe propagar `asyncio.CancelledError` y `_broadcast` debe haber sido invocado exactamente una vez con `{"type": "tracks", "camera_id": "cam1", "tracks": [...]}` (el mismo payload devuelto por el mock).
  </behavior>
  <action>
    En `backend/main.py`, insertar la nueva corrutina inmediatamente después de `_housekeeping_loop` (tras la línea 196, antes de `async def _purge_loop()` en la línea 199), con este cuerpo EXACTO:

    ```python
    async def _tracks_broadcast_loop(interval: float = 0.5) -> None:
        """Publica bboxes de personas por /ws a ritmo fijo, desacoplado del DetectionWorker
        (OPS-05, 29-RESEARCH.md Pattern 1). Solo lectura -- ninguna corrutina ejecuta inferencia,
        ningun hilo del pipeline se toca (CLAUDE.md invariantes 1/5/6)."""
        while True:
            await asyncio.sleep(interval)
            if camera_manager is None:
                continue
            for pipeline in camera_manager.all():
                boxes = pipeline.get_person_boxes()
                await _broadcast({
                    "type": "tracks",
                    "camera_id": pipeline.camera_id,
                    "tracks": boxes,
                })
    ```

    En `lifespan()`, tras la línea `housekeeping_task = asyncio.create_task(_housekeeping_loop(settings.housekeeping_secs))` (línea 540), añadir en la línea siguiente:
    ```python
    tracks_task = asyncio.create_task(_tracks_broadcast_loop())
    ```
    En el bloque de shutdown tras `yield` (línea 542 en adelante), junto a `housekeeping_task.cancel()` (línea 545), añadir en la línea siguiente:
    ```python
    tracks_task.cancel()
    ```

    Añadir el test de `<behavior>` al final de `tests/test_stream.py`, siguiendo literalmente el patrón de imports/mocks de `TEST_077_lifespan_starts_and_stops_camera_pipeline` (`from unittest.mock import MagicMock, patch` ya está importado en el fichero; añadir `AsyncMock` al import y `import asyncio`/`import pytest` si no están ya).
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/test_stream.py -k tracks -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "_tracks_broadcast_loop" backend/main.py` aparece en al menos 3 líneas: la definición (`async def _tracks_broadcast_loop`), la creación de la task (`asyncio.create_task(_tracks_broadcast_loop())`), y NO en el `.cancel()` (esa llamada es sobre `tracks_task`, no sobre la función)
    - `grep -n "tracks_task" backend/main.py` aparece en exactamente 2 líneas: la creación y `tracks_task.cancel()`
    - `grep -n '"type": "tracks"' backend/main.py` → 1 match, dentro de `_tracks_broadcast_loop`
    - El bucle usa `pipeline.get_person_boxes()` (nunca `get_object_boxes()`) y el único `await` dentro del `while True` es `asyncio.sleep(...)` y `_broadcast(...)` — ninguna llamada a `.get()`/`.query()` de base de datos
    - `.venv/Scripts/python.exe -m pytest tests/test_stream.py -k tracks -q` termina en verde
  </acceptance_criteria>
  <done>_tracks_broadcast_loop() creada/cancelada en lifespan() junto a housekeeping_task; test de integración verde confirmando el payload {"type": "tracks", "camera_id": ..., "tracks": [...]}.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser ↔ `/ws` (WebSocket) | Cliente en la LAN recibe, sobre una conexión ya autenticada, bboxes de personas y su `identity_state`/`person_name` en tiempo real — es información nueva sobre la misma conexión, no una superficie nueva |
| `_tracks_broadcast_loop` → `TrackRegistry` | Lectura interna, mismo proceso, sin cruce de confianza — protegida por el `RLock` ya existente de `TrackRegistry` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-01 | Information Disclosure | Mensaje `type:"tracks"` sobre `/ws` | mitigate | Ya protegido por `verify_ws_token` en el `accept()` de `websocket_endpoint` (`backend/main.py:787`), sin cambios en este plan — el nuevo tipo de mensaje viaja por la misma conexión ya autenticada, no se abre superficie nueva |
| T-29-02 | Denial of Service (parcial) | `_broadcast()` invocada por `_tracks_broadcast_loop` a 2Hz hacia clientes potencialmente desconectados | accept | Ya mitigado por `_broadcast()` (`backend/main.py:79-88`), que captura excepciones por cliente individual y limpia `_ws_clients` — reutilizado sin cambios; el throttle de 500ms en el propio bucle limita el volumen máximo de trabajo por camara |
</threat_model>

<verification>
- `.venv/Scripts/python.exe -m pytest tests/test_manager.py tests/test_stream.py -q` — ambos ficheros en verde
- `.venv/Scripts/python.exe -m pytest tests/ -q` — suite completa sin regresiones (519 + 8 nuevos tests de este plan)
</verification>

<success_criteria>
- `CameraPipeline.get_person_boxes()` existe, filtra por `frame_ids()`, normaliza bbox 0-1 y expone `identity_state`/`person_name`
- `_tracks_broadcast_loop()` publica por `/ws` cada 500ms (2Hz) el mensaje `{"type": "tracks", "camera_id": ..., "tracks": [...]}`, registrada/cancelada en `lifespan()` junto a `_housekeeping_loop`
- Ningún hilo del pipeline ni ninguna corrutina del bucle ejecuta inferencia (CLAUDE.md invariantes 1, 5, 6)
- Suite completa en verde
</success_criteria>

<output>
After completion, create `.planning/phases/29-vista-de-operaciones/29-01-SUMMARY.md`
</output>
