---
phase: 29
plan: 03
type: execute
wave: 2
depends_on: ["29-01", "29-02"]
files_modified:
  - frontend/index.html
  - frontend/js/views/dashboard.js
  - frontend/js/websocket.js
  - frontend/js/app.js
autonomous: false
requirements: [OPS-04, OPS-06]

must_haves:
  truths:
    - "La barra de estado del header muestra 3 estados posibles (online/degradado/offline) reflejando pipeline.degraded, health.connected y el estado del WebSocket"
    - "El panel Personas ahora lista las identidades activas (desde TrackRegistry vía el mensaje tracks) con su estado de confirmación"
    - "El panel Alertas activas muestra como máximo 3 alertas, ordenadas por severidad"
    - "Una desconexión WS prolongada (más de 1 ciclo de reconexión) se refleja en el header, no solo en la card de WS existente"
    - "Un observador no familiarizado identifica en menos de 3s si hay alerta activa (verificado por checkpoint humano)"
  artifacts:
    - path: "frontend/js/views/dashboard.js"
      provides: "setCamStatus(state) de 3 estados, computeHeaderState(), loadPipelineHealth(), renderPersonList(), loadActiveAlerts()"
      contains: "function computeHeaderState"
    - path: "frontend/index.html"
      provides: "markup de los paneles Personas ahora y Alertas activas en la columna derecha"
      contains: "persons-now-list"
  key_links:
    - from: "frontend/js/websocket.js (onopen/onclose)"
      to: "frontend/js/views/dashboard.js (setWsConnected)"
      via: "llamada directa en cada cambio de estado de conexión, sustituye el antiguo setCamStatus(bool)"
      pattern: "setWsConnected\\("
    - from: "frontend/js/views/dashboard.js (loadPipelineHealth)"
      to: "/api/v2/cameras/cam1/health"
      via: "fetch periódico cada 4s (Assumption A3, 29-RESEARCH.md)"
      pattern: "cameras/cam1/health"
    - from: "frontend/js/websocket.js (case 'tracks', de 29-02-PLAN.md)"
      to: "frontend/js/views/dashboard.js (renderPersonList)"
      via: "mismo mensaje tracks, sin segunda fuente de datos (29-RESEARCH.md Open Question #3)"
      pattern: "renderPersonList\\(msg\\.tracks\\)"
---

<objective>
Cerrar OPS-04 (la pantalla responde en 3s a las tres preguntas del operador) y OPS-06 (reconexión WS visible sin recargar) extendiendo el header a 3 estados y añadiendo los paneles "Personas ahora" y "Alertas activas" (top-3), todos alimentados por datos ya existentes (mensaje `tracks` de 29-01/29-02, endpoint `/api/v2/cameras/{id}/health` ya existente desde la Fase 17, endpoint `/api/v2/events` ya existente desde la Fase 19) — sin backend nuevo.

Purpose: Es el plan que cierra visualmente la fase — depende de 29-02 (mismo `websocket.js`/`index.html`/`app.js` en edición, conflicto de fichero real) y de 29-01 (el mensaje `tracks` debe existir para que "Personas ahora" tenga datos reales que verificar en el checkpoint).
Output: Header de 3 estados, panel "Personas ahora", panel "Alertas activas", checkpoint humano de los criterios de éxito 1, 4, 5 y 6 del ROADMAP.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/29-vista-de-operaciones/29-CONTEXT.md
@.planning/phases/29-vista-de-operaciones/29-RESEARCH.md
@.planning/phases/29-vista-de-operaciones/29-UI-SPEC.md
@.planning/ROADMAP.md
</context>

<interfaces>
Endpoints ya existentes que este plan consume sin cambios de backend:

```
GET /api/v2/cameras/{camera_id}/health
  → { connected: bool, fps: float, ..., degraded: bool, capture_fps: float, detection_fps: float, ... }
  (backend/main.py:1032-1049 — pipeline.stats()["degraded"] ya presente, asdict(pipeline.health) incluye "connected")

GET /api/v2/events?limit=N
  → { events: [ { id, type, camera_id, ts, severity: "info"|"warning"|"critical", person_name, ... } ], cursor }
  (backend/main.py:810-842, backend/events/types.py:19-46 EventType, 60-74 Event — "newest first" confirmado en
  backend/storage/repositories.py:105)
```

Mensaje WS ya definido por 29-01-PLAN.md / consumido por 29-02-PLAN.md (websocket.js ya tiene `case 'tracks'` llamando a `drawTracks`):
```json
{ "type": "tracks", "camera_id": "cam1", "tracks": [{ "track_id": 7, "bbox": [...], "identity_state": "CONFIRMED", "person_name": "Ana" }] }
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Header de 3 estados — computeHeaderState() combinando health + WS</name>
  <files>frontend/js/views/dashboard.js, frontend/js/websocket.js, frontend/js/app.js</files>
  <read_first>
    - frontend/js/views/dashboard.js completo (189 líneas — `setCamStatus(online)` líneas 32-48 a refactorizar, `fetchCounts()` líneas 62-77 que hoy la invoca con booleano)
    - frontend/js/websocket.js completo, YA MODIFICADO por 29-02-PLAN.md (debe tener `case 'tracks'` y el import de `drawTracks` — leer el fichero tal cual está en disco, no asumir el estado pre-29-02)
    - frontend/js/app.js completo, YA MODIFICADO por 29-02-PLAN.md (debe tener `initTracksOverlay()` ya cableado)
    - .planning/phases/29-vista-de-operaciones/29-UI-SPEC.md sección "Color" (tabla semáforo de estado: verde `#22c55e`/`#4ade80`, ámbar `#f59e0b`/`#fbbf24`, rojo `#ef4444`/`#f87171`) y sección "WebSocket reconnection visibility"
    - .planning/phases/29-vista-de-operaciones/29-CONTEXT.md decisiones D-03, D-10, D-11
    - .planning/phases/29-vista-de-operaciones/29-RESEARCH.md "Pitfall 3: Confundir dropped con degradado" y "Open Question #2" (polling 3-5s recomendado)
  </read_first>
  <behavior>
    No hay runner de test JS (mismo motivo que 29-02). Comportamiento esperado, verificado por grep + el checkpoint del Task 3:
    - `computeHeaderState()` da `'offline'` si `pipelineHealth.connected === false`, sin importar el estado del WS.
    - `computeHeaderState()` da `'degraded'` si `pipelineHealth.connected === true` y (`pipelineHealth.degraded === true` O el WS lleva caído más de 1 ciclo de reconexión).
    - `computeHeaderState()` da `'online'` solo si `connected && !degraded && wsConnected` (o WS caído por menos de 1 ciclo, tolerancia a un blip puntual).
    - `dropped`/`frames_dropped_total` NUNCA aparece en el cálculo (Pitfall 3 — CLAUDE.md invariante 4: puede ser normal).
  </behavior>
  <action>
    En `frontend/js/views/dashboard.js`, sustituir el bloque `setCamStatus` actual (líneas 30-48) por:

    ```javascript
    // ── Header pipeline status: 3 estados (online/degraded/offline) — OPS-04, D-03 ──
    const STATUS_STYLES = {
      online:   { wrap: 'bg-green-500/10 border-green-500/30 text-green-400', dot: 'bg-green-400 pulse', text: 'SISTEMA ONLINE' },
      degraded: { wrap: 'bg-amber-500/10 border-amber-500/30 text-amber-400', dot: 'bg-amber-400 pulse', text: 'SISTEMA DEGRADADO' },
      offline:  { wrap: 'bg-red-500/10 border-red-500/30 text-red-400',       dot: 'bg-red-400',         text: 'SISTEMA OFFLINE' },
    };
    let _camState = null;
    export function setCamStatus(state) {
      if (state === _camState) return;
      _camState = state;
      const wrap = document.getElementById('cam-status');
      const dot  = document.getElementById('status-dot');
      const txt  = document.getElementById('status-text');
      const s = STATUS_STYLES[state] || STATUS_STYLES.offline;
      wrap.className = `flex items-center gap-1.5 px-3 py-1.5 rounded-full border ${s.wrap} text-xs font-medium`;
      dot.className  = `w-1.5 h-1.5 rounded-full ${s.dot}`;
      txt.textContent = s.text;
      if (state === 'offline') showToast('Cámara sin señal — reintentando…', 'warn');
    }

    // ── Estado combinado: pipeline health + WebSocket (D-11) ──────────────
    let _wsConnected = true;
    let _wsCloseCount = 0;
    let _pipelineHealth = { connected: true, degraded: false };

    export function setWsConnected(connected, closeCount = 0) {
      _wsConnected = connected;
      _wsCloseCount = closeCount;
      computeHeaderState();
    }

    export async function loadPipelineHealth() {
      try {
        const res = await fetch('/api/v2/cameras/cam1/health');
        if (!res.ok) throw new Error(`HTTP ${res.status}`);
        const d = await res.json();
        _pipelineHealth = { connected: !!d.connected, degraded: !!d.degraded };
      } catch {
        _pipelineHealth = { connected: false, degraded: true };
      }
      computeHeaderState();
    }
    setInterval(loadPipelineHealth, 4000);

    function computeHeaderState() {
      // D-11: un WS caido mas de 1 ciclo de reconexion (>=2 intentos fallidos, _wsCloseCount > 1)
      // cuenta como degradado. Nunca se usa `dropped`/`frames_dropped_total` aqui (Pitfall 3).
      const wsDegraded = !_wsConnected && _wsCloseCount > 1;
      let state;
      if (!_pipelineHealth.connected) {
        state = 'offline';
      } else if (_pipelineHealth.degraded || wsDegraded) {
        state = 'degraded';
      } else {
        state = 'online';
      }
      setCamStatus(state);
    }
    ```

    En la función `fetchCounts()` existente (líneas 62-77 originales), eliminar las dos llamadas `setCamStatus(true)`/`setCamStatus(false)` (el header ya no se calcula desde `/counts`, sino desde `computeHeaderState()` — fuente única de verdad); dejar el resto de la función intacta (sigue actualizando `stat-total`/`stat-in`/`stat-out`/`events-badge`).

    En `frontend/js/websocket.js`:
    1. Extender el import de dashboard.js (línea 2, ya modificado o no por 29-02 según corresponda) para incluir `setWsConnected`: `import { updateStat, setCamStatus, setWsConnected, showToast } from './views/dashboard.js';` — `setCamStatus` puede quedar sin uso directo en este fichero tras el cambio, en cuyo caso eliminar su import si ESLint/el propio fichero no lo referencia ya.
    2. Añadir `let _wsCloseCount = 0;` junto a `let _wsRetry = 1000;` (línea 8).
    3. En `_ws.onopen` (líneas 39-43), sustituir `setCamStatus(true);` por:
       ```javascript
       _wsCloseCount = 0;
       setWsConnected(true, 0);
       ```
    4. En `_ws.onclose` (líneas 71-75), añadir al principio del cuerpo:
       ```javascript
       _wsCloseCount += 1;
       setWsConnected(false, _wsCloseCount);
       ```
       manteniendo `setWsStatus(false);`/`setTimeout(connectWS, _wsRetry);`/`_wsRetry = Math.min(...)` tal cual.

    En `frontend/js/app.js`, en el bloque de carga inicial (tras `loadHealth(); setInterval(loadHealth, 30000);`, líneas 39-40), añadir:
    ```javascript
    loadPipelineHealth();
    ```
    e importar `loadPipelineHealth` extendiendo el import de `dashboard.js` (línea 3): `import { loadInitialData, loadCamStatus, loadPipelineHealth } from './views/dashboard.js';` (el `setInterval(loadPipelineHealth, 4000)` ya vive dentro de `dashboard.js`, no hace falta duplicarlo en `app.js` — solo la llamada inicial para no esperar 4s al primer pintado).
  </action>
  <verify>
    <automated>node --check frontend/js/views/dashboard.js && node --check frontend/js/websocket.js && node --check frontend/js/app.js && .venv/Scripts/python.exe -m pytest tests/test_frontend_modules.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "function computeHeaderState" frontend/js/views/dashboard.js` → 1 match
    - `grep -n "dropped" frontend/js/views/dashboard.js` → 0 matches (Pitfall 3: nunca usar `dropped` en el cálculo de degradado)
    - `grep -n "cameras/cam1/health" frontend/js/views/dashboard.js` → 1 match
    - `grep -n "setWsConnected" frontend/js/websocket.js` → al menos 2 apariciones (una en `onopen`, otra en `onclose`)
    - `grep -n "_wsCloseCount" frontend/js/websocket.js` → al menos 2 apariciones
    - `grep -n "setCamStatus(true)\|setCamStatus(false)" frontend/js/views/dashboard.js frontend/js/websocket.js` → 0 matches en ambos ficheros (la firma booleana antigua ya no se invoca en ningún sitio)
    - `grep -n "'online'\|'degraded'\|'offline'" frontend/js/views/dashboard.js` → los 3 literales presentes en `STATUS_STYLES`
    - `grep -n "loadPipelineHealth" frontend/js/app.js` → 1 match
    - `.venv/Scripts/python.exe -m pytest tests/test_frontend_modules.py -q` sigue en verde
  </acceptance_criteria>
  <done>setCamStatus acepta 3 estados con los estilos exactos de la tabla semáforo D-03; computeHeaderState combina pipeline.degraded + health.connected + estado WS (>1 ciclo de reconexión, D-11); ninguna llamada antigua a setCamStatus(bool) sobrevive en el repo.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Panel "Personas ahora" + panel "Alertas activas" (top-3)</name>
  <files>frontend/index.html, frontend/js/views/dashboard.js, frontend/js/websocket.js, frontend/js/app.js</files>
  <read_first>
    - frontend/index.html líneas 204-233 (inicio de la sección `<section class="lg:col-span-2 ...">`, STAT CARDS actuales — los nuevos paneles se insertan ANTES de este bloque, como primeros hijos de la sección)
    - frontend/index.html líneas 475-487 (`#events-list`/`#events-empty` — patrón exacto de card+lista+empty state a replicar: `overflow-y-auto` + `max-height` + icono SVG + texto centrado)
    - frontend/js/views/dashboard.js tal como quedó tras Task 1 de este mismo plan
    - frontend/js/websocket.js tal como quedó tras 29-02-PLAN.md + Task 1 de este plan (debe tener ya `case 'tracks'` llamando a `drawTracks`)
    - backend/events/types.py líneas 19-46 (`EventType`, valores como `"INTRUSION"`, `"UNKNOWN_PERSON"`, `"OBJECT_LEFT"`) y líneas 60-74 (`Event`: campos `type`, `severity`, `ts`, `person_name` — estos son los nombres EXACTOS que trae el JSON de `/api/v2/events`)
    - .planning/phases/29-vista-de-operaciones/29-UI-SPEC.md sección "Personas ahora" y "Copywriting Contract" (textos EXACTOS de los estados vacíos)
    - .planning/phases/29-vista-de-operaciones/29-CONTEXT.md decisiones D-04, D-05
  </read_first>
  <behavior>
    - `renderPersonList([])` muestra el estado vacío ("Nadie en escena") y pone el badge de contador a 0.
    - `renderPersonList([{track_id:1, identity_state:'CONFIRMED', person_name:'Ana'}, {track_id:2, identity_state:'UNKNOWN', person_name:null}])` oculta el estado vacío, pinta 2 filas, y la segunda muestra "Desconocido" + etiqueta "desconocido".
    - `loadActiveAlerts()` sobre una respuesta con eventos de severidad `info`/`warning`/`critical` mezclados solo debe considerar `warning`/`critical`, ordenarlos por severidad descendente, y mostrar como máximo 3 (D-04 — nunca más).
  </behavior>
  <action>
    En `frontend/index.html`, insertar estos DOS bloques como primeros hijos de `<section class="lg:col-span-2 flex flex-col gap-4" aria-label="Estadísticas y eventos">` (antes del comentario `<!-- STAT CARDS -->`, línea 207), en este orden (Alertas activas primero, Personas ahora después — UI-SPEC: "positioned above or beside Personas ahora"):

    ```html
    <!-- ALERTAS ACTIVAS (top-3, D-04) -->
    <div class="card bg-slate-900 border border-slate-800 rounded-2xl p-4">
      <div class="flex items-center justify-between mb-3">
        <div class="flex items-center gap-2">
          <svg class="w-4 h-4 text-red-400" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" aria-hidden="true"><path d="M10.29 3.86 1.82 18a2 2 0 0 0 1.71 3h16.94a2 2 0 0 0 1.71-3L13.71 3.86a2 2 0 0 0-3.42 0z"/><line x1="12" y1="9" x2="12" y2="13"/><line x1="12" y1="17" x2="12.01" y2="17"/></svg>
          <h2 class="text-sm font-semibold text-slate-200">Alertas activas</h2>
        </div>
      </div>
      <div id="alerts-active-list" class="overflow-y-auto flex flex-col gap-1.5" style="max-height:140px" aria-label="Alertas activas" aria-live="polite"></div>
      <div id="alerts-active-empty" class="flex flex-col items-center justify-center py-4 text-center">
        <p class="text-slate-500 text-xs font-medium">Todo en orden</p>
        <p class="text-slate-700 text-xs mt-1">Sin alertas activas. Última comprobación: <span id="alerts-active-checked-at" class="mono">--:--:--</span>.</p>
      </div>
    </div>

    <!-- PERSONAS AHORA (D-05) -->
    <div class="card bg-slate-900 border border-slate-800 rounded-2xl p-4">
      <div class="flex items-center justify-between mb-3">
        <div class="flex items-center gap-2">
          <svg class="w-4 h-4 text-blue-400" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" aria-hidden="true"><path d="M17 21v-2a4 4 0 0 0-4-4H5a4 4 0 0 0-4 4v2"/><circle cx="9" cy="7" r="4"/><path d="M23 21v-2a4 4 0 0 0-3-3.87"/><path d="M16 3.13a4 4 0 0 1 0 7.75"/></svg>
          <h2 class="text-sm font-semibold text-slate-200">Personas ahora</h2>
        </div>
        <span id="persons-now-count" class="text-3xl font-bold text-slate-200 tabular-nums leading-none" aria-live="polite" aria-label="Personas activas ahora">0</span>
      </div>
      <div id="persons-now-list" class="overflow-y-auto flex flex-col gap-1" style="max-height:140px" aria-label="Personas activas" aria-live="polite"></div>
      <div id="persons-now-empty" class="flex flex-col items-center justify-center py-4 text-center">
        <p class="text-slate-500 text-xs font-medium">Nadie en escena</p>
        <p class="text-slate-700 text-xs mt-1">No se detectan personas activas en este momento.</p>
      </div>
    </div>
    ```

    En `frontend/js/views/dashboard.js`, añadir al final del fichero. Fix de revisión del plan-checker: dashboard.js llega a esta tarea con 226 líneas (188 originales + 38 netas de Task 1) y `tests/test_frontend_modules.py::TEST_line_limit` exige ≤300 — el bloque siguiente extrae un helper `_statusRow()` compartido por `renderPersonList`/`loadActiveAlerts` en vez de duplicar la construcción de cada fila por separado (65 líneas totales, no 81+), dejando el fichero en ~291 líneas, con margen:

    ```javascript
    // ── Filas compactas compartidas por Personas ahora / Alertas activas (D-04/D-05) ──
    function _statusRow(dotClass, mainText, sideText, sideClass = 'text-slate-600') {
      const row = document.createElement('div');
      row.className = 'flex items-center gap-2.5 py-1';
      const dot = document.createElement('span');
      dot.className = `w-1.5 h-1.5 rounded-full ${dotClass} flex-shrink-0`;
      dot.setAttribute('aria-hidden', 'true');
      const main = document.createElement('span');
      main.className = 'text-xs text-slate-300 flex-1 truncate';
      main.textContent = mainText;
      const side = document.createElement('span');
      side.className = `text-xs ${sideClass}`;
      side.textContent = sideText;
      row.append(dot, main, side);
      return row;
    }

    // ── Personas ahora (D-05) — misma fuente que el overlay, sin segunda vía ──
    const IDENTITY_LABEL = { CONFIRMED: 'confirmado', CANDIDATE: 'verificando', UNKNOWN: 'desconocido', TEMPORARILY_LOST: 'desconocido' };
    const IDENTITY_DOT   = { CONFIRMED: 'bg-green-400', CANDIDATE: 'bg-amber-400', UNKNOWN: 'bg-red-400', TEMPORARILY_LOST: 'bg-red-400' };

    export function renderPersonList(tracks) {
      const list  = document.getElementById('persons-now-list');
      const empty = document.getElementById('persons-now-empty');
      const count = document.getElementById('persons-now-count');
      if (!list || !empty || !count) return;
      const items = tracks || [];
      count.textContent = items.length;
      list.innerHTML = '';
      empty.style.display = items.length ? 'none' : '';
      items.forEach(t => {
        const dotClass = IDENTITY_DOT[t.identity_state] || IDENTITY_DOT.UNKNOWN;
        const label = IDENTITY_LABEL[t.identity_state] || IDENTITY_LABEL.UNKNOWN;
        list.appendChild(_statusRow(dotClass, t.person_name || 'Desconocido', label));
      });
    }

    // ── Alertas activas: top-3 por severidad (D-04) ────────────────────────
    const SEVERITY_RANK = { critical: 2, warning: 1, info: 0 };
    export async function loadActiveAlerts() {
      const panel = document.getElementById('alerts-active-list');
      const empty = document.getElementById('alerts-active-empty');
      const checkedAt = document.getElementById('alerts-active-checked-at');
      if (!panel || !empty) return;
      try {
        const res = await fetch('/api/v2/events?limit=10');
        if (!res.ok) throw new Error(`HTTP ${res.status}`);
        const data = await res.json();
        const alerts = (data.events || [])
          .filter(e => e.severity && e.severity !== 'info')
          .sort((a, b) => (SEVERITY_RANK[b.severity] ?? 0) - (SEVERITY_RANK[a.severity] ?? 0))
          .slice(0, 3);
        panel.innerHTML = '';
        empty.style.display = alerts.length ? 'none' : '';
        alerts.forEach(ev => {
          const sevClass = ev.severity === 'critical' ? 'bg-red-400' : 'bg-amber-400';
          const ts = new Date(ev.ts).toLocaleTimeString('es-ES', { hour12: false });
          panel.appendChild(_statusRow(sevClass, ev.type.replace(/_/g, ' '), ts, 'text-slate-600 mono'));
        });
      } catch {
        empty.style.display = '';
      }
      if (checkedAt) checkedAt.textContent = new Date().toLocaleTimeString('es-ES', { hour12: false });
    }
    setInterval(loadActiveAlerts, 5000);
    ```

    En `frontend/js/websocket.js`, dentro del `case 'tracks'` ya añadido por 29-02-PLAN.md, añadir la llamada a `renderPersonList` junto a `drawTracks`:
    ```javascript
    } else if (msg.type === 'tracks') {
      drawTracks(msg.tracks);
      renderPersonList(msg.tracks);
    }
    ```
    y extender el import de `dashboard.js` (ya modificado en Task 1 de este plan) para incluir `renderPersonList`: `import { updateStat, setCamStatus, setWsConnected, renderPersonList, showToast } from './views/dashboard.js';`

    En `frontend/js/app.js`, extender el import de `dashboard.js` (ya modificado en Task 1) para incluir `loadActiveAlerts`, y añadir la llamada inicial junto a las demás cargas iniciales (tras `loadPipelineHealth();`): `loadActiveAlerts();` (el `setInterval` ya vive dentro de `dashboard.js`, igual que `loadPipelineHealth`).
  </action>
  <verify>
    <automated>node --check frontend/js/views/dashboard.js && node --check frontend/js/websocket.js && node --check frontend/js/app.js && .venv/Scripts/python.exe -m pytest tests/test_frontend_modules.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "Nadie en escena" frontend/index.html` → presente literalmente
    - `grep -n "Todo en orden" frontend/index.html` → presente literalmente
    - `grep -n 'id="persons-now-list"' frontend/index.html` → 1 match; `grep -n 'id="alerts-active-list"' frontend/index.html` → 1 match
    - `grep -n "export function renderPersonList" frontend/js/views/dashboard.js` → 1 match
    - `grep -n "export async function loadActiveAlerts" frontend/js/views/dashboard.js` → 1 match
    - `grep -n "slice(0, 3)" frontend/js/views/dashboard.js` → 1 match (D-04: top-3, nunca la lista completa)
    - `grep -n "renderPersonList(msg.tracks)" frontend/js/websocket.js` → 1 match, en el mismo bloque `else if (msg.type === 'tracks')` que `drawTracks(msg.tracks)`
    - `grep -n "loadActiveAlerts" frontend/js/app.js` → 1 match
    - `wc -l frontend/js/views/dashboard.js` → ≤ 300 (LINE_LIMIT de `tests/test_frontend_modules.py::TEST_line_limit`; el bloque de esta tarea usa un helper `_statusRow()` compartido entre `renderPersonList`/`loadActiveAlerts` deliberadamente, en vez de duplicar la construcción de cada fila, para no acercarse al límite tras el crecimiento de Task 1 — ver nota de fix del plan-checker)
    - `.venv/Scripts/python.exe -m pytest tests/test_frontend_modules.py -q` sigue en verde
  </acceptance_criteria>
  <done>Personas ahora y Alertas activas renderizan desde datos reales (mismo mensaje tracks / GET /api/v2/events ya existente), con los textos exactos del Copywriting Contract, el límite top-3 de alertas por severidad, y dashboard.js dentro del límite de 300 líneas (helper _statusRow compartido en vez de código duplicado por panel).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verificación visual — criterios de éxito 1, 4, 5 y 6 del ROADMAP</name>
  <what-built>
    Header de 3 estados (online/degradado/offline) combinando salud del pipeline y estado del WebSocket; overlay de tracks en canvas sobre el vídeo; panel "Personas ahora"; panel "Alertas activas" (top-3); reconexión WS visible en el header, no solo en la card de WS.
  </what-built>
  <how-to-verify>
    1. Arrancar el backend: `.venv/Scripts/python.exe -m uvicorn backend.main:app --host 0.0.0.0 --port 8000`
    2. Abrir el dashboard con la ventana/DevTools ajustados a 1366×768 y confirmar que NO aparece scroll ni en la página ni en ningún panel individual (criterio de éxito 1 del ROADMAP).
    3. Con la cámara conectada y personas en el encuadre, confirmar que aparecen rectángulos de color (verde/ámbar/rojo según identidad) sobre el vídeo, que se actualizan sin parpadeo ni recarga del `<img>` del vídeo (criterio de éxito 4).
    4. Confirmar que el panel "Personas ahora" lista las mismas identidades que el overlay, con su etiqueta ("confirmado"/"verificando"/"desconocido") coherente con el color del rectángulo correspondiente.
    5. Detener el backend (Ctrl+C) y, sin recargar la página, confirmar que: (a) la card de WS pasa a "Reconectando", (b) el header pasa a DEGRADADO u OFFLINE en menos de ~10s. Reiniciar el backend y confirmar que ambos indicadores vuelven a su estado normal automáticamente, también sin recargar (criterio de éxito 5).
    6. Provocar o esperar una alerta (p. ej. una intrusión o persona desconocida) y cronometrar: un observador que mira la pantalla por primera vez debe poder decir "hay una alerta activa" o "todo bien" en menos de 3 segundos, mirando solo el header y el panel "Alertas activas" (criterio de éxito 6).
  </how-to-verify>
  <resume-signal>Escribe "aprobado" o describe qué falla, citando el número de criterio de éxito del ROADMAP que no se cumple.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `dashboard.js` ← `/api/v2/cameras/cam1/health`, `/api/v2/events` | Endpoints ya existentes desde Fase 17/19, ya protegidos por `Depends(verify)` a nivel de app (`backend/main.py:556`) — este plan solo añade clientes de fetch periódico, ninguna superficie nueva |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-04 | Tampering (bajo riesgo) | `loadActiveAlerts()`/`renderPersonList()` insertan `ev.type`/`person_name` vía `textContent` (nunca `innerHTML`) | mitigate | Uso deliberado de `textContent`/`createElement` en vez de `innerHTML` con interpolación de string, ya verificado en la acción de Task 2 — evita XSS aunque `person_name` contenga caracteres especiales |
| T-29-05 | Information Disclosure (bajo riesgo) | El header expone `pipeline.degraded`/`connected` a cualquier cliente autenticado de la LAN | accept | Mismo modelo de amenaza que el resto del dashboard (LAN, sin exposición pública, `Depends(verify)` ya cubre el acceso) — no es información sensible nueva respecto a lo que `/api/v2/cameras/{id}/health` ya expone desde la Fase 17 |
</threat_model>

<verification>
- `.venv/Scripts/python.exe -m pytest tests/test_frontend_modules.py -q` — sin regresión en el contrato mecánico de Fase 28
- `.venv/Scripts/python.exe -m pytest tests/ -q` — suite completa sin regresiones (no se toca backend en este plan)
- Checkpoint humano (Task 3) aprobado — criterios de éxito 1, 4, 5, 6 del ROADMAP confirmados visualmente
</verification>

<success_criteria>
- Header refleja 3 estados combinando `pipeline.degraded` + `health.connected` + estado WS (>1 ciclo de reconexión), sin usar `dropped` como señal
- "Personas ahora" lista identidades activas desde el mismo mensaje `tracks` que alimenta el overlay
- "Alertas activas" muestra como máximo 3 alertas por severidad, reutilizando `GET /api/v2/events` ya existente
- Checkpoint humano aprobado
</success_criteria>

<output>
After completion, create `.planning/phases/29-vista-de-operaciones/29-03-SUMMARY.md`
</output>
