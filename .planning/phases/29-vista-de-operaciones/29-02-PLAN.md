---
phase: 29
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/index.html
  - frontend/js/components/videoCanvas.js
  - frontend/js/websocket.js
  - frontend/js/app.js
autonomous: true
requirements: [OPS-05]

must_haves:
  truths:
    - "Un <canvas> se dibuja exactamente sobre el vídeo en directo, del mismo tamaño en píxeles CSS que la imagen mostrada"
    - "El overlay se redibuja únicamente cuando llega un mensaje WebSocket type:tracks, nunca por un temporizador o requestAnimationFrame propio"
    - "El overlay nunca toca img.src del vídeo — el cambio de resolución/reintento de conexión siguen siendo los únicos caminos que lo hacen"
    - "El color de cada bbox sigue la tabla semáforo de identidad (verde=CONFIRMED, ámbar=CANDIDATE, rojo=UNKNOWN/TEMPORARILY_LOST)"
  artifacts:
    - path: "frontend/index.html"
      provides: "elemento <canvas id=tracks-overlay> posicionado sobre #video-feed"
      contains: "id=\"tracks-overlay\""
    - path: "frontend/js/components/videoCanvas.js"
      provides: "drawTracks(), initTracksOverlay(), mapeo de coordenadas bajo object-fit:cover"
      contains: "export function drawTracks"
    - path: "frontend/js/websocket.js"
      provides: "case 'tracks' en el dispatcher onmessage, llamando a drawTracks"
      contains: "drawTracks(msg.tracks)"
  key_links:
    - from: "frontend/js/websocket.js (onmessage)"
      to: "frontend/js/components/videoCanvas.js (drawTracks)"
      via: "import + llamada directa en el nuevo case 'tracks'"
      pattern: "drawTracks\\(msg\\.tracks\\)"
    - from: "frontend/js/app.js (DOMContentLoaded)"
      to: "frontend/js/components/videoCanvas.js (initTracksOverlay)"
      via: "llamada explícita, mismo patrón que loadResolutions()"
      pattern: "initTracksOverlay\\(\\)"
---

<objective>
Dibujar el overlay de tracks (OPS-05, criterio de éxito 4 del ROADMAP) sobre `#video-feed` usando un `<canvas>` nuevo, sincronizado con el tamaño mostrado del vídeo y redibujado únicamente cuando llega el mensaje WebSocket `type:"tracks"` que produce 29-01-PLAN.md.

Purpose: Es la mitad "consumidor" del overlay — la mitad "productor" (backend) es 29-01-PLAN.md. Este plan es independiente de 29-01 en fichero (sin conflicto), pero comparte literalmente el contrato de payload documentado en `<interfaces>`.
Output: `<canvas id="tracks-overlay">` en `index.html`, `drawTracks()`/`initTracksOverlay()` en `videoCanvas.js`, nuevo `case 'tracks'` en `websocket.js`, wiring en `app.js`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/29-vista-de-operaciones/29-CONTEXT.md
@.planning/phases/29-vista-de-operaciones/29-RESEARCH.md
@.planning/phases/29-vista-de-operaciones/29-UI-SPEC.md
</context>

<interfaces>
Payload que este plan consume (definido y producido por 29-01-PLAN.md — ver también 29-RESEARCH.md Code Examples):

```json
{
  "type": "tracks",
  "camera_id": "cam1",
  "tracks": [
    { "track_id": 7, "bbox": [0.12, 0.30, 0.28, 0.81], "identity_state": "CONFIRMED", "person_name": "Ana" }
  ]
}
```

`bbox` es `[x1, y1, x2, y2]` normalizado 0-1 respecto al frame de proceso — el frontend NO conoce `process_size`, solo `img.naturalWidth/naturalHeight` (que refleja la resolución real del frame MJPEG servido, confirmada en 29-RESEARCH.md: "MJPEG se sirve en resolución process_size, no nativa"). `identity_state` es uno de `"UNKNOWN" | "CANDIDATE" | "CONFIRMED" | "TEMPORARILY_LOST"`.

Contrato existente a extender, no reescribir (`frontend/js/websocket.js:45-69`):
```javascript
} else if (msg.type === 'recording_failed') {
  updateRecordingStatus(msg.filename, 'failed');
  setRecBadge(false);
}
// <- nuevo case 'tracks' se añade aquí, mismo patrón else-if
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Canvas de overlay sobre #video-feed</name>
  <files>frontend/index.html</files>
  <read_first>
    - frontend/index.html líneas 52-105 (card de vídeo completo: `<img id="video-feed">` en 54-60, `#res-badge` en 63, `#video-offline` en 66-76, `#rec-badge` en 79-83, badge de detección en 86-94 — todos overlays `position:absolute` dentro del mismo contenedor `.card.relative`)
    - .planning/phases/29-vista-de-operaciones/29-UI-SPEC.md sección "Video overlay canvas (OPS-05, success criterion 4)"
    - .planning/phases/29-vista-de-operaciones/29-CONTEXT.md decisiones D-06/D-08
  </read_first>
  <action>
    Insertar el `<canvas>` inmediatamente después de la etiqueta de cierre de `<img id="video-feed" ... >` (tras la línea 60) y ANTES del div `id="res-badge"` (línea 62), dentro del mismo contenedor `.card.relative` (línea 53) para que `position:absolute` se resuelva contra él, igual que `#res-badge`/`#rec-badge`. Markup EXACTO:

    ```html
    <!-- Tracks overlay canvas (OPS-05, D-06/D-07/D-08) — nunca toca img.src -->
    <canvas
      id="tracks-overlay"
      aria-hidden="true"
      style="position:absolute; top:0; left:0; pointer-events:none; display:block;"
    ></canvas>
    ```

    No modificar ninguna otra sección del fichero en esta tarea (ni el header, ni los paneles del lado derecho — eso es 29-03-PLAN.md).
  </action>
  <verify>
    <automated>python -c "import re,pathlib; h=pathlib.Path('frontend/index.html').read_text(encoding='utf-8'); assert h.count('id=\"tracks-overlay\"') == 1; i=h.index('id=\"video-feed\"'); j=h.index('id=\"tracks-overlay\"'); k=h.index('id=\"res-badge\"'); assert i < j < k, 'orden incorrecto'; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'id="tracks-overlay"' frontend/index.html` → exactamente 1 match
    - El `<canvas>` aparece en el fichero DESPUÉS de `id="video-feed"` y ANTES de `id="res-badge"`
    - El atributo `style` contiene literalmente `position:absolute` y `pointer-events:none` (D-08: el overlay nunca debe interceptar clicks ni competir con los badges existentes)
  </acceptance_criteria>
  <done>canvas#tracks-overlay presente en el DOM dentro de .card.relative, posicionado sobre #video-feed, sin interferir con los badges existentes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: drawTracks() + sincronización con object-fit:cover + wiring WS/app</name>
  <files>frontend/js/components/videoCanvas.js, frontend/js/websocket.js, frontend/js/app.js</files>
  <read_first>
    - frontend/js/components/videoCanvas.js completo (65 líneas — patrón de export ya establecido, `document.getElementById(...).addEventListener` a nivel de módulo en la línea 39)
    - frontend/js/websocket.js completo (78 líneas — dispatcher `onmessage` líneas 45-69, import de `setRecBadge` desde `videoCanvas.js` en la línea 5 a extender)
    - frontend/js/app.js completo (44 líneas — import de `loadResolutions` en la línea 7, llamada en la línea 25)
    - .planning/phases/29-vista-de-operaciones/29-RESEARCH.md sección "Pattern 3: Sincronizar `<canvas>` con `<img>` bajo object-fit:cover" (código exacto del cálculo scale+offset), "Anti-Patterns to Avoid" (naturalWidth vs width, no recalcular process_size), "Pitfall 1" (por qué falla si no se deshace el `cover`), "Pitfall 5" (nunca `requestAnimationFrame`/interpolación entre mensajes)
    - .planning/phases/29-vista-de-operaciones/29-UI-SPEC.md sección "Video overlay canvas" (colores exactos, 2px, label 11px `.mono` sobre `rgba(0,0,0,0.6)`)
  </read_first>
  <behavior>
    No hay runner de test JS en el repo (confirmado por `tests/test_frontend_modules.py`, sin `package.json`) — la verificación de este task es estática (grep + `node --check` de sintaxis) más el checkpoint visual de 29-03-PLAN.md Task 3. Comportamiento esperado, documentado para el checkpoint:
    - `drawTracks([])` limpia el canvas sin dibujar nada (lista vacía → sin tracks visibles).
    - `drawTracks([{bbox:[0,0,1,1], identity_state:'CONFIRMED', person_name:'Ana'}])` dibuja un rectángulo verde (#22c55e) que cubre todo el área visible del vídeo (bbox 0-1 completo) con la etiqueta "Ana".
    - Llamar a `drawTracks()` dos veces seguidas nunca dibuja tracks acumulados de la llamada anterior (siempre `clearRect` primero).
  </behavior>
  <action>
    En `frontend/js/components/videoCanvas.js`, añadir al final del fichero (tras la línea 65, antes del `});` de cierre del listener de resolución si aplica — en la práctica, después de todo el contenido existente):

    ```javascript
    // ── Tracks overlay canvas (OPS-05) ─────────────────────
    // D-09: colores del semáforo de identidad, 2px, sin esquinas redondeadas.
    const STATE_COLOR = {
      CONFIRMED: '#22c55e',
      CANDIDATE: '#f59e0b',
      UNKNOWN: '#ef4444',
      TEMPORARILY_LOST: '#ef4444',
    };

    function syncCanvasToImage(canvas, img) {
      const rect = img.getBoundingClientRect();
      canvas.width = rect.width;
      canvas.height = rect.height;
    }

    // D-06/Pitfall 1: object-fit:cover recorta el frame fuente para llenar la
    // caja mostrada — hay que deshacer ese recorte con scale=max()+offset centrado,
    // usando naturalWidth/naturalHeight (resolución intrínseca), nunca width/height
    // (esos reflejan el tamaño mostrado, no el fuente — ver Anti-Patterns).
    function normalizedBoxToCanvasRect(box, img, canvas) {
      const iw = img.naturalWidth, ih = img.naturalHeight;
      const cw = canvas.width, ch = canvas.height;
      if (!iw || !ih || !cw || !ch) return null;
      const scale = Math.max(cw / iw, ch / ih);
      const drawW = iw * scale, drawH = ih * scale;
      const offsetX = (cw - drawW) / 2;
      const offsetY = (ch - drawH) / 2;
      const [x1, y1, x2, y2] = box;
      return {
        x: offsetX + x1 * iw * scale,
        y: offsetY + y1 * ih * scale,
        w: (x2 - x1) * iw * scale,
        h: (y2 - y1) * ih * scale,
      };
    }

    // D-07/Pitfall 5: redibuja SOLO cuando se le llama (mensaje 'tracks' a 2Hz) —
    // nunca requestAnimationFrame ni temporizador propio en este módulo.
    export function drawTracks(tracks) {
      const canvas = document.getElementById('tracks-overlay');
      const img = document.getElementById('video-feed');
      if (!canvas || !img) return;
      const ctx = canvas.getContext('2d');
      ctx.clearRect(0, 0, canvas.width, canvas.height);
      (tracks || []).forEach(t => {
        const rect = normalizedBoxToCanvasRect(t.bbox, img, canvas);
        if (!rect) return;
        const color = STATE_COLOR[t.identity_state] || STATE_COLOR.UNKNOWN;
        ctx.strokeStyle = color;
        ctx.lineWidth = 2;
        ctx.strokeRect(rect.x, rect.y, rect.w, rect.h);

        const label = t.person_name || 'Desconocido';
        ctx.font = '11px "JetBrains Mono", monospace';
        const textW = ctx.measureText(label).width;
        ctx.fillStyle = 'rgba(0,0,0,0.6)';
        ctx.fillRect(rect.x, Math.max(0, rect.y - 16), textW + 8, 16);
        ctx.fillStyle = '#e2e8f0';
        ctx.fillText(label, rect.x + 4, Math.max(11, rect.y - 4));
      });
    }

    let _tracksResizeObserver = null;
    export function initTracksOverlay() {
      const canvas = document.getElementById('tracks-overlay');
      const img = document.getElementById('video-feed');
      if (!canvas || !img || _tracksResizeObserver) return;
      syncCanvasToImage(canvas, img);
      _tracksResizeObserver = new ResizeObserver(() => syncCanvasToImage(canvas, img));
      _tracksResizeObserver.observe(img);
    }
    ```

    En `frontend/js/websocket.js`:
    1. Línea 5, extender el import existente: `import { setRecBadge, drawTracks } from './components/videoCanvas.js';`
    2. Dentro del `if/else if` de `onmessage` (tras el bloque `else if (msg.type === 'recording_failed') { ... }`, antes del cierre `}` en la línea 68), añadir:
       ```javascript
       } else if (msg.type === 'tracks') {
         drawTracks(msg.tracks);
       }
       ```

    En `frontend/js/app.js`:
    1. Línea 7, extender el import existente: `import { loadResolutions, initTracksOverlay } from './components/videoCanvas.js';`
    2. Tras `loadResolutions();` (línea 25), añadir en la línea siguiente: `initTracksOverlay();`
  </action>
  <verify>
    <automated>node --check frontend/js/components/videoCanvas.js && node --check frontend/js/websocket.js && node --check frontend/js/app.js && .venv/Scripts/python.exe -m pytest tests/test_frontend_modules.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "export function drawTracks" frontend/js/components/videoCanvas.js` → 1 match
    - `grep -n "export function initTracksOverlay" frontend/js/components/videoCanvas.js` → 1 match
    - `grep -n "ResizeObserver" frontend/js/components/videoCanvas.js` → al menos 1 match
    - `grep -n "naturalWidth" frontend/js/components/videoCanvas.js` → al menos 1 match; `grep -n "img.width\b" frontend/js/components/videoCanvas.js` → 0 matches (Anti-Pattern: nunca escalar por el atributo `width` mostrado)
    - `grep -n "requestAnimationFrame" frontend/js/components/videoCanvas.js` → 0 matches (Pitfall 5)
    - `grep -c "#22c55e\|#f59e0b\|#ef4444" frontend/js/components/videoCanvas.js` → 3 o más (los 3 colores del semáforo D-09 presentes)
    - `grep -n "img.src" frontend/js/components/videoCanvas.js` → 0 matches en todo el fichero (D-08: el overlay nunca debe tocar `img.src`; ese fichero solo lo hace desde `loadResolutions`/el listener de `resolution-select`, ya existente y sin relación con el overlay)
    - `grep -n "drawTracks(msg.tracks)" frontend/js/websocket.js` → 1 match, dentro de un `else if (msg.type === 'tracks')`
    - `grep -n "initTracksOverlay" frontend/js/app.js` → 1 match
    - `.venv/Scripts/python.exe -m pytest tests/test_frontend_modules.py -q` sigue en verde (límite de 300 líneas, sin `<script>`/`<style>` inline)
  </acceptance_criteria>
  <done>drawTracks() dibuja bboxes normalizados deshaciendo object-fit:cover con naturalWidth/naturalHeight; case 'tracks' en websocket.js llama a drawTracks(msg.tracks); initTracksOverlay() se invoca desde app.js; test_frontend_modules.py sigue verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `websocket.js` ← `/ws` | El cliente ya confía en los mensajes de una conexión WS que él mismo autenticó (token de un solo uso, `verify_ws_token`) — este plan no abre ninguna superficie nueva, solo interpreta un `type` de mensaje adicional sobre la misma conexión |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-03 | Tampering (bajo riesgo) | `drawTracks()` interpola `person_name`/`identity_state` recibidos del servidor directamente en `ctx.fillText` | accept | `fillText` sobre `<canvas>` no ejecuta HTML/JS (a diferencia de `innerHTML`) — no hay vector de XSS aunque `person_name` contenga caracteres especiales; el servidor además ya controla ese campo (nombres enrolados vía `PersonRecognizer`, Fase 9) |
</threat_model>

<verification>
- `node --check frontend/js/components/videoCanvas.js frontend/js/websocket.js frontend/js/app.js` — sintaxis válida en los 3 ficheros
- `.venv/Scripts/python.exe -m pytest tests/test_frontend_modules.py -q` — sin regresión en el contrato mecánico del refactor de Fase 28
</verification>

<success_criteria>
- `<canvas id="tracks-overlay">` posicionado sobre `#video-feed`, sincronizado a su `clientWidth/clientHeight` vía `ResizeObserver`
- `drawTracks()` mapea bbox normalizado → píxeles de canvas deshaciendo `object-fit:cover` correctamente (Pattern 3)
- El overlay se redibuja solo al recibir `type:"tracks"`, nunca por temporizador propio
- El overlay nunca toca `img.src`
</success_criteria>

<output>
After completion, create `.planning/phases/29-vista-de-operaciones/29-02-SUMMARY.md`
</output>
