---
phase: 23-migracion-a-insightface-arcface-con-quality-gating
plan: 02
type: execute
wave: 2
depends_on: ["23-01"]
files_modified:
  - backend/recognizer.py
  - backend/storage/models.py
  - scripts/reenroll.py
  - requirements.txt
  - tests/test_phase9.py
  - tests/test_recognizer_orchestration.py
autonomous: false
requirements:
  - FACE-01
  - FACE-05
  - FACE-06

must_haves:
  truths:
    - "backend/recognizer.py delega en FaceEngine/FaceQualityAssessor/IdentityIndex — ninguna llamada a face_recognition/dlib queda en el modulo"
    - "RecognitionWorker sigue funcionando sin cambios: available/process_crop/prune preservan su firma y contrato"
    - "scripts/reenroll.py reconstruye identidades desde data/gallery/ y reporta migradas vs no migradas"
    - "dlib y face-recognition desaparecen de requirements.txt"
  artifacts:
    - path: "scripts/reenroll.py"
      provides: "Re-enrolamiento masivo desde data/gallery/, con backup de persons.db"
      min_lines: 80
    - path: "tests/test_recognizer_orchestration.py"
      provides: "Tests de PersonRecognizer sobre los componentes de 23-01 (con FaceEngine real o mockeado segun el test)"
      min_lines: 90
  key_links:
    - from: "backend/recognizer.py"
      to: "backend/perception/face/engine.py"
      via: "PersonRecognizer delega deteccion+embedding en FaceEngine en vez de face_recognition"
      pattern: "FaceEngine"
    - from: "backend/pipeline/recognition.py"
      to: "backend/recognizer.py"
      via: "contrato publico sin cambios: available, process_crop(crop, tracker_id), prune(active_ids)"
      pattern: "process_crop"
---

<objective>
Integrar FaceEngine/FaceQualityAssessor/IdentityIndex (construidos y verificados en 23-01) dentro de backend/recognizer.py, preservando el contrato publico que ya consume RecognitionWorker, y ejecutar el re-enrolamiento real que retira dlib del proyecto.

Purpose: cerrar la Fase 23 — el pipeline en vivo pasa a usar ArcFace sin que backend/pipeline/recognition.py sepa que cambio nada por debajo.
Output: recognizer.py reducido a orquestacion, identidades reales migradas desde data/gallery/, dlib/face-recognition fuera de requirements.txt, tasa de aciertos ArcFace vs dlib documentada.
</objective>

<context>
@.planning/phases/23-migracion-a-insightface-arcface-con-quality-gating/23-CONTEXT.md
@.planning/phases/23-migracion-a-insightface-arcface-con-quality-gating/23-01-SUMMARY.md
@backend/recognizer.py
@backend/pipeline/recognition.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PersonRecognizer delega en FaceEngine/FaceQualityAssessor/IdentityIndex</name>
  <files>backend/recognizer.py, backend/storage/models.py, tests/test_recognizer_orchestration.py, tests/test_phase9.py</files>
  <read_first>backend/recognizer.py (completo), backend/perception/face/*.py (de 23-01), backend/pipeline/recognition.py</read_first>
  <behavior>
    - test_process_crop_uses_face_engine: process_crop() invoca FaceEngine.detect()/.embed() en vez de face_recognition.face_locations/face_encodings (verificar con mock/spy sobre el motor, no sobre face_recognition — ese import desaparece).
    - test_process_crop_rejects_low_quality_face: un candidato cuya FaceQuality.passed es False no llega a generar embedding ni a buscar en el indice (ahorro de trabajo, y ningun registro espurio).
    - test_available_reflects_face_engine_availability: PersonRecognizer.available reflete FaceEngine.available, no una constante fija.
    - test_public_contract_unchanged: available/process_crop/prune/list_persons/enroll_named_face conservan exactamente su firma actual (mismo test de regresion de interfaz que valida que RecognitionWorker no necesita cambios).
    - Reutilizar/adaptar los tests existentes de tests/test_phase9.py que ejercitan PersonRecognizer (TEST_093..109) sobre la nueva implementacion — deben seguir pasando con la logica de negocio equivalente (gates de calidad, consenso para nuevo registro, voto mayoritario, re-verificacion), aunque el motor de bajo nivel cambie.
  </behavior>
  <action>
1. La tabla `persons`/`face_encodings` pasa a almacenar embeddings de 512 floats (2048 bytes) en vez de 128 (1024 bytes) — `_blob_to_encoding`/`_NUMPY_SIZE` de la Fase 22 debe actualizarse a 512*4 (float32, el dtype nativo de insightface) o 512*8 si se mantiene float64; decidir consistentemente con lo que `FaceEngine.embed()` devuelve y documentar. Esto es un cambio de esquema — filas existentes con blobs de 1024 bytes (dlib) quedan invalidas para lectura directa, de ahi que el re-enrolamiento (Task 2) sea obligatorio antes de poner esto en produccion, no opcional.

2. Reescribir `process_crop`/`identify_or_register`/`enroll_named_face` para usar `FaceEngine`+`FaceQualityAssessor`+`IdentityIndex` en lugar de `face_recognition.face_locations`/`face_encodings`/`face_distance`. La logica de negocio (gates de tamano/blur ya cubiertas por FaceQualityAssessor, consenso `NEW_PERSON_CONSENSUS`, voto mayoritario `VOTE_WINDOW`, re-verificacion `REVERIFY_INTERVAL`) se conserva — solo cambia que produce el embedding y como se busca el mejor match (`IdentityIndex.search` en vez de `face_recognition.face_distance` + argmin manual).

3. `IdentityIndex` se reconstruye (`rebuild()`) al cargar `_load()` desde la BD, y se actualiza (`add()`) en cada registro/enrolamiento nuevo — mantenerlo sincronizado con `_person_ids`/`_encodings` es responsabilidad de `PersonRecognizer`, `IdentityIndex` no conoce SQLite.

4. Import de `face_recognition`/`fr` desaparece de `backend/recognizer.py` por completo.

Escribir primero los tests nuevos (RED) y verificar cuales de los tests existentes de `test_phase9.py` necesitan ajustarse (los que mockean `backend.recognizer.fr` ya no aplican — decidir si se adaptan a mockear `FaceEngine` o se re-escriben para usar el motor real con fixtures, documentando la decision).
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/test_recognizer_orchestration.py tests/test_phase9.py -v --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - Ninguna referencia a face_recognition/fr en backend/recognizer.py
    - El contrato publico usado por RecognitionWorker no cambia
    - La logica de negocio (consenso, voto, re-verificacion) se preserva con tests equivalentes en verde
  </acceptance_criteria>
  <done>Reconocimiento facial corre sobre ArcFace de punta a punta, con el mismo contrato que consume el pipeline.</done>
</task>

<task type="auto">
  <name>Task 2: scripts/reenroll.py — reconstruccion de identidades desde la galeria</name>
  <files>scripts/reenroll.py</files>
  <read_first>scripts/migrate_embeddings.py (Fase 22, mismo patron de backup+reporte), backend/main.py:382-397 (_save_gallery_capture, formato de data/gallery/)</read_first>
  <behavior>
    - El script recorre data/gallery/{person_id}/*.jpg, ejecuta FaceEngine+FaceQualityAssessor sobre cada imagen, y registra el mejor embedding (o los N mejores, hasta MAX_EMBEDDINGS_PER_PERSON) por persona en una BD reconstruida.
    - Antes de escribir nada, hace backup de data/persons.db (mismo esquema de nombre que migrate_embeddings.py).
    - Reporta: personas totales en data/gallery/, migradas con exito, sin ninguna imagen utilizable (todas las capturas fallan la quality gate o no hay cara detectable), y el nombre asociado a cada una (si data/persons.db original todavia existe, para no perder el nombre ya asignado).
    - Codigo de salida distinto de cero si alguna persona nombrada (no anonima) no se pudo migrar — una persona anonima sin nombre no migrada es tolerable, una persona nombrada sin migrar es una perdida de dato real que el operador debe saber.
  </behavior>
  <action>
1. Escribir `scripts/reenroll.py` con argparse (`--gallery-dir`, `--db-path`, por defecto `data/gallery` y `data/persons.db`).

2. Reutilizar `FaceEngine`/`FaceQualityAssessor` de 23-01 directamente (no reimplementar deteccion aqui).

3. El reporte final sigue el mismo formato tabular que `scripts/migrate_embeddings.py` (Personas totales / Migradas / Sin imagen utilizable / Backup).
  </action>
  <verify>
    <manual>
      Ejecutar contra un data/gallery/ de prueba con 2-3 personas (imagenes reales o del propio equipo, con consentimiento) y confirmar que persons.db resultante tiene embeddings de 512D coherentes y que el reporte es preciso.
    </manual>
  </verify>
  <acceptance_criteria>
    - Backup de persons.db antes de escribir
    - Reporte distingue migradas vs no migradas, con motivo
    - Codigo de salida distinto de cero si una persona nombrada no migra
  </acceptance_criteria>
  <done>Camino de migracion real disponible para pasar de dlib a ArcFace sin perder identidades nombradas.</done>
</task>

<task type="auto">
  <name>Task 3: Retirar dlib/face-recognition</name>
  <files>requirements.txt</files>
  <read_first>requirements.txt</read_first>
  <behavior>
    - requirements.txt ya no lista face-recognition (ni dlib, si aparece como dependencia directa en algun punto).
    - insightface y onnxruntime aparecen con las versiones minimas de la SPEC.
  </behavior>
  <action>
Editar requirements.txt: quitar `face-recognition>=1.3`, anadir `insightface>=0.7.3` y `onnxruntime>=1.19` en la seccion correspondiente. Este paso va **al final**, despues de que Task 1 y Task 2 esten en verde — quitar la dependencia antes arriesgaria un entorno de desarrollo a medio migrar sin forma de volver atras rapido si algo falla.
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/ -q</automated>
  </verify>
  <acceptance_criteria>
    - grep -i "face.recognition\|^dlib" requirements.txt sin resultados
    - Suite completa sigue en verde tras retirar la dependencia
  </acceptance_criteria>
  <done>dlib fuera del proyecto (FACE-06).</done>
</task>

<task type="checkpoint">
  <name>Task 4: Benchmark real ArcFace vs dlib y cierre de la fase</name>
  <files>—</files>
  <action>
Requiere datos reales — no ejecutable en un worktree aislado sin historial de capturas:

1. Sobre un set de validacion de al menos 50 recortes reales del propio proyecto (data/gallery/ acumulada con la camara real), medir la tasa de aciertos de ArcFace con el mismo conjunto de identidades que se uso para evaluar dlib (o, si nunca se evaluo dlib formalmente, establecer la linea base ahora con ambos motores sobre el mismo set, ya que persons.db pre-migracion todavia tiene los embeddings dlib en el backup).

2. Documentar el numero en el SUMMARY de 23-02 — no asumirlo ni extrapolarlo del comportamiento en frames sinteticos.

3. Confirmar en produccion (servidor real, camara conectada) que el reconocimiento sigue identificando a las personas ya migradas y registrando correctamente a una persona nueva.
  </action>
  <verify>
    <manual>Tasa de aciertos documentada con datos reales; identificacion en vivo confirmada contra la camara real.</manual>
  </verify>
  <acceptance_criteria>
    - Tasa de aciertos ArcFace >= dlib sobre el mismo set, documentada con el numero real
    - Identificacion en vivo verificada con camara real
  </acceptance_criteria>
  <done>Fase 23 cerrada: ArcFace en produccion, con evidencia real de que iguala o mejora a dlib.</done>
</task>

</tasks>

<verification>
.venv/Scripts/python.exe -m pytest tests/ -q
Benchmark real documentado en el SUMMARY (Task 4, checkpoint).
</verification>

<success_criteria>
- backend/recognizer.py no importa face_recognition/dlib
- RecognitionWorker funciona sin cambios sobre el nuevo recognizer.py
- scripts/reenroll.py migra identidades reales desde data/gallery/ con reporte preciso
- dlib/face-recognition fuera de requirements.txt
- Tasa de aciertos ArcFace documentada contra un set de validacion real
</success_criteria>

<output>
After completion, create `.planning/phases/23-migracion-a-insightface-arcface-con-quality-gating/23-02-SUMMARY.md`
</output>
