---
phase: 17-frame-broker-y-capture-worker
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/pipeline/__init__.py
  - backend/pipeline/broker.py
  - tests/test_broker.py
autonomous: true
requirements:
  - PIPE-01
  - PIPE-02

must_haves:
  truths:
    - "FrameBroker.publish() nunca bloquea, aunque un suscriptor tarde 1s por frame"
    - "Cada suscriptor tiene su propio slot: un consumidor lento solo se pierde frames a si mismo"
    - "publish() incrementa dropped[name] cuando sobreescribe un slot no consumido"
    - "Subscription.get(timeout) devuelve None al expirar el timeout sin frame nuevo"
    - "close() elimina el suscriptor del broker y desbloquea a quien estuviera esperando"
  artifacts:
    - path: "backend/pipeline/__init__.py"
      provides: "Paquete pipeline con reexport de Frame, FrameBroker, Subscription"
      exports: ["Frame", "FrameBroker", "Subscription"]
      min_lines: 3
    - path: "backend/pipeline/broker.py"
      provides: "Frame dataclass + FrameBroker fan-out latest-frame + Subscription"
      exports: ["Frame", "FrameBroker", "Subscription"]
      min_lines: 90
    - path: "tests/test_broker.py"
      provides: "Tests de no-bloqueo, aislamiento, descarte, timeout y cierre"
      min_lines: 90
  key_links:
    - from: "backend/pipeline/__init__.py"
      to: "backend/pipeline/broker.py"
      via: "reexport de la API publica del paquete"
      pattern: "from backend\\.pipeline\\.broker import"
---

<objective>
Implementar `FrameBroker`: un distribuidor de frames latest-only en el que cada suscriptor tiene su propio slot de un frame. El productor sobreescribe sin bloquear jamas y contabiliza los frames descartados por suscriptor.

Purpose: es la pieza que hace cierto el invariante "la captura nunca espera a la IA". Sin ella, cualquier consumidor lento (YOLO, reconocimiento facial, grabacion) impone su ritmo a la captura.
Output: `backend/pipeline/broker.py` con tests que demuestran el no-bloqueo y el aislamiento entre suscriptores.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-frame-broker-y-capture-worker/17-CONTEXT.md
@propuesta_mejora/SPEC_v2.md
</context>

<interfaces>
<!-- Contrato que broker.py debe exponer (consumido por 17-02 y por toda la Fase 18) -->

```python
@dataclass(slots=True)
class Frame:
    camera_id: str
    seq: int                  # monotonico por camara, empieza en 0
    captured_at: float        # time.monotonic() justo tras cap.read()
    wall_clock: datetime      # datetime.now() del mismo instante
    image: np.ndarray         # BGR, ya reescalado a tamano de proceso

class FrameBroker:
    def subscribe(self, name: str) -> "Subscription": ...
    def publish(self, frame: Frame) -> None: ...          # NUNCA bloquea
    def stats(self) -> dict[str, dict[str, int]]: ...     # {name: {delivered, dropped, last_seq}}
    def close(self) -> None: ...                          # cierra todas las suscripciones

class Subscription:
    name: str
    def get(self, timeout: float | None = None) -> Frame | None: ...
    def close(self) -> None: ...
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Tests de FrameBroker (RED)</name>
  <files>tests/test_broker.py</files>
  <read_first>.planning/phases/17-frame-broker-y-capture-worker/17-CONTEXT.md, tests/conftest.py</read_first>
  <behavior>
    - test_get_returns_published_frame: tras `publish(f)`, `sub.get(timeout=1)` devuelve exactamente `f`.
    - test_publish_never_blocks: con un suscriptor que no consume nunca, publicar 1000 frames tarda menos de 0.5 s.
    - test_slow_subscriber_does_not_slow_producer: dos suscripciones, una consumida en bucle y otra ignorada; el productor publica 200 frames y el suscriptor rapido recibe frames con `seq` creciente sin que el productor supere 1 s.
    - test_dropped_counted_per_subscriber: publicar 5 frames sin consumir deja `stats()["slow"]["dropped"] == 4` y `delivered == 0`; tras un `get()`, `delivered == 1`.
    - test_latest_frame_wins: publicar frames con seq 0..4 sin consumir; `get()` devuelve el de seq 4, no el 0.
    - test_isolation_between_subscribers: suscriptor A consume todo y termina con `dropped == 0`; suscriptor B no consume nada y termina con `dropped > 0`.
    - test_get_timeout_returns_none: `sub.get(timeout=0.05)` sin publicaciones devuelve None y tarda entre 0.04 y 0.3 s.
    - test_close_unblocks_waiter: un hilo esperando en `get(timeout=5)` se desbloquea con None en menos de 1 s tras `sub.close()`.
    - test_publish_after_close_is_noop: publicar tras `sub.close()` no lanza excepcion y el suscriptor desaparece de `stats()`.
    - test_duplicate_subscriber_name_raises: `subscribe("x")` dos veces lanza `ValueError`.
  </behavior>
  <action>
Crear `tests/test_broker.py` con los 10 tests descritos. Importar desde `backend.pipeline.broker`.

Helper local para construir frames:

```python
def make_frame(seq: int, camera_id: str = "cam1") -> Frame:
    return Frame(
        camera_id=camera_id,
        seq=seq,
        captured_at=time.monotonic(),
        wall_clock=datetime.now(),
        image=np.full((4, 4, 3), seq % 256, dtype=np.uint8),
    )
```

Para `test_publish_never_blocks` usar `time.monotonic()` alrededor del bucle de 1000 `publish()`. No usar `time.sleep` en el productor.

Para `test_slow_subscriber_does_not_slow_producer` lanzar el consumidor rapido en un `threading.Thread` daemon que hace `get(timeout=0.01)` en bucle, y medir solo el tiempo del productor.

Marcar los tests sensibles al tiempo con margenes generosos (factor 5x sobre el valor esperado) para que no sean flaky en CI.

IMPORTANTE: los tests DEBEN fallar (RED). `backend/pipeline/` todavia no existe.

Ejecutar: `.venv/Scripts/python.exe -m pytest tests/test_broker.py -v -x` — debe fallar con ModuleNotFoundError.
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/test_broker.py -v -x 2>&amp;1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_broker.py existe con 10 funciones test_*
    - Todos fallan con ModuleNotFoundError: No module named 'backend.pipeline'
  </acceptance_criteria>
  <done>10 tests escritos, todos RED por ausencia del modulo</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implementar Frame, FrameBroker y Subscription (GREEN)</name>
  <files>backend/pipeline/__init__.py, backend/pipeline/broker.py</files>
  <read_first>tests/test_broker.py, .planning/phases/17-frame-broker-y-capture-worker/17-CONTEXT.md</read_first>
  <behavior>
    - Los 10 tests del Task 1 pasan.
    - `publish()` toma un unico lock global breve para iterar suscriptores; por cada uno sobreescribe el slot y notifica su Condition.
    - `get()` espera en la Condition del suscriptor hasta que haya frame o expire el timeout.
    - `stats()` devuelve una copia, no las estructuras internas.
  </behavior>
  <action>
Crear `backend/pipeline/__init__.py`:

```python
"""Pipeline de video desacoplado: captura, broker y workers."""

from backend.pipeline.broker import Frame, FrameBroker, Subscription

__all__ = ["Frame", "FrameBroker", "Subscription"]
```

Crear `backend/pipeline/broker.py` siguiendo esta referencia (el executor puede ajustar detalles menores para pasar los tests):

```python
"""Fan-out latest-frame: un slot por suscriptor, el productor nunca bloquea."""

from __future__ import annotations

import logging
import threading
from dataclasses import dataclass, field
from datetime import datetime

import numpy as np

logger = logging.getLogger(__name__)


@dataclass(slots=True)
class Frame:
    """Un frame capturado, con los timestamps fijados en el momento de la captura.

    El consumidor NO debe mutar `image`: el broker publica la referencia sin copiar.
    """

    camera_id: str
    seq: int
    captured_at: float
    wall_clock: datetime
    image: np.ndarray

    @property
    def age(self) -> float:
        """Segundos transcurridos desde la captura."""
        import time
        return time.monotonic() - self.captured_at


class Subscription:
    """Slot de un frame con espera bloqueante para un unico consumidor."""

    def __init__(self, name: str, broker: FrameBroker) -> None:
        self.name = name
        self._broker = broker
        self._cond = threading.Condition()
        self._slot: Frame | None = None
        self._closed = False
        self.delivered = 0
        self.dropped = 0
        self.last_seq = -1

    def _offer(self, frame: Frame) -> None:
        """Llamado por el broker. Nunca bloquea mas que la adquisicion del Condition."""
        with self._cond:
            if self._closed:
                return
            if self._slot is not None:
                self.dropped += 1
            self._slot = frame
            self.last_seq = frame.seq
            self._cond.notify()

    def get(self, timeout: float | None = None) -> Frame | None:
        with self._cond:
            if self._slot is None and not self._closed:
                self._cond.wait(timeout)
            frame = self._slot
            self._slot = None
            if frame is not None:
                self.delivered += 1
            return frame

    def close(self) -> None:
        with self._cond:
            self._closed = True
            self._slot = None
            self._cond.notify_all()
        self._broker._unsubscribe(self.name)


class FrameBroker:
    """Distribuye el ultimo frame a N suscriptores independientes."""

    def __init__(self) -> None:
        self._lock = threading.Lock()
        self._subs: dict[str, Subscription] = {}

    def subscribe(self, name: str) -> Subscription:
        with self._lock:
            if name in self._subs:
                raise ValueError(f"Subscriber already registered: {name}")
            sub = Subscription(name, self)
            self._subs[name] = sub
            logger.debug("FrameBroker: subscriber registered: %s", name)
            return sub

    def _unsubscribe(self, name: str) -> None:
        with self._lock:
            self._subs.pop(name, None)

    def publish(self, frame: Frame) -> None:
        """Entrega el frame a todos los suscriptores. NUNCA bloquea al productor."""
        with self._lock:
            subs = list(self._subs.values())
        for sub in subs:
            sub._offer(frame)

    def stats(self) -> dict[str, dict[str, int]]:
        with self._lock:
            return {
                name: {
                    "delivered": s.delivered,
                    "dropped": s.dropped,
                    "last_seq": s.last_seq,
                }
                for name, s in self._subs.items()
            }

    def close(self) -> None:
        with self._lock:
            subs = list(self._subs.values())
        for sub in subs:
            sub.close()
```

Nota de diseno: `publish()` copia la lista de suscriptores bajo el lock global y luego los notifica fuera de el, para que un suscriptor no pueda bloquear a los demas.

Ejecutar: `.venv/Scripts/python.exe -m pytest tests/test_broker.py -v` — los 10 tests deben pasar.
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/test_broker.py -v --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - backend/pipeline/broker.py exporta Frame, FrameBroker y Subscription
    - publish() no contiene ninguna llamada bloqueante mas alla de la adquisicion de locks
    - stats() devuelve delivered, dropped y last_seq por suscriptor
    - Los 10 tests de tests/test_broker.py pasan
  </acceptance_criteria>
  <done>10 tests GREEN. FrameBroker con fan-out latest-frame, aislamiento entre suscriptores y contabilidad de descartes.</done>
</task>

<task type="auto">
  <name>Task 3: Verificar que no hay regresion en la suite existente</name>
  <files>—</files>
  <read_first>pytest.ini</read_first>
  <action>
Ejecutar la suite completa para confirmar que el paquete nuevo no rompe nada:

`.venv/Scripts/python.exe -m pytest tests/ -v --tb=short`

Todos los tests preexistentes (test_config, test_database, test_detector, test_stream, test_tracker, test_phase9, test_phase10) deben seguir pasando. Ningun fichero de v1 se ha tocado en este plan, asi que cualquier fallo indica un problema de importacion o de configuracion de pytest.
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/ --tb=line -q</automated>
  </verify>
  <acceptance_criteria>
    - La suite completa pasa
    - No hay warnings nuevos de deprecacion introducidos por backend/pipeline/
  </acceptance_criteria>
  <done>Suite completa GREEN, sin regresiones.</done>
</task>

</tasks>

<verification>
.venv/Scripts/python.exe -m pytest tests/test_broker.py -v --tb=short
.venv/Scripts/python.exe -m pytest tests/ -q
Ambos GREEN.
</verification>

<success_criteria>
- backend/pipeline/broker.py implementa el contrato de SPEC_v2.md §5.1
- publish() demostradamente no bloquea (test con consumidor de 1 s por frame)
- Un consumidor lento solo se pierde frames a si mismo (test de aislamiento)
- stats() reporta delivered, dropped y last_seq por suscriptor
- La suite preexistente sigue pasando
</success_criteria>

<output>
After completion, create `.planning/phases/17-frame-broker-y-capture-worker/17-01-SUMMARY.md`
</output>
