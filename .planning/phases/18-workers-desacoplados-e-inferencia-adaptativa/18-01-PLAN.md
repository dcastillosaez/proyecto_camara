---
phase: 18-workers-desacoplados-e-inferencia-adaptativa
plan: 01
type: execute
wave: 1
depends_on: ["17-02"]
files_modified:
  - backend/pipeline/rate.py
  - backend/pipeline/tracking.py
  - backend/pipeline/detection.py
  - backend/config.py
  - tests/test_adaptive_rate.py
  - tests/test_detection_worker.py
autonomous: true
requirements:
  - PIPE-04
  - DET-05

must_haves:
  truths:
    - "AdaptiveRate decide por tiempo transcurrido, no por contador de frames"
    - "AdaptiveRate baja el FPS efectivo cuando la latencia media supera el presupuesto y lo sube cuando sobra margen"
    - "DetectionWorker corre a su FPS objetivo con independencia del ritmo de captura"
    - "El frame_rate de ByteTrack se sincroniza con el FPS efectivo de deteccion"
    - "TrackRegistry es la unica estructura compartida entre workers, protegida por un RLock"
  artifacts:
    - path: "backend/pipeline/rate.py"
      provides: "AdaptiveRate: control de FPS por escalones con realimentacion de latencia"
      exports: ["AdaptiveRate"]
      min_lines: 70
    - path: "backend/pipeline/tracking.py"
      provides: "TrackRegistry y TrackState: estado compartido de tracks"
      exports: ["TrackRegistry", "TrackState"]
      min_lines: 90
    - path: "backend/pipeline/detection.py"
      provides: "DetectionWorker: YOLO + ByteTrack + zonas + heatmap consumiendo del broker"
      exports: ["DetectionWorker"]
      min_lines: 140
    - path: "tests/test_adaptive_rate.py"
      provides: "Tests de escalones, realimentacion y ritmo"
      min_lines: 70
    - path: "tests/test_detection_worker.py"
      provides: "Tests de ritmo independiente, sincronizacion de frame_rate y registro de tracks"
      min_lines: 80
  key_links:
    - from: "backend/pipeline/detection.py"
      to: "backend/pipeline/rate.py"
      via: "usa AdaptiveRate para decidir que frames procesa"
      pattern: "from backend\\.pipeline\\.rate import"
    - from: "backend/pipeline/detection.py"
      to: "backend/pipeline/tracking.py"
      via: "escribe el estado de tracks en el TrackRegistry"
      pattern: "TrackRegistry"
---

<objective>
Sacar la deteccion de `RTSPStream` a un `DetectionWorker` con FPS objetivo propio, gobernado por un `AdaptiveRate` que reacciona a la latencia real de inferencia.

Purpose: hoy la deteccion corre al ritmo de la captura con un `detect_every` fijo que no reacciona a la carga. Es lo que hace que el video se degrade cuando la CPU sube.
Output: deteccion desacoplada a 8 FPS objetivo, con degradacion automatica bajo carga y estado de tracks accesible para el resto de workers.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-workers-desacoplados-e-inferencia-adaptativa/18-CONTEXT.md
@backend/pipeline/broker.py
@backend/detector.py
@backend/tracker.py
@backend/stream.py
</context>

<interfaces>
```python
class AdaptiveRate:
    STEPS: tuple[float, ...] = (12.0, 8.0, 5.0, 3.0)

    def __init__(self, target_fps: float = 8.0, min_fps: float = 3.0,
                 max_fps: float = 12.0) -> None: ...
    def should_process(self, now: float) -> bool: ...
    def observe(self, latency: float) -> None: ...
    @property
    def effective_fps(self) -> float: ...
    @property
    def stats(self) -> dict[str, float]: ...   # effective_fps, avg_latency, steps_down, steps_up


@dataclass
class TrackState:
    track_id: int
    first_seen: float
    last_seen: float
    bbox: tuple[int, int, int, int]
    confidence: float
    centroid_history: deque          # (t, x, y), maxlen acotado
    zones: set[str]
    zone_entry_times: dict[str, float]
    person_id: int | None = None
    person_name: str | None = None


class TrackRegistry:
    """Estado compartido de tracks. Unico punto de encuentro entre workers."""
    def update_from_detections(self, tracked, now: float) -> list[int]: ...  # devuelve track_ids nuevos
    def get(self, track_id: int) -> TrackState | None: ...
    def snapshot(self) -> dict[int, TrackState]: ...      # copia superficial bajo lock
    def active_ids(self) -> set[int]: ...
    def set_identity(self, track_id: int, person_id: int, name: str | None) -> None: ...
    def prune(self, now: float, ttl: float = 30.0) -> list[int]: ...  # devuelve track_ids expirados


class DetectionWorker:
    def __init__(self, sub: Subscription, detector: PersonDetector,
                 tracker: PersonTracker, registry: TrackRegistry,
                 rate: AdaptiveRate) -> None: ...
    def start(self) -> None: ...
    def stop(self, timeout: float = 5.0) -> None: ...
    @property
    def stats(self) -> dict: ...
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Tests de AdaptiveRate (RED)</name>
  <files>tests/test_adaptive_rate.py</files>
  <read_first>.planning/phases/18-workers-desacoplados-e-inferencia-adaptativa/18-CONTEXT.md</read_first>
  <behavior>
    - test_should_process_respects_target_fps: con `target_fps=10`, llamadas a `should_process` con `now` avanzando 0.01 s devuelven True aproximadamente 10 veces por segundo simulado, no 100.
    - test_first_call_always_processes: la primera llamada a `should_process` siempre devuelve True.
    - test_steps_down_under_load: tras 20 `observe(0.30)` con `target_fps=8` (presupuesto 0.125 s), `effective_fps` es menor que 8.
    - test_does_not_go_below_min_fps: tras 100 `observe(2.0)`, `effective_fps == min_fps`.
    - test_steps_up_when_headroom: partiendo de un escalon bajo, tras N `observe(0.01)` consecutivos `effective_fps` sube.
    - test_does_not_exceed_max_fps: tras 100 `observe(0.001)`, `effective_fps == max_fps`.
    - test_hysteresis_avoids_oscillation: alternar `observe(0.30)` y `observe(0.01)` no produce mas de 2 cambios de escalon en 20 observaciones.
    - test_stats_exposes_effective_fps_and_latency: `stats` contiene las claves documentadas con tipos numericos.
  </behavior>
  <action>
Crear `tests/test_adaptive_rate.py`. Todos los tests usan un reloj simulado (pasar `now` explicito a `should_process`), nunca `time.sleep`. Esto los hace deterministas y rapidos.

El test de histeresis es el que protege contra el fallo de diseno mas probable: un controlador que oscila entre escalones en cada frame.

IMPORTANTE: RED. `backend/pipeline/rate.py` no existe.
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/test_adaptive_rate.py -v -x 2>&amp;1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - 8 tests escritos, todos RED
    - Ningun test usa time.sleep
  </acceptance_criteria>
  <done>8 tests RED con reloj simulado.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implementar AdaptiveRate (GREEN)</name>
  <files>backend/pipeline/rate.py, backend/config.py</files>
  <read_first>tests/test_adaptive_rate.py</read_first>
  <behavior>
    - Los 8 tests del Task 1 pasan.
    - Escalones discretos definidos en `STEPS`, filtrados por `min_fps`/`max_fps` en el constructor.
    - Media movil exponencial de latencia con alpha 0.2.
    - Histeresis: bajar escalon requiere 3 observaciones consecutivas por encima del presupuesto; subir requiere 10 por debajo del 50% del presupuesto.
  </behavior>
  <action>
Crear `backend/pipeline/rate.py`. Referencia de implementacion:

```python
"""Control adaptativo del FPS de inferencia con realimentacion de latencia."""

from __future__ import annotations

import logging

logger = logging.getLogger(__name__)


class AdaptiveRate:
    STEPS: tuple[float, ...] = (12.0, 8.0, 5.0, 3.0)

    _ALPHA = 0.2                # suavizado de la media movil
    _BUDGET_RATIO = 0.8         # presupuesto = (1 / fps) * 0.8
    _DOWN_STREAK = 3            # observaciones seguidas por encima para bajar
    _UP_STREAK = 10             # observaciones seguidas holgadas para subir
    _UP_RATIO = 0.5             # "holgado" = latencia < presupuesto * 0.5

    def __init__(self, target_fps: float = 8.0, min_fps: float = 3.0,
                 max_fps: float = 12.0) -> None:
        self._steps = [f for f in self.STEPS if min_fps <= f <= max_fps] or [target_fps]
        self._idx = min(
            range(len(self._steps)),
            key=lambda i: abs(self._steps[i] - target_fps),
        )
        self._min_fps = min_fps
        self._max_fps = max_fps
        self._last_ts: float | None = None
        self._avg_latency = 0.0
        self._over = 0
        self._under = 0
        self.steps_down = 0
        self.steps_up = 0

    @property
    def effective_fps(self) -> float:
        return self._steps[self._idx]

    def should_process(self, now: float) -> bool:
        if self._last_ts is None:
            self._last_ts = now
            return True
        if now - self._last_ts >= 1.0 / self.effective_fps:
            self._last_ts = now
            return True
        return False

    def observe(self, latency: float) -> None:
        self._avg_latency = (
            latency if self._avg_latency == 0.0
            else self._ALPHA * latency + (1 - self._ALPHA) * self._avg_latency
        )
        budget = (1.0 / self.effective_fps) * self._BUDGET_RATIO

        if self._avg_latency > budget:
            self._over += 1
            self._under = 0
            if self._over >= self._DOWN_STREAK and self._idx < len(self._steps) - 1:
                self._idx += 1
                self.steps_down += 1
                self._over = 0
                logger.info("AdaptiveRate: bajando a %.1f FPS (latencia %.3fs)",
                            self.effective_fps, self._avg_latency)
        elif self._avg_latency < budget * self._UP_RATIO:
            self._under += 1
            self._over = 0
            if self._under >= self._UP_STREAK and self._idx > 0:
                self._idx -= 1
                self.steps_up += 1
                self._under = 0
                logger.info("AdaptiveRate: subiendo a %.1f FPS (latencia %.3fs)",
                            self.effective_fps, self._avg_latency)
        else:
            self._over = 0
            self._under = 0

    @property
    def stats(self) -> dict[str, float]:
        return {
            "effective_fps": self.effective_fps,
            "avg_latency": self._avg_latency,
            "steps_down": float(self.steps_down),
            "steps_up": float(self.steps_up),
        }
```

Anadir a `backend/config.py`:
```python
# --- Inferencia adaptativa (Fase 18) ---
detection_target_fps: float = 8.0
detection_min_fps: float = 3.0
detection_max_fps: float = 12.0
recognition_target_fps: float = 2.0
```
y documentarlas en `.env.example`. Marcar `detect_every` como deprecado con un comentario que apunte a `detection_target_fps`.
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/test_adaptive_rate.py -v</automated>
  </verify>
  <acceptance_criteria>
    - Los 8 tests pasan
    - Settings tiene detection_target_fps, detection_min_fps, detection_max_fps y recognition_target_fps
    - detect_every marcado como deprecado
  </acceptance_criteria>
  <done>8 tests GREEN. AdaptiveRate con escalones e histeresis.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: TrackRegistry con estado compartido acotado</name>
  <files>backend/pipeline/tracking.py, tests/test_track_registry.py</files>
  <read_first>backend/tracker.py, backend/stream.py</read_first>
  <behavior>
    - test_new_tracks_reported: `update_from_detections` devuelve los track_ids que aparecen por primera vez.
    - test_snapshot_is_a_copy: mutar el dict devuelto por `snapshot()` no afecta al registro interno.
    - test_centroid_history_is_bounded: tras 10.000 actualizaciones de un track, `len(centroid_history)` no supera el maxlen configurado.
    - test_prune_removes_stale_tracks: un track sin actualizar durante mas del TTL desaparece y su id se devuelve.
    - test_prune_keeps_active_tracks: un track actualizado recientemente sobrevive al prune.
    - test_set_identity_is_thread_safe: 100 hilos escribiendo identidades distintas no corrompen el estado (verificar con `snapshot()` al final).
    - test_registry_does_not_grow_unbounded: simular 1.000 tracks efimeros con prune periodico y comprobar que el tamano final es acotado.
  </behavior>
  <action>
Crear `backend/pipeline/tracking.py` con `TrackState` y `TrackRegistry`.

Puntos criticos:
- `centroid_history` es un `deque(maxlen=...)`. El maxlen sale de configuracion (`track_history_len`, default 150 ≈ 20 s a 8 FPS). Sin maxlen, es una fuga de memoria garantizada en operacion 24/7.
- Todos los metodos publicos toman `self._lock` (un `threading.RLock`).
- `snapshot()` devuelve `dict(self._tracks)` — copia superficial. Los `TrackState` se comparten por referencia; se documenta que los lectores no deben mutarlos.
- `prune()` devuelve los ids expirados para que el llamador pueda limpiar sus propias cachés (el reconocedor tiene una, `backend/recognizer.py:337`).

Escribir primero `tests/test_track_registry.py` con los 7 tests (RED), luego implementar (GREEN).
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/test_track_registry.py -v</automated>
  </verify>
  <acceptance_criteria>
    - centroid_history usa deque con maxlen
    - Todos los metodos publicos son thread-safe
    - prune() devuelve los ids expirados
    - Los 7 tests pasan
  </acceptance_criteria>
  <done>7 tests GREEN. TrackRegistry acotado y thread-safe.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: DetectionWorker consumiendo del broker</name>
  <files>backend/pipeline/detection.py, tests/test_detection_worker.py</files>
  <read_first>backend/stream.py, backend/detector.py, backend/tracker.py, backend/pipeline/rate.py, backend/pipeline/tracking.py</read_first>
  <behavior>
    - test_runs_at_target_fps: con un broker alimentado a 25 FPS simulados y `target_fps=8`, el detector mock se invoca ~8 veces por segundo, no 25.
    - test_slow_detector_does_not_block_broker: con un detector mock de 200 ms, el productor mantiene su ritmo y `broker.stats()["detector"]["dropped"]` crece.
    - test_tracker_frame_rate_follows_effective_fps: al bajar `AdaptiveRate` de escalon, el worker llama al metodo que actualiza el `frame_rate` del tracker.
    - test_registry_updated_with_tracks: tras procesar frames con detecciones simuladas, `registry.snapshot()` contiene los tracks esperados.
    - test_latency_is_observed: `rate.observe` recibe la latencia real de cada inferencia (verificar con spy).
    - test_stop_is_clean: `stop()` termina el hilo y cierra la suscripcion.
    - test_detector_exception_does_not_kill_worker: si `detect_sv` lanza, el worker loguea y continua con el siguiente frame.
  </behavior>
  <action>
Crear `backend/pipeline/detection.py`. El cuerpo del bucle porta la logica de `RTSPStream._process_frame` relativa a deteccion, tracking, zonas y heatmap (`_update_zones_and_heat`, backend/stream.py:424).

Estructura del bucle:

```python
def _loop(self) -> None:
    while self._running:
        frame = self._sub.get(timeout=1.0)
        if frame is None:
            continue
        if not self._rate.should_process(time.monotonic()):
            continue
        t0 = time.monotonic()
        try:
            sv_dets = self._detector.detect_sv(frame.image)
            tracked, crossings = self._tracker.update(sv_dets)
        except Exception:
            logger.exception("DetectionWorker: fallo de inferencia, se salta el frame")
            continue
        self._rate.observe(time.monotonic() - t0)
        self._sync_tracker_frame_rate()
        new_ids = self._registry.update_from_detections(tracked, frame.wall_clock.timestamp())
        self._update_zones_and_heat(tracked, frame.image.shape)
        self._emit_crossings(crossings, frame)
        self._registry.prune(time.monotonic())
```

Puntos criticos:

1. **`_sync_tracker_frame_rate()`**: si `rate.effective_fps` cambio desde la ultima vez, reconfigurar el `frame_rate` de ByteTrack. Sin esto, el tracker asume un ritmo que no existe y pierde IDs antes de tiempo. Es el riesgo numero uno de la fase — revisar `backend/tracker.py:24` para ver como se pasa `frame_rate`.

2. **`_emit_crossings`**: mantiene el mecanismo actual (`event_loop` + `event_queue`, backend/stream.py) sin cambios. El motor de eventos llega en la Fase 19.

3. **Excepciones**: cualquier fallo de inferencia se loguea y se salta el frame. El worker no muere por un frame corrupto.

4. **Sin `await`**: este worker corre en un `threading.Thread`. La unica interaccion con asyncio es `asyncio.run_coroutine_threadsafe` para la cola de eventos, exactamente como ya hace `RTSPStream`.

Escribir primero los 7 tests (RED) con detector y tracker mockeados, luego implementar.
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/test_detection_worker.py -v --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - DetectionWorker consume del broker y respeta el FPS objetivo
    - El frame_rate del tracker se sincroniza con el FPS efectivo
    - Una excepcion del detector no mata el worker
    - Los 7 tests pasan
  </acceptance_criteria>
  <done>7 tests GREEN. Deteccion desacoplada con ritmo propio y adaptativo.</done>
</task>

</tasks>

<verification>
.venv/Scripts/python.exe -m pytest tests/test_adaptive_rate.py tests/test_track_registry.py tests/test_detection_worker.py -v
.venv/Scripts/python.exe -m pytest tests/ -q
</verification>

<success_criteria>
- AdaptiveRate baja y sube escalones con histeresis, sin oscilar
- DetectionWorker corre a 8 FPS objetivo con captura a 25 FPS
- El frame_rate de ByteTrack sigue al FPS efectivo de deteccion
- TrackRegistry tiene historial acotado y es thread-safe
- Ninguna excepcion de inferencia mata el worker
</success_criteria>

<output>
After completion, create `.planning/phases/18-workers-desacoplados-e-inferencia-adaptativa/18-01-SUMMARY.md`
</output>
